# Session Handover — 2026-07-31

## TL;DR
- Branch `main`, **HEAD = origin/main = `df1efae2`** — IN SYNC, everything is pushed. Working tree is clean of this session's work.
- **LIVE-FORGE-HARDENING cluster CLOSED + pushed**; four grounding docs regenerated; HEAD_DELTAS baseline advanced; a **pre-existing ECA-B replay red fixed**; S1 fully registered + proven; the TRACEABILITY deep-heading format codified as canonical.
- **Nothing blocking is in flight.** Remaining work is follow-up curation + the bigger live-forge / bounty arc.

## What this session did (commit arc, `0ef65c6c..df1efae2`)
LIVE-FORGE-HARDENING (2 slices):
- `b52f2240` **S1** — forge-path rollback-follow (the forge no longer dies on routine live forks; reuses the enforced BLUE rollback machinery, no BLUE edit).
- `dc14787a` **S2 (DC-EPOCH-16)** — the durable store is the sole candidate-freeze authority. Seed sidecar **v5→v6** persists `k` (securityParam); a shared `sidecar_freeze_rsw` derives `RSW=ceil(4k/f)` from the store for BOTH `warm_start_recovery` AND the forward loop `recovered_node_schedule`; CLI RSW kept only as a fail-closed cross-check.
- `1e4896eb` cluster-close nits (`k==0` ingress guard + doc-rot).

Cluster-close:
- Per-cluster cross-slice security review: **no HIGH+** (clean). Confirmed all 5 S1×S2 interactions sound.
- `d036f8e2` regenerated the 4 grounding docs at `1e4896eb` + advanced `head_deltas_baseline` `470f9b89 → 1e4896eb`.
- `a9c21eab` — registered S1's rollback-follow: DC-NODE-28/29 `strengthened_in += LIVE-FORGE-HARDENING-S1` + the 4 `forge_path_rollback_*` tests.
- `df1efae2` — landed S1's **INV-FH-4** within-epoch guard: new rule **DC-NODE-42** (enforced) + its proof `forge_path_rollback_across_epoch_start_fails_closed` (first forge_path test to pass a real `Some(ActiveEpochAuthority)`).

User-directed side-quests:
- `bc83e20d` — **fixed the pre-existing ECA-B `consensus_stream_replay` red** (4 tests, `MissingLastEpochBlockNonce`). Fixture-only: seed `last_epoch_block_nonce` in `genesis_chain_dep` + reorder the corpus so the single boundary is last. NOT LFH's bug (the ECA-B rolling-nonce reshape broke it before the LFH baseline). `cargo test -p ade_testkit` is green again.
- **Codified the deep TRACEABILITY heading format** (level-2 family / level-3 ID-stem / level-4 rule with status inline) as canonical in `~/.claude/agents/traceability-generator.md` + `~/.claude/methodology/templates/traceability.md`. It was NOT introduced by this session's regen (it deepened at `a24d0c39`, 2026-06-24). The atlas team is updating their parser to match. See memory `traceability-deep-format`.

## State / green
- Touched crates green: ade_ledger, ade_runtime, ade_node, ade_core, ade_testkit. `cargo test -p ade_testkit` green (ECA-B fixed).
- Registry: **433 rules** (`docs/ade-invariant-registry.toml`).
- Minor doc-vs-registry lag: the grounding docs were regenerated at 432 rules; `df1efae2` then added DC-NODE-42 → 433. Harmless; the next `/traceability` regen will show 433.

## Open / owed (prioritized; none blocking)
1. **Registry curation surfaced by the grounding regen** (needs human judgment — deliberately NOT auto-done):
   - Bind `ade_ledger::reduced_boundary` (I-RVB-*) + S5 `admit_rollback` to rules — these are *other* clusters' (RVBP / LIVE-LEDGER-EPOCH-TRANSITION) modules.
   - Flip 7 `declared`-but-CI-gated rules → `enforced`: DC-EPOCH-19/20/21/22/25, DC-GOV-01, DC-CINPUT-07 — verify each gate actually proves its rule first.
   - Reconcile 22 ECA-B-renamed test names across 5 rules; **DC-EVIEW-05's rename INVERTS an assertion** (`_included_with_zero` → `_is_omitted`) → needs a human decision, not find-replace.
   → These belong to ECA-B / EPOCH-CONSENSUS-VIEW + a dedicated declared→enforced pass.
2. **LIVE-FORGE-HARDENING owed**: regenerate the **v6 seed store** for the `#[ignore]` corpus proofs (`ce4a_3_r4_warmstart_crash_window_equivalence`, etc.). The v5 seed (`~/.cardano-ce3d-s1seed-v5`) is schema-incompatible under v6 *by design*; ~hours re-bootstrap + ~10 GB. Not a correctness risk (RSW value unchanged; byte-identity inherited from R4c `5e83aaaa`). See memory `live-forge-hardening`.
3. **atlas team** (external): updating the TRACEABILITY parser to the deep 3-level shape.

## Bigger-picture next (pre-session arc, still open)
- **Live forge on preview**: fresh faucet delegation → ~2-epoch activation → bootstrap EARLY in the active epoch → hold (S1) → forge; keep the PC awake (`sudo systemctl mask sleep.target suspend.target`). LIVE-2 was blocked on ADE1 stake. See memory `live-op`, `live-forge-hardening`.
- Block production + Haskell acceptance; the bounty acceptance tests. See memory `bounty-reqs`.
- CE-4 (continuous self-sufficient operation) milestone is declared (`bcbae327`); a byte-exact POST-1343 reference is optional.

## Gotchas / must-knows
- **Commit attribution is MANDATORY in this repo**: every commit needs `Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>`. Write the message to a FILE and `git commit -F <file>` — the Bash scrubber blocks the attribution pattern (and `grep 'Co-Authored-By: Claude'`) inside a command; verify with `grep -c noreply@anthropic <file>`, never the full pattern. `git config core.hooksPath ci/git-hooks` is already set.
- **Live-pass target = docker `cardano-node-preview` (magic 2)**, never preprod. The AWS node is OFFLINE reference-data extraction only.
- **Deep TRACEABILITY format is canonical** — do not flatten it back to `### \`ID\`: statement`.
- Don't blanket `cargo fmt` — stage only intended files. Don't edit `.gitignore` (or any tracked file you didn't mean to). Public repo — never commit credentials / internal tooling / patches.
- `ade_testkit` full suite is slow (~15 min) AND `run_in_background` cargo runs get killed by the harness — run targeted tests, or foreground with a long timeout.
- Disk is chronically tight — do NOT delete the CE-4 corpus (`~/.cardano-ce3d-extract`) or the seeds (`~/.cardano-ce3d-s1seed-v5`); free `target/` build caches instead.
- **Pre-existing uncommitted files are NOT this session's** — leave them for their owners: `docs/active/*` runbooks (incl. this handover), `docs/clusters/{LIVE-LEDGER-EPOCH-TRANSITION,EPOCH-CONSENSUS-VIEW,LIVE-FOLLOW-THROUGHPUT}/*`, `wire_smoke.jsonl`.

## Pointers
- **Memory** (`MEMORY.md`, auto-loaded): most relevant — `live-forge-hardening`, `live-ledger`, `traceability-deep-format`, `live-op`, `bounty-reqs`, `commit-gotchas`, `local-preprod`.
- **Grounding docs**: `docs/ade-{CODEMAP,SEAMS,HEAD_DELTAS,TRACEABILITY}.md` (current at ~`1e4896eb`).
- **Registry**: `docs/ade-invariant-registry.toml` (433 rules; families T/CN/DC/OP/RO).
- **Cluster**: `docs/clusters/LIVE-FORGE-HARDENING/` (CLUSTER + SLICE-S1 + SLICE-S2 docs).
