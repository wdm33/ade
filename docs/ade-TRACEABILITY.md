# Normative Rule Traceability — Ade

> **Status:** Living architectural document. Regenerated by `/traceability`; not hand-edited.
> Per-project instance of `~/.claude/methodology/templates/traceability.md`.

> 171 rules in `docs/ade-invariant-registry.toml` at HEAD (`ee35493`).
> 27 CI scripts under `ci/ci_check_*.sh`.
> Source of truth: `docs/ade-invariant-registry.toml` (canonical, append-only).
> **PHASE4-B4 (Conway cert-state accumulation, fail-closed) close** adds 1 new
> `DC-LEDGER-08` rule (closed/total Conway cert-state accumulation; `status =
> "enforced"`) and strengthens `DC-VAL-06` (`strengthened_in += PHASE4-B4`; the
> cert-state silent-skip removed from `accumulate_tx_certs`). **No new CI script** —
> `ci/ci_check_forbidden_patterns.sh` was extended (S3/S4) to guard the cert-state
> fail-open swallow (it FAILs if the `"non-fatal during replay"` rationale or an
> `Err(_)` swallow arm in `accumulate_tx_certs` reappears). `DC-LEDGER-08` ships full
> mechanical closure (owner-complete decode + total owner-tagged apply + era-dispatched
> fail-closed accumulation + 16-test synthetic positive/replay/adversarial corpus), but
> carries **one honest, environment-blocked open obligation**: the real epoch-576
> cert-state-vs-cardano-node oracle is blocked on the absent epoch-576 UMap snapshot
> (`corpus/cert_state/README.md`), reclassified per tier doctrine — identical to the
> documented open obligations on `DC-TXV-06` / `DC-TXV-03`. See §A0000 and §G #17.
> **PHASE4-B3 (Conway deposit/withdrawal value conservation) close** adds 2 new
> `DC-TXV-*` rules — `DC-TXV-06` (closed/total Conway certificate-deposit
> classification) and `DC-TXV-07` (canonical deposit-parameter authority) — and
> strengthens 4 pre-existing rules (`T-CONSERV-01` / `CN-LEDGER-07` UTxO/value
> conservation now enforced for cert/withdrawal-bearing Conway txs; `DC-VAL-06`
> the cert/withdrawal early-out fail-open removed; `DC-TXV-03` adds the B3 positive
> + adversarial conservation corpora). One new CI script landed —
> `ci/ci_check_deposit_param_authority.sh` (discharges `DC-TXV-07`). **`DC-TXV-06`
> is `status = "partial"`, not `enforced`** — its closed/total cert-deposit
> classification is held by the compiler-exhaustive `match` in
> `ade_ledger::cert_classify` plus 10 named tests, but **there is no dedicated CI
> grep-gate yet** (`ci_check_consensus_closed_enums.sh` `TARGETS` do not cover
> `ade_types/src/conway/` or the cert decoder). That CI grep-gate is a **tracked
> follow-up**, and `status = "partial"` honestly reflects it — it is the **one
> remaining enforcement gap** of the B3 close. The B3 close also **closes the prior
> §H value-conservation completeness gap** (deposit/refund/withdrawal accounting now
> in scope). PHASE4-B2 (tx validity agreement) added the 5-rule `DC-TXV-01..05`
> sub-family (all `enforced`), flipped `DC-MEM-01`/`DC-MEM-02` `declared -> enforced`,
> and populated the previously-empty `ci_script` on the enforced `DC-CONS-*` /
> `DC-VAL-*` / `DC-TXV-*` rules with `ci/ci_check_consensus_closed_enums.sh`.
> **`ci/ci_check_constitution_coverage.sh` PASSES at HEAD `193d2fc`** (verified by
> running the gate during this B3F refresh: `RESULT: PASS`, 170 entries). The B3/B3F
> registry entries are consistent: `DC-TXV-06` is now `enforced` with `ci_script =
> ci/ci_check_conway_cert_classification_closed.sh`, `DC-TXV-06`/`DC-TXV-07` carry
> `cluster = "CL-LEDGER-VERDICT"`
> (in the gate's allowed cluster set), and the B3 cross_refs are bidirectional
> (`T-CONSERV-01`, `CN-LEDGER-07`, `DC-VAL-06`, `DC-TXV-05`, `T-DET-01` reference
> `DC-TXV-06`/`DC-TXV-07` back). **PHASE4-B3F (cert-classification CI-gate +
> decoder strictness follow-up)** flips `DC-TXV-06` `partial -> enforced`: the new
> grep-gate `ci/ci_check_conway_cert_classification_closed.sh` defends the closed/
> total cert-deposit classification (it fails on an open-tail `Other`/`Unknown`
> variant or `#[non_exhaustive]` on `ConwayCert`/`CertDisposition`/`DepositEffect`/
> `CoinSource`, a catch-all decoder arm constructing a `ConwayCert`, or a `_ =>`
> wildcard in `classify`). The prior "DC-TXV-06 has no CI grep-gate" tracked
> follow-up is **RESOLVED/CLOSED**. B3F also strengthens `DC-VAL-06`
> (`strengthened_in += PHASE4-B3F`; `ade_codec::conway::cert::decode_conway_certs`
> now rejects trailing bytes via `CodecError::TrailingBytes` and bounds
> preallocation against a hostile array count). CI scripts now total **27**
> (new `ci/ci_check_conway_cert_classification_closed.sh`). PHASE4-B1 (full block
> validity agreement) close added the 6-rule `DC-VAL-*` sub-family.

---

## Conventions

- **Source of rules.** The canonical, append-only source is the invariant
  registry at `.idd-config.json` `invariant_registry`
  (`docs/ade-invariant-registry.toml`). The registry was renamed from
  `constitution_registry.toml` in the post-Phase-4-N-D IDD-canonicalization
  delta (`ci_check_constitution_coverage.sh` was repointed in `39865f6`).
  Every `[[rules]]` entry whose `status` is not explicitly retired appears
  below. None are deprecated at HEAD.
- **Rule IDs** are stable across regenerations and immutable per
  registry doctrine. They are reproduced exactly as the registry holds them.
- **Families.** The registry groups rules under five family-letter
  prefixes:

  | Prefix | Family |
  |---|---|
  | `T-` | True invariants (project constitution §2) |
  | `DC-` | Derived Cardano compatibility invariants (§3) |
  | `CN-` | Classification-table invariants (`classification_table.md`) |
  | `RO-` | Release obligations (§4a) |
  | `OP-` | Operational invariants (§4b) |

  Within each family, level-3 sub-headings hold one rule each. The
  sub-section ID *is* the rule ID. Within a family, ordering follows
  the registry file order. The `DC-CONS-*` sub-family (8 rules
  introduced by PHASE4-N-B) appears at the end of the `DC-` section in
  registry file order.
- **Four-cell contract.** Every rule carries the canonical four-row table:
  Source, Requirement, Code, Tests, CI. Where the registry has populated
  `code_locus`, `tests`, `ci_script`, those fields drive the row. A row
  marked `_(no enforcement found — gap)_` corresponds to a registry
  entry whose `status` is `declared` (no enforcement promised) or to a
  partial entry that has not yet pointed at a code/test/CI artifact.
- **PHASE4-B2 close (tx validity agreement).** Cluster PHASE4-B2
  (single-transaction validity; the body authority's per-tx half) introduced
  **5 new `DC-TXV-*` rules** (`DC-TXV-01..05`, all `enforced`), flipped
  **`DC-MEM-01` and `DC-MEM-02`** `declared -> enforced` (the mempool gained a
  real `crates/ade_ledger/src/mempool/` BLUE module), and strengthened **6
  pre-existing rules** by appending `PHASE4-B2` to `strengthened_in`:
  `DC-LEDGER-05` and `CN-LEDGER-09` (Conway vkey-witness binding — fail-closed
  Ed25519 coverage over the preserved body hash), `DC-VAL-06` (wrong-size
  vkey/sig now `MalformedWitnessField` on the tx path; unresolvable inputs
  fail-fast), `DC-LEDGER-02` (GREEN no-false-accept tx adversarial corpus),
  `T-ENC-01` (`tx_id` lifted byte-for-byte from the body slice, never
  re-encoded), and `T-DET-01` (tx verdict-surface replay over the Conway-576
  corpus). The new authority surface is `crates/ade_ledger/src/tx_validity/`
  (closed `TxValidityVerdict`/`TxRejectClass`/`TxValidityError`,
  `transition.rs` phase-1 ∧ phase-2 composition with phase-1-first fail-fast,
  `required_signers.rs` closed era-versioned `SignerSource` enumeration over
  every signer source, `witness.rs` fail-closed coverage over the preserved
  body hash, `phase1.rs` decode + state-backed checks) and the mempool BLUE
  module `crates/ade_ledger/src/mempool/{admit,policy}.rs` (closed `AdmitOutcome`
  / `OrderPolicy`; admission verdict equals `tx_validity` verdict; overload
  policy never changes a verdict). The GREEN corpus harnesses live at
  `crates/ade_testkit/src/tx_validity/`. **No new CI script landed in B2** —
  every `DC-TXV-*` / `DC-MEM-*` rule carries
  `ci/ci_check_consensus_closed_enums.sh` (extended in B2 to scan
  `ade_ledger/src/tx_validity/` and `ade_ledger/src/mempool/` for closed
  structured verdicts/outcomes) plus its test surface. The same close
  **populated the previously-empty `ci_script`** on every enforced `DC-CONS-*`
  and `DC-VAL-*` rule with `ci_check_consensus_closed_enums.sh`, which is what
  flips `ci/ci_check_constitution_coverage.sh` from FAIL to **PASS** at HEAD
  (the coverage gate requires a non-empty `ci_script` on every `enforced`
  entry). A **documented deposit/refund value-conservation gap** remains: the
  Conway state-backed coin-conservation check
  (`crates/ade_ledger/src/conway.rs::check_conway_coin_conservation`)
  conservatively skips txs carrying certificates or withdrawals, so a
  mis-accounted deposit/refund/withdrawal tx is not yet rejected on value
  grounds — a known tx-validity-completeness item against `T-CONSERV-01` /
  `CN-LEDGER-07`; see §G and §H.
- **PHASE4-B1 close (full block validity agreement).** Cluster PHASE4-B1
  (block validity agreement; dependency root for B2/B3) introduced **6 new
  `DC-VAL-*` rules** (`DC-VAL-01..06`, all `enforced`) and strengthened **6
  pre-existing rules** by appending `PHASE4-B1` to `strengthened_in` and wiring
  the new block-validity surface: `T-DET-01` (verdict-surface replay over the
  Conway-576 positive corpus), `DC-CRYPTO-01` (Praos single-VRF input +
  leader/nonce range extension, op-cert-signable fix), `DC-CONSENSUS-02`
  (`LedgerView` projection), `DC-LEDGER-02` (GREEN no-false-accept adversarial
  corpus), `CN-CONS-04` (recomputed body-hash binding + VRF-keyhash binding),
  and `CN-EPOCH-01` (`consensus_view` projection). The new authority surface is
  `crates/ade_ledger/src/block_validity/` (closed `Valid`/`Invalid` verdict +
  `FieldKind`/`FieldError` fail-closed field taxonomy, `transition.rs`
  header∧body composition, `header_input.rs` body-hash binding),
  `crates/ade_ledger/src/{consensus_input_extract,consensus_view}.rs` (pure
  canonical-input projection), `crates/ade_core/src/consensus/kes_check.rs`
  (`expect_size` fail-closed header crypto-field guard), and the GREEN harnesses
  `crates/ade_testkit/src/validity/{replay,adversarial,corpus,ledger_view}.rs`.
  **No new CI script landed in B1** — every `DC-VAL-*` rule carries an empty
  `ci_script` and is discharged by its test surface plus structural CI
  (`ci_check_consensus_closed_enums.sh`, `ci_check_no_float_in_consensus.sh`,
  `ci_check_forbidden_patterns.sh`); see §A0 and §G for the surfaced CI gaps.
  A **documented Conway body-witness verification gap** (recorded in
  `crates/ade_testkit/src/validity/adversarial.rs` and
  `corpus/validity/adversarial/README.md`) is fenced by the header authority
  and therefore is not a `block_validity` false-accept — it is a genuine
  pre-release finding scheduled for B2; see §G.
- **PHASE4-N-B close (consensus runtime).** Cluster N-B (Praos consensus
  runtime, 10 slices S-B1..S-B10) introduced **8 new DC-CONS-* rules**
  (`DC-CONS-03..10`) and strengthened **11 pre-existing rules** by
  populating `code_locus`/`tests`/`ci_scripts` arrays and appending
  `PHASE4-N-B` to `strengthened_in`. Four new CI scripts landed —
  `ci/ci_check_no_chaindb_in_consensus_blue.sh`,
  `ci/ci_check_no_float_in_consensus.sh`,
  `ci/ci_check_consensus_closed_enums.sh`,
  `ci/ci_check_no_density_in_fork_choice.sh` — each rule-ID-anchored
  via its header comment. The strengthened rules are: `DC-CONSENSUS-01`
  (partial → enforced), `DC-CONSENSUS-02` (declared → partial),
  `DC-CRYPTO-01` (enforced, augmented test surface), `CN-CRYPTO-02`
  (declared → partial), `CN-EPOCH-01` (declared → partial),
  `DC-EPOCH-02` (enforced, augmented for consensus-side HFC),
  `CN-CONS-01..05` (each `declared` with code_locus/tests/ci wired),
  `DC-CORE-01` (declared, strengthened over ade_runtime::consensus),
  `T-DET-01` (enforced, consensus subset closed via stream replay),
  `T-CORE-02` (enforced, augmented by `ci_check_no_float_in_consensus.sh`).
  Two new crates accompanied the work — `ade_core::consensus` is the
  BLUE consensus surface (16 modules under
  `crates/ade_core/src/consensus/`) and `ade_core_interop` is a RED
  live-peer driver crate gated by `#[ignore]` in CI by default. The
  GREEN orchestrator that threads N-A inputs through BLUE transitions
  lives at `crates/ade_runtime/src/consensus/{chain_selector,candidate_fragment,genesis_parser}.rs`.
- **Phase 4 N-A CI closure (PHASE4-N-A close).** Cluster N-A (Ouroboros
  mini-protocols, 11 protocols across N2N+N2C) strengthened four
  pre-existing registry rules with new tests against real cardano-node
  11.0.1 captures and synthetic agency traces: `T-ENC-03`, `CN-WIRE-07`,
  `DC-PROTO-02`, `DC-PROTO-05`. One new CI script —
  `ci/ci_check_ce_n_a_5_proof.sh` — exercises all 5 conditions of the
  CE-N-A-5 proof obligation. Two new registry IDs (`DC-CORE-01`,
  `DC-PROTO-06`) were added by the cluster — see their entries below.
- **Phase 4 N-D CI closure (`78da6c9`).** Three CI scripts —
  `ci/ci_check_chaindb_contract.sh`, `ci/ci_check_recovery_contract.sh`,
  `ci/ci_check_chaindb_crash_safety.sh` — flipped nine
  recovery/storage rules `declared → enforced`: `T-REC-01`, `T-REC-02`,
  `DC-STORE-01`, `DC-STORE-02`, `DC-STORE-03`, `DC-STORE-05`,
  `CN-STORE-03`, `CN-STORE-04`, `CN-STORE-05`. `DC-STORE-04` remains
  `declared` by design — see §B for the Tier 5 divergence note.
- **CI script BLUE scope (closed at `5b70bee`).** All seven BLUE-scoped
  CI scripts hard-code the full six-crate BLUE list `{ade_codec,
  ade_types, ade_crypto, ade_core, ade_ledger, ade_plutus}` declared
  by `.idd-config.json` plus the 9 BLUE `ade_network` submodule paths.
  No `_(scope gap)_` markers remain.

---

## T — True Invariants (Project Constitution §2)

### `T-DET-01`: Same canonical inputs → same authoritative bytes

| Aspect | Location |
|--------|----------|
| **Source** | Project constitution §2; `comparison_surface_contract.md` (Byte Authority Model §3). |
| **Requirement** | Replaying the same canonical inputs against the same prior state must yield byte-identical authoritative outputs. |
| **Code** | `crates/ade_ledger/src/rules.rs` (`apply_block*` family), `crates/ade_ledger/src/fingerprint.rs` (canonical per-component state hash; PHASE4-B3 adds a Conway-gated deposit-param fold to `fingerprint_pparams` — non-Conway states fingerprint byte-identically, Conway states fold in the canonical deposit params, see `DC-TXV-07`); plus PHASE4-N-B consensus surface — `crates/ade_core/src/consensus/encoding.rs` (canonical state encoding with deterministic op-cert counter map iteration) and `crates/ade_runtime/src/consensus/chain_selector.rs` (GREEN orchestrator that drives BLUE transitions deterministically); plus PHASE4-B1 verdict-surface replay — `crates/ade_testkit/src/validity/replay.rs` (GREEN harness replaying `block_validity` over the 14-block Conway-576 positive corpus); plus PHASE4-B2 tx verdict-surface replay — `crates/ade_testkit/src/tx_validity/` (GREEN harness `replay_tx_validity` driving the BLUE `tx_validity` over every extracted Conway-576 corpus tx twice, surfaces byte-identical). |
| **Tests** | `crates/ade_testkit/tests/ledger_determinism.rs::apply_block_deterministic`; per-era `byron_determinism`, `shelley_determinism`, `allegra_determinism`, `mary_determinism`, `alonzo_determinism`, `babbage_determinism`, `conway_determinism`; plus consensus subset closed via `crates/ade_testkit/tests/consensus_stream_replay.rs::{synthetic_session_replays_identically, synthetic_session_event_kinds_match_corpus, synthetic_session_final_state_matches_corpus, op_cert_regression_header_rejected_mid_stream}` (CE-N-B-5) and the canonical state-roundtrip battery `crates/ade_core/tests/praos_state_canonical_roundtrip.rs::{layout_is_stable, roundtrip_empty_state, roundtrip_genesis_state, roundtrip_populated_state, roundtrip_chain_event_all_variants, roundtrip_chain_selection_reject_all_variants, op_cert_counter_map_iteration_is_deterministic}`; plus PHASE4-B1 `crates/ade_ledger/tests/block_validity_positive_corpus.rs::verdict_stream_replays_identically` (the block-validity verdict surface replays byte-identically over the Conway-576 corpus); plus PHASE4-B2 `crates/ade_ledger/tests/tx_validity_positive_corpus.rs::tx_verdict_stream_replays_identically` (the tx-validity verdict surface replays byte-identically over the Conway-576 corpus); plus PHASE4-B3 `crates/ade_ledger/src/fingerprint.rs::tests::{pparams_fingerprint_stable_for_non_conway, pparams_fingerprint_includes_conway_deposits_when_present}` (the Conway deposit-param fold is byte-identical for non-Conway states and stable when present). |
| **CI** | `ci/ci_check_forbidden_patterns.sh` (BLUE forbidden patterns, all 6 BLUE crates), `ci/ci_check_ledger_determinism.sh` (replay equivalence), `ci/ci_check_consensus_closed_enums.sh` (PHASE4-N-B — no `#[non_exhaustive]`, no `Other`/`Unknown` open-tail variants, no owned `String` or `Box<dyn>` in `ade_core::consensus`). Strengthened for PHASE4-N-B, again for PHASE4-B1 (block-validity verdict-surface replay), and again for PHASE4-B2 (tx-validity verdict-surface replay; no new CI script — discharged by `tx_verdict_stream_replays_identically` plus the structural consensus gates, which now also scan `ade_ledger/src/tx_validity/` and `ade_ledger/src/mempool/`). |

### `T-ENC-01`: Canonical encoding for persisted/hashed/transmitted data

| Aspect | Location |
|--------|----------|
| **Source** | Project constitution §2. Strengthened by PHASE4-B1 (block body hash over preserved CBOR) and PHASE4-B2 (`tx_id` over preserved body slice). |
| **Requirement** | All persisted, hashed, or transmitted authoritative bytes use a single canonical encoding; hash-critical bytes are taken from the preserved wire segment, never re-encoded. |
| **Code** | `crates/ade_codec/src/preserved.rs` (`PreservedCbor<T>`, `pub(crate)` constructor); `crates/ade_ledger/src/block_validity/header_input.rs` (PHASE4-B1 — block body hash computed over the preserved CBOR segment bytes, never re-encoded); `crates/ade_ledger/src/tx_validity/phase1.rs` (PHASE4-B2 — `decode_tx`: `tx_id = blake2b_256` of the body slice lifted byte-for-byte from the full tx CBOR, never a re-encode). |
| **Tests** | `crates/ade_codec/...::preserved_wire_bytes_returned_exactly`; `crates/ade_ledger/...::altered_body_rejected_by_hash_binding`; `crates/ade_ledger/tests/tx_validity_compose.rs::tx_id_uses_preserved_bytes`. |
| **CI** | `ci/ci_check_hash_uses_wire_bytes.sh` — scans all 6 BLUE crates `{ade_codec, ade_types, ade_crypto, ade_core, ade_ledger, ade_plutus}` for `hash(canonical_*)` patterns. Status = `partial` (canonical-encoding surface not yet fully covered; the `tx_id`-preserved-bytes property is proven by test only). |

### `T-ENC-02`: Non-canonical bytes rejected deterministically

| Aspect | Location |
|--------|----------|
| **Source** | Project constitution §2. |
| **Requirement** | Non-canonical encodings of consensus-relevant inputs must be rejected, and rejection must be deterministic. |
| **Code** | _(no enforcement found — gap; registry `code_locus` empty, `status = "declared"`)_ |
| **Tests** | _(no tests found — gap)_ |
| **CI** | _(no CI enforcement — gap)_ |

### `T-ENC-03`: Round-trip identity

| Aspect | Location |
|--------|----------|
| **Source** | Project constitution §2, D-3. |
| **Requirement** | For all valid encodings, `encode(decode(bytes)) == bytes`. |
| **Code** | `crates/ade_codec/src/preserved.rs`; plus per-protocol codecs `crates/ade_network/src/codec/{handshake,n2c_handshake,chain_sync,block_fetch,tx_submission,keep_alive,peer_sharing,local_chain_sync,local_tx_submission,local_state_query,local_tx_monitor}.rs` (PHASE4-N-A S-A2). |
| **Tests** | `crates/ade_codec/...::full_corpus_round_trip` — 10,500 contiguous blocks across 7 eras; per-protocol synthetic `codec::<proto>::tests::roundtrip_every_variant` across all 11 mini-protocols (S-A2); plus six real-capture corpora harvested from cardano-node 11.0.1 — `handshake_real_capture_corpus`, `chain_sync_real_capture_corpus`, `block_fetch_real_capture_corpus`, `keep_alive_real_capture_corpus`, `peer_sharing_real_capture_corpus` (preprod local), `n2c_handshake_real_capture_corpus::real_capture_round_trips_byte_identical`. |
| **CI** | `ci/ci_check_cbor_round_trip.sh` (block-corpus round-trip); `ci/ci_check_ce_n_a_5_proof.sh` (CE-N-A-5 condition 3 — real-capture round-trip across 6 captured mini-protocols). |

### `T-CORE-01`: Authoritative logic pure and replayable

| Aspect | Location |
|--------|----------|
| **Source** | Project constitution §2. |
| **Requirement** | Authoritative logic must be pure, side-effect-free, and replayable. |
| **Code** | `crates/ade_ledger/src/rules.rs`. |
| **Tests** | `crates/ade_testkit/tests/ledger_determinism.rs::apply_block_deterministic`, `apply_block_never_returns_ok`. |
| **CI** | `ci/ci_check_forbidden_patterns.sh`, `ci/ci_check_dependency_boundary.sh`. |

### `T-CORE-02`: Forbidden nondeterminism

| Aspect | Location |
|--------|----------|
| **Source** | Project constitution §2, D-2 through D-5. |
| **Requirement** | No wall-clock, unseeded randomness, floats, `HashMap`/`HashSet`/`IndexMap`, `tokio`, `async fn`, or nondeterministic collections may appear in BLUE code. |
| **Code** | All six BLUE crates carry `#![deny(unsafe_code)]`, `#![deny(clippy::unwrap_used)]`, `#![deny(clippy::expect_used)]`, `#![deny(clippy::panic)]`, `#![deny(clippy::float_arithmetic)]` in `lib.rs`. PHASE4-N-B narrows further: `crates/ade_core/src/consensus/*` uses Rational arithmetic (e.g. `crates/ade_core/src/consensus/vrf_cert.rs`) and never f32/f64 — verified by `ci_check_no_float_in_consensus.sh`. |
| **Tests** | _(structural — enforced by lint + CI text scans)_; consensus-scoped Rational evidence in `crates/ade_core/tests/vrf_cert_threshold.rs::{taylor_exp_cmp_le_zero_x_returns_false, taylor_exp_cmp_le_x_equals_one_returns_true, taylor_exp_cmp_le_monotone_in_x, is_leader_determinism, is_leader_known_vector_matches_reference}`. |
| **CI** | `ci/ci_check_forbidden_patterns.sh` — scans `{ade_codec, ade_types, ade_crypto, ade_core, ade_ledger, ade_plutus}` for `HashMap`, `HashSet`, `IndexMap`, `SystemTime`, `Instant`, `std::fs`, `std::net`, `tokio`, `async fn`, `f32`/`f64`, `anyhow`, `rand::thread_rng`, `thread::spawn`, plus `unsafe` outside an allowlist. PHASE4-N-B added `ci/ci_check_no_float_in_consensus.sh` — narrowly scans `crates/ade_core/src/consensus/` for `f32`/`f64` tokens, redundant with `forbidden_patterns` for `ade_core` but pinned to the consensus tree to anchor `DC-CONS-{07,08,09}`. |

### `T-CORE-03`: Explicit state transitions

| Aspect | Location |
|--------|----------|
| **Source** | Project constitution §2, §5.2. |
| **Requirement** | State transitions must consume old state and produce new state, never partial-mutate. |
| **Code** | _(no enforcement found — gap; registry `code_locus` empty, `status = "declared"`. Pattern is followed by `apply_block`, `recover`, `apply_nonce_input`, `apply_op_cert`, `apply_rollback`, `select_best_chain`, `validate_and_apply_header` but not explicitly verified by lint or test)_ |
| **Tests** | _(no tests found — gap)_ |
| **CI** | _(no CI enforcement — gap)_ |

### `T-CORE-04`: Illegal states unrepresentable

| Aspect | Location |
|--------|----------|
| **Source** | Project constitution §2, §5.1. |
| **Requirement** | Illegal states must be unrepresentable via the type system where practical. |
| **Code** | _(no enforcement found — gap; structural style followed by `BlockVerdict`, `TxOutcome`, era-tagged tx enums, plus the closed PHASE4-N-B consensus enums `ChainEvent`, `ChainSelectionReject`, `HeaderValidationError`, `NonceInput`, `RollBackRequest`, `OpCertCounterError`, but not audited)_ |
| **Tests** | _(no tests found — gap)_ |
| **CI** | _(no CI enforcement — gap)_ |

### `T-ERR-01`: Structured authoritative errors

| Aspect | Location |
|--------|----------|
| **Source** | Project constitution §2. |
| **Requirement** | Errors in authoritative paths must be structured, comparable, and canonical. |
| **Code** | `crates/ade_ledger/src/error.rs` (`LedgerError`, `ValidityError`, `ConservationError`, `WitnessError`, `ScriptError`, `MintError`, `CertificateError`, `FeeError`, `EpochError`, `TranslationError`, `StructuralError`, `DecodingError`, `RuleNotYetEnforcedError`). |
| **Tests** | `crates/ade_ledger/...::ledger_error_equality`, `conservation_error_display`, `codec_error_conversion`. |
| **CI** | _(no CI enforcement — gap; status = `partial`)_ |

### `T-ERR-02`: Fail-fast on safety violation

| Aspect | Location |
|--------|----------|
| **Source** | Project constitution §2, E-1. |
| **Requirement** | Safety violations must fail-fast deterministically. |
| **Code** | _(no enforcement found — gap; style is followed in `apply_block*` returns and in PHASE4-N-B `validate_and_apply_header` short-circuiting but not audited)_ |
| **Tests** | _(no tests found — gap)_ |
| **CI** | _(no CI enforcement — gap)_ |

### `T-BUILD-01`: No semantic build variability

| Aspect | Location |
|--------|----------|
| **Source** | Project constitution §2. |
| **Requirement** | No build profile, feature flag, `cfg`, or optimization mode may alter authoritative semantics. |
| **Code** | All BLUE `lib.rs` files; no `#[cfg(feature = ...)]` admitted on BLUE source. |
| **Tests** | _(structural — text scan)_ |
| **CI** | `ci/ci_check_no_semantic_cfg.sh` — scans all 6 BLUE crates. |

### `T-BUILD-02`: One semantic interpretation per protocol version

| Aspect | Location |
|--------|----------|
| **Source** | Project constitution §2. |
| **Requirement** | Exactly one semantic interpretation may exist for a given protocol version and input set. |
| **Code** | _(no enforcement found — gap; status = `declared`)_ |
| **Tests** | _(no tests found — gap)_ |
| **CI** | _(no CI enforcement — gap)_ |

### `T-REC-01`: Recovery is replay-equivalent

| Aspect | Location |
|--------|----------|
| **Source** | Project constitution §2. |
| **Requirement** | Restart must produce byte-identical authoritative state to a clean replay. |
| **Code** | `crates/ade_runtime/src/recovery.rs` (snapshot + forward-replay, S-36); `crates/ade_runtime/src/chaindb/crash_safety.rs` (kill-strategy framework, S-37). |
| **Tests** | `crates/ade_runtime/src/recovery.rs::recover_from_snapshot_and_replay_forward`, `recover_from_genesis_when_no_snapshot`, `apply_failure_surfaces_with_slot`, `snapshot_decode_failure_surfaces_as_error`, `snapshot_with_no_post_blocks_is_ok`; `crates/ade_runtime/tests/stress_kill_harness.rs::stress_kill_smoke`, `stress_kill_1000`, `snapshot_table_intact_after_kill_loop`; `crates/ade_runtime/src/chaindb/crash_safety.rs::persistent_passes_crash_safety_with_no_kill`. |
| **CI** | `ci/ci_check_recovery_contract.sh`, `ci/ci_check_chaindb_crash_safety.sh`. Flipped `declared → enforced` in `78da6c9`. |

### `T-REC-02`: State derivable by replay

| Aspect | Location |
|--------|----------|
| **Source** | Project constitution §2. |
| **Requirement** | All authoritative state must be derivable by replay from the canonical inputs. |
| **Code** | `crates/ade_runtime/src/recovery.rs::recover`. |
| **Tests** | `crates/ade_runtime/src/recovery.rs::recover_from_snapshot_and_replay_forward`, `recover_from_genesis_when_no_snapshot`. |
| **CI** | `ci/ci_check_recovery_contract.sh`. Flipped `declared → enforced` in `78da6c9`. |

### `T-COLL-01`: Deterministic iteration order

| Aspect | Location |
|--------|----------|
| **Source** | Project constitution §2, D-5. |
| **Requirement** | All semantically meaningful collections must have deterministic iteration order. |
| **Code** | _(no enforcement found — gap; pattern is followed in `ade_ledger` and `ade_core::consensus` via `BTreeMap`/sorted `Vec` — including the op-cert counter map in `praos_state.rs` — but no compile-time prohibition beyond `forbidden_patterns` ban on `HashMap`)_ |
| **Tests** | `crates/ade_core/tests/praos_state_canonical_roundtrip.rs::op_cert_counter_map_iteration_is_deterministic` and the corresponding unit `consensus::encoding::tests::op_cert_counter_map_iteration_is_deterministic` exercise the BTreeMap ordering on the op-cert counter map used in `PraosChainDepState`. _(Registry has no direct linkage from this rule yet — partial proof via DC-CONS-04 evidence.)_ |
| **CI** | Implicitly via `ci/ci_check_forbidden_patterns.sh` (bans `HashMap`/`HashSet`/`IndexMap` in BLUE). |

### `T-BOUND-01`: Shell-to-core boundary converts nondeterminism

| Aspect | Location |
|--------|----------|
| **Source** | Project constitution §2. |
| **Requirement** | The shell may observe nondeterminism but must convert it to deterministic canonical inputs before crossing into core. |
| **Code** | _(no enforcement found — gap; convention followed at `ade_runtime::recovery` boundary and at the PHASE4-N-B GREEN orchestrator `ade_runtime::consensus::chain_selector::process_stream_input` — which translates RED `StreamInput` into canonical BLUE `HeaderInput`/`NonceInput`/`RollBackRequest` — but not audited)_ |
| **Tests** | _(no tests found — gap)_ |
| **CI** | _(no CI enforcement — gap)_ |

### `T-BOUND-02`: BLUE never depends on RED

| Aspect | Location |
|--------|----------|
| **Source** | Project constitution §2, GP-4. |
| **Requirement** | Authoritative crates must never depend on shell crates. |
| **Code** | All `crates/*/Cargo.toml`. |
| **Tests** | _(structural — enforced by Cargo + CI text scan)_ |
| **CI** | `ci/ci_check_dependency_boundary.sh` — scans all 6 BLUE crates for any `ade_runtime` reference. |

### `T-INGRESS-01`: Named canonical decode chokepoints

| Aspect | Location |
|--------|----------|
| **Source** | Project constitution §2, audit #21. |
| **Requirement** | All authoritative external bytes must enter core through named canonical decode/validation chokepoints; bypasses forbidden except via explicit CI-whitelisted sites. |
| **Code** | `crates/ade_codec/src/preserved.rs` (`PreservedCbor::new` `pub(crate)`); era decoders `decode_byron_block`, `decode_shelley_block`, `decode_allegra_block`, `decode_mary_block`, `decode_alonzo_block`, `decode_babbage_block`, `decode_conway_block`, `decode_block_envelope` (all in `ade_codec`); `PlutusScript::from_cbor` in `crates/ade_plutus/src/evaluator.rs`. |
| **Tests** | _(no tests found — gap; status = `declared` despite partial enforcement)_ |
| **CI** | `ci/ci_check_ingress_chokepoints.sh` — scans all 6 BLUE crates. Check 1 forbids `PreservedCbor::new` outside `ade_codec`. Check 2 verifies the `decode_block_envelope` named chokepoint exists. Check 3 forbids raw CBOR decoding outside `ade_codec`, with `ade_plutus/src/evaluator.rs` allowlisted because `PlutusScript::from_cbor` is the named Plutus-script CBOR chokepoint. |

### `T-CI-01`: Every true invariant has mechanical CI

| Aspect | Location |
|--------|----------|
| **Source** | Project constitution §2, CI-1. |
| **Requirement** | Every true invariant must have at least one mechanical CI enforcement; no waivers. |
| **Code** | `ci/`. |
| **Tests** | _(structural)_ |
| **CI** | `ci/ci_check_constitution_coverage.sh` — reads `docs/ade-invariant-registry.toml` and validates schema, coverage, tiers, status. Registry path was repointed from `constitution_registry.toml` in commit `39865f6`. |

### `T-KEY-01`: Signing in shell only

| Aspect | Location |
|--------|----------|
| **Source** | Project constitution §2, audit #20. |
| **Requirement** | Signing and private-key operations must live in the shell; only verification belongs in core. |
| **Code** | `crates/ade_crypto/src/lib.rs` (verify-only surface). |
| **Tests** | _(structural — text scan)_ |
| **CI** | `ci/ci_check_no_signing_in_blue.sh` — scans all 6 BLUE crates and bans `SigningKey`/`SecretKey`/`PrivateKey`/`private_key`/`sign_message`/`sign_block`. |

### `T-CONS-01`: Chain selection on canonical observables

| Aspect | Location |
|--------|----------|
| **Source** | Project constitution §2, 01_core §12. |
| **Requirement** | Chain selection must depend only on canonical observables; same candidates produce the same tip. |
| **Code** | _(registry `code_locus` empty — gap; PHASE4-N-B lands the substantive chain-selection surface — `crates/ade_core/src/consensus/fork_choice.rs::select_best_chain` and `crates/ade_core/src/consensus/candidate.rs` — but this rule's registry entry has not yet been wired. De-facto enforcement via `DC-CONSENSUS-01` and `DC-CONS-03`.)_ |
| **Tests** | De-facto via `DC-CONSENSUS-01` / `DC-CONS-03` tests. |
| **CI** | De-facto via `ci/ci_check_no_density_in_fork_choice.sh` (PHASE4-N-B) — bans density-based ordering on the caught-up Praos fork-choice surface. _(Registry linkage gap.)_ |

### `T-CONS-02`: Consensus must not depend on host nondeterminism

| Aspect | Location |
|--------|----------|
| **Source** | Project constitution §2, 01_core §3 D-2, audit #9. |
| **Requirement** | Authoritative consensus decisions must not depend on wall-clock, arrival order, scheduler, or OS behavior. |
| **Code** | _(registry `code_locus` empty — gap; PHASE4-N-B `crates/ade_core/src/consensus/{fork_choice,header_validate,candidate,era_schedule,nonce,op_cert}.rs` together discharge the substantive obligation: no wall-clock anywhere in BLUE consensus, slot→time is `pure (EraSchedule, SystemStart, SlotNo) -> u64`, no scheduler dependence in transitions. Registry not yet linked.)_ |
| **Tests** | De-facto via `CN-CONS-05` tests. |
| **CI** | De-facto via `ci/ci_check_no_float_in_consensus.sh`, `ci/ci_check_no_density_in_fork_choice.sh`, `ci/ci_check_forbidden_patterns.sh` (no clocks/threads in BLUE). _(Registry linkage gap.)_ |

### `T-CONSERV-01`: UTxO and asset conservation

| Aspect | Location |
|--------|----------|
| **Source** | Project constitution §2, audit #13. |
| **Requirement** | UTxO and asset conservation must hold for every accepted transition, except for explicitly authorized mint/burn/rewards/treasury flows. |
| **Code** | `crates/ade_ledger/src/value.rs`, `crates/ade_ledger/src/byron.rs`; plus PHASE4-B3 — `crates/ade_ledger/src/conway.rs::check_conway_coin_conservation` now enforces the **full** Conway preservation-of-value equation (`sum(inputs) + withdrawals + refunds == sum(outputs) + fee + deposits + donation`) for cert/withdrawal-bearing txs; the conservative cert/withdrawal early-out shipped in B2 has been removed, so deposit/refund/withdrawal-bearing txs are now in scope (closes the §H gap). |
| **Tests** | `crates/ade_testkit/tests/..::all_eras_replay_summary`, `byron_replay_all_1500`, `shelley_replay_all_1500`; plus PHASE4-B3 `crates/ade_ledger/tests/conway_conservation_full.rs::{conway_conservation_full, conservation_early_out_removed}`, `crates/ade_ledger/tests/conway_conservation_adversarial.rs::{adversarial_imbalanced_via_deposit, adversarial_imbalanced_via_withdrawal, adversarial_no_false_accept}`, `crates/ade_ledger/tests/conway_conservation_positive_synthetic.rs::{synthetic_balanced_cert_withdrawal_txs_are_valid, synthetic_positive_verdict_stream_replays_byte_identical}`, and the real-corpus positive coverage `crates/ade_ledger/tests/tx_validity_positive_corpus.rs::{all_corpus_txs_valid, corpus_tx_count_nonzero}`. |
| **CI** | `ci/ci_check_differential_divergence.sh`; plus PHASE4-B3 `ci/ci_check_ledger_determinism.sh` (the cert/withdrawal-bearing conservation path now folds into the deterministic ledger surface). The full-equation accept/reject property has no dedicated conservation-corpus CI gate — proven by the named tests; see §G. |

### `T-NOSPEND-01`: No double-spend

| Aspect | Location |
|--------|----------|
| **Source** | Project constitution §2, audit #14. |
| **Requirement** | No input or equivalent spend authority may be consumed more than once in an accepted canonical chain. |
| **Code** | `crates/ade_ledger/src/utxo.rs`. |
| **Tests** | `crates/ade_ledger/...::check_duplicate_inputs_catches_dupes`, `duplicate_inputs_detected`; `crates/ade_testkit/tests/..::all_eras_replay_summary`. |
| **CI** | `ci/ci_check_differential_divergence.sh`. |

### `T-EPOCH-01`: One authoritative committee per epoch

| Aspect | Location |
|--------|----------|
| **Source** | Project constitution §2, 01_core §13, audit #18. |
| **Requirement** | Exactly one authoritative committee and governance interpretation per epoch. |
| **Code** | _(no enforcement found — gap; Conway governance code exists in `ade_ledger/src/governance.rs` but registry has not yet wired this rule to it)_ |
| **Tests** | _(no tests found — gap)_ |
| **CI** | _(no CI enforcement — gap)_ |

### `T-CAUSAL-01`: No retroactive reinterpretation

| Aspect | Location |
|--------|----------|
| **Source** | Project constitution §2, audit #17. |
| **Requirement** | Future decisions may not leak into present validation; later states may not retroactively reinterpret prior checkpoints. |
| **Code** | _(no enforcement found — gap; convention is followed by atomic Conway ratification/enactment and by the PHASE4-N-B nonce rotation — `nonce.rs` promotes candidate→epoch nonce only at the epoch boundary — but not lifted into a CI gate)_ |
| **Tests** | _(no tests found — gap)_ |
| **CI** | _(no CI enforcement — gap)_ |

### `T-TRANSPORT-01`: No transport nondeterminism in authority

| Aspect | Location |
|--------|----------|
| **Source** | Project constitution §2, audit #4. |
| **Requirement** | Socket fragmentation, mux ordering, timeouts, and other transport nondeterminism must not leak into authoritative logic. |
| **Code** | _(registry `code_locus` empty — gap; structurally protected by `ci_check_no_async_in_blue.sh` and the PHASE4-N-B `ci_check_no_chaindb_in_consensus_blue.sh`. Registry linkage gap.)_ |
| **Tests** | _(no tests found — gap)_ |
| **CI** | De-facto via `ci/ci_check_no_async_in_blue.sh` and `ci/ci_check_no_chaindb_in_consensus_blue.sh`. _(Registry linkage gap.)_ |

### `T-RESOURCE-01`: Bounded resource allocation pre-validation

| Aspect | Location |
|--------|----------|
| **Source** | Project constitution §2, audit #5. |
| **Requirement** | Untrusted inputs must not allocate unbounded authoritative resources before deterministic validation. |
| **Code** | _(no enforcement found — gap; PHASE4-N-A DoS hardening replaced unbounded `Vec::with_capacity(len)` with size-checked alternatives in 8 codecs at S-A10 close, but no registry linkage yet)_ |
| **Tests** | _(no tests found — gap)_ |
| **CI** | _(no CI enforcement — gap)_ |

### `T-PLATFORM-01`: No host-environment leakage

| Aspect | Location |
|--------|----------|
| **Source** | Project constitution §2, audit #16. |
| **Requirement** | No host-environment property (locale, timezone, architecture, platform) may influence authoritative computation. |
| **Code** | _(no enforcement found — gap; convention followed via `ade_ledger` purity and PHASE4-N-B `ade_core::consensus` purity but not audited)_ |
| **Tests** | _(no tests found — gap)_ |
| **CI** | Implicitly via `ci/ci_check_forbidden_patterns.sh` and `ci/ci_check_no_float_in_consensus.sh`. |

---

## DC — Derived Cardano Compatibility Invariants (Project Constitution §3)

### `DC-CBOR-01`: CBOR round-trip identity for all era types

| Aspect | Location |
|--------|----------|
| **Source** | Project constitution §3; refines `T-ENC-01`, `T-ENC-03`. |
| **Requirement** | Cardano CBOR decode/encode round-trips to identical bytes for every era type. |
| **Code** | `crates/ade_codec/src/{byron,shelley,allegra,mary,alonzo,babbage,conway}/`. |
| **Tests** | _(no tests recorded in registry — gap; the corpus suite under `crates/ade_testkit/tests/` exercises this end-to-end but the registry has no `tests` array)_ |
| **CI** | `ci/ci_check_cbor_round_trip.sh`. |

### `DC-CBOR-02`: Wire-byte preservation on hash-critical paths

| Aspect | Location |
|--------|----------|
| **Source** | Project constitution §3; refines `T-ENC-01`, `T-DET-01`. |
| **Requirement** | Original wire bytes preserved for hash computation on hash-critical paths (Byte Authority Model). |
| **Code** | `crates/ade_codec/src/preserved.rs::PreservedCbor`. |
| **Tests** | _(no tests recorded in registry — gap)_ |
| **CI** | `ci/ci_check_hash_uses_wire_bytes.sh` — scans all 6 BLUE crates. |

### `DC-CRYPTO-01`: Crypto matches Haskell node on KAT

| Aspect | Location |
|--------|----------|
| **Source** | Project constitution §3; refines `T-CORE-01`, `T-DET-01`. |
| **Requirement** | Crypto verification is pure and matches the Haskell node on all test vectors. |
| **Code** | `crates/ade_crypto/src/`; plus PHASE4-N-B consensus-side VRF wrapper `crates/ade_core/src/consensus/vrf_cert.rs` (typed 41-byte Praos VRF input + Rational threshold check). |
| **Tests** | `crates/ade_crypto/...::blake2b_256_rfc_test_vectors`, `ed25519_rfc_8032_test_1`, `vrf_ietf_draft03_section_a3`; plus PHASE4-N-B consensus integration tests `crates/ade_core/tests/vrf_cert_threshold.rs::{verify_vrf_cert_accepts_valid_proof, verify_vrf_cert_rejects_wrong_alpha, verify_vrf_cert_rejects_malformed_proof}`. |
| **CI** | `ci/ci_check_crypto_vectors.sh`. Strengthened for PHASE4-N-B (S-B3 typed VRF input — 8-byte BE slot ‖ 32-byte epoch nonce ‖ 1-byte role tag, NONCE=0x4E / LEADER=0x4C — built by a single audited function so callers cannot accidentally cross domains). Strengthened again for PHASE4-B1 (Praos single-VRF input + op-cert-signable fix proven against 14 real Conway headers); `ci_check_crypto_vectors.sh` remains the gate. |

### `DC-CRYPTO-02`: Signing confined to shell

| Aspect | Location |
|--------|----------|
| **Source** | Project constitution §3; refines `T-BOUND-01`. |
| **Requirement** | All signing operations must be confined to the shell. |
| **Code** | `crates/ade_crypto/src/lib.rs`. |
| **Tests** | _(no tests — structural)_ |
| **CI** | `ci/ci_check_no_signing_in_blue.sh` — scans all 6 BLUE crates. |

### `DC-LEDGER-01`: `apply_block` pure and deterministic

| Aspect | Location |
|--------|----------|
| **Source** | Project constitution §3; refines `T-CORE-01`, `T-CORE-03`. |
| **Requirement** | `apply_block(state, block)` must be pure and deterministic. |
| **Code** | `crates/ade_ledger/src/rules.rs`. |
| **Tests** | `crates/ade_ledger/...::apply_block_byron_ebb_passes_through`, `apply_block_deterministic`, `crates/ade_testkit/tests/..::all_eras_determinism_summary`. |
| **CI** | `ci/ci_check_ledger_determinism.sh` (CE-74). |

### `DC-LEDGER-02`: Same inputs → byte-identical ledger state

| Aspect | Location |
|--------|----------|
| **Source** | Project constitution §3; refines `T-DET-01`. Strengthened by PHASE4-B1 (block adversarial corpus) and PHASE4-B2 (tx adversarial corpus). |
| **Requirement** | Same genesis plus same blocks must yield byte-identical ledger state; no mutation of a real block or tx ever yields `Valid` (no false accept). |
| **Code** | `crates/ade_ledger/src/state.rs`, `crates/ade_ledger/src/fingerprint.rs`; plus PHASE4-B1 `crates/ade_testkit/src/validity/adversarial.rs` (GREEN no-false-accept block adversarial corpus — deterministic mutators M1–M6 derive adversarial blocks from the real Conway-576 corpus; no mutation ever yields `Valid` through `block_validity`); plus PHASE4-B2 `crates/ade_testkit/src/tx_validity/adversarial.rs` (GREEN no-false-accept tx adversarial corpus — deterministic witness/value/input mutations replay byte-identically through `tx_validity`, no mutation ever yields `Valid`). |
| **Tests** | `crates/ade_testkit/tests/..::utxo_state_deterministic`, `all_eras_determinism_summary`, `boundary_fingerprint_matches_pins`; plus PHASE4-B1 `crates/ade_ledger/tests/block_validity_adversarial_corpus.rs::{no_mutation_is_ever_valid, adversarial_replays_identically}`; plus PHASE4-B2 `crates/ade_ledger/tests/tx_validity_adversarial_corpus.rs::{no_mutation_is_ever_valid, each_mutation_maps_to_expected_class, adversarial_replays_identically}` (no false accept across the adversarial cases). |
| **CI** | `ci/ci_check_ledger_determinism.sh`, `ci/ci_check_differential_divergence.sh`. Status = `partial` (boundary-snapshot precision; per-block live differential deferred to external harness). Strengthened for PHASE4-B1 (block no-false-accept corpus) and PHASE4-B2 (tx no-false-accept corpus) — no new CI script. |

### `DC-LEDGER-03`: Tx/block validity matches Haskell

| Aspect | Location |
|--------|----------|
| **Source** | Project constitution §3; refines `T-DET-01`, `T-ERR-01`. |
| **Requirement** | Validity verdicts for transactions and blocks must agree with the Haskell node on all tested inputs. |
| **Code** | `crates/ade_ledger/src/byron.rs`, `crates/ade_ledger/src/rules.rs`, `crates/ade_ledger/src/plutus_eval.rs`. |
| **Tests** | `crates/ade_ledger/...::check_duplicate_inputs_catches_dupes`, `resolve_inputs_missing_input`, `missing_witnesses_rejected`; `crates/ade_testkit/tests/..::all_eras_replay_summary`, `all_plutus_boundaries_aggregate_zero_rejections`, `plutus_era_contiguous_smoke`. |
| **CI** | `ci/ci_check_differential_divergence.sh`. Status = `partial` (CE-88 oracle-diff deferred to release-gate work). |

### `DC-LEDGER-04`: Epoch boundary computations match Haskell

| Aspect | Location |
|--------|----------|
| **Source** | Project constitution §3; refines `T-DET-01`. |
| **Requirement** | Epoch boundary computations (stake snapshots, rewards) must match the Haskell node. |
| **Code** | `crates/ade_ledger/src/epoch.rs`, `crates/ade_ledger/src/rules.rs`. |
| **Tests** | `crates/ade_testkit/tests/..::precise_boundary_comparison_eta_diagnosis`, `alonzo_epoch_boundary_end_to_end`, `regular_epoch_boundary_comparison`, `conway_epoch_boundary_end_to_end`. |
| **CI** | _(no CI enforcement — gap; CE-71/CE-72 proofs landed but no `ci_check_epoch_boundary.sh` gate exists)_ |

### `DC-PLUTUS-01`: UPLC evaluation determinism

| Aspect | Location |
|--------|----------|
| **Source** | Project constitution §3; refines `T-DET-01`, `T-CORE-01`. |
| **Requirement** | Same script + same redeemers/datum/context + same cost model → identical UPLC result and budget. |
| **Code** | _(no enforcement recorded in registry — gap; `crates/ade_plutus/src/evaluator.rs` is the actual implementation)_ |
| **Tests** | _(no tests recorded — gap; `aiken_fixture_tx_evaluates_end_to_end` proves the path end-to-end)_ |
| **CI** | _(no CI enforcement — gap)_ |

### `DC-PLUTUS-02`: Deterministic budget-exhaustion error

| Aspect | Location |
|--------|----------|
| **Source** | Project constitution §3; refines `T-ERR-01`. |
| **Requirement** | Budget exhaustion must produce a deterministic structured error. |
| **Code** | _(no enforcement recorded — gap; `crates/ade_plutus/src/evaluator.rs::PlutusError` covers it but registry not yet wired)_ |
| **Tests** | _(no tests — gap)_ |
| **CI** | _(no CI enforcement — gap)_ |

### `DC-CONSENSUS-01`: Chain selection matches Haskell

| Aspect | Location |
|--------|----------|
| **Source** | Project constitution §3; refines `T-DET-01`, `T-CORE-01`. Cross-refs `DC-CONS-03`, `DC-CONS-05`, `DC-CONS-06`, `CN-CONS-01`. |
| **Requirement** | Chain selection must be deterministic and match Haskell node behavior. |
| **Code** | `crates/ade_core/src/consensus/fork_choice.rs::select_best_chain` (block-number first, then `TiebreakerView`); `crates/ade_core/src/consensus/candidate.rs` (closed `CandidateFragment` + `TiebreakerView` types); `crates/ade_core/src/consensus/rollback.rs` (typed `apply_rollback`); GREEN orchestrator `crates/ade_runtime/src/consensus/chain_selector.rs::process_stream_input`; RED live-peer driver `crates/ade_core_interop/src/lib.rs` + `crates/ade_core_interop/src/bin/live_consensus_session.rs`. |
| **Tests** | Fork-choice corpus battery `crates/ade_core/tests/fork_choice_corpus.rs::{higher_block_no_wins, equal_block_no_tiebreaker_decides, fork_before_immutable_tip_rejected, exceeded_rollback_rejected, tiebreaker_loss_keeps_current, replay_is_deterministic, reject_reason_bytes_are_stable, no_candidates_returns_error}` plus the unit-level tiebreaker tests `consensus::fork_choice::tests::{tiebreaker_prefer_lower_slot_wins, tiebreaker_prefer_higher_op_cert_wins_on_equal_slot_and_issuer, tiebreaker_prefer_lower_vrf_value_wins_on_full_tie}`; rollback corpus `crates/ade_core/tests/rollback_corpus.rs::{rollback_within_k_succeeds, rollback_exceeding_k_rejected_with_typed_reason, rollback_before_immutable_tip_rejected, rollback_event_bytes_are_stable, rollback_is_deterministic, rollback_equivalent_to_truncated_replay}`; orchestrator tests `consensus::chain_selector::tests::{header_arrival_updates_state_and_selector, rollback_walks_back_via_recent_snapshots, rollback_to_block_older_than_snapshots_rejected, epoch_boundary_emits_no_event}`; stream-replay closure `crates/ade_testkit/tests/consensus_stream_replay.rs::{synthetic_session_replays_identically, synthetic_session_event_kinds_match_corpus, synthetic_session_final_state_matches_corpus, op_cert_regression_header_rejected_mid_stream}` (CE-N-B-5); live-peer evidence `crates/ade_core_interop/tests/live_consensus_session.rs::cardano_node_session_sustained_window` (CE-N-B-6, `#[ignore]` by default). |
| **CI** | `ci/ci_check_no_density_in_fork_choice.sh` (PHASE4-N-B — bans density-based ordering on caught-up Praos fork-choice); `ci/ci_check_consensus_closed_enums.sh` (no `#[non_exhaustive]`, no open-tail variants, no owned `String` or `Box<dyn>` in consensus error/event taxonomies); `ci/ci_check_no_chaindb_in_consensus_blue.sh` (no `ChainDb`/`chain_db` references in BLUE consensus tree). Flipped `partial → enforced` in PHASE4-N-B. |

### `DC-CONSENSUS-02`: Pure leadership verification

| Aspect | Location |
|--------|----------|
| **Source** | Project constitution §3; refines `T-CORE-01`. |
| **Requirement** | Leadership verification must be pure. |
| **Code** | `crates/ade_core/src/consensus/leader_schedule.rs` (`query_leader_schedule`, `is_leader_for_vrf_output`); `crates/ade_core/src/consensus/ledger_view.rs` (`LedgerView` trait — extractive forecast surface); `crates/ade_core/src/consensus/vrf_cert.rs` (Praos VRF threshold check using Rational); plus PHASE4-B1 `crates/ade_ledger/src/consensus_view.rs` (pure ledger-side `LedgerView` projection feeding leadership inputs). |
| **Tests** | Unit-level `consensus::leader_schedule::tests::{query_uses_state_epoch_nonce_for_vrf_input, query_returns_unknown_pool_when_no_vrf_key, query_returns_outside_forecast_range_for_far_future, query_does_not_mutate_state, is_leader_for_vrf_output_delegates_to_vrf_cert}`; corpus tests `crates/ade_core/tests/leader_schedule_corpus.rs::{corpus_returns_canonical_answer_for_known_pools, corpus_rejects_unknown_pool, corpus_rejects_out_of_forecast_horizon, corpus_is_leader_helper_matches_pinned_probe, corpus_is_deterministic_across_runs}`; plus PHASE4-B1 `crates/ade_ledger/tests/consensus_view.rs::{view_returns_corpus_pool_stake_and_vrf_keyhash, view_unknown_pool_returns_none, view_unknown_epoch_returns_none, view_is_pure}` and `consensus_view::tests::pool_distr_view_no_hashmap`. |
| **CI** | _(no CI enforcement — gap; status = `partial`. The leader-schedule surface is exercised by the corpus tests above but no dedicated CI script gates it. Discharged structurally via `ci/ci_check_no_async_in_blue.sh`, `ci/ci_check_no_float_in_consensus.sh`, and `ci/ci_check_no_chaindb_in_consensus_blue.sh` over the consensus tree.)_ Flipped `declared → partial` in PHASE4-N-B; strengthened again for PHASE4-B1 (ledger-side `consensus_view` projection) — still no dedicated CI script. |

### `DC-PROTO-01`: Deterministic protocol state machines

| Aspect | Location |
|--------|----------|
| **Source** | Project constitution §3; refines `T-CORE-03`. |
| **Requirement** | Protocol state machines must have deterministic transitions. |
| **Code** | Per-protocol pure transition modules under `crates/ade_network/src/{chain_sync,block_fetch,tx_submission,keep_alive,peer_sharing}/{state,agency,signal,event,transition}.rs` plus the 4 N2C state machines under `crates/ade_network/src/n2c/{local_chain_sync,local_tx_submission,local_state_query,local_tx_monitor}/{state,agency,event,transition}.rs` (PHASE4-N-A S-A3..S-A8). |
| **Tests** | Per-protocol synthetic transition batteries across all 11 mini-protocols — see registry `tests:` array for full enumeration (`chain_sync::transition::tests::*`, `block_fetch::transition::tests::*`, `tx_submission::transition::tests::*`, `keep_alive::transition::tests::*`, `peer_sharing::transition::tests::*`, `n2c::*::transition::tests::*`); plus event-trace corpora `signal_trace_synthetic_divergence_corpus`, `block_fetch_frame_corpus`, `tx_submission2_mempool_trace`, `keep_alive_event_trace`, `peer_sharing_event_trace`, `local_chain_sync_event_trace`, `local_tx_submission_event_trace`, `local_state_query_event_trace`, `local_tx_monitor_event_trace`. |
| **CI** | _(registry `ci_script` empty — gap; de-facto via `ci/ci_check_ce_n_a_5_proof.sh` cluster gate and the CI scripts BLUE-list scoping. Registry needs follow-up wiring.)_ |

### `DC-PROTO-02`: Transcript equivalence with Haskell

| Aspect | Location |
|--------|----------|
| **Source** | Project constitution §3; refines `T-DET-01`. |
| **Requirement** | Mini-protocol transcripts must be equivalent to Haskell's. |
| **Code** | Per-protocol pure transitions and signal/event types under `crates/ade_network/src/{chain_sync,block_fetch,tx_submission,keep_alive,peer_sharing}/{state,agency,signal,event,transition}.rs` (PHASE4-N-A S-A3..S-A7). |
| **Tests** | Synthetic transition batteries across chain-sync, block-fetch, tx-submission, keep-alive, peer-sharing; plus real-capture transcript checks against cardano-node 11.0.1 wire bytes — `chain_sync_real_capture_corpus`, `block_fetch_real_capture_corpus`, `keep_alive_real_capture_corpus`, `peer_sharing_real_capture_corpus`. |
| **CI** | `ci/ci_check_ce_n_a_5_proof.sh` (PHASE4-N-A — runs CE-N-A-5 condition 3 real-capture round-trip + condition 4 agency-trace transcript over real captures). Registry `ci_script` field still empty; status remains `declared`. |

### `DC-STORE-01`: Power-loss recovery is replay-equivalent

| Aspect | Location |
|--------|----------|
| **Source** | Project constitution §3; refines `T-REC-01`. |
| **Requirement** | Recovery from power-loss must produce replay-equivalent state. |
| **Code** | `crates/ade_runtime/src/chaindb/crash_safety.rs`; `crates/ade_runtime/tests/stress_kill_harness.rs`. |
| **Tests** | `crates/ade_runtime/tests/stress_kill_harness.rs::{stress_kill_smoke, stress_kill_1000, snapshot_table_intact_after_kill_loop}`; `crates/ade_runtime/src/chaindb/crash_safety.rs::persistent_passes_crash_safety_with_no_kill`. |
| **CI** | `ci/ci_check_chaindb_crash_safety.sh`. Flipped `declared → enforced` in `78da6c9`. |

### `DC-STORE-02`: Append-only finalized provenance

| Aspect | Location |
|--------|----------|
| **Source** | Project constitution §3; refines `T-REC-02`. |
| **Requirement** | Finalized data must be append-only. |
| **Code** | `crates/ade_runtime/src/chaindb/persistent.rs`, `crates/ade_runtime/src/chaindb/contract.rs`. |
| **Tests** | `crates/ade_runtime/src/chaindb/persistent.rs::{persistent_passes_contract, reopen_observes_committed_block}`; `crates/ade_runtime/src/chaindb/contract.rs::in_memory_passes_contract`. |
| **CI** | `ci/ci_check_chaindb_contract.sh`. Flipped `declared → enforced` in `78da6c9`. |

### `DC-STORE-03`: Atomic snapshots

| Aspect | Location |
|--------|----------|
| **Source** | Project constitution §3; refines `T-REC-01`. |
| **Requirement** | Snapshots must be atomic — fully written or absent. |
| **Code** | `crates/ade_runtime/src/chaindb/snapshot_contract.rs`, `crates/ade_runtime/src/chaindb/persistent.rs`. |
| **Tests** | `crates/ade_runtime/src/chaindb/persistent.rs::{persistent_passes_snapshot_contract, snapshots_persist_across_reopen, corrupted_magic_returns_corruption_error}`; `crates/ade_runtime/src/chaindb/snapshot_contract.rs::in_memory_passes_snapshot_contract`. |
| **CI** | `ci/ci_check_chaindb_contract.sh`. Flipped `declared → enforced` in `78da6c9`. |

### `DC-INGRESS-01`: Block/tx/protocol decode through chokepoints

| Aspect | Location |
|--------|----------|
| **Source** | Project constitution §3; refines `T-INGRESS-01`. |
| **Requirement** | Block, tx, and protocol-message decoding must enter core through named chokepoints. |
| **Code** | _(registry `code_locus` empty — gap; chokepoints listed under `T-INGRESS-01` Code row — `ade_codec` per-era decoders plus `PlutusScript::from_cbor` in `ade_plutus/src/evaluator.rs`)_ |
| **Tests** | _(no tests — gap)_ |
| **CI** | _(no CI enforcement linked in registry — gap; `ci/ci_check_ingress_chokepoints.sh` is the de-facto enforcer per CODEMAP, scanning all 6 BLUE crates with `ade_plutus/src/evaluator.rs` allowlisted on Check 3 — registry needs update)_ |

### `DC-INGRESS-02`: Storage rehydration via same chokepoints

| Aspect | Location |
|--------|----------|
| **Source** | Project constitution §3; refines `T-INGRESS-01`, `T-ENC-01`. |
| **Requirement** | Storage rehydration must enter core through the same canonical decode chokepoints as network ingress. |
| **Code** | _(no enforcement — gap; `recover` currently delegates to caller-supplied `Recoverable::decode_snapshot`; convention not enforced)_ |
| **Tests** | _(no tests — gap)_ |
| **CI** | _(no CI enforcement — gap)_ |

### `DC-REF-01`: Reference-source provenance

| Aspect | Location |
|--------|----------|
| **Source** | Project constitution §3; refines `T-DET-01`, `T-CI-01`. |
| **Requirement** | Every claimed equivalence check must identify its reference source, extraction method, and reproducibility path. |
| **Code** | `crates/ade_testkit/src/harness/provenance.rs`. |
| **Tests** | `crates/ade_testkit/...::provenance_validation`, `self_comparison_block_fields`. |
| **CI** | `ci/ci_check_ref_provenance.sh`. |

### `DC-PROTO-03`: Full N2N mini-protocol surface

| Aspect | Location |
|--------|----------|
| **Source** | Project constitution §3; refines `T-DET-01`, `T-CORE-03`. |
| **Requirement** | Implement N2N: Handshake, ChainSync, BlockFetch, TxSubmission2, KeepAlive, PeerSharing. |
| **Code** | `crates/ade_network/src/codec/{handshake,chain_sync,block_fetch,tx_submission,keep_alive,peer_sharing}.rs` (PHASE4-N-A S-A2). |
| **Tests** | `codec::handshake::tests::roundtrip_every_variant`, `codec::chain_sync::tests::roundtrip_every_variant`, `codec::block_fetch::tests::roundtrip_every_variant`, `codec::tx_submission::tests::roundtrip_every_variant`, `codec::keep_alive::tests::roundtrip_every_variant`, `codec::peer_sharing::tests::roundtrip_every_variant`. |
| **CI** | _(no CI enforcement linked in registry — gap; de-facto via `ci_check_ce_n_a_5_proof.sh` + `ci_check_cbor_round_trip.sh`)_ |

### `DC-PROTO-04`: Full N2C mini-protocol surface

| Aspect | Location |
|--------|----------|
| **Source** | Project constitution §3; refines `T-DET-01`, `T-CORE-03`. |
| **Requirement** | Implement N2C: Handshake, LocalChainSync, LocalTxSubmission, LocalStateQuery, LocalTxMonitor. |
| **Code** | `crates/ade_network/src/codec/{n2c_handshake,local_chain_sync,local_tx_submission,local_state_query,local_tx_monitor}.rs` plus the 4 N2C state machines under `crates/ade_network/src/n2c/{local_chain_sync,local_tx_submission,local_state_query,local_tx_monitor}/{state,agency,event,transition}.rs`. |
| **Tests** | Per-protocol synthetic transition batteries — see registry `tests:` array (full enumeration of `n2c::*::transition::tests::*` + `codec::local_*::tests::roundtrip_every_variant`) — plus event-trace corpora `local_chain_sync_event_trace`, `local_tx_submission_event_trace`, `local_state_query_event_trace`, `local_tx_monitor_event_trace`. |
| **CI** | _(no CI enforcement linked in registry — gap; de-facto via `ci_check_ce_n_a_5_proof.sh` + `ci_check_cbor_round_trip.sh`)_ |

### `DC-PROTO-05`: Closed version negotiation

| Aspect | Location |
|--------|----------|
| **Source** | Project constitution §3; refines `T-ENC-02`, `T-CORE-03`. |
| **Requirement** | Version negotiation is closed: enumerated N2N/N2C versions, explicit handshake, deterministic refusal on mismatch. |
| **Code** | `crates/ade_network/src/handshake/{mod,state,agency,selection,transition,version_table}.rs`. |
| **Tests** | Synthetic transition battery + real-capture handshake corpora against cardano-node 11.0.1 — `handshake_real_capture_corpus`, `n2c_handshake_real_capture_corpus`. PHASE4-N-A strengthening = +5 real-capture tests (3 N2N + 2 N2C handshake). |
| **CI** | `ci/ci_check_ce_n_a_5_proof.sh` (PHASE4-N-A — runs CE-N-A-5 condition 1: handshake version negotiation deterministic). |

### `DC-EPOCH-01`: Conway governance timing

| Aspect | Location |
|--------|----------|
| **Source** | Project constitution §3; refines `T-CAUSAL-01`, `T-EPOCH-01`. |
| **Requirement** | Proposals accumulate during the epoch; ratification and enactment are atomic at the epoch boundary; pulsing distributes DRep stake computation across the epoch. |
| **Code** | `crates/ade_ledger/src/governance.rs`, `crates/ade_ledger/src/rules.rs`. |
| **Tests** | `crates/ade_testkit/tests/..::conway_epoch_boundary_end_to_end`, `crates/ade_ledger/...::conway_governance_ratification_test`. |
| **CI** | _(no CI enforcement — gap; CE-72 proven but no dedicated CI gate)_ |

### `DC-EPOCH-02`: HFC translations and forecast horizon

| Aspect | Location |
|--------|----------|
| **Source** | Project constitution §3; CE-79 Tier 2 closure; `docs/active/CE-73_reclassification.md`. Cross-refs `DC-CONS-07`. |
| **Requirement** | Hard fork transitions are triggered at deterministic slot/epoch boundaries; era translation functions are mandatory; the forecast horizon extends to the era boundary. CE-73-semantic: 22/22 encoding-independent fields match oracle (Tier 2 closed). CE-73-bytes: explicit non-goal (Tier 4). PHASE4-N-B adds the consensus-side HFC scheduling surface — `EraSchedule` typed value + slot/era/time translation. |
| **Code** | `crates/ade_ledger/src/hfc.rs` (ledger-side translation); `crates/ade_core/src/consensus/era_schedule.rs` (PHASE4-N-B consensus-side typed `EraSchedule` with constructor invariants — non-empty, monotone, non-zero slot/epoch lengths — and pure `slot_to_time_ms`). |
| **Tests** | Ledger-side: `crates/ade_testkit/tests/..::{translation_summary_proof, translation_comparison_surface, transition_proof_surface}`. Consensus-side: `consensus::era_schedule::tests::{locate_first_slot_of_each_era, locate_last_slot_of_each_era, forecast_horizon_boundary, slot_to_time_monotone_increasing, slot_to_time_overflow_returns_structured_error, eraschedule_constructor_rejects_empty, eraschedule_constructor_rejects_non_monotonic, eraschedule_constructor_rejects_zero_slot_length, eraschedule_constructor_rejects_zero_epoch_length}`; corpus tests `crates/ade_core/tests/hfc_schedule_corpus.rs::{mainnet_corpus_translation_matches_oracle, preprod_corpus_translation_matches_oracle}`. |
| **CI** | `ci/ci_check_hfc_translation.sh` (CE-73-semantic, ledger-side). Consensus-side EraSchedule is covered by `ci/ci_check_no_float_in_consensus.sh` and `ci/ci_check_no_chaindb_in_consensus_blue.sh` (`DC-CONS-07`, `DC-CONS-08`). |

### `DC-STORE-04`: ChainDB structure (Immutable/Volatile/Ledger)

| Aspect | Location |
|--------|----------|
| **Source** | Project constitution §3; refines `T-REC-01`, `T-REC-02`. |
| **Requirement** | ImmutableDB (append-only when k-deep), VolatileDB (recent within k), LedgerDB (snapshots + forward replay). |
| **Code** | _(documented Tier 5 divergence — not a gap. The rule names cardano-node's literal three-DB topology; Ade has deliberately diverged per CE-79 Tier 5 to a single redb-backed store in `crates/ade_runtime/src/chaindb/persistent.rs`.)_ |
| **Tests** | _(documented Tier 5 divergence)_ |
| **CI** | _(documented Tier 5 divergence — the semantic guarantees DC-STORE-04 was designed to deliver survive and are now mechanically enforced under `DC-STORE-02` and `DC-STORE-03`. The rule remains `status = "declared"` per IDD append-only doctrine. See §B for the full rationale.)_ |

### `DC-STORE-05`: Recovery is snapshot + forward replay

| Aspect | Location |
|--------|----------|
| **Source** | Project constitution §3; refines `T-REC-01`. |
| **Requirement** | Recovery loads the most recent valid snapshot and replays forward from the ImmutableDB tip; not full genesis replay. |
| **Code** | `crates/ade_runtime/src/recovery.rs::recover` (S-36). |
| **Tests** | `crates/ade_runtime/src/recovery.rs::{recover_from_snapshot_and_replay_forward, recover_from_genesis_when_no_snapshot, no_starting_point_error, snapshot_with_no_post_blocks_is_ok}`. |
| **CI** | `ci/ci_check_recovery_contract.sh`. Flipped `declared → enforced` in `78da6c9`. |

### `DC-STORE-06`: VolatileDB validation mode

| Aspect | Location |
|--------|----------|
| **Source** | Project constitution §3; refines `T-INGRESS-01`. |
| **Requirement** | VolatileDB uses `ValidateAll` after unclean shutdown; `NoValidation` acceptable during clean operation as optimization. |
| **Code** | _(no enforcement — gap; only single validation mode exposed at HEAD; also subject to the DC-STORE-04 Tier 5 divergence since Ade has no separate VolatileDB)_ |
| **Tests** | _(no tests — gap)_ |
| **CI** | _(no CI enforcement — gap)_ |

### `DC-QUERY-01`: Version-gated era-aware N2C queries

| Aspect | Location |
|--------|----------|
| **Source** | Project constitution §3; refines `T-DET-01`, `DC-PROTO-04`. |
| **Requirement** | N2C queries must be era-aware, typed, and version-gated. |
| **Code** | _(no enforcement — gap)_ |
| **Tests** | _(no tests — gap)_ |
| **CI** | _(no CI enforcement — gap)_ |

### `DC-NET-01`: Three-tier peer management

| Aspect | Location |
|--------|----------|
| **Source** | Project constitution §3; refines `T-RESOURCE-01`, `T-TRANSPORT-01`. |
| **Requirement** | Peer selection uses cold/warm/hot tiers with bounded admission, per-peer resource limits, and eviction policies. |
| **Code** | _(no enforcement — gap)_ |
| **Tests** | _(no tests — gap)_ |
| **CI** | _(no CI enforcement — gap)_ |

### `DC-LEDGER-05`: Era-specific witness binding

| Aspect | Location |
|--------|----------|
| **Source** | Project constitution §3; refines `T-DET-01`. Strengthened by PHASE4-B2 (Conway vkey-witness binding). |
| **Requirement** | Byron TxWitness, Shelley+ WitsVKey/Scripts/BootstrapWitnesses, Alonzo+ Redeemers/Datums, Conway governance witnesses. |
| **Code** | `crates/ade_ledger/src/witness.rs`, `crates/ade_ledger/src/scripts.rs`, `crates/ade_plutus/src/evaluator.rs`; `crates/ade_ledger/src/tx_validity/witness.rs` (PHASE4-B2-S1 — Conway vkey-witness binding: fail-closed Ed25519 coverage over the preserved body hash). |
| **Tests** | `crates/ade_ledger/...::{witness_info_no_plutus, witness_info_plutus_detection, empty_witness_set}`, `crates/ade_plutus/...::aiken_fixture_tx_evaluates_end_to_end`; `crates/ade_ledger/tests/tx_witness_closure.rs::{all_required_covered_is_valid, signature_over_wrong_body_rejected, witness_correct_key_wrong_body_rejected, wrong_size_signature_rejected, wrong_size_vkey_rejected}`. |
| **CI** | _(no dedicated CI script — gap; status = `partial`. The Conway witness-closure surface is kept structurally closed by `ci/ci_check_consensus_closed_enums.sh` over `ade_ledger/src/tx_validity/`, but no dedicated witness-binding CI gate exists. See §G.)_ |

### `DC-LEDGER-06`: Pure ScriptContext derivation

| Aspect | Location |
|--------|----------|
| **Source** | Project constitution §3; refines `T-PLATFORM-01`, `T-DET-01`. |
| **Requirement** | ScriptContext/TxInfo must be derived from tx + ledger state + network-wide constants; no host-environment data. |
| **Code** | _(registry `code_locus` empty — gap; `crates/ade_plutus/src/script_context.rs` is the de-facto implementation)_ |
| **Tests** | _(no tests recorded — gap)_ |
| **CI** | _(no CI enforcement — gap)_ |

### `DC-LEDGER-07`: Mixed-version verdict agreement

| Aspect | Location |
|--------|----------|
| **Source** | Project constitution §3; refines `T-DET-01`, `T-BUILD-02`. |
| **Requirement** | Coexisting supported versions must return the same validity verdict for consensus-relevant inputs. |
| **Code** | _(no enforcement — gap; release-tier obligation, not run at HEAD)_ |
| **Tests** | _(no tests — gap)_ |
| **CI** | _(no CI enforcement — gap)_ |

### `DC-LEDGER-08`: Closed/total Conway cert-state accumulation (fail-closed)

| Aspect | Location |
|--------|----------|
| **Source** | Cardano ledger spec (Conway CDDL certificate tags 0..18; delegation/pool state transitions); IDD fail-fast + closed-surface doctrine. PHASE4-B4 new rule (`status = "enforced"`). Cross-refs `DC-VAL-06`, `DC-TXV-06`, `DC-LEDGER-07`, `T-DET-01`, `CN-LEDGER-07`. |
| **Requirement** | Conway cert-state accumulation is a closed, total, era-versioned transition: certificates decode through the era-correct closed grammar (Conway via `decode_conway_certs` retaining all owner payloads, tags 0..18) selected by explicit era dispatch — never the Shelley 6-variant decoder on Conway bytes, never reduced into the 7-variant Shelley `Certificate`, never with payload fields dropped. Every certificate resolves to an owner-tagged disposition: it mutates B4-owned `CertState` (delegation/pool), or it is owner-tagged to `ConwayGovState` and routed out-of-mutation-scope (observed, not swallowed), or it is a structured reject (`NotValidInEra` for removed tags 5/6, `Malformed` for bad CBOR, `UnsupportedUntilStateOwner` only for genuinely-ownerless cases). Composite certs (tags 10/12/13) carry both a B4-owned mutation and an owner-tagged governance effect; both are represented. No certificate is flattened to neutral, decode-dropped, or apply-swallowed; a decode or apply error propagates as a structured `LedgerError` and halts the block transition. Incomplete or best-effort accumulation is a forbidden fail-open. (Wiring the owner-tagged `ConwayGovState` effects into applied governance state is PHASE4-B5, not B4.) |
| **Code** | `crates/ade_types/src/conway/cert.rs` (owner-complete `ConwayCert` — every variant retains its owner payloads); `crates/ade_types/src/shelley/cert.rs` (`PoolRegistrationCert.owners`); `crates/ade_codec/src/conway/cert.rs` (`decode_conway_certs` payload retention + `decode_drep`, total over tags 0..18); `crates/ade_codec/src/shelley/cert.rs` (shared `read_pool_registration_cert`) — PHASE4-B4-S1 decoder-completeness clause; `crates/ade_ledger/src/delegation.rs` (`apply_conway_cert` + the owner-tagged `ConwayCertAction` / `ConwayCertOutcome` apply model, total over the 18 tags) — PHASE4-B4-S2 apply-totality clause; `crates/ade_ledger/src/rules.rs` (`accumulate_tx_certs`, era-dispatched fail-closed — the Conway path runs the Conway decoder + apply model, the Shelley path the Shelley decoder; a decode or apply `Err` propagates and halts, never swallowed) — PHASE4-B4-S3/S4. |
| **Tests** | `crates/ade_codec/tests/conway_cert_decode_complete.rs::{each_tag_retains_owner_payloads, drep_grammar_total}` (decoder completeness — every tag retains its owner payloads, DRep grammar total); `crates/ade_ledger/src/delegation.rs::tests::{conway_cert_action_total, apply_outcome_agrees_with_action, removed_tag_rejects_as_era_invalid, drep_registration_is_owner_tagged_not_applied}` (apply model total + owner-tagged routing); `crates/ade_ledger/src/rules.rs::tests::{era_dispatch_conway_accumulates_via_conway_path, era_dispatch_shelley_accumulates_via_shelley_path, conway_decode_error_is_fail_closed, conway_unknown_tag_is_fail_closed, conway_removed_tag_is_fail_closed, conway_apply_error_is_fail_closed, conway_governance_cert_routed_out_of_scope}` (era dispatch + fail-closed propagation + governance routing); `crates/ade_ledger/tests/cert_state_corpus.rs::{positive_synthetic_cert_state_accumulates, cert_state_replay_byte_identical, adversarial_no_false_accept}` (synthetic positive accumulation, byte-identical replay, adversarial no-false-accept — the B4 cert-state corpus, distinct from the same-named conservation-adversarial test). |
| **CI** | `ci/ci_check_forbidden_patterns.sh` — extended in PHASE4-B4-S3/S4 to guard the cert-state fail-open swallow: it FAILs CI if the `"non-fatal during replay"` rationale string reappears anywhere under `crates/`, and if `accumulate_tx_certs` in `crates/ade_ledger/src/rules.rs` contains an `Err(_)` swallow arm. Gate PASSES at HEAD `ee35493` (verified during this refresh, exit 0). _(Enforcement gap, surfaced honestly — see §A0000: the real epoch-576 cert-state-vs-cardano-node oracle is **environment-blocked** (the epoch-576 UMap snapshot is absent post-extraction; see `corpus/cert_state/README.md`). Reclassified environment-blocked per tier doctrine — identical to the documented open obligation on `DC-TXV-06` / `DC-TXV-03`. Mechanical closure IS shipped — owner-complete decode + total owner-tagged apply + era-dispatched fail-closed accumulation + synthetic positive / replay / adversarial corpus; real-chain agreement remains the open obligation.)_ |

### `DC-DIFF-01`: First-divergence localization

| Aspect | Location |
|--------|----------|
| **Source** | Project constitution §3; refines `T-DET-01`, `DC-REF-01`. |
| **Requirement** | Differential harness must localize first divergence between Ade and reference oracle. |
| **Code** | `crates/ade_testkit/src/harness/ledger_diff.rs`. |
| **Tests** | `crates/ade_testkit/...::diff_ledger_sequence`, `mismatched_block_count_returns_error`. |
| **CI** | _(no CI enforcement — gap; status = `partial`)_ |

### `DC-MEM-01`: Mempool rules subset of block/ledger rules

| Aspect | Location |
|--------|----------|
| **Source** | Project constitution §3; refines `T-DET-01`, `T-CORE-01`. Flipped `declared -> enforced` by PHASE4-B2. |
| **Requirement** | Mempool acceptance rules must not contradict block/ledger acceptance rules — a tx admitted to the mempool is exactly a tx that `tx_validity` deems `Valid` against the accumulating view; admission never accepts what block/ledger validity would reject. |
| **Code** | `crates/ade_ledger/src/mempool/admit.rs` (admission delegates to `tx_validity` against the accumulating `MempoolState`; closed `AdmitOutcome`). |
| **Tests** | `crates/ade_ledger/tests/mempool_admission.rs::{valid_tx_admitted_and_accumulates, invalid_tx_rejected_no_false_accept, admission_equals_tx_validity_verdict, dependent_tx_admitted_against_accumulating_state}`. |
| **CI** | `ci/ci_check_consensus_closed_enums.sh` (extended in B2 to scan `ade_ledger/src/mempool/` — `AdmitOutcome` must stay a closed structured value). |

### `DC-MEM-02`: Deterministic overload shedding

| Aspect | Location |
|--------|----------|
| **Source** | Project constitution §3; refines `T-DET-01`, `T-CORE-02`. Flipped `declared -> enforced` by PHASE4-B2. |
| **Requirement** | Overload shedding follows a deterministic ordering policy, not timing-dependent collapse — the policy may decide *which* txs are kept under pressure but may never change a tx's `Valid`/`Invalid` verdict (Tier-5 policy independence). |
| **Code** | `crates/ade_ledger/src/mempool/policy.rs` (closed `OrderPolicy`; deterministic ordering independent of the validity verdict). |
| **Tests** | `crates/ade_ledger/tests/mempool_admission.rs::{policy_does_not_change_validity, determinism}`. |
| **CI** | `ci/ci_check_consensus_closed_enums.sh` (`OrderPolicy` must stay a closed structured value). |

### `DC-CORE-01`: BLUE crates are sync-only (no async); BLUE consensus ⇎ ChainDb

| Aspect | Location |
|--------|----------|
| **Source** | Project constitution §2, `T-CORE-02`; PHASE4-N-A scope decisions §Decision 2 (`docs/active/PHASE4-N-A_scope_decisions.md`); strengthened by PHASE4-N-B over `ade_core::consensus` + `ade_runtime::consensus`. |
| **Requirement** | BLUE authoritative crates are sync-only: no `async fn`, `.await`, `tokio::`, `async_std::`, `Future`, `futures::`, task spawning, async channels, or timers. Async runtime concerns are confined to RED transport/runtime code. PHASE4-N-B narrows further: BLUE consensus (`ade_core::consensus`) may not reference `ChainDb`/`chain_db` — those belong to RED `ade_runtime::chaindb`. |
| **Code** | `crates/ade_runtime/src/consensus/chain_selector.rs` (GREEN orchestrator that bridges BLUE consensus transitions to RED N-A network inputs; never receives sockets or tokio types); `crates/ade_core_interop/src/lib.rs` + `crates/ade_core_interop/src/bin/live_consensus_session.rs` (RED live-peer driver crate; depends on ade_core only via typed Orchestrator surface). |
| **Tests** | `consensus::chain_selector::tests::{header_arrival_updates_state_and_selector, rollback_walks_back_via_recent_snapshots, rollback_to_block_older_than_snapshots_rejected, epoch_boundary_emits_no_event}`; `crates/ade_core_interop/tests/live_consensus_session.rs::cardano_node_session_sustained_window` (`#[ignore]` by default). |
| **CI** | `ci/ci_check_no_async_in_blue.sh` — bans `async fn`/`.await`/`tokio::`/`async_std::`/`Future`/`futures::`/spawn/timers anywhere in BLUE; scans all 6 BLUE crates plus the 9 `ade_network` submodule paths and `crates/ade_core/`. `ci/ci_check_no_chaindb_in_consensus_blue.sh` (PHASE4-N-B) — bans `ChainDb`/`chain_db` tokens anywhere under `crates/ade_core/src/consensus/`. Together they discharge the BLUE/RED partition for consensus. |

### `DC-PROTO-06`: BLUE protocol transitions are pure functions of explicit version

| Aspect | Location |
|--------|----------|
| **Source** | Project constitution §2; PHASE4-N-A invariants §7 decision 1 (`docs/active/PHASE4-N-A_invariants.md`). |
| **Requirement** | BLUE mini-protocol transitions are pure functions of (canonical prior state, canonical input message, selected protocol version, deterministic configuration); no ambient session-glue state may alter authoritative behavior. Selected version is an explicit input, never read from RED context. |
| **Code** | Per-protocol pure transition modules under `crates/ade_network/src/{chain_sync,block_fetch,tx_submission,keep_alive,peer_sharing}/{state,agency,signal,event,transition}.rs` plus the four N2C state machines under `crates/ade_network/src/n2c/{local_chain_sync,local_tx_submission,local_state_query,local_tx_monitor}/{state,agency,event,transition}.rs`. All transition signatures take selected protocol version as an explicit typed parameter. |
| **Tests** | Same large per-protocol transition battery as `DC-PROTO-01` — the `version_gating_rejects_out_of_version_message` test on every state machine is the explicit DC-PROTO-06 evidence anchor, plus the per-protocol `wrong_agency_returns_error` tests and the event-trace corpora. |
| **CI** | `ci/ci_check_ce_n_a_5_proof.sh` (CE-N-A-5 condition 2 — version-gating rejection across all 11 protocols). |

### `DC-CONS-03`: Praos chain selection ordering (no density)

| Aspect | Location |
|--------|----------|
| **Source** | Project constitution §3; ouroboros-consensus (pinned reference: cardano-node 10.6.2 → ouroboros-consensus ~0.22.x); cross-refs `DC-CONSENSUS-01`, `CN-CONS-01`. |
| **Requirement** | Praos chain selection ordering: block number first, then Praos `TiebreakerView` (slot, issuer, op-cert issue number, VRF output). Density-based ordering is reserved for Genesis/catch-up and must not be used for caught-up Praos fork-choice. |
| **Code** | `crates/ade_core/src/consensus/fork_choice.rs::select_best_chain`; `crates/ade_core/src/consensus/candidate.rs` (closed `CandidateFragment` + `TiebreakerView`); `crates/ade_runtime/src/consensus/candidate_fragment.rs` (GREEN candidate-fragment builder). |
| **Tests** | `consensus::fork_choice::tests::{tiebreaker_prefer_lower_slot_wins, tiebreaker_prefer_higher_op_cert_wins_on_equal_slot_and_issuer, tiebreaker_prefer_lower_vrf_value_wins_on_full_tie, no_candidates_returns_no_candidates_error, equal_to_current_keeps_current_via_tiebreaker_loss}`; `consensus::candidate::tests::{tiebreaker_view_eq_is_field_wise, candidate_fragment_carries_anchor_block_no}`; corpus tests `crates/ade_core/tests/fork_choice_corpus.rs::{higher_block_no_wins, equal_block_no_tiebreaker_decides, fork_before_immutable_tip_rejected, exceeded_rollback_rejected, tiebreaker_loss_keeps_current, replay_is_deterministic, reject_reason_bytes_are_stable, no_candidates_returns_error}` (CE-N-B-1). |
| **CI** | `ci/ci_check_no_density_in_fork_choice.sh` (bans any case-insensitive `density` reference in `fork_choice.rs` / `candidate.rs` except a `// no-density:` audit-marker comment); `ci/ci_check_no_chaindb_in_consensus_blue.sh`; `ci/ci_check_no_float_in_consensus.sh`; `ci/ci_check_consensus_closed_enums.sh`. |

### `DC-CONS-04`: Praos chain-dep state authority (consensus-owned, not ledger)

| Aspect | Location |
|--------|----------|
| **Source** | Project constitution §3; ouroboros-consensus `PraosChainDepState`; cross-refs `DC-CONSENSUS-02`, `DC-CONS-10`. |
| **Requirement** | Praos chain-dep state (`evolving`/`candidate`/`epoch`/`previous_epoch`/`lab`/`last_epoch_block` nonces, op-cert counters, `last_slot`) is owned by N-B consensus, not by the ledger, and evolves deterministically as a function of validated headers and epoch boundaries. |
| **Code** | `crates/ade_core/src/consensus/praos_state.rs` (`PraosChainDepState` + op-cert counter `BTreeMap`); `crates/ade_core/src/consensus/events.rs`; `crates/ade_core/src/consensus/errors.rs`; `crates/ade_core/src/consensus/encoding.rs` (canonical state encoding); `crates/ade_core/src/consensus/nonce.rs` (`apply_nonce_input` + epoch-boundary rotation); `crates/ade_core/src/consensus/op_cert.rs`; `crates/ade_core/src/consensus/header_validate.rs`; `crates/ade_core/src/consensus/header_summary.rs`. |
| **Tests** | `consensus::praos_state::tests::{op_cert_upsert_rejects_regression, op_cert_upsert_rejects_equal_counter, op_cert_upsert_accepts_strictly_increasing, genesis_state_is_well_formed, nonce_zero_constant_is_zero_bytes}`; `consensus::encoding::tests::op_cert_counter_map_iteration_is_deterministic`; canonical-roundtrip battery `crates/ade_core/tests/praos_state_canonical_roundtrip.rs::{layout_is_stable, roundtrip_empty_state, roundtrip_genesis_state, roundtrip_populated_state, roundtrip_chain_event_all_variants, roundtrip_chain_selection_reject_all_variants, decode_rejects_unknown_discriminant, decode_rejects_short_array, op_cert_counter_map_iteration_is_deterministic}`; nonce evolution `consensus::nonce::tests::*` (11 unit tests) + `crates/ade_core/tests/nonce_evolution_corpus.rs::{within_epoch_evolving_nonce_matches_corpus, epoch_boundary_freezes_and_rotates_correctly, nonce_evolution_replay_is_deterministic}`; op-cert `consensus::op_cert::tests::*` (8 unit tests) + `crates/ade_core/tests/op_cert_counter_corpus.rs::{normal_progression_records_highest_counter_per_window, regression_after_progression_rejected_with_typed_error, op_cert_replay_is_deterministic}`; header-validate `consensus::header_validate::tests::{pipeline_short_circuits_on_first_failure, nonce_contribution_uses_nonce_role_vrf_output_not_leader_role}` + `crates/ade_core/tests/header_validate_compose.rs::{valid_header_accepted_advances_state, header_with_slot_regression_rejected, header_with_block_no_regression_rejected, header_with_op_cert_regression_rejected, header_with_invalid_vrf_proof_rejected, header_beyond_forecast_horizon_rejected, validate_replay_is_deterministic}`. |
| **CI** | `ci/ci_check_consensus_closed_enums.sh` (no `#[non_exhaustive]`, no `Other`/`Unknown` open-tail variants, no owned `String` or `Box<dyn>` in `errors.rs`/`encoding.rs`/`events.rs`). |

### `DC-CONS-05`: Rollback bounded by k blocks

| Aspect | Location |
|--------|----------|
| **Source** | Project constitution §3; ouroboros-consensus consensus report; cross-refs `DC-CONS-06`, `CN-CONS-01`. |
| **Requirement** | Authoritative rollback must never exceed the security parameter k measured in blocks (mainnet k = 2160). Rollback requests deeper than k return `ExceededRollback`. Forecast/stability windows may be slot-based but must accommodate at least k+1 blocks. |
| **Code** | `crates/ade_core/src/consensus/rollback.rs::apply_rollback`; `crates/ade_core/src/consensus/events.rs` (`ChainEvent::RollBack`, `ChainSelectionReject::ExceededRollback` / `ForkBeforeImmutableTip`). |
| **Tests** | Unit-level `consensus::rollback::tests::{rollback_preserves_immutable_tip, rollback_preserves_security_param, rollback_with_zero_depth_is_noop, rollback_to_equal_block_no_as_immutable_succeeds, rollback_to_one_below_immutable_rejected}`; corpus tests `crates/ade_core/tests/rollback_corpus.rs::{rollback_within_k_succeeds, rollback_exceeding_k_rejected_with_typed_reason, rollback_before_immutable_tip_rejected, rollback_event_bytes_are_stable, rollback_is_deterministic, rollback_equivalent_to_truncated_replay}` (CE-N-B-2). |
| **CI** | `ci/ci_check_consensus_closed_enums.sh`, `ci/ci_check_no_chaindb_in_consensus_blue.sh`. |

### `DC-CONS-06`: Rollback = truncated replay; immutable tip final

| Aspect | Location |
|--------|----------|
| **Source** | Project constitution §3; `T-DET-01`; cross-refs `DC-CONS-05`, `T-DET-01`. |
| **Requirement** | `rollback(state, depth)` produces state byte-identical to truncated replay from the nearest checkpoint. Rollback that would cross the immutable tip (≥ k deep) returns `ForkBeforeImmutableTip` and never alters state. |
| **Code** | `crates/ade_core/src/consensus/rollback.rs`. |
| **Tests** | `crates/ade_core/tests/rollback_corpus.rs::{rollback_equivalent_to_truncated_replay, rollback_is_deterministic, rollback_within_k_succeeds, rollback_before_immutable_tip_rejected, rollback_event_bytes_are_stable}`; `consensus::rollback::tests::{rollback_to_one_below_immutable_rejected, rollback_preserves_immutable_tip}`. |
| **CI** | `ci/ci_check_consensus_closed_enums.sh`. |

### `DC-CONS-07`: EraSchedule is the canonical HFC value

| Aspect | Location |
|--------|----------|
| **Source** | Project constitution §3; cross-refs `DC-CORE-01`, `DC-EPOCH-02`. |
| **Requirement** | BLUE consensus must consume the HFC schedule only as a typed `EraSchedule` value anchored to `BootstrapAnchorHash`. Genesis text parsing happens in RED; BLUE never reads files, JSON, or operator config directly. The schedule is part of replay evidence. |
| **Code** | `crates/ade_core/src/consensus/era_schedule.rs` (BLUE typed value); `crates/ade_runtime/src/consensus/genesis_parser.rs` (RED parser, deterministic anchor-hash computation, never reached by BLUE). |
| **Tests** | Constructor validation: `consensus::era_schedule::tests::{eraschedule_constructor_rejects_empty, eraschedule_constructor_rejects_non_monotonic, eraschedule_constructor_rejects_zero_slot_length, eraschedule_constructor_rejects_zero_epoch_length}`. Anchor-hash determinism: `consensus::genesis_parser::tests::{anchor_hash_deterministic, anchor_hash_distinguishes_inputs}`. Corpus oracle agreement (CE-N-B-3): `crates/ade_core/tests/hfc_schedule_corpus.rs::{mainnet_corpus_translation_matches_oracle, preprod_corpus_translation_matches_oracle, bootstrap_anchor_hash_distinguishes_genesis_variants}`; parser-against-corpus `crates/ade_runtime/tests/genesis_parser_corpus.rs::{mainnet_parser_eras_match_corpus_oracle, preprod_parser_eras_match_corpus_oracle, parser_anchor_is_deterministic}`. |
| **CI** | `ci/ci_check_no_chaindb_in_consensus_blue.sh` (anchors the FC/IS boundary — BLUE consensus never imports ChainDb / chain_db). |

### `DC-CONS-08`: slot→time pure of wall-clock

| Aspect | Location |
|--------|----------|
| **Source** | Project constitution §3; cross-refs `CN-CONS-05`, `DC-CORE-01`. |
| **Requirement** | `slot_to_time(EraSchedule, SystemStart, SlotNo)` is a pure function; no BLUE consensus path may consult the wall clock to derive a slot or UTC instant for an authoritative decision. |
| **Code** | `crates/ade_core/src/consensus/era_schedule.rs::slot_to_time_ms`. |
| **Tests** | `consensus::era_schedule::tests::{slot_to_time_monotone_increasing, slot_to_time_overflow_returns_structured_error, determinism_across_runs, locate_first_slot_of_each_era, locate_last_slot_of_each_era, locate_before_system_start_errors}`; `crates/ade_core/tests/hfc_schedule_corpus.rs::{mainnet_corpus_translation_matches_oracle, preprod_corpus_translation_matches_oracle}`. |
| **CI** | `ci/ci_check_no_float_in_consensus.sh` (the slot→time arithmetic must be integer-only); `ci/ci_check_forbidden_patterns.sh` (bans `SystemTime`/`Instant` in BLUE). |

### `DC-CONS-09`: Forecast horizon fail-fast

| Aspect | Location |
|--------|----------|
| **Source** | Project constitution §3; cross-refs `DC-EPOCH-02`, `DC-CONS-07`. |
| **Requirement** | Consensus-derived queries for slots beyond the ledger-view safe zone return `OutsideForecastRange`, never guessed values. The bound is derived from era history + safe zone + HFC schedule, not encoded as a magic constant in caller code. |
| **Code** | `crates/ade_core/src/consensus/era_schedule.rs`; `crates/ade_core/src/consensus/leader_schedule.rs::query_leader_schedule`. |
| **Tests** | `consensus::era_schedule::tests::forecast_horizon_boundary`; `consensus::leader_schedule::tests::query_returns_outside_forecast_range_for_far_future`; `crates/ade_core/tests/hfc_schedule_corpus.rs::{mainnet_corpus_translation_matches_oracle, preprod_corpus_translation_matches_oracle}`; `crates/ade_core/tests/leader_schedule_corpus.rs::corpus_rejects_out_of_forecast_horizon`. |
| **CI** | _(no dedicated CI script — gap; status = `enforced` via test surface, but no text-scan gate. Discharged structurally via the closed-enum gate `ci_check_consensus_closed_enums.sh` and the BLUE consensus tree-wide scans.)_ |

### `DC-CONS-10`: Op-cert counter monotonicity

| Aspect | Location |
|--------|----------|
| **Source** | Project constitution §3; ouroboros-consensus `OperationalCertificate`; cross-refs `DC-CONS-04`. |
| **Requirement** | A header's op-cert issue counter must be ≥ the highest observed counter for the same (pool, kes_period). Regression yields `HeaderInvalid` with a typed `OpCertCounterError` reason; `ChainDepState` never accepts a regression. |
| **Code** | `crates/ade_core/src/consensus/op_cert.rs::apply_op_cert`; `crates/ade_core/src/consensus/praos_state.rs` (op-cert counter `BTreeMap`); `crates/ade_core/src/consensus/errors.rs` (`OpCertCounterError`); `crates/ade_core/src/consensus/header_validate.rs`. |
| **Tests** | `consensus::op_cert::tests::{apply_op_cert_inserts_first_observation, apply_op_cert_advances_existing_strictly, apply_op_cert_rejects_equal_counter, apply_op_cert_rejects_lower_counter, apply_op_cert_independent_kes_periods_dont_collide, apply_op_cert_independent_pools_dont_collide, apply_op_cert_does_not_touch_nonces, apply_op_cert_does_not_touch_last_slot_or_block_no}`; `consensus::praos_state::tests::{op_cert_upsert_rejects_regression, op_cert_upsert_rejects_equal_counter, op_cert_upsert_accepts_strictly_increasing}`; corpus tests `crates/ade_core/tests/op_cert_counter_corpus.rs::{normal_progression_records_highest_counter_per_window, regression_after_progression_rejected_with_typed_error, op_cert_replay_is_deterministic}`; header-validate `crates/ade_core/tests/header_validate_compose.rs::{header_with_op_cert_regression_rejected, valid_header_accepted_advances_state}`. |
| **CI** | _(no dedicated CI script — gap; status = `enforced` via test surface. Discharged structurally via `ci_check_consensus_closed_enums.sh` ensuring the `OpCertCounterError` variant stays closed and `ci_check_no_chaindb_in_consensus_blue.sh` enforcing the FC/IS boundary.)_ |


### `DC-VAL-01`: Validity verdict is a pure function of canonical inputs

| Aspect | Location |
|--------|----------|
| **Source** | Cardano ledger spec; Ouroboros Praos spec; IDD determinism doctrine (cross-refs `DC-CORE-01`, `T-DET-01`, `DC-CONSENSUS-01`). |
| **Requirement** | A block's validity verdict is a pure function of `(LedgerState, PraosChainDepState, EraSchedule, LedgerView, block_cbor)`. No wall-clock, arrival order, `HashMap`/`HashSet` iteration, float, or ambient state may influence it. |
| **Code** | `crates/ade_ledger/src/consensus_input_extract.rs` (canonical nonce/field projection), `crates/ade_ledger/src/consensus_view.rs` (pure ledger-side `LedgerView` impl), `crates/ade_core/src/consensus/ledger_view.rs` (`LedgerView` trait — extractive forecast surface). |
| **Tests** | `crates/ade_ledger/src/consensus_input_extract.rs::tests::{extract_nonces_field_order, extract_nonces_requires_exactly_five, extract_nonces_is_deterministic}`; `crates/ade_ledger/tests/consensus_view.rs::{view_returns_corpus_pool_stake_and_vrf_keyhash, view_unknown_pool_returns_none, view_unknown_epoch_returns_none, view_is_pure}`; corpus self-consistency `corpus_loads_and_is_self_consistent`. |
| **CI** | _(no dedicated CI script — gap; status = `enforced` via test surface only. Discharged structurally over the BLUE tree by `ci/ci_check_forbidden_patterns.sh` (no `HashMap`/`HashSet`/float/wall-clock in BLUE) and, for the consensus inputs, `ci/ci_check_no_float_in_consensus.sh`. No `ci/ci_check_block_validity.sh` exists — see §A0 / §G.)_ |

### `DC-VAL-02`: Valid iff header AND body authority accept

| Aspect | Location |
|--------|----------|
| **Source** | Cardano ledger spec; Ouroboros Praos spec (cross-refs `DC-LEDGER-02`, `CN-CONS-04`, `DC-CONS-04`). |
| **Requirement** | A block is `Valid` iff both the consensus header authority (`validate_and_apply_header`) and the ledger body authority (`apply_block_with_verdicts`) accept it. No path may produce a `Valid` verdict while skipping either authority. |
| **Code** | `crates/ade_ledger/src/block_validity/` (closed verdict/error taxonomy substrate); `crates/ade_ledger/src/block_validity/transition.rs` + `crates/ade_ledger/src/block_validity/header_input.rs` (header ∧ body composition — header authority decided, then body authority applied). |
| **Tests** | `crates/ade_ledger/tests/block_validity_compose.rs::{valid_block_evolves_both_states, header_before_body_fail_fast}`. |
| **CI** | _(no dedicated CI script — gap; status = `enforced` via test surface only. Discharged structurally by `ci/ci_check_consensus_closed_enums.sh` keeping the verdict/error taxonomy closed. See §A0 / §G.)_ |

### `DC-VAL-03`: Header-before-body fail-fast ordering

| Aspect | Location |
|--------|----------|
| **Source** | Ouroboros Praos spec; IDD fail-fast doctrine (cross-refs `DC-VAL-02`). |
| **Requirement** | The header is validated before the body; body validation never runs on a header-invalid block. The first failing authority determines the reason. |
| **Code** | `crates/ade_ledger/src/block_validity/transition.rs` (header authority decided before body; body authority unreachable on header failure). |
| **Tests** | `crates/ade_ledger/tests/block_validity_compose.rs::header_before_body_fail_fast`. |
| **CI** | _(no dedicated CI script — gap; status = `enforced` via test surface only. See §A0 / §G.)_ |

### `DC-VAL-04`: Verdict agrees with reference (positive + adversarial)

| Aspect | Location |
|--------|----------|
| **Source** | cardano-node reference behavior; Cardano ledger spec (cross-refs `DC-LEDGER-02`, `CN-LEDGER-02`). |
| **Requirement** | Ade's `Valid`/`Invalid` verdict for a block equals the reference cardano-node verdict, including the reason class where the reference exposes it — established over both a positive corpus (real valid blocks) and a mandatory adversarial corpus (blocks the reference rejects). |
| **Code** | `crates/ade_ledger/src/block_validity/` (closed `Valid`/`Invalid` + reject-class comparison surface); `crates/ade_testkit/src/validity/replay.rs` (GREEN positive-corpus replay over all 14 Conway-576 blocks); `crates/ade_testkit/src/validity/adversarial.rs` (GREEN deterministic mutators M1–M6 deriving adversarial blocks from the real corpus). |
| **Tests** | `crates/ade_ledger/tests/block_validity_positive_corpus.rs::{corpus_block_count_is_14, all_corpus_blocks_valid, verdict_stream_replays_identically}`; `crates/ade_ledger/tests/block_validity_adversarial_corpus.rs::{no_mutation_is_ever_valid, each_mutation_maps_to_expected_class, adversarial_replays_identically}`. |
| **CI** | _(no dedicated CI script — gap; status = `enforced` via positive + adversarial corpus test surface. The CE-B1-4 no-false-accept property (`no_mutation_is_ever_valid`) and the 14/14 positive-corpus property (`all_corpus_blocks_valid`) have no CI gate. See §A0 / §G.)_ |

### `DC-VAL-05`: Total state evolution (invalid → unchanged)

| Aspect | Location |
|--------|----------|
| **Source** | IDD explicit-total-transition doctrine; Cardano ledger spec (cross-refs `DC-LEDGER-02`). |
| **Requirement** | A `Valid` block yields evolved `(LedgerState', PraosChainDepState')`; an `Invalid` block yields the unchanged input states plus a structured reason. No partial or in-place mutation occurs on the invalid path. |
| **Code** | `crates/ade_ledger/src/block_validity/` (closed verdict taxonomy: `Valid` carries evolved-state stats, `Invalid` carries a structured reason); `crates/ade_ledger/src/block_validity/transition.rs` (`Valid` returns evolved `(ledger', chain_dep')`; `Invalid` returns input clones). |
| **Tests** | `crates/ade_ledger/tests/block_validity_compose.rs::{invalid_block_leaves_state_unchanged, valid_block_evolves_both_states}`. |
| **CI** | _(no dedicated CI script — gap; status = `enforced` via test surface only. Discharged structurally by `ci/ci_check_consensus_closed_enums.sh` (closed verdict taxonomy). See §A0 / §G.)_ |

### `DC-VAL-06`: Fail-closed field checks (no silent skip)

| Aspect | Location |
|--------|----------|
| **Source** | Cardano ledger spec (mandatory witness/field checks); IDD fail-fast doctrine (cross-refs `DC-LEDGER-02`, `T-ENC-01`, `DC-CRYPTO-01`, `DC-TXV-06`). Strengthened by PHASE4-B1 (block fail-closed field checks), PHASE4-B2 (tx witness/input fail-closed checks), PHASE4-B3 (cert/withdrawal conservation early-out removed), PHASE4-B3F (decoder strictness), and PHASE4-B4 (cert-state silent-skip removal). |
| **Requirement** | Every crypto-input, field-size, and structural check on the authority path rejects (produces `Invalid`) on wrong size or shape and never silently skips. The pattern `if X.len() == K { check } else { skip }` is forbidden in BLUE validation; size checks go through a helper that returns an error. No defined-but-unwired check and no tautological (value-compared-to-itself) guard may stand in for a real check. |
| **Code** | `crates/ade_ledger/src/block_validity/verdict.rs` (`FieldKind`/`FieldError` closed fail-closed field taxonomy); `crates/ade_core/src/consensus/kes_check.rs` (`expect_size` fail-closed header crypto-field guard); `crates/ade_testkit/src/validity/adversarial.rs` (M1 truncated VRF proof, M3 flipped KES sig, M4 slot-beyond-horizon mutators); plus PHASE4-B2 tx-path fail-closed checks — `crates/ade_ledger/src/tx_validity/witness.rs` (wrong-size vkey/sig -> `MalformedWitnessField` via `from_bytes`, never skipped) and `crates/ade_ledger/src/tx_validity/required_signers.rs` (unresolvable input / collateral input is fail-fast, never a silent skip); plus PHASE4-B3 — `crates/ade_ledger/src/conway.rs::check_conway_coin_conservation` no longer carries the cert/withdrawal early-out (the B2 `if certs.is_some() || withdrawals.is_some() { skip }` fail-open is **removed**), so a cert/withdrawal-bearing imbalanced tx is now rejected on value grounds rather than silently skipped; the §9.1 reject-precedence ordering is deterministic; plus PHASE4-B3F — `crates/ade_codec/src/conway/cert.rs::decode_conway_certs` now rejects trailing bytes after the cert array (`CodecError::TrailingBytes`) and bounds preallocation against a hostile array count, closing two fail-open decoder paths; plus PHASE4-B4 — `crates/ade_ledger/src/rules.rs::accumulate_tx_certs` no longer swallows a cert-state decode/apply `Err` (the prior "non-fatal during replay" silent-skip is **removed**), so a Conway cert that fails to decode or apply now propagates a structured `LedgerError` and halts the block transition rather than being silently skipped (see `DC-LEDGER-08`). |
| **Tests** | `crates/ade_core/src/consensus/kes_check.rs::tests::expect_size_rejects_wrong_length`; `crates/ade_core/tests/praos_header_validate.rs::praos_malformed_kes_sig_rejected`; `crates/ade_ledger/tests/block_validity_adversarial_corpus.rs::{no_mutation_is_ever_valid, each_mutation_maps_to_expected_class}`; plus PHASE4-B2 `crates/ade_ledger/tests/tx_witness_closure.rs::{wrong_size_signature_rejected, wrong_size_vkey_rejected, unresolvable_input_is_fail_fast, unresolvable_collateral_input_is_fail_fast}`; plus PHASE4-B3 `crates/ade_ledger/tests/conway_conservation_full.rs::{conway_conservation_full, conservation_early_out_removed, reject_reason_precedence_is_deterministic, state_dependent_unaccountable_rejects}` and the adversarial suite `crates/ade_ledger/tests/conway_conservation_adversarial.rs::{adversarial_imbalanced_via_deposit, adversarial_imbalanced_via_withdrawal, adversarial_unknown_cert_tag_rejects_as_decode, adversarial_removed_tag_rejects_as_era_invalid, adversarial_truncated_withdrawals_rejects_as_decode, adversarial_state_dependent_unaccountable_rejects, adversarial_no_false_accept, adversarial_verdicts_replay_byte_identical}`; plus PHASE4-B3F decoder-strictness `crates/ade_codec/tests/conway_cert_classification.rs::{trailing_bytes_after_cert_array_rejected, huge_array_count_rejects_without_overallocating}` (the cert-array decoder rejects trailing bytes and bounds preallocation, never silently truncating or over-allocating on a hostile count). |
| **CI** | `ci/ci_check_consensus_closed_enums.sh` (keeps the block-validity AND tx-validity reject/field/signer taxonomies closed over `ade_ledger/src/block_validity/` and `ade_ledger/src/tx_validity/`). `strengthened_in += ["PHASE4-B1", "PHASE4-B2", "PHASE4-B3", "PHASE4-B3F", "PHASE4-B4"]`. PHASE4-B4 also wires `ci/ci_check_forbidden_patterns.sh` as a complementary fail-open guard (it FAILs if the cert-state `"non-fatal during replay"` swallow rationale or an `Err(_)` swallow arm in `accumulate_tx_certs` reappears — see `DC-LEDGER-08`). The general fail-open-pattern ban (`if X.len()==K { } else { skip }`) is still **not** mechanically text-scanned — proven only by the adversarial corpora plus the B3 `conservation_early_out_removed` regression test. See §G — strong candidate for a new `ci/ci_check_no_fail_open_len_guard.sh`. |

---

### `DC-TXV-01`: Pure tx_validity

| Aspect | Location |
|--------|----------|
| **Source** | Cardano ledger spec; IDD determinism doctrine (cross-refs `T-DET-01`). PHASE4-B2 new rule. |
| **Requirement** | `tx_validity` is a pure function of `(LedgerState, tx_cbor)`. No wall-clock, arrival order, `HashMap`/`HashSet` iteration, float, or ambient state may influence a transaction's `Valid`/`Invalid` verdict. |
| **Code** | `crates/ade_ledger/src/tx_validity/transition.rs` (`tx_validity`: pure composition over `(&LedgerState, &[u8])`); `crates/ade_ledger/src/tx_validity/phase1.rs` (`tx_phase_one` + `decode_tx`, no I/O / clock / rand). |
| **Tests** | `crates/ade_ledger/tests/tx_validity_compose.rs::tx_validity_is_deterministic`. |
| **CI** | `ci/ci_check_consensus_closed_enums.sh` (closed verdict taxonomy over `ade_ledger/src/tx_validity/`); discharged structurally over the BLUE tree by `ci/ci_check_forbidden_patterns.sh` (no `HashMap`/`HashSet`/float/wall-clock in BLUE). |

### `DC-TXV-02`: Phase-1 AND phase-2

| Aspect | Location |
|--------|----------|
| **Source** | Cardano ledger spec (UTXOW/UTXO/PPUP, Plutus phase-2). PHASE4-B2 new rule. |
| **Requirement** | A transaction is `Valid` iff both phase-1 (structural + UTxO rules + witnesses) and phase-2 (Plutus, when scripts are present) accept it. No path may produce a `Valid` verdict while skipping either phase. |
| **Code** | `crates/ade_ledger/src/tx_validity/transition.rs` (`tx_validity`: fail-fast — phase-1 decided first; phase-2 dispatch via `plutus_eval::try_evaluate_tx` never runs when phase-1 fails or when no Plutus scripts are present); `crates/ade_ledger/src/tx_validity/phase1.rs` (`tx_phase_one` composes the B2-S1 witness closure + `validate_conway_state_backed`). |
| **Tests** | `crates/ade_ledger/tests/tx_validity_compose.rs::{valid_tx_is_valid_and_applies, phase1_failure_short_circuits_phase2}`. |
| **CI** | `ci/ci_check_consensus_closed_enums.sh`. |

### `DC-TXV-03`: Verdict agreement (positive + adversarial)

| Aspect | Location |
|--------|----------|
| **Source** | cardano-node reference behavior; Cardano ledger spec. PHASE4-B2 new rule. |
| **Requirement** | Ade's `Valid`/`Invalid` verdict for a transaction equals the reference cardano-node verdict, including the reason class where the reference exposes it — established over a positive corpus (real on-chain txs) and a mandatory adversarial corpus (txs the reference rejects). A false-accept is release-blocking. |
| **Code** | `crates/ade_testkit/src/tx_validity/` (GREEN harness: extracts every on-wire Conway tx from the committed Conway-576 corpus blocks and drives the BLUE `tx_validity` over each — partial scope at `track_utxo=false`: structural + witness closure; UTxO-dependent checks deferred); `crates/ade_ledger/src/tx_validity/phase1.rs` (`tx_phase_one` gates the UTxO-dependent state-backed checks behind `track_utxo`, mirroring the block path's `verify_conway_witness_closure` + `run_phase_one_composers` split — the witness closure runs unconditionally); `crates/ade_testkit/src/tx_validity/adversarial.rs` (B2 adversarial half); plus PHASE4-B3 — the cert/withdrawal-bearing positive corpora (real + synthetic) and the adversarial conservation corpus exercise the now-full coin-conservation path. |
| **Tests** | `crates/ade_ledger/tests/tx_validity_positive_corpus.rs::{corpus_tx_count_nonzero, all_corpus_txs_valid}` (real Conway txs all `Valid`); `crates/ade_ledger/tests/tx_validity_adversarial_corpus.rs::{no_mutation_is_ever_valid, each_mutation_maps_to_expected_class, adversarial_replays_identically}` (no false accept across the B2 adversarial cases); plus PHASE4-B3 positive corpora `crates/ade_ledger/tests/conway_conservation_positive_synthetic.rs::{synthetic_balanced_cert_withdrawal_txs_are_valid, synthetic_positive_verdict_stream_replays_byte_identical}` and the cert/withdrawal-bearing real-corpus coverage in `tx_validity_positive_corpus.rs`, plus the B3 adversarial conservation corpus `crates/ade_ledger/tests/conway_conservation_adversarial.rs::{adversarial_imbalanced_via_deposit, adversarial_imbalanced_via_withdrawal, adversarial_unknown_cert_tag_rejects_as_decode, adversarial_removed_tag_rejects_as_era_invalid, adversarial_truncated_withdrawals_rejects_as_decode, adversarial_state_dependent_unaccountable_rejects, adversarial_no_false_accept, adversarial_verdicts_replay_byte_identical}`. |
| **CI** | `ci/ci_check_consensus_closed_enums.sh`. `strengthened_in += ["PHASE4-B2", "PHASE4-B3"]`. The no-false-accept property (`no_mutation_is_ever_valid`, `adversarial_no_false_accept`) and the positive-corpus property (`all_corpus_txs_valid`, `synthetic_balanced_cert_withdrawal_txs_are_valid`) have no dedicated corpus CI gate — proven by the test surface. See §G. |

### `DC-TXV-04`: Total evolution

| Aspect | Location |
|--------|----------|
| **Source** | IDD explicit-total-transition doctrine; Cardano ledger spec. PHASE4-B2 new rule. |
| **Requirement** | A `Valid` transaction yields an applied `LedgerState'` (the mempool's accumulating view); an `Invalid` transaction leaves the input state unchanged plus a structured reason. No partial or in-place mutation occurs on the invalid path. |
| **Code** | `crates/ade_ledger/src/tx_validity/transition.rs` (`tx_validity`: `Valid` -> applied state via `rules::apply_conway_tx_to_utxo`; every `Invalid` path returns `invalid()` which clones the input state unchanged); `crates/ade_ledger/src/tx_validity/verdict.rs` (closed `TxValidityVerdict`/`TxRejectClass`/`TxValidityError`; total `class()`). |
| **Tests** | `crates/ade_ledger/tests/tx_validity_compose.rs::{valid_tx_is_valid_and_applies, invalid_tx_leaves_state_unchanged, class_mapping_is_total}`. |
| **CI** | `ci/ci_check_consensus_closed_enums.sh` (`TxValidityVerdict`/`TxRejectClass`/`TxValidityError` must stay closed structured values). |

### `DC-TXV-05`: Closed era-versioned required-signer enumeration

| Aspect | Location |
|--------|----------|
| **Source** | Cardano ledger spec (UTXOW required-signers per era); IDD fail-fast + closed-surface doctrine. PHASE4-B2 new rule. |
| **Requirement** | For each era, `required_signers(state, tx_body)` is a closed, explicit, era-versioned function over every signer source (resolved input payment credentials, explicit required-signers, certificate key hashes, withdrawal key hashes, Conway governance/voting witnesses, collateral-input implications). A signer source not represented in the era's closed enumeration is impossible to silently omit; incomplete enumeration is a forbidden false-accept path. |
| **Code** | `crates/ade_ledger/src/tx_validity/required_signers.rs` (closed era-versioned `SignerSource` enum + `required_signers`/`tx_derived_required_signers` grounded in Conway `getConwayWitsVKeyNeeded` + `getVKeyWitnessConwayTxCert`); `crates/ade_ledger/src/tx_validity/witness.rs` (fail-closed coverage over the preserved body hash); `crates/ade_ledger/src/rules.rs` (`verify_conway_witness_closure` body-path wiring). |
| **Tests** | `crates/ade_ledger/tests/tx_witness_closure.rs::{all_required_covered_is_valid, missing_input_payment_witness_rejected, missing_explicit_required_signer_rejected, missing_withdrawal_witness_rejected, missing_certificate_witness_rejected, missing_governance_voter_witness_rejected, unresolvable_input_is_fail_fast, unresolvable_collateral_input_is_fail_fast, script_credential_input_not_a_vkey_signer, script_credential_certificate_not_a_vkey_signer, collateral_payment_key_is_required, non_conway_era_is_unsupported, closure_is_deterministic}` (within the 20-test `tx_witness_closure` suite covering the 6-source closure hard gates). |
| **CI** | `ci/ci_check_consensus_closed_enums.sh` (`SignerSource` / `RequiredSignerError` / `WitnessClosureError` must stay closed). |

### `DC-TXV-06`: Closed/total Conway certificate-deposit classification

| Aspect | Location |
|--------|----------|
| **Source** | Cardano ledger spec (Conway CDDL certificate tags 0..18; UTXO deposit/refund accounting); IDD fail-fast + closed-surface doctrine. PHASE4-B3 new rule; CI-enforced in PHASE4-B3F. Cross-refs `T-CONSERV-01`, `CN-LEDGER-07`, `DC-VAL-06`, `DC-TXV-05`, `DC-TXV-07`. |
| **Requirement** | For each era, the certificate-deposit classification `map(state, cert)` is a closed, total, era-versioned function: every certificate variant resolves to exactly one of `new_deposit(coin)` / `refund(coin)` / `neutral` / `explicit_reject`, with the coin sourced from the cert (Conway explicit-deposit variants) or the protocol parameter (legacy variants). An unrecognized cert tag, malformed cert CBOR, or undecodable withdrawals field is a deterministic reject, never a silent neutral. State-dependent cases that cannot be accounted (e.g. pool re-registration charged as a new deposit) reject with a structured `UnsupportedStateDependentDepositAccounting` error rather than guessing. Incomplete classification is a forbidden false-accept path feeding the value-conservation equation. |
| **Code** | `crates/ade_codec/src/conway/cert.rs` (`decode_conway_certs`, closed `ConwayCert` grammar — total decode over tags 0..18); `crates/ade_codec/src/error.rs` (`CodecError::UnknownCertTag`; `RemovedInConway` for tags 5/6); `crates/ade_types/src/conway/cert.rs` (`ConwayCert`, `CertDisposition`, `DepositEffect`, `CoinSource` — closed sum types); `crates/ade_ledger/src/cert_classify.rs` (`classify`: compiler-exhaustive `match` mapping every variant to exactly one disposition); `crates/ade_ledger/src/error.rs` (`UnsupportedStateDependentDepositAccounting`); `corpus/conway_certs/{classification_table.md,tags.json}`. |
| **Tests** | `crates/ade_codec/tests/conway_cert_classification.rs::{decode_total_over_tags_0_18, unknown_cert_tag_is_codec_error, removed_tag_5_6_is_not_valid_in_conway, malformed_cert_cbor_rejected, decode_is_replay_deterministic}`; `crates/ade_ledger/src/cert_classify.rs::tests::{class_mapping_is_total, legacy_unregistration_unresolved_is_unsupported_state_dependent, legacy_unregistration_resolves_recorded_deposit, pool_reregistration_is_neutral, pool_new_registration_charges_pool_deposit}`; plus PHASE4-B3F decoder-strictness regressions `crates/ade_codec/tests/conway_cert_classification.rs::{trailing_bytes_after_cert_array_rejected, huge_array_count_rejects_without_overallocating}`. |
| **CI** | `ci/ci_check_conway_cert_classification_closed.sh` — dedicated grep-gate landed in PHASE4-B3F that flips `status = "partial" -> "enforced"`. It fails CI on: (1) `#[non_exhaustive]` or an open-tail `Other`/`Unknown` variant on `ConwayCert` / `CertDisposition` / `DepositEffect` / `CoinSource` in `crates/ade_types/src/conway/cert.rs`; (2) a missing `CodecError::UnknownCertTag` reject or a catch-all decoder arm (single-line `_ => …ConwayCert::` or block-form `_ => { … ConwayCert:: … }`) in `crates/ade_codec/src/conway/cert.rs` (the reintroduced-Shelley-fallback anti-pattern); (3) any `_ =>` wildcard arm in `crates/ade_ledger/src/cert_classify.rs` (`classify` must stay compiler-exhaustive). Gate **PASSES** at HEAD `193d2fc` (`PASS: Conway cert classification is closed and total (DC-TXV-06)`, exit 0). The closure no longer rests on compiler-exhaustive `match` + tests alone — it is now CI-enforced. _(Residue: `crates/ade_types/src/conway/cert.rs` is still outside `ci_check_consensus_closed_enums.sh` `TARGETS`, but the dedicated gate above covers the closure, so `DC-TXV-06` is genuinely `enforced`.)_ See §A000. |

### `DC-TXV-07`: Canonical deposit-parameter authority

| Aspect | Location |
|--------|----------|
| **Source** | IDD determinism doctrine (deposit params are canonical input); Conway protocol-parameter surface. PHASE4-B3 new rule. Cross-refs `T-DET-01`, `T-CONSERV-01`, `CN-LEDGER-07`, `DC-TXV-06`. |
| **Requirement** | All deposit/refund amounts used by Conway transaction value-conservation accounting must be sourced from canonical ledger protocol parameters or explicit certificate fields, never from testkit defaults, shell configuration, ambient `ConwayGovParams`, or fallback constants. |
| **Code** | `crates/ade_ledger/src/pparams.rs` (`ConwayOnlyDepositParams`, `ConwayDepositParams` — the canonical deposit-param value); `crates/ade_ledger/src/state.rs` (`LedgerState.conway_deposit_params` + the `conway_deposit_view` boundary view, which fails fast via `ValidationEnvironmentError::MissingConwayDepositParams` when a Conway state lacks deposit params — see `crates/ade_ledger/src/error.rs`); `crates/ade_ledger/src/fingerprint.rs` (`fingerprint_pparams`, Conway-gated deposit-param fold). |
| **Tests** | `crates/ade_ledger/src/pparams.rs::tests::{conway_deposit_params_canonical_source, non_conway_state_has_no_conway_deposit_params}`; `crates/ade_ledger/src/fingerprint.rs::tests::{pparams_fingerprint_stable_for_non_conway, pparams_fingerprint_includes_conway_deposits_when_present}`. |
| **CI** | `ci/ci_check_deposit_param_authority.sh` (PHASE4-B3 — greps the 6 BLUE crates and FAILs on any non-canonical deposit-param source: testkit `ConwayGovParams` reference in a BLUE path, `ProtocolParameters::default()` feeding `drep_deposit`/`gov_action_deposit`/`key_deposit`/`pool_deposit`, an inline literal assigned to a Conway deposit field outside `conway_deposit_params`, or `env::var` feeding a deposit param). `strengthened_in += ["PHASE4-B3"]`. |

---

## CN — Classification Table Invariants (`classification_table.md`)

> Most `CN-*` rules remain `status = "declared"` in the registry at HEAD —
> three were flipped `declared → enforced` in `78da6c9` (CN-STORE-03/04/05).
> Five (`CN-CONS-01..05`) were strengthened in PHASE4-N-B by populating
> `code_locus`/`tests`/`ci_script` arrays while still carrying their
> declared/partial status. One (`CN-EPOCH-01`) was flipped
> `declared → partial`. One (`CN-CRYPTO-02`) was flipped
> `declared → partial`. The remaining classification-table entries are
> restatements of `T-*` invariants and inherit enforcement through their
> `cross_ref` targets, but the registry has not yet propagated the
> linkage. Each entry below records the canonical requirement, points
> at the cross-referenced `T-*` / `DC-*` rule where de-facto enforcement
> lives, and flags the registry gap.

### `CN-WIRE-01`: Hash-critical bytes preserved

| Aspect | Location |
|--------|----------|
| **Source** | `classification_table.md` §A. |
| **Requirement** | Hash-critical original bytes must be preserved and used on all hash/signature-critical paths. |
| **Code** | De-facto via `T-ENC-01` / `DC-CBOR-02` — `crates/ade_codec/src/preserved.rs`. _(Registry direct linkage gap.)_ |
| **Tests** | De-facto via `T-ENC-03` / `DC-CBOR-01`. |
| **CI** | `ci/ci_check_hash_uses_wire_bytes.sh`. |

### `CN-WIRE-02`: One canonical internal encoding

| Aspect | Location |
|--------|----------|
| **Source** | `classification_table.md` §A. |
| **Requirement** | Internal replay/state surfaces must use exactly one canonical project encoding. |
| **Code** | De-facto via `T-ENC-01`. _(Registry linkage gap.)_ |
| **Tests** | De-facto via `T-DET-01`. |
| **CI** | _(no CI enforcement — gap)_ |

### `CN-WIRE-03`: Deserialization equivalence across versions

| Aspect | Location |
|--------|----------|
| **Source** | `classification_table.md` §A (directly addresses Nov 2025 fork). |
| **Requirement** | Consensus-critical deserialization must be equivalent across all supported versions and code paths. |
| **Code** | _(no enforcement — gap)_ |
| **Tests** | _(no tests — gap)_ |
| **CI** | _(no CI enforcement — gap; release-tier obligation per `RO-REL-01`)_ |

### `CN-WIRE-04`: Malformed inputs rejected pre-mutation

| Aspect | Location |
|--------|----------|
| **Source** | `classification_table.md` §A. |
| **Requirement** | Malformed consensus-relevant inputs must be rejected deterministically before any authoritative state transition. |
| **Code** | De-facto via `ade_codec` decoders + `apply_block` early validation + PHASE4-N-B `validate_and_apply_header` short-circuiting. _(Registry linkage gap.)_ |
| **Tests** | _(no tests — gap)_ |
| **CI** | _(no CI enforcement — gap)_ |

### `CN-WIRE-05`: No permissive legacy parser

| Aspect | Location |
|--------|----------|
| **Source** | `classification_table.md` §A. |
| **Requirement** | No legacy or compatibility parser may accept bytes that the canonical parser rejects. |
| **Code** | _(no enforcement — gap; single-parser convention not audited)_ |
| **Tests** | _(no tests — gap)_ |
| **CI** | _(no CI enforcement — gap)_ |

### `CN-WIRE-06`: Era-aware decode chokepoints

| Aspect | Location |
|--------|----------|
| **Source** | `classification_table.md` §A. |
| **Requirement** | Every network/storage ingress path must pass through named era-aware decode chokepoints. |
| **Code** | De-facto via `T-INGRESS-01` chokepoints (per-era `decode_*_block` in `ade_codec`, plus `PlutusScript::from_cbor` in `ade_plutus/src/evaluator.rs`). |
| **Tests** | _(no tests — gap)_ |
| **CI** | `ci/ci_check_ingress_chokepoints.sh`. |

### `CN-WIRE-07`: Closed versioned message types

| Aspect | Location |
|--------|----------|
| **Source** | `classification_table.md` §A. |
| **Requirement** | Each protocol-visible message must decode into one closed, versioned message type. |
| **Code** | `crates/ade_network/src/codec/error.rs`, `crates/ade_network/src/codec/version.rs`, plus the 11 closed-taxonomy codec modules under `crates/ade_network/src/codec/`. |
| **Tests** | Per-protocol shape checks across all 11 protocols + real-capture corpora (see registry `tests:` array for full enumeration). |
| **CI** | `ci/ci_check_ce_n_a_5_proof.sh` (PHASE4-N-A close — runs the real-capture round-trip + version-gating + malformed-frame + agency-trace evidence battery per CE-N-A-5 conditions 2/3/5). |

### `CN-LEDGER-01`: `apply_block` purely functional

| Aspect | Location |
|--------|----------|
| **Source** | `classification_table.md` §B. |
| **Requirement** | `apply_block` must be a pure deterministic function of prior state and canonical block input. |
| **Code** | De-facto via `T-CORE-01`, `DC-LEDGER-01` — `crates/ade_ledger/src/rules.rs`. |
| **Tests** | De-facto via `DC-LEDGER-01`. |
| **CI** | `ci/ci_check_forbidden_patterns.sh`, `ci/ci_check_ledger_determinism.sh`. |

### `CN-LEDGER-02`: Same inputs → byte-identical state

| Aspect | Location |
|--------|----------|
| **Source** | `classification_table.md` §B. |
| **Requirement** | Same genesis/bootstrap + same block sequence must yield byte-identical authoritative state. |
| **Code** | De-facto via `T-DET-01`, `DC-LEDGER-02`. |
| **Tests** | De-facto via `DC-LEDGER-02`. |
| **CI** | `ci/ci_check_ledger_determinism.sh`, `ci/ci_check_differential_divergence.sh`. |

### `CN-LEDGER-03`: Tx/block verdicts match Haskell oracle

| Aspect | Location |
|--------|----------|
| **Source** | `classification_table.md` §B. |
| **Requirement** | Validity decisions must match the Cardano reference oracle per era/protocol version. |
| **Code** | De-facto via `DC-LEDGER-03`. |
| **Tests** | De-facto via `DC-LEDGER-03`. |
| **CI** | `ci/ci_check_differential_divergence.sh`. |

### `CN-LEDGER-04`: Mixed-version verdict equivalence

| Aspect | Location |
|--------|----------|
| **Source** | `classification_table.md` §B. |
| **Requirement** | Any two supported production versions that coexist must return the same validity verdict for every consensus-relevant input. |
| **Code** | _(no enforcement — gap)_ |
| **Tests** | _(no tests — gap)_ |
| **CI** | _(no CI enforcement — gap; release-tier per `RO-REL-01`)_ |

### `CN-LEDGER-05`: One semantic processing result per input

| Aspect | Location |
|--------|----------|
| **Source** | `classification_table.md` §B. |
| **Requirement** | Each feature has one semantic processing result; no alternate path may disagree on already-applied / failed / valid. |
| **Code** | _(no enforcement — gap)_ |
| **Tests** | _(no tests — gap)_ |
| **CI** | _(no CI enforcement — gap)_ |

### `CN-LEDGER-06`: Deterministic failure-state residue

| Aspect | Location |
|--------|----------|
| **Source** | `classification_table.md` §B. |
| **Requirement** | Failure-state residue must be deterministic and consensus-neutral. |
| **Code** | _(no enforcement — gap)_ |
| **Tests** | _(no tests — gap)_ |
| **CI** | _(no CI enforcement — gap)_ |

### `CN-LEDGER-07`: UTxO/asset conservation

| Aspect | Location |
|--------|----------|
| **Source** | `classification_table.md` §B (cross-ref `T-CONSERV-01`). |
| **Requirement** | UTxO and asset conservation must hold for every accepted transition, except where protocol rules explicitly authorize mint/burn/rewards/treasury. |
| **Code** | De-facto via `T-CONSERV-01` — `crates/ade_ledger/src/value.rs`, `crates/ade_ledger/src/byron.rs`; PHASE4-B3 adds the Conway full-equation check `crates/ade_ledger/src/conway.rs::check_conway_coin_conservation` (cert/withdrawal-bearing txs in scope; deposit amounts sourced canonically via `DC-TXV-06`/`DC-TXV-07`). `strengthened_in += ["PHASE4-B3"]`. |
| **Tests** | De-facto via `T-CONSERV-01`; registry `tests` carries `conway_conservation_full`. |
| **CI** | `ci/ci_check_differential_divergence.sh`; plus PHASE4-B3 `ci/ci_check_ledger_determinism.sh`. Registry `ci_script` is still empty for this entry (the rule remains `status = "declared"` — enforcement is de-facto via the `enforced` `T-CONSERV-01`); see §G. |

### `CN-LEDGER-08`: No double-spend

| Aspect | Location |
|--------|----------|
| **Source** | `classification_table.md` §B (cross-ref `T-NOSPEND-01`). |
| **Requirement** | No input or equivalent spend authority may be consumed more than once. |
| **Code** | De-facto via `T-NOSPEND-01` — `crates/ade_ledger/src/utxo.rs`. |
| **Tests** | De-facto via `T-NOSPEND-01`. |
| **CI** | `ci/ci_check_differential_divergence.sh`. |

### `CN-LEDGER-09`: Era-specific witness binding

| Aspect | Location |
|--------|----------|
| **Source** | `classification_table.md` §B. Strengthened by PHASE4-B2 (Conway witness binding over inputs/certs/withdrawals/voters/collateral). |
| **Requirement** | Witnesses must bind exactly to the intended body, certificates, withdrawals, governance actions, and scripts for the era; an extra irrelevant witness never substitutes for a missing required signer. |
| **Code** | `crates/ade_ledger/src/tx_validity/required_signers.rs` (PHASE4-B2-S1 — Conway required-signer enumeration over inputs/certs/withdrawals/voters/collateral, grounded in `getConwayWitsVKeyNeeded`); `crates/ade_ledger/src/tx_validity/witness.rs` (each required key bound by a witness whose Ed25519 sig over the preserved body hash verifies). |
| **Tests** | `crates/ade_ledger/tests/tx_witness_closure.rs::{all_required_covered_is_valid, extra_irrelevant_witness_does_not_substitute, missing_certificate_witness_rejected, missing_withdrawal_witness_rejected, missing_governance_voter_witness_rejected, witness_correct_key_wrong_body_rejected}`. |
| **CI** | _(no dedicated CI script — gap; status = `partial`. Verdict/signer-source taxonomy kept closed by `ci/ci_check_consensus_closed_enums.sh` over `ade_ledger/src/tx_validity/`, but no dedicated witness-binding CI gate exists. See §G.)_ |

### `CN-LEDGER-10`: Explicit governance/certificate transitions

| Aspect | Location |
|--------|----------|
| **Source** | `classification_table.md` §B. |
| **Requirement** | Conway governance and certificate transitions must occur only through explicit legal state transitions. |
| **Code** | De-facto via `DC-EPOCH-01` — `crates/ade_ledger/src/governance.rs`. |
| **Tests** | De-facto via `DC-EPOCH-01`. |
| **CI** | _(no CI enforcement — gap)_ |

### `CN-EPOCH-01`: Epoch-boundary-only activation

| Aspect | Location |
|--------|----------|
| **Source** | `classification_table.md` §C. |
| **Requirement** | Stake, rewards, parameter changes, and governance effects may activate only at protocol-defined epoch boundaries. |
| **Code** | De-facto via `crates/ade_ledger/src/epoch.rs`, `governance.rs`; PHASE4-N-B `crates/ade_core/src/consensus/leader_schedule.rs` (epoch-nonce-driven leadership at epoch boundary), `crates/ade_core/src/consensus/ledger_view.rs` (forecast surface); plus PHASE4-B1 `crates/ade_ledger/src/consensus_view.rs` (pure ledger-side epoch-projection feeding the leadership view). |
| **Tests** | `consensus::leader_schedule::tests::query_uses_state_epoch_nonce_for_vrf_input`; `crates/ade_core/tests/leader_schedule_corpus.rs::{corpus_returns_canonical_answer_for_known_pools, corpus_rejects_unknown_pool, corpus_is_deterministic_across_runs}`. plus PHASE4-B1 `crates/ade_ledger/tests/consensus_view.rs::{view_returns_corpus_pool_stake_and_vrf_keyhash, view_unknown_epoch_returns_none, view_is_pure}`. _(De-facto via `DC-LEDGER-04` for ledger-side epoch boundary.)_ |
| **CI** | _(no dedicated CI script — gap; status = `partial`. Strengthened via PHASE4-N-B and PHASE4-B1 but no text-scan gate exists.)_ Flipped `declared → partial` in PHASE4-N-B. |

### `CN-EPOCH-02`: One authoritative committee per epoch

| Aspect | Location |
|--------|----------|
| **Source** | `classification_table.md` §C (cross-ref `T-EPOCH-01`). |
| **Requirement** | At each slot or epoch point there is exactly one authoritative committee and governance interpretation. |
| **Code** | _(no enforcement — gap; cross-ref `T-EPOCH-01` also a gap)_ |
| **Tests** | _(no tests — gap)_ |
| **CI** | _(no CI enforcement — gap)_ |

### `CN-EPOCH-03`: Stake snapshots derivable from chain state

| Aspect | Location |
|--------|----------|
| **Source** | `classification_table.md` §C. |
| **Requirement** | Stake snapshots and reward computations must be derivable solely from canonical chain state. |
| **Code** | De-facto via `crates/ade_ledger/src/epoch.rs::rotate_snapshots`, `compute_rewards`. _(Registry linkage gap.)_ |
| **Tests** | De-facto via `DC-LEDGER-04`. |
| **CI** | _(no CI enforcement — gap)_ |

### `CN-EPOCH-04`: Causal temporal ordering

| Aspect | Location |
|--------|----------|
| **Source** | `classification_table.md` §C (cross-ref `T-CAUSAL-01`). |
| **Requirement** | Future decisions may not leak into present validation; later states may not retroactively reinterpret prior checkpoints. |
| **Code** | _(no enforcement — gap; cross-ref `T-CAUSAL-01` also a gap)_ |
| **Tests** | _(no tests — gap)_ |
| **CI** | _(no CI enforcement — gap)_ |

### `CN-CONS-01`: Deterministic chain selection

| Aspect | Location |
|--------|----------|
| **Source** | `classification_table.md` §D (cross-refs `T-DET-01`, `T-CONS-01`). |
| **Requirement** | Chain selection must be deterministic for the same candidate chains and protocol observables. |
| **Code** | `crates/ade_core/src/consensus/fork_choice.rs`, `crates/ade_core/src/consensus/candidate.rs`. |
| **Tests** | `crates/ade_core/tests/fork_choice_corpus.rs::{replay_is_deterministic, reject_reason_bytes_are_stable, higher_block_no_wins, equal_block_no_tiebreaker_decides}`. |
| **CI** | `ci/ci_check_no_density_in_fork_choice.sh`. PHASE4-N-B strengthened (`declared → partial`); status remains `partial` in registry. |

### `CN-CONS-02`: Rollout skew anti-divergence

| Aspect | Location |
|--------|----------|
| **Source** | `classification_table.md` §D. |
| **Requirement** | Supported rollout skew must not allow a single adversarial input to induce persistent honest-node consensus divergence. |
| **Code** | `crates/ade_core/src/consensus/fork_choice.rs` (rejection-reason determinism limits divergence surface). |
| **Tests** | `crates/ade_core/tests/fork_choice_corpus.rs::{reject_reason_bytes_are_stable, replay_is_deterministic}`. |
| **CI** | `ci/ci_check_no_density_in_fork_choice.sh`, `ci/ci_check_consensus_closed_enums.sh`. Strengthened in PHASE4-N-B; status remains `declared` (release-tier obligation; CI scripts wired as structural anchors). |

### `CN-CONS-03`: Post-partition convergence

| Aspect | Location |
|--------|----------|
| **Source** | `classification_table.md` §D. |
| **Requirement** | After temporary partition, honest nodes must converge using only protocol-defined observables and declared emergency procedures. |
| **Code** | `crates/ade_core/src/consensus/fork_choice.rs`. |
| **Tests** | `crates/ade_core/tests/fork_choice_corpus.rs::{higher_block_no_wins, equal_block_no_tiebreaker_decides, tiebreaker_loss_keeps_current}`. |
| **CI** | `ci/ci_check_no_density_in_fork_choice.sh`. Strengthened in PHASE4-N-B; status remains `declared`. |

### `CN-CONS-04`: Header/body binding

| Aspect | Location |
|--------|----------|
| **Source** | `classification_table.md` §D. |
| **Requirement** | Header validation must bind exactly to the accepted body and consensus context. |
| **Code** | `crates/ade_core/src/consensus/header_validate.rs::validate_and_apply_header`, `crates/ade_core/src/consensus/header_summary.rs`, `crates/ade_core/src/consensus/fork_choice.rs`; plus PHASE4-B1 `crates/ade_core/src/consensus/kes_check.rs` (VRF-keyhash binding + KES authentication for Praos headers) and `crates/ade_ledger/src/block_validity/{transition,header_input}.rs` (wired body-hash binding — recomputed segwit body hash compared to the validated header `body_hash` before body application). |
| **Tests** | `consensus::header_validate::tests::{pipeline_short_circuits_on_first_failure, nonce_contribution_uses_nonce_role_vrf_output_not_leader_role}`; `crates/ade_core/tests/header_validate_compose.rs::{valid_header_accepted_advances_state, header_with_slot_regression_rejected, header_with_block_no_regression_rejected, header_with_op_cert_regression_rejected, header_with_invalid_vrf_proof_rejected, header_beyond_forecast_horizon_rejected, validate_replay_is_deterministic}`; `consensus::candidate::tests::candidate_fragment_carries_anchor_block_no`; plus PHASE4-B1 `crates/ade_core/tests/praos_header_validate.rs::{conway_corpus_headers_all_pass_leader_check, praos_vrf_keyhash_mismatch_rejected}` and `crates/ade_ledger/tests/block_validity_compose.rs::altered_body_rejected_by_hash_binding`. |
| **CI** | _(no dedicated CI script — gap; status = `declared` despite the B1 enforcement work — see §G #3. Strengthened in PHASE4-N-B and PHASE4-B1 via the test surface; discharged structurally via `ci_check_consensus_closed_enums.sh` ensuring `HeaderValidationError` stays closed.)_ |

### `CN-CONS-05`: No host nondeterminism in consensus

| Aspect | Location |
|--------|----------|
| **Source** | `classification_table.md` §D (cross-refs `T-CORE-02`, `T-CONS-02`). |
| **Requirement** | Authoritative consensus decisions must not depend on wall-clock, arrival order, scheduler, or OS behavior. |
| **Code** | `crates/ade_core/src/consensus/header_validate.rs`, `crates/ade_core/src/consensus/fork_choice.rs`, `crates/ade_core/src/consensus/candidate.rs`. |
| **Tests** | `crates/ade_core/tests/header_validate_compose.rs::validate_replay_is_deterministic`; `consensus::header_validate::tests::{pipeline_short_circuits_on_first_failure, nonce_contribution_uses_nonce_role_vrf_output_not_leader_role}`; `crates/ade_core/tests/fork_choice_corpus.rs::{replay_is_deterministic, reject_reason_bytes_are_stable}`. |
| **CI** | `ci/ci_check_no_density_in_fork_choice.sh`, `ci/ci_check_no_float_in_consensus.sh`. Strengthened in PHASE4-N-B; status remains `declared`. |

### `CN-PLUTUS-01`: Deterministic UPLC eval per input

| Aspect | Location |
|--------|----------|
| **Source** | `classification_table.md` §E. |
| **Requirement** | Same script + same redeemers/datum/context + same cost model → identical result and budget. |
| **Code** | De-facto via `crates/ade_plutus/src/evaluator.rs`. _(Registry linkage gap.)_ |
| **Tests** | De-facto via `crates/ade_plutus/...::aiken_fixture_tx_evaluates_end_to_end`. |
| **CI** | _(no CI enforcement — gap)_ |

### `CN-PLUTUS-02`: Single deterministic failure shape

| Aspect | Location |
|--------|----------|
| **Source** | `classification_table.md` §E. |
| **Requirement** | Budget exhaustion and script failure must have a single deterministic failure shape. |
| **Code** | _(no enforcement — gap; structural via `PlutusError` but not audited)_ |
| **Tests** | _(no tests — gap)_ |
| **CI** | _(no CI enforcement — gap)_ |

### `CN-PLUTUS-03`: Canonical ScriptContext derivation

| Aspect | Location |
|--------|----------|
| **Source** | `classification_table.md` §E. |
| **Requirement** | ScriptContext must be canonically and completely derived from tx + ledger state. |
| **Code** | De-facto via `crates/ade_plutus/src/script_context.rs`. _(Registry linkage gap.)_ |
| **Tests** | _(no tests — gap)_ |
| **CI** | _(no CI enforcement — gap)_ |

### `CN-PLUTUS-04`: No host-environment in script results

| Aspect | Location |
|--------|----------|
| **Source** | `classification_table.md` §E (cross-ref `T-CORE-02`, `T-PLATFORM-01`). |
| **Requirement** | No host-environment property may influence script results. |
| **Code** | De-facto via `T-CORE-02`. |
| **Tests** | _(no tests — gap)_ |
| **CI** | `ci/ci_check_forbidden_patterns.sh` — covers all 6 BLUE crates including `ade_plutus`. |

### `CN-PROTO-01`: Typed miniprotocol state machines

| Aspect | Location |
|--------|----------|
| **Source** | `classification_table.md` §F. |
| **Requirement** | Each miniprotocol must be an explicit deterministic state machine with legal typed transitions only. |
| **Code** | De-facto via `DC-PROTO-01` — full per-protocol state machines under `crates/ade_network/src/`. _(Registry linkage gap.)_ |
| **Tests** | De-facto via `DC-PROTO-01`. |
| **CI** | De-facto via `ci/ci_check_ce_n_a_5_proof.sh`. _(Registry linkage gap.)_ |

### `CN-PROTO-02`: Transcript-deterministic peer behavior

| Aspect | Location |
|--------|----------|
| **Source** | `classification_table.md` §F. |
| **Requirement** | For the same peer transcript, authoritative state and outbound transcript must be identical. |
| **Code** | De-facto via `DC-PROTO-02`. _(Registry linkage gap.)_ |
| **Tests** | De-facto via `DC-PROTO-02`. |
| **CI** | _(no CI enforcement — gap; release-tier discharge via `RO-REL-02`)_ |

### `CN-PROTO-03`: Strict agency enforcement

| Aspect | Location |
|--------|----------|
| **Source** | `classification_table.md` §F. |
| **Requirement** | Agency must be enforced strictly; impossible messages must fail deterministically. |
| **Code** | De-facto via the closed `*Agency` enums + `wrong_agency_returns_error` tests on every state machine. _(Registry linkage gap.)_ |
| **Tests** | De-facto via `DC-PROTO-01` (`wrong_agency_returns_error` family across all 11 protocols). |
| **CI** | _(no CI enforcement — gap)_ |

### `CN-PROTO-04`: No transport nondeterminism

| Aspect | Location |
|--------|----------|
| **Source** | `classification_table.md` §F (cross-ref `T-TRANSPORT-01`). |
| **Requirement** | Socket fragmentation, mux ordering, arrival order, timeout behavior must not leak nondeterminism into authoritative logic. |
| **Code** | _(no enforcement — gap; cross-ref `T-TRANSPORT-01` also a gap. PHASE4-N-A locked decision: BLUE never receives sockets / tokio / arrival-order context; PHASE4-N-B added the GREEN orchestrator that translates RED inputs to canonical BLUE form, but no CI text-scan exists for the property)_ |
| **Tests** | _(no tests — gap)_ |
| **CI** | _(no CI enforcement — gap; structurally protected by `ci_check_no_async_in_blue.sh` + `ci_check_no_chaindb_in_consensus_blue.sh`)_ |

### `CN-PROTO-05`: Bounded resource on untrusted input

| Aspect | Location |
|--------|----------|
| **Source** | `classification_table.md` §F (cross-ref `T-RESOURCE-01`). |
| **Requirement** | Untrusted network inputs must not allocate unbounded authoritative resources before deterministic validation. |
| **Code** | _(no enforcement — gap; PHASE4-N-A DoS hardening at S-A10 close replaced unbounded `Vec::with_capacity(len)` with size-checked alternatives in 8 codecs, but no registry linkage yet)_ |
| **Tests** | _(no tests — gap)_ |
| **CI** | _(no CI enforcement — gap)_ |

### `CN-NET-01`: Trusted relay topology only

| Aspect | Location |
|--------|----------|
| **Source** | `classification_table.md` §G (operational; cross-ref `OP-NET-01`). |
| **Requirement** | Block producer must not accept arbitrary public peer connectivity; only trusted relay topology. |
| **Code** | _(operational invariant — not a code rule)_ |
| **Tests** | _(operational)_ |
| **CI** | _(operational — no in-repo enforcement applicable)_ |

### `CN-NET-02`: Diverse relay paths

| Aspect | Location |
|--------|----------|
| **Source** | `classification_table.md` §G (operational; cross-ref `OP-NET-02`). |
| **Requirement** | Relay paths geographically and topologically diverse. |
| **Code** | _(operational)_ |
| **Tests** | _(operational)_ |
| **CI** | _(operational)_ |

### `CN-NET-03`: No peer/ASN/region dominance

| Aspect | Location |
|--------|----------|
| **Source** | `classification_table.md` §G (operational; cross-ref `OP-NET-03`). |
| **Requirement** | No single peer, ASN, region, or operator cluster may dominate the node's authoritative view. |
| **Code** | _(operational)_ |
| **Tests** | _(operational)_ |
| **CI** | _(operational)_ |

### `CN-NET-04`: Anti-starvation peer selection

| Aspect | Location |
|--------|----------|
| **Source** | `classification_table.md` §G. |
| **Requirement** | Peer selection and promotion policies must not allow an adversary-controlled set to deterministically starve honest views. |
| **Code** | _(no enforcement — gap)_ |
| **Tests** | _(no tests — gap)_ |
| **CI** | _(no CI enforcement — gap)_ |

### `CN-MEM-01`: Bounded mempool admission

| Aspect | Location |
|--------|----------|
| **Source** | `classification_table.md` §H. |
| **Requirement** | Untrusted inbound work must be admitted through deterministic bounded policies before consuming scarce authoritative resources. |
| **Code** | _(no enforcement — gap)_ |
| **Tests** | _(no tests — gap)_ |
| **CI** | _(no CI enforcement — gap)_ |

### `CN-MEM-02`: Mempool starvation protection

| Aspect | Location |
|--------|----------|
| **Source** | `classification_table.md` §H (operational; cross-ref `OP-MEM-01`). |
| **Requirement** | Mempool pressure and peer churn must not starve block validation, chain selection, or persistence. |
| **Code** | _(operational)_ |
| **Tests** | _(operational)_ |
| **CI** | _(operational)_ |

### `CN-MEM-03`: Deterministic shed policy

| Aspect | Location |
|--------|----------|
| **Source** | `classification_table.md` §H. |
| **Requirement** | Under overload, work shedding must follow deterministic policy. |
| **Code** | _(no enforcement — gap)_ |
| **Tests** | _(no tests — gap)_ |
| **CI** | _(no CI enforcement — gap)_ |

### `CN-MEM-04`: Mempool ⊆ block acceptance

| Aspect | Location |
|--------|----------|
| **Source** | `classification_table.md` §H. |
| **Requirement** | Mempool acceptance rules must never contradict block/ledger acceptance rules for the same semantics. |
| **Code** | _(no enforcement — gap)_ |
| **Tests** | _(no tests — gap)_ |
| **CI** | _(no CI enforcement — gap)_ |

### `CN-STORE-01`: Anchor-verified storage initialization

| Aspect | Location |
|--------|----------|
| **Source** | `classification_table.md` §I; `shelley_bootstrap_contract.md`. |
| **Requirement** | No authoritative storage initialization may occur before bootstrap or anchor verification succeeds. |
| **Code** | _(no enforcement — gap; `ade_node` does not yet compose chaindb + ledger. PHASE4-N-B adds `BootstrapAnchorHash` and ties `EraSchedule` to it — `DC-CONS-07` — but the storage-side anchor binding is still owed by cluster N-E)_ |
| **Tests** | _(no tests — gap)_ |
| **CI** | _(no CI enforcement — gap)_ |

### `CN-STORE-02`: WAL/checkpoint anchor binding

| Aspect | Location |
|--------|----------|
| **Source** | `classification_table.md` §I. |
| **Requirement** | WAL entries, checkpoints, and recovered artifacts must be bound to exactly one anchor or bootstrap lineage. |
| **Code** | _(no enforcement — gap; `PersistentChainDb` has no anchor binding yet)_ |
| **Tests** | _(no tests — gap)_ |
| **CI** | _(no CI enforcement — gap)_ |

### `CN-STORE-03`: Crash recovery = clean replay

| Aspect | Location |
|--------|----------|
| **Source** | `classification_table.md` §I (cross-ref `T-REC-01`). |
| **Requirement** | Crash recovery must produce the same authoritative state as clean replay over canonical inputs. |
| **Code** | `crates/ade_runtime/src/chaindb/crash_safety.rs`; `crates/ade_runtime/tests/stress_kill_harness.rs`. |
| **Tests** | `crates/ade_runtime/tests/stress_kill_harness.rs::{stress_kill_smoke, stress_kill_1000, snapshot_table_intact_after_kill_loop}`; `crates/ade_runtime/src/chaindb/crash_safety.rs::persistent_passes_crash_safety_with_no_kill`. |
| **CI** | `ci/ci_check_chaindb_crash_safety.sh`. Flipped `declared → enforced` in `78da6c9`. |

### `CN-STORE-04`: Atomic checkpoints

| Aspect | Location |
|--------|----------|
| **Source** | `classification_table.md` §I (cross-ref `T-REC-01`). |
| **Requirement** | Checkpoints must be atomic: fully committed and valid, or absent. |
| **Code** | `crates/ade_runtime/src/chaindb/snapshot_contract.rs`, `crates/ade_runtime/src/chaindb/persistent.rs`. |
| **Tests** | `crates/ade_runtime/src/chaindb/persistent.rs::{persistent_passes_snapshot_contract, snapshots_persist_across_reopen, corrupted_magic_returns_corruption_error}`; `crates/ade_runtime/src/chaindb/snapshot_contract.rs::in_memory_passes_snapshot_contract`. |
| **CI** | `ci/ci_check_chaindb_contract.sh`. Flipped `declared → enforced` in `78da6c9`. |

### `CN-STORE-05`: Append-only finalized provenance

| Aspect | Location |
|--------|----------|
| **Source** | `classification_table.md` §I (cross-ref `T-REC-02`). |
| **Requirement** | Finalized provenance must be append-only, auditable, replay-derivable. |
| **Code** | `crates/ade_runtime/src/chaindb/persistent.rs`; `crates/ade_runtime/src/chaindb/contract.rs`. |
| **Tests** | `crates/ade_runtime/src/chaindb/persistent.rs::{persistent_passes_contract, reopen_observes_committed_block}`; `crates/ade_runtime/src/chaindb/contract.rs::in_memory_passes_contract`. |
| **CI** | `ci/ci_check_chaindb_contract.sh`. Flipped `declared → enforced` in `78da6c9`. |

### `CN-STORE-06`: Storage re-decode via canonical chokepoints

| Aspect | Location |
|--------|----------|
| **Source** | `classification_table.md` §I (cross-ref `DC-STORE-06`). |
| **Requirement** | On-disk bytes must re-enter through the same canonical validation/decode chokepoints as network inputs. |
| **Code** | _(no enforcement — gap; `recover` currently delegates decode to caller `Recoverable::decode_snapshot`)_ |
| **Tests** | _(no tests — gap)_ |
| **CI** | _(no CI enforcement — gap)_ |

### `CN-CRYPTO-01`: Verification in core, signing outside

| Aspect | Location |
|--------|----------|
| **Source** | `classification_table.md` §J (cross-ref `T-KEY-01`, `DC-CRYPTO-02`). |
| **Requirement** | Verification belongs to the authoritative core; signing belongs outside it. |
| **Code** | De-facto via `T-KEY-01` — `crates/ade_crypto/src/lib.rs` (verify-only). |
| **Tests** | _(structural)_ |
| **CI** | `ci/ci_check_no_signing_in_blue.sh` (via cross-ref). |

### `CN-CRYPTO-02`: Domain-separated consensus hashes

| Aspect | Location |
|--------|----------|
| **Source** | `classification_table.md` §J. |
| **Requirement** | All consensus-relevant hashes must be domain-separated and unambiguous. |
| **Code** | `crates/ade_core/src/consensus/vrf_cert.rs` (PHASE4-N-B S-B3: typed Praos VRF input — 8-byte BE slot ‖ 32-byte epoch nonce ‖ 1-byte role tag, NONCE=0x4E / LEADER=0x4C — built by a single audited function so callers cannot accidentally cross domains). |
| **Tests** | `crates/ade_core/tests/vrf_cert_threshold.rs::{vrf_input_layout_is_41_bytes_with_correct_tag, vrf_role_tags_match_convention, vrf_input_byte_layout}`. |
| **CI** | _(no dedicated CI script — gap; status = `partial`. Strengthened in PHASE4-N-B via the typed-input pattern and test surface; discharged structurally via `ci_check_consensus_closed_enums.sh`.)_ Flipped `declared → partial` in PHASE4-N-B. |

### `CN-CRYPTO-03`: Deterministic ordering before hashing

| Aspect | Location |
|--------|----------|
| **Source** | `classification_table.md` §J. |
| **Requirement** | All collections with consensus meaning must be ordered deterministically before hashing or comparison. |
| **Code** | De-facto via `T-COLL-01` + `ci_check_forbidden_patterns.sh` (no `HashMap` in BLUE) + PHASE4-N-B `BTreeMap`-only op-cert counter map in `praos_state.rs`. _(Registry linkage gap.)_ |
| **Tests** | _(no tests — gap; PHASE4-N-B `op_cert_counter_map_iteration_is_deterministic` provides de-facto proof of one instance)_ |
| **CI** | `ci/ci_check_forbidden_patterns.sh`. |

### `CN-CRYPTO-04`: No fallback after verification failure

| Aspect | Location |
|--------|----------|
| **Source** | `classification_table.md` §J. |
| **Requirement** | Verification failure must fail once and deterministically; no implicit parser or serialization fallback. |
| **Code** | _(no enforcement — gap; convention followed in `ade_crypto` and in PHASE4-N-B `validate_and_apply_header` short-circuiting but not audited)_ |
| **Tests** | _(no tests — gap)_ |
| **CI** | _(no CI enforcement — gap)_ |

### `CN-BUILD-01`: No semantic cfg/feature

| Aspect | Location |
|--------|----------|
| **Source** | `classification_table.md` §K (cross-ref `T-BUILD-01`). |
| **Requirement** | No build profile, feature flag, cfg, or optimization mode may alter authoritative semantics or persisted bytes. |
| **Code** | De-facto via `T-BUILD-01` — BLUE `lib.rs` files. |
| **Tests** | _(structural)_ |
| **CI** | `ci/ci_check_no_semantic_cfg.sh`. |

### `CN-BUILD-02`: Runtime, not compile-time, variability

| Aspect | Location |
|--------|----------|
| **Source** | `classification_table.md` §K (cross-ref `T-BUILD-02`). |
| **Requirement** | All semantic variability must be explicit runtime protocol data, not hidden compile-time choice. |
| **Code** | _(no enforcement — gap; cross-ref `T-BUILD-02` also a gap)_ |
| **Tests** | _(no tests — gap)_ |
| **CI** | `ci/ci_check_no_semantic_cfg.sh` partially covers via flag prohibition. |

### `CN-BUILD-03`: One semantic interpretation per version+anchor

| Aspect | Location |
|--------|----------|
| **Source** | `classification_table.md` §K (cross-ref `T-BUILD-02`). |
| **Requirement** | Exactly one semantic interpretation may exist for a given protocol version and bootstrap anchor. |
| **Code** | _(no enforcement — gap; PHASE4-N-B introduced `BootstrapAnchorHash` as the consensus-side anchor but the cross-version rule has no audit)_ |
| **Tests** | _(no tests — gap)_ |
| **CI** | _(no CI enforcement — gap)_ |

### `CN-BUILD-04`: Operator-config cannot weaken semantics

| Aspect | Location |
|--------|----------|
| **Source** | `classification_table.md` §K. |
| **Requirement** | Operator configuration may tune transport/logging/telemetry but may not silently weaken ledger/consensus/persistence semantics. |
| **Code** | _(no enforcement — gap; `ade_node` does not yet have an operator config surface)_ |
| **Tests** | _(no tests — gap)_ |
| **CI** | _(no CI enforcement — gap)_ |

### `CN-TEST-01`: Differential fuzzing across versions

| Aspect | Location |
|--------|----------|
| **Source** | `classification_table.md` §L (release; cross-ref `RO-TEST-01`). |
| **Requirement** | Consensus-relevant inputs must be fuzzed differentially across all supported versions; any verdict mismatch is release-blocking. |
| **Code** | _(release obligation)_ |
| **Tests** | _(release obligation)_ |
| **CI** | _(no CI enforcement — release obligation, deferred)_ |

### `CN-TEST-02`: Regression corpus pinning

| Aspect | Location |
|--------|----------|
| **Source** | `classification_table.md` §L (release; cross-ref `RO-TEST-02`). |
| **Requirement** | Every malformed or discrepant input that ever caused a fork or parser disagreement becomes a permanent regression corpus entry. |
| **Code** | `crates/ade_testkit/src/harness/regression_corpus.rs` (de-facto). |
| **Tests** | _(release obligation)_ |
| **CI** | _(no CI enforcement — release obligation)_ |

### `CN-TEST-03`: Verdict stability under replay

| Aspect | Location |
|--------|----------|
| **Source** | `classification_table.md` §L (release; cross-ref `RO-TEST-03`). |
| **Requirement** | Previously failed, duplicate, or boundary-case inputs must remain verdict-stable under resubmission and replay. |
| **Code** | _(release obligation)_ |
| **Tests** | _(release obligation)_ |
| **CI** | _(no CI enforcement — release obligation)_ |

### `CN-REL-01`: Mixed-topology release gate

| Aspect | Location |
|--------|----------|
| **Source** | `classification_table.md` §L (release; cross-ref `RO-REL-01`). |
| **Requirement** | A release is not mainnet-eligible without mixed-version topology consensus equivalence on adversarial inputs. |
| **Code** | _(release obligation)_ |
| **Tests** | _(release obligation)_ |
| **CI** | _(no CI enforcement — release obligation)_ |

### `CN-REL-02`: Ecosystem-level fault threshold

| Aspect | Location |
|--------|----------|
| **Source** | `classification_table.md` §L (release; cross-ref `RO-REL-03`). |
| **Requirement** | No single implementation bug should exceed the protocol's intended safety/liveness fault threshold at ecosystem level. |
| **Code** | _(release obligation — operational/governance)_ |
| **Tests** | _(release obligation)_ |
| **CI** | _(no CI enforcement — release obligation)_ |

### `CN-REL-03`: Cross-implementation agreement

| Aspect | Location |
|--------|----------|
| **Source** | `classification_table.md` §L (release; cross-ref `RO-REL-02`). |
| **Requirement** | Cross-implementation accept/reject agreement on authoritative corpora is release-blocking. |
| **Code** | _(release obligation)_ |
| **Tests** | _(release obligation)_ |
| **CI** | _(no CI enforcement — release obligation)_ |

### `CN-OPS-01`: Reconciliation from canonical chain only

| Aspect | Location |
|--------|----------|
| **Source** | `classification_table.md` §M (operational; cross-ref `OP-OPS-01`). |
| **Requirement** | Post-incident reconciliation must be derived solely from the recovered canonical chain. |
| **Code** | _(operational)_ |
| **Tests** | _(operational)_ |
| **CI** | _(operational)_ |

### `CN-OPS-02`: Explicit emergency recovery criteria

| Aspect | Location |
|--------|----------|
| **Source** | `classification_table.md` §M (operational; cross-ref `OP-OPS-02`). |
| **Requirement** | Emergency recovery procedures have explicit admissibility criteria, deterministic inputs/outputs, defined authority thresholds. |
| **Code** | _(operational)_ |
| **Tests** | _(operational)_ |
| **CI** | _(operational)_ |

### `CN-OPS-03`: Deterministic incident evidence

| Aspect | Location |
|--------|----------|
| **Source** | `classification_table.md` §M (operational; cross-ref `OP-OPS-03`). |
| **Requirement** | Incident evidence must allow reconstruction of the canonical decision path without nondeterministic logs. |
| **Code** | _(operational)_ |
| **Tests** | _(operational)_ |
| **CI** | _(operational)_ |

### `CN-META-01`: Every invariant has mechanical enforcement

| Aspect | Location |
|--------|----------|
| **Source** | `classification_table.md` §N (cross-ref `T-CI-01`). |
| **Requirement** | Every claimed invariant must have at least one mechanical enforcement point. |
| **Code** | _(meta — enforced indirectly via `ci_check_constitution_coverage.sh`)_ |
| **Tests** | _(meta)_ |
| **CI** | `ci/ci_check_constitution_coverage.sh`. |

### `CN-META-02`: Deterministic structured failure shape

| Aspect | Location |
|--------|----------|
| **Source** | `classification_table.md` §N (cross-ref `T-ERR-01`). |
| **Requirement** | Every consensus-relevant failure mode must have a deterministic structured error shape. |
| **Code** | De-facto via `T-ERR-01` plus PHASE4-N-B closed consensus error taxonomy (`HeaderValidationError`, `OpCertCounterError`, `ChainSelectionReject`, `NonceError`, etc.). |
| **Tests** | De-facto via `T-ERR-01`. |
| **CI** | `ci/ci_check_consensus_closed_enums.sh` (PHASE4-N-B) discharges this for the consensus subset. |

### `CN-META-03`: Reproducible equivalence claims

| Aspect | Location |
|--------|----------|
| **Source** | `classification_table.md` §N (cross-ref `DC-REF-01`). |
| **Requirement** | Every equivalence claim must be reproducible from named fixtures, oracle versions, replay inputs. |
| **Code** | De-facto via `DC-REF-01`. |
| **Tests** | De-facto via `DC-REF-01`. |
| **CI** | `ci/ci_check_ref_provenance.sh`. |

---

## RO — Release Obligations (Project Constitution §4a)

> All `RO-*` rules are release-tier obligations. None have `code_locus`,
> `tests`, or `ci_script` populated in the registry. They are
> release-gate criteria, not HEAD-time enforcement points; this is by
> design and is recorded here as the explicit "no CI at HEAD" state.

### `RO-TEST-01`: Differential fuzzing release gate

| Aspect | Location |
|--------|----------|
| **Source** | Project constitution §4a; cross-ref `CN-TEST-01`. |
| **Requirement** | Consensus-relevant inputs fuzzed differentially across all supported versions; any verdict mismatch is release-blocking. |
| **Code** | _(release obligation — no HEAD enforcement)_ |
| **Tests** | _(release obligation)_ |
| **CI** | _(release obligation; will require new CI gate at release-prep time)_ |

### `RO-TEST-02`: Permanent regression corpus

| Aspect | Location |
|--------|----------|
| **Source** | Project constitution §4a; cross-ref `CN-TEST-02`. |
| **Requirement** | Every fork/mismatch/parser disagreement that ever occurred becomes a permanent regression corpus entry. |
| **Code** | `crates/ade_testkit/src/harness/regression_corpus.rs` (de-facto host). |
| **Tests** | _(release obligation)_ |
| **CI** | _(release obligation)_ |

### `RO-TEST-03`: Resubmission verdict stability

| Aspect | Location |
|--------|----------|
| **Source** | Project constitution §4a; cross-ref `CN-TEST-03`. |
| **Requirement** | Failed, duplicate, and boundary-case inputs remain verdict-stable under resubmission and replay. |
| **Code** | _(release obligation)_ |
| **Tests** | _(release obligation)_ |
| **CI** | _(release obligation)_ |

### `RO-REL-01`: Mixed-version mainnet eligibility

| Aspect | Location |
|--------|----------|
| **Source** | Project constitution §4a; cross-ref `CN-REL-01`. |
| **Requirement** | Release not mainnet-eligible without mixed-version topology consensus equivalence on adversarial inputs. |
| **Code** | _(release obligation)_ |
| **Tests** | _(release obligation)_ |
| **CI** | _(release obligation)_ |

### `RO-REL-02`: Cross-implementation corpus agreement

| Aspect | Location |
|--------|----------|
| **Source** | Project constitution §4a; cross-ref `CN-REL-03`. |
| **Requirement** | Cross-implementation accept/reject agreement on authoritative corpora is release-blocking. |
| **Code** | _(release obligation)_ |
| **Tests** | _(release obligation)_ |
| **CI** | _(release obligation)_ |

### `RO-REL-03`: Single-bug fault threshold

| Aspect | Location |
|--------|----------|
| **Source** | Project constitution §4a. |
| **Requirement** | No single implementation bug should exceed the protocol's intended safety/liveness fault threshold at ecosystem level. |
| **Code** | _(release obligation — ecosystem-level)_ |
| **Tests** | _(release obligation)_ |
| **CI** | _(release obligation)_ |

---

## OP — Operational Invariants (Project Constitution §4b)

> All `OP-*` rules are operational invariants. They constrain how the
> node is deployed and operated; they are not properties of the source
> tree. There is no HEAD-time CI enforcement for any of them by design.

### `OP-NET-01`: Trusted relay topology for BP

| Aspect | Location |
|--------|----------|
| **Source** | Project constitution §4b; cross-ref `CN-NET-01`. |
| **Requirement** | Block producer connects only through trusted relay topology. |
| **Code** | _(operational)_ |
| **Tests** | _(operational)_ |
| **CI** | _(operational — no in-repo enforcement)_ |

### `OP-NET-02`: Diverse relay paths

| Aspect | Location |
|--------|----------|
| **Source** | Project constitution §4b; cross-ref `CN-NET-02`. |
| **Requirement** | Relay paths geographically and topologically diverse. |
| **Code** | _(operational)_ |
| **Tests** | _(operational)_ |
| **CI** | _(operational)_ |

### `OP-NET-03`: No peer/ASN/region dominance

| Aspect | Location |
|--------|----------|
| **Source** | Project constitution §4b; cross-ref `CN-NET-03`. |
| **Requirement** | No single peer, ASN, region, or operator cluster dominates the node's authoritative view. |
| **Code** | _(operational)_ |
| **Tests** | _(operational)_ |
| **CI** | _(operational)_ |

### `OP-MEM-01`: Validation/selection not starved by mempool

| Aspect | Location |
|--------|----------|
| **Source** | Project constitution §4b; cross-ref `CN-MEM-02`. |
| **Requirement** | Mempool pressure and peer churn must not starve block validation, chain selection, or persistence. |
| **Code** | _(operational — scheduling priority)_ |
| **Tests** | _(operational)_ |
| **CI** | _(operational)_ |

### `OP-OPS-01`: Reconciliation from canonical chain only

| Aspect | Location |
|--------|----------|
| **Source** | Project constitution §4b; cross-ref `CN-OPS-01`. |
| **Requirement** | Post-incident reconciliation derived solely from recovered canonical chain. |
| **Code** | _(operational)_ |
| **Tests** | _(operational)_ |
| **CI** | _(operational)_ |

### `OP-OPS-02`: Emergency recovery admissibility

| Aspect | Location |
|--------|----------|
| **Source** | Project constitution §4b; cross-ref `CN-OPS-02`. |
| **Requirement** | Emergency recovery procedures have explicit admissibility criteria, deterministic inputs/outputs, authority thresholds. |
| **Code** | _(operational)_ |
| **Tests** | _(operational)_ |
| **CI** | _(operational)_ |

### `OP-OPS-03`: Deterministic incident evidence

| Aspect | Location |
|--------|----------|
| **Source** | Project constitution §4b; cross-ref `CN-OPS-03`. |
| **Requirement** | Incident evidence sufficient to reconstruct canonical decision path without nondeterministic logs. |
| **Code** | _(operational)_ |
| **Tests** | _(operational)_ |
| **CI** | _(operational)_ |

---

## Named ingress chokepoint registry (cross-cutting, T-INGRESS-01 / DC-INGRESS-01 / CN-WIRE-06)

The full named-chokepoint list at HEAD `b9ff041`. `ci/ci_check_ingress_chokepoints.sh` enforces that raw CBOR decoding outside `ade_codec` is forbidden except at these named sites; Check 3 currently allowlists exactly the one non-`ade_codec` entry. Adding a new chokepoint requires (a) listing it in the script's header comment, (b) adding the corresponding path to the allowlist if it lives outside `ade_codec`, and (c) updating this table.

| # | Chokepoint | Crate | Surface |
|---|---|---|---|
| 1 | `decode_block_envelope` | `ade_codec` | Era-tagged block envelope |
| 2 | `decode_byron_ebb_block` | `ade_codec` | Byron EBB block CBOR |
| 3 | `decode_byron_regular_block` | `ade_codec` | Byron regular block CBOR |
| 4 | `decode_shelley_block` | `ade_codec` | Shelley block CBOR |
| 5 | `decode_allegra_block` | `ade_codec` | Allegra block CBOR |
| 6 | `decode_mary_block` | `ade_codec` | Mary block CBOR |
| 7 | `decode_alonzo_block` | `ade_codec` | Alonzo block CBOR |
| 8 | `decode_babbage_block` | `ade_codec` | Babbage block CBOR |
| 9 | `decode_conway_block` | `ade_codec` | Conway block CBOR |
| 10 | `decode_address` | `ade_codec` | Address CBOR (Byron/Shelley legacy + Shelley) |
| 11 | `PlutusScript::from_cbor` | `ade_plutus` | Plutus script CBOR (distinct surface from block CBOR; uses aiken/pallas internally) |

The PHASE4-N-B consensus surface does *not* introduce a new named-chokepoint entry — `EraSchedule` enters BLUE consensus as a typed-value parameter built from genesis text in RED (`crates/ade_runtime/src/consensus/genesis_parser.rs`), not as a CBOR-decoded chokepoint, so the chokepoint registry above is unchanged.

---

## Repo-wide CI scripts not yet pinned to a rule

Three CI scripts under `ci/` enforce repo hygiene rather than a single
invariant. They are run on every push but do not appear in any rule's
CI row above:

| Script | What it enforces | Why it is not pinned to a registry rule |
|---|---|---|
| `ci/ci_check_no_secrets.sh` | No credentials, IPs, hostnames, or keys committed to the tree. | Project hygiene rule (per `MEMORY.md::feedback_no_credential_leaks.md`); not an invariant in the project constitution. Candidate for an `OP-SEC-*` entry in a future registry pass. |
| `ci/ci_check_pallas_quarantine.sh` | `pallas-*` references confined to `ade_plutus`. | Phase 2C obligation `O-29.2` (CODEMAP §CI enforcement table) — confines aiken/pallas transitive types to the quarantine crate. Candidate for a `CN-WIRE-*` strengthening or a new `DC-PLUTUS-*` entry. |
| `ci/git-hooks/commit-msg` | Local commit-msg hook rejects commits without `Co-Authored-By: Claude` trailer. | Per-repo IDD scaffolding (HEAD_DELTAS §5 second table). Not a constitution rule. Not applicable to TRACEABILITY as currently scoped. |

---

## Cross-references and consistency warnings

These warnings are surfaced for the parent's downstream review; they do
not block doc generation.

### A0000. PHASE4-B4 close — 1 new rule (`DC-LEDGER-08`), 1 strengthening (`DC-VAL-06`), 0 new CI scripts (existing gate extended); one honest environment-blocked open obligation

Cluster PHASE4-B4 (Conway cert-state accumulation, fail-closed) closed with the following registry deltas. **No new CI script landed** — `ci/ci_check_forbidden_patterns.sh` was *extended* in S3/S4 to guard the cert-state fail-open swallow. The registry total moves to **171 rules** at HEAD `ee35493` (+1 from `DC-LEDGER-08`); the prior-page narrative count of 170 is from the B3F baseline and is superseded by this refresh.

**New rule (`DC-LEDGER-08` within DC):**

| Rule | Status | Code authority | Discharging CI |
|---|---|---|---|
| `DC-LEDGER-08` | **enforced** (PHASE4-B4) | `ade_codec::conway::cert` (`decode_conway_certs` payload retention + `decode_drep`) + `ade_codec::shelley::cert` (`read_pool_registration_cert`) + `ade_types::conway::cert` (owner-complete `ConwayCert`) + `ade_types::shelley::cert` (`PoolRegistrationCert.owners`) + `ade_ledger::delegation` (`apply_conway_cert` + `ConwayCertAction`/`ConwayCertOutcome` owner-tagged apply model, total over 18 tags) + `ade_ledger::rules` (`accumulate_tx_certs` era-dispatched fail-closed) + 16 named tests | `ci_check_forbidden_patterns.sh` (extended to guard the cert-state fail-open swallow) |

**Strengthening (existing rule):**

| Rule | Action | PHASE4-B4 contribution |
|---|---|---|
| `DC-VAL-06` | enforced (strengthened); `strengthened_in += ["PHASE4-B1", "PHASE4-B2", "PHASE4-B3", "PHASE4-B3F", "PHASE4-B4"]` | the cert-state silent-skip is removed from `rules.rs::accumulate_tx_certs` (the prior `"non-fatal during replay"` swallow); `ci_check_forbidden_patterns.sh` extended to FAIL on its reintroduction (the `"non-fatal during replay"` rationale string anywhere under `crates/`, or an `Err(_)` swallow arm in `accumulate_tx_certs`) |

**CI delta (0 new scripts; 1 extension):**

| CI script | Rule-ID anchor (per header comment) | Rules linked from CI row |
|---|---|---|
| `ci/ci_check_forbidden_patterns.sh` (extended PHASE4-B4-S3/S4) | DC-LEDGER-08 | `DC-LEDGER-08`, `DC-VAL-06` (plus its pre-existing `T-CORE-02` / `T-DET-01` / `T-CORE-01` anchors) |

**HONEST ENFORCEMENT GAP — `DC-LEDGER-08` real-chain oracle is ENVIRONMENT-BLOCKED.** The mechanical closure IS shipped and CI-guarded: owner-complete decode (`each_tag_retains_owner_payloads`, `drep_grammar_total`), total owner-tagged apply (`conway_cert_action_total`, `apply_outcome_agrees_with_action`, `removed_tag_rejects_as_era_invalid`, `drep_registration_is_owner_tagged_not_applied`), era-dispatched fail-closed accumulation (`era_dispatch_conway_accumulates_via_conway_path`, `era_dispatch_shelley_accumulates_via_shelley_path`, `conway_decode_error_is_fail_closed`, `conway_unknown_tag_is_fail_closed`, `conway_removed_tag_is_fail_closed`, `conway_apply_error_is_fail_closed`, `conway_governance_cert_routed_out_of_scope`), and a synthetic positive/replay/adversarial corpus (`positive_synthetic_cert_state_accumulates`, `cert_state_replay_byte_identical`, `adversarial_no_false_accept` in `crates/ade_ledger/tests/cert_state_corpus.rs`). **What is open:** the real epoch-576 cert-state-vs-cardano-node oracle is **environment-blocked** — the epoch-576 UMap snapshot is absent post-extraction (recorded in `corpus/cert_state/README.md` and `corpus/validity/conway_epoch576/README.md`). The registry records this in `DC-LEDGER-08`'s `open_obligation` field, **reclassified environment-blocked per the tier doctrine — identical to the documented open obligations on `DC-TXV-06` and `DC-TXV-03`**. This is not a silent gap and not a false-accept on the closed surface; it is a real-chain agreement obligation that cannot be discharged until the UMap snapshot is re-extracted. Surfaced in §G #17.

**Cross-checks (CODEMAP / HEAD_DELTAS / registry — all consistent at HEAD `ee35493`):**

1. **CODEMAP** (`docs/ade-CODEMAP.md`, regenerated for B4) carries the PHASE4-B4 surface: the owner-tagged apply model in `ade_ledger::delegation` (`ConwayCertAction`, `ConwayCertEnv`, `ConwayCertOutcome`, `GovernanceOwner`, `GovernanceCertEffect`, `OwnerTaggedEffect` — +6 canonical types), the owner-completion of `ConwayCert`, and the `owners` field on `PoolRegistrationCert`. Every code module named in the `DC-LEDGER-08` Code row above appears in CODEMAP under the BLUE `ade_codec`/`ade_types`/`ade_ledger` entries. No mismatch.
2. **HEAD_DELTAS** (`docs/ade-HEAD_DELTAS.md`, regenerated for B4, HEAD `ee35493`) narrates the PHASE4-B4 arc across five commits (`ae1300a`, `228415b`, `da30706`, `302d22c`, `ee35493`) and §5 lists the `ci/ci_check_forbidden_patterns.sh` extension. The CI script in the `DC-LEDGER-08` CI row is present in HEAD_DELTAS. No orphaned CI script introduced.
3. **Registry** (`docs/ade-invariant-registry.toml`): `DC-LEDGER-08` is present with `status = "enforced"`, `ci_script = "ci/ci_check_forbidden_patterns.sh"`, `cluster = "CL-LEDGER-VERDICT"`, and a populated `open_obligation`. All 16 `tests` array entries were verified to exist as functions in the codebase (see Test cross-check in §E). **REGISTRY-HYGIENE DEFECT — the constitution-coverage gate now FAILs.** `DC-LEDGER-08` lists 5 cross_refs (`DC-VAL-06`, `DC-TXV-06`, `DC-LEDGER-07`, `T-DET-01`, `CN-LEDGER-07`), but **none of the 5 target rules reference `DC-LEDGER-08` back** — so `ci/ci_check_constitution_coverage.sh` reports five `not bidirectional` failures and `RESULT: FAILED` at HEAD `ee35493` (the gate PASSed at the B3F baseline). The doctrine requires `cross_ref` arrays to be bidirectional. This is a **registry fix** (the registry is the canonical, append-only source — TRACEABILITY is read-only on it), not a doc-generation issue: append `"DC-LEDGER-08"` to the `cross_ref` arrays of `DC-VAL-06`, `DC-TXV-06`, `DC-LEDGER-07`, `T-DET-01`, and `CN-LEDGER-07`, then rerun the gate. Surfaced as a blocking item in §G #18.

**Status:** cluster work closed (`ee35493`). CE-B4 mechanical evidence reproducible via `cargo test -p ade_codec --test conway_cert_decode_complete`, `cargo test -p ade_ledger delegation::tests rules::tests`, `cargo test -p ade_ledger --test cert_state_corpus`, and `bash ci/ci_check_forbidden_patterns.sh` (PASS, exit 0, verified during this refresh). The real epoch-576 cert-state oracle remains the one open obligation, environment-blocked.

### A000. PHASE4-B3 close (+ PHASE4-B3F follow-up) — 2 new `DC-TXV-*` rules, 4 strengthenings, 2 new CI scripts; the DC-TXV-06 CI-grep-gate gap is now CLOSED

Cluster PHASE4-B3 (Conway deposit/withdrawal value conservation) closed with the following registry deltas. **One new CI script landed** (`ci/ci_check_deposit_param_authority.sh`, discharging `DC-TXV-07`). The B3 close **closes the prior §H value-conservation completeness gap**: the B2 cert/withdrawal early-out fail-open in `check_conway_coin_conservation` is removed and the full preservation-of-value equation now runs for cert/withdrawal-bearing Conway txs. **PHASE4-B3F follow-up** then landed the second CI script `ci/ci_check_conway_cert_classification_closed.sh`, which flips `DC-TXV-06` `partial -> enforced` (the previously-tracked CI-grep-gate gap is now CLOSED), and strengthened `DC-VAL-06` with two decoder-strictness regressions.

**New rules (`DC-TXV-06`, `DC-TXV-07` within DC):**

| Rule | Status | Code authority | Discharging CI |
|---|---|---|---|
| `DC-TXV-06` | **enforced** (PHASE4-B3F) | `ade_codec::conway::cert` (`decode_conway_certs`, `CodecError::UnknownCertTag`, `RemovedInConway`; B3F: rejects trailing bytes + bounds prealloc) + `ade_types::conway::cert` (`ConwayCert`/`CertDisposition`/`DepositEffect`/`CoinSource`) + `ade_ledger::cert_classify::classify` (compiler-exhaustive `match`) + 12 named tests | `ci_check_conway_cert_classification_closed.sh` |
| `DC-TXV-07` | enforced | `ade_ledger::pparams` (`ConwayOnlyDepositParams`/`ConwayDepositParams`) + `ade_ledger::state` (`conway_deposit_params`/`conway_deposit_view` fail-fast) + `ade_ledger::fingerprint` (Conway-gated fold) | `ci_check_deposit_param_authority.sh` |

**Strengthenings (existing rules):**

| Rule | Action | PHASE4-B3 contribution |
|---|---|---|
| `T-CONSERV-01` | enforced (strengthened); `strengthened_in += ["PHASE4-B3"]` | full Conway preservation-of-value equation for cert/withdrawal-bearing txs (early-out removed) via `conway.rs::check_conway_coin_conservation`; tests `conway_conservation_full` + adversarial + positive-synthetic; `ci_check_ledger_determinism.sh` covers the path |
| `CN-LEDGER-07` | declared (strengthened); `strengthened_in += ["PHASE4-B3"]` | de-facto via `T-CONSERV-01`; registry `ci_script` still empty (rule remains `declared`) |
| `DC-VAL-06` | enforced (strengthened); `strengthened_in += ["PHASE4-B1", "PHASE4-B2", "PHASE4-B3", "PHASE4-B3F"]` | cert/withdrawal early-out fail-open removed; §9.1 reject-precedence deterministic; `conservation_early_out_removed` regression test; B3F: decoder rejects trailing bytes (`CodecError::TrailingBytes`) + bounds preallocation; tests `trailing_bytes_after_cert_array_rejected`, `huge_array_count_rejects_without_overallocating` |
| `DC-TXV-03` | enforced (strengthened); `strengthened_in += ["PHASE4-B2", "PHASE4-B3"]` | B3 positive (real + synthetic) and adversarial conservation corpora added to the verdict-agreement surface |

**New CI scripts (2):**

| CI script | Rule-ID anchor | Rules linked from CI row |
|---|---|---|
| `ci/ci_check_deposit_param_authority.sh` (PHASE4-B3) | DC-TXV-07 | `DC-TXV-07` |
| `ci/ci_check_conway_cert_classification_closed.sh` (PHASE4-B3F) | DC-TXV-06 | `DC-TXV-06` |

**THE PRIOR DC-TXV-06 CI-GREP-GATE GAP IS NOW CLOSED (PHASE4-B3F).** `DC-TXV-06` is now `status = "enforced"`: PHASE4-B3F added the dedicated grep-gate `ci/ci_check_conway_cert_classification_closed.sh`, which defends the closed/total cert-deposit classification surface against regression. The gate fails CI on (a) `#[non_exhaustive]` or an open-tail `Other`/`Unknown` variant on `ConwayCert`/`CertDisposition`/`DepositEffect`/`CoinSource` in `crates/ade_types/src/conway/cert.rs`; (b) a missing `CodecError::UnknownCertTag` reject or any catch-all decoder arm constructing a `ConwayCert` (single-line or block-form) in `crates/ade_codec/src/conway/cert.rs`; (c) a `_ =>` wildcard arm in `crates/ade_ledger/src/cert_classify.rs`. The gate PASSES at HEAD `193d2fc` (`PASS: Conway cert classification is closed and total (DC-TXV-06)`, exit 0). The classification no longer rests on compiler-exhaustive `match` + tests alone — it is CI-enforced. The only residue: `crates/ade_types/src/conway/cert.rs` is still outside `ci_check_consensus_closed_enums.sh` `TARGETS`, but the dedicated gate covers the closure, so `DC-TXV-06` is genuinely enforced — not an overclaim.

**The constitution-coverage gate PASSES.** `ci/ci_check_constitution_coverage.sh` exits 0 (`RESULT: PASS`, 170 entries) at HEAD `193d2fc` — verified by running it during this B3F refresh. The B3 registry entries are internally consistent:

1. **`DC-TXV-06` is now `status = "enforced"`** with `ci_script = ci/ci_check_conway_cert_classification_closed.sh` (PHASE4-B3F) — the gate's non-empty-`ci_script`-on-`enforced` requirement is satisfied.
2. **`DC-TXV-06` and `DC-TXV-07` carry `cluster = "CL-LEDGER-VERDICT"`**, which is in the gate's allowed cluster set.
3. **`DC-TXV-06` is now `status = "enforced"`** with a non-empty `ci_script` (`ci_check_conway_cert_classification_closed.sh`); the gate's `enforced`-entry requirement is satisfied.
4. **Cross_refs are bidirectional.** `DC-TXV-06`/`DC-TXV-07` reference `T-CONSERV-01`, `CN-LEDGER-07`, `DC-VAL-06`, `DC-TXV-05`, `T-DET-01`, and each other; those target rules list `DC-TXV-06`/`DC-TXV-07` back in their `cross_ref` arrays (verified in `docs/ade-invariant-registry.toml`).

With PHASE4-B3F the DC-TXV-06 CI grep-gate is **landed and CLOSED** — `status = "enforced"` is now the correct classification. There is **no remaining enforcement gap** in the B3/B3F cert-classification + value-conservation surface; every B3/B3F rule (`DC-TXV-06`, `DC-TXV-07`, `T-CONSERV-01`, `DC-VAL-06`, `DC-TXV-03`) carries a discharging CI script.

**Status:** cluster work closed; PHASE4-B3F follow-up closed; **constitution-coverage gate PASSES**. The DC-TXV-06 CI grep-gate is **landed (no longer a follow-up)**. CE-B3/B3F enforcement evidence reproducible via `cargo test -p ade_ledger --test conway_conservation_full --test conway_conservation_adversarial --test conway_conservation_positive_synthetic`, `cargo test -p ade_codec --test conway_cert_classification`, `cargo test -p ade_ledger cert_classify::tests pparams::tests fingerprint::tests`, `bash ci/ci_check_deposit_param_authority.sh`, `bash ci/ci_check_conway_cert_classification_closed.sh` (PASS, exit 0), and `bash ci/ci_check_constitution_coverage.sh` (PASS, 170 entries, exits 0).

### A00. PHASE4-B2 close — 5 new `DC-TXV-*` rules + 2 `DC-MEM-*` flips + 6 strengthenings + 0 new CI scripts

Cluster PHASE4-B2 (tx validity agreement; the body authority's per-tx half) closed with the following registry deltas. All `DC-TXV-*` rules are `status = "enforced"`; **no new CI script landed** — instead the existing `ci/ci_check_consensus_closed_enums.sh` was *extended* to scan `ade_ledger/src/tx_validity/` and `ade_ledger/src/mempool/`, and the previously-empty `ci_script` on the enforced `DC-CONS-*` / `DC-VAL-*` / new `DC-TXV-*` rules was populated with it, which flips `ci/ci_check_constitution_coverage.sh` to **PASS**.

**New rules (`DC-TXV-*` sub-family within DC):**

| Rule | Status | Code authority | Discharging CI |
|---|---|---|---|
| `DC-TXV-01` | enforced | `tx_validity/{transition,phase1}.rs` | `ci_check_consensus_closed_enums.sh` + `ci_check_forbidden_patterns.sh` |
| `DC-TXV-02` | enforced | `tx_validity/{transition,phase1}.rs` (phase-1 ∧ phase-2, fail-fast) | `ci_check_consensus_closed_enums.sh` |
| `DC-TXV-03` | enforced | `testkit/tx_validity/` positive + adversarial harness | `ci_check_consensus_closed_enums.sh` (no dedicated corpus gate — see §G) |
| `DC-TXV-04` | enforced | `tx_validity/{transition,verdict}.rs` (total evolution) | `ci_check_consensus_closed_enums.sh` |
| `DC-TXV-05` | enforced | `tx_validity/{required_signers,witness}.rs` + `rules.rs` | `ci_check_consensus_closed_enums.sh` |

**`DC-MEM-*` flips (`declared -> enforced`):**

| Rule | Status | Code authority | Discharging CI |
|---|---|---|---|
| `DC-MEM-01` | enforced | `mempool/admit.rs` (admission verdict == `tx_validity` verdict) | `ci_check_consensus_closed_enums.sh` (`AdmitOutcome` closed) |
| `DC-MEM-02` | enforced | `mempool/policy.rs` (deterministic ordering; policy never changes a verdict) | `ci_check_consensus_closed_enums.sh` (`OrderPolicy` closed) |

**Strengthenings (existing rules):**

| Rule | Action | PHASE4-B2 contribution |
|---|---|---|
| `DC-LEDGER-05` | partial (strengthened); `strengthened_in += ["PHASE4-B2"]` | added `tx_validity/witness.rs` (Conway vkey-witness binding) + `tx_witness_closure.rs` tests |
| `CN-LEDGER-09` | partial (strengthened); `strengthened_in += ["PHASE4-B2"]` | `code_locus` and `tests` populated with `required_signers.rs` + `witness.rs` + `tx_witness_closure.rs` (was de-facto via DC-LEDGER-05) |
| `DC-VAL-06` | enforced (strengthened); `strengthened_in += ["PHASE4-B1", "PHASE4-B2"]` | added tx-path fail-closed checks (wrong-size vkey/sig, unresolvable input/collateral); `ci_script` populated with `ci_check_consensus_closed_enums.sh` |
| `DC-LEDGER-02` | partial (strengthened); `strengthened_in += ["PHASE4-B1", "PHASE4-B2"]` | added GREEN no-false-accept tx adversarial corpus (`tx_validity/adversarial.rs`) |
| `T-ENC-01` | partial (strengthened); `strengthened_in += ["PHASE4-B1", "PHASE4-B2"]` | added `tx_validity/phase1.rs` (`tx_id` over preserved body slice) + `tx_id_uses_preserved_bytes` test |
| `T-DET-01` | enforced (strengthened); `strengthened_in += ["PHASE4-N-B", "PHASE4-B1", "PHASE4-B2"]` | added tx verdict-surface replay (`testkit/tx_validity/`, `tx_verdict_stream_replays_identically`) |

**Documented deposit/refund value-conservation gap (known gap; see §H):**
PHASE4-B2-S4 found and fixed a real fail-open — the Conway state-backed path never checked coin-level value conservation, so `outputs + fee != inputs` was accepted as `Valid` at `track_utxo=true`. The fix (`crates/ade_ledger/src/conway.rs::check_conway_coin_conservation`) enforces `sum(inputs) == sum(outputs) + fee + donation` for **cert/withdrawal-free** txs. Full preservation-of-value with deposit/refund/withdrawal accounting is **deferred**: the check conservatively skips txs carrying certificates or withdrawals (so it never false-rejects), which means a mis-accounted deposit/refund/withdrawal tx is not yet caught on value grounds. This is a tx-validity-completeness item against `T-CONSERV-01` / `CN-LEDGER-07`. Recorded in `MEMORY.md::project_value_conservation_deposits_deferred.md` and in `crates/ade_ledger/src/conway.rs`. Surfaced in §G and §H.

**Status:** closed. CE-B2 evidence reproducible via
`cargo test -p ade_ledger --test tx_witness_closure`,
`--test tx_validity_compose`,
`--test tx_validity_positive_corpus`,
`--test tx_validity_adversarial_corpus`,
`--test mempool_admission`; `ci/ci_check_constitution_coverage.sh` PASSES (168 entries).

### A0. PHASE4-B1 close — 6 new `DC-VAL-*` rules + 6 strengthenings + 0 new CI scripts

Cluster PHASE4-B1 (full block validity agreement; dependency root for B2/B3) closed with the following registry deltas. All `DC-VAL-*` rules are `status = "enforced"` via test surface; **no new CI script landed**.

**New rules (`DC-VAL-*` sub-family within DC):**

| Rule | Status | Code authority | Discharging CI |
|---|---|---|---|
| `DC-VAL-01` | enforced | `consensus_input_extract.rs` + `consensus_view.rs` + `consensus/ledger_view.rs` | _(none — structural via `ci_check_forbidden_patterns.sh` + `ci_check_no_float_in_consensus.sh`)_ |
| `DC-VAL-02` | enforced | `block_validity/{transition,header_input}.rs` | _(none — structural via `ci_check_consensus_closed_enums.sh`)_ |
| `DC-VAL-03` | enforced | `block_validity/transition.rs` | _(none)_ |
| `DC-VAL-04` | enforced | `block_validity/` + `validity/replay.rs` + `validity/adversarial.rs` | _(none — positive + adversarial corpus tests only)_ |
| `DC-VAL-05` | enforced | `block_validity/transition.rs` | _(none — structural via `ci_check_consensus_closed_enums.sh`)_ |
| `DC-VAL-06` | enforced | `block_validity/verdict.rs` + `consensus/kes_check.rs` + `validity/adversarial.rs` | _(none — see §G, fail-open-pattern ban not text-scanned)_ |

**Strengthenings (existing rules):**

| Rule | Action | PHASE4-B1 contribution |
|---|---|---|
| `T-DET-01` | enforced (strengthened); `strengthened_in += ["PHASE4-B1"]` | added `crates/ade_testkit/src/validity/replay.rs` + `verdict_stream_replays_identically` (verdict-surface replay over Conway-576) |
| `DC-CRYPTO-01` | enforced (strengthened); `strengthened_in += ["PHASE4-B1"]` | Praos single-VRF input + leader/nonce range extension; op-cert-signable format fix; `kes_check.rs` wiring; 14/14 real Conway headers pass leader check |
| `DC-CONSENSUS-02` | partial (strengthened); `strengthened_in += ["PHASE4-N-B", "PHASE4-B1"]` | added ledger-side `consensus_view.rs` `LedgerView` projection + `consensus_view` tests |
| `DC-LEDGER-02` | partial (strengthened); `strengthened_in += ["PHASE4-B1"]` | added GREEN no-false-accept adversarial corpus (`adversarial.rs`, `no_mutation_is_ever_valid`) |
| `CN-CONS-04` | declared (strengthened); `strengthened_in += ["PHASE4-N-B", "PHASE4-B1"]` | recomputed segwit body-hash binding (`block_validity/{transition,header_input}.rs`) + VRF-keyhash binding + KES authentication (`kes_check.rs`) |
| `CN-EPOCH-01` | partial (strengthened); `strengthened_in += ["PHASE4-N-B", "PHASE4-B1"]` | added ledger-side `consensus_view.rs` epoch-projection + `consensus_view` tests |

**Documented Conway body-witness verification gap (known gap, fenced; to close in B2):**
The adversarial corpus mutator M6 (forge a witness, then patch the header `body_hash`
to the mutated body) lands fail-closed at `HeaderInvalid` (KES) because the KES
signature signs the header body — which includes `body_hash` — and the header
pipeline runs before the body authority. So a forged spend cannot pass
`block_validity` by any byte mutation of a real block. **However**, when the inner
block is fed *directly* to `ade_ledger::rules::apply_block_with_verdicts` for a
Conway block (bypassing the header, which a real chain never does), the fabricated
vkey witness is **not** rejected — the Conway body authority runs structural +
Plutus dispatch without per-tx Ed25519 vkey-witness verification. This is **not
reachable through `block_validity`** (the header fences it), so it is not a CE-B1-4
false-accept, but it is a genuine pre-release finding against the witness-verification
invariant (`DC-LEDGER-05` / `CN-LEDGER-09`). Recorded in
`crates/ade_testkit/src/validity/adversarial.rs` (M6 doc comment) and
`corpus/validity/adversarial/README.md` (§ "Separate, deeper note"). **Scheduled for
B2 (tx validity).** Not double-recorded as a registry gap because it lives outside the
`block_validity` authority surface B1 closed; surfaced here in §G.

**Discrepancy note (prompt vs registry):** the close summary listed `T-ENC-01` among
the PHASE4-B1 strengthenings, but the registry `T-ENC-01` entry carries no
`PHASE4-B1` in `strengthened_in` (it remains `status = "partial"` with no
`strengthened_in` field). No change was made to the `T-ENC-01` rule entry above. See §G #6.

**Status:** closed. CE-B1 evidence reproducible via
`cargo test -p ade_ledger --test block_validity_compose`,
`--test block_validity_positive_corpus`, `--test block_validity_adversarial_corpus`,
`cargo test -p ade_core --test praos_header_validate`,
`cargo test -p ade_ledger --test consensus_view`.

### A. PHASE4-N-B close — 8 new rules + 11 strengthenings + 4 new CI scripts

Cluster PHASE4-N-B (Praos consensus runtime, 10 slices S-B1..S-B10) closed with the following registry deltas. All four CI scripts run green at HEAD `b9ff041`.

**New rules (DC-CONS-* sub-family within DC):**

| Rule | Status | Code authority | Discharging CI |
|---|---|---|---|
| `DC-CONS-03` | enforced | `fork_choice.rs` + `candidate.rs` + `candidate_fragment.rs` | `ci_check_no_density_in_fork_choice.sh` + `ci_check_no_chaindb_in_consensus_blue.sh` + `ci_check_no_float_in_consensus.sh` + `ci_check_consensus_closed_enums.sh` |
| `DC-CONS-04` | enforced | `praos_state.rs` + `encoding.rs` + `nonce.rs` + `op_cert.rs` + `header_validate.rs` + `header_summary.rs` + `events.rs` + `errors.rs` | `ci_check_consensus_closed_enums.sh` |
| `DC-CONS-05` | enforced | `rollback.rs` + `events.rs` | `ci_check_consensus_closed_enums.sh` + `ci_check_no_chaindb_in_consensus_blue.sh` |
| `DC-CONS-06` | enforced | `rollback.rs` | `ci_check_consensus_closed_enums.sh` |
| `DC-CONS-07` | enforced | `era_schedule.rs` + `genesis_parser.rs` (RED) | `ci_check_no_chaindb_in_consensus_blue.sh` |
| `DC-CONS-08` | enforced | `era_schedule.rs` | `ci_check_no_float_in_consensus.sh` |
| `DC-CONS-09` | enforced | `era_schedule.rs` + `leader_schedule.rs` | (test surface only — no dedicated CI) |
| `DC-CONS-10` | enforced | `op_cert.rs` + `praos_state.rs` + `errors.rs` + `header_validate.rs` | (test surface only — no dedicated CI) |

**Strengthenings (existing rules):**

| Rule | Action | Phase4-N-B contribution |
|---|---|---|
| `DC-CONSENSUS-01` | partial → enforced; `strengthened_in += ["PHASE4-N-B"]` | `code_locus` and `tests` populated with the full N-B fork-choice / rollback / orchestrator / live-peer surface; three CI scripts wired |
| `DC-CONSENSUS-02` | declared → partial; `strengthened_in += ["PHASE4-N-B"]` | `code_locus` and `tests` populated with leader-schedule + ledger-view + vrf-cert surface |
| `DC-CRYPTO-01` | enforced (strengthened); `strengthened_in += ["PHASE4-N-B"]` | augmented `tests` with consensus-side `verify_vrf_cert_*` battery; typed 41-byte VRF input added in S-B3 |
| `CN-CRYPTO-02` | declared → partial; `strengthened_in += ["PHASE4-N-B"]` | `code_locus` = `vrf_cert.rs`; tests = `vrf_input_*` battery |
| `CN-EPOCH-01` | declared → partial; `strengthened_in += ["PHASE4-N-B"]` | `code_locus` and `tests` populated with leader-schedule + ledger-view surface |
| `DC-EPOCH-02` | enforced (strengthened); `strengthened_in += ["PHASE4-N-B"]` | augmented `code_locus` and `tests` with consensus-side `EraSchedule` + slot/era/time translation |
| `CN-CONS-01..05` | each `code_locus`/`tests`/`ci_script` populated; `strengthened_in += ["PHASE4-N-B"]` | `CN-CONS-01` flipped `declared → partial`; the rest remain `declared` (release-tier obligations) with structural CI anchors |
| `DC-CORE-01` | declared (strengthened); `strengthened_in += ["PHASE4-N-A", "PHASE4-N-B"]` | `code_locus` augmented with `ade_runtime::consensus::chain_selector` + `ade_core_interop`; `ci_check_no_chaindb_in_consensus_blue.sh` wired as a complementary gate |
| `T-DET-01` | enforced (strengthened); `strengthened_in += ["PHASE4-N-B"]` | consensus subset closed via `consensus_stream_replay` (CE-N-B-5); added `ci_check_consensus_closed_enums.sh` |
| `T-CORE-02` | enforced (strengthened); `strengthened_in += ["PHASE4-N-B"]` | added `ci_check_no_float_in_consensus.sh` as a narrowly-scoped Rational-only gate over the consensus tree |

**New CI scripts (4):**

| CI script | Rule-ID anchor (per header comment) | Rules linked from CI row |
|---|---|---|
| `ci/ci_check_no_chaindb_in_consensus_blue.sh` | DC-CORE-01 + DC-CONS-07 | `DC-CORE-01`, `DC-CONS-03`, `DC-CONS-05`, `DC-CONS-07`, `DC-CONSENSUS-01` |
| `ci/ci_check_no_float_in_consensus.sh` | T-CORE-02 + DC-CONS-{07,08,09} | `T-CORE-02`, `DC-CONS-03`, `DC-CONS-08`, `CN-CONS-05` |
| `ci/ci_check_consensus_closed_enums.sh` | DC-CONS-04 + DC-CONS-10 + T-DET-01 | `T-DET-01`, `DC-CONS-03`, `DC-CONS-04`, `DC-CONS-05`, `DC-CONS-06`, `DC-CONSENSUS-01`, `CN-META-02` |
| `ci/ci_check_no_density_in_fork_choice.sh` | DC-CONS-03 | `DC-CONS-03`, `CN-CONS-01`, `CN-CONS-02`, `CN-CONS-03`, `CN-CONS-05`, `DC-CONSENSUS-01` |

**Status:** closed. Cluster CE-N-B-1 through CE-N-B-6 evidence reproducible via `cargo test -p ade_core --test fork_choice_corpus` / `rollback_corpus` / `hfc_schedule_corpus` / `leader_schedule_corpus`, `cargo test -p ade_testkit --test consensus_stream_replay`, and `cargo test -p ade_core_interop --test live_consensus_session --release -- --ignored` (operator-driven against pinned cardano-node 10.6.2).

### A2. PHASE4-N-A close — registry strengthenings + new IDs (carried forward from prior refresh)

Cluster PHASE4-N-A (Ouroboros mini-protocols, 11 protocols across N2N + N2C) closed by populating `tests` on four pre-existing registry rules (`T-ENC-03`, `CN-WIRE-07`, `DC-PROTO-02`, `DC-PROTO-05`) and by adding two new IDs (`DC-CORE-01`, `DC-PROTO-06`). Two new CI scripts landed: `ci/ci_check_no_async_in_blue.sh` (S-A1) and `ci/ci_check_ce_n_a_5_proof.sh` (cluster close). See HEAD_DELTAS §5 for the full table.

### A3. Phase 4 N-D CI closure — CLOSED at `78da6c9` (carried forward)

Commit `78da6c9` added three CI scripts (`ci_check_chaindb_contract.sh`, `ci_check_recovery_contract.sh`, `ci_check_chaindb_crash_safety.sh`) and flipped nine recovery/storage rules `declared → enforced`. See HEAD_DELTAS §5 for the full table.

### B. `DC-STORE-04` Tier 5 divergence (documented, not a gap)

`DC-STORE-04` literally describes cardano-node's three-DB architecture
(ImmutableDB + VolatileDB + LedgerDB). Ade has deliberately diverged
per CE-79 Tier 5 (intentional-divergence on non-hash-critical
operator-facing surfaces, per `MEMORY.md::feedback_diverge_on_internal_surfaces.md`):
a single redb-backed store with logical separation via key prefixes
(`slots_by_hash`, `hashes_by_slot`, `snapshots_by_slot`) under
`crates/ade_runtime/src/chaindb/persistent.rs`.

The **semantic guarantees** that DC-STORE-04 was designed to deliver —
append-only finalized data and atomic snapshots — survive in Ade and
are now mechanically enforced under `DC-STORE-02` and `DC-STORE-03`
respectively, both flipped `declared → enforced` in `78da6c9`. The rule
remains `status = "declared"` in the registry rather than `enforced`
because Ade does not implement the literal three-DB topology DC-STORE-04
names; per IDD discipline, rules are append-only and may only be
strengthened, not weakened or repointed. A future `DC-STORE-04A`
strengthening (or a deprecation event paired with a major version bump)
is the legal way to reclassify.

This is recorded in the registry as a comment block above the
`DC-STORE-04` entry (commit `78da6c9`), in the `DC-STORE-04` row above,
and here in §B. **It does not read as an enforcement gap.** It is a
documented Tier 5 divergence with the semantic guarantee preserved
elsewhere and mechanically enforced.

### C. Registry entries that point at moved/renamed artifacts

- The registry path itself was renamed from `docs/constitution_registry.toml`
  to `docs/ade-invariant-registry.toml`. The header comment in
  `docs/ade-invariant-registry.toml` still reads "Constitution Registry"
  and still names "the project constitution" as authority; this is
  consistent with the IDD-canonicalization migration recorded in
  HEAD_DELTAS §5.
- No rule in the registry currently names a path that does not exist
  in the tree, except where the field is empty (which is recorded
  above as a gap rather than a misreference).

### D. CODEMAP cross-check

Every BLUE / GREEN / RED module named in a Code row above resolves to a
module in `docs/ade-CODEMAP.md` — except the PHASE4-N-B additions which
landed after the CODEMAP `5b70bee` regen baseline:

- `crates/ade_codec/src/preserved.rs` — CODEMAP §BLUE → `ade_codec`.
- `crates/ade_crypto/src/{lib,blake2b,...}.rs` — CODEMAP §BLUE → `ade_crypto`.
- `crates/ade_ledger/src/{rules,fingerprint,utxo,value,byron,epoch,governance,witness,scripts,plutus_eval,hfc,error,state}.rs` — CODEMAP §BLUE → `ade_ledger`.
- `crates/ade_plutus/src/{evaluator,script_context,tx_eval,cost_model}.rs` — CODEMAP §BLUE → `ade_plutus`.
- `crates/ade_network/src/{codec,handshake,chain_sync,block_fetch,tx_submission,keep_alive,peer_sharing,n2c,mux}/` — CODEMAP §BLUE → `ade_network (BLUE submodules)` (added in the post-PHASE4-N-A CODEMAP refresh).
- `crates/ade_runtime/src/{recovery,chaindb}.rs` — CODEMAP §RED → `ade_runtime`.
- `crates/ade_runtime/tests/stress_kill_harness.rs` — CODEMAP §RED → `ade_runtime`.
- `crates/ade_testkit/src/harness/*.rs` — CODEMAP §GREEN → `ade_testkit`.

**PHASE4-N-B additions not yet enumerated in CODEMAP:**
- `crates/ade_core/src/consensus/` — 16 modules: `candidate.rs`, `encoding.rs`, `era_schedule.rs`, `errors.rs`, `events.rs`, `fork_choice.rs`, `header_summary.rs`, `header_validate.rs`, `leader_schedule.rs`, `ledger_view.rs`, `mod.rs`, `nonce.rs`, `op_cert.rs`, `praos_state.rs`, `rollback.rs`, `vrf_cert.rs`. The `ade_core` crate is currently listed as an empty BLUE placeholder in CODEMAP §BLUE.
- `crates/ade_runtime/src/consensus/` — 3 modules: `chain_selector.rs` (GREEN orchestrator under a RED crate), `candidate_fragment.rs`, `genesis_parser.rs` (RED parser).
- `crates/ade_core_interop/` — new RED workspace crate hosting `src/lib.rs` (Orchestrator surface) + `src/bin/live_consensus_session.rs` + `tests/live_consensus_session.rs`.

**PHASE4-B1 additions not yet enumerated in CODEMAP, SEAMS, or HEAD_DELTAS:**
- `crates/ade_ledger/src/block_validity/` — 5 modules (BLUE): `mod.rs`, `verdict.rs` (closed `Valid`/`Invalid` + `FieldKind`/`FieldError` taxonomy), `transition.rs` (header ∧ body composition; total state evolution), `header_input.rs` (body-hash binding), `encoding.rs`.
- `crates/ade_ledger/src/{consensus_input_extract,consensus_view}.rs` — 2 BLUE modules (pure canonical-input projection + ledger-side `LedgerView` impl).
- `crates/ade_core/src/consensus/kes_check.rs` — extended for B1 (`expect_size` fail-closed guard + VRF-keyhash binding + KES authentication). The CODEMAP `ade_core::consensus` row above lists 16 modules; `kes_check.rs` is the 17th and is not yet enumerated.
- `crates/ade_testkit/src/validity/` — 5 GREEN modules: `mod.rs`, `corpus.rs`, `ledger_view.rs`, `replay.rs` (positive-corpus harness), `adversarial.rs` (M1–M6 mutators).
- New test files: `crates/ade_ledger/tests/{block_validity_compose,block_validity_positive_corpus,block_validity_adversarial_corpus,consensus_view}.rs`; `crates/ade_core/tests/praos_header_validate.rs`.

**PHASE4-B2 additions not yet enumerated in CODEMAP, SEAMS, or HEAD_DELTAS:**
- `crates/ade_ledger/src/tx_validity/` — 7 BLUE modules: `mod.rs`, `verdict.rs` (closed `TxValidityVerdict`/`TxRejectClass`/`TxValidityError`), `transition.rs` (phase-1 ∧ phase-2 composition; total state evolution), `phase1.rs` (decode + state-backed checks), `required_signers.rs` (closed era-versioned `SignerSource` enumeration), `witness.rs` (fail-closed Ed25519 coverage over the preserved body hash), `encoding.rs`.
- `crates/ade_ledger/src/mempool/` — 3 BLUE modules: `mod.rs`, `admit.rs` (closed `AdmitOutcome`; admission == `tx_validity`), `policy.rs` (closed `OrderPolicy`; deterministic shedding).
- `crates/ade_ledger/src/conway.rs::check_conway_coin_conservation` — new coin-conservation check (PHASE4-B2-S4); conservative scope (see §H).
- `crates/ade_testkit/src/tx_validity/` — GREEN modules: `mod.rs`, `extract.rs`, `valid_synthetic.rs`, `adversarial.rs` (tx positive/adversarial corpus harness).
- New test files: `crates/ade_ledger/tests/{tx_witness_closure,tx_validity_compose,tx_validity_positive_corpus,tx_validity_adversarial_corpus,mempool_admission}.rs`.

**PHASE4-B3 additions (CODEMAP regenerated at HEAD `193d2fc` — these should now be present; verify on the next read):**
- `crates/ade_ledger/src/cert_classify.rs` — BLUE module: closed/total Conway certificate-deposit classification (`classify`, compiler-exhaustive `match`).
- `crates/ade_codec/src/conway/cert.rs` — BLUE: `decode_conway_certs` + closed `ConwayCert` grammar; `crates/ade_codec/src/error.rs` adds `CodecError::UnknownCertTag` / `RemovedInConway`.
- `crates/ade_types/src/conway/cert.rs` — BLUE: closed `ConwayCert` / `CertDisposition` / `DepositEffect` / `CoinSource` sum types.
- `crates/ade_ledger/src/pparams.rs` — BLUE: `ConwayOnlyDepositParams` / `ConwayDepositParams` (canonical deposit-param value); `crates/ade_ledger/src/state.rs` gains `conway_deposit_params` + `conway_deposit_view`; `crates/ade_ledger/src/error.rs` gains `ValidationEnvironmentError::MissingConwayDepositParams`.
- `crates/ade_ledger/src/conway.rs::check_conway_coin_conservation` — full preservation-of-value equation (early-out removed); `crates/ade_ledger/src/fingerprint.rs::fingerprint_pparams` Conway-gated deposit fold.
- New test files: `crates/ade_ledger/tests/{conway_conservation_full,conway_conservation_adversarial,conway_conservation_positive_synthetic}.rs`; `crates/ade_codec/tests/conway_cert_classification.rs`. New corpus: `corpus/conway_certs/{classification_table.md,tags.json}`.

The CODEMAP regenerated at HEAD `193d2fc` (per the close brief) should carry these modules. Earlier baselines listed `block_validity` but **no `tx_validity`, `mempool`, or `cert_classify`** — confirm the fresh CODEMAP picks up the full B2 + B3 BLUE authority surface.

CODEMAP refresh is required to pick up these modules and to update the
CI-script count (19 → 25; +4 PHASE4-N-B + 2 PHASE4-N-A counted in the
prior refresh). The next `/codemap` regeneration will resolve this.
This is a known doc-version skew, not a code drift.

### E. HEAD_DELTAS §5 cross-check

HEAD_DELTAS §5 at HEAD (last regen at `56bfa7b`) catalogues CI scripts
up to the PHASE4-N-A close. **§5 does not yet list the four new CI
scripts added during PHASE4-N-B** — `ci_check_no_chaindb_in_consensus_blue.sh`,
`ci_check_no_float_in_consensus.sh`, `ci_check_consensus_closed_enums.sh`,
`ci_check_no_density_in_fork_choice.sh`. This is a known doc skew —
HEAD_DELTAS is regenerated on phase boundaries (per
`.idd-config.json::head_deltas_baseline` doctrine), and the next
`/head-deltas` run will pick them up. PHASE4-B1 **added no CI script**, so §5
has no missing-script skew from B1; but HEAD_DELTAS does not yet narrate the
`block_validity` authority surface or the `DC-VAL-*` rules — same regen-on-phase-boundary skew.
PHASE4-B2 likewise **added no CI script** (it *extended* `ci_check_consensus_closed_enums.sh`
and populated previously-empty `ci_script` fields), so §5 has no missing-script skew from B2;
but the §5 row for `ci_check_consensus_closed_enums.sh` still describes only the B1 extension
(`ade_core/src/consensus/` + `ade_ledger/src/block_validity/`) and does not yet reflect the B2
extension to `ade_ledger/src/tx_validity/` + `ade_ledger/src/mempool/`. HEAD_DELTAS also does
not yet narrate the `tx_validity` / `mempool` authority surface or the `DC-TXV-*` / `DC-MEM-*`
rules — same regen-on-phase-boundary skew. The next `/head-deltas` run resolves both.

PHASE4-B3 **added one CI script** — `ci/ci_check_deposit_param_authority.sh`
(discharges `DC-TXV-07`). PHASE4-B3F **added a second** —
`ci/ci_check_conway_cert_classification_closed.sh` (discharges `DC-TXV-06`,
flipping it `partial -> enforced`) — bringing the total under `ci/ci_check_*.sh`
to **27**. HEAD_DELTAS §5 does not yet list either (regen-on-phase-boundary
skew); the next `/head-deltas` run resolves this.

All 27 active CI scripts under `ci/ci_check_*.sh` appear in at least one
rule's CI row above except the three repo-hygiene scripts listed in
"Repo-wide CI scripts not yet pinned to a rule." No CI script is
orphaned. The prior **`DC-TXV-06` rule-without-CI gap is now CLOSED** — PHASE4-B3F
landed `ci/ci_check_conway_cert_classification_closed.sh`, the rule is `enforced`,
and the constitution-coverage gate PASSES.

**Test cross-check:** every test function named in a Tests row above is
either (a) a unit-test name verified to exist in the codebase
(`consensus::*::tests::*` under `crates/ade_core/src/consensus/`), (b)
an integration-test function in `crates/ade_core/tests/` (10 files,
verified), `crates/ade_testkit/tests/consensus_stream_replay.rs`
(verified), `crates/ade_core_interop/tests/live_consensus_session.rs`
(verified), or `crates/ade_runtime/tests/genesis_parser_corpus.rs`
(verified), or (c) a pre-existing test from prior cluster work. No
test name in a Tests row was guessed — every name is reachable by
`grep -rE "fn <name>"` in the listed file.

### F. SEAMS cross-check

`docs/ade-SEAMS.md` exists at HEAD (`5b70bee` baseline). No closed
registry referenced from rule Source fields above was found to be
missing from SEAMS §3 in the spot checks performed during this refresh.
A full SEAMS regeneration is recommended after the next
`/seams` invocation to pick up the PHASE4-N-B closed enums
(`ChainEvent`, `ChainSelectionReject`, `HeaderValidationError`,
`OpCertCounterError`, `NonceError`, the closed `NonceInput` /
`RollBackRequest` / `StreamInput` input enums, the typed
`EraSchedule` and `BootstrapAnchorHash` values) as closed registries, plus the
PHASE4-B1 closed taxonomies in `crates/ade_ledger/src/block_validity/verdict.rs`
(the closed `Valid`/`Invalid` block verdict enum and the `FieldKind`/`FieldError`
fail-closed field taxonomy) — none of which appear in SEAMS §3 at the current baseline.
**PHASE4-B2 staleness:** SEAMS still lists the seam "B2 (tx validity)" and "N-E (mempool)"
as `candidate` / `proposed` future attach points — both are now *realized* in `ade_ledger`
(`tx_validity/` and `mempool/`), not deferred. SEAMS §3 also does not yet carry the new
PHASE4-B2 closed registries: `TxValidityVerdict` / `TxRejectClass` / `TxValidityError`,
the closed era-versioned `SignerSource` enum, `WitnessClosureError`, and the mempool
`AdmitOutcome` / `OrderPolicy`. A `/seams` regeneration is recommended to reclassify the
B2/N-E seams from candidate to closed and to register these enums.

### G. Open questions for the user

1. **`DC-CONS-09` and `DC-CONS-10` have no dedicated CI script.** Both
   are marked `status = "enforced"` via test surface only. Worth
   considering whether (a) the existing `ci_check_consensus_closed_enums.sh`
   already discharges them structurally (current implicit answer), or
   (b) a new narrow CI script — e.g. a corpus replay gate — should be
   added. No action taken in this refresh.
2. **`DC-CONSENSUS-02` registry status is `partial` with empty `ci_script`.**
   The leader-schedule corpus tests + structural CIs discharge it
   de-facto, but no dedicated `ci/ci_check_leader_schedule.sh` exists.
   Recommend adding one or accepting the de-facto discharge.
3. **`CN-CONS-04` (header/body binding) `code_locus` and `tests`
   populated but `ci_script` empty.** Same de-facto pattern as above —
   `ci_check_consensus_closed_enums.sh` keeps `HeaderValidationError`
   closed but no dedicated header-validate CI exists.
4. **CN-EPOCH-01 newly flipped to `partial`** with consensus-side
   evidence; ledger-side `epoch.rs`/`governance.rs` evidence also
   exists via `DC-LEDGER-04`. Consider unifying under a single CI gate
   or accepting the split.
5. **`ade_core` CODEMAP entry needs a substantial rewrite** — the
   "empty placeholder" classification is no longer accurate (16 BLUE
   consensus modules under `src/consensus/`, +`kes_check.rs` from B1). The
   next `/codemap` refresh will fix this, but the gap is worth flagging now.
6. **All six `DC-VAL-*` rules are `status = "enforced"` with empty
   `ci_script`.** They are proven by their test surface (positive +
   adversarial corpora, compose tests) and discharged structurally by
   `ci_check_consensus_closed_enums.sh` / `ci_check_forbidden_patterns.sh` /
   `ci_check_no_float_in_consensus.sh`, but **no dedicated
   `ci/ci_check_block_validity.sh` exists**. The CE-B1-4 no-false-accept
   property (`no_mutation_is_ever_valid`) and the 14/14 positive-corpus
   property (`all_corpus_blocks_valid`) currently have no CI gate. Recommend
   a single block-validity corpus CI gate, or accept the de-facto discharge.
7. **`DC-VAL-06`'s fail-open-pattern ban is not text-scanned.** The rule
   forbids `if X.len() == K { check } else { skip }` in BLUE validation, but
   no CI script greps for that pattern — it is proven only by the adversarial
   corpus. Strong candidate for a new `ci/ci_check_no_fail_open_len_guard.sh`.
8. **Documented Conway body-witness verification gap (B2 follow-up).** The
   Conway body authority (`apply_block_with_verdicts`) does not perform per-tx
   Ed25519 vkey-witness verification when reached directly (bypassing the
   header). Fenced by the header authority inside `block_validity`, so not a
   B1 false-accept, but a genuine pre-release finding against
   `DC-LEDGER-05` / `CN-LEDGER-09`. Scheduled for B2. Confirm the B2 cluster
   carries this as an entry-condition proof obligation.
9. **`T-ENC-01` strengthening discrepancy.** The PHASE4-B1 close summary
   named `T-ENC-01` as strengthened, but the registry entry shows no
   `PHASE4-B1` in `strengthened_in` and remains `partial`. Either the
   registry needs `strengthened_in += ["PHASE4-B1"]` (if B1 genuinely touched
   the canonical-encoding surface) or the close summary overstated. No edit
   was made to the `T-ENC-01` rule entry pending user judgment.
   **Resolved by PHASE4-B2:** the registry now carries `strengthened_in =
   ["PHASE4-B1", "PHASE4-B2"]` on `T-ENC-01` and a non-empty B1/B2 `code_locus`,
   so the discrepancy is closed — B1/B2 genuinely touched the canonical-encoding
   surface (block body hash; `tx_id` over preserved bytes). The rule remains
   `partial` (canonical-encoding coverage incomplete), not a regression.
10. **Deposit/refund value-conservation gap (B2 follow-up).** The Conway
    coin-conservation check (`check_conway_coin_conservation`) conservatively
    skips txs carrying certificates or withdrawals, so a mis-accounted
    deposit/refund/withdrawal tx is not yet rejected on value grounds — a
    real tx-validity-completeness gap on the bounty surface against
    `T-CONSERV-01` / `CN-LEDGER-07`. See §H. Confirm whether the follow-up
    (full preservation-of-value with deposit/refund/withdrawal accounting) is
    carried as an explicit entry-condition proof obligation on a future cluster.
11. **All five `DC-TXV-*` rules and both `DC-MEM-*` rules share one CI script.**
    `ci_check_consensus_closed_enums.sh` discharges them structurally (closed
    verdict / signer-source / admission taxonomies) but there is **no dedicated
    tx-validity corpus CI gate** — the no-false-accept property
    (`no_mutation_is_ever_valid`) and the 103/103 positive-corpus property
    (`all_corpus_txs_valid`) are proven by the test surface only. Recommend a
    single tx-validity corpus CI gate, or accept the de-facto discharge (mirrors
    the §G #6 / #7 block-validity situation).
12. **`DC-TXV-03` positive corpus runs at `track_utxo=false` (partial scope).**
    The positive/adversarial corpus harness exercises structural + witness
    closure but defers UTxO-dependent checks (the corpus blocks' consumed UTxOs
    are not all resolvable from the committed snapshot). Confirm whether closing
    the UTxO-resolved scope is a future-cluster obligation.
13. **`DC-LEDGER-05` and `CN-LEDGER-09` remain `partial` with no dedicated CI.**
    The Conway witness-closure surface is now real and tested, but the
    era-specific witness binding is discharged only structurally
    (`ci_check_consensus_closed_enums.sh` keeps the taxonomy closed). Consider a
    dedicated witness-binding CI gate.
14. **`DC-TXV-06` CI grep-gate — RESOLVED/CLOSED in PHASE4-B3F.** The prior
    tracked follow-up is closed: `ci/ci_check_conway_cert_classification_closed.sh`
    landed and flips `DC-TXV-06` `partial -> enforced`. The gate defends the closed/
    total cert-deposit classification surface — failing CI on `#[non_exhaustive]` or
    an open-tail `Other`/`Unknown` variant on `ConwayCert`/`CertDisposition`/
    `DepositEffect`/`CoinSource`, a catch-all decoder arm constructing a `ConwayCert`,
    or a `_ =>` wildcard in `classify`. Gate PASSES at HEAD `193d2fc` (exit 0).
    Residue (not a gap): `crates/ade_types/src/conway/cert.rs` remains outside
    `ci_check_consensus_closed_enums.sh` `TARGETS`, but the dedicated gate covers the
    closure — `DC-TXV-06` is genuinely `enforced`.
15. **`ci/ci_check_constitution_coverage.sh` PASSES at HEAD `193d2fc`** (verified by
    running the gate during this B3F refresh: exit 0, `RESULT: PASS`, 170 entries). The B3/B3F registry entries
    are consistent: (a) `DC-TXV-06`/`DC-TXV-07` carry `cluster = "CL-LEDGER-VERDICT"`,
    which is in the gate's allowed cluster set; (b) the cross_refs from DC-TXV-06/07
    to `T-CONSERV-01`, `CN-LEDGER-07`, `DC-VAL-06`, `DC-TXV-05`, `T-DET-01` (and
    between DC-TXV-06↔DC-TXV-07) **are bidirectional** — the target rules list the
    new IDs back in their `cross_ref` arrays. No registry-hygiene defect remains for
    these entries.
16. **`CN-LEDGER-07` is now strengthened (`strengthened_in += ["PHASE4-B3"]`) but
    remains `status = "declared"` with an empty `ci_script`.** Enforcement is
    de-facto via the `enforced` `T-CONSERV-01` (whose Conway path now covers
    cert/withdrawal-bearing txs). `declared` rules are not required by the
    constitution-coverage gate to carry a `ci_script`, so this is not a gate failure.
    Consider flipping `CN-LEDGER-07` to `partial`/`enforced` with a pointer to
    `conway.rs::check_conway_coin_conservation` + `ci_check_ledger_determinism.sh`,
    or accept the de-facto discharge via `T-CONSERV-01`.
17. **`DC-LEDGER-08` real-chain cert-state oracle is ENVIRONMENT-BLOCKED (PHASE4-B4).**
    The mechanical closure is shipped and CI-guarded (owner-complete decode + total
    owner-tagged apply + era-dispatched fail-closed accumulation + synthetic positive /
    replay / adversarial corpus; `ci_check_forbidden_patterns.sh` guards the swallow).
    What remains open is the real epoch-576 cert-state-vs-cardano-node oracle: the
    epoch-576 UMap snapshot is absent post-extraction (`corpus/cert_state/README.md`,
    `corpus/validity/conway_epoch576/README.md`). Recorded in the registry's
    `open_obligation` field on `DC-LEDGER-08`, reclassified environment-blocked per the
    tier doctrine — same constraint as `DC-TXV-06` / `DC-TXV-03`. Confirm whether the
    real-chain agreement obligation is carried as an explicit entry-condition proof
    obligation on a future cluster (to be discharged when the UMap snapshot is
    re-extracted), or accepted as an environment-blocked open obligation. This is the
    one open item from PHASE4-B4; the closed surface itself is enforced. See §A0000.
18. **BLOCKING — `DC-LEDGER-08` cross_refs are not bidirectional; the
    constitution-coverage gate FAILs at HEAD `ee35493`.** `DC-LEDGER-08` lists
    `cross_ref = ["DC-VAL-06", "DC-TXV-06", "DC-LEDGER-07", "T-DET-01",
    "CN-LEDGER-07"]`, but none of those five rules list `DC-LEDGER-08` back, so
    `ci/ci_check_constitution_coverage.sh` reports five `not bidirectional` errors
    and `RESULT: FAILED` (the gate PASSed at the B3F baseline `193d2fc`). This is a
    registry-hygiene defect from the B4 close, not a TRACEABILITY-generation issue —
    the registry `cross_ref` arrays are the canonical, append-only source and
    TRACEABILITY is read-only on them. **Fix:** append `"DC-LEDGER-08"` to the
    `cross_ref` array of each of `DC-VAL-06`, `DC-TXV-06`, `DC-LEDGER-07`,
    `T-DET-01`, `CN-LEDGER-07` in `docs/ade-invariant-registry.toml`, then rerun
    `bash ci/ci_check_constitution_coverage.sh` to re-confirm PASS. Until then the
    coverage CI is red.

---

## H. Value-conservation completeness gap — CLOSED in PHASE4-B3 (tracked against T-CONSERV-01 / CN-LEDGER-07)

> **Update (PHASE4-B3 close):** this gap is now **closed**. The cert/withdrawal
> early-out was removed from `check_conway_coin_conservation` and the **full**
> Conway preservation-of-value equation (`sum(inputs) + withdrawals + refunds ==
> sum(outputs) + fee + deposits + donation`) now runs for cert/withdrawal-bearing
> Conway txs, with deposit/refund amounts sourced canonically (`DC-TXV-06` closed/
> total cert-deposit classification + `DC-TXV-07` canonical deposit-param authority).
> Proven by `conway_conservation_full`, `conservation_early_out_removed`, the
> adversarial conservation corpus (`adversarial_imbalanced_via_{deposit,withdrawal}`,
> `adversarial_no_false_accept`), and the positive corpora (real + synthetic). The
> historical description below is retained for provenance.

PHASE4-B2-S4 (`crates/ade_ledger/src/conway.rs`) fixed a real fail-open and
introduced `check_conway_coin_conservation`, which enforces
`sum(inputs) == sum(outputs) + fee + donation` for **certificate- and
withdrawal-free** Conway transactions. This closes the pure-spend
value-conservation hole the tx adversarial corpus surfaced (a tx with
`outputs + fee != inputs` was previously accepted as `Valid` at `track_utxo=true`).

**Still deferred:** full preservation-of-value accounting —
`sum(inputs) + withdrawals + refunds == sum(outputs) + fee + deposits + donation`.
The current check **conservatively skips** txs that carry certificates or
withdrawals (so it can never false-*reject* them), which means a mis-accounted
deposit/refund/withdrawal tx is not yet caught on value grounds. Other checks
may still reject such a tx, but value conservation specifically would not.

This is the one **known, documented tx-validity-completeness gap** against the
value-preservation invariant (`T-CONSERV-01` — UTxO/asset conservation; mirrored
in the declared `CN-LEDGER-07`). It is **not** a silent gap: it is recorded in
`MEMORY.md::project_value_conservation_deposits_deferred.md` and in the
`conway.rs` source comment, and surfaced here and in §G #10. It does **not**
read as a `DC-TXV-*` enforcement gap (those rules close the verdict/witness/total-
evolution surface, all `enforced`) — it is a completeness boundary on the
value-conservation half of phase-1, scoped conservatively so it never
false-rejects.

---

## What does NOT belong here

- Implementation details (those live in CODEMAP).
- Extension-point rules (those live in SEAMS).
- Change history (lives in HEAD_DELTAS).
- Project-management state (slice status, owner).

This doc is purely **rule → enforcement**.

---

## Generation Notes

Generated by `/traceability` from:

- Registry: `docs/ade-invariant-registry.toml` (171 entries at HEAD `ee35493`; all `status != deprecated` — none deprecated; family counts T=29, DC=60, CN=69, RO=6, OP=7 = 171; +1 from PHASE4-B4's `DC-LEDGER-08`). The constitution-coverage gate was last run green during the B3F refresh (`RESULT: PASS`); rerun `ci/ci_check_constitution_coverage.sh` after this B4 refresh to re-confirm coverage over the 171st entry. The B3/B3F registry entries are consistent: `DC-TXV-06` is now `status = "enforced"` with `ci_script = ci/ci_check_conway_cert_classification_closed.sh`, `DC-TXV-06`/`DC-TXV-07` carry `cluster = "CL-LEDGER-VERDICT"` (in the gate's allowed set), and the cross_refs are bidirectional. **No remaining enforcement gap** in the cert-classification surface — the prior DC-TXV-06 CI-grep-gate follow-up is CLOSED in PHASE4-B3F.
- Normative docs (per `.idd-config.json::normative_docs`): `docs/active/CE-79_gate_statement.md`, `docs/active/CE-79_tier5_addendum.md`, `docs/active/comparison_surface_contract.md`, `docs/active/shelley_bootstrap_contract.md`, `docs/active/CE-73_reclassification.md`, `CLAUDE.md`.
- Cluster docs scanned: `docs/clusters/PHASE4-B3/` (cluster + S1..S6); `docs/clusters/PHASE4-B2/`; `docs/clusters/PHASE4-B1/`; `docs/clusters/completed/PHASE4-N-{A,B,D}/`. Primary source is the invariant registry; cluster docs consulted for `strengthened_in` provenance.
- CODEMAP: `docs/ade-CODEMAP.md` regenerated at HEAD `193d2fc` (per the close brief); used for code-locus alignment of the B3 modules (`cert_classify.rs`, `conway/cert.rs` in `ade_codec`/`ade_types`, `pparams.rs`, `conway.rs::check_conway_coin_conservation`). See §D — confirm the fresh CODEMAP carries the full B2 + B3 BLUE authority surface (`tx_validity`, `mempool`, `cert_classify`).
- HEAD_DELTAS: `docs/ade-HEAD_DELTAS.md` (baseline `d509f02`; pre-`85a50dc`; §5 cross-check (§E) flags the doc skew — `tx_validity`/`mempool` surface and the B2 extension of `ci_check_consensus_closed_enums.sh` are not yet narrated).
- CI scripts verified at HEAD `193d2fc`: **27** total under `ci/ci_check_*.sh`. PHASE4-B3 added one — `ci/ci_check_deposit_param_authority.sh` (discharges `DC-TXV-07`; greps the 6 BLUE crates for non-canonical deposit-param sources). PHASE4-B3F added one — `ci/ci_check_conway_cert_classification_closed.sh` (discharges `DC-TXV-06`, flips it `partial -> enforced`; PASSES, exit 0). PHASE4-B2 added none (it extended `ci_check_consensus_closed_enums.sh`); PHASE4-B1 added none. Four new in PHASE4-N-B: `ci_check_no_chaindb_in_consensus_blue.sh`, `ci_check_no_float_in_consensus.sh`, `ci_check_consensus_closed_enums.sh`, `ci_check_no_density_in_fork_choice.sh`. Two new in PHASE4-N-A: `ci_check_no_async_in_blue.sh` (S-A1), `ci_check_ce_n_a_5_proof.sh` (S-A10). Three RED-scope from N-D (`78da6c9`): `ci_check_chaindb_contract.sh`, `ci_check_recovery_contract.sh`, `ci_check_chaindb_crash_safety.sh`. All confirmed to carry rule-ID-anchored header comments.

Every `_(no enforcement found — gap)_` line corresponds to a registry
`code_locus`/`tests`/`ci_script` field that is empty for an entry whose
`status` is `declared`, `partial`, or where a cross-referenced rule is
itself a gap. Closing a gap means populating the registry; the next
TRACEABILITY regeneration will lift the gap automatically.

`DC-STORE-04`'s rows are intentionally **not** marked gaps — see §B for
the documented Tier 5 divergence rationale.
