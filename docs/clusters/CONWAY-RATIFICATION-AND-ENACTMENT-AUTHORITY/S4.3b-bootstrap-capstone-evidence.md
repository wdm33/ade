# S4.3b-bootstrap — operational capstone evidence (both obligations GREEN)

The S4.3b V11 migration is preparatory + inert. Before S4.3c consumes it (live epoch authority), this capstone
closes the two SEPARATE proof obligations the user mandated. Both are green; S4.3c may now consume an
already-valid V11 store rather than manufacturing one on first use.

## Obligation B — real historical witness decoding (committed `163e1428`)

Proposal `69c948cd..#0`'s ACTUAL on-chain `ParameterChange.update` (33 bytes, extracted from the local Preview
epoch-1095 db-analyser state, NOT a synthetic round-trip) decodes via the S4.3b closed decoder:

| field | value |
|---|---|
| raw update | `a214821a00fbc5201b00000002540be40015821a044aa2001b00000004a817c800` |
| blake2b256 | `4b70f9513bb1768b34680ada28c9bf27bfe4f0cdf885d4003de9f9c78cec4d2b` |
| map key `0x14`=20 → maxTxExUnits | `[mem 16_500_000, steps 10_000_000_000]` |
| map key `0x15`=21 → maxBlockExUnits | `[mem 72_000_000, steps 20_000_000_000]` |
| `unsupported_fields` | `{}` (touches only the two exec-units keys) |
| memory-only? | YES — both steps == the 1095 curPParams steps (10e9 / 20e9), so S4.3c supports it; a changed steps would be `UnsupportedRatifiedAction` |

Keys 20/21 are proven against real bytes (the CBOR literally uses those keys). Reproduced hermetically by
`cre_s4_3b_gate5_real_witness_manifest` (ade_ledger, runs in CI) + the `#[ignore]` live re-extraction
`cre_s4_3b_gate5_real_witness_update_decodes` (asserts the committed bytes ARE the chain's: identical bytes +
blake2b).

## Obligation A — V11 live-seed lineage

Fresh, SEPARATE native Mithril V11 store re-bootstrapped from the verified Preview snapshot; the old stores
(`.cardano-s4-1b-v2`, `.cardano-ce3d-*`) are untouched.

### Bootstrap anchor (verified Mithril snapshot)

| field | value |
|---|---|
| network / era | preview (magic **2**) / Conway |
| certified point | slot **115676685** / block `2e0ac2d3de8e1cf06861471eb6d7d0e2c08c43c57c9e473be0a8ae95263e99db` |
| certificate hash | `8d8fc9065882b9b816ae687cae0bca7f25f82b716790def48c9edc8484ea2b1e` |
| genesis hash | `363498d1024f84bb39d3fa9593ce391483cb40d479b87233f868d6e57c3a400d` |
| seed epoch | **1338** |
| seed artifact commit | `d3d318cf5024dd7488d141ed61e8bc5888f6c103a1add476f32f3455fe378abd` |
| data-dir | `/home/ts/.cardano-s4-3b-v11` (FRESH) |

### The V11 seed (decoded → assembled → persisted) — SOURCE-BOUND

Read within epoch 1338 (applied-through slot 115678394, before any follow). Each field is bound DIRECTLY to
the decoded snapshot source — never `Unversioned`, never a fabricated default:

| field | source | persisted |
|---|---|---|
| `num_dormant` | 0 | **`Bound(0)`** |
| `max_block_ex_units` | `Bound{mem 72_000_000, steps 20_000_000_000}` | **identical** |
| `prev_pparam_action` | `Enacted(2a2dc37b22939d3ae7395c8a409d4d0625201c88926d641d6f4441c3287e39ba#0)` | **identical** |
| pparams fingerprint | — | `390fe88d63695f393bb90b04b53ac474115dd44f6d4ccd251d14a16eff0c9d40` |
| gov fingerprint | — | `2a9c0c92837b47676d52454a5bda5f7384279f7c737e957d8baecd54a72f7a3a` |
| gov shape | — | proposals 47, committee 8, vote_delegations 58519 (S4.2 gate threaded + S4.3b inert V11 fields) |

The block ExUnits is `72M` (not `62M`) because the witness enactment (epoch 1096) raised it and it is steady by
epoch 1338 — a nice cross-consistency with obligation B. `prev_pparam_action` is a LATER enacted root than the
witness (`2a2dc37b`, not `69c948cd`), as expected 242 epochs downstream.

### Warm-restart recovery — PROVEN

`ade node run --network preview --data-dir /home/ts/.cardano-s4-3b-v11 --peer 127.0.0.1:3002` (no
`--bootstrap-mithril`) RECOVERED the durable V11 store cleanly (loaded ~3 GB, no VrfCert / BridgeMissing) and —
post-ECA-5 — crossed the 1338→1339 boundary WITHOUT corruption. Re-reading the recovered store:

- `num_dormant` `Bound(0)`, `max_block_ex_units` `Bound{72M,20B}`, `prev_pparam_action` `Enacted(2a2dc37b..#0)`
  — **IDENTICAL** (carried forward through recovery AND the boundary).
- **pparams fingerprint `390fe88d…` — IDENTICAL** across the restart (it binds the block ExUnits; pparams do not
  change mid-epoch). This is the load-bearing V11 fingerprint invariant.
- The gov fingerprint changed (`2a9c0c92`→`f8b81f34`, proposals 47→48) — ONLY because the live follow advanced
  the proposal set and crossed into epoch 1339. Not a recovery defect; the V11 seed fields within it are
  asserted identical.

Byte-identical persist→decode→fingerprint→recover is ALSO proven hermetically (the deterministic-suite gates
`cre_s4_3b_gate3_v11_pparams_bound_round_trips` + `cre_s4_3b_gate3_v3_gov_round_trips_prev_action`).

Reproduced by `crates/ade_runtime/tests/cre_s4_3b_v11_seed_evidence.rs` (`#[ignore]`, local artifacts).

### Old stores untouched + Unversioned

The legacy `.cardano-s4-1b-v2` (V2) and `.cardano-ce3d-*` (V1) stores are byte-untouched (mtimes predate this
work; a FRESH data-dir was used). They decode as `Unversioned` for both new fields — proven hermetically by
`cre_s4_3b_gate1_v10_pparams_unversioned_byte_identical` + `cre_s4_3b_gate1_v1_v2_gov_unversioned_byte_identical`.

## Cross-checks

- **S4.4 ratification anchor unchanged** with the V11-decoded census state: `cre_s4_oracle_anchor_ratify_decision`
  still reproduces the oracle exactly — ending-epoch 1094 → `target ratified=false / 0 ratified`; 1095 →
  `target ratified=true / only-1-ratified`. S4.3b's inert fields do not touch the ratify gate.
- **No inert S4.3b field changes S4.3a behaviour**: the whole `ade_ledger` (798) + `ade_runtime` (476) suites and
  the S4.3a proof gates pass unchanged.

## Verdict

Both obligations GREEN. S4.3c may proceed on the already-valid V11 store — it must NOT manufacture its own
(source-lineage proof and consumption proof stay separate).
