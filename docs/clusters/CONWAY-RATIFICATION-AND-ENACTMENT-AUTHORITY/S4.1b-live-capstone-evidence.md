# S4.1b — live V2 governance seed evidence (operational capstone)

Operational evidence for the S4.1b re-bootstrap: a NEW, separate V2 store re-bootstrapped from the verified
Preview Mithril snapshot (the CE-3d Unversioned store is untouched), proving the V2 governance seed is
source-bound and survives a warm restart. Reproduced by `crates/ade_runtime/tests/s4_1b_v2_seed_evidence.rs`
(`#[ignore]`, reads local artifacts) — operational evidence, NOT a deterministic-suite gate. The `ade` binary
is the S4.1 release build (origin/main through `2bf3b41d`).

## Bootstrap anchor (verified Mithril snapshot)

| field | value |
|---|---|
| network / profile | preview (network_magic **2**) |
| shelley genesis hash | `363498d1024f84bb39d3fa9593ce391483cb40d479b87233f868d6e57c3a400d` |
| certified point | slot **115676685** / block `2e0ac2d3de8e1cf06861471eb6d7d0e2c08c43c57c9e473be0a8ae95263e99db` |
| certificate hash | (manifest `certificate_hash_hex`) |
| seed epoch / era | **1338** / **Conway** |
| source S1a | `preview-snapshot/db/ledger/115676685/state` (37 MB, ancillary IOG-signed) |

## New V2 store (separate lineage)

- data-dir: `/home/ts/.cardano-s4-1b-v2` (FRESH; NOT `/home/ts/.cardano-ce3d-*`, which stays `Unversioned`)
- accumulator store: `epoch-accumulator.redb`
- The `EpochAccumulator` (which carries `gov_state.num_dormant`) is sealed at the seed when the node begins
  following (ChainSync start), so `load_current` reports `LAST_SLOT` = the seed point (115676685). The node is
  stopped right after this seal — before the follow crosses the 1338→1339 boundary — so the store is exactly
  the V2 seed with a within-epoch chain.

## The V2 seed (decoded → assembled → persisted)

| field | value |
|---|---|
| decoded `numDormantEpochs` (source) | **0** |
| persisted `gov_state.num_dormant` | **`Bound(0)`** — SOURCE-BOUND (V2 lineage; a genuine `Bound(0)` from the real decode, not a fabricated `Unversioned→0`; the non-zero binding is proven hermetically by `s4_1b_assembled_seed_binds_num_dormant_to_the_decoded_source` = `Bound(5)`) |
| V2 governance fingerprint | `d3520353258537be2a58b213fd80031bbedb6dcaef1d00498650829733e60d80` |
| gov shape | proposals **47**, committee **8**, vote_delegations **0** |

`vote_delegations=0` is correct: only the versioned `num_dormant` is bound into the live seed; the ratify-gate
fields (thresholds / vote_delegations / drep_expiry) stay EMPTY — import-not-activate holds, and S4.2 threads
them.

## Warm-restart (live recovery equality) — PROVEN

`ade node run --network preview --data-dir /home/ts/.cardano-s4-1b-v2` (no `--bootstrap-mithril`) RECOVERED
the store CLEANLY: `relay run loop exited (recovered/bootstrapped epoch=Some(1338), tip slot=Some(115676685))`
— no VRF failure, no `BridgeMissing`. Re-reading the recovered store yields `num_dormant = Bound(0)` and
governance fingerprint `d3520353…` — **IDENTICAL** to the pre-restart seed. The V2 seed survives the live warm
restart byte-for-byte.

To isolate this from an UNRELATED, pre-existing residual: the store's followed chain is kept WITHIN epoch 1338
(the node is stopped right after the bootstrap seals the accumulator, before the follow crosses the 1338→1339
boundary), so the warm-restart replays only within-epoch blocks. A restart that first FOLLOWS across the
seed→seed+1 boundary re-materializes the boundary block and hits the shared
`Header(VrfCert(VerificationFailed))` residual at slot 115689630 (`b3c_stake_residual` /
`eca5_boundary_crossing`) — a borderline leader-VRF stake issue, NOT related to `num_dormant` (S4.1 touches no
header/VRF path), and a separate cluster's concern. Keeping the chain within-epoch cleanly separates the S4.1b
seed-lineage claim from that residual.

The restart-equality is ALSO proven at the authoritative-codec level (the exact path the store
persists/recovers through): `s4_1b_v2_bound_dormancy_survives_durable_restart` (a `Bound(4)` accumulator
recovers byte-for-byte through `encode/decode_epoch_accumulator`) and
`cre_s4_1b_recovered_v2_store_ratifies_the_anchor_outcome` (a recovered V2 store ratifies the anchor at 1095).

## Negative fixture (preserved, not migrated)

- The legacy CE-3d `Unversioned` store is untouched. A DRep-expiry-dependent path over an `Unversioned` store
  still yields `DormantStateRequired` (hermetic:
  `s4_1b_legacy_unversioned_store_with_live_drep_expiry_is_terminal`).

## Proof shape

Source-bound + replay-stable-across-restart + real-offset + V2 fingerprint — NOT "old==new". Then **S4.2**
consumes THIS valid V2 store: the narrow question is whether the live path threads the imported authority
(incl. `num_dormant`) into the ACTIVE ratify gate correctly.
