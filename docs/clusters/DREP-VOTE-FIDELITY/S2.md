# Invariant Slice — DREP-VOTE-FIDELITY S2

## Slice Header
**Slice Name:** DRep cross-resolve negative + fingerprint test, CI gate, strengthen DC-LEDGER-10
**Cluster:** DREP-VOTE-FIDELITY
**Status:** Merged
**CEs addressed:** CE-2, CE-3, CE-4 (CE-1 landed in S1).

## Mechanical Acceptance Criteria
- **AC-1 (cross-resolve negative):**
  `governance.rs::committee_fidelity_tests::drep_keyhash_scripthash_do_not_cross_resolve`
  — a key-hash DRep voter (resolving to `DRep::KeyHash`) does NOT tally a
  `ScriptHash` DRep's stake of equal bytes (yes-stake 0 → DRep threshold fails →
  ratification denied); the matching `KeyHash` DRep stake does ratify. The
  discriminant is the only difference.
- **AC-2 (fingerprint):** `drep_vote_discriminant_changes_fingerprint`
  (`credential_fidelity_corpus.rs`) — two proposals differing only in a DRep
  voter's discriminant fingerprint differently.
- **AC-3 (CI gate):** `ci_check_credential_discriminant_closed.sh` extended —
  asserts `GovActionState.drep_votes` carries `StakeCredential` and
  `governance.rs` has no `DRep::KeyHash(..).or_else(..)` OR-fallback. PASS.
- **AC-4 (strengthen DC-LEDGER-10):** `strengthened_in += DREP-VOTE-FIDELITY`;
  tests += 2; code_locus extended; open_obligation follow-up (c) marked RESOLVED.
  `ci_check_constitution_coverage.sh` PASS (173 entries).
- **AC-5:** `cargo test -p ade_ledger` green; credential gate PASS; testkit's 10
  env-blocked failures unchanged.

## Declared non-goals
- `spo_votes` (pools are always key-hash; permanent non-goal).
