# Invariant Slice — B5-S4

## Slice Header

**Slice Name:** Gov-state corpus + adversarial + CI gate + DC-LEDGER-09 enforcement

**Cluster:** PHASE4-B5 — Conway governance certificate accumulation authority

**Status:** Merged

**Cluster Exit Criteria Addressed:**
- [ ] **CE-5 (replay + fingerprint migration):** synthetic positive corpus
  replays byte-identical over the gov-state fingerprint surface; real epoch-576
  VState oracle documented environment-blocked.
- [ ] **CE-6 (adversarial no-false-accept):** missing-env DRep, decode-layer
  hazards, and double-resign each handled fail-closed / deterministically.
- [ ] **CE-7 (CI gate + enforcement):** `ci/ci_check_gov_cert_accumulation_closed.sh`
  green; DC-LEDGER-09 flipped declared→enforced with populated
  `code_locus`/`tests`/`ci_script`; bidirectional cross_refs;
  `ci_check_constitution_coverage.sh` PASS.

---

## Mechanical Acceptance Criteria

- **AC-1:** `crates/ade_ledger/tests/gov_state_corpus.rs` —
  `positive_synthetic_gov_state_accumulates`,
  `gov_state_accumulation_replays_byte_identical`,
  `adversarial_drep_register_update_missing_env_rejected`,
  `adversarial_decode_layer_rejects_guard_gov_path`,
  `adversarial_double_resign_is_deterministic` pass.
- **AC-2:** Block-path tests in `rules.rs` —
  `gov_accumulation_applies_drep_registration_into_gov_state`,
  `gov_apply_error_halts_accumulation` pass.
- **AC-3:** `corpus/gov_state/README.md` documents the closed scope and the
  environment-blocked real oracle (reclassified per tier doctrine).
- **AC-4:** `ci/ci_check_gov_cert_accumulation_closed.sh` PASS (drop comment gone,
  apply wired, env fail-fast wired, gov dispatch exhaustive).
- **AC-5:** DC-LEDGER-09 `status = "enforced"`; `ci_check_constitution_coverage.sh`
  PASS (DC-LEDGER-09 ↔ DC-LEDGER-08/DC-VAL-06/DC-TXV-06/T-DET-01/CN-LEDGER-07
  bidirectional).
- **AC-6:** `cargo test -p ade_ledger` green; no new workspace failures vs the
  recorded baseline.

---

## Environment-Blocked (reclassified, not a gap)
- Real epoch-576 gov-state (VState)-vs-cardano-node oracle — UMap/ledger snapshot
  absent. Same constraint as DC-LEDGER-08 / DC-TXV-06; documented in
  `corpus/gov_state/README.md`.

## Declared follow-up (NOT a B5 CE)
- GOVCERT committee-membership tx-validity gate (OQ-3).
