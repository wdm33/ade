# Invariant Slice — B5-S3

## Slice Header

**Slice Name:** Wire gov-cert accumulation into block application — carry `gov_state` forward, remove B4's observe-and-drop (fingerprint migration)

**Cluster:** PHASE4-B5 — Conway governance certificate accumulation authority

**Status:** Proposed

**Cluster Exit Criteria Addressed:**
- [ ] **CE-4 (fail-closed wiring):** the B4 `// observed and routed out of B4
  mutation scope` drop in `accumulate_tx_certs` is replaced by application; a gov
  apply error propagates as `LedgerError` and halts the block transition.
- [ ] **CE-5 (fingerprint migration, mechanics):** `gov_state` is carried forward
  through `apply_block` (cert-accumulated when tracked) instead of nulled — the
  deliberate provenance migration (T-DET-01). Replay-equivalence corpus is B5-S4.

---

## Slice-Entry Proof Obligations (gating)

- **PO-1 (the null was the gap).** Both `apply_block` paths set `gov_state: None`
  (`rules.rs:307`, `:386`), nulling governance state at every block. The fix is
  to thread `state.gov_state` forward through `process_block_certificates` /
  `accumulate_tx_certs` and emit the accumulated value.
- **PO-2 (ratification interaction — confirm empirically).** The epoch-boundary
  ratification path (`rules.rs:1152-1226`) reads `current_state.gov_state`.
  Carrying gov_state forward means boundaries now see cert-accumulated maps. With
  empty `proposals` (not cert-accumulated; a separate path) ratification yields
  no `treasury_withdrawn`, so chained-replay value-conservation is unaffected.
  **Confirm:** `cargo test --workspace` shows no new failure in boundary/epoch/
  determinism/value-conservation suites. If a suite breaks, analyze whether it is
  the intended migration (regenerate pin) or a real regression (stop).
- **PO-3 (gating parity).** `gov_state == None` ⇒ governance not tracked ⇒ gov
  half skipped (parallel to `track_utxo` gating cert-state). DC-LEDGER-09's
  "applied, never dropped" governs the tracked path (`gov_state` present).

---

## Mechanical Acceptance Criteria

- **AC-1:** `process_block_certificates` and `accumulate_tx_certs` return
  `(CertState, Option<ConwayGovState>)`; the Conway loop calls
  `apply_conway_gov_cert` per cert when `gov_state` is `Some`, threading the lazy
  `gov_cert_env()`.
- **AC-2:** Both `apply_block` constructors set `gov_state` to the accumulated
  value (no `gov_state: None`). The observe-and-drop comment is gone.
- **AC-3:** All B4 `accumulate_tx_certs` fail-closed tests still pass under the
  new signature (decode/unknown-tag/removed-tag/apply-error propagation).
- **AC-4:** `cargo test --workspace` shows no new failures vs the recorded
  baseline (two pre-existing env-blocked conditions excepted).

---

## Hard Prohibitions
- No `gov_state: None` left in an `apply_block` constructor.
- No swallowing a gov apply error (it must propagate as `LedgerError`).
- No corpus/CI work (B5-S4); no committee-membership gate (OQ-3).

## Explicit Non-Goals
- Synthetic/adversarial corpus, replay-byte-identical assertion, CI gate, registry
  flip (B5-S4).
