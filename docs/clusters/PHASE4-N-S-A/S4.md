# Slice A4 — Sub-cluster close

**Cluster:** PHASE4-N-S-A. **CLOSED.**

## §1 Registry flips

3 N-S-A rules: `declared` → `enforced`.

| Rule | Tests | CI gate |
|---|---|---|
| `CN-KES-HEADER-01` | 3 (corpus byte-match + replay + shell verify) | `ci/ci_check_unsigned_header_preimage_single_source.sh` |
| `DC-KES-HEADER-01` | 1 (replay anchor) | — |
| `CN-PREIMAGE-FIXTURE-01` | 1 (corpus byte-match) | — |

## §2 Strengthenings

| Rule | `strengthened_in += "PHASE4-N-S-A"` |
|---|---|
| `CN-FORGE-01` | Real KES signature now used in Succeeded path |
| `DC-CONS-18` | Body-hash binding under real signed header + new CI gate |

## §3 New A4 integration test

`shell_kes_sign_header_produces_verifiable_signature` (in
`ade_runtime::producer::producer_shell::tests`): synthetic
`UnsignedHeaderPreImage` → `kes_sign_header` → signature
verifies via the validator's `verify_kes_signature`
primitive. End-to-end shell-API contract proof.

## §4 New CI gate

`ci/ci_check_unsigned_header_preimage_single_source.sh`
enforces an allow-list of 4 files permitted to reference
`UnsignedHeaderPreImage`. Defense-in-depth on top of the
type's private-inner-field constructor restriction.

## §5 Exit criteria (all met)

- [x] CE-A-1 to CE-A-8 from cluster doc §4 — all green.
- [x] `cargo test --workspace --lib` clean.
- [x] All 3 CI gates pass (the new
  `ci_check_unsigned_header_preimage_single_source` plus
  the carry-forward `ci_check_leader_check_authority` +
  `ci_check_producer_coordinator_no_secrets`).
- [x] 3 N-S-A rules enforced; 2 carry-forward strengthenings.

## §6 N-S-A handoff

The KES-signs-real-unsigned-header bridge is in place. The
cross-impl byte-match against the validator's authority
holds for every corpus block. The producer shell exposes
the typed signing API. `run_real_forge` produces blocks
whose KES signatures verify under their own
`verify_header_kes`.

What N-S-A does NOT close (deliberately, per the cluster
plan's honest scope):

- The synthetic-stake `full_stake` integration test still
  emits `ForgeFailed` because the surrounding ledger /
  chain-dep / era-schedule / pool-distr state isn't a real
  Praos chain context. Real chain state is N-S-C's
  operator-pass deliverable.
- Cardano-node accepts an Ade-forged block over N2N is
  N-S-C C1/C2 — requires both the A bridge AND the B
  outbound-relay AND operator action.

PHASE4-N-S-A close at this commit.
