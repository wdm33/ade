# Invariant Slice — OQ5 S2

## Slice Header
**Slice Name:** Credential-fidelity corpus + CI gate + enforce DC-LEDGER-10
**Cluster:** OQ5-CREDENTIAL-FIDELITY
**Status:** Merged

**CEs addressed:** CE-5 (replay), CE-6 (adversarial/distinctness no-false-accept),
CE-7 (CI + enforce). CE-1..CE-4 landed in S1.

## Mechanical Acceptance Criteria
- **AC-1:** `crates/ade_ledger/tests/credential_fidelity_corpus.rs` —
  `keyhash_scripthash_same_bytes_are_distinct_certstate`,
  `keyhash_scripthash_same_bytes_are_distinct_govstate`,
  `discriminant_changes_fingerprint_corpus`,
  `credential_accumulation_replays_byte_identical` pass. A key-hash and a
  script-hash credential over identical 28 bytes are distinct CertState and
  ConwayGovState keys (no collapse); a script-hash DRep registration lands under
  the ScriptHash key, never collapsed to a key-hash.
- **AC-2:** `ci/ci_check_credential_discriminant_closed.sh` PASS — defends (1)
  StakeCredential stays the closed enum (no tuple-struct reappearance), (2) both
  decoders preserve the tag (no `let (_cred_type|_tag` discard), (3) no
  tuple-construction `StakeCredential(<hash>)` coercion on the BLUE path.
- **AC-3:** `corpus/credential_fidelity/README.md` documents the closed scope, the
  intentional dual fingerprint migration, and the environment-blocked real-oracle
  obligation.
- **AC-4:** DC-LEDGER-10 `status = "enforced"` with code_locus / tests / ci_script
  populated; `ci_check_constitution_coverage.sh` PASS (173 entries; bidirectional
  cross_refs into T-DET-01/T-ENC-03/DC-LEDGER-08/DC-LEDGER-09/DC-TXV-05).
- **AC-5:** `cargo test -p ade_ledger` green; gov + cert-classification +
  credential gates PASS; no new workspace failures (the 10 testkit failures are
  the unchanged pre-existing env-blocked corpus set).

## Environment-blocked (reclassified)
- Real-chain agreement of the discriminated keys vs cardano-node's
  `Credential`-keyed `UMap`/`VState` — snapshots absent locally.

## Declared non-goals
- Withdrawal / required-signer / address credential discriminant; the
  `Hash28`-keyed stake-distribution snapshot; the Byron credential surface.
