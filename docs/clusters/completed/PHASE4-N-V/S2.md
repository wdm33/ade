# PHASE4-N-V · S2 — forge_block emits enveloped bytes + forge↔decode gate

> Parent: [`cluster.md`](./cluster.md). Addresses **CE-V-4, CE-V-5**; confirms OQ5.
> The core fix: `forge_block` output now round-trips through `decode_block`.

## What shipped
- **`forge_block` (BLUE, `crates/ade_ledger/src/producer/forge.rs`)** wraps its
  `ShelleyBlock::ade_encode` output (the bare inner Conway block, `array(5)`) in
  the canonical era envelope `[era, block]` via
  `ade_codec::encode_block_envelope(CardanoEra::Conway, &inner_bytes)`.
  `ForgedBlock.bytes` is now `[7, block]`; `ForgedBlock.block` is unchanged.
- **Regression-lock test** `forge_block_output_decodes_via_decode_block`
  (forge.rs `#[cfg(test)]`): `decode_block(forge_block(base_tick()).bytes)` is
  `Ok`, era `Conway`, `computed_body_hash == forged body_hash` (DC-CONS-18
  binding survives the envelope), and the re-decoded inner header fields (slot,
  block_number, body_hash) match. **Fails on the pre-N-V tree.**
- **CI gate** `ci/ci_check_forge_decode_round_trip.sh` (2 guards: `forge_block`
  wraps via `encode_block_envelope`; the round-trip test exists).

## OQ5 (no consumer of bare-block output) — confirmed
- `self_accept.rs` `.bytes` are `AcceptedBlock`'s own field, not `forge_block`'s.
- No test asserts `ForgedBlock.bytes` byte-shape (only `ForgedBlock.block`).
- `forge_handler_variants::full_stake_answer_reaches_self_accept_and_rejects`
  now decodes past the envelope and reaches `self_accept`, still returning
  `ForgeFailed` against its empty `pool_distr_view` (VRF cert: unknown pool) —
  no `ForgeSucceeded` panic. All `ade_node` + `ade_ledger` tests green.

## Exit criteria (S2 portion)
- [x] CE-V-4: `forge_block_output_decodes_via_decode_block` passes.
- [x] CE-V-5: `ci_check_forge_decode_round_trip.sh` passes.
- [x] `cargo test -p ade_ledger` + `cargo test -p ade_node` clean.

## Forbidden in S2 (inherited)
- No second envelope encoder; no bare-block output; no body-bucket re-encode;
  no serve-side wire-wrap; no WAL.
