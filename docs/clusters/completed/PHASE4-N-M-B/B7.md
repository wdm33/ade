# Slice B7 — Cluster close + commit + push

**Cluster:** PHASE4-N-M-B
**CEs:** all 11 (final flip + strengthenings)
**Dependencies:** B1..B6
**Registry effects on merge:**
- CN-ADMIT-01, CN-ADMIT-02 → `enforced`
- DC-ADMIT-01..09 → `enforced`
- CN-NODE-01.strengthened_in += "PHASE4-N-M-B"
- CN-STORE-03.strengthened_in += "PHASE4-N-M-B"  (per-admit replay-equivalence is a strengthening, not a new rule)
- DC-WAL-03.strengthened_in += "PHASE4-N-M-B"
- T-DET-01.strengthened_in += "PHASE4-N-M-B"
- DC-LOG-01.strengthened_in += "PHASE4-N-M-B" (bidirectional vocabulary closure)

## Intent

Final close-out:
1. Flip 11 N-M-B rules `declared → enforced` in registry.
2. Apply the 5 strengthenings above.
3. Refresh grounding docs: CODEMAP, SEAMS, HEAD_DELTAS,
   TRACEABILITY.
4. Run full workspace test suite + all relevant CI gates.
5. Commit + push with the trailer-required commit message.

## §12 Mechanical Acceptance Criteria

- [ ] Registry file shows 11 rules `enforced`, 5
  strengthening entries appended; no rule weakened, no ID
  reassigned.
- [ ] `cargo build -p ade_node && cargo test -p ade_node` is
  green.
- [ ] All N-M-B CI gates pass: `ci/ci_check_admission_*.sh` +
  `ci/ci_check_lagging_is_evidence_only.sh` +
  `ci/ci_check_admission_log_vocabulary_closed.sh` +
  `ci/ci_check_admission_no_refscript_skip.sh` +
  `ci/ci_check_admission_runner_closure.sh` +
  `ci/ci_check_admit_replay_equivalence.sh` +
  `ci/ci_check_node_mode_closure.sh`.
- [ ] CODEMAP shows new modules
  `ade_node::admission::{verdict, seed_to_snapshot, runner}`
  + `ade_node::admission_log::{event, writer}` with color +
  authority entries.
- [ ] SEAMS lists `AdmissionLogEvent` + `AgreementVerdict` as
  closed sums (no extension points).
- [ ] HEAD_DELTAS includes the PHASE4-N-M-B cluster summary.
- [ ] TRACEABILITY rows for all 11 N-M-B rules show enforced
  status with quoted CI script + test names.
- [ ] Commit body cites all CEs + carries the
  `Co-Authored-By: Claude Opus 4.7 (1M context)
  <noreply@anthropic.com>` trailer.
- [ ] `git push origin main` succeeds; HEAD recorded in
  pickup memory.

## §14 Hard Prohibitions

- No premature status flip (DC-WAL-03 / CN-STORE-03 stay as
  strengthened, not new).
- No commit without registry deltas + CI gates passing.
- No `--no-verify` push.

## §15 Non-Goals

- No sub-cluster C work (operator pass) — separate cluster.
- No A1.1 reference-script work — separate slice.
