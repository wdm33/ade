# Invariant Slice — DREP-VOTE-FIDELITY S1

## Slice Header
**Slice Name:** Discriminate drep_votes; exact-variant DRep stake resolution
**Cluster:** DREP-VOTE-FIDELITY
**Status:** Merged
**CEs addressed:** CE-1, CE-2, CE-3 (S2 = corpus + CI gate + strengthen).

## The change
1. `ade_types/src/conway/governance.rs`: `GovActionState.drep_votes`
   `Vec<(Hash28,Vote)>` → `Vec<(StakeCredential,Vote)>`. `spo_votes` stays
   `Hash28` (pools key-hash only).
2. `ade_ledger/src/governance.rs` `lookup_stake`: the voter `StakeCredential`
   maps to EXACTLY one `DRep::{KeyHash,ScriptHash}(h)` stake key — the
   `DRep::KeyHash(c).or_else(ScriptHash(c))` OR-fallback removed.
3. `ade_ledger/src/fingerprint.rs`: renamed `write_committee_vote_list` →
   `write_credential_vote_list` (now serves committee + drep; output-identical
   rename); `drep_votes` routed through it; `spo_votes` stays `write_vote_list`.
4. GREEN loader: renamed `parse_committee_vote_map` → `parse_credential_vote_map`;
   `drep_votes` parsed through it (tag-preserving). GREEN `epoch_oracle_comparison`
   debug consumers updated to exact-variant mapping (no OR-fallback).

## Mechanical Acceptance Criteria
- **AC-1:** `drep_votes` is `StakeCredential`-typed; compiles.
- **AC-2:** no key/script OR-fallback in `lookup_stake` (exact variant match).
- **AC-3:** `cargo build --workspace` + `cargo test -p ade_ledger` green; no golden
  drift; testkit's 10 env-blocked failures unchanged.

## Hard Prohibitions
- No `DRep::KeyHash(c).or_else(ScriptHash(c))` / key-script cross-resolution.
- No `spo_votes` change (non-goal).

## Explicit Non-Goals
- Cross-resolve negative + distinctness corpus, CI gate, DC-LEDGER-10 strengthen
  (S2).
