# Invariant Slice — B3-S6

## 2. Slice Header

### Slice Name
Adversarial conservation corpus (no false accept)

### Cluster
PHASE4-B3 — Full Conway tx value-conservation accounting

### Status
Merged (verified green).

### Cluster Exit Criteria Addressed
- [x] **CE-B3-6** — `cargo test -p ade_ledger --test conway_conservation_adversarial` PASS (8/8):
  value-imbalanced (deposit / withdrawal), unknown-cert-tag, removed-tag (5/6),
  truncated-withdrawals, and state-dependent-unaccountable txs → structured reject with the
  correct §9.1 class; **no mutation ever yields Valid**; verdicts replay byte-identically.

### Slice Dependencies
- B3-S1, B3-S2, B3-S3, B3-S4 (the full guard + §9.1 precedence).

---

## 4. Intent
Make a false accept impossible on the conservation surface: prove that every adversarial
cert/withdrawal-bearing mutation is rejected with the correct, highest-precedence reason, and that
no mutation is ever accepted.

---

## 5. Scope
- `crates/ade_ledger/tests/conway_conservation_adversarial.rs` (NEW, test-only / GREEN over the
  BLUE guard). Controlled UTxO + seeded `CertState` + canonical `ConwayDepositParams` at
  `track_utxo=true` (the B2 family-(B) pattern). No snapshot loaded.
- **Out of scope:** real on-chain adversarial txs (not needed — adversarial coverage is synthetic
  by design); block-apply cert-state path (PHASE4-B4).

---

## 6. Execution Boundary
- BLUE: none changed (exercises the S4 guard; if a family required a guard change, that would mean
  S4 was incomplete — none did).
- GREEN: the adversarial test harness + synthetic CBOR cert/withdrawal builders.
- RED: none.

---

## 8. Invariants Strengthened or Introduced
- Strengthens `DC-TXV-03` (mandatory adversarial corpus, no false accept) and `DC-VAL-06`
  (fail-closed on every malformed/era-invalid/unaccountable cert+withdrawal path). Tests appended
  to both registry entries.

---

## 11. Replay, Crash, and Epoch Validation
Tests (all PASS): `adversarial_imbalanced_via_deposit`, `adversarial_imbalanced_via_withdrawal`,
`adversarial_unknown_cert_tag_rejects_as_decode`, `adversarial_removed_tag_rejects_as_era_invalid`,
`adversarial_truncated_withdrawals_rejects_as_decode`,
`adversarial_state_dependent_unaccountable_rejects`, `adversarial_no_false_accept`,
`adversarial_verdicts_replay_byte_identical`.

§9.1 precedence proven: each multi-fault tx rejects with the highest-precedence reason
(unknown-cert-AND-imbalanced → decode; removed-tag-AND-imbalanced → era-invalid; etc.).

---

## 12. Mechanical Acceptance Criteria
- [x] `cargo test -p ade_ledger --test conway_conservation_adversarial` PASS (8/8).
- [x] S4 regression (`conway_conservation_full`) + `ade_ledger --lib` stay green.

---

## 13. Failure Modes
Every adversarial family maps to its §9.1 structured reject: `Decoding`, `EraInvalidCertificate`,
`UnsupportedStateDependentDeposit`, `Conservation`. The no-false-accept sweep asserts the
complement (no `Ok`/`Valid`) across the whole set.

---

## 14. Hard Prohibitions
- Positive-only coverage forbidden — this is the mandatory adversarial half.
- No softened assertion; a false accept is release-blocking.
- A lower-precedence reason must not mask a higher one (proven by the precedence tests).
- No BLUE-logic change in this slice.

---

## 15. Explicit Non-Goals
- Block-apply cert-state path (PHASE4-B4); withdrawal-equals-balance; modifying the S4 guard.

---

## 18. Authority Reminder
Planning/review aid. Authority: `docs/ade-invariant-registry.toml` + Cardano ledger spec.
