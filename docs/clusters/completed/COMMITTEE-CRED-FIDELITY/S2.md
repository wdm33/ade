# Invariant Slice — COMMITTEE-CRED-FIDELITY S2

## Slice Header
**Slice Name:** Committee cross-resolve negative + distinctness corpus, CI gate, strengthen DC-LEDGER-10
**Cluster:** COMMITTEE-CRED-FIDELITY
**Status:** Merged
**CEs addressed:** CE-2, CE-3, CE-6 (CE-1/CE-4/CE-5 landed in S1).

## Mechanical Acceptance Criteria
- **AC-1 (cross-resolve negative — the OQ5 review's ask):**
  `governance.rs::committee_fidelity_tests::committee_keyhash_scripthash_do_not_cross_resolve`
  — a hot KeyHash key resolving to cold KeyHash(0xCC) does NOT ratify against a
  ScriptHash(0xCC) committee member of equal bytes (committee quorum fails),
  while a matching KeyHash(0xCC) member does ratify. The discriminant is the only
  difference; full-credential equality is the gate.
- **AC-2 (distinctness + fingerprint):**
  `committee_keyhash_scripthash_same_bytes_distinct`,
  `committee_discriminant_changes_fingerprint`
  (`credential_fidelity_corpus.rs`).
- **AC-3 (CI gate):** `ci/ci_check_credential_discriminant_closed.sh` extended —
  asserts `ConwayGovState.committee` is `StakeCredential`-keyed and
  `GovActionState.committee_votes` carries `StakeCredential`. PASS.
- **AC-4 (strengthen DC-LEDGER-10):** `strengthened_in += COMMITTEE-CRED-FIDELITY`;
  `code_locus` + `tests` extended (3 committee tests). No new rule.
  `ci_check_constitution_coverage.sh` PASS (173 entries).
- **AC-5:** `cargo test -p ade_ledger` green; credential + gov + cert gates PASS;
  testkit's 10 env-blocked failures unchanged.

## Environment-blocked (reclassified)
- Real-chain committee-state agreement vs cardano-node — same as DC-LEDGER-10.

## Declared non-goals
- `drep_votes` / `pool_votes` discrimination; the tx voting-procedures decoder.
