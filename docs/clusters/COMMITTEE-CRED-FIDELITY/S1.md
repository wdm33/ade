# Invariant Slice — COMMITTEE-CRED-FIDELITY S1

## Slice Header
**Slice Name:** Discriminate committee member + vote credentials; full-credential ratification
**Cluster:** COMMITTEE-CRED-FIDELITY
**Status:** Proposed
**CEs addressed:** CE-1, CE-2, CE-3, CE-4, CE-5 (S2 = corpus + CI gate + registry strengthen)

## The change (atomic; compile green as one unit)

1. **Types**
   - `crates/ade_types/src/conway/governance.rs:42`: `GovActionState.committee_votes`
     `Vec<(Hash28, Vote)>` → `Vec<(StakeCredential, Vote)>` (add
     `use crate::shelley::cert::StakeCredential;`). **`drep_votes` and `spo_votes`
     stay `Vec<(Hash28, Vote)>`** (non-goals — pool is always key-hash; drep is a
     parallel follow-up).
   - `crates/ade_ledger/src/state.rs:87`: `ConwayGovState.committee`
     `BTreeMap<Hash28, u64>` → `BTreeMap<StakeCredential, u64>`.

2. **governance.rs — full-credential equality (no `.hash()` in the committee path)**
   - `evaluate_ratification` / `check_ratification` `committee_members` param →
     `&BTreeMap<StakeCredential, u64>`.
   - Resolution (~209-219): `committee_hot_keys.iter().find(|(hot,_)| *hot ==
     hot_cred)` (both `&StakeCredential`, direct equality — `hot_cred` is now the
     discriminated committee vote credential); active-member check
     `active_members.iter().any(|(c,_)| *c == cold)` (both `&StakeCredential`).
     **Remove the `.hash()` calls on the committee path.** (The stake-distribution
     `.hash()` adapters elsewhere in rules.rs/governance.rs are unrelated — leave
     them.)

3. **fingerprint.rs**
   - committee map writer (~273): `write_hash28(cred)` → `write_stake_credential(cred)`.
   - `write_gov_action_state` (~566): `committee_votes` needs a NEW
     `write_committee_vote_list(&[(StakeCredential, Vote)])` (mirror
     `write_vote_list` but `write_stake_credential` for the voter, sort by the
     credential's `Ord`). `drep_votes`/`spo_votes` keep `write_vote_list`
     (`Hash28`).

4. **GREEN loader** (`crates/ade_testkit/src/harness/snapshot_loader.rs`)
   - `CommitteeState.members` (`:1956`) → `BTreeMap<StakeCredential, u64>`;
     `parse_committee_state` (~2031, `members.insert(Hash28(hash), expiry)`) reads
     the `Credential ColdCommitteeRole` key/script tag (comment at :2045 confirms
     it's a discriminated Credential on the wire) and builds KeyHash/ScriptHash.
   - `committee_votes`: the proposal-vote voter (`parse_governance_proposals`
     ~2307 via `parse_vote_map`). `parse_vote_map` is shared with drep/spo votes
     (which stay `Hash28`); add a committee-specific variant
     (`parse_committee_vote_map`) that reads the voter key/script tag and returns
     `Vec<(StakeCredential, Vote)>`, OR have parse_vote_map return the tag and map
     it for committee only. Where a tag is genuinely unavailable, default
     `KeyHash` with a `// GREEN: tag unavailable` note.

5. **Fingerprint goldens:** regenerate any that drift (likely NONE — golden states
   have empty committee/votes). If `golden_empty_state_per_era` drifts, STOP and
   report.

## Mechanical Acceptance Criteria
- **AC-1:** committee + committee_votes are `StakeCredential`-typed; compiles.
- **AC-2:** no `.hash()` on the committee resolution path in governance.rs.
- **AC-3:** new decode/build preserves the committee voter+member tag (loader).
- **AC-4:** `cargo test -p ade_ledger` + `cargo build --workspace` green; testkit's
  pre-existing env-blocked failures unchanged (no new failures).

## Hard Prohibitions
- No `.hash()` standing in for committee-credential identity in ratification.
- No bare-`Hash28` committee member/vote on the BLUE path.
- Do NOT change `drep_votes`/`spo_votes` types or the tx voting-procedures
  decoder (non-goals / out of scope).

## Explicit Non-Goals
- Negative/distinctness corpus, CI-gate extension, DC-LEDGER-10 strengthen (S2).
- drep/pool voter discrimination.
