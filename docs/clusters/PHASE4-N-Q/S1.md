# Slice S1 — Planning artifacts + registry entries

**Cluster:** PHASE4-N-Q
**Strengthens:** none yet (declarative slice). Lands the
planning ladder + the four new registry entries as
`status = "declared"`.
**Introduces:** registry IDs `CN-PROD-01`, `CN-PROD-02`,
`DC-PROD-01`, `DC-PROD-02` — all `declared`, flipped to
`enforced` on S6 close.
**Dependencies:** PHASE4-N-P close (HEAD `037bad8`).

## §1 Intent

Land the pre-implementation IDD pipeline output for the live
producer-mode cluster:

1. The invariants sketch with the user's key-custody amendment
   (coordinator is GREEN pure state machine; key custody RED;
   signing modeled as a closed effect).
2. The cluster slice plan (6 slices).
3. The full cluster doc.
4. The four new registry entries declared.

No code in this slice. S2 onward is implementation.

Mirrors `PHASE4-N-P/S1` (planning + proof obligation), without
a standalone proof-obligation document: N-Q's "unknown
compatibility facts" are smaller (slot-period arithmetic + N2N
handshake server-role + Conway-genesis structure) and are
folded into the cluster doc rather than getting their own
artifact.

## §2 Scope

### Files

- `docs/planning/phase4-n-q-invariants.md` — `/invariants`
  output. 16 must-be-true (I1–I11), 16 must-never (N1–N16),
  deterministic surface, replay-equivalence claims (incl. R3
  forge-result event stream), state-transition table,
  TCB color hypothesis (with reclassification of N-G's
  `n2n_server` to GREEN), resolved OQs.
- `docs/planning/phase4-n-q-cluster-slice-plan.md` —
  `/cluster-plan` output. 6-slice ordering, dependency graph,
  per-slice MACs.
- `docs/clusters/PHASE4-N-Q/cluster.md` — full cluster doc.
- `docs/clusters/PHASE4-N-Q/S1.md` — this slice doc.
- `docs/ade-invariant-registry.toml` — append `CN-PROD-01`,
  `CN-PROD-02`, `DC-PROD-01`, `DC-PROD-02` as `status =
  "declared"`.

### Not in scope (S2+)

- Implementing the coordinator / shell / listener / produce
  mode — those are S2..S5.
- Operator runbook / private-testnet helpers — S6.
- Retiring the `live_block_production_session` stub — S5.
- Cluster-close discipline (grounding-doc regen, archive,
  registry strengthenings) — S6.

S1 is purely documentary + registry. No `.rs` file is touched.

## §3 Mechanical acceptance criteria

1. `docs/planning/phase4-n-q-invariants.md` exists with all of:
   - §1 (I1–I11) including I7 (key custody RED-confined) +
     I11 (closed `ProducerLogEvent` vocabulary).
   - §2 (N1–N16) including N9 (no secret material in
     `CoordinatorState`), N15 (no socket addresses in
     replayable stream), N16 (no real-time replay claim).
   - §5 corrected `coordinator_init` signature (no
     KesSecret/VrfSigningKey/ColdSigningKey).
   - §5 `Effect::RequestForge` + `Event::ForgeSucceeded`/
     `ForgeFailed`.
   - §6 TCB table with `n2n_server` reclassified GREEN.
   - §7 with all 10 OQs resolved.
2. `docs/planning/phase4-n-q-cluster-slice-plan.md` exists
   with 6-slice plan (S1 planning → S2 GREEN coordinator → S3
   RED shell → S4 RED listener → S5 ade_node Mode::Produce →
   S6 operator runbook + close).
3. `docs/clusters/PHASE4-N-Q/cluster.md` exists with §1
   primary invariant, §2 in/out scope, §3 slice index, §4
   exit criteria (≥ 16 MACs), §5 hard prohibitions, §6 replay
   obligations.
4. `docs/clusters/PHASE4-N-Q/S1.md` (this file) exists.
5. `docs/ade-invariant-registry.toml` contains the four new
   entries, each with:
   - `status = "declared"`
   - `introduced_in = "PHASE4-N-Q"`
   - `strengthened_in = []`
   - `code_locus`/`tests`/`ci_script` placeholders pointing
     at the slice that will populate them.
6. CN-PROD-02 statement explicitly references the
   "GREEN coordinator never owns or stores private signing
   material" T-tier-boundary clause.
7. DC-PROD-01 statement explicitly forbids socket addresses
   in the replayable event stream.
8. DC-PROD-02 statement narrows replay to canonical
   slot-tick + forge-result streams (NOT real wall-clock).
9. No code committed. Slice is documentation + registry only.
10. Commit with the project-override trailer.

## §4 Hard prohibitions

- **No code in this slice.** Any `.rs` change is out of N-Q-S1
  scope.
- **No registry rewrites of existing N-G entries.** The
  `n2n_server` reclassification (RED → GREEN) is a
  description-only fix in TRACEABILITY/CODEMAP at cluster
  close (S6); N-G's registry entries stay as-is.
- **No premature "enforced" status.** All four N-Q entries
  land as `declared`. Status flips to `enforced` only when
  the corresponding implementation slice + tests + CI script
  are in place (S6 close).
- **No carry-forward of secret material into CoordinatorState
  in the cluster doc or registry statement.** N9 is
  load-bearing for the doctrine.

## §5 Replay obligations preserved

S1 introduces no new replay obligations. The replay-
equivalence claim (DC-PROD-02) is the *target* of S2 onward;
this slice declares it without enforcing it.

## §6 Closure protocol

1. The five files in §2 exist.
2. Self-check: re-read §3 MACs against the doc; every item
   ticks.
3. Commit via `git commit -F` with the project-override
   trailer.
4. Push.
5. Move to S2.

## §7 References

- Invariants: [`docs/planning/phase4-n-q-invariants.md`](../../planning/phase4-n-q-invariants.md).
- Cluster plan: [`docs/planning/phase4-n-q-cluster-slice-plan.md`](../../planning/phase4-n-q-cluster-slice-plan.md).
- Cluster doc: [`cluster.md`](cluster.md).
- Precedent: `docs/clusters/completed/PHASE4-N-P/S1.md`
  (planning + proof obligation pattern).
- Doctrine: [[feedback-shell-must-not-overstate-semantic-truth]],
  [[feedback-bounded-smoke-slices]],
  [[feedback-fail-closed-validation]].
