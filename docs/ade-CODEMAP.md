# Module Authority Map — Ade

> **Status:** Living architectural document. Regenerated; not hand-edited.
> Per-project instance of `~/.claude/methodology/templates/codemap.md`.

> 11 crates, 391 canonical types, 1453 tests, 40 CI checks at HEAD (`694dd74`).

---

## Conventions

- A **module** in Ade is a Cargo workspace crate (smallest independently-buildable unit). One exception: `ade_network` is split by *submodule color* — its BLUE submodules and its RED submodules are documented as two separate entries below, because `.idd-config.json` `core_paths` resolves BLUE at the submodule path level rather than crate-wide. The `ade_core::consensus` submodule sits *inside* the BLUE `ade_core` crate and is covered by that entry. The `ade_ledger::block_validity` / `ade_ledger::consensus_view` / `ade_ledger::tx_validity` / `ade_ledger::mempool::admit` / `ade_ledger::mempool::ingress` / `ade_ledger::cert_classify` modules sit inside the BLUE `ade_ledger` crate and are covered by that entry; the **new `ade_ledger::producer` sub-tree (PHASE4-N-C — `forge`, `self_accept`, `state`)** and the **new top-level `ade_ledger::block_body_hash` module (PHASE4-N-C S4 single body-hash authority)** are likewise BLUE under the already-BLUE `ade_ledger` crate prefix. The RED `ade_ledger::consensus_input_extract`, the GREEN `ade_ledger::mempool::policy`, and the GREEN `ade_ledger::mempool::canonicalize` sit inside the BLUE `ade_ledger` crate but carry a different color by their own module doc-comment and the cluster TCB Color Maps — they are surfaced as sub-classification notes inside the `ade_ledger` entry. The `ade_testkit::mempool` sub-tree (PHASE4-N-E S2), the `ade_testkit::governance` sub-tree (PROPOSAL-PROCEDURES-DECODE PP-S2), and the **new `ade_testkit::producer` sub-tree (PHASE4-N-C — `reference_vectors`, `fixtures`, `replay`, `cross_impl_adapter`)** sit inside the GREEN `ade_testkit` crate, classified GREEN by their own module doc-comments and the cluster TCB Color Maps.
- Modules are listed by TCB color (BLUE → GREEN → RED), alphabetical within each color.
- TCB color sources, in order of authority:
  1. `.idd-config.json` `core_paths` — substring match against absolute path. BLUE matches: `ade_codec`, `ade_types`, `ade_crypto`, `ade_core`, `ade_ledger`, `ade_plutus`, and the 9 `ade_network` submodule paths (`mux/frame.rs`, `codec/`, `handshake/`, `chain_sync/`, `block_fetch/`, `tx_submission/`, `keep_alive/`, `peer_sharing/`, `n2c/`).
  2. `.idd-config.json` `_core_paths_doc` — `ade_runtime` is RED; `ade_testkit` is GREEN; `ade_node` is RED; `ade_network::mux::transport` and `ade_network::session` are RED; `ade_network::lib` and `ade_network::mux::mod` are GREEN barrels. `ade_core_interop` is **RED** per its `Cargo.toml` header comment and the PHASE4-N-B TCB Color Map; the `ade_core_interop::tx_submission` (S4) and `ade_core_interop::local_tx_submission` (S5) modules carry a GREEN sub-classification by their own module doc-comments and the PHASE4-N-E TCB Color Map. The RED binaries `ade_core_interop::bin::live_tx_submission_session` (S6) and **`ade_core_interop::bin::live_block_production_session` (PHASE4-N-C S7, NEW)** are the operator-action live evidence pattern (RED by their own file-header comments and the established `live_consensus_session` precedent).
  3. `docs/clusters/PHASE4-N-B/cluster.md` § "TCB Color Map" — `era_schedule`, `praos_state`, `vrf_cert`, `nonce`, `op_cert`, `leader_schedule`, `header_validate`, `fork_choice`, `rollback` are BLUE; `chain_selector`, `candidate_fragment` are GREEN; `genesis_parser` is RED; `ade_testkit::consensus` is GREEN; `ade_core_interop` is RED.
  4. `docs/clusters/completed/PHASE4-B1/cluster.md` § "TCB Color Map" — `ade_ledger::consensus_view`, `ade_ledger::block_validity` are BLUE; `ade_core::consensus::{header_validate, kes_check}` extensions are BLUE; `ade_testkit::validity` is GREEN; `ade_ledger::consensus_input_extract` is RED.
  5. PHASE4-B2 (tx-validity agreement) — `ade_ledger::tx_validity::*` and `ade_ledger::mempool::admit` are BLUE; `ade_ledger::mempool::policy` is GREEN (Tier-5); `ade_testkit::tx_validity` is GREEN.
  6. PHASE4-B3/B5 (tx value-conservation + Conway governance-cert accumulation) — all new/changed modules are BLUE under the already-BLUE crate prefixes.
  7. PHASE4-N-E (mempool wire ingress) — `ade_ledger::mempool::ingress` is BLUE (S1); `ade_ledger::mempool::canonicalize` is GREEN (S3); `ade_testkit::mempool::ingress_replay` is GREEN (S2); `ade_core_interop::tx_submission` is GREEN (S4); `ade_core_interop::local_tx_submission` is GREEN (S5); `ade_core_interop::bin::live_tx_submission_session` is RED (S6).
  8. PROPOSAL-PROCEDURES-DECODE — `ade_codec::conway::governance` is BLUE; the new `ProposalProcedure` canonical type in `ade_types::conway::governance` is BLUE; `ade_testkit::governance::proposal_procedures_replay` is GREEN.
  9. **NEW — `docs/clusters/PHASE4-N-C/cluster.md` § "TCB Color Map"** (cluster landed at HEAD; not yet archived to `completed/`) — `ade_ledger::producer::{forge, self_accept, state}` (S3/S4/S5) are **BLUE** (under the already-BLUE `ade_ledger` crate prefix and per the per-module `// Core Contract:` banners; originally planned at `ade_core::consensus::*` and relocated to `ade_ledger::producer::*` because `ade_ledger` already depends on `ade_core` and the forge body needs `LedgerState` + `mempool::admit` — the inverse would close a Cargo cycle, classification unchanged); `ade_ledger::block_body_hash` (S4 single body-hash authority) is **BLUE**; `ade_codec::shelley::opcert` (S2) and `ade_codec::shelley::tx_components` (S3/S4) are **BLUE**; `ade_core::consensus::opcert_validate` (S2) is **BLUE**; `ade_crypto::kes::KesSignature` (S1 closed signature artifact) is **BLUE**; `ade_runtime::producer::{signing, keys}` (S1) and `ade_runtime::producer::{scheduler, broadcast}` (S6) are **RED**; `ade_runtime::producer::tick_assembler` (S6) is **GREEN** (deterministic glue; pure-fn closure mechanically enforced by `ci/ci_check_scheduler_closure.sh`); `ade_testkit::producer::{reference_vectors, fixtures, replay, cross_impl_adapter}` (S1/S3/S4/S7) is **GREEN**; `ade_core_interop::bin::live_block_production_session` (S7) is **RED** (operator-action live evidence binary, matching `live_consensus_session.rs` + `live_tx_submission_session.rs`).

- **Active cluster at HEAD (PHASE4-N-C).** Located at `docs/clusters/PHASE4-N-C/` (9 files: `cluster.md`, 7 slice docs `N-C-S1.md..N-C-S7.md`, and `CE-N-C-8_PROCEDURE.md`). The cluster is **closed at HEAD `694dd74`**: all 14 registry rules introduced or strengthened (`DC-CRYPTO-03/04/05`, `DC-CONS-11/12/13/14/15/16`, `DC-LEDGER-12`, `CN-CONS-06/07`, `OP-OPS-04/05`; strengthens `T-DET-01` + `T-ENC-01`) are at `status = "enforced"`. CE-N-C-8's live half carries an `open_obligation` on `CN-CONS-06` (`blocked_until_operator_stake_available` per the OQ-11 conditional-closure pattern). Cluster docs have not yet moved to `docs/clusters/completed/PHASE4-N-C/` — `/cluster-close` will perform that archive when run; the regen here reflects HEAD as-is. Carried-active alongside: `docs/clusters/PHASE4-N-B/` (live consensus tip-agreement still pending operator log).

- **Delta since prior CODEMAP HEAD `928c2be` (this regeneration — PHASE4-N-C cluster, 7 slices S1–S7 over commits since `928c2be`).**

  **Structural deltas summarized:**
  - **NEW BLUE sub-tree:** `ade_ledger::producer::{forge, self_accept, state}` (S3 forge + S5 self-accept + S3 ProducerTick). Pure transition `forge_block(&ProducerTick) -> Result<(ForgedBlock, Vec<ForgeEffects>), ForgeError>`; type-level broadcast token `AcceptedBlock` whose constructor lives only in `self_accept.rs` (CN-CONS-07).
  - **NEW BLUE top-level module:** `ade_ledger::block_body_hash` (S4). Single Cardano-compatible canonical body-hash authority — `block_body_hash(&ShelleyBlock)` and `block_body_hash_from_buckets(...)` are the only two body-hash entry points; both `producer::forge` and `block_validity::header_input::computed_body_hash` consume them. Closes DC-CONS-16 (producer/validator hash the same bytes through the same encoder).
  - **NEW BLUE codec modules:** `ade_codec::shelley::opcert` (S2 — closed opcert 4-tuple encoder/decoder, `OpCertCodecError`; sole sanctioned standalone-opcert byte authority outside the embedded-in-header path); `ade_codec::shelley::tx_components` (S3/S4 — preserved-byte splitter for Conway `[body, witness_set, is_valid, aux_data]` 4-tuples, `TxComponents`).
  - **NEW BLUE consensus module:** `ade_core::consensus::opcert_validate` (S2) — `opcert_validate(opcert, cold_vk, expected_period, prev_counter) -> Result<(), OpCertError>`. Single RED→BLUE entry point for opcert acceptance (cold-signature, KES-period match, counter strictly-monotonic-per-(cold-key, node)).
  - **NEW closed canonical types (+10):** `ProducerTick`, `ForgedBlock`, `ForgeError`, `ForgeEffects`, `AcceptedBlock`, `SelfAcceptError`, `OpCertError`, `OpCertCodecError`, `TxComponents<'a>`, `KesSignature`. **Canonical type count: 381 → 391 (+10).** Per-BLUE-crate breakdown at HEAD: `ade_codec` 10 (+2: `OpCertCodecError`, `TxComponents`), `ade_types` 81 (unchanged), `ade_crypto` 13 (+1: `KesSignature` — the only BLUE closed signature artifact added by S1; private-key wrappers stay in RED `ade_runtime::producer::signing`), `ade_core` 44 (+1: `OpCertError`), `ade_ledger` 137 (+6: `ProducerTick`, `ForgedBlock`, `ForgeError`, `ForgeEffects`, `AcceptedBlock`, `SelfAcceptError`), `ade_plutus` 8, `ade_network` 98 = 391.
  - **NEW RED sub-tree:** `ade_runtime::producer::{signing, keys, scheduler, broadcast}` + GREEN `tick_assembler`. `signing` (S1) holds `VrfSigningKey`/`KesSecret`/`ColdSigningKey` with zeroize-on-drop; exposes `vrf_prove`/`kes_sign`/`kes_update`. `keys` (S1) parses cardano-cli `*.skey` text envelopes. `scheduler` (S6) is a pure RED state transition `scheduler_step(state, input, ledger_view) -> (state', effects)` mirroring N-B's `process_stream_input` shape — no wall-clock, no I/O. `broadcast` (S6) is a FIFO `BroadcastQueue` whose `enqueue` takes `AcceptedBlock` by value (type-level CN-CONS-07 gate). `tick_assembler` (S6, **GREEN**) composes captured RED outputs into a canonical `ProducerTick` deterministically.
  - **NEW RED operator-action live binary:** `ade_core_interop::bin::live_block_production_session` (S7) — CE-N-C-8 evidence capture (sustained-window N2N session against a private cardano-node; writes `docs/clusters/PHASE4-N-C/CE-N-C-LIVE_<date>.log`). Conditional on operator-provided testnet SPO stake; default hermetic main exits without connecting.
  - **NEW GREEN test harness sub-tree:** `ade_testkit::producer::{reference_vectors, fixtures, replay, cross_impl_adapter}`. `reference_vectors` (S1) — frozen `(seed, alpha) -> expected_proof/output` triples for VRF + KES with full Sum6 update-chain coverage (periods 0, 1, 32, 63). `fixtures` (S3/S4) + `replay` (S3/S4) — 3 in-source `ProducerReplayFixture` entries (`fixture_empty_mempool_leader`, `fixture_non_leader`, `fixture_two_tx_mempool_leader`) with baked-in signed artifacts. `cross_impl_adapter` (S7) — mechanical half of CN-CONS-06: decode round-trip + body-hash binding + decoder/encoder structural-field agreement over the synthetic corpus.
  - **NEW CI gates (+8):** `ci/ci_check_forge_purity.sh` (CE-N-C-3 / DC-CONS-13/14/15), `ci/ci_check_no_producer_body_encoder.sh` (CE-N-C-4 / DC-CONS-16), `ci/ci_check_no_private_keys_in_corpus.sh` (CE-N-C-3 / DC-CRYPTO-03/04/05 corpus discipline), `ci/ci_check_opcert_closed.sh` (CE-N-C-2 / DC-CONS-11/12), `ci/ci_check_private_key_custody.sh` (CE-N-C-1 / DC-CRYPTO-03/04/05 + OP-OPS-04), `ci/ci_check_producer_corpus_present.sh` (CE-N-C-7 / CN-CONS-06 mechanical half), `ci/ci_check_scheduler_closure.sh` (CE-N-C-6 / OP-OPS-05 + DC-CONS-13 + DC-MEM-03), `ci/ci_check_self_accept_gate.sh` (CE-N-C-5 / CN-CONS-07). **CI script count: 32 → 40 (+8).**
  - **NEW registry rules / strengthenings (+14):** `DC-CRYPTO-03/04/05` (VRF/KES signing transcript + KES evolution discipline); `DC-CONS-11/12` (opcert kes_period + strict-monotonic counter); `DC-CONS-13/14/15/16` (forge purity + replay byte-equality + leader-check gate + producer/validator body-hash parity); `DC-LEDGER-12` (every forged tx is admissible via `mempool::admit`); `CN-CONS-06/07` (cross-impl acceptance + self-acceptance bridge); `OP-OPS-04/05` (operator-supplied keys + slot-deadline SLA). Strengthens `T-DET-01` (producer authority surface) and `T-ENC-01` (producer body-hash byte path). All 14 at `status = "enforced"` at HEAD; CN-CONS-06 carries an `open_obligation` for the live half (CE-N-C-8 conditional).
  - **NEW test inventory (+67).** Per-crate at HEAD: `ade_codec` 151 → 162 (+11: 8 inline `opcert.rs` + 3 inline `tx_components.rs`). `ade_crypto` 49 → 51 (+2 new inline tests in `kes.rs` for `KesSignature` round-trip). `ade_core` 119 → 126 (+7 inline `opcert_validate.rs`). `ade_ledger` 478 → 492 (+14: 5 inline `forge.rs` + 5 inline `self_accept.rs` + 3 inline `block_body_hash.rs` + 1 elsewhere from minor edits). `ade_testkit` 304 → 312 (+8: 3 inline `cross_impl_adapter.rs` + 4 inline `replay.rs` + 1 integration test file `regen_producer_fixtures.rs` exporting a `#[test]` regen helper). `ade_runtime` 33 → 58 (+25: 10 inline `signing.rs` + 4 inline `keys.rs` + 4 inline `scheduler.rs` + 3 inline `broadcast.rs` + 3 inline `tick_assembler.rs` + 1 integration `producer_pipeline_slot_deadline.rs`). Other crates unchanged. **Test inventory 1386 → 1453 (+67).**
  - **No new outbound or inbound crate edges** introduced by N-C. `ade_runtime::producer` reuses the already-established `ade_runtime → {ade_core, ade_codec, ade_crypto, ade_ledger, ade_types}` edges; `ade_ledger::producer` reuses `ade_ledger → {ade_core, ade_codec, ade_crypto, ade_types}`; `ade_core_interop::bin::live_block_production_session` reuses `ade_core_interop → {ade_runtime, ade_network, …}`.

- Counts:
  - Crates: 11, from `Cargo.toml` `[workspace] members`. Unchanged.
  - Canonical types: 391, from `grep -rE "^(pub )?(struct|enum) "` across the full BLUE scope. Breakdown: `ade_codec` 10, `ade_types` 81, `ade_crypto` 13, `ade_core` 44, `ade_ledger` 137, `ade_plutus` 8, plus the 9 BLUE `ade_network` submodule paths 98. Registry `canonical_type_registry: null`, so a structural count is used. **+10 since `928c2be`** — PHASE4-N-C producer types listed above.
  - Tests: 1453 — count of `#[test]` / `#[tokio::test]` attributes across `crates/`. Reported as approximate per the template's fallback rule (test runner not executed). Per-crate at HEAD: `ade_codec` 162 (+11), `ade_types` 23, `ade_crypto` 51 (+2), `ade_core` 126 (+7), `ade_ledger` 492 (+14), `ade_plutus` 28, `ade_testkit` 312 (+8), `ade_runtime` 58 (+25), `ade_network` 176, `ade_node` 0, `ade_core_interop` 25. **+67 since `928c2be`**.
  - CI checks: 40 — file count under `ci/ci_check_*.sh`. **+8 since `928c2be`** — `ci_check_forge_purity.sh`, `ci_check_no_producer_body_encoder.sh`, `ci_check_no_private_keys_in_corpus.sh`, `ci_check_opcert_closed.sh`, `ci_check_private_key_custody.sh`, `ci_check_producer_corpus_present.sh`, `ci_check_scheduler_closure.sh`, `ci_check_self_accept_gate.sh` (all PHASE4-N-C). Full inventory at HEAD: `cbor_round_trip`, `ce_n_a_5_proof`, `chaindb_contract`, `chaindb_crash_safety`, `consensus_closed_enums`, `constitution_coverage`, `conway_cert_classification_closed`, `credential_discriminant_closed`, `crypto_vectors`, `dependency_boundary`, `deposit_param_authority`, `differential_divergence`, `forbidden_patterns`, **`forge_purity`** *(NEW)*, `gov_cert_accumulation_closed`, `hash_uses_wire_bytes`, `hfc_translation`, `ingress_chokepoints`, `ledger_determinism`, `mempool_ingress_closure`, `mempool_ingress_replay`, `module_headers`, `no_async_in_blue`, `no_chaindb_in_consensus_blue`, `no_density_in_fork_choice`, `no_float_in_consensus`, **`no_private_keys_in_corpus`** *(NEW)*, **`no_producer_body_encoder`** *(NEW)*, `no_secrets`, `no_semantic_cfg`, `no_signing_in_blue`, **`opcert_closed`** *(NEW)*, `pallas_quarantine`, **`private_key_custody`** *(NEW)*, **`producer_corpus_present`** *(NEW)*, `proposal_procedures_closed`, `recovery_contract`, `ref_provenance`, **`scheduler_closure`** *(NEW)*, **`self_accept_gate`** *(NEW)*. The forward-looking `ci/ci_check_no_fail_open_in_validation.sh` (the `if len == K { check } else { skip }` grep gate, DC-VAL-06 / DC-TXV) is still **not** shipped. No `.github/workflows/` yet.

---

## BLUE Modules — Pure Functional Core

> **Shared header (applies to every BLUE entry below).** Every `.rs` source file begins with the contract banner
> `// Core Contract:` and the following deny attributes are present in each crate's `lib.rs` (or, for `ade_network`,
> at the crate root — the BLUE submodules inherit them):
> `#![deny(unsafe_code)]`, `#![deny(clippy::unwrap_used)]`, `#![deny(clippy::expect_used)]`,
> `#![deny(clippy::panic)]`, `#![deny(clippy::float_arithmetic)]`.
> CI scripts that enforce the shared rules (all 7 scope the full BLUE set — the 6 BLUE crates plus the 9 BLUE `ade_network` submodule paths declared in `.idd-config.json` `core_paths`):
> - `ci/ci_check_module_headers.sh` — banner first-line check.
> - `ci/ci_check_forbidden_patterns.sh` — `HashMap`, `HashSet`, `IndexMap`/`IndexSet`/`indexmap::`, `SystemTime`, `Instant`, `std::fs`, `std::net`, `tokio`, `async fn`, `f32`/`f64`, `anyhow`, `rand::thread_rng`, `thread::spawn`, plus `unsafe` outside a documented allowlist.
> - `ci/ci_check_dependency_boundary.sh` — no BLUE crate depends on a RED crate.
> - `ci/ci_check_no_signing_in_blue.sh` — `SigningKey`/`SecretKey`/`PrivateKey`/`private_key`/`sign_message`/`sign_block` forbidden in BLUE.
> - `ci/ci_check_no_semantic_cfg.sh` — `#[cfg(feature = ...)]` and `cfg!(feature = ...)` forbidden in BLUE.
> - `ci/ci_check_hash_uses_wire_bytes.sh` — no hashing of `canonical_bytes` / re-encoded bytes in BLUE.
> - `ci/ci_check_ingress_chokepoints.sh` — only named `decode_*` chokepoints construct `PreservedCbor`.
> - `ci/ci_check_pallas_quarantine.sh` — `pallas-*` references confined to `ade_plutus`.
> - `ci/ci_check_no_async_in_blue.sh` *(PHASE4-N-A, S-A1)* — async constructs forbidden anywhere in the BLUE scope. Enforces DC-CORE-01.
>
> Three additional CI scripts narrow the shared header to the `ade_core::consensus` tree (PHASE4-N-B): `ci/ci_check_no_chaindb_in_consensus_blue.sh`, `ci/ci_check_no_float_in_consensus.sh`, `ci/ci_check_consensus_closed_enums.sh` (TARGETS scope: `crates/ade_core/src/consensus/`, `crates/ade_ledger/src/block_validity/`, `crates/ade_ledger/src/tx_validity/`, `crates/ade_ledger/src/mempool/`).
> A fourth narrow check enforces a single fork-choice rule: `ci/ci_check_no_density_in_fork_choice.sh` (DC-CONS-03).
> A fifth narrow check (PHASE4-B3) enforces deposit-parameter authority: `ci/ci_check_deposit_param_authority.sh` (DC-TXV-07).
> A sixth narrow check (PHASE4-B5) enforces gov-cert accumulation closure: `ci/ci_check_gov_cert_accumulation_closed.sh` (DC-LEDGER-09).
> A seventh narrow check (OQ5/COMMITTEE/DREP/ENACTMENT-COMMITTEE-FIDELITY + ENACTMENT-COMMITTEE-WRITEBACK) enforces credential-discriminant closure: `ci/ci_check_credential_discriminant_closed.sh` (DC-LEDGER-10).
> An eighth narrow check (PROPOSAL-PROCEDURES-DECODE, PP-S1) enforces proposal_procedures closure: `ci/ci_check_proposal_procedures_closed.sh` (DC-LEDGER-11).
>
> **NEW — Eight narrow checks added by PHASE4-N-C (the producer-authority gate set):**
> - `ci/ci_check_forge_purity.sh` (DC-CONS-13/14/15) — grep gate on `crates/ade_ledger/src/producer/forge.rs`: forbids `std::time::SystemTime`, `Instant`, `rand`, `HashMap`, `std::env`, `std::fs`, `tokio`, `async`. Asserts forge is a pure function of `ProducerTick`.
> - `ci/ci_check_no_producer_body_encoder.sh` (DC-CONS-16) — repo-wide gate: forbids any new `pub fn .*encode_block_body` outside the canonical single-authority site. Producer + validator hash the same bytes through the same encoder.
> - `ci/ci_check_no_private_keys_in_corpus.sh` (DC-CRYPTO-03/04/05 corpus discipline) — gate on `crates/ade_testkit/fixtures/producer/` + `crates/ade_testkit/src/producer/fixtures.rs`: forbids byte-literal patterns plausibly being private-key seeds; replay corpora carry signed artifacts only.
> - `ci/ci_check_opcert_closed.sh` (DC-CONS-11/12) — gate on `crates/ade_codec/src/shelley/opcert.rs` + `crates/ade_core/src/consensus/opcert_validate.rs`: closed `OpCertCodecError` + closed `OpCertError`, no parallel opcert encoders/decoders elsewhere, monotonic-counter discipline preserved at the producer-side acceptance boundary.
> - `ci/ci_check_private_key_custody.sh` (DC-CRYPTO-03/04/05 + OP-OPS-04) — gate on the full BLUE scope + `ade_runtime::producer/`: no `*SigningKey` / `KesSecret` / `ColdSigningKey` types in BLUE public APIs; no `cardano_crypto::vrf::VrfDraft03::prove` / `KesAlgorithm::sign_kes` / `update_kes` call outside `ade_runtime::producer::signing`.
> - `ci/ci_check_producer_corpus_present.sh` (CN-CONS-06 mechanical half) — checks that `crates/ade_testkit/src/producer/fixtures.rs` carries at least one fixture with a non-empty `expected_forged` capture so the cross-impl adapter can mechanically assert byte-identical bind.
> - `ci/ci_check_scheduler_closure.sh` (DC-CONS-13 + DC-MEM-03 + OP-OPS-05) — six guards on `crates/ade_runtime/src/producer/{scheduler.rs, broadcast.rs, tick_assembler.rs}`: closed `SchedulerInput`/`SchedulerEffect`/`SchedulerHaltReason`/`BroadcastError`/`TickAssemblyError`, `BroadcastQueue::enqueue` takes `AcceptedBlock` by value (no `&AcceptedBlock`, no raw `Vec<u8>`), `assemble_tick` is a pure function (no `tokio`/`SystemTime`/`Instant`/`rand`/`HashMap`/`async`).
> - `ci/ci_check_self_accept_gate.sh` (CN-CONS-07) — six guards on `crates/ade_ledger/src/producer/self_accept.rs`: `AcceptedBlock` struct-literal construction confined to `self_accept.rs`, exactly one `-> [Result<]AcceptedBlock` return-type site, no `impl Default|From|TryFrom for AcceptedBlock`, `AcceptedBlock.bytes` private, `SelfAcceptError` closed (no `#[non_exhaustive]`, no `String`-bearing variant), `self_accept` body calls `block_validity(` and does not re-implement validator sub-steps (`validate_and_apply_header`, `decode_block`, `block_body_hash` forbidden in production view of the file), and no `pub fn` returns raw `Vec<u8>` / `&[u8]` outside the `as_bytes` / `into_bytes` accessors.
>
> Two checks narrow the shared header to the wire-ingress path of `ade_ledger::mempool` (PHASE4-N-E):
> - `ci/ci_check_mempool_ingress_closure.sh` (DC-MEM-03, S1).
> - `ci/ci_check_mempool_ingress_replay.sh` (DC-MEM-04, S2 + S3).
>
> A BLUE crate or BLUE `ade_network` submodule that adds a feature flag, an async function, a `HashMap`, or a RED dep fails CI on push.
> The 3 RED-scope CI scripts (`ci_check_chaindb_contract.sh`, `ci_check_recovery_contract.sh`, `ci_check_chaindb_crash_safety.sh`) and the 1 evidence script `ci_check_ce_n_a_5_proof.sh` are not part of this shared header — they are documented in the cross-module CI matrix at the bottom.

---

### `ade_codec`

> **Status change at HEAD `694dd74` (PHASE4-N-C S2 + S3/S4 — two new closed sub-modules under `shelley/`).** Between `928c2be` and HEAD, the Shelley codec gained two new BLUE sub-modules: `ade_codec::shelley::opcert` (S2 — closed 4-tuple opcert encoder/decoder + `OpCertCodecError`, the sole sanctioned standalone-opcert byte authority) and `ade_codec::shelley::tx_components` (S3/S4 — preserved-byte splitter for Conway `[body, witness_set, is_valid, aux_data]` 4-tuples + `TxComponents<'a>`). The pre-N-C `ade_codec::shelley::block` encoder/decoder for the header `operational_cert` field is unchanged at HEAD; the new standalone `opcert.rs` module exposes a separate top-level entry point that produces cardano-cli-byte-identical output for fixture parity. All prior PP-S1/B3/B4/B5/OQ5 narrative carries forward.

| Attribute | Value |
|-----------|-------|
| **Purpose** | Owns Cardano-canonical CBOR ingress: the only place in the workspace that turns raw bytes into typed semantic values, with wire-byte preservation for every hash-bearing structure. **NEW — PHASE4-N-C:** also owns the canonical standalone opcert byte authority (`shelley::opcert::{encode_opcert, decode_opcert}` — cardano-cli-text-envelope parity) and the canonical Conway-tx preserved-byte splitter (`shelley::tx_components::split_conway_tx_components` — reused by both the BLUE block-forge body assembler in `ade_ledger::producer::forge` and the body-hash recomputation path). |
| **Creates** | `PreservedCbor<T>`, `RawCbor`, `BlockEnvelope`, `ByronDecodedBlock`, `CodecContext`, `CodecError` (incl. `UnknownCertTag`, `DuplicateMapKey`, `TrailingBytes`, `InvalidCborStructure`), `ContainerEncoding`, `IntWidth`, plus era-tagged block/tx wrappers under `byron/`, `shelley/`, `allegra/`, `mary/`, `alonzo/`, `babbage/`, `conway/`. Functions (not types): `conway::cert::{decode_conway_certs, decode_drep}`, `conway::withdrawals::{decode_withdrawals, withdrawals_sum}`, `conway::governance::{decode_proposal_procedures, encode_proposal_procedures}` (PP-S1), `shelley::cert::read_pool_registration_cert`, both era `decode_stake_credential`. **NEW — PHASE4-N-C:** `OpCertCodecError` (closed sum: `BadArrayHeader`, `BadFieldType`, `WrongHotVkeyLength`, `WrongSigmaLength`, `SequenceNumberOverflow`, `KesPeriodOverflow`, `TrailingBytes`); `TxComponents<'a> { body_bytes, witness_set_bytes, is_valid, aux_data_bytes }`. Functions: `shelley::opcert::{encode_opcert, decode_opcert, write_opcert_fields_into}`; `shelley::tx_components::split_conway_tx_components`. |
| **Interprets** | All canonical Cardano CBOR — block envelopes, era-specific blocks, tx bodies, tx outs, certificates, addresses. Conway certificate array (closed over CDDL tags 0..18, owner-complete) and Conway withdrawals map (deduplicated). Both era credential decoders preserve the key/script tag. Sole authority for `PreservedCbor::new` (constructor is `pub(crate)`). CIP-1694 `proposal_procedure` array via PP-S1's typed decoder/encoder. **NEW — PHASE4-N-C:** the standalone cardano-cli `OperationalCertificate` 4-tuple (`[hot_vkey: bstr(32), sequence_number: uint, kes_period: uint, sigma: bstr(64)]`) and the Conway transaction 4-tuple (`[body, witness_set, is_valid, aux_data_or_nil]`) — preserved-byte slicing only; no re-encoding. |
| **MUST NOT** | (1) Construct `PreservedCbor` outside `ade_codec` (`pub(crate)` + `ci_check_ingress_chokepoints.sh`). (2) Re-encode wire bytes when computing hashes (`ci_check_hash_uses_wire_bytes.sh`). (3) Use any forbidden BLUE pattern. (4) Depend on any other workspace crate except `ade_types`. (5) `conway::cert` (DC-LEDGER-08) — no unknown-tag swallow; owner-complete; no catch-all. (6) `conway::withdrawals` — no last-wins on duplicate `RewardAccount`. (7) `decode_stake_credential` (DC-LEDGER-10) — must not erase the credential tag. (8) `conway::governance` (DC-LEDGER-11) — no silent skip on unknown `GovAction`; no opaque pass-through at body codec key 20. **(9) NEW — `shelley::opcert` (DC-CONS-11/12, S2):** must produce/consume the standalone 4-tuple cardano-byte-identically (the `cardano-api` reference encoder is the byte oracle); must reject every shape failure with a dedicated `OpCertCodecError` variant (no catch-all, no `#[non_exhaustive]`); must not become a second header-embedded opcert encoder (single-authority is owned by `shelley::block`'s `operational_cert` field path — `opcert.rs` is the standalone fixture-parity entry only). Enforced by `ci/ci_check_opcert_closed.sh`. **(10) NEW — `shelley::tx_components` (DC-CONS-13/16, S3/S4):** must return preserved-byte slices that alias the input buffer (no `to_vec` / clones); must reject `tx_cbor` that is not a definite 4-tuple with `CodecError::InvalidCborStructure`; must reject trailing bytes after the 4 elements; must not re-encode the boolean validity flag or the auxiliary-data CBOR null. The output is the single canonical input shape both `ade_ledger::producer::forge::forge_block` and the body-hash recomputation rely on. |
| **Inbound deps** | `ade_ledger`, `ade_plutus`, `ade_testkit`, `ade_network`, `ade_runtime`, `ade_core_interop`. **No new inbound crate edge in PHASE4-N-C** — the new `shelley::opcert` is consumed by `ade_ledger::producer::forge` via the already-established `ade_ledger → ade_codec` edge; `shelley::tx_components` is similarly consumed via that edge. |
| **Outbound deps** | `ade_types`. No external dependencies; std-only. Dev-deps: `serde_json`, `toml`. **No new outbound edge in PHASE4-N-C.** |
| **Entry points** | `ade_codec::cbor::envelope::decode_block_envelope`, `ade_codec::cbor`, `ade_codec::traits::AdeEncode`, `ade_codec::CodecContext`, per-era `decode_*_block`, `ade_codec::address::decode_address`. B2: `ade_codec::conway::tx::decode_conway_tx_body`. B3: `ade_codec::conway::cert::decode_conway_certs` and `ade_codec::conway::withdrawals::{decode_withdrawals, withdrawals_sum}`. PP-S1: `ade_codec::conway::governance::{decode_proposal_procedures, encode_proposal_procedures}`. **NEW — PHASE4-N-C:** `ade_codec::shelley::opcert::{encode_opcert, decode_opcert, OpCertCodecError}` (consumed by `ade_ledger::producer::forge` and the S2 fixture-parity tests); `ade_codec::shelley::tx_components::{split_conway_tx_components, TxComponents}` (consumed by `ade_ledger::producer::forge` for the 4-bucket body layout and by the body-hash recomputation path). |
| **Key modules** | `cbor/`, `byron/`, `shelley/` (incl. `cert.rs`'s shared `pub(crate) read_pool_registration_cert`, **NEW — `opcert.rs`** standalone 4-tuple encoder/decoder + 8 inline tests, **NEW — `tx_components.rs`** Conway tx-tuple splitter + 3 inline tests), `allegra/`, `mary/`, `alonzo/`, `babbage/`, `conway/` (incl. `cert.rs` owner-complete, `withdrawals.rs`, `governance.rs` PP-S1), `address/`, `preserved.rs`, `traits.rs`, `primitives.rs`, `error.rs`. |

---

### `ade_core`

> **Status change at HEAD `694dd74` (PHASE4-N-C S2 — new BLUE consensus module `opcert_validate`).** Between `928c2be` and HEAD, the `ade_core::consensus` tree gained `opcert_validate.rs` — the single sanctioned RED→BLUE entry point for accepting an `OperationalCert` value at the `ProducerTick → forge_block` boundary. Wraps the existing `ade_crypto::kes::verify_opcert` byte authority and layers on (a) KES-period equality against the expected period at the forged slot (DC-CONS-11), (b) strict-monotonic counter discipline per (cold-key, node) with the `prev_counter == None` first-opcert allowance (DC-CONS-12), and (c) closed shape rejection. All prior consensus-tree narrative carries forward.

| Attribute | Value |
|-----------|-------|
| **Purpose** | BLUE authoritative Praos consensus core. Owns canonical types and pure state-transitions that decide which header / chain Ade accepts: HFC era schedule + slot↔era↔time translation, Praos chain-dep state (nonce / op-cert counters), VRF cert verification + leader-eligibility predicate, KES signature + op-cert period verification wired into header admission, header validation pipeline, fork-choice, rollback authority, leader-schedule query, canonical encodings of all chain-dep state and chain events. **NEW — PHASE4-N-C:** owns the producer-side opcert acceptance authority (`opcert_validate` — the single sanctioned RED→BLUE op-cert entry, mirror of the validator-side `verify_header_kes`). |
| **Creates** | **Schedule:** `BootstrapAnchorHash`, `EraSchedule`, `EraSummary`, `EraLocation`. **State:** `PraosChainDepState`, `OpCertCounterMap`, `Nonce`. **Events/points:** `Point`, `ChainHash`, `BlockDistance`, `SecurityParam`, `ChainEvent`, `ChainSelectionReject`. **Errors:** `HFCError`, `SlotTimeError`, `OutsideForecastRange`, `HeaderValidationError`, `FieldError`, `FieldKind`, `VrfCertError`, `OpCertCounterError`, `NonceEvolutionError`, `LeaderScheduleError`. **Header surface:** `HeaderInput`, `HeaderVrf`, `HeaderKes`, `ValidatedHeaderSummary`, `HeaderApplied`. **Fork-choice:** `TiebreakerView`, `CandidateFragment`, `ChainSelectorState`, `ForkChoiceError`. **Op-cert/nonce:** `OpCertObservation`, `NonceInput`. **VRF:** `VrfRole`, `VerifiedVrf`, `StakeFraction`, `ActiveSlotsCoeff`. **Leader schedule:** `LeaderScheduleQuery`, `LeaderScheduleAnswer`. **Ledger view boundary:** `LedgerView` trait. **Rollback:** `RollBackRequest`, `RollBackApplied`. **Encoding:** `DecodeError`. **NEW — PHASE4-N-C:** `OpCertError` (closed sum: `BadColdSignature`, `PeriodMismatch { found, expected }`, `CounterRepeat { counter }`, `CounterRegression { found, prev }`, `BadHotVkeyLength { found }`, `BadSigmaLength { found }`, `MalformedColdVk`). 44 public types across the consensus tree (+1 since `928c2be`). |
| **Interprets** | Canonical inputs from the `ade_runtime` shell — `HeaderInput` projections, `EraSchedule` materialized once from genesis JSON, `LedgerView` snapshots from `ade_ledger::consensus_view::PoolDistrView`, ordered `StreamInput` events. KES check verifies hot KES key signature over the header body CBOR bytes. All inputs are typed values. **NEW — PHASE4-N-C:** `opcert_validate` consumes an `OperationalCert`, the operator's `Ed25519VerificationKey` cold key, an `expected_period: u64` (derived RED-side from slot + anchor; pure-input on `ProducerTick`), and an `Option<u64> prev_counter`. It interprets nothing at the wire layer — its inputs are typed values produced by the codec / RED loader. |
| **MUST NOT** | (1) Take or accept a `&ChainDb` reference (`ci_check_no_chaindb_in_consensus_blue.sh`). (2) Use any `f32`/`f64` (`ci_check_no_float_in_consensus.sh`). (3) Add `#[non_exhaustive]` to any consensus enum, open-tail `Other`/`Unknown`, owned `String` fields, or `Box<dyn ...>` (`ci_check_consensus_closed_enums.sh`). (4) Reference `density` in `fork_choice.rs`/`candidate.rs` outside a `// no-density:` annotation (`ci_check_no_density_in_fork_choice.sh`). (5) Read wall-clock. (6) Use `HashMap`/`HashSet`; `OpCertCounterMap` uses `BTreeMap`. (7) `async fn`, `.await`, `tokio` (`ci_check_no_async_in_blue.sh`). (8) Construct a `PreservedCbor`. (9) Re-derive stake snapshots (DC-CONSENSUS-02). (10) Bypass the canonical `validate_and_apply_header` pipeline. (11) B1 fail-closed (DC-VAL-06). (12) B1 KES rule (DC-CRYPTO-01). (13) All shared-header BLUE rules. **(14) NEW — `consensus::opcert_validate` (DC-CONS-11/12, S2):** `OpCertError` MUST remain closed — no `#[non_exhaustive]`, no `String`-bearing variants, no catch-all variant; the only sanctioned construction sites are inside `opcert_validate.rs` itself (enforced by `ci/ci_check_opcert_closed.sh` guards 3 + 4). `opcert_validate` MUST run shape checks first, then the cold-signature check, then the period equality check, then the counter discipline (this is the documented order and the test-pinned order; reordering changes the error verdict for ambiguous inputs and breaks replay equivalence). `prev_counter == None` MUST be the only path that accepts a starting counter; `prev_counter == Some(c)` MUST reject `opcert.sequence_number == c` (repeat) AND `opcert.sequence_number < c` (regression) with distinct variants. `opcert_validate` MUST NOT call into any external counter store, ambient state, or wall-clock — `prev_counter` is supplied by the caller (RED durable per-node state) and BLUE never trusts an in-memory-only counter. `opcert_validate` MUST NOT be the producer's only signature surface — `verify_header_kes` still owns the validator-side KES check, and the two surfaces share `ade_crypto::kes::verify_opcert` as the byte authority. |
| **Inbound deps** | `ade_ledger` (B1 + N-C — `block_validity` calls `validate_and_apply_header`; `producer::forge` calls `opcert_validate` + `is_leader_for_vrf_output`), `ade_runtime` (consensus orchestrator + genesis parser + N-C producer scheduler), `ade_testkit`, `ade_core_interop`. **No new inbound crate edge in PHASE4-N-C** — the `ade_ledger::producer → ade_core::consensus::opcert_validate` consumption reuses the established `ade_ledger → ade_core` edge. |
| **Outbound deps** | `ade_types`, `ade_crypto` (`blake2b`, `verify_vrf`, `kes::{verify_kes, verify_opcert, build_opcert_signable}` — the last three are unchanged; N-C reuses them). Dev-deps: `ade_testkit`, `serde_json`, `cardano-crypto`. |
| **Entry points** | `use ade_core::consensus::{...}` aggregator, `ade_core::consensus::ledger_view::LedgerView`, `ade_core::consensus::vrf_cert::{vrf_input, verify_praos_vrf, ...}`, `ade_core::consensus::kes_check::{verify_header_kes, expect_size}`, `ade_core::consensus::praos_state::*`, `ade_core::consensus::header_summary::*`. Top-level transitions: `validate_and_apply_header`, `select_best_chain`, `apply_rollback`, `apply_nonce_input`, `apply_op_cert`, `query_leader_schedule` + `is_leader_for_vrf_output`, `verify_vrf_cert` + `verify_praos_vrf` + `check_leader_claim` + `is_leader`, `tiebreaker_prefer`, `encode/decode_chain_dep_state`, `encode/decode_chain_event`. **NEW — PHASE4-N-C:** `ade_core::consensus::opcert_validate::{opcert_validate, OpCertError}` (the single producer-side opcert acceptance entry; consumed by `ade_ledger::producer::forge`). |
| **Key modules** | `consensus/era_schedule.rs`, `consensus/praos_state.rs`, `consensus/events.rs`, `consensus/errors.rs`, `consensus/vrf_cert.rs`, `consensus/kes_check.rs`, `consensus/nonce.rs`, `consensus/op_cert.rs`, `consensus/leader_schedule.rs`, `consensus/header_summary.rs` + `consensus/header_validate.rs`, `consensus/candidate.rs`, `consensus/fork_choice.rs`, `consensus/rollback.rs`, `consensus/ledger_view.rs`, `consensus/encoding.rs`, **NEW — `consensus/opcert_validate.rs`** (PHASE4-N-C S2; 234 LOC + 7 inline tests). |

---

### `ade_crypto`

> **Status change at HEAD `694dd74` (PHASE4-N-C S1 — new closed signature artifact `KesSignature`).** Between `928c2be` and HEAD, `ade_crypto::kes` gained the closed `KesSignature` type — a `[u8; 448]` Sum6KES signature wrapper with a length-validating `from_bytes`, a redact-on-format `Debug` impl, and the public `SUM6_KES_SIG_LEN` constant. This is the BLUE counterpart of the RED `KesSecret`: the producer's RED `kes_sign` returns `KesSignature`, BLUE consumes it across the RED→BLUE boundary as a captured artifact (no private-key bytes). All prior crypto-tree narrative carries forward.

| Attribute | Value |
|-----------|-------|
| **Purpose** | Owns pure cryptographic verification primitives — Blake2b hashing, Ed25519 / Byron-bootstrap signature verification, KES verification with operational certificates, VRF verification, plus the **closed signature-artifact types** (`KesSignature`, the previously existing `VrfProof` + `VrfOutput` + `Ed25519Signature`) that BLUE consumes across the RED→BLUE boundary. Verification only — signing lives in `ade_runtime::producer::signing`. |
| **Creates** | `Blake2b224`, `Blake2b256`, `HashAlgorithm` trait, `Ed25519VerificationKey`, `Ed25519Signature`, `ByronExtendedVerificationKey`, `KesVerificationKey`, `KesPeriod`, `OperationalCertData`, `VrfVerificationKey`, `VrfProof`, `VrfOutput`, `CryptoError`. **NEW — PHASE4-N-C S1:** `KesSignature(pub [u8; SUM6_KES_SIG_LEN])` — closed 448-byte Sum6KES signature artifact (BLUE consumes; never holds private-key material); `SUM6_KES_SIG_LEN: usize = 448` (public constant; BLUE callers construct via `KesSignature::from_bytes`). |
| **Interprets** | Verification key / signature / proof byte structures. Not a CBOR parser — accepts already-decoded byte slices. |
| **MUST NOT** | (1) Implement signing (`ci_check_no_signing_in_blue.sh`). (2) Allocate global state. (3) Use any BLUE forbidden pattern. (4) Use `unsafe` outside the allowlisted FFI in `src/vrf.rs`. (5) `build_opcert_signable` must produce the spec-correct raw concatenation. **(6) NEW — `KesSignature` (DC-CRYPTO-04, S1):** must remain a closed length-pinned wrapper — no `#[non_exhaustive]`, no public field other than the byte array, no `From<Vec<u8>>` impl bypassing the length check; the only construction path is `KesSignature::from_bytes(&[u8])` which returns `Err(CryptoError::MalformedSignature)` on `len != 448`. The custom `Debug` impl MUST redact the bytes (length-tagged only — no hex spew). `KesSignature` MUST NOT carry any `PartialOrd`/`Ord`/`Hash` derive that would allow ambient-ordering channels to leak. **(7) NEW — companion forbid (DC-CRYPTO-04, S1):** no `Drop` impl that zeroizes — `KesSignature` is BLUE and not secret; zeroize discipline lives in `ade_runtime::producer::signing` for `KesSecret`. |
| **Inbound deps** | `ade_core`, `ade_ledger`, `ade_plutus`, `ade_testkit`, `ade_core_interop`, `ade_runtime`. **New consumption sites in PHASE4-N-C** — `ade_runtime::producer::signing::kes_sign` returns `KesSignature`; `ade_ledger::producer::state::ProducerTick` carries `kes_signature: KesSignature`; `ade_runtime::producer::tick_assembler::TickInputs` carries the same. No new outbound crate edges. |
| **Outbound deps** | `ade_types`, `blake2`, `ed25519-dalek`, `cardano-crypto` (vrf-draft03 + kes-sum + dsign features, `default-features = false`). |
| **Entry points** | `ade_crypto::blake2b::blake2b_256`, `blake2b_224`, `blake2b_256`, `block_header_hash`, `transaction_id`, `script_hash`, `credential_hash`, `verify_ed25519`, `verify_byron_bootstrap`, `verify_kes`, `verify_opcert`, `verify_vrf`. **NEW — PHASE4-N-C:** `ade_crypto::kes::{KesSignature, SUM6_KES_SIG_LEN}` (consumed by `ade_runtime::producer::signing::kes_sign`'s return type, by `ade_ledger::producer::state::ProducerTick.kes_signature`, by `ade_runtime::producer::tick_assembler::TickInputs.kes_signature`, and by `ade_testkit::producer::fixtures`'s baked-in signed-artifact constants). |

---

### `ade_ledger`

> **Status change at HEAD `694dd74` (PHASE4-N-C — three new BLUE sub-modules + one new BLUE top-level module).** Between `928c2be` and HEAD: (a) the new `ade_ledger::producer/` sub-tree (`forge.rs` + `self_accept.rs` + `state.rs` + `mod.rs`) hosts the BLUE block-forge authority — pure transition `forge_block(&ProducerTick) -> Result<(ForgedBlock, Vec<ForgeEffects>), ForgeError>` (S3), the self-acceptance bridge with the type-level `AcceptedBlock` broadcast token whose constructor lives only in `self_accept.rs` (S5), and the canonical `ProducerTick` input value (S3); (b) the new `ade_ledger::block_body_hash` top-level module (S4) hosts the **single** Cardano-compatible block-body-hash recipe — `block_body_hash(&ShelleyBlock)` and `block_body_hash_from_buckets(...)` are the only two body-hash entry points and both `producer::forge` and `block_validity::header_input` consume them; (c) the host-crate placement for the producer sub-tree was originally planned at `ade_core::consensus::*` and relocated to `ade_ledger::producer::*` because `ade_ledger` already depends on `ade_core` and the forge body needs `LedgerState` + `mempool::admit::*` — the inverse would close a Cargo cycle (classification unchanged: BLUE, deterministic, fail-closed). All prior B-track / N-E / PROPOSAL-PROCEDURES-DECODE narrative carries forward.

| Attribute | Value |
|-----------|-------|
| **Purpose** | The functional core (ledger half): stateless ledger rules for every Cardano era; the B1 top-level block-validity verdict; the B2 top-level transaction-validity verdict + mempool admission; the B3 full Conway value-conservation accounting; the B4 closed Conway cert-state accumulation; the B5 closed Conway governance-cert accumulation; the live committee-enactment write-back; the single BLUE chokepoint `mempool::ingress::mempool_ingress` from wire ingress into `admit`. **NEW — PHASE4-N-C:** the BLUE producer authority — the single sanctioned `forge_block` transition over canonical `ProducerTick` (DC-CONS-13/14/15 + DC-LEDGER-12), the self-acceptance bridge with the type-level `AcceptedBlock` broadcast token (CN-CONS-07), and the canonical block-body-hash recipe (DC-CONS-16). |
| **Creates** | Carry-forward: `LedgerState`, `EpochState`, `UTxOState`, `TxOut`, `Value`, `MultiAsset`, `AssetName`, `DelegationState`, `CertState`, `PoolParams`, `ConwayGovState`, `GovActionState`, `RatificationResult`, `EnactmentEffects`, `ProtocolParameters`, `BlockApplyResult`, `BlockVerdict`, `TxVerdict`, `ScriptPosture`, `EpochBoundaryResult`, `LedgerFingerprint`, `Rational`, `WitnessInfo`, layered `LedgerError`. **B1:** `PoolDistrView`, `PoolEntry`, `BlockValidityOutcome`, `DecodedBlock`, `BlockValidityVerdict`, `BlockRejectClass`, `BlockValidityError`, `FieldError`, `FieldKind`, `MissingInput`, `VerdictSurface`, `SurfaceDecodeError`. **B2:** `TxValidityVerdict`, `TxRejectClass`, `TxValidityError`, `TxValidityOutcome`, `TxVerdictSurface`, `TxSurfaceDecodeError`, `DecodedTx`, `VKeyWitnessRef`, `WitnessClosureError`, `WitnessField`, `SignerSource`, `RequiredSigners`, `RequiredSignerError`, `RequiredSignerField`, `ResolvedInputs`, `ResolvedOutput`, `MempoolState`, `AdmitOutcome` (BLUE); `OrderPolicy` (GREEN). **B3:** `ConwayOnlyDepositParams`, `ConwayDepositParams`, `ValidationEnvironmentError`, `UnsupportedStateDependentDepositAccounting`, `EraInvalidCertificateError`. **B4:** `ConwayCertAction`, `GovernanceOwner`, `GovernanceCertEffect`, `OwnerTaggedEffect`, `ConwayCertOutcome`, `ConwayCertEnv`. **B5:** `GovCertEnv`. **PHASE4-N-E S1:** `IngressSource`, `IngressEvent`. **PHASE4-N-E S3:** `PeerId`, `PeerSubmissionQueue`. **NEW — PHASE4-N-C:** `ProducerTick` (S3, closed canonical producer-tick input — 16 fields explicitly carrying slot, base_state, mempool, mempool_tx_bytes, pparams, leader_answer, vrf_proof, vrf_output, vrf_vkey, kes_period, kes_signature, opcert, cold_vk, prev_opcert_counter, block_number, prev_hash, protocol_version); `ForgedBlock { bytes, block }` (S3); `ForgeError` (closed: `NotLeader`, `OpCertRejected`, `TxSetNotAdmissiblePrefix`, `MempoolWidthMismatch`, `MempoolAcceptedMismatch`, `BadKesSignatureLength`, `TxComponentSplit`); `ForgeEffects::ReadyForSelfAccept { … }`; `AcceptedBlock` (S5, type-level broadcast token — private `bytes` field, struct-literal construction confined to `self_accept.rs`); `SelfAcceptError::Rejected(BlockValidityError)`. Functions: `producer::forge::forge_block`, `producer::self_accept::self_accept`, `block_body_hash::{block_body_hash, block_body_hash_from_buckets}`. |
| **Interprets** | Decoded canonical types from `ade_codec`. Hosts per-era validation composers and the late-era witness decoder. B1: `block_validity::header_input::decode_block`; `block_validity::encoding::*`. B2: `tx_validity::phase1::decode_tx`. B3: `conway::check_conway_coin_conservation` runs the full equation. B4: `rules::accumulate_tx_certs` era-dispatches. B5: feeds owner-complete `ConwayCert` into `gov_cert::apply_conway_gov_cert`. ENACTMENT-COMMITTEE-WRITEBACK: `rules::apply_epoch_boundary*` applies via `governance::apply_committee_enactment`. PHASE4-N-E S1: `mempool_ingress` is a pure pass-through. **NEW — PHASE4-N-C:** `producer::forge::forge_block` interprets the canonical `ProducerTick` only — it composes (a) the validator's `is_leader_for_vrf_output` over `tick.leader_answer` ⊕ `tick.vrf_output` (NC-VRF-3 single source of leader truth), (b) the producer-side `opcert_validate` over `tick.opcert` ⊕ `tick.cold_vk` ⊕ derived `expected_period` ⊕ `tick.prev_opcert_counter`, (c) re-admission of `tick.mempool_tx_bytes` against `tick.base_state` via the canonical `mempool::admit::admit` (DC-LEDGER-12; no producer-side re-admit; the prefix must match the snapshot's `mempool.accepted()` order byte-identically), (d) preserved-byte assembly of the Conway 4-bucket block body via `ade_codec::shelley::tx_components::split_conway_tx_components` and the body-hash recipe in `block_body_hash::block_body_hash_from_buckets`. `producer::self_accept::self_accept` interprets the forged byte slice exactly once — by handing it to `block_validity::transition::block_validity` (the single closed validator authority — `self_accept` never re-implements validator sub-steps; `ci/ci_check_self_accept_gate.sh` guard 5 forbids any `validate_and_apply_header` / `decode_block` / `block_body_hash` call inside `self_accept.rs`'s production view). |
| **MUST NOT** | (1) Perform I/O. (2) Use `HashMap`, floats, or any other BLUE-forbidden pattern. (3) Hash anything other than wire bytes. (4) Construct `PreservedCbor` outside named decoders. (5) Depend on `ade_runtime`. (6) Use signing primitives or `#[cfg(feature)]` gates. (7) Omit `// Core Contract:` banner. (8) B1 `consensus_view` — no rederived stake snapshot. (9) B1 `block_validity` — never `Valid` while skipping header/body authority. (10) B1 — unchanged state on invalid path. (11) B1/B2 — fail-closed crypto-field checks. (12) B2 `tx_validity` — pure function of `(LedgerState, tx_cbor)`. (13) B2 — never `Valid` while skipping phase-1 / phase-2. (14) B2 — unchanged state on Invalid. (15) B2 — `required_signers` CLOSED + era-versioned. (16) B2 — witness coverage fail-closed. (17) B2 `mempool::admit` — admit iff `tx_validity` is `Valid` (NO FALSE ACCEPT). (18) B2 `mempool::policy` (GREEN) — must not call `tx_validity` or touch accumulating state. (19) B3 `cert_classify` — closed total over `ConwayCert`. (20) B3 `pparams` — every deposit/refund sourced from canonical state. (21) B3 `conway::check_conway_coin_conservation` — full equation with §9.1 precedence. (22) B3 `fingerprint` — byte-identical deposit-param fold when `conway_deposit_params == None`. (23) B4 `delegation::apply_conway_cert` — owner-complete; tags 5/6 → `EraInvalidCertificate`. (24) B4 `rules::accumulate_tx_certs` — never swallow decode/apply errors. (25) B5 `gov_cert::apply_conway_gov_cert` — must not mutate B4-owned `CertState`; closed match; env fail-fast. (26) B5 `rules` — must not observe-and-drop governance effects. (27) DC-LEDGER-10 — gov maps key on discriminated `StakeCredential`. (28) PHASE4-N-E S1 — `mempool_ingress` source-invariant; `MempoolState.accumulating` write site frozen; no production `admit(` callers outside `admit.rs` + `ingress.rs`. (29) PHASE4-N-E S3 — `canonicalize_peer_streams` deterministic round-robin by sorted `PeerId`. (30) PP-S1 — typed `Vec<ProposalProcedure>` consumption only. **(31) NEW — `producer::forge::forge_block` (DC-CONS-13/14/15 + DC-LEDGER-12, S3):** must be a pure function of `&ProducerTick` — no `std::time::SystemTime` / `Instant` / `rand` / `HashMap` iteration / `std::env` / `std::fs` / `tokio` / `async` reachable from the function body (enforced by `ci/ci_check_forge_purity.sh`). Must call `is_leader_for_vrf_output(&tick.leader_answer, &tick.vrf_output)` and return `ForgeError::NotLeader` when false — no producer-side fork of `is_leader` / `check_leader_claim` (NC-VRF-3). Must call `opcert_validate(&tick.opcert, &tick.cold_vk, expected_period, tick.prev_opcert_counter)` and return `ForgeError::OpCertRejected(OpCertError)` on rejection; must not bypass any of the four sub-checks. Must re-admit every entry of `tick.mempool_tx_bytes` against `tick.base_state` in the supplied order via the canonical `mempool::admit::admit` and return `ForgeError::TxSetNotAdmissiblePrefix` / `MempoolWidthMismatch` / `MempoolAcceptedMismatch` on width / order / membership disagreement with `tick.mempool.accepted()`. Must build the 4-bucket Conway block body via `ade_codec::shelley::tx_components::split_conway_tx_components` and hash via `block_body_hash::block_body_hash_from_buckets` — no parallel body-hash recipe (DC-CONS-16). Must NOT carry a `String`-bearing `ForgeError` variant (replay surface stays byte-stable). Empty mempool → empty body when leader (OQ-7); the forbidden transition is non-leader forge, not empty-body forge. **(32) NEW — `producer::self_accept::self_accept` (CN-CONS-07, S5):** the function returns `Result<AcceptedBlock, SelfAcceptError>` and there is **exactly one** `-> [Result<]AcceptedBlock` return-type site in the workspace (guard 1b of `ci_check_self_accept_gate.sh`); `AcceptedBlock`'s struct-literal constructor must remain inside `self_accept.rs` (guard 1a); `AcceptedBlock.bytes` must remain private (guard 2); `SelfAcceptError` must remain a closed sum without `#[non_exhaustive]` and without `String`-bearing variants (guard 3); `self_accept`'s body must call `block_validity(` exactly (guard 4) and must NOT re-implement validator sub-steps `validate_and_apply_header(` / `decode_block(` / `block_body_hash(` (guard 5); no `pub fn` in `self_accept.rs` returns raw `Vec<u8>` / `&[u8]` outside the `as_bytes` / `into_bytes` accessors on `AcceptedBlock` (guard 6); no `impl Default for AcceptedBlock` / `From<…>` / `TryFrom<…>` exists anywhere (guard 1c). **(33) NEW — `producer::state::ProducerTick` (DC-CONS-13/14, S3):** must remain a closed (non-`#[non_exhaustive]`) struct; must NOT carry private-key fields (`*SigningKey`, `KesSecret`, `ColdSigningKey`) — replay corpora carrying `ProducerTick` are private-key-free by construction. Mechanically enforced by `ci/ci_check_no_private_keys_in_corpus.sh` guards 2 + 3 (no `#[non_exhaustive]` on the struct; no private-key field). **(34) NEW — `block_body_hash::{block_body_hash, block_body_hash_from_buckets}` (DC-CONS-16, S4):** must be the **single** body-hash authority in the workspace — no parallel `block_body_hash` / `compute_body_hash` / `encode_block_body_then_hash` definition is permitted (enforced by `ci/ci_check_no_producer_body_encoder.sh`). The recipe is `blake2b_256(blake2b_256(tx_bodies) ‖ blake2b_256(witness_sets) ‖ blake2b_256(metadata) ‖ blake2b_256(invalid_txs OR empty))` and inputs are PRESERVED CBOR slices including each bucket's outer array/map header; `invalid_txs == None` is hashed as the empty byte string. Producer and validator MUST share this recipe (`producer::forge` and `block_validity::header_input::computed_body_hash` are the two and only call sites in production code). |
| **Inbound deps** | `ade_testkit`, `ade_core_interop`, **NEW — `ade_runtime`** (the N-C S6 scheduler + tick assembler + broadcast import `ade_ledger::producer::{forge::*, self_accept, AcceptedBlock, SelfAcceptError}`, `ade_ledger::state::LedgerState`, `ade_ledger::pparams::ProtocolParameters`, `ade_ledger::mempool::admit::MempoolState`, `ade_ledger::producer::state::ProducerTick`; this is a new `ade_runtime → ade_ledger` consumption *site*, not a new crate-level edge — the edge already existed pre-N-C and is legal under the RED → BLUE direction). |
| **Outbound deps** | `ade_types`, `ade_crypto` (incl. NEW use sites of `KesSignature` in `ProducerTick`), `ade_codec` (incl. NEW use sites of `shelley::tx_components::split_conway_tx_components` in `producer::forge`), `ade_plutus`, `ade_core` (B1 + N-C — `producer::forge` calls `consensus::leader_schedule::is_leader_for_vrf_output` + `consensus::opcert_validate::opcert_validate`; `producer::self_accept` calls `consensus::{era_schedule, ledger_view, praos_state}`), `minicbor`, `num-bigint`, `num-integer`, `num-traits`. Dev-dep: `ade_testkit`. |
| **Entry points** | `ade_ledger::rules::apply_block` (8 import sites), `apply_block_classified`, `apply_block_with_accounting`, `apply_block_with_verdicts`, `apply_conway_tx_to_utxo` (B2), `apply_epoch_boundary_full`, `ade_ledger::state::{LedgerState, EpochState}`, `ade_ledger::pparams::*`, `ade_ledger::utxo::UTxOState`, `ade_ledger::hfc::translate_era`, `ade_ledger::fingerprint::fingerprint`, `ade_ledger::governance::*`, `ade_ledger::delegation::*`, `ade_ledger::epoch::*`, `ade_ledger::witness::*`. B1: `ade_ledger::block_validity::*`, `ade_ledger::consensus_view::{PoolDistrView, PoolEntry}`. B2: `ade_ledger::tx_validity::{...}`, `ade_ledger::mempool::{admit, MempoolState, AdmitOutcome, ...}`. B3: `ade_ledger::cert_classify::classify`, `ade_ledger::conway::check_conway_coin_conservation`. B4: `ade_ledger::delegation::*`; `ade_ledger::rules::accumulate_tx_certs`. B5: `ade_ledger::gov_cert::apply_conway_gov_cert`. ENACTMENT-COMMITTEE-WRITEBACK: `ade_ledger::governance::apply_committee_enactment`. PHASE4-N-E: `ade_ledger::mempool::{mempool_ingress, IngressEvent, IngressSource, canonicalize_peer_streams, PeerId, PeerSubmissionQueue}`. **NEW — PHASE4-N-C:** `ade_ledger::producer::{forge::{forge_block, ForgeError, ForgeEffects, ForgedBlock}, self_accept::{self_accept, AcceptedBlock, SelfAcceptError}, state::ProducerTick}` (consumed by `ade_runtime::producer::{scheduler, tick_assembler, broadcast}` and by `ade_testkit::producer::{replay, cross_impl_adapter}`); `ade_ledger::block_body_hash::{block_body_hash, block_body_hash_from_buckets}` (consumed by `block_validity::header_input::computed_body_hash` and by `ade_ledger::producer::forge` — and by `ade_testkit::producer::cross_impl_adapter` for the body-hash binding assertion). |
| **Key modules** | `byron.rs`, `shelley.rs`, `mary.rs`, `alonzo.rs`, `babbage.rs`, `conway.rs`, `rules.rs`, `state.rs`, `utxo.rs`, `delegation.rs`, `epoch.rs`, `governance.rs`, `hfc.rs`, `late_era_validation.rs`, `phase.rs`, `plutus_eval.rs`, `pparams.rs`, `rational.rs`, `scripts.rs`, `value.rs`, `witness.rs`, `fingerprint.rs`, `error.rs`, `gov_cert.rs` (B5), `consensus_view.rs` (B1), `block_validity/` (B1), `consensus_input_extract.rs` (B1, **RED**), `tx_validity/` (B2), `mempool/` (B2 + N-E), `cert_classify.rs` (B3), **NEW — `block_body_hash.rs`** (PHASE4-N-C S4; 147 LOC + 3 inline tests — the single body-hash authority), **NEW — `producer/`** (PHASE4-N-C S3/S5): `producer/mod.rs` (21 LOC barrel; re-exports `self_accept::{self_accept, AcceptedBlock, SelfAcceptError}`), `producer/state.rs` (74 LOC — `ProducerTick`), `producer/forge.rs` (534 LOC + 5 inline tests — `forge_block`, `ForgeError`, `ForgedBlock`, `ForgeEffects`), `producer/self_accept.rs` (364 LOC + 5 inline tests — `self_accept`, `AcceptedBlock`, `SelfAcceptError`). |

> **GREEN sub-classification (`ade_ledger::mempool::policy`, `ade_ledger::mempool::canonicalize`).** Carry-forward — physically inside the BLUE `ade_ledger` crate but **GREEN** per their own module doc-comments and the cluster TCB Color Maps; partition is review-and-test-enforced for the broad shape with narrow grep gates pinning specific properties (`ci_check_mempool_ingress_replay.sh` check 6).

> **RED sub-classification (`ade_ledger::consensus_input_extract`).** Carry-forward — physically inside the BLUE `ade_ledger` crate but **RED** per its own module doc-comment and the PHASE4-B1 TCB Color Map; reached only by RED snapshot loader and GREEN corpus harness.

> **NEW — `ade_ledger::producer` BLUE classification rests on three layers:** (a) the crate prefix is BLUE under `.idd-config.json` `core_paths`; (b) every file under `producer/` begins with the `// Core Contract:` banner (enforced by `ci_check_module_headers.sh`); (c) the narrow CI gates `ci_check_forge_purity.sh` (forbids `SystemTime` / `rand` / `HashMap` / `std::env` / `std::fs` / `tokio` / `async` in `forge.rs`), `ci_check_self_accept_gate.sh` (6 guards on `self_accept.rs`), `ci_check_no_producer_body_encoder.sh` (single body-hash authority, repo-wide), `ci_check_no_private_keys_in_corpus.sh` (`ProducerTick` carries no private-key fields), and `ci_check_opcert_closed.sh` (the producer-side opcert acceptance surface stays closed) pin the BLUE shape mechanically.

> **All prior closure narratives unchanged at HEAD.** B-track + N-E + PROPOSAL-PROCEDURES-DECODE all carry forward.

---

### `ade_network` *(BLUE submodules)*

> **Status carried from prior CODEMAP — unchanged at HEAD `694dd74`.** Per-submodule BLUE scoping per `.idd-config.json` `_core_paths_doc`. The crate is split into its BLUE half (this entry) and its RED half (further down). Two submodules — `lib.rs` (the barrel) and `mux/mod.rs` (a re-export shim) — are GREEN and carry no authority. Slice provenance: cluster PHASE4-N-A, slices S-A1 through S-A10. PHASE4-N-C did not touch this crate (the producer's N2N delivery path is stubbed in `ade_runtime::producer::broadcast` / `ade_core_interop::bin::live_block_production_session` and the full block-fetch server-side delivery is N-A-successor scope per the S6 honest-scope note).

| Attribute | Value |
|-----------|-------|
| **Purpose** | Owns the Cardano Ouroboros mini-protocol authority — the closed wire grammar (CBOR codecs) and pure state machines for all 11 N2N + N2C mini-protocols, plus the BLUE Ouroboros mux frame primitive. Encoders/decoders and transitions only; bytes-on-the-wire and effect emission belong to the RED half of the crate. |
| **Creates** | **Wire grammar (`codec/`):** `HandshakeMessage`, `N2cHandshakeMessage`, `ChainSyncMessage`, `BlockFetchMessage`, `TxSubmission2Message`, `KeepAliveMessage`, `PeerSharingMessage`, `LocalChainSyncMessage`, `LocalTxSubmissionMessage`, `LocalStateQueryMessage`, `LocalTxMonitorMessage`; closed payload types; structured `CodecError` + `ProtocolKind`. **State machines:** one closed `*State` / `*Agency` / `*Output` / `*Error` quad per protocol. **Mux primitives (`mux/frame.rs`):** `MuxFrame`, `MuxHeader`, `MuxMode`, `MiniProtocolId`, `MuxError`. |
| **Interprets** | The on-wire CBOR of every Ouroboros mini-protocol message (closed grammar) and the 8-byte Ouroboros mux frame header. Block/header/tx-body bytes inside RollForward / Block / Tx are opaque pass-through (DC-PROTO-06); LSQ Query/Result payloads also opaque. |
| **MUST NOT** | (1) `async fn`, `.await`, `tokio`, `async_std`, `futures::`, async-runtime spawn, or async timers in any file under the 9 BLUE paths. (2) Decode block / header / tx-body bytes. (3) Construct a `PreservedCbor` outside `ade_codec`'s named chokepoints. (4) Add `#[non_exhaustive]` to any wire-message enum or introduce a generic `Codec<P>` trait. (5) Define a generic `Agency<P>` wrapper. (6) Read a selected version from ambient session state. (7) Redefine `TxId` / `SlotNo` / `Hash32` / `CardanoEra`. (8) Depend on `pallas-network`. (9) Hold `String` in any error variant. (10) Use any other BLUE forbidden pattern. |
| **Inbound deps** | `ade_core_interop` (live consensus session binary, N-E S4/S5 bridges, S6 `live_tx_submission_session` binary, **N-C S7 `live_block_production_session` binary — all RED → BLUE**). |
| **Outbound deps** | `ade_types`, `ade_codec`. No external deps in the BLUE submodules. |
| **Entry points** | Codec entry points per protocol; `ade_network::codec::primitives::*`; `ade_network::codec::version::*`; `ade_network::codec::{CodecError, ProtocolKind}`. State-machine `*_transition` per protocol. Mux: `ade_network::mux::frame::{encode_frame, decode_frame, ...}`. |
| **Key modules** | `codec/` (11 protocols + `primitives.rs` + `version.rs` + `error.rs`); `handshake/`, `chain_sync/`, `block_fetch/`, `tx_submission/`, `keep_alive/`, `peer_sharing/`; `n2c/`; `mux/frame.rs`. |

---

### `ade_plutus`

> **Status carried from prior CODEMAP — unchanged at HEAD `694dd74`.** PHASE4-N-C did not touch `ade_plutus`.

| Attribute | Value |
|-----------|-------|
| **Purpose** | Quarantine boundary between the Ade-canonical ledger and the ported UPLC evaluator from `aiken-lang/aiken` (pinned to tag `v1.1.21`). Exposes Ade-canonical types only; aiken- and pallas-originated types are strictly internal. |
| **Creates** | `PlutusScript`, `PlutusLanguage`, `EvalOutput`, `PlutusError`, `CostModels`, `DecoderMode`, `PerScriptResult`, `TxEvalResult`. |
| **Interprets** | UPLC scripts (Plutus V1/V2/V3) and `CostModels` CBOR. Phase-two transaction evaluation. `PlutusScript::from_cbor` is a named ingress chokepoint. |
| **MUST NOT** | (1) Re-export any `pallas_*` or `aiken_uplc::` type. (2) Allow another BLUE crate to bypass the canonical entry. (3) Activate PV11 builtins. (4) Use any BLUE-forbidden pattern. (5) Construct `PreservedCbor` outside `ade_codec`. |
| **Inbound deps** | `ade_ledger`, `ade_testkit`. |
| **Outbound deps** | `ade_types`, `ade_crypto`, `ade_codec`, `aiken_uplc` (git, tag `v1.1.21`), `pallas-primitives` (internal-only). |
| **Entry points** | `ade_plutus::eval_tx_phase_two`, `ade_plutus::tx_eval::*`, `ade_plutus::evaluator::*`, `ade_plutus::cost_model::*`. |
| **Key modules** | `evaluator.rs`, `cost_model.rs`, `script_context.rs`, `script_verdict.rs`, `tx_eval.rs`. |

---

### `ade_types`

> **Status carried from prior CODEMAP — unchanged at HEAD `694dd74`.** PHASE4-N-C did not add any canonical type to `ade_types` — the new closed canonical types all live in their authoritative crates (`ProducerTick`/`ForgedBlock`/`ForgeError`/`ForgeEffects`/`AcceptedBlock`/`SelfAcceptError` in `ade_ledger`; `OpCertError` in `ade_core`; `OpCertCodecError`/`TxComponents` in `ade_codec`; `KesSignature` in `ade_crypto`). The `OperationalCert` struct under `ade_types::shelley::block` is unchanged; the producer's standalone opcert encoder (`ade_codec::shelley::opcert`) consumes the already-existing type.

| Attribute | Value |
|-----------|-------|
| **Purpose** | Owns the canonical Cardano domain vocabulary — primitives, era enum, address forms, era-tagged transaction bodies / outputs / certificates, governance types — used by every other workspace crate as the lingua franca. The schema, separated from the codec. |
| **Creates** | `CardanoEra`, `SlotNo`, `BlockNo`, `EpochNo`, `Hash28`, `Hash32`, `Coin`, `Lovelace`, `NetworkId`, `Nonce`, `TxIn`, `RewardAccount`, `PoolId`, `Address`, `ByronAddress`, `Credential`, `StakeCredential`, `Certificate`, `PoolRegistrationCert`, `ConwayCert`, `CertDisposition` / `DepositEffect` / `CoinSource`, `MIRCert`, `MIRPot`, `DRep`, `GovAction`, `GovActionState`, `GovActionId`, `Anchor`, `ProposalProcedure` (PP-S1), `OperationalCert`, `NativeScript`, `PlutusV1Script`, `Datum`, `DatumOption`, `MultiAsset`, `AssetName`, `CostModel`, `ExUnits`, plus per-era tx-body / tx-out / witness wrappers. |
| **Interprets** | None — produce-only. Domain types are constructed by `ade_codec` decoders or `ade_ledger` rules. |
| **MUST NOT** | (1) Construct or decode `PreservedCbor`. (2) Use any BLUE-forbidden pattern. (3) Depend on any workspace crate. (4) Add open/extensible variants to closed enums without a versioned gate. B4 — additive owner-completion. OQ5 — discriminated `StakeCredential`. PP-S1 — `ProposalProcedure` 4-field closed shape; `ConwayTxBody.proposal_procedures` typed. |
| **Inbound deps** | `ade_codec`, `ade_crypto`, `ade_ledger`, `ade_plutus`, `ade_runtime`, `ade_testkit`, `ade_network`, `ade_core`, `ade_core_interop`. |
| **Outbound deps** | None. |
| **Entry points** | `ade_types::CardanoEra`, `ade_types::tx::{Coin, TxIn, RewardAccount}`, `ade_types::{Hash32, SlotNo, Hash28}`, `ade_types::conway::tx::ConwayTxBody`, `ade_types::conway::cert::*`, `ade_types::conway::governance::{ProposalProcedure, Anchor, GovAction, GovActionId}`, `ade_types::shelley::block::{OperationalCert, ProtocolVersion, ShelleyHeader, ShelleyBlock, VrfData}` (the last set is the **new heavy-import surface for PHASE4-N-C** — `ade_ledger::producer::*` + `ade_runtime::producer::tick_assembler` + `ade_testkit::producer::*` all import these). |
| **Key modules** | `primitives.rs`, `era.rs`, `tx.rs`, `address/`, `byron/`, `shelley/` (incl. `block.rs` carrying `OperationalCert` + `ShelleyHeader` + `ShelleyBlock`), `allegra/`, `mary/`, `alonzo/`, `babbage/`, `conway/`. |

---

## GREEN Modules — Deterministic Glue

> Deterministic, non-authoritative. May depend on BLUE; must not affect authoritative outputs.

### `ade_testkit`

> **Status change at HEAD `694dd74` (PHASE4-N-C — new GREEN producer test-harness sub-tree).** Between `928c2be` and HEAD, `ade_testkit` gained the `producer/` sub-tree (5 files: `mod.rs` barrel; `reference_vectors.rs` — frozen VRF + Sum6KES reference vectors with full update-chain coverage at periods 0, 1, 32, 63; `fixtures.rs` — 3 in-source `ProducerReplayFixture` entries with baked-in signed artifacts; `replay.rs` — replay harness running `forge_block` over each fixture and asserting byte-identicality; `cross_impl_adapter.rs` — mechanical half of CN-CONS-06 asserting decode round-trip + body-hash binding + decoder/encoder structural agreement). The sub-tree is **GREEN** per its module doc-comment ("GREEN test harness for the PHASE4-N-C producer surface") and the PHASE4-N-C TCB Color Map. Registered in `crates/ade_testkit/src/lib.rs` (`pub mod producer;`). A regen helper `crates/ade_testkit/tests/regen_producer_fixtures.rs` exists for re-deriving the baked-in artifact bytes via the RED `ade_runtime::producer::signing` wrappers (`#[ignore]`-gated in the default test run). All prior PP-S2 / N-E S2 / B-track narrative carries forward.

| Attribute | Value |
|-----------|-------|
| **Purpose** | Test infrastructure: differential harness, oracle snapshot loader, regression corpus, genesis loader, era mapping, transcript replay, diff reporting; N-B consensus harness; B1 block-validity harness (`validity/`); B2 transaction-validity harness (`tx_validity/`); N-E S2 mempool-ingress replay harness (`mempool/`); PP-S2 canonical synthetic `proposal_procedures` corpus + replay harness (`governance/`); **PHASE4-N-C producer test harness (`producer/`) — frozen VRF/KES reference vectors driving the RED `signing` round-trip tests (S1), in-source `ProducerReplayFixture` entries with baked-in signed artifacts (S3/S4), replay harness asserting `forge_block` byte-identicality (S3/S4), and the mechanical half of CN-CONS-06 in `cross_impl_adapter` (S7)**. Not authoritative — produces comparisons and serves the `replay_cmd` test suites. |
| **Creates** | **Carry-forward (selected):** `Era`, `OracleManifest`, `LoadedSnapshot`, `RegressionCorpus`, `ComparisonSurface`, `DifferentialReport`, `DiffReport`, `Divergence`, `LedgerDiffReport`, `ProtocolDiffReport`, `BlockFields`, `ExpectedVerdict`, `HarnessError`, `Transcript`, `StateHash`, `SnapshotHeader`. **Consensus (N-B):** `LedgerViewStub`, `EpochStakeFixture`, `PoolFixture`, `ReplayStep`, `ReplayResult`. **Validity (B1):** `ConwayValidityCorpus` + `CorpusLoadError`. **B2 (`tx_validity/`):** `ExtractedTx`, `TxReplay`, `ValidCase`, `WitnessMutation`, etc. **N-E S2 (`mempool/`):** `BTrackCase`, `ExpectedOutcome`. **PP-S2 (`governance/`):** `CorpusEntry`, `ReplayOutcome`. **NEW — PHASE4-N-C (`producer/`):** `VrfReferenceVector { seed, alpha, expected_proof, expected_output }` (S1), `KesReferenceVector { seed, … }` (S1), `ProducerReplayFixture { label, ticks, expected_forged, expected_err_tag }` (S3/S4), `ExpectedErr` (closed forge-error discriminant tag — replay corpus assertion target). Functions: `producer::reference_vectors::{vrf_reference_vectors, kes_reference_vectors}` (S1), `producer::fixtures::{fixture_empty_mempool_leader, fixture_non_leader, fixture_two_tx_mempool_leader, producer_replay_fixtures}` (S3/S4), `producer::replay::*` (the harness functions exercising `forge_block` over the corpus, asserting `forge_block_pure_no_io` + `forge_block_replay_byte_identical`). |
| **Interprets** | Oracle reference snapshots, the regression-corpus manifest, the N-B consensus corpus, the B1 validity corpus, on-wire Conway transactions inside committed Conway-576 corpus blocks. PP-S2 — fixtures built via the BLUE encoder. **NEW — PHASE4-N-C (`producer/`):** the harness interprets nothing at the wire layer — `producer::fixtures` builds canonical `ProducerTick` values from fixed 32-byte seeds with **baked-in signed-artifact byte constants** (the regen helper materializes them via the RED signing wrappers, prints them, and a human pastes them in). The replay path drives `ade_ledger::producer::forge::forge_block` over each tick and asserts (a) byte-identicality across two replays, (b) byte-identicality against any captured `expected_forged`, and (c) for negative cases, the matching `ForgeError` discriminant. `cross_impl_adapter` decodes the forged bytes via `ade_codec::shelley::block::decode_shelley_block_inner` and asserts the body-hash binding via `ade_ledger::block_body_hash::block_body_hash` — the same recipe both the producer and validator use. |
| **MUST NOT** | (1) Affect authoritative outputs. (2) Be linked from any BLUE crate. (3) Introduce nondeterminism that leaks into stored fixtures. (4) `consensus::ledger_view_stub` `BTreeMap` only. (5) Stream-replay determinism. (6) B1 `validity::replay` determinism. (7) B2 `tx_validity` no-false-accept. (8) B3 snapshot-loader deposit-param materialization-only. (9–10) Snapshot loader fail-closed. (11) N-E S2 — use `mempool_ingress` (not `admit`); single-step. (12) PP-S2 — sanctioned `ProposalProcedure {…}` site; BLUE encoder for fixtures. **(13) NEW — `producer/fixtures.rs` (DC-CRYPTO-03/04/05 corpus discipline, S3):** must NOT carry private-key byte literals — replay corpora carry signed artifacts only (`VrfProof` / `KesSignature` / `OpCert.sigma`), never `*SigningKey`/`KesSecret`/`ColdSigningKey` bytes (enforced by `ci/ci_check_no_private_keys_in_corpus.sh`). The 32-byte seeds embedded in the fixtures are *VRF input seeds* (Praos `eta` material) — not private keys. The regen helper `tests/regen_producer_fixtures.rs` is the **only** call site in the harness that imports RED signing primitives; the helper is `#[ignore]`-gated and its output is hand-copied into `fixtures.rs` (no automated round-trip through RED). **(14) NEW — `producer/replay.rs` (DC-CONS-14, S3):** must drive `forge_block` exactly — must not re-implement the forge transition, must not paper over a `Result::Err`, must not iterate the in-source fixture vector in non-deterministic order (the harness reads from `producer_replay_fixtures(): Vec<ProducerReplayFixture>` and iterates in index order). `forge_block_pure_no_io` is the no-IO test name; `forge_block_replay_byte_identical` the byte-identicality test name (test names are doc-pinned to the cluster's CE-N-C-3). **(15) NEW — `producer/cross_impl_adapter.rs` (CN-CONS-06 mechanical half, S7):** the adapter MUST drive `producer_replay_fixtures()` (no parallel corpus); MUST assert (i) `decode_shelley_block_inner(forged.bytes).is_ok()`, (ii) `block_body_hash(&decoded) == decoded.header.body_hash`, (iii) decoded `header.body_hash` / `operational_cert.sequence_number` / `operational_cert.kes_period` equal the in-memory `ForgedBlock.block` fields. The adapter MUST NOT claim "passes cardano-node KES/VRF verify" (the corpus carries all-zero KES/VRF artifacts by design — the crypto-level cross-impl claim is by-design out of reach of any CI gate against this corpus, deferred to CE-N-C-8's operator-action live evidence). **(16) NEW — `producer/reference_vectors.rs` (DC-CRYPTO-03/04/05, S1):** the vectors must pin `(seed, alpha) -> expected_proof/output` triples computed from `cardano-crypto`'s deterministic generation/signing path (the test in `ade_runtime::producer::signing` re-derives the same bytes through the RED wrapper and asserts byte equality — drift between the wrapper and the underlying crate fails the test). Coverage MUST include VRF (distinct seeds + alphas), KES at periods 0, 1, 32, 63 (every branch of the Sum6 recursion), and update-chain fingerprints at three representative periods. |
| **Inbound deps** | None at compile time; consumption is via integration tests and dev-dep links from `ade_core`, `ade_runtime`, `ade_ledger`. `ade_core_interop` is a non-dev consumer (N-E S4/S5 imports). **No new inbound crate edge in PHASE4-N-C** — the `ade_runtime::producer::signing` test path that consumes `ade_testkit::producer::reference_vectors` is a dev-dep link, not a runtime edge. |
| **Outbound deps** | `ade_types`, `ade_codec`, `ade_crypto`, `ade_core`, `ade_ledger`, `ade_plutus`, `ade_runtime`, `blake2`, `flate2`, `tar`, `serde`, `serde_json`, `toml`. **NEW use-sites in PHASE4-N-C** — `cardano-crypto` (dev-dep, used by `producer/reference_vectors.rs` to compute the expected proof/sig bytes at vector materialization time); `ade_ledger::{producer::*, block_body_hash, mempool::admit::*, state::*, pparams::*}` consumption by the new `producer/` sub-tree. No new crate-level edges. |
| **Entry points** | `ade_testkit::harness::*`; N-B `consensus::*`; B1 `validity::*`; B2 `tx_validity::*`. N-E S2: `ade_testkit::mempool::*`. PP-S2: `ade_testkit::governance::*`. **NEW — PHASE4-N-C:** `ade_testkit::producer::{reference_vectors::{vrf_reference_vectors, kes_reference_vectors, VrfReferenceVector, KesReferenceVector}, fixtures::{fixture_empty_mempool_leader, fixture_non_leader, fixture_two_tx_mempool_leader, producer_replay_fixtures, ProducerReplayFixture, ExpectedErr}, replay::*, cross_impl_adapter}` (consumed by `ade_runtime::producer::signing`'s tests, by the regen helper at `crates/ade_testkit/tests/regen_producer_fixtures.rs`, and by the inline `mod tests` blocks in `replay.rs` + `cross_impl_adapter.rs`). |
| **Key modules** | `harness/`, `consensus/`, `validity/`, `tx_validity/`, `mempool/`, `governance/`, **NEW — `producer/`** (PHASE4-N-C): `mod.rs` (16-LOC barrel), `reference_vectors.rs` (130 LOC), `fixtures.rs` (274 LOC), `replay.rs` (216 LOC + 4 inline tests), `cross_impl_adapter.rs` (184 LOC + 3 inline tests). **Integration tests of note:** `tests/proposal_procedures_corpus_replay.rs` (PP-S2), **NEW — `tests/regen_producer_fixtures.rs`** (PHASE4-N-C S3 — `#[ignore]`-gated regen helper that imports RED signing primitives and prints the baked-in artifact bytes for hand-paste into `fixtures.rs`). |

> **Classification note carried forward.** `ade_testkit` reads files from disk in test helpers and drives BLUE authorities from corpus inputs. I/O exists only to materialize oracle/corpus fixtures consumed by tests and to compose deterministic GREEN harnesses. The PHASE4-N-C `producer/` sub-tree adds *zero* I/O (fixtures are entirely in-source; the regen helper is the only test that touches RED signing and it is `#[ignore]`-gated). PP-S2's `governance/` sub-tree similarly adds zero I/O.

---

## RED Modules — Imperative Shell

> I/O, network, storage, clocks, retries. May depend on BLUE/GREEN. Must not modify core state directly or construct semantic types unsafely.

### `ade_core_interop` *(PHASE4-N-B S-B10; PHASE4-N-E S4/S5/S6; **PHASE4-N-C S7**)*

> **Status change at HEAD `694dd74` (PHASE4-N-C S7 — new RED operator-action live binary `live_block_production_session`).** Between `928c2be` and HEAD, `ade_core_interop` gained the new binary `src/bin/live_block_production_session.rs` — the CE-N-C-8 evidence-capture pass. It loads operator-supplied cardano-cli `.skey` envelopes via `ade_runtime::producer::keys`, opens an N2N session to a private cardano-node, builds the producer baseline (ledger, chain-dep, leader-schedule), drives the full RED scheduler → GREEN tick-assembler → BLUE forge → BLUE self_accept pipeline at each elected leader slot, and writes one JSON-Lines record per attempted slot to `docs/clusters/PHASE4-N-C/CE-N-C-LIVE_<date>.log`. Full N2N block-fetch server-side delivery is stubbed (N-A-successor scope) — the binary logs "would submit via block-fetch server-side" in place of a real submit. The binary is RED by its own header comment and matches the established `live_consensus_session.rs` + `live_tx_submission_session.rs` pattern. All prior N-B follow / N-E S4/S5/S6 narrative carries forward.

| Attribute | Value |
|-----------|-------|
| **Purpose** | Live cardano-node interop driver. Hosts (a) the N-B consensus orchestrator readiness probe `fresh_orchestrator(k, initial_nonce)` and its `live_consensus_session` binary; (b) the N-E S4 GREEN adapter + `ingest_n2n_events` that bridges N2N `InventoryEvent`s into `mempool_ingress`; (c) the N-E S5 GREEN adapter + `ingest_n2c_events` that bridges N2C `LocalTxSubmissionEvent`s into the same chokepoint; (d) the N-E S6 `live_tx_submission_session` RED binary; **(e) NEW — the N-C S7 `live_block_production_session` RED binary that performs the CE-N-C-8 evidence-capture pass against a real cardano-node preprod relay, driving the full Ade producer pipeline at each elected leader slot.** The crate is RED per `Cargo.toml` and the cluster TCB Color Maps; the S4/S5 sub-modules carry a GREEN sub-classification by their own module doc-comments. The S6 and S7 binaries are RED by their own header comments. |
| **Creates** | `fresh_orchestrator` (N-B). N-E S4 sub-module: `PeerAccumulator { peer, txs }` (sub-classified GREEN). N-E S5 sub-module: `ClientAccumulator { client, txs }` (sub-classified GREEN). Functions: `event_to_ingress` + `ingest_n2n_events` (S4); `local_event_to_ingress` + `ingest_n2c_events` (S5). **N-E S6:** no public Rust types — the binary is RED glue. **NEW — N-C S7 `live_block_production_session`:** also no public Rust types — RED glue over the established S4 GREEN surface, the BLUE `ade_network` codecs / state machines, and the new BLUE `ade_ledger::producer::*` + RED `ade_runtime::producer::*` surfaces. |
| **Interprets** | Live N2N byte streams from a real cardano-node peer. S4/S5 sub-modules interpret nothing at the bytes level. **NEW — N-C S7 `live_block_production_session`:** interprets cardano-cli `.skey` text envelopes (via `ade_runtime::producer::keys` — the actual envelope parsing lives RED-side in `keys.rs`); drives the BLUE protocol grammar on live wire bytes for chain-sync follow; consumes live `LedgerView` snapshots to assemble `ProducerTick` values; writes a deterministic JSON-Lines transcript for CE-N-C-8 evidence. |
| **MUST NOT** | (1) Contain authoritative state-transition logic. (2) Be depended on by any other workspace crate. (3) Run by default in CI — closure-gate tests are `#[ignore]`-gated (CE-N-B-6, CE-N-E-6, **NEW — CE-N-C-8**). (4) Bypass `ade_codec`'s `PreservedCbor` chokepoints. (5) Read versions out of ambient context (DC-PROTO-06). (6) Vendor cardano-node mainnet genesis blobs. (7) N-E S4/S5 — must not branch on `IngressSource`, must not re-encode `tx_bytes`, must not bypass `mempool_ingress`. (8) N-E S6 binary — no direct `admit(`, no shadowing of `IngressSource`/`IngressEvent`/`PeerId`, no parallel codecs/transitions. **(9) NEW — N-C S7 `live_block_production_session` binary (RED, CN-CONS-06 live half + OP-OPS-04 + OP-OPS-05):** must NOT bypass the BLUE producer authorities — every forge MUST go through `ade_ledger::producer::forge::forge_block` over a canonical `ProducerTick` (no inline body assembly, no parallel header construction); every accept MUST go through `ade_ledger::producer::self_accept::self_accept` (no fabrication of `AcceptedBlock`, no shadow of the verdict — `AcceptedBlock` has no public constructor outside `self_accept.rs` and the type-level CN-CONS-07 gate is mechanical here too). Must NOT load private-key material into any BLUE call path — `KesSecret`/`VrfSigningKey`/`ColdSigningKey` stay inside `ade_runtime::producer::signing` (the binary holds them in RED scope only to drive `vrf_prove`/`kes_sign`/`kes_update` and obtains `KesSignature`/`VrfProof`/`OpCert` artifacts to thread into the `TickInputs`). Must NOT generate keys (OP-OPS-04 — Ade is not a key generator; the operator supplies `.skey` files). Must NOT write a `CE-N-C-LIVE_<date>.log` with synthetic/fabricated entries — the log MUST reflect an actual run. The default `main` MUST stay hermetic (prints readiness and exits); `--connect` is the explicit operator-action flag. Must keep the `#[ignore]`-gated build-and-start test offline. Must NOT regress the `OP-OPS-05` slot-deadline SLA in a way that produces a non-leader log when it should produce a leader log (the wall-clock measurement is operational, not hash-critical, but a regression here costs evidence and is a release-blocking quality bar). |
| **Inbound deps** | None. |
| **Outbound deps** | `ade_core`, `ade_codec`, `ade_crypto`, `ade_ledger`, `ade_runtime` (**NEW heavy use sites in PHASE4-N-C** — the S7 binary imports `ade_runtime::producer::{keys, signing, tick_assembler, scheduler, broadcast}`), `ade_network`, `ade_testkit`, `ade_types`, `tokio`. Total Cargo deps: 8 workspace crates + `tokio`. No new crate-level edges in N-C — the S7 binary reuses the already-established `ade_core_interop → ade_runtime` and `ade_core_interop → ade_ledger` edges. |
| **Entry points** | `ade_core_interop::fresh_orchestrator(k, initial_nonce)`, binary `live_consensus_session` (CE-N-B-6 evidence). N-E S4: `ade_core_interop::tx_submission::*`. N-E S5: `ade_core_interop::local_tx_submission::*`. N-E S6 binary: `live_tx_submission_session` (CE-N-E-6). **NEW — N-C S7 binary: `live_block_production_session` (CE-N-C-8 evidence — conditional on operator-provided testnet SPO stake).** No new public Rust API; the binary entry point is `fn main()`. |
| **Key modules** | `src/lib.rs`, `src/follow.rs` (N-B), `src/tx_submission.rs` (N-E S4), `src/local_tx_submission.rs` (N-E S5), `src/bin/live_consensus_session.rs`, `src/bin/live_tx_submission_session.rs` (N-E S6), **NEW — `src/bin/live_block_production_session.rs`** (PHASE4-N-C S7; 247 LOC; default hermetic `main` + explicit `--connect` live pass). |

> **Gap surfaced (carried + extended).** `ade_core_interop` at HEAD is a *readiness probe + bridge + live evidence capture binary*. CE-N-B-6 (live consensus tip agreement) **closes via manual evidence log against a real cardano-node peer; live capture pending operator run.** CE-N-E-6 (live N2N tx-submission evidence) is **CLOSED at HEAD**. CE-N-E-7 (live N2C tx-submission evidence) is **deferred** as cross-cluster obligation `CE-NODE-N2C-LTX`. **NEW — CE-N-C-8 (live block-production evidence, CN-CONS-06 live half) is at `status = "enforced"` with an `open_obligation` on CN-CONS-06: `blocked_until_operator_stake_available`. The mechanical half is closed (`cross_impl_adapter` over the synthetic S3 corpus); the live half awaits testnet SPO stake provisioning per `docs/clusters/PHASE4-N-C/CE-N-C-8_PROCEDURE.md`.** The S7 binary's RED classification is review-and-doc enforced (matching the S6 precedent — no narrow CI gate scopes the partition).

---

### `ade_network` *(RED submodules + capture binaries)*

> **Status carried from prior CODEMAP — unchanged at HEAD `694dd74`.** The RED half of `ade_network`: `mux::transport`, `session`, and the 7 `src/bin/capture_*` binaries. PHASE4-N-C did not touch this crate.

| Attribute | Value |
|-----------|-------|
| **Purpose** | The only place inside `ade_network` where I/O happens — tokio-based TCP / Unix-socket scaffolding (`mux::transport`), the session-composition placeholder (`session`), and capture binaries that perform real handshakes against pinned cardano-node 11.0.1 peers. |
| **Creates** | `MuxTransport`. `session/mod.rs` is an empty placeholder. The capture binaries produce no public Rust types. |
| **Interprets** | Live TCP / Unix-socket byte streams; bytes flow into the BLUE mux frame decoder and protocol codecs without re-interpretation. |
| **MUST NOT** | (1) Contain protocol logic. (2) Define or modify any closed wire-message enum, agency type, or state. (3) Read a selected protocol version from ambient state. (4) Replace BLUE codec / state machine with a "fast path". (5) Bypass `ade_codec`'s `PreservedCbor` chokepoints. (6) Be depended on by any BLUE submodule of `ade_network`. |
| **Inbound deps** | `ade_core_interop`. At the library level no BLUE crate imports the RED submodules. |
| **Outbound deps** | `tokio`, `ade_types`, `ade_codec`, plus BLUE `ade_network::codec::*` and `ade_network::mux::frame`. |
| **Entry points** | `ade_network::mux::transport::{MuxTransport, open_tcp}`. The capture binaries. |
| **Key modules** | `mux/transport.rs`, `session/mod.rs` (placeholder), `bin/capture_*`. |

> **Gap surfaced (carried).** `ade_network::session::mod.rs` is still a placeholder. The session driver remains owed by a future orchestration cluster — likely the same node-binary cluster that owns `CE-NODE-N2C-LTX` and the producer-side N2N block-fetch server delivery that S7's `live_block_production_session` currently stubs.

---

### `ade_node`

> **Status carried from prior CODEMAP — unchanged at HEAD `694dd74`.** Still a hello-world stub. PHASE4-N-C did not touch `ade_node`; the producer pipeline is reachable today through the `live_block_production_session` operator-action binary and the `producer_pipeline_slot_deadline.rs` integration test, not through any `ade_node` orchestration.

| Attribute | Value |
|-----------|-------|
| **Purpose** | Binary entry point for the node process. Reserved as the assembly point for runtime + ledger + chain DB + network + consensus orchestrator + mempool + the deferred N2C server + the deferred producer-side N2N block-fetch server. |
| **Creates** | None. |
| **Interprets** | None yet. |
| **MUST NOT** | (1) Bypass `ade_codec` to construct semantic types from bytes. (2) Modify `ade_ledger` or `ade_core::consensus` state in place — transitions go through `ade_ledger::rules::*`, `ade_ledger::block_validity::*`, `ade_ledger::tx_validity::*`, `ade_ledger::mempool::admit`, `ade_ledger::mempool::mempool_ingress`, **NEW — `ade_ledger::producer::forge::forge_block` + `ade_ledger::producer::self_accept::self_accept`**, or `ade_core::consensus::*` BLUE transitions. (3) Take an inbound dep from any other crate. (4) Read versions out of ambient context. (5) Construct an `OrchestratorState` outside `ade_runtime::consensus::chain_selector` or `ade_core_interop::fresh_orchestrator`. (6) Forward-looking — must reach the mempool only through `mempool_ingress`. (7) Forward-looking `CE-NODE-N2C-LTX` — must drive the GREEN bridge, not introduce a parallel path. (8) PP-S1 forward-looking — typed `Vec<ProposalProcedure>` only. **(9) NEW — Forward-looking PHASE4-N-C / CN-CONS-07:** any `ade_node`-level producer glue must drive the BLUE `forge_block` → `self_accept` pipeline; must NOT fabricate an `AcceptedBlock` (the type-level CN-CONS-07 gate enforces this — no public constructor outside `self_accept.rs`); must NOT introduce a parallel body-hash recipe (DC-CONS-16; `ci/ci_check_no_producer_body_encoder.sh` enforces this repo-wide); must NOT bypass `opcert_validate` at the producer's RED→BLUE boundary; must hold all private-key material inside `ade_runtime::producer::{signing, keys}` and pass only signed artifacts (`KesSignature`, `VrfProof`, `OpCert`) into `ProducerTick`. The default `ade_node` binary must NOT enable producer mode unless an operator-supplied `.skey` set is loaded and the operator opts in explicitly. |
| **Inbound deps** | None. |
| **Outbound deps** | None at present. |
| **Entry points** | `main()` in `crates/ade_node/src/main.rs`. |

> **Gap surfaced (carried).** `ade_node` is still a hello-world stub; its MUST NOT list is forward-looking with nothing to enforce today.

---

### `ade_runtime`

> **Status change at HEAD `694dd74` (PHASE4-N-C — new RED `producer/` sub-tree).** Between `928c2be` and HEAD, `ade_runtime` gained the `producer/` sub-tree (6 files: `mod.rs` barrel; `signing.rs` (S1) — RED private-key custody for `VrfSigningKey`/`KesSecret`/`ColdSigningKey` with zeroize-on-drop + `vrf_prove`/`kes_sign`/`kes_update`; `keys.rs` (S1) — cardano-cli `*.skey` text-envelope loader with `KeyLoadError`; `tick_assembler.rs` (S6, **GREEN**) — pure assembler stitching captured RED outputs into a canonical `ProducerTick`; `scheduler.rs` (S6) — pure RED state transition `scheduler_step(state, input, ledger_view) -> (state', effects)` mirroring N-B's `process_stream_input`; `broadcast.rs` (S6) — FIFO `BroadcastQueue` whose `enqueue` takes `AcceptedBlock` by value, the type-level CN-CONS-07 gate). All prior N-D / N-B / N-A narrative carries forward.

| Attribute | Value |
|-----------|-------|
| **Purpose** | The imperative shell. Hosts (a) the `ChainDb` / `SnapshotStore` storage abstractions (N-D); (b) the GREEN chain-selector orchestrator + GREEN `CandidateFragment` materializer (N-B); (c) the RED genesis-text parser (N-B); **(d) NEW — the RED producer-side scaffolding (`producer/`): private-key custody (`signing.rs`), cardano-cli `.skey` text-envelope loader (`keys.rs`), the pure RED scheduler state-transition (`scheduler.rs`), the GREEN deterministic tick-assembler (`tick_assembler.rs`), and the type-level-gated FIFO broadcast queue (`broadcast.rs`)**. The crate remains RED; `chain_selector.rs` / `candidate_fragment.rs` / `tick_assembler.rs` are GREEN by file-level classification; `genesis_parser.rs` / `signing.rs` / `keys.rs` / `scheduler.rs` / `broadcast.rs` are RED. |
| **Creates** | **ChainDb (`chaindb/`):** `ChainDb`, `SnapshotStore`, `KillStrategy<D>`, `ChainTip`, `StoredBlock`, `ChainDbError`, `InMemoryChainDb`, `PersistentChainDb`, `PersistentChainDbOptions`, `SyncCadence`, `NoKill`, `BlockIter<'a>`. **Recovery:** `Recoverable`, `StartingState`, `RecoveryReport<R>`, `RecoveryError<E>`. **Genesis (RED):** `NetworkMagic`, `GenesisBlob`, `GenesisBundle<'a>`, `GenesisParseError`. **Orchestrator (GREEN):** `StreamInput`, `OrchestratorError`, `OrchestratorState`, `RollbackSnapshot`, `DEFAULT_SNAPSHOT_LIMIT`. **Materializer (GREEN):** `build_candidate_fragment`. **NEW — PHASE4-N-C `producer/` (RED unless tagged):** `VrfSigningKey` (RED, S1; opaque 64-byte libsodium-secret wrapper; zeroize-on-drop; redact-on-`Debug`); `KesSecret` (RED, S1; in-memory Sum6KES signing tree; zeroize-on-drop; redact-on-`Debug`); `ColdSigningKey` (RED, S1; Ed25519 cold-key wrapper; zeroize-on-drop; redact-on-`Debug`); `SigningError` (RED, S1; closed sum: `MalformedKey`, `PeriodOutOfRange`, `EvolutionBackwards`, `CryptoBackend(CryptoError)`); `KeyLoadError` (RED, S1; closed sum carrying `io::ErrorKind` rather than the path to avoid leaking filesystem layout). `TickInputs` (GREEN, S6; closed RED-supplied tick inputs); `TickAssemblyError` (GREEN, S6; closed: `VrfProofMalformed`, `MempoolWidthMismatch`). `SchedulerInput` (RED, S6; closed: `SlotTick { slot, inputs }`, `ChainAdvanced { ledger, chain_dep, mempool }`); `SchedulerEffect` (RED, S6; closed: `EnqueueBroadcast(AcceptedBlock)`, `SilentNonLeader { slot }`, `HaltOnInvariant { slot, reason }`, `HaltOnAssembly { slot, reason }`); `SchedulerHaltReason` (RED, S6; closed: `Forge(ForgeError)`, `SelfAccept(SelfAcceptError)`); `SchedulerState` (RED, S6). `BroadcastError` (RED, S6; closed: `QueueFull`, `Shutdown`); `BroadcastQueue` (RED, S6; FIFO over `AcceptedBlock`). Functions: `signing::{vrf_prove, kes_sign, kes_update}`; `keys::{load_vrf_signing_key_skey, load_kes_signing_key_skey, load_cold_signing_key_skey}` + the three envelope type-string constants; `tick_assembler::assemble_tick`; `scheduler::scheduler_step`; `broadcast::BroadcastQueue::{new, enqueue, pop, len, shutdown}`. |
| **Interprets** | Block bytes for content-addressed storage. Genesis JSON. N-A `StreamInput` events. **NEW — PHASE4-N-C:** cardano-cli `.skey` text envelopes (`keys.rs` parses the JSON envelope, decodes the CBOR `cborHex` payload, validates the `type` field against the expected algorithm string, and constructs the matching RED secret wrapper — paths never appear in `KeyLoadError`); RED-supplied signed artifacts assembled into the canonical `ProducerTick` (`tick_assembler.rs`); RED scheduler input stream of `SlotTick` / `ChainAdvanced` events (`scheduler.rs`); typed `AcceptedBlock` values handed across the BLUE→RED boundary into the broadcast queue (`broadcast.rs`). |
| **MUST NOT** | (1) Depend on `ade_ledger`. *(N-C exception: now-permitted by the established RED → BLUE direction; the BLUE `ade_ledger::producer::*` is consumed by `ade_runtime::producer::*` which is RED/GREEN.)* (2) Leak `redb` through the `chaindb::*` public surface. (3) Have a second public chaindb path. (4) Couple snapshots to a specific state type. (5) Apply automatic snapshot pruning. (6) Allow partial-recovery success. (7) Add async recovery surface. (8) Treat "not found" as an error path or silently retry on real errors. (9) Consensus sub-tree GREEN `chain_selector` must not affect authoritative outputs. (10) Genesis parser — structured errors only. (11) `candidate_fragment` materializer — pure constructor. **(12) NEW — `producer::signing` (DC-CRYPTO-03/04/05 + OP-OPS-04, S1):** must NOT expose `pub struct .*SigningKey` outside `producer/` (`ci/ci_check_private_key_custody.sh`); must NOT call `cardano_crypto::vrf::VrfDraft03::prove` / `cardano_crypto::kes::KesAlgorithm::sign_kes` / `update_kes` outside `signing.rs`; must NOT return raw `[u8; N]` / `Vec<u8>` from any `pub fn` (every signing output is wrapped in a closed BLUE type — `VrfProof`, `VrfOutput`, `KesSignature`); must carry a custom (non-derived) `Debug` impl on every secret-bearing struct that redacts the bytes; must implement `Drop` that zeroizes the byte buffer on `VrfSigningKey`, `KesSecret`, `ColdSigningKey`; KES evolution is one-way — `kes_sign` rejects `period > current_period + evolutions_remaining` (`SigningError::PeriodOutOfRange`), `kes_update` rejects `to < from` or `to > from + evolutions_remaining` (`SigningError::EvolutionBackwards`); `SigningError` is closed (no `#[non_exhaustive]`, no String-bearing variant). **(13) NEW — `producer::keys` (OP-OPS-04, S1):** must NOT generate keys — Ade is not a key generator; only parses operator-supplied cardano-cli text envelopes. Must NOT include the file path in `KeyLoadError` (path strings must not leak via logs); `KeyLoadError` carries `std::io::ErrorKind`, not `std::io::Error` with its captured path. Must validate the envelope `type` field against the expected algorithm string (`VrfSigningKey_PraosVRF`, `KesSigningKey_ed25519_kes_2^6`, `StakePoolSigningKey_ed25519` — the three constants are the only sanctioned envelope types at HEAD). **(14) NEW — `producer::tick_assembler` (DC-CONS-14, S6, GREEN):** must be a pure function `assemble_tick(slot, base_state, mempool, inputs) -> Result<ProducerTick, TickAssemblyError>` — no `tokio`, no `SystemTime`/`Instant`, no `rand`, no `HashMap`, no `async` (enforced by `ci/ci_check_scheduler_closure.sh`); must NOT invoke RED signing primitives (the signed artifacts arrive as inputs); must NOT read I/O; must verify `inputs.mempool_tx_bytes.len() == mempool.accepted().len()` (`TickAssemblyError::MempoolWidthMismatch`). Captured RED outputs MUST produce a byte-identical `ProducerTick` across two replays. **(15) NEW — `producer::scheduler` (DC-CONS-13 + OP-OPS-05, S6):** `scheduler_step(state, input, ledger_view) -> (state', effects)` is a pure value transition — no wall-clock inside the function body, no I/O, no rand; the outer driver (binary layer) feeds the slot number from a wall-clock source. Identical `(state, input, ledger_view)` MUST produce identical `(state', effects)` (test-pinned by `tick_assembler_deterministic_over_captured_red_outputs`). Once `state.halted == Some(reason)`, subsequent `SlotTick` inputs MUST be ignored and the same halt reason re-emitted (deterministic halt; no recovery). `SchedulerInput`/`SchedulerEffect`/`SchedulerHaltReason` MUST remain closed (no `#[non_exhaustive]`, no `String`-bearing variant). **(16) NEW — `producer::broadcast` (CN-CONS-07, S6):** `BroadcastQueue::enqueue` MUST take `AcceptedBlock` by value (move) — no `&AcceptedBlock`, no `Vec<u8>` overload, no `From<Vec<u8>>` (forbidden by `ci/ci_check_scheduler_closure.sh`); the type-level CN-CONS-07 gate is mechanical here: the only way to obtain an `AcceptedBlock` is via `ade_ledger::producer::self_accept::self_accept`. Iteration of the FIFO MUST use `VecDeque`'s deterministic order (no `HashMap`); `BroadcastError` MUST remain closed; the broadcast queue is back-pressure-only (`QueueFull` does NOT halt the scheduler — only `SelfAcceptError` halts). |
| **Inbound deps** | `ade_testkit` (non-dev dep, used by `stream_replay`), `ade_core_interop`. The binary `chaindb_kill_target` lives at `src/bin/`. **NEW heavy consumption sites in PHASE4-N-C** — `ade_core_interop::bin::live_block_production_session` imports the full `producer/` sub-tree; `ade_testkit::producer::reference_vectors` is consumed by the inline tests in `signing.rs` (dev-dep edge). |
| **Outbound deps** | `ade_types`, `ade_core`, `ade_crypto`, `ade_codec`, `redb`, `serde_json`. **NEW outbound use sites in PHASE4-N-C** — `ade_ledger` (the `producer/scheduler.rs` + `producer/broadcast.rs` import `ade_ledger::producer::{forge::*, self_accept, AcceptedBlock, SelfAcceptError}`, `ade_ledger::state::LedgerState`, `ade_ledger::pparams::ProtocolParameters`, `ade_ledger::mempool::admit::MempoolState`; `producer/tick_assembler.rs` imports the same plus `ade_ledger::producer::state::ProducerTick`); `cardano-crypto` (production dep now — `producer/signing.rs` calls `cardano_crypto::vrf::VrfDraft03::prove` and `cardano_crypto::kes::{KesAlgorithm, Sum6Kes}`; previously cardano-crypto was a dev-dep only). The `ade_runtime → ade_ledger` edge is a legal RED → BLUE direction. Dev-deps: `tempfile`, `ade_testkit`. |
| **Entry points** | **Chaindb / recovery:** `ade_runtime::chaindb::*`, `ade_runtime::recovery::*`. **Consensus:** `ade_runtime::consensus::chain_selector::*`, `ade_runtime::consensus::candidate_fragment::*`, `ade_runtime::consensus::genesis_parser::*`. **NEW — PHASE4-N-C `producer/`:** `ade_runtime::producer::signing::{vrf_prove, kes_sign, kes_update, VrfSigningKey, KesSecret, ColdSigningKey, SigningError}`; `ade_runtime::producer::keys::{load_vrf_signing_key_skey, load_kes_signing_key_skey, load_cold_signing_key_skey, KeyLoadError, VRF_SIGNING_KEY_TYPE, KES_SIGNING_KEY_TYPE, POOL_SIGNING_KEY_TYPE}`; `ade_runtime::producer::tick_assembler::{assemble_tick, TickInputs, TickAssemblyError}`; `ade_runtime::producer::scheduler::{scheduler_step, SchedulerInput, SchedulerEffect, SchedulerHaltReason, SchedulerState}`; `ade_runtime::producer::broadcast::{BroadcastQueue, BroadcastError}`. |
| **Key modules** | **`chaindb/`**, **`recovery.rs`**, **`consensus/`** (`genesis_parser.rs` RED + `candidate_fragment.rs` GREEN + `chain_selector.rs` GREEN), **`bin/chaindb_kill_target.rs`**, **NEW — `producer/`** (PHASE4-N-C): `producer/mod.rs` (23 LOC barrel), `producer/signing.rs` (600 LOC + 10 inline tests, **RED**), `producer/keys.rs` (376 LOC + 4 inline tests, **RED**), `producer/tick_assembler.rs` (211 LOC + 3 inline tests, **GREEN**), `producer/scheduler.rs` (478 LOC + 4 inline tests, **RED**), `producer/broadcast.rs` (265 LOC + 3 inline tests, **RED**). Integration test: `tests/producer_pipeline_slot_deadline.rs` (PHASE4-N-C S6, wall-clock slot-deadline measurement). |
| **Mechanical enforcement** | Three carry-forward chaindb / recovery / crash-safety scripts. **NEW — eight PHASE4-N-C CI scripts back the producer surface:** `ci/ci_check_forge_purity.sh` (DC-CONS-13/14/15, scope `ade_ledger::producer::forge`), `ci/ci_check_no_producer_body_encoder.sh` (DC-CONS-16, repo-wide), `ci/ci_check_no_private_keys_in_corpus.sh` (DC-CRYPTO-03/04/05 corpus discipline, scope `ade_testkit/fixtures/` + `ade_testkit::producer::fixtures`), `ci/ci_check_opcert_closed.sh` (DC-CONS-11/12, scope `ade_codec::shelley::opcert` + `ade_core::consensus::opcert_validate`), `ci/ci_check_private_key_custody.sh` (DC-CRYPTO-03/04/05 + OP-OPS-04, scope full BLUE + `ade_runtime::producer`), `ci/ci_check_producer_corpus_present.sh` (CN-CONS-06 mechanical half, scope `ade_testkit::producer::fixtures`), `ci/ci_check_scheduler_closure.sh` (DC-CONS-13 + DC-MEM-03 + OP-OPS-05, scope `ade_runtime::producer/{scheduler,broadcast,tick_assembler}`), `ci/ci_check_self_accept_gate.sh` (CN-CONS-07, scope `ade_ledger::producer::self_accept`). |

> **Gap surfaced (carried + extended).** No dedicated CI script enforces the GREEN/RED partition *inside* `ade_runtime::consensus`. **NEW — same gap class extends to `ade_runtime::producer`:** the GREEN sub-classification of `producer::tick_assembler` inside the RED `ade_runtime` crate prefix is partially gated by `ci/ci_check_scheduler_closure.sh` check for pure-fn shape (`no tokio / SystemTime / Instant / rand / HashMap / async` in `tick_assembler.rs`), but the broader BLUE/GREEN/RED partition is "by file" and enforced by review + header comments.

---

## Cross-Module Rules (project-wide)

### Dependency direction

`ade_core_interop` → `{ade_core, ade_codec, ade_crypto, ade_ledger, ade_runtime, ade_network, ade_testkit, ade_types, tokio}` is legal (RED leaf binary). **No new crate-level outbound edge in PHASE4-N-C** — the S7 binary reuses these.
`ade_runtime` → `{ade_core, ade_crypto, ade_codec, ade_types, redb, serde_json, cardano-crypto, **NEW — ade_ledger**}` is legal (RED → BLUE; the new `ade_runtime → ade_ledger` edge introduced by PHASE4-N-C is RED-shell consuming BLUE producer authorities and is mechanically conformant with `ci_check_dependency_boundary.sh` — the script forbids BLUE → RED, not RED → BLUE).
`ade_testkit` → `{ade_core, ade_ledger, ade_plutus, ade_runtime, ade_crypto, ade_codec, ade_types, **NEW dev-dep use site of cardano-crypto**}` is legal (GREEN).
`ade_network` (BLUE submodules) → `{ade_codec, ade_types}` is legal.
`ade_network` (RED submodules + capture bins) → `{tokio, ade_codec, ade_types, ade_network::codec::*, ade_network::mux::frame}` is legal.
`ade_ledger` → `{ade_core, ade_plutus, ade_crypto, ade_codec, ade_types, minicbor}` is legal (BLUE among BLUEs). The new `producer/` sub-tree reuses these edges — no new outbound from `ade_ledger`.
`ade_core` → `{ade_types, ade_crypto, minicbor}` is legal (BLUE among BLUEs). `consensus::opcert_validate` reuses these.
`ade_plutus` → `{ade_crypto, ade_codec, ade_types}` is legal.
`ade_crypto` → `{ade_types}` is legal. The new `KesSignature` adds no outbound edge.
`ade_codec` → `{ade_types}` is legal. The new `shelley::opcert` + `shelley::tx_components` reuse this.
`ade_types` → `{}`.

**Forbidden directions.** Any BLUE crate (or BLUE `ade_network` submodule) depending on `ade_runtime`, `ade_node`, `ade_core_interop`, or the RED half of `ade_network` is a CI failure (`ci_check_dependency_boundary.sh` + `ci_check_no_async_in_blue.sh`). Any non-`ade_plutus` crate referring to `pallas_*` is a CI failure (`ci_check_pallas_quarantine.sh`). Any reference to `ChainDb` / `chain_db` inside `crates/ade_core/src/consensus/` is a CI failure (`ci_check_no_chaindb_in_consensus_blue.sh`). B1/B2/N-E — BLUE/GREEN sub-partition inside `ade_ledger::mempool` is review-and-test-enforced for the broad partition, with two narrow grep gates. B3 — deposit-param-authority gate. PP-S1 — `ProposalProcedure {…}` struct-literal scan. **NEW — PHASE4-N-C dependency notes (the 8 producer CI gates pin specific properties):** any new `pub fn .*encode_block_body` outside the canonical authority is a CI failure (`ci_check_no_producer_body_encoder.sh`, DC-CONS-16). Any private-key type (`*SigningKey`/`KesSecret`/`ColdSigningKey`) appearing in BLUE public APIs is a CI failure (`ci_check_private_key_custody.sh`, DC-CRYPTO-03/04/05 + OP-OPS-04). Any `cardano_crypto::vrf::VrfDraft03::prove` / `cardano_crypto::kes::KesAlgorithm::sign_kes` / `update_kes` call outside `ade_runtime::producer::signing` is a CI failure (same script). Any `SystemTime`/`Instant`/`rand`/`HashMap`/`std::env`/`std::fs`/`tokio`/`async` in `ade_ledger::producer::forge` is a CI failure (`ci_check_forge_purity.sh`, DC-CONS-13/14/15). Any `AcceptedBlock {…}` struct-literal outside `self_accept.rs` is a CI failure (`ci_check_self_accept_gate.sh` guard 1a, CN-CONS-07). Any `impl Default | From<…> | TryFrom<…> for AcceptedBlock` anywhere is a CI failure (same script guard 1c). Any parallel opcert encoder/decoder outside `ade_codec::shelley::opcert` + the header-embedded path in `ade_codec::shelley::block` + `ade_core::consensus::opcert_validate` is a CI failure (`ci_check_opcert_closed.sh`, DC-CONS-11/12). Any `BroadcastQueue::enqueue` taking `&AcceptedBlock` or raw bytes is a CI failure (`ci_check_scheduler_closure.sh`, CN-CONS-07). Any private-key byte literal in a replay corpus fixture is a CI failure (`ci_check_no_private_keys_in_corpus.sh`).

### Naming convention

All crates are prefixed `ade_`. TCB color is not encoded in the crate name. The authoritative classifier is `.idd-config.json` `core_paths` plus the cluster doc TCB Color Maps for sub-crate scopes; CI scripts hard-code their BLUE list. The seven full-BLUE-scoped scripts use the full BLUE set: 6 BLUE crates + 9 BLUE `ade_network` paths. The B3 `ci_check_deposit_param_authority.sh` scopes the 6 BLUE crates. Four narrow scripts target `crates/ade_core/src/consensus/`. The two N-E scripts scope `crates/ade_ledger/src/mempool/*` + `crates/ade_testkit/src/mempool/*`. The PP-S1 `ci_check_proposal_procedures_closed.sh` scopes `conway/{governance.rs, tx.rs}` paths. **NEW — the 8 PHASE4-N-C scripts narrow as follows:** `ci_check_forge_purity.sh` scopes `crates/ade_ledger/src/producer/forge.rs`. `ci_check_no_producer_body_encoder.sh` scopes repo-wide (single body-hash authority). `ci_check_no_private_keys_in_corpus.sh` scopes `crates/ade_testkit/src/producer/fixtures.rs` + `crates/ade_testkit/fixtures/producer/`. `ci_check_opcert_closed.sh` scopes `crates/ade_codec/src/shelley/opcert.rs` + `crates/ade_core/src/consensus/opcert_validate.rs`. `ci_check_private_key_custody.sh` scopes the full BLUE set + `crates/ade_runtime/src/producer/`. `ci_check_producer_corpus_present.sh` scopes `crates/ade_testkit/src/producer/fixtures.rs`. `ci_check_scheduler_closure.sh` scopes `crates/ade_runtime/src/producer/{scheduler,broadcast,tick_assembler}.rs`. `ci_check_self_accept_gate.sh` scopes `crates/ade_ledger/src/producer/self_accept.rs` + repo-wide `AcceptedBlock` literal scan. **No CI script narrowly scopes the new `live_block_production_session.rs` binary** — its RED classification rests on its file-header comment, its placement under `src/bin/`, and the `#[ignore]`-gated closure-gate test pattern (matching `live_consensus_session.rs` + `live_tx_submission_session.rs`).

### CI enforcement (40 scripts under `ci/`)

| Script | Enforces | Scope |
|---|---|---|
| `ci_check_cbor_round_trip.sh` | T-ENC-03, DC-CBOR-01, DC-CBOR-02 | golden corpus |
| `ci_check_ce_n_a_5_proof.sh` *(PHASE4-N-A, S-A10)* | CE-N-A-5 5-condition evidence | `ade_network` (RED + real-capture corpus) |
| `ci_check_chaindb_contract.sh` | DC-STORE-02, DC-STORE-03, CN-STORE-04, CN-STORE-05 | `ade_runtime --lib chaindb::` (RED) |
| `ci_check_chaindb_crash_safety.sh` | T-REC-01 (crash variant), DC-STORE-01, CN-STORE-03 | `ade_runtime --test stress_kill_harness` (RED) |
| `ci_check_consensus_closed_enums.sh` *(PHASE4-N-B, S-B2; extended B1, B2)* | DC-CONS-04, DC-CONS-10, T-DET-01, DC-VAL-02/04/05/06, DC-TXV-01/02/04/05, DC-MEM-01/02 | `consensus/` + `block_validity/` + `tx_validity/` + `mempool/` |
| `ci_check_constitution_coverage.sh` | invariant-registry ↔ code/test coverage | repo-wide |
| `ci_check_conway_cert_classification_closed.sh` *(PHASE4-B3F)* | DC-TXV-06 | `ade_types::conway::cert` + `ade_codec::conway::cert` + `ade_ledger::cert_classify` |
| `ci_check_credential_discriminant_closed.sh` *(OQ5/COMMITTEE/DREP/ENACTMENT-COMMITTEE-FIDELITY + WRITEBACK)* | DC-LEDGER-10 | `ade_types::shelley::cert` + both era `ade_codec` decoders + `ade_ledger::{state, governance, fingerprint, rules}` + `ade_types::conway::governance` |
| `ci_check_crypto_vectors.sh` | crypto KAT regression | `ade_crypto` |
| `ci_check_dependency_boundary.sh` | T-BOUND-02 — BLUE ⇎ RED separation | full BLUE (6 crates + 9 `ade_network` paths) |
| `ci_check_deposit_param_authority.sh` *(PHASE4-B3)* | DC-TXV-07 | 6 BLUE crates |
| `ci_check_differential_divergence.sh` | DC-DIFF-* | replay outputs |
| `ci_check_forbidden_patterns.sh` | T-CORE-02 + `unsafe` allowlist | full BLUE |
| **`ci_check_forge_purity.sh`** *(NEW — PHASE4-N-C S3)* | **DC-CONS-13/14/15 — `forge_block` purity (no `SystemTime`/`Instant`/`rand`/`HashMap`/`std::env`/`std::fs`/`tokio`/`async`)** | `crates/ade_ledger/src/producer/forge.rs` |
| `ci_check_gov_cert_accumulation_closed.sh` *(PHASE4-B5)* | DC-LEDGER-09 | `ade_ledger::{gov_cert, rules, error, state}` |
| `ci_check_hash_uses_wire_bytes.sh` | DC-CBOR-02, T-ENC-01 | full BLUE |
| `ci_check_hfc_translation.sh` | DC-EPOCH-02 | `ade_ledger::hfc` |
| `ci_check_ingress_chokepoints.sh` | DC-INGRESS-01, T-INGRESS-01 | full BLUE |
| `ci_check_ledger_determinism.sh` | DC-LEDGER-01 | `ade_ledger` |
| `ci_check_mempool_ingress_closure.sh` *(PHASE4-N-E, S1)* | DC-MEM-03 (strengthens DC-MEM-01) | `ade_ledger::mempool/{ingress,admit,mod}.rs` + repo-wide `admit(` scan |
| `ci_check_mempool_ingress_replay.sh` *(PHASE4-N-E, S2 + S3)* | DC-MEM-04 | `ade_testkit::mempool/*` + `ade_testkit::lib.rs` + `ade_ledger::mempool::canonicalize` |
| `ci_check_module_headers.sh` | CE-04 contract banner | full BLUE |
| `ci_check_no_async_in_blue.sh` *(PHASE4-N-A, S-A1)* | DC-CORE-01 | full BLUE |
| `ci_check_no_chaindb_in_consensus_blue.sh` *(PHASE4-N-B, S-B1)* | DC-CORE-01 + DC-CONS-07 | `crates/ade_core/src/consensus/` |
| `ci_check_no_density_in_fork_choice.sh` *(PHASE4-N-B, S-B8)* | DC-CONS-03 | `fork_choice.rs` + `candidate.rs` |
| `ci_check_no_float_in_consensus.sh` *(PHASE4-N-B, S-B1)* | T-CORE-02 + DC-CONS-07/08/09 | `crates/ade_core/src/consensus/` |
| **`ci_check_no_private_keys_in_corpus.sh`** *(NEW — PHASE4-N-C S3)* | **DC-CRYPTO-03/04/05 corpus discipline — replay corpora carry signed artifacts only, no private-key bytes** | `crates/ade_testkit/src/producer/fixtures.rs` + `crates/ade_testkit/fixtures/producer/` |
| **`ci_check_no_producer_body_encoder.sh`** *(NEW — PHASE4-N-C S4)* | **DC-CONS-16 — single canonical body-hash authority; no parallel `encode_block_body` / `block_body_hash` definitions** | repo-wide grep gate |
| `ci_check_no_secrets.sh` | no credentials/IPs/keys | repo-wide |
| `ci_check_no_semantic_cfg.sh` | T-BUILD-01 | full BLUE |
| `ci_check_no_signing_in_blue.sh` | CE-05, T-KEY-01 | full BLUE |
| **`ci_check_opcert_closed.sh`** *(NEW — PHASE4-N-C S2)* | **DC-CONS-11/12 — closed `OpCertCodecError` + closed `OpCertError`; no parallel opcert encoders/decoders; counter-monotonicity discipline preserved** | `crates/ade_codec/src/shelley/opcert.rs` + `crates/ade_core/src/consensus/opcert_validate.rs` |
| `ci_check_pallas_quarantine.sh` | O-29.2 | non-`ade_plutus` |
| **`ci_check_private_key_custody.sh`** *(NEW — PHASE4-N-C S1)* | **DC-CRYPTO-03/04/05 + OP-OPS-04 — `*SigningKey`/`KesSecret`/`ColdSigningKey` confined to `ade_runtime::producer/`; no `cardano_crypto::vrf/kes` signing calls outside `signing.rs`; no `pub fn` returning raw `[u8; N]`/`Vec<u8>` in `signing.rs`** | full BLUE + `crates/ade_runtime/src/producer/` |
| **`ci_check_producer_corpus_present.sh`** *(NEW — PHASE4-N-C S7)* | **CN-CONS-06 mechanical half — at least one fixture with non-empty `expected_forged` captured bytes** | `crates/ade_testkit/src/producer/fixtures.rs` |
| `ci_check_proposal_procedures_closed.sh` *(PROPOSAL-PROCEDURES-DECODE PP-S1)* | DC-LEDGER-11 | `conway/{governance,tx}.rs` paths + repo-wide `ProposalProcedure {` scan |
| `ci_check_recovery_contract.sh` | T-REC-01, T-REC-02, DC-STORE-05 | `ade_runtime --lib recovery::` (RED) |
| `ci_check_ref_provenance.sh` | DC-REF-01 | reference corpus |
| **`ci_check_scheduler_closure.sh`** *(NEW — PHASE4-N-C S6)* | **DC-CONS-13 + DC-MEM-03 + OP-OPS-05 — closed `SchedulerInput`/`SchedulerEffect`/`SchedulerHaltReason`/`BroadcastError`/`TickAssemblyError`; `BroadcastQueue::enqueue` takes `AcceptedBlock` by value; `assemble_tick` pure** | `crates/ade_runtime/src/producer/{scheduler,broadcast,tick_assembler}.rs` |
| **`ci_check_self_accept_gate.sh`** *(NEW — PHASE4-N-C S5)* | **CN-CONS-07 — 6 guards: `AcceptedBlock` struct-literal scoped to `self_accept.rs`; exactly one `-> [Result<]AcceptedBlock` site; no Default/From/TryFrom impls; private `bytes` field; closed `SelfAcceptError`; `self_accept` calls `block_validity(` and not validator sub-steps; no raw-bytes `pub fn` outside accessors** | `crates/ade_ledger/src/producer/self_accept.rs` + repo-wide `AcceptedBlock` literal scan |

> **Post-`928c2be` CI delta. +8.** All eight new gates are the PHASE4-N-C producer-authority gate set. **CI inventory 32 → 40.** The forward-looking `ci/ci_check_no_fail_open_in_validation.sh` (DC-VAL-06 / DC-TXV) is still **not** shipped — carried gap.

> **Carried residual gaps (unchanged at HEAD, plus the new N-C-introduced ones).** (a) `ci/ci_check_no_fail_open_in_validation.sh` not shipped. (b) `ade_ledger::mempool` BLUE/GREEN broad partition review-and-test-enforced. (c) `ade_ledger::consensus_input_extract` RED sub-classification review-and-doc-enforced. (d) `ade_runtime::consensus` GREEN/RED partition "by file". (e) CE-N-B-6 manual evidence still pending operator log. (f) `ade_types::conway::cert` types outside the `consensus_closed_enums.sh` `TARGETS` array. (g) `ade_ledger::delegation` owner-tagged action types outside any narrow closed-enum gate. (h) DC-LEDGER-08/09/10 real epoch-576 oracle environment-blocked. (i) Conway block-body vkey-witness closure not yet wired into `rules.rs` block-body loop. (j) `ade_core_interop::{tx_submission, local_tx_submission}` GREEN sub-classification review-and-doc-enforced. (k) `ade_ledger::mempool::canonicalize` GREEN sub-classification partial CI coverage. (l) S6 `live_tx_submission_session` RED classification review-and-doc-enforced. (m) `CE-NODE-N2C-LTX` deferred to future node-binary cluster. (n) PP-S2 `ade_testkit::governance` "canonical-by-BLUE-encoder" property is a design invariant, not mechanical. (o) `voting_procedures` / `ParameterChange.update` / `NewConstitution.raw` / typed `RewardAccount` for `proposal_procedure.return_addr` deferred per OQ-1/2/3/4 (future PP-S1 strengthenings). **(p) NEW — `ade_runtime::producer::tick_assembler` GREEN sub-classification inside the RED `ade_runtime` crate prefix is partially gated by `ci_check_scheduler_closure.sh` (pure-fn shape) but the broader BLUE/GREEN/RED partition is "by file" — no dedicated CI script.** **(q) NEW — `ade_core_interop::bin::live_block_production_session` (S7) RED classification is review-and-doc enforced (matching the S6 precedent — no narrow CI gate scopes the partition).** **(r) NEW — CE-N-C-8 live evidence (CN-CONS-06 live half) carries `blocked_until_operator_stake_available` per the OQ-11 conditional-closure pattern; reopen criteria: provision testnet SPO stake, run the binary per `CE-N-C-8_PROCEDURE.md`, append the captured log path to CN-CONS-06's `evidence_notes`. The crypto-level cross-impl claim is by-design out of reach of any CI gate against the synthetic S3 corpus (all-zero KES/VRF artifacts).** **(s) NEW — the N-C S7 binary stubs N2N block-fetch server-side delivery; full producer-side relay-mesh behaviour is N-A-successor scope and re-opens the `ade_network::session` placeholder gap.**
