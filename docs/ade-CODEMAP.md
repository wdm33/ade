# Module Authority Map — Ade

> **Status:** Living architectural document. Regenerated; not hand-edited.
> Per-project instance of `~/.claude/methodology/templates/codemap.md`.

> 11 crates, 417 canonical types, 1551 tests, 52 CI checks at HEAD (`efe1fb9`).

---

## Conventions

- A **module** in Ade is a Cargo workspace crate (smallest independently-buildable unit). One exception: `ade_network` is split by *submodule color* — its BLUE submodules and its RED submodules are documented as two separate entries below, because `.idd-config.json` `core_paths` resolves BLUE at the submodule path level rather than crate-wide. The `ade_core::consensus` submodule sits *inside* the BLUE `ade_core` crate and is covered by that entry. The `ade_ledger::block_validity` / `ade_ledger::consensus_view` / `ade_ledger::tx_validity` / `ade_ledger::mempool::admit` / `ade_ledger::mempool::ingress` / `ade_ledger::cert_classify` modules sit inside the BLUE `ade_ledger` crate and are covered by that entry; the `ade_ledger::producer` sub-tree (PHASE4-N-C — `forge`, `self_accept`, `state`; PHASE4-N-G — `served_chain`) and the `ade_ledger::block_body_hash` top-level module (PHASE4-N-C S4 single body-hash authority) are likewise BLUE under the already-BLUE `ade_ledger` crate prefix. **NEW — PHASE4-N-H — `ade_ledger::receive` sub-tree** (BLUE under the already-BLUE `ade_ledger` crate prefix) hosts the receive-side header→body bridge authority: `admitted` (the `AdmittedBlock` token + `admit_via_block_validity` single-construction-site admission gate), `events` (closed `ReceiveEvent`/`ReceiveEffect`/`ReceiveError` sums), `pending_header_cache` (BTreeMap-backed cache), `chain_write` (narrow `ChainDbWrite` trait), `reducer` (pure `receive_apply` + `receive_apply_sequence`). The RED `ade_ledger::consensus_input_extract`, the GREEN `ade_ledger::mempool::policy`, and the GREEN `ade_ledger::mempool::canonicalize` sit inside the BLUE `ade_ledger` crate but carry a different color by their own module doc-comment and the cluster TCB Color Maps — they are surfaced as sub-classification notes inside the `ade_ledger` entry. The `ade_testkit::mempool` sub-tree (PHASE4-N-E S2), the `ade_testkit::governance` sub-tree (PROPOSAL-PROCEDURES-DECODE PP-S2), and the `ade_testkit::producer` sub-tree (PHASE4-N-C) sit inside the GREEN `ade_testkit` crate, classified GREEN by their own module doc-comments and the cluster TCB Color Maps. **PHASE4-N-G — `ade_runtime::network` sub-tree** sits inside the RED `ade_runtime` crate and hosts the per-peer N2N **server** session driver (RED). **NEW — PHASE4-N-H — `ade_runtime::receive` sub-tree** sits inside the RED `ade_runtime` crate and hosts the GREEN+RED receive-side glue: `events_to_state` (GREEN pass-through adapter), `in_memory_chain_write` (GREEN `ChainDbWriter` wrapping any `ChainDb`), `orchestrator` (RED per-peer client-role dispatch driver).
- Modules are listed by TCB color (BLUE → GREEN → RED), alphabetical within each color.
- TCB color sources, in order of authority:
  1. `.idd-config.json` `core_paths` — substring match against absolute path. BLUE matches: `ade_codec`, `ade_types`, `ade_crypto`, `ade_core`, `ade_ledger` (covers the new `ade_ledger::receive` sub-tree), `ade_plutus`, and the 9 `ade_network` submodule paths (`mux/frame.rs`, `codec/`, `handshake/`, `chain_sync/`, `block_fetch/`, `tx_submission/`, `keep_alive/`, `peer_sharing/`, `n2c/`).
  2. `.idd-config.json` `_core_paths_doc` — `ade_runtime` is RED; `ade_testkit` is GREEN; `ade_node` is RED; `ade_network::mux::transport` and `ade_network::session` are RED; `ade_network::lib` and `ade_network::mux::mod` are GREEN barrels. `ade_core_interop` is **RED** per its `Cargo.toml` header comment and the PHASE4-N-B TCB Color Map; the `ade_core_interop::tx_submission` (S4) and `ade_core_interop::local_tx_submission` (S5) modules carry a GREEN sub-classification by their own module doc-comments. The RED binaries `ade_core_interop::bin::live_tx_submission_session` (N-E S6), `ade_core_interop::bin::live_block_production_session` (N-C S7), `ade_core_interop::bin::live_block_fetch_session` (N-G S7), and **`ade_core_interop::bin::live_block_follow_session` (PHASE4-N-H S6, NEW)** are the operator-action live evidence pattern (RED by their own file-header comments and the established `live_consensus_session` precedent).
  3. `docs/clusters/completed/PHASE4-N-B/cluster.md` § "TCB Color Map" — `era_schedule`, `praos_state`, `vrf_cert`, `nonce`, `op_cert`, `leader_schedule`, `header_validate`, `fork_choice`, `rollback` are BLUE; `chain_selector`, `candidate_fragment` are GREEN; `genesis_parser` is RED; `ade_testkit::consensus` is GREEN; `ade_core_interop` is RED.
  4. `docs/clusters/completed/PHASE4-B1/cluster.md` § "TCB Color Map" — `ade_ledger::consensus_view`, `ade_ledger::block_validity` are BLUE; `ade_core::consensus::{header_validate, kes_check}` extensions are BLUE; `ade_testkit::validity` is GREEN; `ade_ledger::consensus_input_extract` is RED.
  5. PHASE4-B2 (tx-validity agreement) — `ade_ledger::tx_validity::*` and `ade_ledger::mempool::admit` are BLUE; `ade_ledger::mempool::policy` is GREEN (Tier-5); `ade_testkit::tx_validity` is GREEN.
  6. PHASE4-B3/B5 (tx value-conservation + Conway governance-cert accumulation) — all new/changed modules are BLUE under the already-BLUE crate prefixes.
  7. PHASE4-N-E (mempool wire ingress) — `ade_ledger::mempool::ingress` is BLUE (S1); `ade_ledger::mempool::canonicalize` is GREEN (S3); `ade_testkit::mempool::ingress_replay` is GREEN (S2); `ade_core_interop::tx_submission` is GREEN (S4); `ade_core_interop::local_tx_submission` is GREEN (S5); `ade_core_interop::bin::live_tx_submission_session` is RED (S6).
  8. PROPOSAL-PROCEDURES-DECODE — `ade_codec::conway::governance` is BLUE; the new `ProposalProcedure` canonical type in `ade_types::conway::governance` is BLUE; `ade_testkit::governance::proposal_procedures_replay` is GREEN.
  9. PHASE4-N-C — `ade_ledger::producer::{forge, self_accept, state}` BLUE; `ade_ledger::block_body_hash` BLUE; `ade_codec::shelley::{opcert, tx_components}` BLUE; `ade_core::consensus::opcert_validate` BLUE; `ade_crypto::kes::KesSignature` BLUE; `ade_runtime::producer::{signing, keys, scheduler, broadcast}` RED; `ade_runtime::producer::tick_assembler` GREEN; `ade_testkit::producer::*` GREEN; `ade_core_interop::bin::live_block_production_session` RED.
  10. PHASE4-N-G — `ade_network::chain_sync::server` and `ade_network::block_fetch::server` BLUE; `ade_ledger::producer::served_chain` BLUE; the `accepted_block_header_bytes` accessor on `ade_ledger::block_validity::header_input` BLUE; `ade_runtime::producer::{broadcast_to_served, served_chain_lookups}` GREEN; `ade_runtime::network::n2n_server` RED; `ade_core_interop::bin::live_block_fetch_session` RED.
  11. **NEW — `docs/clusters/completed/PHASE4-N-H/cluster.md` § "TCB Color Map"** — `ade_ledger::receive::{admitted, chain_write, events, pending_header_cache, reducer}` (S1, S2) are **BLUE** (under the already-BLUE `ade_ledger` crate prefix and per per-file `// Core Contract:` banners; the closure shape is gated mechanically by `ci/ci_check_admitted_block_closure.sh` + `ci/ci_check_receive_reducer_closure.sh`); `ade_runtime::receive::events_to_state` and `ade_runtime::receive::in_memory_chain_write` (S3) are **GREEN** (pure deterministic glue; pass-through discipline mechanically enforced by `ci/ci_check_receive_replay_purity.sh`); `ade_runtime::receive::orchestrator` (S4) is **RED** (per-peer client-role N2N dispatch driver, file-header RED banner; key-boundary gate `ci/ci_check_receive_orchestrator_no_producer_dep.sh`); `ade_core_interop::bin::live_block_follow_session` (S6) is **RED** (operator-action live evidence binary, matches the `live_block_production_session` + `live_block_fetch_session` precedent).

- **Active cluster at HEAD (none).** PHASE4-N-H (receive-side header→body bridge, Path A admit-only scope) is **closed at HEAD `efe1fb9`** and archived under `docs/clusters/completed/PHASE4-N-H/`. The cluster's 5 new registry rules at `status = "enforced"` are `CN-CONS-08`, `DC-CONS-19`, `DC-PROTO-09`, `CN-PROTO-07`; the 5th new rule `DC-CONS-20` remains `status = "declared"` with `open_obligation = "rollback_side_blocked_until_ledger_snapshot_cluster"` (Path A scope edge — the admit-side half is enforced under CN-CONS-08 + DC-CONS-19 + DC-PROTO-09; the rollback-side half awaits a follow-on ledger-snapshot/rollback cluster). The 6th new rule `RO-LIVE-02` (live cross-impl follow evidence) is `status = "partial"` + `open_obligation = "blocked_until_operator_peer_available"`, mirroring the RO-LIVE-01 pattern. Existing rules **strengthened in PHASE4-N-H**: `T-DET-01`, `T-ENC-01`, `DC-CONS-13`, `DC-CONS-16`, `DC-PROTO-06`, `CN-CONS-07`. PHASE4-N-G's `RO-LIVE-01` and PHASE4-N-C's `CN-CONS-06` both still carry their existing operator-action `open_obligation`s (carried). `docs/clusters/` currently contains only the archive of the PHASE4-N-B follow-on (CE-N-B-6 manual evidence still pending operator log) and the closed `completed/` set (12 directories).

- **Delta since prior CODEMAP HEAD `a280954` (this regeneration — PHASE4-N-H cluster, 6 slices S1–S6 over commits `b019ee3..efe1fb9`).**

  **Structural deltas summarized:**
  - **NEW BLUE sub-tree:** `ade_ledger::receive/` (S1, S2). Five files: `admitted.rs` (the `AdmittedBlock` private-constructor token + `admit_via_block_validity` single-public-constructor + `AdmittedOutcome`); `chain_write.rs` (narrow `ChainDbWrite` trait taking `AdmittedBlock` by value + closed `ChainWriteError`/`ChainWriteErrorKind` sums — the trait surface preserves the admission gate across the BLUE→GREEN boundary); `events.rs` (closed `ReceiveEvent` sum with exactly three variants `RollForward`/`RollBackward`/`BlockDelivered` — locally-originated chain-sync/block-fetch messages are not constructible here per CN-PROTO-07; closed `ReceiveEffect`/`ReceiveError`/`NoOpReason` + `TargetPoint`/`TipPoint`); `pending_header_cache.rs` (`PendingHeaderCache`, BTreeMap-backed with insert/get/contains/remove/evict_below/iter — idempotent on byte-identity, `ByteConflict` on byte-divergence); `reducer.rs` (`ReceiveState` bundling ledger + chain_dep + pending_headers, plus `receive_apply` + `receive_apply_sequence` — pure BLUE, total, deterministic; `RollForward` caches header bytes WITHOUT mutating ledger/chain_dep (Invariant I-6); `BlockDelivered` runs `admit_via_block_validity` then commits ledger/chain_dep atomically; `RollBackward` returns `Err(RollbackOutOfScope)` per Path A scope edge). **CN-CONS-07 strengthened in N-H:** the producer broadcast token (`AcceptedBlock`) and the receive admission token (`AdmittedBlock`) are deliberately disjoint types; cross-use is mechanically impossible.
  - **NEW GREEN+RED sub-tree (inside RED `ade_runtime`):** `ade_runtime::receive/` (S3, S4). Three files: `events_to_state.rs` (GREEN; `lift_chain_sync_signal` + `lift_block_fetch_event` — pure pass-through, NEVER decodes header_bytes or block_bytes; the BLUE reducer's `BlockDelivered` branch is the canonical decode site); `in_memory_chain_write.rs` (GREEN; `ChainDbWriter<'a, D: ChainDb>` impl `ChainDbWrite` — derives `(slot, hash)` from the AdmittedBlock bytes via `decode_block` and calls `ChainDb::put_block`); `orchestrator.rs` (RED; `PerPeerReceiveState` carrying receive_state + handshake-negotiated chain_sync_version + block_fetch_version; `dispatch_chain_sync_inbound`/`dispatch_block_fetch_inbound` — decode one mini-protocol frame, lift via the GREEN adapter, run the BLUE reducer; `ReceiveDispatchError` closed sum). Pure state-machine driver — no socket I/O lives here; the socket layer is one level up at the binary boundary. Key-boundary doctrine: `ade_runtime::receive::*` MUST NOT import from `crate::producer::{signing, broadcast, scheduler}` — enforced by `ci/ci_check_receive_orchestrator_no_producer_dep.sh`.
  - **NEW RED operator-action live binary:** `ade_core_interop::bin::live_block_follow_session` (S6, 141 LOC). The CE-N-H-6 evidence-capture pass — an Ade follower fed real RollForward + BlockDelivered from a private cardano-node peer. Default hermetic `main` prints readiness + exits; `--connect` is the explicit operator-action flag. **Honest scope at HEAD:** the actual tokio socket wiring from `ade_runtime::receive::orchestrator` to the wire is stubbed pending operator-action — the binary in `--connect` mode prints the wiring stub plan + exits without opening sockets. Writes `docs/clusters/PHASE4-N-H/CE-N-H-LIVE_<date>.log` once the operator-action pass runs.
  - **NEW integration tests under `ade_runtime/tests/`:** `receive_session_transcript_replay.rs` (S3 — synthetic session-transcript replay: `receive_session_transcript_replay_byte_identical`); `receive_two_peer_independence.rs` (S4 — OQ-4 multi-peer determinism: `two_peers_admit_same_block_into_shared_chaindb`, `two_peers_per_session_transcripts_match_solo_runs`); `receive_pipeline_corpus_drive.rs` (S5 — mechanical cross-impl drive — CE-N-H-5: `receive_pipeline_corpus_drive_admits_every_block`, `receive_pipeline_corpus_drive_chaindb_tip_matches_expected`, `receive_pipeline_corpus_drive_admitted_bytes_equal_corpus_bytes`, `receive_pipeline_corpus_drive_ledger_fingerprint_changes_on_admit`). **NEW integration test under `ade_core_interop/tests/`:** `live_block_follow_session.rs` (1 test, hermetic-mode smoke check matching the live-binary pattern).
  - **NEW closed canonical types (+13).** Per source file: `AdmittedBlock`, `AdmittedOutcome` (in `ade_ledger::receive::admitted`); `ChainWriteError`, `ChainWriteErrorKind` (in `ade_ledger::receive::chain_write`); `TargetPoint`, `TipPoint`, `ReceiveEvent`, `ReceiveEffect`, `NoOpReason`, `ReceiveError` (in `ade_ledger::receive::events`); `PendingHeaderCache`, `PendingHeaderCacheError` (in `ade_ledger::receive::pending_header_cache`); `ReceiveState` (in `ade_ledger::receive::reducer`). The three GREEN+RED types `ChainDbWriter`, `PerPeerReceiveState`, `ReceiveDispatchError` live inside the RED `ade_runtime` crate prefix and are not counted in the BLUE canonical-type total. **Canonical type count: 404 → 417 (+13).** Per-BLUE-crate breakdown at HEAD: `ade_codec` 10, `ade_types` 81, `ade_crypto` 13, `ade_core` 44, `ade_ledger` **152 (+13)**, `ade_plutus` 8, BLUE `ade_network` 109 = 417. (Note — the BLUE total now sums to 417 because the canonical-type count for the BLUE scope is computed by `grep -E "^(pub )?(struct\|enum) "` across the 6 BLUE crates plus the 9 BLUE `ade_network` paths, and 308 + 109 = 417.)
  - **NEW CI gates (+5):** `ci/ci_check_admitted_block_closure.sh` (S1; CE-N-H-1 + CN-PROTO-07; repo-wide grep — `pub fn .* AdmittedBlock` only at `admitted.rs::admit_via_block_validity`; `pub struct AdmittedBlock` only in `admitted.rs`; no `pub use ... AdmittedBlock as ...` rename re-exports; `events.rs` MUST NOT reference `RequestNext`/`RequestRange`/`ClientDone`/`FindIntersect` — CN-PROTO-07 closure), `ci/ci_check_receive_reducer_closure.sh` (S2; CE-N-H-2 + CN-CONS-08 + DC-CONS-19; scopes `crates/ade_ledger/src/receive/reducer.rs` — no `HashMap`/`HashSet`/wall-clock/`tokio`/`rand` in production; positive grep for `admit_via_block_validity` + `RollbackOutOfScope`; the `roll_forward` helper body MUST NOT assign to `state.ledger` or `state.chain_dep` — Invariant I-6 enforced by AWK-extracted function-body scan), `ci/ci_check_receive_replay_purity.sh` (S3; CE-N-H-3 + DC-PROTO-09; scopes `crates/ade_runtime/src/receive/{events_to_state,in_memory_chain_write}.rs` — no `HashMap`/`HashSet`/wall-clock/`tokio`/`rand`; `events_to_state.rs` MUST NOT call `decode_block_envelope`/`decode_block`/`accepted_block_header_bytes` — pass-through only; positive presence of the lifters + `ChainDbWriter`'s `impl ChainDbWrite`), `ci/ci_check_receive_orchestrator_no_producer_dep.sh` (S4; key-boundary; scopes `crates/ade_runtime/src/receive/` — forbids any `producer::signing`/`producer::broadcast`/`producer::scheduler`/`crate::producer::` import + forbids the producer-side private-key/broadcast type names `VrfSigningKey`/`KesSecret`/`KesSigningKey`/`SigningError`/`BroadcastQueue`), `ci/ci_check_receive_paths_corpus_present.sh` (S6; CE-N-H-5 + CE-N-H-6 binary presence; checks the three integration tests exist with their named test functions; the live binary src + Cargo.toml `[[bin]]` entry exist; the procedure doc `CE-N-H-6_PROCEDURE.md` exists in either `docs/clusters/PHASE4-N-H/` pre-close or `docs/clusters/completed/PHASE4-N-H/` post-close). **CI script count: 47 → 52 (+5).**
  - **NEW registry rules / strengthenings:** 6 new entries — `CN-CONS-08` (enforced, single-admission-authority for receive-side), `DC-CONS-19` (enforced, body-decode hash binds to cached header at `(slot, block_hash)`), `DC-CONS-20` (declared, ChainDb-ledger-chain_dep lockstep — admit half implicitly enforced via CN-CONS-08 + DC-CONS-19; rollback half blocked on follow-on ledger-snapshot cluster), `DC-PROTO-09` (enforced, GREEN adapter pass-through purity + transcript-replay determinism), `CN-PROTO-07` (enforced, receive-side events closure — locally-originated chain-sync/block-fetch messages unrepresentable as `ReceiveEvent` constructors), `RO-LIVE-02` (partial — live cross-impl follow evidence, `blocked_until_operator_peer_available`). Strengthens `T-DET-01`, `T-ENC-01`, `DC-CONS-13`, `DC-CONS-16`, `DC-PROTO-06`, `CN-CONS-07` (each gained `PHASE4-N-H` in their `strengthened_in` array — receive-side analogues of producer-side properties: byte-identical replay, single body-hash recipe, version-pinned wire grammar, broadcast-vs-admission token separation). **Total registry rules: 196 → 202 (+6).**
  - **NEW test inventory (+43).** Per-crate at HEAD: `ade_codec` 162 (unchanged), `ade_types` 23 (unchanged), `ade_crypto` 51 (unchanged), `ade_core` 126 (unchanged), `ade_ledger` 502 → **522 (+20)** (3 inline tests in `admitted.rs` + 3 in `events.rs` + 5 in `pending_header_cache.rs` + 1 in `chain_write.rs` + 7 in `reducer.rs` = 19 inline; an additional `cert_classify` test landed alongside per repo refresh = 20), `ade_plutus` 28 (unchanged), `ade_testkit` 312 (unchanged), `ade_runtime` 78 → **100 (+22)** (4 inline tests in `receive/orchestrator.rs` + 8 in `receive/events_to_state.rs` + 2 in `receive/in_memory_chain_write.rs` = 14 inline; plus 4 + 1 + 2 = 7 new integration tests across `tests/receive_pipeline_corpus_drive.rs` + `tests/receive_session_transcript_replay.rs` + `tests/receive_two_peer_independence.rs`; remaining +1 absorbed by inline-test refinements), `ade_network` 200 (unchanged), `ade_node` 0, `ade_core_interop` 26 → **27 (+1)** (the `tests/live_block_follow_session.rs` hermetic smoke). **Test inventory 1508 → 1551 (+43)**, reported approximate per the template's fallback rule (test runner not executed; counts from `grep -cE "#\[test\]|#\[tokio::test\]"` workspace-wide).
  - **No new crate-level outbound edges.** PHASE4-N-H reuses the established `ade_runtime → {ade_ledger, ade_network, ade_core, ade_codec, ade_types, ade_crypto}` set; the new `ade_runtime::receive::*` files import `ade_ledger::receive::*` + `ade_network::{chain_sync::signal, block_fetch::event, codec::{chain_sync, block_fetch, version}}` over existing edges. The new `live_block_follow_session` binary reuses the already-established `ade_core_interop → {ade_runtime, ade_network, ade_ledger, ade_codec, ade_types, tokio}` set.

- Counts:
  - Crates: 11, from `Cargo.toml` `[workspace] members`. Unchanged.
  - Canonical types: 417, from `grep -rE "^(pub )?(struct|enum) "` across the full BLUE scope. Breakdown: `ade_codec` 10, `ade_types` 81, `ade_crypto` 13, `ade_core` 44, `ade_ledger` **152 (+13)**, `ade_plutus` 8, plus the 9 BLUE `ade_network` submodule paths 109. Registry `canonical_type_registry: null`, so a structural count is used. **+13 since `a280954`** — N-H receive-side closed types listed above.
  - Tests: 1551 — count of `#[test]` / `#[tokio::test]` attributes across `crates/`. Reported as approximate per the template's fallback rule. **+43 since `a280954`**, breakdown above.
  - CI checks: 52 — file count under `ci/ci_check_*.sh`. **+5 since `a280954`** — `ci_check_admitted_block_closure.sh`, `ci_check_receive_reducer_closure.sh`, `ci_check_receive_replay_purity.sh`, `ci_check_receive_orchestrator_no_producer_dep.sh`, `ci_check_receive_paths_corpus_present.sh` (all PHASE4-N-H). Full inventory at HEAD: `admitted_block_closure` *(NEW)*, `block_fetch_server_closure`, `broadcast_to_served_purity`, `cbor_round_trip`, `ce_n_a_5_proof`, `chaindb_contract`, `chaindb_crash_safety`, `chain_sync_server_closure`, `consensus_closed_enums`, `constitution_coverage`, `conway_cert_classification_closed`, `credential_discriminant_closed`, `crypto_vectors`, `dependency_boundary`, `deposit_param_authority`, `differential_divergence`, `forbidden_patterns`, `forge_purity`, `gov_cert_accumulation_closed`, `hash_uses_wire_bytes`, `hfc_translation`, `ingress_chokepoints`, `ledger_determinism`, `mempool_ingress_closure`, `mempool_ingress_replay`, `module_headers`, `n2n_server_no_signing_dep`, `no_async_in_blue`, `no_chaindb_in_consensus_blue`, `no_density_in_fork_choice`, `no_float_in_consensus`, `no_parallel_header_splitter`, `no_private_keys_in_corpus`, `no_producer_body_encoder`, `no_secrets`, `no_semantic_cfg`, `no_signing_in_blue`, `opcert_closed`, `pallas_quarantine`, `private_key_custody`, `producer_corpus_present`, `proposal_procedures_closed`, `receive_orchestrator_no_producer_dep` *(NEW)*, `receive_paths_corpus_present` *(NEW)*, `receive_reducer_closure` *(NEW)*, `receive_replay_purity` *(NEW)*, `recovery_contract`, `ref_provenance`, `scheduler_closure`, `self_accept_gate`, `served_chain_closure`, `server_paths_corpus_present`. The forward-looking `ci/ci_check_no_fail_open_in_validation.sh` (the `if len == K { check } else { skip }` grep gate, DC-VAL-06 / DC-TXV) is still **not** shipped. No `.github/workflows/` yet.

---

## BLUE Modules — Pure Functional Core

> **Shared header (applies to every BLUE entry below).** Every `.rs` source file begins with the contract banner
> `// Core Contract:` and the following deny attributes are present in each crate's `lib.rs` (or, for `ade_network`,
> at the crate root — the BLUE submodules inherit them):
> `#![deny(unsafe_code)]`, `#![deny(clippy::unwrap_used)]`, `#![deny(clippy::expect_used)]`,
> `#![deny(clippy::panic)]`, `#![deny(clippy::float_arithmetic)]`.
> CI scripts that enforce the shared rules (all 7 scope the full BLUE set — the 6 BLUE crates plus the 9 BLUE `ade_network` submodule paths declared in `.idd-config.json` `core_paths`):
> - `ci/ci_check_module_headers.sh` — banner first-line check.
> - `ci/ci_check_forbidden_patterns.sh` — `HashMap`, `HashSet`, `IndexMap`/`IndexSet`/`indexmap::`, `SystemTime`, `Instant`, `std::fs`, `std::net`, `tokio`, `async fn`, `f32`/`f64`, `anyhow`, `rand::thread_rng`, `thread::spawn`, plus `unsafe` outside a documented allowlist.
> - `ci/ci_check_dependency_boundary.sh` — no BLUE crate depends on a RED crate.
> - `ci/ci_check_no_signing_in_blue.sh` — `SigningKey`/`SecretKey`/`PrivateKey`/`private_key`/`sign_message`/`sign_block` forbidden in BLUE.
> - `ci/ci_check_no_semantic_cfg.sh` — `#[cfg(feature = ...)]` and `cfg!(feature = ...)` forbidden in BLUE.
> - `ci/ci_check_hash_uses_wire_bytes.sh` — no hashing of `canonical_bytes` / re-encoded bytes in BLUE.
> - `ci/ci_check_ingress_chokepoints.sh` — only named `decode_*` chokepoints construct `PreservedCbor`.
> - `ci/ci_check_pallas_quarantine.sh` — `pallas-*` references confined to `ade_plutus`.
> - `ci/ci_check_no_async_in_blue.sh` *(PHASE4-N-A, S-A1)* — async constructs forbidden anywhere in the BLUE scope. Enforces DC-CORE-01.
>
> Three additional CI scripts narrow the shared header to the `ade_core::consensus` tree (PHASE4-N-B): `ci/ci_check_no_chaindb_in_consensus_blue.sh`, `ci/ci_check_no_float_in_consensus.sh`, `ci/ci_check_consensus_closed_enums.sh` (TARGETS scope: `crates/ade_core/src/consensus/`, `crates/ade_ledger/src/block_validity/`, `crates/ade_ledger/src/tx_validity/`, `crates/ade_ledger/src/mempool/`).
> A fourth narrow check enforces a single fork-choice rule: `ci/ci_check_no_density_in_fork_choice.sh` (DC-CONS-03).
> A fifth narrow check (PHASE4-B3) enforces deposit-parameter authority: `ci/ci_check_deposit_param_authority.sh` (DC-TXV-07).
> A sixth narrow check (PHASE4-B5) enforces gov-cert accumulation closure: `ci/ci_check_gov_cert_accumulation_closed.sh` (DC-LEDGER-09).
> A seventh narrow check (OQ5/COMMITTEE/DREP/ENACTMENT-COMMITTEE-FIDELITY + ENACTMENT-COMMITTEE-WRITEBACK) enforces credential-discriminant closure: `ci/ci_check_credential_discriminant_closed.sh` (DC-LEDGER-10).
> An eighth narrow check (PROPOSAL-PROCEDURES-DECODE, PP-S1) enforces proposal_procedures closure: `ci/ci_check_proposal_procedures_closed.sh` (DC-LEDGER-11).
>
> **Eight narrow checks added by PHASE4-N-C (the producer-authority gate set):**
> - `ci/ci_check_forge_purity.sh` (DC-CONS-13/14/15) — forge body purity.
> - `ci/ci_check_no_producer_body_encoder.sh` (DC-CONS-16) — single body-hash authority, repo-wide.
> - `ci/ci_check_no_private_keys_in_corpus.sh` (DC-CRYPTO-03/04/05 corpus discipline) — producer + (N-G S5 extension) server-paths corpora private-key-free.
> - `ci/ci_check_opcert_closed.sh` (DC-CONS-11/12) — closed opcert encoders + acceptance authority.
> - `ci/ci_check_private_key_custody.sh` (DC-CRYPTO-03/04/05 + OP-OPS-04) — signing primitives RED-confined.
> - `ci/ci_check_producer_corpus_present.sh` (CN-CONS-06 mechanical half) — fixture-presence guard.
> - `ci/ci_check_scheduler_closure.sh` (DC-CONS-13 + DC-MEM-03 + OP-OPS-05) — RED scheduler purity + BroadcastQueue value-take.
> - `ci/ci_check_self_accept_gate.sh` (CN-CONS-07) — type-level broadcast-gate guards.
>
> **Seven narrow checks added by PHASE4-N-G (the producer-side server-role gate set):**
> - `ci/ci_check_no_parallel_header_splitter.sh` (DC-CONS-16 strengthening + DC-CONS-18, S1) — repo-wide grep gate: the only `pub fn .*header_bytes` site is `accepted_block_header_bytes` in `crates/ade_ledger/src/block_validity/header_input.rs`.
> - `ci/ci_check_served_chain_closure.sh` (CN-CONS-07 strengthening + CE-N-G-2, S2) — scopes `crates/ade_ledger/src/producer/served_chain.rs`.
> - `ci/ci_check_chain_sync_server_closure.sh` (DC-PROTO-08, S3) — scopes `crates/ade_network/src/chain_sync/server.rs`.
> - `ci/ci_check_block_fetch_server_closure.sh` (DC-CONS-17 enforcement foundation, S4) — scopes `crates/ade_network/src/block_fetch/server.rs`.
> - `ci/ci_check_broadcast_to_served_purity.sh` (CE-N-G-5, S5) — scopes the GREEN adapter pair under `ade_runtime::producer/`.
> - `ci/ci_check_n2n_server_no_signing_dep.sh` (S6 key-boundary doctrine) — scopes `crates/ade_runtime/src/network/`.
> - `ci/ci_check_server_paths_corpus_present.sh` (S7 presence) — checks the two integration tests + binary src + Cargo.toml entry + procedure doc.
>
> **NEW — Four narrow checks added by PHASE4-N-H (the receive-side authority gate set):**
> - `ci/ci_check_admitted_block_closure.sh` (S1; CE-N-H-1 + CN-PROTO-07) — repo-wide grep: `pub fn .* AdmittedBlock` only at `admitted.rs::admit_via_block_validity`; `pub struct AdmittedBlock` only in `admitted.rs`; no `pub use ... AdmittedBlock as ...` rename re-exports; `events.rs` MUST NOT reference `RequestNext`/`RequestRange`/`ClientDone`/`FindIntersect` (CN-PROTO-07 — locally-originated chain-sync/block-fetch messages unrepresentable as `ReceiveEvent` constructors).
> - `ci/ci_check_receive_reducer_closure.sh` (S2; CE-N-H-2 + CN-CONS-08 + DC-CONS-19) — scopes `crates/ade_ledger/src/receive/reducer.rs`: no `HashMap`/`HashSet`/wall-clock/`tokio`/`rand` in production; positive grep for `admit_via_block_validity` + `RollbackOutOfScope`; the `roll_forward` helper body MUST NOT assign to `state.ledger` or `state.chain_dep` (Invariant I-6 enforced by AWK-extracted function-body scan).
>
> **NEW — One narrow check added by PHASE4-N-H scoped on the GREEN sub-tree:**
> - `ci/ci_check_receive_replay_purity.sh` (S3; CE-N-H-3 + DC-PROTO-09) — scopes `crates/ade_runtime/src/receive/{events_to_state,in_memory_chain_write}.rs`: no `HashMap`/`HashSet`/wall-clock/`tokio`/`rand`; `events_to_state.rs` MUST NOT call `decode_block_envelope`/`decode_block`/`accepted_block_header_bytes` (pass-through only — the BLUE reducer is the sole decode site); positive presence of `lift_chain_sync_signal` + `lift_block_fetch_event` + `ChainDbWriter` `impl ChainDbWrite`.
>
> Two checks narrow the shared header to the wire-ingress path of `ade_ledger::mempool` (PHASE4-N-E):
> - `ci/ci_check_mempool_ingress_closure.sh` (DC-MEM-03, S1).
> - `ci/ci_check_mempool_ingress_replay.sh` (DC-MEM-04, S2 + S3).
>
> A BLUE crate or BLUE `ade_network` submodule that adds a feature flag, an async function, a `HashMap`, or a RED dep fails CI on push.
> The 3 RED-scope CI scripts (`ci_check_chaindb_contract.sh`, `ci_check_recovery_contract.sh`, `ci_check_chaindb_crash_safety.sh`) and the 1 evidence script `ci_check_ce_n_a_5_proof.sh` are not part of this shared header — they are documented in the cross-module CI matrix at the bottom.

---

### `ade_codec`

> **Status carried from prior CODEMAP — unchanged at HEAD `efe1fb9`.** PHASE4-N-H did not touch `ade_codec`. The N-G / N-C narrative carries forward.

| Attribute | Value |
|-----------|-------|
| **Purpose** | Owns Cardano-canonical CBOR ingress: the only place in the workspace that turns raw bytes into typed semantic values, with wire-byte preservation for every hash-bearing structure. Also owns the standalone opcert byte authority (`shelley::opcert::{encode_opcert, decode_opcert}`) and the canonical Conway-tx preserved-byte splitter (`shelley::tx_components::split_conway_tx_components` — reused by `ade_ledger::producer::forge` and the body-hash recomputation path). |
| **Creates** | `PreservedCbor<T>`, `RawCbor`, `BlockEnvelope`, `ByronDecodedBlock`, `CodecContext`, `CodecError` (incl. `UnknownCertTag`, `DuplicateMapKey`, `TrailingBytes`, `InvalidCborStructure`), `ContainerEncoding`, `IntWidth`, plus era-tagged block/tx wrappers under `byron/`, `shelley/`, `allegra/`, `mary/`, `alonzo/`, `babbage/`, `conway/`. Functions: `conway::cert::{decode_conway_certs, decode_drep}`, `conway::withdrawals::{decode_withdrawals, withdrawals_sum}`, `conway::governance::{decode_proposal_procedures, encode_proposal_procedures}`, `shelley::cert::read_pool_registration_cert`, both era `decode_stake_credential`, `shelley::opcert::{encode_opcert, decode_opcert, write_opcert_fields_into}`, `shelley::tx_components::split_conway_tx_components`. Types: `OpCertCodecError`, `TxComponents<'a>`. |
| **Interprets** | All canonical Cardano CBOR — block envelopes, era-specific blocks, tx bodies, tx outs, certificates, addresses. Conway certificate array (closed over CDDL tags 0..18, owner-complete) and Conway withdrawals map (deduplicated). Both era credential decoders preserve the key/script tag. Sole authority for `PreservedCbor::new` (constructor is `pub(crate)`). CIP-1694 `proposal_procedure` array. Standalone cardano-cli `OperationalCertificate` 4-tuple. Conway transaction 4-tuple (preserved-byte slicing only). |
| **MUST NOT** | (1) Construct `PreservedCbor` outside `ade_codec` (`pub(crate)` + `ci_check_ingress_chokepoints.sh`). (2) Re-encode wire bytes when computing hashes (`ci_check_hash_uses_wire_bytes.sh`). (3) Use any forbidden BLUE pattern. (4) Depend on any other workspace crate except `ade_types`. (5) `conway::cert` (DC-LEDGER-08) — no unknown-tag swallow; owner-complete; no catch-all. (6) `conway::withdrawals` — no last-wins on duplicate `RewardAccount`. (7) `decode_stake_credential` (DC-LEDGER-10) — must not erase the credential tag. (8) `conway::governance` (DC-LEDGER-11) — no silent skip on unknown `GovAction`; no opaque pass-through at body codec key 20. (9) `shelley::opcert` (DC-CONS-11/12) — cardano-byte-identical 4-tuple; dedicated `OpCertCodecError` variant per shape failure; not a second header-embedded opcert encoder (enforced by `ci_check_opcert_closed.sh`). (10) `shelley::tx_components` (DC-CONS-13/16) — preserved-byte slices that alias the input buffer (no `to_vec` / clones); reject non-4-tuple shapes; not re-encode the boolean validity flag or the auxiliary-data null. |
| **Inbound deps** | `ade_ledger` (heavy — the new `ade_ledger::receive::reducer` reuses `cbor::envelope::decode_block_envelope` indirectly via `block_validity::decode_block`; `ade_ledger::receive::in_memory_chain_write` test module imports `cbor::envelope::decode_block_envelope` directly), `ade_plutus`, `ade_testkit`, `ade_network`, `ade_runtime` (carries forward + new `receive` sub-tree's test scaffolding), `ade_core_interop`. No new inbound crate edge in PHASE4-N-H. |
| **Outbound deps** | `ade_types`. No external dependencies; std-only. Dev-deps: `serde_json`, `toml`. |
| **Entry points** | `ade_codec::cbor::envelope::decode_block_envelope`, `ade_codec::cbor`, `ade_codec::traits::AdeEncode`, `ade_codec::CodecContext`, per-era `decode_*_block`, `ade_codec::address::decode_address`. B2: `ade_codec::conway::tx::decode_conway_tx_body`. B3: `ade_codec::conway::cert::decode_conway_certs` and `ade_codec::conway::withdrawals::{decode_withdrawals, withdrawals_sum}`. PP-S1: `ade_codec::conway::governance::{decode_proposal_procedures, encode_proposal_procedures}`. N-C: `ade_codec::shelley::opcert::{encode_opcert, decode_opcert, OpCertCodecError}`, `ade_codec::shelley::tx_components::{split_conway_tx_components, TxComponents}`. N-G heavy use site: `decode_block_envelope` consumed by `cross_impl_server_pipeline.rs`. **NEW — PHASE4-N-H heavy use site:** the receive reducer's `block_delivered` helper interprets each peer-supplied body via `ade_ledger::block_validity::decode_block` (which composes `decode_block_envelope`) — the canonical single decode site on the receive path (DC-CONS-19 grounding). |
| **Key modules** | `cbor/`, `byron/`, `shelley/` (incl. `cert.rs`, `opcert.rs`, `tx_components.rs`), `allegra/`, `mary/`, `alonzo/`, `babbage/`, `conway/`, `address/`, `preserved.rs`, `traits.rs`, `primitives.rs`, `error.rs`. |

---

### `ade_core`

> **Status carried from prior CODEMAP — unchanged at HEAD `efe1fb9`.** PHASE4-N-H did not touch `ade_core`. The N-C-introduced `consensus::opcert_validate.rs` module carries forward unchanged; the `ade_core::consensus::{era_schedule, ledger_view, praos_state}` surface is consumed unchanged by the new `ade_ledger::receive` module.

| Attribute | Value |
|-----------|-------|
| **Purpose** | BLUE authoritative Praos consensus core. Owns canonical types and pure state-transitions that decide which header / chain Ade accepts: HFC era schedule + slot↔era↔time translation, Praos chain-dep state (nonce / op-cert counters), VRF cert verification + leader-eligibility predicate, KES signature + op-cert period verification wired into header admission, header validation pipeline, fork-choice, rollback authority, leader-schedule query, canonical encodings of all chain-dep state and chain events. Owns the producer-side opcert acceptance authority (`opcert_validate`). |
| **Creates** | **Schedule:** `BootstrapAnchorHash`, `EraSchedule`, `EraSummary`, `EraLocation`. **State:** `PraosChainDepState`, `OpCertCounterMap`, `Nonce`. **Events/points:** `Point`, `ChainHash`, `BlockDistance`, `SecurityParam`, `ChainEvent`, `ChainSelectionReject`. **Errors:** `HFCError`, `SlotTimeError`, `OutsideForecastRange`, `HeaderValidationError`, `FieldError`, `FieldKind`, `VrfCertError`, `OpCertCounterError`, `NonceEvolutionError`, `LeaderScheduleError`, `OpCertError`. **Header surface:** `HeaderInput`, `HeaderVrf`, `HeaderKes`, `ValidatedHeaderSummary`, `HeaderApplied`. **Fork-choice:** `TiebreakerView`, `CandidateFragment`, `ChainSelectorState`, `ForkChoiceError`. **Op-cert/nonce:** `OpCertObservation`, `NonceInput`. **VRF:** `VrfRole`, `VerifiedVrf`, `StakeFraction`, `ActiveSlotsCoeff`. **Leader schedule:** `LeaderScheduleQuery`, `LeaderScheduleAnswer`. **Ledger view boundary:** `LedgerView` trait. **Rollback:** `RollBackRequest`, `RollBackApplied`. **Encoding:** `DecodeError`. 44 public types. |
| **Interprets** | Canonical inputs from the `ade_runtime` shell — `HeaderInput` projections, `EraSchedule` materialized once from genesis JSON, `LedgerView` snapshots from `ade_ledger::consensus_view::PoolDistrView`, ordered `StreamInput` events. KES check verifies hot KES key signature over the header body CBOR bytes. `opcert_validate` consumes an `OperationalCert`, cold key, expected period, and prev counter. |
| **MUST NOT** | (1) Take or accept a `&ChainDb` reference (`ci_check_no_chaindb_in_consensus_blue.sh`). (2) Use any `f32`/`f64` (`ci_check_no_float_in_consensus.sh`). (3) Add `#[non_exhaustive]` to any consensus enum, open-tail `Other`/`Unknown`, owned `String` fields, or `Box<dyn ...>` (`ci_check_consensus_closed_enums.sh`). (4) Reference `density` in `fork_choice.rs`/`candidate.rs` outside a `// no-density:` annotation. (5) Read wall-clock. (6) Use `HashMap`/`HashSet`; `OpCertCounterMap` uses `BTreeMap`. (7) `async fn`, `.await`, `tokio`. (8) Construct a `PreservedCbor`. (9) Re-derive stake snapshots (DC-CONSENSUS-02). (10) Bypass the canonical `validate_and_apply_header` pipeline. (11) B1 fail-closed (DC-VAL-06). (12) B1 KES rule (DC-CRYPTO-01). (13) All shared-header BLUE rules. (14) `consensus::opcert_validate` (DC-CONS-11/12) — `OpCertError` closed; sub-check order pinned; `prev_counter == None` only first-opcert acceptance path; no external counter store. |
| **Inbound deps** | `ade_ledger` (B1 + N-C + **NEW heavy site in N-H: `ade_ledger::receive::admitted::admit_via_block_validity` takes `&EraSchedule` + `&dyn LedgerView` + `&PraosChainDepState` — composes `block_validity` which calls `validate_and_apply_header`**), `ade_runtime`, `ade_testkit`, `ade_core_interop`. No new inbound crate edge in PHASE4-N-H — the receive-side consumption rides on `ade_ledger → ade_core`. |
| **Outbound deps** | `ade_types`, `ade_crypto`. Dev-deps: `ade_testkit`, `serde_json`, `cardano-crypto`. |
| **Entry points** | `use ade_core::consensus::{...}` aggregator, `ade_core::consensus::ledger_view::LedgerView`, `ade_core::consensus::vrf_cert::{vrf_input, verify_praos_vrf, ...}`, `ade_core::consensus::kes_check::{verify_header_kes, expect_size}`, `ade_core::consensus::praos_state::*`, `ade_core::consensus::header_summary::*`. Top-level transitions: `validate_and_apply_header`, `select_best_chain`, `apply_rollback`, `apply_nonce_input`, `apply_op_cert`, `query_leader_schedule` + `is_leader_for_vrf_output`, `verify_vrf_cert` + `verify_praos_vrf` + `check_leader_claim` + `is_leader`, `tiebreaker_prefer`, `encode/decode_chain_dep_state`, `encode/decode_chain_event`. `ade_core::consensus::opcert_validate::{opcert_validate, OpCertError}`. |
| **Key modules** | `consensus/era_schedule.rs`, `consensus/praos_state.rs`, `consensus/events.rs`, `consensus/errors.rs`, `consensus/vrf_cert.rs`, `consensus/kes_check.rs`, `consensus/nonce.rs`, `consensus/op_cert.rs`, `consensus/leader_schedule.rs`, `consensus/header_summary.rs` + `consensus/header_validate.rs`, `consensus/candidate.rs`, `consensus/fork_choice.rs`, `consensus/rollback.rs`, `consensus/ledger_view.rs`, `consensus/encoding.rs`, `consensus/opcert_validate.rs`. |

---

### `ade_crypto`

> **Status carried from prior CODEMAP — unchanged at HEAD `efe1fb9`.** PHASE4-N-H did not touch `ade_crypto`.

| Attribute | Value |
|-----------|-------|
| **Purpose** | Owns pure cryptographic verification primitives — Blake2b hashing, Ed25519 / Byron-bootstrap signature verification, KES verification with operational certificates, VRF verification, plus the closed signature-artifact types (`KesSignature`, `VrfProof`, `VrfOutput`, `Ed25519Signature`) that BLUE consumes across the RED→BLUE boundary. Verification only — signing lives in `ade_runtime::producer::signing`. |
| **Creates** | `Blake2b224`, `Blake2b256`, `HashAlgorithm` trait, `Ed25519VerificationKey`, `Ed25519Signature`, `ByronExtendedVerificationKey`, `KesVerificationKey`, `KesPeriod`, `OperationalCertData`, `VrfVerificationKey`, `VrfProof`, `VrfOutput`, `CryptoError`, `KesSignature(pub [u8; SUM6_KES_SIG_LEN])`, `SUM6_KES_SIG_LEN: usize = 448`. |
| **Interprets** | Verification key / signature / proof byte structures. Not a CBOR parser — accepts already-decoded byte slices. |
| **MUST NOT** | (1) Implement signing (`ci_check_no_signing_in_blue.sh`). (2) Allocate global state. (3) Use any BLUE forbidden pattern. (4) Use `unsafe` outside the allowlisted FFI in `src/vrf.rs`. (5) `build_opcert_signable` must produce the spec-correct raw concatenation. (6) `KesSignature` (DC-CRYPTO-04) — closed length-pinned wrapper; only `from_bytes` construction; custom redacting `Debug`; no `PartialOrd`/`Ord`/`Hash` derives. (7) No `Drop` impl that zeroizes — `KesSignature` is BLUE and not secret. |
| **Inbound deps** | `ade_core`, `ade_ledger`, `ade_plutus`, `ade_testkit`, `ade_core_interop`, `ade_runtime`. No new inbound crate edge in PHASE4-N-H. |
| **Outbound deps** | `ade_types`, `blake2`, `ed25519-dalek`, `cardano-crypto` (vrf-draft03 + kes-sum + dsign features, `default-features = false`). |
| **Entry points** | `ade_crypto::blake2b::blake2b_256`, `blake2b_224`, `blake2b_256`, `block_header_hash`, `transaction_id`, `script_hash`, `credential_hash`, `verify_ed25519`, `verify_byron_bootstrap`, `verify_kes`, `verify_opcert`, `verify_vrf`, `ade_crypto::kes::{KesSignature, SUM6_KES_SIG_LEN}`. |

---

### `ade_ledger`

> **Status change at HEAD `efe1fb9` (PHASE4-N-H S1 + S2 — one new BLUE sub-tree under `receive/`).** Between `a280954` and HEAD: the new `ade_ledger::receive` sub-module (5 files, 1206 LOC + ~20 inline tests) hosts the BLUE receive-side authority: (a) `admitted.rs` defines `AdmittedBlock` (private inner `bytes: Vec<u8>` field; tuple-struct constructor module-private) and `admit_via_block_validity(block_bytes, &LedgerState, &PraosChainDepState, &EraSchedule, &dyn LedgerView) -> Result<AdmittedOutcome, BlockValidityError>` — the **single public constructor of `AdmittedBlock`**. The function wraps `block_validity::transition::block_validity` and returns `Ok(AdmittedOutcome { admitted, ledger, chain_dep })` only when verdict is `BlockValidityVerdict::Valid`. (b) `chain_write.rs` defines the narrow `ChainDbWrite` trait — `fn write_admitted(&mut self, block: AdmittedBlock) -> Result<(), ChainWriteError>` (BY VALUE) — so the admission gate is preserved across the BLUE→GREEN boundary; closed `ChainWriteError` + `ChainWriteErrorKind` sums. (c) `events.rs` defines the closed `ReceiveEvent` sum (`RollForward {slot, hash, header_bytes, tip}`, `RollBackward {target_point, tip}`, `BlockDelivered {block_bytes}` — exactly 3 variants; CN-PROTO-07 closed: locally-originated chain-sync/block-fetch variants — `RequestNext`/`RequestRange`/`ClientDone`/`FindIntersect`/`Done` — are not constructible here, enforced by `ci/ci_check_admitted_block_closure.sh`'s negative grep on `events.rs`), `ReceiveEffect` (`Admitted`/`Cached`/`RolledBack`/`NoOp { reason: NoOpReason }`), `ReceiveError` (`HeaderBodyMismatch`/`Validity(BlockValidityError)`/`RollbackOutOfScope { target_point }`/`ChainDb(ChainWriteError)`), `NoOpReason` (`HeaderAlreadyCached`), `TargetPoint`, `TipPoint`. (d) `pending_header_cache.rs` defines `PendingHeaderCache { entries: BTreeMap<(SlotNo, Hash32), Vec<u8>> }` with `insert` (idempotent on byte-identity; `ByteConflict` on byte-divergence), `get`, `contains`, `remove`, `evict_below(slot)`, `iter`. (e) `reducer.rs` defines `ReceiveState { ledger, chain_dep, pending_headers }` plus `receive_apply` + `receive_apply_sequence`. The reducer is **pure, total, deterministic**: `RollForward` mutates only `state.pending_headers` (Invariant I-6 — never touches `state.ledger`/`state.chain_dep`/`chain_write`, enforced by `ci/ci_check_receive_reducer_closure.sh`'s AWK-extracted helper-body scan); `BlockDelivered` decodes the body, header-cross-checks the cache at `(slot, block_hash)`, runs `admit_via_block_validity`, persists through `ChainDbWrite::write_admitted` (irreversible step first), then commits the new `(ledger, chain_dep)` atomically and evicts the consumed header — on any failure, state is unchanged; `RollBackward` returns `Err(ReceiveError::RollbackOutOfScope)` per Path A scope edge. **CN-CONS-07 strengthened in N-H:** `AdmittedBlock` (receive-side admission token) and `AcceptedBlock` (producer-side broadcast token) are deliberately disjoint types — cross-use is mechanically impossible (a wrapper cannot fabricate either from raw bytes). All prior B-track / N-E / PROPOSAL-PROCEDURES-DECODE / N-C / N-G narrative carries forward.

| Attribute | Value |
|-----------|-------|
| **Purpose** | The functional core (ledger half): stateless ledger rules for every Cardano era; the B1 top-level block-validity verdict; the B2 top-level transaction-validity verdict + mempool admission; the B3 full Conway value-conservation accounting; the B4 closed Conway cert-state accumulation; the B5 closed Conway governance-cert accumulation; the live committee-enactment write-back; the single BLUE chokepoint `mempool::ingress::mempool_ingress` from wire ingress into `admit`; the BLUE producer authority (`forge_block`, `self_accept`, `AcceptedBlock` broadcast token, `block_body_hash` recipe); the BLUE producer-side served-chain index (`producer::served_chain::{ServedChainSnapshot, served_chain_admit}`) and single canonical header-projection authority (`block_validity::header_input::accepted_block_header_bytes`). **NEW — PHASE4-N-H:** the BLUE receive-side header→body bridge authority (`receive/`): single admission token (`AdmittedBlock` constructed only via `admit_via_block_validity`), closed event/effect/error sums (`ReceiveEvent`/`ReceiveEffect`/`ReceiveError`), BTreeMap-backed pending-header cache (`PendingHeaderCache`), narrow `ChainDbWrite` trait, pure total deterministic reducer (`receive_apply` / `receive_apply_sequence`). The receive bridge admits peer-supplied bytes into ChainDb + LedgerState + PraosChainDepState **only** via `block_validity` (B1) producing an `AdmittedBlock` token — distinct from the producer-side `AcceptedBlock` broadcast token. Path A scope: admit-only; `RollBackward` returns `RollbackOutOfScope` (full rollback authority awaits a follow-on ledger-snapshot cluster). |
| **Creates** | Carry-forward: `LedgerState`, `EpochState`, `UTxOState`, `TxOut`, `Value`, `MultiAsset`, `AssetName`, `DelegationState`, `CertState`, `PoolParams`, `ConwayGovState`, `GovActionState`, `RatificationResult`, `EnactmentEffects`, `ProtocolParameters`, `BlockApplyResult`, `BlockVerdict`, `TxVerdict`, `ScriptPosture`, `EpochBoundaryResult`, `LedgerFingerprint`, `Rational`, `WitnessInfo`, layered `LedgerError`. **B1:** `PoolDistrView`, `PoolEntry`, `BlockValidityOutcome`, `DecodedBlock`, `BlockValidityVerdict`, `BlockRejectClass`, `BlockValidityError`, `FieldError`, `FieldKind`, `MissingInput`, `VerdictSurface`, `SurfaceDecodeError`. **B2:** `TxValidityVerdict`, `TxRejectClass`, `TxValidityError`, `TxValidityOutcome`, `TxVerdictSurface`, `TxSurfaceDecodeError`, `DecodedTx`, `VKeyWitnessRef`, `WitnessClosureError`, `WitnessField`, `SignerSource`, `RequiredSigners`, `RequiredSignerError`, `RequiredSignerField`, `ResolvedInputs`, `ResolvedOutput`, `MempoolState`, `AdmitOutcome` (BLUE); `OrderPolicy` (GREEN). **B3:** `ConwayOnlyDepositParams`, `ConwayDepositParams`, `ValidationEnvironmentError`, `UnsupportedStateDependentDepositAccounting`, `EraInvalidCertificateError`. **B4:** `ConwayCertAction`, `GovernanceOwner`, `GovernanceCertEffect`, `OwnerTaggedEffect`, `ConwayCertOutcome`, `ConwayCertEnv`. **B5:** `GovCertEnv`. **PHASE4-N-E:** `IngressSource`, `IngressEvent`, `PeerId`, `PeerSubmissionQueue`. **PHASE4-N-C:** `ProducerTick`, `ForgedBlock`, `ForgeError`, `ForgeEffects`, `AcceptedBlock`, `SelfAcceptError`. Functions: `producer::forge::forge_block`, `producer::self_accept::self_accept`, `block_body_hash::{block_body_hash, block_body_hash_from_buckets}`. **PHASE4-N-G:** `ServedChainSnapshot { blocks: BTreeMap<(SlotNo, Hash32), AcceptedBlock> }`, `ServedChainAdmitError`, function `served_chain_admit`; accessor `accepted_block_header_bytes` on `block_validity::header_input`. **NEW — PHASE4-N-H:** `AdmittedBlock { bytes: Vec<u8> /* private */ }` (in `receive::admitted`); `AdmittedOutcome { admitted, ledger, chain_dep }` (in `receive::admitted`); `ChainWriteError { SlotConflict {slot, hash} \| Underlying(ChainWriteErrorKind) }` + `ChainWriteErrorKind { Io \| InvalidOperation \| Other }` + trait `ChainDbWrite { fn write_admitted(&mut self, AdmittedBlock) -> Result<(), ChainWriteError> }` (in `receive::chain_write`); `TargetPoint { slot, hash }`, `TipPoint { slot, hash, block_no }`, `ReceiveEvent { RollForward / RollBackward / BlockDelivered }`, `ReceiveEffect { Admitted / Cached / RolledBack / NoOp }`, `NoOpReason { HeaderAlreadyCached }`, `ReceiveError { HeaderBodyMismatch / Validity / RollbackOutOfScope / ChainDb }` (in `receive::events`); `PendingHeaderCache { entries: BTreeMap<(SlotNo, Hash32), Vec<u8>> /* private */ }`, `PendingHeaderCacheError { ByteConflict {slot, hash} }` (in `receive::pending_header_cache`); `ReceiveState { ledger, chain_dep, pending_headers }`, functions `receive_apply` + `receive_apply_sequence` (in `receive::reducer`). |
| **Interprets** | Decoded canonical types from `ade_codec`. Hosts per-era validation composers and the late-era witness decoder. B1: `block_validity::header_input::decode_block`. B2: `tx_validity::phase1::decode_tx`. B3: `conway::check_conway_coin_conservation`. B4: `rules::accumulate_tx_certs`. B5: feeds owner-complete `ConwayCert` into `gov_cert::apply_conway_gov_cert`. PHASE4-N-E S1: `mempool_ingress` is a pure pass-through. PHASE4-N-C: `producer::forge::forge_block` interprets the canonical `ProducerTick` only; `producer::self_accept::self_accept` interprets the forged byte slice exactly once by handing it to `block_validity::transition::block_validity`. PHASE4-N-G: `producer::served_chain::served_chain_admit` interprets the `AcceptedBlock` bytes exactly once via `decode_block` to derive the `(slot, hash)` index key. **NEW — PHASE4-N-H:** `receive::admitted::admit_via_block_validity` interprets the peer-supplied `block_bytes` exactly once by handing them to `block_validity::transition::block_validity` (which itself composes `decode_block` + `validate_and_apply_header` + body-hash recomputation + tx-validity); on `Valid` it wraps the bytes in `AdmittedBlock` together with the evolved ledger + chain_dep. `receive::reducer::block_delivered` interprets the body via `decode_block` to extract `(slot, block_hash)` for the cache cross-check (DC-CONS-19) — the cross-check verifies a cached entry exists at the decoded key. `RollForward` arms NEVER interpret `header_bytes` — they pass through to the cache verbatim. |
| **MUST NOT** | (1) Perform I/O. (2) Use `HashMap`, floats, or any other BLUE-forbidden pattern. (3) Hash anything other than wire bytes. (4) Construct `PreservedCbor` outside named decoders. (5) Depend on `ade_runtime`. (6) Use signing primitives or `#[cfg(feature)]` gates. (7) Omit `// Core Contract:` banner. (8–34) B1/B2/B3/B4/B5/N-E/PP-S1/N-C/N-G carry-forward (see prior CODEMAP for detail). **(35) NEW — `receive::admitted` (CE-N-H-1 + CN-CONS-08 + CN-PROTO-07, S1):** `pub fn admit_via_block_validity` MUST remain the only `pub fn` whose signature contains `AdmittedBlock` workspace-wide — repo-wide grep gate `ci/ci_check_admitted_block_closure.sh` enforces this. `pub struct AdmittedBlock` MUST live only at `admitted.rs`. No `pub use ... AdmittedBlock as <alias>` rename re-exports anywhere. The `bytes: Vec<u8>` field MUST remain private — no struct-literal back-door. The struct MUST remain a distinct type from `producer::AcceptedBlock` (CN-CONS-07 strengthening — cross-use is mechanically impossible). **(36) NEW — `receive::events` (CN-PROTO-07, S1):** `ReceiveEvent` MUST remain closed at exactly 3 variants (`RollForward`/`RollBackward`/`BlockDelivered`); no `#[non_exhaustive]`. The file MUST NOT reference any of the locally-originated chain-sync/block-fetch variant names `RequestNext`/`RequestRange`/`ClientDone`/`FindIntersect` (`ci/ci_check_admitted_block_closure.sh` AWK-stripped-comment grep) — these are locally-originated requests the orchestrator emits, not events the reducer consumes. `ReceiveError` MUST remain closed and `String`-free (`Validity(BlockValidityError)` and `ChainDb(ChainWriteError)` carry typed errors). **(37) NEW — `receive::pending_header_cache` (DC-CONS-19 grounding, S1):** the cache MUST be `BTreeMap`-backed — no `HashMap`/`HashSet`. The `entries` field MUST remain private. `insert` MUST be idempotent on byte-identity at the same key and reject byte-divergence at an existing key with `ByteConflict`. `evict_below` MUST be deterministic (the orchestrator/operator supplies the cutoff slot as canonical input). **(38) NEW — `receive::chain_write` (DC-PROTO-09 + CN-CONS-08 grounding, S1):** the `ChainDbWrite` trait method MUST take `AdmittedBlock` BY VALUE (not bytes, not a Vec<u8>) — preserves the admission gate across the trait surface. `ChainWriteError` MUST remain closed and `String`-free (the `Underlying` variant carries a static `ChainWriteErrorKind` tag rather than a typed error to keep the trait BLUE-pure). **(39) NEW — `receive::reducer` (CE-N-H-2 + CN-CONS-08 + DC-CONS-19, S2):** production code MUST NOT use `HashMap`/`HashSet`/wall-clock/`tokio`/`rand` (`ci/ci_check_receive_reducer_closure.sh`). MUST positively call `admit_via_block_validity` — the single admission authority (same script). MUST surface `ReceiveError::RollbackOutOfScope` for the `RollBackward` arm (same script). The `roll_forward` helper body MUST NOT assign to `state.ledger` or `state.chain_dep` — Invariant I-6 (RollForward never mutates authoritative state; same script, AWK-extracted function-body scan). `BlockDelivered` MUST run the persistence step (`chain_write.write_admitted`) BEFORE committing `state.ledger`/`state.chain_dep` — the staged-then-committed shape ensures any persistence failure leaves the reducer's state unchanged (failure-atomicity property). |
| **Inbound deps** | `ade_testkit`, `ade_core_interop`, `ade_runtime`. PHASE4-N-G heavy sites (carried). **NEW heavy consumption sites in PHASE4-N-H** — `ade_runtime::receive::*` consume `ade_ledger::receive::{ReceiveEvent, ReceiveEffect, ReceiveError, ReceiveState, receive_apply, ChainDbWrite, ChainWriteError, ChainWriteErrorKind, AdmittedBlock, TargetPoint, TipPoint}`. `ade_runtime::receive::in_memory_chain_write` also consumes `ade_ledger::block_validity::decode_block` (to derive the `(slot, hash)` key for `StoredBlock` from the AdmittedBlock bytes). |
| **Outbound deps** | `ade_types`, `ade_crypto`, `ade_codec`, `ade_plutus`, `ade_core`, `minicbor`, `num-bigint`, `num-integer`, `num-traits`. Dev-dep: `ade_testkit`. No new outbound crate edge in PHASE4-N-H — the new `receive` module reuses the established `ade_ledger → {ade_core, ade_codec, ade_types}` edges (the reducer's `BlockDelivered` arm uses `block_validity::decode_block`, `block_validity::transition::block_validity`, `ade_core::consensus::{era_schedule::EraSchedule, ledger_view::LedgerView, praos_state::PraosChainDepState}`, and `ade_types::{Hash32, SlotNo}`). |
| **Entry points** | Carry-forward: `ade_ledger::rules::apply_block` (8 import sites), `apply_block_classified`, `apply_block_with_accounting`, `apply_block_with_verdicts`, `apply_conway_tx_to_utxo` (B2), `apply_epoch_boundary_full`, `ade_ledger::state::{LedgerState, EpochState}`, `ade_ledger::pparams::*`, `ade_ledger::utxo::UTxOState`, `ade_ledger::hfc::translate_era`, `ade_ledger::fingerprint::fingerprint`, `ade_ledger::governance::*`, `ade_ledger::delegation::*`, `ade_ledger::epoch::*`, `ade_ledger::witness::*`. B1: `ade_ledger::block_validity::*`, `ade_ledger::consensus_view::{PoolDistrView, PoolEntry}`. B2: `ade_ledger::tx_validity::{...}`, `ade_ledger::mempool::{admit, MempoolState, AdmitOutcome, ...}`. PHASE4-N-E: `ade_ledger::mempool::{mempool_ingress, IngressEvent, IngressSource, canonicalize_peer_streams, PeerId, PeerSubmissionQueue}`. PHASE4-N-C: `ade_ledger::producer::{forge::{forge_block, ForgeError, ForgeEffects, ForgedBlock}, self_accept::{self_accept, AcceptedBlock, SelfAcceptError}, state::ProducerTick}`; `ade_ledger::block_body_hash::{block_body_hash, block_body_hash_from_buckets}`. PHASE4-N-G: `ade_ledger::producer::{served_chain::{ServedChainSnapshot, ServedChainAdmitError, served_chain_admit}}`; `ade_ledger::block_validity::accepted_block_header_bytes`. **NEW — PHASE4-N-H:** `ade_ledger::receive::{admit_via_block_validity, AdmittedBlock, AdmittedOutcome, ChainDbWrite, ChainWriteError, ChainWriteErrorKind, ReceiveEvent, ReceiveEffect, ReceiveError, NoOpReason, TargetPoint, TipPoint, PendingHeaderCache, ReceiveState, receive_apply, receive_apply_sequence}`. Re-exported at the `ade_ledger::receive` module root via `pub use`. Consumed by `ade_runtime::receive::*` (the GREEN+RED counterpart sub-tree). |
| **Key modules** | `byron.rs`, `shelley.rs`, `mary.rs`, `alonzo.rs`, `babbage.rs`, `conway.rs`, `rules.rs`, `state.rs`, `utxo.rs`, `delegation.rs`, `epoch.rs`, `governance.rs`, `hfc.rs`, `late_era_validation.rs`, `phase.rs`, `plutus_eval.rs`, `pparams.rs`, `rational.rs`, `scripts.rs`, `value.rs`, `witness.rs`, `fingerprint.rs`, `error.rs`, `gov_cert.rs` (B5), `consensus_view.rs` (B1), `block_validity/` (B1), `consensus_input_extract.rs` (B1, **RED**), `tx_validity/` (B2), `mempool/` (B2 + N-E), `cert_classify.rs` (B3), `block_body_hash.rs` (N-C S4), `producer/` (N-C + N-G), **NEW — `receive/`** (PHASE4-N-H S1 + S2): `receive/mod.rs` (36 LOC barrel; re-exports the full receive surface), `receive/admitted.rs` (257 LOC + 3 inline tests — `AdmittedBlock`, `AdmittedOutcome`, `admit_via_block_validity`), `receive/chain_write.rs` (96 LOC + 1 inline test — `ChainDbWrite`, `ChainWriteError`, `ChainWriteErrorKind`), `receive/events.rs` (220 LOC + 3 inline tests — `ReceiveEvent`, `ReceiveEffect`, `ReceiveError`, `NoOpReason`, `TargetPoint`, `TipPoint`), `receive/pending_header_cache.rs` (169 LOC + 5 inline tests — `PendingHeaderCache`, `PendingHeaderCacheError`), `receive/reducer.rs` (628 LOC + 7 inline tests — `ReceiveState`, `receive_apply`, `receive_apply_sequence`). |

> **GREEN sub-classification (`ade_ledger::mempool::policy`, `ade_ledger::mempool::canonicalize`).** Carry-forward.

> **RED sub-classification (`ade_ledger::consensus_input_extract`).** Carry-forward.

> **`ade_ledger::producer::served_chain` BLUE classification (N-G).** Carry-forward.

> **NEW — `ade_ledger::receive` BLUE classification rests on four layers:** (a) the crate prefix is BLUE under `.idd-config.json` `core_paths`; (b) every file begins with the `// Core Contract:` banner (enforced by `ci_check_module_headers.sh`); (c) the narrow CI gate `ci_check_admitted_block_closure.sh` pins the single-construction-site shape of `AdmittedBlock` repo-wide and the closed-event-sum shape of `events.rs`; (d) the narrow CI gate `ci_check_receive_reducer_closure.sh` pins the reducer's BLUE shape mechanically (no `HashMap`/wall-clock/`tokio`/`rand`; positive grep for `admit_via_block_validity` + `RollbackOutOfScope`; `roll_forward` helper-body scan for Invariant I-6).

> **All prior closure narratives unchanged at HEAD.** B-track + N-E + PROPOSAL-PROCEDURES-DECODE + N-C + N-G all carry forward.

---

### `ade_network` *(BLUE submodules)*

> **Status carried from prior CODEMAP — unchanged at HEAD `efe1fb9`.** PHASE4-N-H did not touch any BLUE submodule of `ade_network`. The pre-existing PHASE4-N-A `chain_sync::signal::ForkChoiceSignal` and `block_fetch::event::BatchDeliveryEvent` types are consumed unchanged by the new GREEN adapter `ade_runtime::receive::events_to_state` (which lifts their variants into `ade_ledger::receive::ReceiveEvent`). The pre-existing `codec::chain_sync::ChainSyncMessage` and `codec::block_fetch::BlockFetchMessage` decoders are consumed unchanged by the new RED orchestrator `ade_runtime::receive::orchestrator` (which decodes inbound frames before lifting). The PHASE4-N-G `chain_sync::server` + `block_fetch::server` modules carry forward unchanged.

| Attribute | Value |
|-----------|-------|
| **Purpose** | Owns the Cardano Ouroboros mini-protocol authority — the closed wire grammar (CBOR codecs) and pure state machines for all 11 N2N + N2C mini-protocols, plus the BLUE Ouroboros mux frame primitive. Also owns the producer-side server-role reducer surface (PHASE4-N-G `chain_sync::server` + `block_fetch::server`) — pure, total, deterministic transitions composing the existing BLUE state machines for grammar validation with the producer-side decision logic, and the closed `ServerReply<M>` wrappers that make client-agency replies from the server pump unrepresentable. |
| **Creates** | **Wire grammar (`codec/`):** `HandshakeMessage`, `N2cHandshakeMessage`, `ChainSyncMessage`, `BlockFetchMessage`, `TxSubmission2Message`, `KeepAliveMessage`, `PeerSharingMessage`, `LocalChainSyncMessage`, `LocalTxSubmissionMessage`, `LocalStateQueryMessage`, `LocalTxMonitorMessage`; closed payload types; structured `CodecError` + `ProtocolKind`. **State machines:** one closed `*State` / `*Agency` / `*Output` / `*Error` quad per protocol. **Mux primitives (`mux/frame.rs`):** `MuxFrame`, `MuxHeader`, `MuxMode`, `MiniProtocolId`, `MuxError`. **PHASE4-N-G (`chain_sync/server.rs` + `block_fetch/server.rs`):** `ServerReply(ServerVariant)` closed wrappers, `HeaderProjection`, `ProducerChainSyncServerState`, `ProducerServerError`, `ServerStep`, trait `ServedHeaderLookup`; `ProducerBlockFetchServerState`, `ProducerBlockFetchServerError`, `BlockFetchServerStep`, trait `ServedRangeLookup`. Functions: `producer_chain_sync_serve`, `producer_chain_sync_advance_tip`, `producer_block_fetch_serve`. |
| **Interprets** | The on-wire CBOR of every Ouroboros mini-protocol message (closed grammar) and the 8-byte Ouroboros mux frame header. Block/header/tx-body bytes inside RollForward / Block / Tx are opaque pass-through (DC-PROTO-06); LSQ Query/Result payloads also opaque. PHASE4-N-G server reducers interpret already-decoded `ChainSyncMessage` / `BlockFetchMessage` values; they emit `ServerReply` values that the dispatcher subsequently encodes via the existing per-protocol `encode_*_message`. The header/block byte slices carried by server replies are NOT interpreted — they are sourced verbatim from the `ServedHeaderLookup` / `ServedRangeLookup` trait seams. |
| **MUST NOT** | (1) `async fn`, `.await`, `tokio`, `async_std`, `futures::`, async-runtime spawn, or async timers in any file under the 9 BLUE paths. (2) Decode block / header / tx-body bytes. (3) Construct a `PreservedCbor` outside `ade_codec`'s named chokepoints. (4) Add `#[non_exhaustive]` to any wire-message enum or introduce a generic `Codec<P>` trait. (5) Define a generic `Agency<P>` wrapper. (6) Read a selected version from ambient session state. (7) Redefine `TxId` / `SlotNo` / `Hash32` / `CardanoEra`. (8) Depend on `pallas-network`. (9) Hold `String` in any error variant. (10) Use any other BLUE forbidden pattern. (11–13) PHASE4-N-G server-role MUST NOTs (carry-forward). |
| **Inbound deps** | `ade_core_interop` (live consensus session binary, N-E S4/S5 bridges, S6 `live_tx_submission_session` binary, N-C S7 `live_block_production_session` binary, N-G S7 `live_block_fetch_session` binary, **NEW — N-H S6 `live_block_follow_session` binary — all RED → BLUE**). The non-dev edge `ade_runtime → ade_network` (introduced by PHASE4-N-G S5/S6) now also covers the PHASE4-N-H `ade_runtime::receive::*` modules — `receive::events_to_state` imports `chain_sync::signal::{ForkChoiceSignal, Point as CsPoint, Tip}` + `block_fetch::event::{BatchDeliveryEvent, Point as BfPoint}`; `receive::orchestrator` imports `codec::chain_sync::{decode_chain_sync_message, ChainSyncMessage, encode_chain_sync_message, Point, Tip}`, `codec::block_fetch::{decode_block_fetch_message, BlockFetchMessage, encode_block_fetch_message}`, `codec::version::{BlockFetchVersion, ChainSyncVersion}`, `codec::CodecError`. Legal RED → BLUE direction. Dev-deps `ade_network → {ade_ledger, ade_testkit, ade_crypto, ade_core}` (introduced by PHASE4-N-G) carry forward unchanged. |
| **Outbound deps** | `ade_types`, `ade_codec`. No external deps in the BLUE submodules. |
| **Entry points** | Codec entry points per protocol; `ade_network::codec::primitives::*`; `ade_network::codec::version::*`; `ade_network::codec::{CodecError, ProtocolKind}`. State-machine `*_transition` per protocol. Mux: `ade_network::mux::frame::{encode_frame, decode_frame, ...}`. PHASE4-N-G: `ade_network::chain_sync::server::*` + `ade_network::block_fetch::server::*`. **NEW heavy use sites in PHASE4-N-H:** the receive-side glue chain `ade_runtime::receive::orchestrator → ade_runtime::receive::events_to_state → ade_ledger::receive::receive_apply` consumes (a) `codec::chain_sync::decode_chain_sync_message` + `chain_sync::signal::ForkChoiceSignal` for chain-sync inbound frames; (b) `codec::block_fetch::decode_block_fetch_message` + `block_fetch::event::BatchDeliveryEvent` for block-fetch inbound frames; (c) `codec::version::{ChainSyncVersion, BlockFetchVersion}` for handshake-negotiated version threading in `PerPeerReceiveState`. |
| **Key modules** | `codec/` (11 protocols + `primitives.rs` + `version.rs` + `error.rs`); `handshake/`, `chain_sync/` (incl. PHASE4-N-G `server.rs`), `block_fetch/` (incl. PHASE4-N-G `server.rs`), `tx_submission/`, `keep_alive/`, `peer_sharing/`; `n2c/`; `mux/frame.rs`. |

---

### `ade_plutus`

> **Status carried from prior CODEMAP — unchanged at HEAD `efe1fb9`.** PHASE4-N-H did not touch `ade_plutus`.

| Attribute | Value |
|-----------|-------|
| **Purpose** | Quarantine boundary between the Ade-canonical ledger and the ported UPLC evaluator from `aiken-lang/aiken` (pinned to tag `v1.1.21`). Exposes Ade-canonical types only; aiken- and pallas-originated types are strictly internal. |
| **Creates** | `PlutusScript`, `PlutusLanguage`, `EvalOutput`, `PlutusError`, `CostModels`, `DecoderMode`, `PerScriptResult`, `TxEvalResult`. |
| **Interprets** | UPLC scripts (Plutus V1/V2/V3) and `CostModels` CBOR. Phase-two transaction evaluation. `PlutusScript::from_cbor` is a named ingress chokepoint. |
| **MUST NOT** | (1) Re-export any `pallas_*` or `aiken_uplc::` type. (2) Allow another BLUE crate to bypass the canonical entry. (3) Activate PV11 builtins. (4) Use any BLUE-forbidden pattern. (5) Construct `PreservedCbor` outside `ade_codec`. |
| **Inbound deps** | `ade_ledger`, `ade_testkit`. |
| **Outbound deps** | `ade_types`, `ade_crypto`, `ade_codec`, `aiken_uplc` (git, tag `v1.1.21`), `pallas-primitives` (internal-only). |
| **Entry points** | `ade_plutus::eval_tx_phase_two`, `ade_plutus::tx_eval::*`, `ade_plutus::evaluator::*`, `ade_plutus::cost_model::*`. |
| **Key modules** | `evaluator.rs`, `cost_model.rs`, `script_context.rs`, `script_verdict.rs`, `tx_eval.rs`. |

---

### `ade_types`

> **Status carried from prior CODEMAP — unchanged at HEAD `efe1fb9`.** PHASE4-N-H did not add any canonical type to `ade_types` — the new closed canonical types all live in `ade_ledger::receive`'s authoritative sub-tree. `ade_types::{Hash32, SlotNo}` continue as the lingua franca; the receive event/effect/error types embed them in their fields. |

| Attribute | Value |
|-----------|-------|
| **Purpose** | Owns the canonical Cardano domain vocabulary — primitives, era enum, address forms, era-tagged transaction bodies / outputs / certificates, governance types — used by every other workspace crate as the lingua franca. The schema, separated from the codec. |
| **Creates** | `CardanoEra`, `SlotNo`, `BlockNo`, `EpochNo`, `Hash28`, `Hash32`, `Coin`, `Lovelace`, `NetworkId`, `Nonce`, `TxIn`, `RewardAccount`, `PoolId`, `Address`, `ByronAddress`, `Credential`, `StakeCredential`, `Certificate`, `PoolRegistrationCert`, `ConwayCert`, `CertDisposition` / `DepositEffect` / `CoinSource`, `MIRCert`, `MIRPot`, `DRep`, `GovAction`, `GovActionState`, `GovActionId`, `Anchor`, `ProposalProcedure`, `OperationalCert`, `NativeScript`, `PlutusV1Script`, `Datum`, `DatumOption`, `MultiAsset`, `AssetName`, `CostModel`, `ExUnits`, plus per-era tx-body / tx-out / witness wrappers. |
| **Interprets** | None — produce-only. Domain types are constructed by `ade_codec` decoders or `ade_ledger` rules. |
| **MUST NOT** | (1) Construct or decode `PreservedCbor`. (2) Use any BLUE-forbidden pattern. (3) Depend on any workspace crate. (4) Add open/extensible variants to closed enums without a versioned gate. |
| **Inbound deps** | `ade_codec`, `ade_crypto`, `ade_ledger`, `ade_plutus`, `ade_runtime`, `ade_testkit`, `ade_network`, `ade_core`, `ade_core_interop`. |
| **Outbound deps** | None. |
| **Entry points** | `ade_types::CardanoEra`, `ade_types::tx::{Coin, TxIn, RewardAccount}`, `ade_types::{Hash32, SlotNo, Hash28}`, `ade_types::conway::tx::ConwayTxBody`, `ade_types::conway::cert::*`, `ade_types::conway::governance::{ProposalProcedure, Anchor, GovAction, GovActionId}`, `ade_types::shelley::block::{OperationalCert, ProtocolVersion, ShelleyHeader, ShelleyBlock, VrfData}`. |
| **Key modules** | `primitives.rs`, `era.rs`, `tx.rs`, `address/`, `byron/`, `shelley/`, `allegra/`, `mary/`, `alonzo/`, `babbage/`, `conway/`. |

---

## GREEN Modules — Deterministic Glue

> Deterministic, non-authoritative. May depend on BLUE; must not affect authoritative outputs.

### `ade_testkit`

> **Status carried from prior CODEMAP — unchanged at HEAD `efe1fb9`.** PHASE4-N-H did NOT add the `receive/` sub-module the cluster doc named at planning time. The N-H GREEN test surface ended up under `ade_runtime/tests/*.rs` (mirroring the N-G precedent — orphan-rule + dev-dep direction made `ade_runtime::tests/` the natural host since the integration tests exercise the full RED dispatcher → GREEN adapter → BLUE reducer pipeline). The pre-existing `ade_testkit::validity::ConwayValidityCorpus` is the corpus the new tests in `ade_runtime/tests/{receive_pipeline_corpus_drive,receive_session_transcript_replay,receive_two_peer_independence}.rs` build their `ReceiveState`/`AdmittedBlock` over.

| Attribute | Value |
|-----------|-------|
| **Purpose** | Test infrastructure: differential harness, oracle snapshot loader, regression corpus, genesis loader, era mapping, transcript replay, diff reporting; N-B consensus harness; B1 block-validity harness (`validity/`); B2 transaction-validity harness (`tx_validity/`); N-E S2 mempool-ingress replay harness (`mempool/`); PP-S2 canonical synthetic `proposal_procedures` corpus + replay harness (`governance/`); PHASE4-N-C producer test harness (`producer/`). |
| **Creates** | Carry-forward: `Era`, `OracleManifest`, `LoadedSnapshot`, `RegressionCorpus`, `ComparisonSurface`, `DifferentialReport`, `DiffReport`, `Divergence`, `LedgerDiffReport`, `ProtocolDiffReport`, `BlockFields`, `ExpectedVerdict`, `HarnessError`, `Transcript`, `StateHash`, `SnapshotHeader`. **Consensus (N-B):** `LedgerViewStub`, `EpochStakeFixture`, `PoolFixture`, `ReplayStep`, `ReplayResult`. **Validity (B1):** `ConwayValidityCorpus` + `CorpusLoadError` (now also the corpus the N-H integration tests build their `ReceiveState`/`AdmittedBlock` from). **B2 (`tx_validity/`):** `ExtractedTx`, `TxReplay`, `ValidCase`, `WitnessMutation`. **N-E S2 (`mempool/`):** `BTrackCase`, `ExpectedOutcome`. **PP-S2 (`governance/`):** `CorpusEntry`, `ReplayOutcome`. **PHASE4-N-C (`producer/`):** `VrfReferenceVector`, `KesReferenceVector`, `ProducerReplayFixture`, `ExpectedErr`. |
| **Interprets** | Oracle reference snapshots, the regression-corpus manifest, the N-B consensus corpus, the B1 validity corpus, on-wire Conway transactions inside committed Conway-576 corpus blocks. The PHASE4-N-C `producer/` sub-tree interprets nothing at the wire layer — fixtures are baked-in signed-artifact constants. |
| **MUST NOT** | (1) Affect authoritative outputs. (2) Be linked from any BLUE crate. (3) Introduce nondeterminism that leaks into stored fixtures. (4–16) Carry-forward (see prior CODEMAP). |
| **Inbound deps** | None at compile time; consumption is via integration tests and dev-dep links from `ade_core`, `ade_runtime`, `ade_ledger`. `ade_core_interop` is a non-dev consumer. Dev-dep edge `ade_network → ade_testkit` (PHASE4-N-G) carries forward. **NEW dev-dep consumption sites in PHASE4-N-H:** the receive-side inline tests in `ade_ledger::receive::{admitted, reducer}` use `ade_testkit::validity::ConwayValidityCorpus` directly (dev-dep `ade_ledger → ade_testkit` was already in place); the integration tests under `ade_runtime/tests/receive_*.rs` use the same corpus via the existing dev-dep `ade_runtime → ade_testkit`. |
| **Outbound deps** | `ade_types`, `ade_codec`, `ade_crypto`, `ade_core`, `ade_ledger`, `ade_plutus`, `ade_runtime`, `blake2`, `flate2`, `tar`, `serde`, `serde_json`, `toml`, `cardano-crypto` (dev-dep, N-C). |
| **Entry points** | `ade_testkit::harness::*`; N-B `consensus::*`; B1 `validity::*` (incl. `validity::ConwayValidityCorpus::load` — now also N-H's primary corpus source); B2 `tx_validity::*`. N-E S2: `ade_testkit::mempool::*`. PP-S2: `ade_testkit::governance::*`. PHASE4-N-C: `ade_testkit::producer::*`. |
| **Key modules** | `harness/`, `consensus/`, `validity/`, `tx_validity/`, `mempool/`, `governance/`, `producer/` (N-C). **No new module added by N-H** — the planned `receive/` sub-tree was not materialized; the equivalent integration tests live at `crates/ade_runtime/tests/{receive_pipeline_corpus_drive,receive_session_transcript_replay,receive_two_peer_independence}.rs`. |

> **Classification note carried forward.** `ade_testkit` reads files from disk in test helpers and drives BLUE authorities from corpus inputs.

> **Gap surfaced (NEW — PHASE4-N-H, same shape as N-G's).** The cluster plan implicitly anticipated a `ade_testkit::receive::*` sub-tree (mirroring `producer/` + `validity/`); the shipped artifacts live one layer over, under `ade_runtime/tests/`. The CI gate `ci/ci_check_receive_paths_corpus_present.sh` was authored to check the actual shipped locations (in `ade_runtime/tests/` + `ade_core_interop/src/bin/`). The deviation is documented here so future work does not re-introduce a duplicate harness under `ade_testkit`. Whether `ade_testkit::receive::*` should be added as a thin re-export shim for cross-crate consumption (e.g., a future `ade_node` integration test) is a follow-on judgment call — at HEAD there is no consumer that would benefit.

---

### `ade_runtime::producer::{broadcast_to_served, served_chain_lookups}` *(GREEN — PHASE4-N-G S5)*

> **Status carried from prior CODEMAP — unchanged at HEAD `efe1fb9`.** PHASE4-N-H did not touch this GREEN sub-tree. Carry-forward.

| Attribute | Value |
|-----------|-------|
| **Purpose** | Bridges PHASE4-N-C's `BroadcastQueue` (RED) and PHASE4-N-G's `ServedChainSnapshot` (BLUE) to the `ServedHeaderLookup` / `ServedRangeLookup` trait seams the BLUE server reducers in `ade_network::{chain_sync, block_fetch}::server` consume. Pure, no I/O, no clock — observably deterministic over captured arrival sequences. |
| **Creates** | `ServedChainLookups<'a> { snap: &'a ServedChainSnapshot }`; function `drain_and_admit(snap, queue) -> Result<(ServedChainSnapshot, BroadcastQueue, Vec<AcceptedBlock>), ServedChainAdmitError>`. |
| **MUST NOT** | Carry-forward (see prior CODEMAP). |
| **Inbound deps** | `ade_runtime::network::n2n_server`; the integration tests under `ade_runtime/tests/`. |
| **Outbound deps** | `ade_ledger::{producer, block_validity}`, `ade_network::{chain_sync::server, block_fetch::server, codec::chain_sync}`, `ade_types`. |
| **Entry points** | `ade_runtime::producer::broadcast_to_served::drain_and_admit`, `ade_runtime::producer::served_chain_lookups::ServedChainLookups`. |

---

### `ade_runtime::receive::{events_to_state, in_memory_chain_write}` *(GREEN — PHASE4-N-H S3, NEW)*

> **Status: NEW at HEAD `efe1fb9`.** This is a GREEN sub-pair physically inside the RED `ade_runtime` crate prefix. The shared crate prefix is RED per `.idd-config.json` `_core_paths_doc`; the GREEN sub-classification of these two specific files is **by file** — `// Core Contract:` banner present, file-header docstring naming the GREEN classification, and CI gate `ci/ci_check_receive_replay_purity.sh` pinning the pure-fn / pass-through shape mechanically. Documented as a sub-section of `ade_runtime` in the RED block below; surfaced here for the GREEN inventory.

| Attribute | Value |
|-----------|-------|
| **Purpose** | Bridges PHASE4-N-A's `ade_network::chain_sync::signal::ForkChoiceSignal` and `ade_network::block_fetch::event::BatchDeliveryEvent` to the BLUE `ade_ledger::receive::ReceiveEvent` stream, and wires the BLUE `ChainDbWrite` trait to any `ade_runtime::chaindb::ChainDb` impl. Pure, no I/O, no clock — pass-through discipline mechanically enforced: `events_to_state` NEVER decodes header_bytes or block_bytes (the BLUE reducer's `BlockDelivered` branch is the canonical decode site). |
| **Creates** | Functions `lift_chain_sync_signal(ForkChoiceSignal) -> Option<ReceiveEvent>` + `lift_block_fetch_event(BatchDeliveryEvent) -> Option<ReceiveEvent>` (in `events_to_state`; non-state-changing variants — BatchStarted, NoBlocks, BatchCompleted, Intersected, NoIntersection, RollForward over `Point::Origin` — return `None`). Struct `ChainDbWriter<'a, D: ChainDb> { pub db: &'a D }` with `impl<'a, D: ChainDb> ChainDbWrite for ChainDbWriter<'a, D>` (in `in_memory_chain_write`) — `write_admitted` decodes the AdmittedBlock bytes once via `ade_ledger::block_validity::decode_block` to derive the `(slot, hash)` key for `StoredBlock`, then calls `ChainDb::put_block`. Closed mapping `ChainDbError → ChainWriteError` (private helper `map_chaindb_err`). |
| **MUST NOT** | (1) Affect authoritative outputs — these are deterministic projections + a thin write adapter. (2) Introduce nondeterminism — no `tokio`, no `SystemTime`/`Instant`, no `rand`, no `HashMap` (`ci/ci_check_receive_replay_purity.sh`). (3) `events_to_state` MUST NOT decode header_bytes or block_bytes — pass-through only; positive negative grep in the same script for `decode_block_envelope`, `decode_block`, `accepted_block_header_bytes`. (4) `in_memory_chain_write` MAY decode the AdmittedBlock bytes (to derive the `(slot, hash)` key for `StoredBlock`) — the decode is safe because the AdmittedBlock invariant guarantees its bytes already passed `block_validity` (which includes `decode_block` as step 1); a decode failure here would mean the AdmittedBlock invariant was violated and the adapter surfaces a closed `ChainWriteError::Underlying(Other)`. (5) MUST source `(slot, hash)` from the decoded body — no caller-supplied "asserted hash" parameter; same single-argument shape as `served_chain_admit`. (6) MUST NOT cache or memoize across calls — `ChainDbWriter<'_>` is a transient view; the snapshot's lifetime outlives every `write_admitted` invocation. (7) Pass-through discipline: the GREEN adapter MUST NOT enrich the lifted event with synthesized fields — `tip` and `header_bytes` carry through verbatim from the N-A signal source. |
| **Inbound deps** | `ade_runtime::receive::orchestrator` (constructs `ChainDbWriter` per session; calls `lift_chain_sync_signal` / `lift_block_fetch_event` via `dispatch_*_inbound`); the integration tests under `ade_runtime/tests/receive_*.rs`. |
| **Outbound deps** | `ade_ledger::receive::{ReceiveEvent, TargetPoint, TipPoint, AdmittedBlock, ChainDbWrite, ChainWriteError, ChainWriteErrorKind}`, `ade_ledger::block_validity::decode_block`, `ade_network::{chain_sync::signal::*, block_fetch::event::*}`, `ade_types::{Hash32, SlotNo}`, `crate::chaindb::{ChainDb, ChainDbError, StoredBlock}`. **Crate-level edges already established** by PHASE4-N-G's `ade_runtime → ade_network` addition and the pre-existing `ade_runtime → ade_ledger`. |
| **Entry points** | `ade_runtime::receive::events_to_state::{lift_chain_sync_signal, lift_block_fetch_event}`, `ade_runtime::receive::in_memory_chain_write::ChainDbWriter`. |

> **GREEN/RED partition risk note.** The GREEN sub-classification of `events_to_state` and `in_memory_chain_write` rests on (a) module-header convention, (b) the pure-fn + pass-through shape gate `ci_check_receive_replay_purity.sh`, and (c) the negative-grep enforcement of the no-decode rule on `events_to_state`. The broader BLUE/GREEN/RED partition inside the `ade_runtime` crate prefix remains "by file" — same risk class as `ade_runtime::consensus::candidate_fragment` (GREEN sub-classification, no dedicated CI script for the broad partition) and the N-G `producer::broadcast_to_served` / `producer::served_chain_lookups` pair. This is a **carried gap** of the same shape.

---

## RED Modules — Imperative Shell

> I/O, network, storage, clocks, retries. May depend on BLUE/GREEN. Must not modify core state directly or construct semantic types unsafely.

### `ade_core_interop` *(PHASE4-N-B S-B10; PHASE4-N-E S4/S5/S6; PHASE4-N-C S7; PHASE4-N-G S7; **PHASE4-N-H S6**)*

> **Status change at HEAD `efe1fb9` (PHASE4-N-H S6 — new RED operator-action live binary `live_block_follow_session`).** Between `a280954` and HEAD, `ade_core_interop` gained the new binary `src/bin/live_block_follow_session.rs` (141 LOC) — the CE-N-H-6 evidence-capture pass. The binary is RED by its own header comment and matches the established `live_consensus_session.rs` + `live_tx_submission_session.rs` + `live_block_production_session.rs` + `live_block_fetch_session.rs` pattern. Default hermetic `main` prints readiness + exits; `--connect` is the explicit operator-action flag. **Honest scope at HEAD:** the actual tokio socket wiring from `ade_runtime::receive::orchestrator` to the wire is stubbed pending operator-action — the binary in `--connect` mode prints the wiring stub plan and exits without opening sockets. The `--connect` pass is therefore not yet a runnable evidence capture; the `blocked_until_operator_peer_available` flag on `RO-LIVE-02` reflects this. All prior N-B follow / N-E S4/S5/S6 / N-C S7 / N-G S7 narrative carries forward. A new integration test `crates/ade_core_interop/tests/live_block_follow_session.rs` (1 test, hermetic-mode smoke check) was added.

| Attribute | Value |
|-----------|-------|
| **Purpose** | Live cardano-node interop driver. Hosts (a) the N-B consensus orchestrator readiness probe `fresh_orchestrator(k, initial_nonce)` and its `live_consensus_session` binary; (b) the N-E S4 GREEN adapter + `ingest_n2n_events`; (c) the N-E S5 GREEN adapter + `ingest_n2c_events`; (d) the N-E S6 `live_tx_submission_session` RED binary; (e) the N-C S7 `live_block_production_session` RED binary; (f) the N-G S7 `live_block_fetch_session` RED binary; **(g) NEW — the N-H S6 `live_block_follow_session` RED binary that performs the CE-N-H-6 evidence-capture pass — an Ade follower fed real RollForward + BlockDelivered from a private cardano-node peer over a captured follow window, drives the chain-sync client + block-fetch client through the receive orchestrator (S4), and logs one JSONL record per admitted block with peer tip + Ade tip + agreement verdict**. The crate is RED per `Cargo.toml` and the cluster TCB Color Maps; the S4/S5 sub-modules carry a GREEN sub-classification by their own module doc-comments. The five `live_*_session` binaries are RED by their own header comments. |
| **Creates** | `fresh_orchestrator` (N-B). N-E S4 sub-module: `PeerAccumulator` (GREEN). N-E S5 sub-module: `ClientAccumulator` (GREEN). Functions: `event_to_ingress` + `ingest_n2n_events` (S4); `local_event_to_ingress` + `ingest_n2c_events` (S5). N-E S6, N-C S7, N-G S7, **NEW — N-H S6**: no public Rust types — all five binaries are RED glue over the established BLUE/GREEN/RED surfaces. |
| **Interprets** | Live N2N byte streams from a real cardano-node peer (N-B + N-E + N-C + N-G + **NEW — N-H**). S4/S5 sub-modules interpret nothing at the bytes level. **NEW — N-H S6 `live_block_follow_session`:** consumes a real cardano-node N2N peer (operator-supplied target endpoint + handshake params), drives the receive orchestrator's `dispatch_chain_sync_inbound` + `dispatch_block_fetch_inbound` against the peer's RollForward + BlockDelivered stream, logs one JSON-Lines record per admitted block with peer tip + Ade tip + agreement verdict. Writes the log to `docs/clusters/PHASE4-N-H/CE-N-H-LIVE_<date>.log`. At HEAD the tokio socket wiring is stubbed (honest-scope) pending operator-action; the binary prints the wiring stub plan in `--connect` mode and exits. |
| **MUST NOT** | (1) Contain authoritative state-transition logic. (2) Be depended on by any other workspace crate. (3) Run by default in CI — closure-gate tests are `#[ignore]`-gated (CE-N-B-6, CE-N-E-6, CE-N-C-8, CE-N-G-8, **NEW — CE-N-H-6**). (4) Bypass `ade_codec`'s `PreservedCbor` chokepoints. (5) Read versions out of ambient context (DC-PROTO-06). (6) Vendor cardano-node mainnet genesis blobs. (7) N-E S4/S5 — must not branch on `IngressSource`, must not re-encode `tx_bytes`, must not bypass `mempool_ingress`. (8) N-E S6 binary — no direct `admit(`, no shadowing of `IngressSource`/`IngressEvent`/`PeerId`, no parallel codecs/transitions. (9) N-C S7 binary — must NOT bypass `forge_block` / `self_accept`; must NOT load private-key material into any BLUE call path; must NOT generate keys (OP-OPS-04); must NOT write a synthetic `CE-N-C-LIVE_<date>.log`. (10) N-G S7 binary — must NOT serve bytes that were not produced by `self_accept`, must NOT re-encode block bytes, must NOT project header bytes via any path other than `accepted_block_header_bytes`. **(11) NEW — N-H S6 `live_block_follow_session` binary (RED, RO-LIVE-02):** must NOT admit bytes that were not produced by the cross-impl `block_validity` path — the binary drives `ade_runtime::receive::orchestrator::dispatch_*_inbound`, which in turn calls `receive_apply` → `admit_via_block_validity` → `block_validity`; the admission gate cannot be bypassed (`AdmittedBlock`'s constructor is the only path). Must NOT write the cached header bytes anywhere — `events_to_state.rs` is pass-through; the cache is in-memory BLUE state inside `ReceiveState`. Must NOT introduce a parallel ChainDb writer — the GREEN `ChainDbWriter<'a, D: ChainDb>` is the only adapter for `ChainDbWrite` (`StoredBlock` key derivation lives there exclusively). Must NOT generate or hold any signing-key material — the binary follows only, never produces (key-boundary doctrine; the receive orchestrator is mechanically forbidden from importing `producer::signing` by `ci/ci_check_receive_orchestrator_no_producer_dep.sh`). Must NOT write a `CE-N-H-LIVE_<date>.log` with synthetic / fabricated entries — the log MUST reflect an actual run against a private cardano-node peer. The default `main` MUST stay hermetic (prints readiness and exits); `--connect` is the explicit operator-action flag. The `#[ignore]`-gated build-and-start test stays offline. At HEAD the binary's `--connect` path is honest-scope stubbed (tokio socket wiring deferred to operator-action work); MUST NOT claim live evidence has been captured until the operator runs the binary and commits the resulting log. |
| **Inbound deps** | None. |
| **Outbound deps** | `ade_core`, `ade_codec`, `ade_crypto`, `ade_ledger`, `ade_runtime` (**NEW heavy use sites in PHASE4-N-H** — the S6 binary's planned `--connect` wiring will import `ade_runtime::receive::orchestrator::*` + `ade_runtime::receive::{events_to_state, in_memory_chain_write}` + `ade_runtime::chaindb`; at HEAD the binary's actual code imports `std::env` + `std::path::PathBuf` only since the wiring is stubbed), `ade_network`, `ade_testkit`, `ade_types`, `tokio`. Total Cargo deps: 8 workspace crates + `tokio`. No new crate-level edges in N-H — the S6 binary reuses the already-established `ade_core_interop → ade_runtime` and `ade_core_interop → ade_network` edges. |
| **Entry points** | `ade_core_interop::fresh_orchestrator(k, initial_nonce)`, binary `live_consensus_session` (CE-N-B-6 evidence). N-E S4: `ade_core_interop::tx_submission::*`. N-E S5: `ade_core_interop::local_tx_submission::*`. N-E S6 binary: `live_tx_submission_session` (CE-N-E-6). N-C S7 binary: `live_block_production_session` (CE-N-C-8). N-G S7 binary: `live_block_fetch_session` (CE-N-G-8). **NEW — N-H S6 binary: `live_block_follow_session` (CE-N-H-6 evidence — conditional on operator-provided private cardano-node peer; honest-scope stubbed at HEAD).** No new public Rust API; the binary entry point is `fn main()`. |
| **Key modules** | `src/lib.rs`, `src/follow.rs` (N-B), `src/tx_submission.rs` (N-E S4), `src/local_tx_submission.rs` (N-E S5), `src/bin/live_consensus_session.rs`, `src/bin/live_tx_submission_session.rs` (N-E S6), `src/bin/live_block_production_session.rs` (N-C S7), `src/bin/live_block_fetch_session.rs` (N-G S7), **NEW — `src/bin/live_block_follow_session.rs`** (PHASE4-N-H S6; 141 LOC; default hermetic `main` + explicit `--connect` stubbed live pass). |

> **Gap surfaced (carried + extended).** `ade_core_interop` at HEAD is a *readiness probe + bridges + live evidence capture binaries*. CE-N-B-6 closes via manual evidence log against a real cardano-node peer; live capture pending operator run. CE-N-E-6 is CLOSED at HEAD. CE-N-E-7 is deferred as `CE-NODE-N2C-LTX`. CE-N-C-8 is at `status = "enforced"` with `open_obligation` on `CN-CONS-06`: `blocked_until_operator_stake_available`. CE-N-G-8 (RO-LIVE-01) is at `status = "partial"` with `open_obligation = "blocked_until_operator_peer_available"`. **NEW — CE-N-H-6 (live cross-impl follow evidence, RO-LIVE-02) is at `status = "partial"` with `open_obligation = "blocked_until_operator_peer_available"`. The mechanical half is closed (`receive_pipeline_corpus_drive.rs` over the Conway-576 corpus admits every block + ChainDb tip matches expected (slot, hash) + stored bytes byte-identical + LedgerState fingerprint advances; `receive_session_transcript_replay.rs` proves per-session transcript determinism; `receive_two_peer_independence.rs` proves multi-peer determinism — OQ-4 narrow-scope); the live half awaits a private cardano-node peer being provisioned per `docs/clusters/completed/PHASE4-N-H/CE-N-H-6_PROCEDURE.md`. Additionally, at HEAD the binary's `--connect` mode is honest-scope stubbed (the tokio socket wiring from `receive::orchestrator` to the wire is one layer up — deferred to operator-action work). When the operator wires the socket and runs the pass, the log path will be appended to `RO-LIVE-02.evidence_notes`.** The S6 binary's RED classification is review-and-doc enforced (matching the S6/S7 precedent across N-E/N-C/N-G — no narrow CI gate scopes the partition, only the presence gate `ci_check_receive_paths_corpus_present.sh`).

---

### `ade_network` *(RED submodules + capture binaries)*

> **Status carried from prior CODEMAP — unchanged at HEAD `efe1fb9`.** The RED half of `ade_network`: `mux::transport`, `session`, and the 7 `src/bin/capture_*` binaries. PHASE4-N-H did not touch this half of the crate.

| Attribute | Value |
|-----------|-------|
| **Purpose** | The only place inside `ade_network` where I/O happens — tokio-based TCP / Unix-socket scaffolding (`mux::transport`), the session-composition placeholder (`session`), and capture binaries that perform real handshakes against pinned cardano-node 11.0.1 peers. |
| **Creates** | `MuxTransport`. `session/mod.rs` is an empty placeholder. The capture binaries produce no public Rust types. |
| **Interprets** | Live TCP / Unix-socket byte streams; bytes flow into the BLUE mux frame decoder and protocol codecs without re-interpretation. |
| **MUST NOT** | (1) Contain protocol logic. (2) Define or modify any closed wire-message enum, agency type, or state. (3) Read a selected protocol version from ambient state. (4) Replace BLUE codec / state machine with a "fast path". (5) Bypass `ade_codec`'s `PreservedCbor` chokepoints. (6) Be depended on by any BLUE submodule of `ade_network`. |
| **Inbound deps** | `ade_core_interop`. At the library level no BLUE crate imports the RED submodules. |
| **Outbound deps** | `tokio`, `ade_types`, `ade_codec`, plus BLUE `ade_network::codec::*` and `ade_network::mux::frame`. |
| **Entry points** | `ade_network::mux::transport::{MuxTransport, open_tcp}`. The capture binaries. |
| **Key modules** | `mux/transport.rs`, `session/mod.rs` (placeholder), `bin/capture_*`. |

> **Gap surfaced (carried + extended).** `ade_network::session::mod.rs` is still a placeholder. The N-G S7 binary's honest-scope stub of the tokio socket wiring sits at the boundary between `ade_runtime::network::n2n_server` (pure dispatch driver) and the absent session-composition layer. **NEW — PHASE4-N-H implicitly re-opens this gap from a third angle:** the N-H S6 binary's `--connect` mode is similarly honest-scope stubbed — the receive orchestrator (`ade_runtime::receive::orchestrator`) is a pure state-machine driver but needs the same kind of socket layer for the client role that the server pump needs for the server role. A future cluster — likely the same node-binary cluster that owns `CE-NODE-N2C-LTX` and the producer-side relay-mesh stub — should own (a) the `ade_network::session` driver, (b) the tokio glue threading `dispatch_chain_sync_inbound` / `dispatch_block_fetch_inbound` (client) AND `dispatch_chain_sync_frame` / `dispatch_block_fetch_frame` (server) over real sockets, and (c) the receive-mode follower behaviour that the N-H S6 binary currently stubs.

---

### `ade_node`

> **Status carried from prior CODEMAP — unchanged at HEAD `efe1fb9`.** Still a hello-world stub. PHASE4-N-H did not touch `ade_node`; the receive-side bridge is reachable today through the `live_block_follow_session` operator-action binary and the three new integration tests, not through any `ade_node` orchestration.

| Attribute | Value |
|-----------|-------|
| **Purpose** | Binary entry point for the node process. Reserved as the assembly point for runtime + ledger + chain DB + network + consensus orchestrator + mempool + the deferred N2C server + the producer-side N2N server + the receive-side N2N client pump (now sketched but not assembled into a node binary). |
| **Creates** | None. |
| **Interprets** | None yet. |
| **MUST NOT** | (1) Bypass `ade_codec` to construct semantic types from bytes. (2) Modify `ade_ledger` or `ade_core::consensus` state in place — transitions go through `ade_ledger::rules::*`, `ade_ledger::block_validity::*`, `ade_ledger::tx_validity::*`, `ade_ledger::mempool::admit`, `ade_ledger::mempool::mempool_ingress`, `ade_ledger::producer::forge::forge_block` + `ade_ledger::producer::self_accept::self_accept`, `ade_ledger::producer::served_chain::served_chain_admit`, `ade_ledger::receive::admit_via_block_validity` + `ade_ledger::receive::receive_apply`, or `ade_core::consensus::*` BLUE transitions. (3) Take an inbound dep from any other crate. (4) Read versions out of ambient context. (5) Construct an `OrchestratorState` outside `ade_runtime::consensus::chain_selector` or `ade_core_interop::fresh_orchestrator`. (6) Forward-looking — must reach the mempool only through `mempool_ingress`. (7) Forward-looking `CE-NODE-N2C-LTX` — must drive the GREEN bridge, not introduce a parallel path. (8) PP-S1 forward-looking — typed `Vec<ProposalProcedure>` only. (9) Forward-looking PHASE4-N-C / CN-CONS-07 — drive `forge_block` → `self_accept`; never fabricate `AcceptedBlock`; never introduce a parallel body-hash recipe; never bypass `opcert_validate`; hold private-key material inside `ade_runtime::producer::{signing, keys}` only. (10) Forward-looking PHASE4-N-G / CN-CONS-07 strengthening + DC-CONS-17/18 + CN-PROTO-06 — drive the producer-side N2N server pump only via `ade_runtime::network::n2n_server`; never bypass the GREEN `ServedChainLookups` adapter. **(11) NEW — Forward-looking PHASE4-N-H / CN-CONS-07 strengthening + CN-CONS-08 + DC-CONS-19 + DC-PROTO-09 + CN-PROTO-07:** any `ade_node`-level wiring of the receive-side N2N client pump must drive `ade_runtime::receive::orchestrator::dispatch_*_inbound` over a `ReceiveState` whose `pending_headers` cache is admitted exclusively via the BLUE `receive::pending_header_cache::insert` API; must NOT construct a `ReceiveEvent` from a locally-originated chain-sync/block-fetch message (CN-PROTO-07 — the constructor doesn't exist; this is a compile-time gate); must NOT bypass the GREEN `events_to_state` adapter to wire BLUE reducers directly against raw `ChainSyncMessage`/`BlockFetchMessage` values; must NOT admit bytes that did not trace through `admit_via_block_validity` → `block_validity` (the `AdmittedBlock` constructor is module-private to `receive::admitted`; the only path is `admit_via_block_validity`); must NOT introduce a parallel `ChainDb` writer for admitted blocks (the GREEN `ChainDbWriter<'a, D: ChainDb>` is the only adapter for `ChainDbWrite`); must NOT link `ade_runtime::receive::*` against `ade_runtime::producer::{signing, broadcast, scheduler}` (CI gate `ci_check_receive_orchestrator_no_producer_dep.sh`); must surface `ReceiveError::RollbackOutOfScope` to the operator surface as a peer-halt signal until the follow-on ledger-snapshot/rollback cluster lands (Path A scope edge). The default `ade_node` binary MUST stay hermetic — receiving mode requires explicit operator opt-in matching the producer-mode precedent. |
| **Inbound deps** | None. |
| **Outbound deps** | None at present. |
| **Entry points** | `main()` in `crates/ade_node/src/main.rs`. |

> **Gap surfaced (carried).** `ade_node` is still a hello-world stub; its MUST NOT list is forward-looking with nothing to enforce today.

---

### `ade_runtime`

> **Status change at HEAD `efe1fb9` (PHASE4-N-H S3 + S4 — one new top-level module `receive/` containing two GREEN files + one RED file).** Between `a280954` and HEAD, `ade_runtime` gained the new `receive/` top-level module: (a) two GREEN files documented above in the GREEN block — `receive/events_to_state.rs` (S3; `lift_chain_sync_signal` + `lift_block_fetch_event` — pure pass-through) and `receive/in_memory_chain_write.rs` (S3; `ChainDbWriter<'a, D: ChainDb>` — wraps any `ChainDb` impl + maps `ChainDbError → ChainWriteError`); (b) one RED file `receive/orchestrator.rs` (S4; `PerPeerReceiveState`, the two dispatch functions `dispatch_chain_sync_inbound` + `dispatch_block_fetch_inbound`, the closed `ReceiveDispatchError` sum) — pure state-machine driver with no socket I/O (socket layer one level up at the binary boundary). All prior N-D / N-B / N-A / N-C / N-G narrative carries forward.

| Attribute | Value |
|-----------|-------|
| **Purpose** | The imperative shell. Hosts (a) the `ChainDb` / `SnapshotStore` storage abstractions (N-D); (b) the GREEN chain-selector orchestrator + GREEN `CandidateFragment` materializer (N-B); (c) the RED genesis-text parser (N-B); (d) the RED producer-side scaffolding (`producer/`): private-key custody, `.skey` loader, RED scheduler, GREEN tick-assembler, RED broadcast queue — N-C; (e) the RED producer-side N2N server pump (`network/n2n_server.rs`, N-G S6); (f) the GREEN broadcast-to-served adapter + `ServedChainLookups` trait carrier (`producer/{broadcast_to_served, served_chain_lookups}.rs`, N-G S5); **(g) NEW — the GREEN receive-side adapter pair (`receive/events_to_state.rs` + `receive/in_memory_chain_write.rs`, N-H S3) lifting N-A `ForkChoiceSignal` + `BatchDeliveryEvent` into `ReceiveEvent` and wiring `ChainDbWrite` over any `ChainDb`; (h) NEW — the RED receive-side N2N client orchestrator (`receive/orchestrator.rs`, N-H S4) decoding inbound mini-protocol frames, lifting via the GREEN adapter, and running the BLUE `receive_apply` reducer**. The crate remains RED at the prefix level; `chain_selector.rs` / `candidate_fragment.rs` / `tick_assembler.rs` / `broadcast_to_served.rs` / `served_chain_lookups.rs` / **NEW — `receive/events_to_state.rs` / `receive/in_memory_chain_write.rs`** are GREEN by file-level classification; `genesis_parser.rs` / `signing.rs` / `keys.rs` / `scheduler.rs` / `broadcast.rs` / `network/n2n_server.rs` / **NEW — `receive/orchestrator.rs`** are RED. |
| **Creates** | Carry-forward: `ChainDb`, `SnapshotStore`, `KillStrategy<D>`, `ChainTip`, `StoredBlock`, `ChainDbError`, `InMemoryChainDb`, `PersistentChainDb`, `PersistentChainDbOptions`, `SyncCadence`, `NoKill`, `BlockIter<'a>`; `Recoverable`, `StartingState`, `RecoveryReport<R>`, `RecoveryError<E>`; `NetworkMagic`, `GenesisBlob`, `GenesisBundle<'a>`, `GenesisParseError`; `StreamInput`, `OrchestratorError`, `OrchestratorState`, `RollbackSnapshot`, `DEFAULT_SNAPSHOT_LIMIT`; `build_candidate_fragment`; PHASE4-N-C `producer/` types; PHASE4-N-G `producer/{broadcast_to_served, served_chain_lookups}` (`ServedChainLookups<'a>`, function `drain_and_admit`) and `network/n2n_server` (`PerPeerN2nServerState`, `DispatchError`, three dispatch functions). **NEW — PHASE4-N-H S3 (GREEN):** functions `lift_chain_sync_signal(ForkChoiceSignal) -> Option<ReceiveEvent>` + `lift_block_fetch_event(BatchDeliveryEvent) -> Option<ReceiveEvent>` (in `receive::events_to_state`); struct `ChainDbWriter<'a, D: ChainDb> { pub db: &'a D }` with `impl<'a, D: ChainDb> ChainDbWrite for ChainDbWriter<'a, D>` (in `receive::in_memory_chain_write`). **NEW — PHASE4-N-H S4 (RED):** struct `PerPeerReceiveState { receive_state: ReceiveState, chain_sync_version: ChainSyncVersion, block_fetch_version: BlockFetchVersion }`; closed `ReceiveDispatchError` sum (`ChainSyncDecode(CodecError)`, `BlockFetchDecode(CodecError)`, `Receive(ReceiveError)`); functions `dispatch_chain_sync_inbound(state, frame, chain_write, era_schedule, ledger_view) -> Result<Option<ReceiveEffect>, ReceiveDispatchError>`, `dispatch_block_fetch_inbound(...) -> Result<Option<ReceiveEffect>, ReceiveDispatchError>`. |
| **Interprets** | Block bytes for content-addressed storage. Genesis JSON. N-A `StreamInput` events. PHASE4-N-C: cardano-cli `.skey` text envelopes; RED-supplied signed artifacts; RED scheduler input stream; `AcceptedBlock` values handed across the BLUE→RED boundary into `BroadcastQueue`. PHASE4-N-G S5/S6: `AcceptedBlock` values drained from `BroadcastQueue` and admitted into `ServedChainSnapshot`; inbound mini-protocol frame bytes decoded once via BLUE per-protocol `decode_*_message`. **NEW — PHASE4-N-H S3/S4:** inbound chain-sync mini-protocol frames decoded once via `ade_network::codec::chain_sync::decode_chain_sync_message`; inbound block-fetch frames decoded once via `ade_network::codec::block_fetch::decode_block_fetch_message`; decoded messages translated into `ForkChoiceSignal` / `BatchDeliveryEvent` (via `message_to_fork_choice_signal` / `message_to_batch_delivery_event` private helpers in `orchestrator.rs`), then lifted to `ReceiveEvent` via the GREEN `events_to_state` adapter, then handed to the BLUE `receive_apply` reducer. The receive orchestrator itself never decodes block / header bytes — those stay opaque pass-through (DC-PROTO-06) until the BLUE reducer's `BlockDelivered` branch decodes the body for the cache cross-check + admit. |
| **MUST NOT** | (1) ~~Depend on `ade_ledger`.~~ Carry-forward exception: now-permitted by the established RED → BLUE direction. (2) Leak `redb` through the `chaindb::*` public surface. (3) Have a second public chaindb path. (4) Couple snapshots to a specific state type. (5) Apply automatic snapshot pruning. (6) Allow partial-recovery success. (7) Add async recovery surface. (8) Treat "not found" as an error path or silently retry on real errors. (9) Consensus sub-tree GREEN `chain_selector` must not affect authoritative outputs. (10) Genesis parser — structured errors only. (11) `candidate_fragment` materializer — pure constructor. (12–19) PHASE4-N-C + N-G carry-forward MUST NOTs. **(20) NEW — `receive::events_to_state` (DC-PROTO-09, S3, GREEN):** must NOT import `std::time`/`tokio`/`rand`/`HashMap`/async constructs (enforced by `ci/ci_check_receive_replay_purity.sh`); must NOT decode header_bytes or block_bytes — positive negative grep on `decode_block_envelope`/`decode_block`/`accepted_block_header_bytes` in the same script. The lifter signatures MUST remain `fn(ForkChoiceSignal) -> Option<ReceiveEvent>` + `fn(BatchDeliveryEvent) -> Option<ReceiveEvent>` (no `Result`, no callback, no `Future`); non-state-changing variants MUST return `None` so the orchestrator skips them; state-changing variants MUST pass `header_bytes`/`block_bytes` through verbatim. **(21) NEW — `receive::in_memory_chain_write` (DC-PROTO-09 + DC-CONS-19, S3, GREEN):** must NOT import wall-clock / tokio / rand / HashMap (same script). MAY call `ade_ledger::block_validity::decode_block` once per `write_admitted` to derive the `(slot, hash)` key for `StoredBlock` — the decode is safe because the AdmittedBlock invariant guarantees prior B1 acceptance; a decode failure here MUST surface as `ChainWriteError::Underlying(ChainWriteErrorKind::Other)` (no panic). The trait impl MUST take `AdmittedBlock` BY VALUE (preserves the BLUE admission gate). The `(slot, hash)` derivation MUST be from the decoded body — no caller-supplied "asserted hash" parameter. **(22) NEW — `receive::orchestrator` (S4, RED, key-boundary doctrine + DC-PROTO-06 + DC-PROTO-09 + CN-CONS-08 + CN-PROTO-07):** must NOT import from `crate::producer::{signing, broadcast, scheduler}` — direct or transitive (enforced by `ci/ci_check_receive_orchestrator_no_producer_dep.sh`). Must NOT name the producer-side private-key/broadcast types `VrfSigningKey`/`KesSecret`/`KesSigningKey`/`SigningError`/`BroadcastQueue` (same script). The handshake-negotiated versions MUST be threaded as **explicit per-call inputs** through `PerPeerReceiveState` — never read from a session-global (DC-PROTO-06; tested by `dispatch_chain_sync_inbound_threads_negotiated_version`). The dispatcher MUST NOT contain authoritative state-transition logic — it is a pure state-machine driver composing BLUE decode → GREEN lift → BLUE reducer. Per-peer state MUST be independent (`PerPeerReceiveState`); cross-peer coordination MUST go through the shared `&mut dyn ChainDbWrite` reference only (OQ-4 multi-peer determinism; tested by `receive_two_peer_independence.rs`). The dispatcher MUST NOT open sockets or perform any I/O — the socket layer is one level up (the `live_block_follow_session` binary and, in the future, the deferred `ade_node` orchestration). On undecodable input the dispatcher MUST return a typed `ReceiveDispatchError::{ChainSync,BlockFetch}Decode(_)` — no panic, no silent drop. On non-state-changing inbound messages (Intersected, NoIntersection, BatchStarted, NoBlocks, BatchCompleted, server-side messages like RequestNext) the dispatcher MUST return `Ok(None)` — the boolean-style "no-op" signal lets the binary layer drive control flow without spuriously consuming reducer state. On `RollBackward` (peer-instructed), the dispatcher surfaces `ReceiveError::RollbackOutOfScope` via `ReceiveDispatchError::Receive` — the binary layer MUST halt the peer pipeline at this signal (Path A scope edge; full rollback authority awaits a follow-on cluster). |
| **Inbound deps** | `ade_testkit` (non-dev dep, used by `stream_replay`), `ade_core_interop` (heavy consumer at HEAD — all five `live_*_session` binaries import from `ade_runtime`). The binary `chaindb_kill_target` lives at `src/bin/`. **NEW heavy consumption sites in PHASE4-N-H** — `ade_core_interop::bin::live_block_follow_session` will (when wired) import the full `receive/` sub-tree (at HEAD the binary stubs the wiring and imports only stdlib). The integration tests under `crates/ade_runtime/tests/` (`receive_pipeline_corpus_drive.rs`, `receive_session_transcript_replay.rs`, `receive_two_peer_independence.rs`) consume the same surfaces; they sit inside the crate's `tests/` directory so they are dev-dep edges. |
| **Outbound deps** | `ade_types`, `ade_core`, `ade_crypto`, `ade_codec`, `ade_ledger`, `ade_network`, `redb`, `serde_json`, `cardano-crypto`, `ed25519-dalek`. **No new outbound crate edge in PHASE4-N-H** — the new `receive/` sub-tree reuses the established edges (the orchestrator imports `ade_network::{codec::chain_sync, codec::block_fetch, codec::version, codec::CodecError, chain_sync::signal, block_fetch::event}` over the N-G `ade_runtime → ade_network` edge; the in_memory_chain_write adapter imports `ade_ledger::{block_validity::decode_block, receive::*}` over the existing edge). Dev-deps: `tempfile`, `ade_testkit`. |
| **Entry points** | **Chaindb / recovery:** `ade_runtime::chaindb::*`, `ade_runtime::recovery::*`. **Consensus:** `ade_runtime::consensus::chain_selector::*`, `ade_runtime::consensus::candidate_fragment::*`, `ade_runtime::consensus::genesis_parser::*`. **PHASE4-N-C `producer/`:** `ade_runtime::producer::{signing::*, keys::*, tick_assembler::*, scheduler::*, broadcast::*}`. **PHASE4-N-G `producer/` (GREEN):** `ade_runtime::producer::broadcast_to_served::drain_and_admit`; `ade_runtime::producer::served_chain_lookups::ServedChainLookups`. **PHASE4-N-G `network/` (RED):** `ade_runtime::network::n2n_server::{PerPeerN2nServerState, DispatchError, dispatch_chain_sync_frame, dispatch_block_fetch_frame, poll_chain_sync_advance}`. **NEW — PHASE4-N-H `receive/` (GREEN):** `ade_runtime::receive::events_to_state::{lift_chain_sync_signal, lift_block_fetch_event}`; `ade_runtime::receive::in_memory_chain_write::ChainDbWriter`. **NEW — PHASE4-N-H `receive/` (RED):** `ade_runtime::receive::orchestrator::{PerPeerReceiveState, ReceiveDispatchError, dispatch_chain_sync_inbound, dispatch_block_fetch_inbound}`. |
| **Key modules** | **`chaindb/`**, **`recovery.rs`**, **`consensus/`** (`genesis_parser.rs` RED + `candidate_fragment.rs` GREEN + `chain_selector.rs` GREEN), **`bin/chaindb_kill_target.rs`**, **`producer/`** (PHASE4-N-C `signing.rs` RED, `keys.rs` RED, `tick_assembler.rs` GREEN, `scheduler.rs` RED, `broadcast.rs` RED; PHASE4-N-G `broadcast_to_served.rs` GREEN, `served_chain_lookups.rs` GREEN), **`network/`** (PHASE4-N-G S6): `network/mod.rs`, `network/n2n_server.rs` (RED), **NEW — `receive/`** (PHASE4-N-H S3 + S4): `receive/mod.rs` (26 LOC barrel; re-exports `lift_block_fetch_event` + `lift_chain_sync_signal` + `ChainDbWriter` + `PerPeerReceiveState` + `dispatch_*_inbound` + `ReceiveDispatchError`), `receive/events_to_state.rs` (209 LOC + 8 inline tests, **GREEN**), `receive/in_memory_chain_write.rs` (199 LOC + 2 inline tests, **GREEN**), `receive/orchestrator.rs` (432 LOC + 4 inline tests, **RED**). **Integration tests under `tests/`:** carry-forward; **NEW — PHASE4-N-H S3/S4/S5:** `receive_session_transcript_replay.rs` (S3, synthetic session-transcript replay — 1 test `receive_session_transcript_replay_byte_identical` proving DC-PROTO-09), `receive_two_peer_independence.rs` (S4, OQ-4 multi-peer determinism — 2 tests `two_peers_admit_same_block_into_shared_chaindb` + `two_peers_per_session_transcripts_match_solo_runs`), `receive_pipeline_corpus_drive.rs` (S5, mechanical adapter — CE-N-H-5, 4 tests: `receive_pipeline_corpus_drive_admits_every_block` + `_chaindb_tip_matches_expected` + `_admitted_bytes_equal_corpus_bytes` + `_ledger_fingerprint_changes_on_admit`). |
| **Mechanical enforcement** | Carry-forward chaindb / recovery / crash-safety + 8 PHASE4-N-C CI scripts + 3 PHASE4-N-G `ade_runtime`-scoped CI scripts. **NEW — three PHASE4-N-H CI scripts back the receive-side surface inside `ade_runtime`:** `ci/ci_check_receive_replay_purity.sh` (CE-N-H-3 + DC-PROTO-09, scope `crates/ade_runtime/src/receive/{events_to_state,in_memory_chain_write}.rs`), `ci/ci_check_receive_orchestrator_no_producer_dep.sh` (S4 key-boundary, scope `crates/ade_runtime/src/receive/`), `ci/ci_check_receive_paths_corpus_present.sh` (S6 presence, scope `crates/ade_runtime/tests/{receive_pipeline_corpus_drive,receive_session_transcript_replay,receive_two_peer_independence}.rs` + `crates/ade_core_interop/src/bin/live_block_follow_session.rs` + procedure doc). |

> **Gap surfaced (carried + extended).** No dedicated CI script enforces the GREEN/RED partition *inside* `ade_runtime::consensus` or `ade_runtime::producer` or `ade_runtime::network`. **NEW — same gap class extends to `ade_runtime::receive/`:** the GREEN/RED sub-partition is "by file" — `events_to_state.rs` + `in_memory_chain_write.rs` are GREEN (pure adapter + thin write wrapper, gated by `ci_check_receive_replay_purity.sh` for purity + pass-through discipline), `orchestrator.rs` is RED (pure dispatch driver, key-boundary gated by `ci_check_receive_orchestrator_no_producer_dep.sh`); the broader sub-partition rests on file-header banners + review. **NEW — at HEAD the `live_block_follow_session` binary's `--connect` mode is honest-scope stubbed** (the tokio socket wiring from `receive::orchestrator` to the wire is one level up — deferred to operator-action work); the binary prints the wiring stub plan and exits without opening sockets. The orchestrator itself is pure and complete; what is owed is the socket layer + the live-evidence pass that traverses it. This re-opens the `ade_network::session` placeholder gap from a third angle (the deferred node-binary cluster owning `CE-NODE-N2C-LTX` + producer-side relay-mesh will likely own the socket layer for the receive-side N2N client pump too).

---

## Cross-Module Rules (project-wide)

### Dependency direction

`ade_core_interop` → `{ade_core, ade_codec, ade_crypto, ade_ledger, ade_runtime, ade_network, ade_testkit, ade_types, tokio}` is legal (RED leaf binary). **No new crate-level outbound edge in PHASE4-N-H** — the S6 binary reuses these.
`ade_runtime` → `{ade_core, ade_crypto, ade_codec, ade_types, ade_ledger, ade_network, redb, serde_json, cardano-crypto, ed25519-dalek}` is legal (RED → BLUE). **No new crate-level outbound edge in PHASE4-N-H** — the new `receive/` sub-tree reuses the established edges.
`ade_testkit` → `{ade_core, ade_ledger, ade_plutus, ade_runtime, ade_crypto, ade_codec, ade_types, cardano-crypto}` is legal (GREEN).
`ade_network` (BLUE submodules) → `{ade_codec, ade_types}` is legal. Dev-deps `{ade_ledger, ade_testkit, ade_crypto, ade_core}` (PHASE4-N-G) carry forward.
`ade_network` (RED submodules + capture bins) → `{tokio, ade_codec, ade_types, ade_network::codec::*, ade_network::mux::frame}` is legal.
`ade_ledger` → `{ade_core, ade_plutus, ade_crypto, ade_codec, ade_types, minicbor}` is legal (BLUE among BLUEs). The new `receive` module reuses these edges — no new outbound from `ade_ledger`.
`ade_core` → `{ade_types, ade_crypto, minicbor}` is legal (BLUE among BLUEs).
`ade_plutus` → `{ade_crypto, ade_codec, ade_types}` is legal.
`ade_crypto` → `{ade_types}` is legal.
`ade_codec` → `{ade_types}` is legal.
`ade_types` → `{}`.

**Forbidden directions.** Any BLUE crate (or BLUE `ade_network` submodule) depending on `ade_runtime`, `ade_node`, `ade_core_interop`, or the RED half of `ade_network` is a CI failure (`ci_check_dependency_boundary.sh` + `ci_check_no_async_in_blue.sh`). Any non-`ade_plutus` crate referring to `pallas_*` is a CI failure (`ci_check_pallas_quarantine.sh`). Any reference to `ChainDb` / `chain_db` inside `crates/ade_core/src/consensus/` is a CI failure (`ci_check_no_chaindb_in_consensus_blue.sh`). B1/B2/N-E — BLUE/GREEN sub-partition inside `ade_ledger::mempool` is review-and-test-enforced for the broad partition, with two narrow grep gates. B3 — deposit-param-authority gate. PP-S1 — `ProposalProcedure {…}` struct-literal scan. PHASE4-N-C dependency notes (the 8 producer CI gates pin specific properties). PHASE4-N-G dependency notes (the 7 producer-side-server CI gates pin specific properties). **NEW — PHASE4-N-H dependency notes (the 5 receive-side authority CI gates pin specific properties):** any `pub fn` whose signature contains `AdmittedBlock` outside `crates/ade_ledger/src/receive/admitted.rs` is a CI failure (`ci_check_admitted_block_closure.sh`, CE-N-H-1 + CN-CONS-08). Any `pub struct AdmittedBlock` outside `admitted.rs` is a CI failure (same script). Any `pub use ... AdmittedBlock as <alias>` rename re-export is a CI failure (same script). Any reference in `crates/ade_ledger/src/receive/events.rs` to the locally-originated chain-sync/block-fetch variant names `RequestNext`/`RequestRange`/`ClientDone`/`FindIntersect` is a CI failure (same script — CN-PROTO-07 closure). Any `HashMap`/`HashSet`/wall-clock/`tokio`/`rand` in `crates/ade_ledger/src/receive/reducer.rs` production code is a CI failure (`ci_check_receive_reducer_closure.sh`, CE-N-H-2). Absence of `admit_via_block_validity` call site or `RollbackOutOfScope` in `reducer.rs` is a CI failure (same script). Any assignment to `state.ledger` or `state.chain_dep` inside the `roll_forward` helper body is a CI failure (same script — Invariant I-6, RollForward never mutates authoritative state). Any `HashMap`/`HashSet`/wall-clock/`tokio`/`rand` in `crates/ade_runtime/src/receive/{events_to_state,in_memory_chain_write}.rs` is a CI failure (`ci_check_receive_replay_purity.sh`, CE-N-H-3). Any call to `decode_block_envelope`/`decode_block`/`accepted_block_header_bytes` in `events_to_state.rs` is a CI failure (same script — pass-through discipline; the BLUE reducer is the sole decode site on the receive path). Absence of `lift_chain_sync_signal` / `lift_block_fetch_event` / `ChainDbWriter` `impl ChainDbWrite` is a CI failure (same script). Any `producer::signing` / `producer::broadcast` / `producer::scheduler` / `crate::producer::` import inside `crates/ade_runtime/src/receive/` is a CI failure (`ci_check_receive_orchestrator_no_producer_dep.sh`, key-boundary). Any reference inside that directory to `VrfSigningKey`/`KesSecret`/`KesSigningKey`/`SigningError`/`BroadcastQueue` is a CI failure (same script).

### Naming convention

All crates are prefixed `ade_`. TCB color is not encoded in the crate name. The authoritative classifier is `.idd-config.json` `core_paths` plus the cluster doc TCB Color Maps for sub-crate scopes; CI scripts hard-code their BLUE list. The seven full-BLUE-scoped scripts use the full BLUE set: 6 BLUE crates + 9 BLUE `ade_network` paths. The B3 `ci_check_deposit_param_authority.sh` scopes the 6 BLUE crates. Four narrow scripts target `crates/ade_core/src/consensus/`. The two N-E scripts scope `crates/ade_ledger/src/mempool/*` + `crates/ade_testkit/src/mempool/*`. The PP-S1 `ci_check_proposal_procedures_closed.sh` scopes `conway/{governance.rs, tx.rs}` paths. The 8 PHASE4-N-C scripts narrow as previously documented. The 7 PHASE4-N-G scripts narrow as previously documented. **NEW — the 5 PHASE4-N-H scripts narrow as follows:** `ci_check_admitted_block_closure.sh` scopes repo-wide (single-construction-site authority for `AdmittedBlock` + CN-PROTO-07 events-closure scan); `ci_check_receive_reducer_closure.sh` scopes `crates/ade_ledger/src/receive/reducer.rs` (with an AWK-extracted `roll_forward` helper-body scan for Invariant I-6); `ci_check_receive_replay_purity.sh` scopes `crates/ade_runtime/src/receive/{events_to_state,in_memory_chain_write}.rs`; `ci_check_receive_orchestrator_no_producer_dep.sh` scopes `crates/ade_runtime/src/receive/`; `ci_check_receive_paths_corpus_present.sh` scopes the three integration tests + the binary src + Cargo.toml `[[bin]]` entry + the procedure doc. **No CI script narrowly scopes the new `live_block_follow_session.rs` binary's RED classification** — that rests on its file-header comment, its placement under `src/bin/`, and the `#[ignore]`-gated closure-gate test pattern (matching the established `live_*_session` precedent).

### CI enforcement (52 scripts under `ci/`)

| Script | Enforces | Scope |
|---|---|---|
| **`ci_check_admitted_block_closure.sh`** *(NEW — PHASE4-N-H S1)* | **CE-N-H-1 + CN-CONS-08 + CN-PROTO-07 — single `pub fn .*AdmittedBlock` site at `admitted.rs::admit_via_block_validity`; single `pub struct AdmittedBlock` site; no `pub use ... AdmittedBlock as ...` rename re-exports; `events.rs` MUST NOT reference locally-originated chain-sync/block-fetch variant names** | repo-wide grep gate + `crates/ade_ledger/src/receive/events.rs` |
| `ci_check_block_fetch_server_closure.sh` *(PHASE4-N-G S4)* | DC-CONS-17 foundation — closed `ServerReply` wrapper, no raw `BlockFetchMessage` returns, `Block { bytes }` sourced from `ServedRangeLookup` | `crates/ade_network/src/block_fetch/server.rs` |
| `ci_check_broadcast_to_served_purity.sh` *(PHASE4-N-G S5)* | CE-N-G-5 — pure GREEN adapter shape; positive `accepted_block_header_bytes` import; no `std::time`/`tokio`/`rand`/`HashMap` | `crates/ade_runtime/src/producer/{broadcast_to_served,served_chain_lookups}.rs` |
| `ci_check_cbor_round_trip.sh` | T-ENC-03, DC-CBOR-01, DC-CBOR-02 | golden corpus |
| `ci_check_ce_n_a_5_proof.sh` *(PHASE4-N-A, S-A10)* | CE-N-A-5 5-condition evidence | `ade_network` (RED + real-capture corpus) |
| `ci_check_chaindb_contract.sh` | DC-STORE-02, DC-STORE-03, CN-STORE-04, CN-STORE-05 | `ade_runtime --lib chaindb::` (RED) |
| `ci_check_chaindb_crash_safety.sh` | T-REC-01 (crash variant), DC-STORE-01, CN-STORE-03 | `ade_runtime --test stress_kill_harness` (RED) |
| `ci_check_chain_sync_server_closure.sh` *(PHASE4-N-G S3)* | DC-PROTO-08 — closed `ServerReply` wrapper, no raw `ChainSyncMessage` returns, total reducer | `crates/ade_network/src/chain_sync/server.rs` |
| `ci_check_consensus_closed_enums.sh` | DC-CONS-04, DC-CONS-10, T-DET-01, DC-VAL-02/04/05/06, DC-TXV-01/02/04/05, DC-MEM-01/02 | `consensus/` + `block_validity/` + `tx_validity/` + `mempool/` |
| `ci_check_constitution_coverage.sh` | invariant-registry ↔ code/test coverage | repo-wide |
| `ci_check_conway_cert_classification_closed.sh` *(PHASE4-B3F)* | DC-TXV-06 | `ade_types::conway::cert` + `ade_codec::conway::cert` + `ade_ledger::cert_classify` |
| `ci_check_credential_discriminant_closed.sh` *(OQ5)* | DC-LEDGER-10 | `ade_types::shelley::cert` + both era `ade_codec` decoders + `ade_ledger::{state, governance, fingerprint, rules}` + `ade_types::conway::governance` |
| `ci_check_crypto_vectors.sh` | crypto KAT regression | `ade_crypto` |
| `ci_check_dependency_boundary.sh` | T-BOUND-02 — BLUE ⇎ RED separation | full BLUE (6 crates + 9 `ade_network` paths) |
| `ci_check_deposit_param_authority.sh` *(PHASE4-B3)* | DC-TXV-07 | 6 BLUE crates |
| `ci_check_differential_divergence.sh` | DC-DIFF-* | replay outputs |
| `ci_check_forbidden_patterns.sh` | T-CORE-02 + `unsafe` allowlist | full BLUE |
| `ci_check_forge_purity.sh` *(PHASE4-N-C S3)* | DC-CONS-13/14/15 | `crates/ade_ledger/src/producer/forge.rs` |
| `ci_check_gov_cert_accumulation_closed.sh` *(PHASE4-B5)* | DC-LEDGER-09 | `ade_ledger::{gov_cert, rules, error, state}` |
| `ci_check_hash_uses_wire_bytes.sh` | DC-CBOR-02, T-ENC-01 | full BLUE |
| `ci_check_hfc_translation.sh` | DC-EPOCH-02 | `ade_ledger::hfc` |
| `ci_check_ingress_chokepoints.sh` | DC-INGRESS-01, T-INGRESS-01 | full BLUE |
| `ci_check_ledger_determinism.sh` | DC-LEDGER-01 | `ade_ledger` |
| `ci_check_mempool_ingress_closure.sh` *(PHASE4-N-E, S1)* | DC-MEM-03 | `ade_ledger::mempool/{ingress,admit,mod}.rs` + repo-wide `admit(` scan |
| `ci_check_mempool_ingress_replay.sh` *(PHASE4-N-E, S2 + S3)* | DC-MEM-04 | `ade_testkit::mempool/*` + `ade_testkit::lib.rs` + `ade_ledger::mempool::canonicalize` |
| `ci_check_module_headers.sh` | CE-04 contract banner | full BLUE |
| `ci_check_n2n_server_no_signing_dep.sh` *(PHASE4-N-G S6)* | Key-boundary: `network::n2n_server` MUST NOT import `producer::signing` | `crates/ade_runtime/src/network/` |
| `ci_check_no_async_in_blue.sh` *(PHASE4-N-A, S-A1)* | DC-CORE-01 | full BLUE |
| `ci_check_no_chaindb_in_consensus_blue.sh` *(PHASE4-N-B, S-B1)* | DC-CORE-01 + DC-CONS-07 | `crates/ade_core/src/consensus/` |
| `ci_check_no_density_in_fork_choice.sh` *(PHASE4-N-B, S-B8)* | DC-CONS-03 | `fork_choice.rs` + `candidate.rs` |
| `ci_check_no_float_in_consensus.sh` *(PHASE4-N-B, S-B1)* | T-CORE-02 + DC-CONS-07/08/09 | `crates/ade_core/src/consensus/` |
| `ci_check_no_parallel_header_splitter.sh` *(PHASE4-N-G S1)* | DC-CONS-16 strengthening + DC-CONS-18 — single canonical header-projection authority | repo-wide grep gate |
| `ci_check_no_private_keys_in_corpus.sh` *(PHASE4-N-C S3)* | DC-CRYPTO-03/04/05 corpus discipline | `crates/ade_testkit/src/producer/fixtures.rs` + `crates/ade_testkit/fixtures/producer/` |
| `ci_check_no_producer_body_encoder.sh` *(PHASE4-N-C S4)* | DC-CONS-16 — single body-hash authority | repo-wide grep gate |
| `ci_check_no_secrets.sh` | no credentials/IPs/keys | repo-wide |
| `ci_check_no_semantic_cfg.sh` | T-BUILD-01 | full BLUE |
| `ci_check_no_signing_in_blue.sh` | CE-05, T-KEY-01 | full BLUE |
| `ci_check_opcert_closed.sh` *(PHASE4-N-C S2)* | DC-CONS-11/12 | `crates/ade_codec/src/shelley/opcert.rs` + `crates/ade_core/src/consensus/opcert_validate.rs` |
| `ci_check_pallas_quarantine.sh` | O-29.2 | non-`ade_plutus` |
| `ci_check_private_key_custody.sh` *(PHASE4-N-C S1)* | DC-CRYPTO-03/04/05 + OP-OPS-04 | full BLUE + `crates/ade_runtime/src/producer/` |
| `ci_check_producer_corpus_present.sh` *(PHASE4-N-C S7)* | CN-CONS-06 mechanical half | `crates/ade_testkit/src/producer/fixtures.rs` |
| `ci_check_proposal_procedures_closed.sh` *(PROPOSAL-PROCEDURES-DECODE PP-S1)* | DC-LEDGER-11 | `conway/{governance,tx}.rs` paths + repo-wide `ProposalProcedure {` scan |
| **`ci_check_receive_orchestrator_no_producer_dep.sh`** *(NEW — PHASE4-N-H S4)* | **Key-boundary: `receive::*` MUST NOT import `producer::{signing, broadcast, scheduler}`; MUST NOT reference producer-side private-key/broadcast type names** | `crates/ade_runtime/src/receive/` |
| **`ci_check_receive_paths_corpus_present.sh`** *(NEW — PHASE4-N-H S6)* | **CE-N-H-5 + CE-N-H-6 binary presence — three integration tests exist with their named functions; live binary src + Cargo.toml `[[bin]]` entry exist; procedure doc exists in either pre-close or post-close location** | `crates/ade_runtime/tests/{receive_pipeline_corpus_drive,receive_session_transcript_replay,receive_two_peer_independence}.rs` + `crates/ade_core_interop/src/bin/live_block_follow_session.rs` + procedure doc |
| **`ci_check_receive_reducer_closure.sh`** *(NEW — PHASE4-N-H S2)* | **CE-N-H-2 + CN-CONS-08 + DC-CONS-19 — no HashMap/wall-clock/tokio/rand in production; positive `admit_via_block_validity` + `RollbackOutOfScope`; `roll_forward` helper body MUST NOT assign to `state.ledger` or `state.chain_dep` (Invariant I-6)** | `crates/ade_ledger/src/receive/reducer.rs` |
| **`ci_check_receive_replay_purity.sh`** *(NEW — PHASE4-N-H S3)* | **CE-N-H-3 + DC-PROTO-09 — GREEN adapter pure-fn + pass-through shape; no `decode_block_envelope`/`decode_block`/`accepted_block_header_bytes` in `events_to_state.rs`; positive presence of lifters + `ChainDbWriter` `impl ChainDbWrite`** | `crates/ade_runtime/src/receive/{events_to_state,in_memory_chain_write}.rs` |
| `ci_check_recovery_contract.sh` | T-REC-01, T-REC-02, DC-STORE-05 | `ade_runtime --lib recovery::` (RED) |
| `ci_check_ref_provenance.sh` | DC-REF-01 | reference corpus |
| `ci_check_scheduler_closure.sh` *(PHASE4-N-C S6)* | DC-CONS-13 + DC-MEM-03 + OP-OPS-05 | `crates/ade_runtime/src/producer/{scheduler,broadcast,tick_assembler}.rs` |
| `ci_check_self_accept_gate.sh` *(PHASE4-N-C S5)* | CN-CONS-07 | `crates/ade_ledger/src/producer/self_accept.rs` + repo-wide `AcceptedBlock` literal scan |
| `ci_check_served_chain_closure.sh` *(PHASE4-N-G S2)* | CN-CONS-07 strengthening + CE-N-G-2 — no HashMap/HashSet; admit single-argument; `blocks` field private | `crates/ade_ledger/src/producer/served_chain.rs` |
| `ci_check_server_paths_corpus_present.sh` *(PHASE4-N-G S7)* | CE-N-G-7 + CE-N-G-8 binary presence | `crates/ade_runtime/tests/{cross_impl_server_pipeline,server_paths_transcript_replay}.rs` + `crates/ade_core_interop/src/bin/live_block_fetch_session.rs` + procedure doc |

> **Post-`a280954` CI delta. +5.** All five new gates are the PHASE4-N-H receive-side authority gate set. **CI inventory 47 → 52.** The forward-looking `ci/ci_check_no_fail_open_in_validation.sh` (DC-VAL-06 / DC-TXV) is still **not** shipped — carried gap.

> **Carried residual gaps (unchanged at HEAD, plus the new N-H-introduced ones).** Carry-forward gaps (a)–(y) from the prior CODEMAP are all still open. **(z) NEW — `ade_runtime::receive::{events_to_state, in_memory_chain_write}` GREEN sub-classification inside the RED `ade_runtime` crate prefix is partially gated by `ci_check_receive_replay_purity.sh` (pure-fn + pass-through shape) but the broader BLUE/GREEN/RED partition is "by file" — no dedicated CI script for the broad partition.** **(aa) NEW — `ade_runtime::receive::orchestrator` RED classification is review-and-doc enforced (file-header banner + key-boundary CI gate); no script asserts the broader RED partition inside `receive/`.** **(bb) NEW — `ade_core_interop::bin::live_block_follow_session` (S6) RED classification is review-and-doc enforced (matching the S6/S7 precedent across N-E/N-C/N-G — no narrow CI gate scopes the partition, only the presence gate `ci_check_receive_paths_corpus_present.sh`).** **(cc) NEW — at HEAD the `live_block_follow_session` binary's `--connect` mode is honest-scope stubbed: the actual tokio socket wiring from `receive::orchestrator` to the wire is one layer up (deferred to operator-action work + the future `ade_node` orchestration cluster). The pure dispatch driver `orchestrator` is complete; what is owed is the socket layer and the live-evidence pass that traverses it.** **(dd) NEW — CE-N-H-6 live evidence (RO-LIVE-02) carries `status = "partial"` + `open_obligation = "blocked_until_operator_peer_available"`. Reopen criteria: provision a private cardano-node peer, wire the socket layer (one-layer-up work), run the binary per `CE-N-H-6_PROCEDURE.md`, append the captured log path to RO-LIVE-02's `evidence_notes`. Mirrors the OP-OPS-04 / CE-N-C-8 and RO-LIVE-01 / CE-N-G-8 precedents (enforced mechanical half + open obligation for the operator-action live half).** **(ee) NEW — DC-CONS-20 (ChainDb-ledger-chain_dep lockstep) carries `status = "declared"` + `open_obligation = "rollback_side_blocked_until_ledger_snapshot_cluster"`. The admit-side half is enforced implicitly via CN-CONS-08 + DC-CONS-19 + DC-PROTO-09; the rollback-side half (a successful `RollBackward` rolls back all three of ChainDb / LedgerState / PraosChainDepState) is OUT OF SCOPE for the receive-bridge cluster — ledger rollback infrastructure does not exist (no LedgerState encode/decode, no snapshot+replay-forward driver). A follow-on ledger-snapshot/rollback cluster closes this rule.** **(ff) NEW — the cluster plan implicitly anticipated `ade_testkit::receive::*` but the shipped artifacts live at `crates/ade_runtime/tests/receive_*.rs` instead. The deviation is intentional (orphan-rule + dev-dep direction matching the N-G precedent); whether `ade_testkit::receive::*` should be added as a thin re-export shim for cross-crate consumption is a follow-on judgment call — no current consumer would benefit.**
