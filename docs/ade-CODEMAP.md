# Module Authority Map — Ade

> **Status:** Living architectural document. Regenerated; not hand-edited.
> Per-project instance of `~/.claude/methodology/templates/codemap.md`.

> 11 crates, 456 canonical types, 2213 tests, 116 CI checks at HEAD (`80dac1f7`, PHASE4-N-F-G-A cluster close).
>
> **PHASE4-N-F-G-A delta (forge fidelity on the `--mode node` spine):** **NO BLUE crate changed** (456 canonical types unchanged). All changes land in the RED shell (`ade_runtime`), the RED binary/driver (`ade_node`), and the GREEN test crate (`ade_testkit`). The cluster makes the `--mode node` forge path source **real** forge constants instead of stubs/defaults, and fail closed on two boundaries that the prior path masked: a before-genesis-anchor clock→slot saturation (S3) and an off-epoch forge slot (S4). Four sub-themes: **S1** a NEW GREEN `#[cfg(test)]` genesis-consistency pinning harness (`ade_testkit::consensus::genesis_pinning`) that drives the REAL `bootstrap_initial_state` warm-start against a committed private-net Ade-as-leader reference fixture; **S2/S2a** real operator-config ingress (`ade_node::operator_forge` retires the `parse_simple_*` stubs and uses the real `parse_opcert_envelope` + `parse_shelley_genesis`; the recovered ledger now carries the **current** oracle-bound `ProtocolParameters` + protocol version instead of `ProtocolParameters::default()`, sourced via the NEW GREEN `ade_runtime::consensus_inputs::protocol_params` parser + the hash-bound `LiveConsensusInputsCanonical::require_forge_current_pparams` preimage accessor); **S3** a checked clock→slot guard (`ade_runtime::clock::checked_millis_to_slot` + closed `SlotAlignmentError`) that fails closed before-anchor instead of saturating to `start_slot`; **S4** an epoch-boundary fail-closed guard (`ade_node::node_sync::forge_epoch_admission` + closed `ForgeEpochAdmission`) that fails the off-epoch forge **before** leadership / KES signing. **Registry → 313 rules**: NEW `DC-EPOCH-03` (single-epoch forge fail-closed on the `--mode node` spine) at `status = enforced`; NEW `DC-NODE-06` (self-accept→serve handoff, shape B) at `status = declared` (a forward sketch for the next sub-cluster **PHASE4-N-F-G-B** — NOT enforced by G-A). Seven `strengthened_in += "PHASE4-N-F-G-A"` bumps. Four NEW CI gates. **Boundary honesty (load-bearing — do not soften):** G-A is **forge-fidelity hardening on the relay spine** — real config ingress + current pparams + two new fail-closed boundaries. It does **NOT** serve, admit, gossip, or advance a durable tip; the forge stays **subordinate + self-accept-only**; the N-F-E containment gate is **byte-unchanged**; `run_relay_loop` still halts before any `ForgeTick` on the empty binary source (the `On` arm is forge-CAPABLE but **not observable** — RO-LIVE-01 follow-on). **BA-02 is satisfied nowhere.** The S1 fixture is **evidence input, never runtime authority**.

---

## Conventions

- A **module** in Ade is a Cargo workspace crate (smallest independently-buildable unit). Source: `Cargo.toml` `[workspace] members` (11 entries). One exception to crate-granularity: `ade_network` is split by *submodule color* — `.idd-config.json` `core_paths` resolves BLUE at the submodule path level (9 paths), so its BLUE / GREEN / RED submodules are documented as separate entries.
- Several BLUE crates host sub-trees that carry a *different* TCB color by their own module banner and cluster TCB Color Map; and the RED `ade_runtime` and `ade_node` crates host numerous **GREEN-by-content** sub-trees. These are surfaced as sub-classification notes inside the owning crate's entry (or, where load-bearing, as their own GREEN/RED entries below).
- Modules are listed by TCB color (BLUE → GREEN → RED), alphabetical within each color.

### TCB color sources (in order of authority)

1. `.idd-config.json` `core_paths` — substring match against absolute path. BLUE crate prefixes: `ade_codec`, `ade_types`, `ade_crypto`, `ade_core`, `ade_ledger`, `ade_plutus`. Plus the 9 BLUE `ade_network` submodule paths: `mux/frame.rs`, `codec/`, `handshake/`, `chain_sync/`, `block_fetch/`, `tx_submission/`, `keep_alive/`, `peer_sharing/`, `n2c/`.
2. `.idd-config.json` `_core_paths_doc` — `ade_runtime` is RED; `ade_testkit` is GREEN; `ade_node` is RED; `ade_network::mux::transport` + `ade_network::session` are nominally RED (but `session/` is GREEN-by-content per PHASE4-N-L). `ade_core_interop` is RED.
3. **Module doc-comment banners** — every BLUE/GREEN source file opens with the `// Core Contract:` banner followed by `//! BLUE …` / `//! GREEN …` / `//! RED …`. This is the per-file authority that distinguishes GREEN-by-content from RED inside `ade_runtime` / `ade_node`. Verified directly against the tree at HEAD for every new/changed module in this regeneration (`protocol_params.rs` → `//! GREEN`; `clock.rs` `checked_millis_to_slot` is in the GREEN-by-content clock seam; `genesis_pinning.rs` → `// GREEN` `#[cfg(test)]`; `node_sync.rs` `forge_epoch_admission` → `GREEN — pure`; `operator_forge.rs` → `//! RED`; `node_lifecycle.rs` → `//! RED`).
4. Cluster TCB Color Maps under `docs/clusters/` and `docs/clusters/completed/` — §5 "TCB color map (FC/IS partition)" of each `cluster.md`.
5. The invariant registry `docs/ade-invariant-registry.toml` (313 rules at HEAD) — each rule's `code_locus` + `statement` is the authoritative source for the **MUST NOT** rows below.

### Active cluster at HEAD

**PHASE4-N-F-G-A — CLOSING (forge fidelity on the `--mode node` spine).** HEAD `80dac1f7` is the S4 close-tail commit. The cluster is the first of three planned PHASE4-N-F-G sub-clusters (G-A forge fidelity / G-B self-accept→serve handoff / G-C live operator serve) and hardens the N-F-F forge-on path so the constants it forges with are *real*, and so two boundaries the prior path masked **fail closed**. Slices: **S1** (`2684a618` doc, `b5decb3e` impl GREEN genesis-consistency pinning harness; **S1b** `50c6a5b0`/`23addfbb` the committed private-net Ade-as-leader reference fixture), **S2a** (`38680107`/`3dba81db` the current protocol-parameters source: GREEN `protocol_params` parser + `require_forge_current_pparams` + install at seed/import), **S2** (`a5afb013` doc, `11704998` real opcert/genesis ingress + recovered-current constants), **S3** (`2a27d352` doc, `2049ec9d` the checked `checked_millis_to_slot` slot-alignment guard), **S4** (`5204a8d8` doc, `80dac1f7` the off-epoch `forge_epoch_admission` fail-closed guard).

**Boundary language (load-bearing — do not soften):** G-A is **forge-fidelity hardening on the relay spine** — real cardano-cli config ingress (opcert + genesis), oracle-bound **current** `ProtocolParameters` (no longer `::default()`), a before-anchor clock→slot fail-closed guard, and an off-epoch forge fail-closed guard *before* leadership / KES signing. It does **NOT** serve / admit / gossip / advance a durable tip. The forge stays **subordinate + self-accept-only**; `run_relay_loop`'s containment is **semantically unchanged** (G-A consumes the checked clock + sources current pparams inside the existing fenced `forge_one_from_recovered`; the off-epoch guard sits *inside* that fence, before leadership). On the empty binary source the loop still halts before any `ForgeTick` — the `On` arm is forge-CAPABLE but **not observable** (RO-LIVE-01 follow-on). There is **NO serve / admit / gossip / durable-tip / BA-02 / RO-LIVE acceptance claim**. The S1 reference fixture is **evidence input, never runtime authority** (never a production source of eta0 / stake / ASC / VRF keyhash; the in-test sidecar pre-seed is `#[cfg(test)]`-confined, the CN-CINPUT-02 populate-side fence keeps production population on the verified-bootstrap path only).

**What the binary `Node` arm actually runs (precise wiring honesty):**
- `main()` `Mode::Node → run_node_lifecycle → run_node_lifecycle_inner`. The arm is **fully wired and durable for bootstrap + recovery**: a true first run runs `first_run_mithril_bootstrap` (fail-closes on `verify_mithril_binding` before any state is admitted); a non-empty store runs `warm_start_recovery` (WAL replay → verified recovered `BootstrapState` via `bootstrap_initial_state(RequiredFromRecoveredProvenance)`). Both arms produce the **single recovered `BootstrapState`**.
- **N-F-F forge-intent flip (carried):** the arm calls `classify_forge_intent(...)` over operator-key flag *presence*. `Off` → `run_relay_loop(…, None)` (exact N-F-E relay); `On(paths)` → `build_operator_forge_material → coordinator_init → ForgeActivation → run_relay_loop(…, Some(&mut activation))`; `PartialKeySet` → `NodeLifecycleError::ForgeKeyIngress` (`EXIT_NODE_FORGE_KEY_INGRESS_FAILED = 44`).
- **G-A S2 forge constants (NEW):** the `On` arm's `ForgeActivation` now derives `protocol_version` + `pparams` from the **recovered current ledger view** (`node_lifecycle::current_forge_constants_from_recovered`), not produce-path defaults; the operator config is ingested through the real `parse_opcert_envelope` + `parse_shelley_genesis` (`operator_forge`, S2), and the recovered ledger carries the oracle-bound current `ProtocolParameters` installed at seed/import time (`admission::{seed_to_snapshot, bootstrap}` via `require_forge_current_pparams`, S2a).
- **G-A S3 clock guard (NEW):** the `ForgeTick` branch derives its slot via `checked_millis_to_slot` — a before-anchor tick fails closed (`SlotAlignmentError::BeforeGenesisAnchor`, recorded in `last_slot_alignment_fail`) instead of the saturating `millis_to_slot` mask to `start_slot`.
- **G-A S4 epoch guard (NEW):** `forge_one_from_recovered` calls `forge_epoch_admission(slot, era_schedule, seed_epoch)` **before** `query_leader_schedule` — an off-epoch (or unlocatable) slot fails closed (`ForgeEpochAdmission::OffEpoch`) before leadership / KES signing, and the forge path drives **no** `NonceInput::EpochBoundary` / `CandidateFreeze` nonce promotion (cross-epoch production is a separate nonce-roll cluster, DC-EPOCH-03).
- **Empty-source honesty (both arms, carried).** `NodeBlockSource::in_memory(Vec::new())` is empty; the planner halts on iteration 1 BEFORE any `SyncOnce` or `ForgeTick`. The `On` arm is **forge-CAPABLE but not observable**. Observable forge is the RO-LIVE-01 follow-on.
- The L6 BA-02 evidence correlator (`ba02_evidence`) remains a **library surface reached by no binary arm** (tested only). **BA-02 is satisfied nowhere.**

**No BLUE crate was modified by G-A** (verified: every BLUE crate's `(pub )?struct|enum` count is unchanged — `ade_ledger` 177, `ade_core` 49, `ade_codec` 11, `ade_types` 81, `ade_crypto` 21, `ade_plutus` 8, `ade_network` BLUE submodules 109; canonical-type total holds at **456**). All G-A code lands in RED `ade_runtime` (`clock` checked guard, `consensus_inputs::protocol_params` GREEN parser, `consensus_inputs::canonical` accessor), RED `ade_node` (`operator_forge` real parsers, `node_lifecycle` S2/S3 wiring, `node_sync` S4 epoch guard, `admission::{seed_to_snapshot, bootstrap}` S2a install), and GREEN `ade_testkit` (`consensus::genesis_pinning` `#[cfg(test)]`). It uses `BTreeMap` (never `HashMap`), sorted keys, integer-only rational arithmetic (**no float path** in `protocol_params`), and no clock/rand/float in production paths beyond the existing RED clock seam (the `On` arm's `SystemClock`).

The cluster introduces / extends:
- **GREEN (current-pparams parser)** `ade_runtime::consensus_inputs::protocol_params` (NEW, S2a) — the cardano-cli `query protocol-parameters` JSON parser. `parse_protocol_parameters_json(bytes, network_magic) -> Result<ProtocolParameters, ProtocolParamsParseError>` converts the oracle's protocol-parameters JSON into a canonical BLUE `ProtocolParameters` so the recovered ledger carries the **current** protocol version + modeled parameters instead of `ProtocolParameters::default()`. **No float path (hard rule):** rational unit-interval / non-negative-interval literals (`poolPledgeInfluence`, `monetaryExpansion`, `treasuryCut`) are preserved as strings via `serde_json::value::RawValue` and converted to exact `ade_ledger::rational::Rational` by **integer** decimal/scientific parsing — no `f64`, no `as f64`, no serde float deserialization; a literal that cannot be represented exactly fails closed (`InexactRational`). DC-CONS-IN-related; S2a.
- **GREEN (checked clock→slot guard)** `ade_runtime::clock::checked_millis_to_slot` + closed `SlotAlignmentError` (NEW, S3) — alongside the existing saturating `millis_to_slot` and the RED `SystemClock` seam. `checked_millis_to_slot(tick_millis, start_millis, start_slot, slot_length_ms)` returns `Err(SlotAlignmentError::BeforeGenesisAnchor)` for `tick_millis < start_millis` (the exact case the saturating path masks to `start_slot`), otherwise the EXACT `millis_to_slot` result. Pure integer arithmetic; no wall-clock, no float. The saturating `millis_to_slot` is left intact for the non-forge callers.
- **GREEN (hash-bound current-pparams accessor)** `ade_runtime::consensus_inputs::canonical::require_forge_current_pparams` (NEW, S2a) — `LiveConsensusInputsCanonical` now carries `protocol_params_json: Option<String>` **OUTSIDE** the frozen 15-field canonical fingerprint (the fingerprint already commits to `protocol_params_hash`, so the preimage is a non-fingerprinted carry — it does not alter the bundle fingerprint). `require_forge_current_pparams()` requires the preimage be present (`PreimageAbsent`), hash-bind to the fingerprinted `protocol_params_hash` (`BindMismatch`), and parse exactly (via the GREEN `protocol_params` parser) — fail-closed `ForgeCurrentPParamsError` otherwise.
- **GREEN (test-only pinning harness)** `ade_testkit::consensus::genesis_pinning` (NEW, S1) — a `#[cfg(test)]` genesis-consistency pinning harness. Reads the committed private-net Ade-as-leader reference fixture (S1b), builds the recovered seed-epoch surface, drives the **REAL** `bootstrap_initial_state` warm-start, and pins Ade's recovered values + leader-eligibility inputs against the genesis-derived reference. Non-authoritative test infrastructure; comparisons over observable/derived surfaces only (DC-COMPAT-01).
- **GREEN (epoch-boundary fail-closed guard)** `ade_node::node_sync::forge_epoch_admission` + closed `ForgeEpochAdmission` (NEW, S4) — a pure guard derived solely from `(slot, era_schedule, seed_epoch)` via the BLUE `EraSchedule::locate`. Within the seed epoch ⇒ `WithinSeedEpoch`; any other (or unlocatable) slot ⇒ `OffEpoch { … }`. `forge_one_from_recovered` calls it **before** `query_leader_schedule`, so an off-epoch forge fails closed before leadership / KES signing. DC-EPOCH-03.
- **RED (real operator-config ingress)** `ade_node::operator_forge` (EXTENDED, S2) — retires the `parse_simple_opcert_json` / `parse_simple_genesis_json` stubs **on the node path** and uses the real cardano-cli closed-contract parsers `ade_runtime::producer::opcert_envelope::parse_opcert_envelope` + `ade_runtime::producer::genesis_parser::parse_shelley_genesis` (the genesis reuse extracts clock/KES/network constants ONLY — never a starting-state source).
- **RED (forge-constant wiring)** `ade_node::node_lifecycle` (EXTENDED, S2/S3) — the `On`-arm `ForgeTick` consumes the checked clock guard (S3) and sources `protocol_version` + `pparams` from the recovered current ledger view (S2); `ForgeActivation` gains `last_slot_alignment_fail: Option<SlotAlignmentError>`. `run_relay_loop`'s containment is semantically unchanged.
- **RED (current-pparams install)** `ade_node::admission::{seed_to_snapshot, bootstrap}` (EXTENDED, S2a) — `build_seed_ledger` installs the supplied **current** `ProtocolParameters` (not `::default()`) into the recovered ledger at seed/import time; the forge-capable caller passes the oracle-bound parameters via `require_forge_current_pparams`. Warm-start preserves them.
- **NEW CI gate** `ci_check_genesis_consistency_fixture_present.sh` (S1, CE-G-A-1) — the three fixture files are committed + well-formed + Ade-as-leader (eta0 == genesis_hash_hex, ASC, non-empty pool_distribution, a VRF keyhash per pool), and **no secret key material** leaked into the committed fixture dir.
- **NEW CI gate** `ci_check_recovered_ledger_pparams_sourced.sh` (S2a, CE-G-A-2a) — the recovered ledger's `protocol_params` are sourced from the operator bundle's oracle preimage (`require_forge_current_pparams`) at the forge-capable seed import — never `ProtocolParameters::default()` / genesis-initial; fails closed if a future change reverts to a defaulted pparams.
- **NEW CI gate** `ci_check_node_forge_real_cli_ingress.sh` (S2, CE-G-A-2) — the `--mode node` operator-forge ingress loads config through the real `parse_opcert_envelope` + `parse_shelley_genesis` and retires the `parse_simple_*` stubs on the node path; fails closed if a simple-JSON parser is reintroduced there.
- **NEW CI gate** `ci_check_node_forge_single_epoch_fail_closed.sh` (S4, CE-G-A-4 / DC-EPOCH-03) — `forge_one_from_recovered` calls `forge_epoch_admission` **before** `query_leader_schedule`; the guard derives the candidate epoch via the BLUE `EraSchedule::locate` (no fabricated epoch math); the node forge path drives **no** `NonceInput::EpochBoundary` / `CandidateFreeze` nonce promotion. Fails closed if the guard is reordered after leadership, the epoch is fabricated, or a nonce roll is introduced.
- **Registry:** 313 rules at HEAD (311 → 313). **NEW `DC-EPOCH-03`** (single-epoch forge fail-closed on the `--mode node` spine) at `status = enforced` (`tests` + `ci_script` populated; `code_locus` cites `node_sync.rs` + `node_lifecycle.rs` + `run_loop_planner.rs` + `ade_core::consensus::nonce`). **NEW `DC-NODE-06`** (self-accept→serve handoff, shape B) at `status = declared` — a forward sketch for **PHASE4-N-F-G-B** (`tests`/`ci_script` empty; NOT enforced by G-A). Seven `strengthened_in += "PHASE4-N-F-G-A"` bumps (incl. CN-NODE-01, DC-NODE-05, and the cross-referenced family rules).

**Governance note.** G-A upholds the load-bearing structural lines. The forge stays **subordinate**: the GREEN planner still learns only whether a slot is *due* (content-blind `ForgeSlotStatus`), never who is a leader; the single recovered `BootstrapState` is the forge base (no second bootstrap, CN-NODE-01); the single durable tip-advance path remains `run_node_sync → pump_block`; the forge advances no tip and is **self-accept-only** (the N-F-E containment gate is byte-unchanged). The two new boundaries are **fail-closed walls, not silent saturations** — a before-anchor slot and an off-epoch slot are *errors*, never a stale-`start_slot` / stale-`eta0` sign. The S1 fixture is **evidence input, never runtime authority**. `protocol_params` carries **no float path** — rationals are exact integer arithmetic or fail closed. `DC-NODE-06` is a *declared* sketch (the serve handoff is the next sub-cluster G-B): a forward-declared rule keeps the invariant visible without claiming enforcement.

**Carried-forward (PHASE4-N-F-F, operator-key ingress + the forge-on flip).** GREEN `ade_node::forge_intent` (closed `ForgeIntent { On(ForgePaths), Off }`; pure tri-state `classify_forge_intent`; closed `ForgeIntentError::PartialKeySet`). RED `ade_node::operator_forge` (the single named operator-material ingress site; closed 6-variant `OperatorForgeError`; `OperatorForgeMaterial` not `Debug`/`Serialize`). RED `ade_node::node_lifecycle` (`--mode node` arm classifies forge intent and dispatches `None`/`Some(ForgeActivation)`; `NodeLifecycleError::ForgeKeyIngress`). CN-NODE-03 enforced. **G-A extends the `On` arm's `ForgeActivation` assembly + the `forge_one_from_recovered` fence; it does not alter the intent classifier or the custody-confined ingress surface.**

**Carried-forward (PHASE4-N-F-E, the forge-tick spine).** GREEN `ade_node::run_loop_planner` (closed 4-variant `LoopStep` incl. `ForgeTick`; content-blind `ForgeSlotStatus { Due, NotDue }`; pure `forge_slot_status` monotonic guard). RED `ade_node::node_lifecycle::run_relay_loop` (`forge: Option<&mut ForgeActivation>`; the `ForgeTick` branch derives the slot via the clock seam — **now `checked_millis_to_slot`, G-A S3** — reuses `CoordinatorState::kes_period_for_slot`, calls exactly one fenced `node_sync::forge_one_from_recovered`, advances NO durable tip, serves/admits/gossips nothing — self-accept-only, CE-E-4). `ade_node::node_sync::forge_one_from_recovered` (recovered `BootstrapState` → **G-A S4 `forge_epoch_admission` guard** → `PoolDistrView::from_seed_epoch_consensus_inputs` → `ForgeRequestContext` → `run_real_forge`). DC-NODE-05 enforced (strengthened by G-A). **G-A leaves `run_relay_loop`'s containment semantically unchanged.**

**Carried-forward (PHASE4-N-F-D, the run-loop spine).** The live RELAY run-loop: GREEN `ade_node::run_loop_planner` (pure `plan_loop_step` over the closed `LoopStep`), RED `ade_node::node_lifecycle::run_relay_loop`, and RED `ade_node::node_sync`'s content-blind `NodeBlockSource` readiness. `run_node_sync` is UNMODIFIED. Relay-only + hermetic; live unbounded peer remains the RO-LIVE-01 follow-on.

**Carried-forward (PHASE4-N-F-C, the node lifecycle).** RED `ade_node::node_lifecycle` (the single `PHASE4-N-F-C-LIFECYCLE-OWNER`), RED `ade_node::node_sync` (closed `NodeBlockSource`; `run_node_sync`; `forge_one_from_recovered`), GREEN `ade_node::ba02_evidence` (the BA-02 evidence correlator, library/unwired), the `Mode::Node` variant, and the RED `ade_runtime::chaindb` `list_seed_epoch_consensus_anchor_fps()` discovery surface — all unchanged in shape. CN-NODE-01 / DC-SYNC-01 / RO-LIVE-06.

**Carried-forward (PHASE4-N-F-A, still load-bearing below).** The recovered seed-epoch consensus-input CAPABILITY surface — **BLUE** `ade_ledger::seed_consensus_inputs` (closed `SeedEpochConsensusInputs` + SOLE codec, `SEED_CINPUT_SCHEMA_VERSION = 1`), the `PoolDistrView::from_seed_epoch_consensus_inputs` projection (`consensus_view`), the additive `WalEntry::SeedEpochConsensusInputsImported` (tag 3) + `ReplayOutcome`/`RecoveredBootstrapProvenance` (`wal`), **GREEN** `ade_runtime::seed_consensus_merge`, **RED** `ade_runtime::{seed_consensus_provenance, bootstrap, genesis_bootstrap, mithril_bootstrap}`, and the anchor-fp-keyed `chaindb` sidecar surface (redb `SCHEMA_VERSION = 3`) — is inventoried unchanged. **Carried-forward (PHASE4-N-Z / N-Y).** The Mithril production-bootstrap composition, forward-sync (`forward_sync::{reducer, pump}`), and the bootstrap-anchor / genesis-source BLUE types are unchanged in shape.

**Cluster-doc location.** The PHASE4-N-F-G-A cluster doc + slice docs (S1, S1b, S2a, S2, S3, S4) live under `docs/clusters/PHASE4-N-F-G-A/`; on close they archive under `docs/clusters/completed/`. Every prior closed cluster doc — the **PHASE4-N-F-A / N-F-C / N-F-D / N-F-E / N-F-F** sets, the entire **PHASE4-N-Q / N-R-\* / N-S-\*** set, the **N-M-\*** sub-trees, **N-O**, **N-P**, **N-T**, **N-V**, **N-W**, **N-X**, **N-Y**, **N-Z** — is archived under `docs/clusters/completed/`.

This regeneration is an **incremental, scoped refresh** of the prior full inventory (on-disk doc at the N-F-F close, HEAD `4eb7610`) to catch up through **PHASE4-N-F-G-A** (forge fidelity, closing now at `80dac1f7`), plus the mechanical counts that drifted. Unrelated module entries (every BLUE crate, and the N-Q/N-R/N-S/N-W/N-X/N-Y/N-Z/N-F-A/N-F-C/N-F-D/N-F-E/N-F-F surfaces) are carried forward unchanged.

### Counts (mechanical, with sources)

| Count | Value | Source / command |
|---|---|---|
| Crates | **11** | `grep -cE '"crates/' Cargo.toml` (`[workspace] members`). **Δ vs prior (11): 0.** G-A added no crate. |
| Canonical types | **456** | `grep -rhE "^(pub )?(struct\|enum) " --include='*.rs'` over the 6 BLUE crate `src/` trees + the 9 BLUE `ade_network` submodule paths, summed at `80dac1f7`. Breakdown: `ade_codec` 11, `ade_types` 81, `ade_crypto` 21, `ade_core` 49, `ade_ledger` **177**, `ade_plutus` 8 (= 347) + `ade_network` BLUE submodules 109 (`mux/frame.rs` 5, `codec/` 38, `handshake/` 9, `chain_sync/` 11, `block_fetch/` 10, `tx_submission/` 5, `keep_alive/` 5, `peer_sharing/` 5, `n2c/` 21). `canonical_type_registry: null`, so the structural grep is authoritative. **Δ vs prior CODEMAP (456): 0** — G-A touched **no BLUE crate** (every BLUE crate count is byte-for-byte unchanged). All G-A types (`SlotAlignmentError`, `ProtocolParamsParseError`, `ForgeCurrentPParamsError`, `ForgeEpochAdmission`) live in RED `ade_runtime` / `ade_node` and are not canonical-counted; `ProtocolParameters` / `Rational` are pre-existing BLUE types in `ade_ledger`. |
| Tests | **2213** | `grep -rEc "#\[test\]\|#\[tokio::test\]" --include='*.rs' crates/`, summed at `80dac1f7`. Approximate per the template fallback rule (count of attributes, not a runner enumeration). **Δ vs prior on-disk doc (2188): +25** — the G-A surface: `protocol_params.rs` (exact-rational parse + no-float + fail-closed tests), `clock.rs` (`checked_millis_to_slot_matches_millis_to_slot_when_aligned`, `checked_millis_to_slot_before_anchor_fails_closed`), `canonical.rs` (`require_forge_current_pparams` present/hash-mismatch tests), `genesis_pinning.rs` (the four S1 pinning tests), `node_sync.rs` (`forge_epoch_admission_*` + `node_forge_off_epoch_slot_fails_closed` + no-nonce-promotion), `node_lifecycle.rs` (`node_forge_protocol_version_and_pparams_from_recovered_current_view`, before-anchor fail-closed), and `seed_to_snapshot.rs` (S2a current-pparams install + warm-start preservation). |
| CI checks | **116** | `ls ci/ci_check_*.sh \| wc -l` at `80dac1f7`. CI lives entirely under `ci/` (`ci_dirs = ["ci"]`); there is no `.github/workflows/` invariant gate at HEAD. **Δ vs prior (112): +4** — the four **G-A** gates `ci_check_genesis_consistency_fixture_present.sh` (S1), `ci_check_recovered_ledger_pparams_sourced.sh` (S2a), `ci_check_node_forge_real_cli_ingress.sh` (S2), `ci_check_node_forge_single_epoch_fail_closed.sh` (S4). |
| Registry rules | **313** | `grep -cE "^id = " docs/ade-invariant-registry.toml` = 313 in the working tree; `git show HEAD:…` = **313 committed** (count stable). Reference only — not a header count. **Δ vs prior (311): +2** — the new `DC-EPOCH-03` (`status = enforced`) + the new `DC-NODE-06` (`status = declared`, a G-B forward sketch). Seven `strengthened_in += "PHASE4-N-F-G-A"` bumps. |

---

## BLUE Modules — Pure Functional Core

> **Shared header (applies to every BLUE entry below).** Every `.rs` source file begins with the `// Core Contract:` banner and the following deny attributes are present in each crate's `lib.rs` (or, for `ade_network`, at the crate root — BLUE submodules inherit them):
> `#![deny(unsafe_code)]`, `#![deny(clippy::unwrap_used)]`, `#![deny(clippy::expect_used)]`, `#![deny(clippy::panic)]`, `#![deny(clippy::float_arithmetic)]`.
>
> Cross-cutting BLUE CI gates (all scope the full BLUE set — 6 BLUE crates + 9 BLUE `ade_network` submodule paths):
> - `ci_check_module_headers.sh` — banner first-line check.
> - `ci_check_forbidden_patterns.sh` — `HashMap`/`HashSet`/`IndexMap`/`indexmap::`, `SystemTime`, `Instant`, `std::fs`, `std::net`, `tokio`, `async fn`, `f32`/`f64`, `anyhow`, `rand::thread_rng`, `thread::spawn`, `unsafe` outside an allowlist.
> - `ci_check_dependency_boundary.sh` — no BLUE crate depends on a RED crate.
> - `ci_check_no_signing_in_blue.sh` — signing operations forbidden in BLUE.
> - `ci_check_no_semantic_cfg.sh` — `#[cfg(feature = …)]` / `cfg!(feature = …)` forbidden in BLUE.
> - `ci_check_hash_uses_wire_bytes.sh` — no hashing of re-encoded bytes in BLUE.
> - `ci_check_ingress_chokepoints.sh` — only named `decode_*` chokepoints construct `PreservedCbor`.
> - `ci_check_pallas_quarantine.sh` — `pallas-*` confined to `ade_plutus`.
> - `ci_check_no_async_in_blue.sh` — async constructs forbidden in BLUE (DC-CORE-01).
>
> Narrow BLUE gates: `ci_check_no_chaindb_in_consensus_blue.sh`, `ci_check_no_float_in_consensus.sh`, `ci_check_consensus_closed_enums.sh`, `ci_check_no_density_in_fork_choice.sh` (DC-CONS-03), `ci_check_deposit_param_authority.sh` (DC-TXV-07), `ci_check_gov_cert_accumulation_closed.sh` (DC-LEDGER-09), `ci_check_credential_discriminant_closed.sh` (DC-LEDGER-10), `ci_check_proposal_procedures_closed.sh` (DC-LEDGER-11), `ci_check_conway_cert_classification_closed.sh`, the N-C producer-authority gate set, the N-G server-role gate set, the N-H receive gate set, the N-I/N-J rollback+snapshot gate set, the mempool-ingress gate set, the N-P KES gate, the N-R/N-S/N-V BLUE gates, the N-W gate `ci_check_producer_praos_vrf.sh` (CN-FORGE-04), the N-X gate `ci_check_tag24_wire_authority.sh` (CN-WIRE-08), the N-Y gates `ci_check_forward_sync_chokepoint_only.sh` (DC-SYNC-01), `ci_check_mithril_uses_bootstrap_initial_state.sh` (CN-MITHRIL-01), `ci_check_no_haskell_fingerprint_equality.sh` (DC-COMPAT-01), the workspace-wide `ci_check_registry_code_locus_exists.sh`, the N-Z `ci_check_mithril_seed_point_independence.sh` (DC-MITHRIL-02), the N-F-A `ci_check_consensus_input_provenance.sh` (CN-CINPUT-02), the N-F-C node-lifecycle gates `ci_check_lifecycle_owner_uses_bootstrap_initial_state.sh` (CN-NODE-01), `ci_check_node_sync_via_pump.sh` (DC-SYNC-01), `ci_check_ba02_evidence_closed.sh` (RO-LIVE-06), the N-F-D run-loop gates `ci_check_loop_planner_closed.sh` + `ci_check_node_run_loop_containment.sh` (extended in place by N-F-E; DC-NODE-05 / CN-NODE-02 / CE-E-4), and the N-F-F operator-key-ingress gates `ci_check_forge_intent_closed.sh` + `ci_check_operator_forge_no_secret_leak.sh` (CN-NODE-03) with `ci_check_node_binary_uses_single_bootstrap.sh` (CN-NODE-01, `ReceiveState::new` owner allow-list). **N-F-G-A added** four forge-fidelity gates: `ci_check_genesis_consistency_fixture_present.sh` (S1, CE-G-A-1 — committed Ade-as-leader fixture, no leaked secret), `ci_check_recovered_ledger_pparams_sourced.sh` (S2a, CE-G-A-2a — recovered-ledger pparams sourced from the oracle preimage, never defaulted), `ci_check_node_forge_real_cli_ingress.sh` (S2, CE-G-A-2 — real `parse_opcert_envelope` + `parse_shelley_genesis` on the node path, `parse_simple_*` retired), and `ci_check_node_forge_single_epoch_fail_closed.sh` (S4, CE-G-A-4 / DC-EPOCH-03 — off-epoch fail-closed before leadership, no nonce promotion).

---

### `ade_codec`

| Attribute | Value |
|-----------|-------|
| **Purpose** | Owns Cardano-canonical CBOR ingress — the only place in the workspace that turns raw bytes into typed semantic values, with wire-byte preservation for every hash-bearing structure. Owns the standalone opcert byte authority and the canonical Conway-tx preserved-byte splitter, the canonical block-envelope encoder `cbor::envelope::encode_block_envelope`, and (N-X) the single workspace **tag-24 CBOR-in-CBOR wire-envelope authority** `cbor::tag24::{wrap_tag24, unwrap_tag24}` + closed `TagEnvelopeError`. Also exposes the shared canonical CBOR read/write primitives (`cbor::{read_array_header, read_map_header, read_uint, read_bytes, write_*_canonical, canonical_width, ContainerEncoding, IntWidth}`) the BLUE codecs in `ade_ledger` (incl. the `SeedEpochConsensusInputs` + WAL codecs, and the `LiveConsensusInputsCanonical` 15-field fingerprint encoder in `ade_runtime`) build on. |
| **Creates** | `PreservedCbor<T>`, `RawCbor`, `BlockEnvelope`, `ByronDecodedBlock`, `CodecContext`, `CodecError` (incl. `UnknownCertTag`, `DuplicateMapKey`, `TrailingBytes`, `InvalidCborStructure`), `ContainerEncoding`, `IntWidth`, era-tagged block/tx wrappers, `OpCertCodecError`, `TxComponents<'a>`, `TagEnvelopeError` (closed: `NotTag24`, `NotByteString`, `Truncated`, `TrailingBytes`). Functions: `conway::cert::{decode_conway_certs, decode_drep}`, `conway::withdrawals::{decode_withdrawals, withdrawals_sum}`, `conway::governance::{decode,encode}_proposal_procedures`, `shelley::opcert::{encode_opcert, decode_opcert}`, `shelley::tx_components::split_conway_tx_components`, `cbor::envelope::{decode_block_envelope, encode_block_envelope}`, `cbor::tag24::{wrap_tag24, unwrap_tag24}`. |
| **Interprets** | All canonical Cardano CBOR — block envelopes (`[era, block]`), era-specific blocks, tx bodies/outs, certificates, addresses. `unwrap_tag24` is the single workspace authority that strips a tag-24 (`0xd8 0x18`) envelope, returning a zero-copy borrow of the verbatim inner bytes (no re-encode) and failing closed with a typed `TagEnvelopeError`. `cbor::mod::{read_bytes, read_text, skip_item}` fail-close (no panic) on an overflowing declared length via `checked_add`. |
| **MUST NOT** | (1) Construct `PreservedCbor` outside `ade_codec` (`pub(crate)` + `ci_check_ingress_chokepoints.sh`). (2) Re-encode wire bytes when computing hashes. (3) Use any forbidden BLUE pattern. (4) Depend on any workspace crate except `ade_types`. (5) `conway::cert` (DC-LEDGER-08) — no unknown-tag swallow. (6) `conway::withdrawals` — no last-wins on duplicate `RewardAccount`. (7) `decode_stake_credential` (DC-LEDGER-10) — must not erase the credential tag. (8) `conway::governance` (DC-LEDGER-11) — no silent skip on unknown `GovAction`. (9) `shelley::opcert` (DC-CONS-11/12). (10) `shelley::tx_components` (DC-CONS-13/16). (11) `encode_block_envelope` (CN-FORGE-03) — the workspace's single block-envelope encoder; no parallel serializer. (12) `cbor::tag24` (CN-WIRE-08) — `wrap_tag24`/`unwrap_tag24` defined exactly once; `unwrap_tag24` fails closed (typed `TagEnvelopeError`, never panic); inner bytes copied verbatim / zero-copy borrowed; `read_bytes`/`read_text`/`skip_item` reject an overflowing declared length without panic (`ci_check_tag24_wire_authority.sh`). |
| **Inbound deps** | `ade_ledger` (heavy), `ade_plutus`, `ade_testkit`, `ade_network`, `ade_runtime`, `ade_core_interop`, `ade_node`. |
| **Outbound deps** | `ade_types`. std-only; dev-deps `serde_json`, `toml`. |
| **Entry points** | `ade_codec::cbor::envelope::{decode_block_envelope, encode_block_envelope}`, `ade_codec::{wrap_tag24, unwrap_tag24, TagEnvelopeError}` (crate-root re-exports), `ade_codec::cbor::{read_array_header, read_map_header, read_uint, read_bytes, write_uint_canonical, write_bytes_canonical, write_array_header, write_map_header, canonical_width, ContainerEncoding, IntWidth}`, `ade_codec::traits::AdeEncode`, `ade_codec::CodecContext`, per-era `decode_*_block`, `ade_codec::address::decode_address`, `ade_codec::conway::tx::decode_conway_tx_body`, `ade_codec::conway::cert::decode_conway_certs`, `ade_codec::conway::governance::{decode,encode}_proposal_procedures`, `ade_codec::shelley::opcert::{encode_opcert, decode_opcert}`, `ade_codec::shelley::tx_components::split_conway_tx_components`. |
| **Key modules** | `cbor/` (incl. `envelope.rs`, `tag24.rs`, `mod.rs`), `byron/`, `shelley/` (incl. `cert.rs`, `opcert.rs`, `tx_components.rs`), `allegra/`, `mary/`, `alonzo/`, `babbage/`, `conway/`, `address/`, `preserved.rs`, `traits.rs`, `primitives.rs`, `error.rs`. |

---

### `ade_core`

| Attribute | Value |
|-----------|-------|
| **Purpose** | BLUE authoritative Praos consensus core. Owns canonical types and pure state-transitions deciding which header/chain Ade accepts: HFC era schedule + slot↔era↔time (incl. `EraSchedule::locate` — the BLUE epoch locator the G-A `forge_epoch_admission` guard derives the candidate epoch from), Praos chain-dep state, VRF cert verification + leader-eligibility, KES + op-cert period verification, header validation, fork-choice, header-level rollback, leader-schedule query, canonical encodings, producer-side opcert acceptance, the closed BLUE leader-check evaluator (`consensus::leader_check`, N-R-A), and the single era→leader-VRF-input authority `consensus::vrf_cert::{ExpectedVrfInput, leader_vrf_input, leader_value_for}` (N-W). The `consensus::nonce` epoch-boundary transitions (`CandidateFreeze` / `EpochBoundary`) exist but are **undriven on the `--mode node` forge path** (DC-EPOCH-03). |
| **Creates** | **Schedule:** `BootstrapAnchorHash`, `EraSchedule`, `EraSummary`, `EraLocation`. **State:** `PraosChainDepState`, `OpCertCounterMap`, `Nonce`. **Events/points:** `Point`, `ChainHash`, `BlockDistance`, `SecurityParam`, `ChainEvent`, `ChainSelectionReject`. **Errors:** `HFCError`, `SlotTimeError`, `HeaderValidationError`, `VrfCertError`, `OpCertCounterError`, `NonceEvolutionError`, `LeaderScheduleError`, `OpCertError`. **Header surface:** `HeaderInput`, `HeaderVrf`, `HeaderKes`, `ValidatedHeaderSummary`, `HeaderApplied`. **Fork-choice:** `TiebreakerView`, `CandidateFragment`, `ChainSelectorState`, `ForkChoiceError`. **VRF/leader:** `VrfRole`, `VerifiedVrf`, `StakeFraction`, `ActiveSlotsCoeff`, `LeaderScheduleQuery`, `LeaderScheduleAnswer`, `ExpectedVrfInput` (closed 2-variant era-discriminated leader-VRF-input enum). **Boundary:** `LedgerView` trait. **Rollback:** `RollBackRequest`, `RollBackApplied`. **`leader_check`:** `LeaderCheckVerdict`, `LeaderCheckError`, `VrfOutputFingerprint`, `LeaderProofFingerprint`. 49 public types. |
| **Interprets** | Canonical inputs from the `ade_runtime` shell. KES check verifies the hot-KES signature via `ade_crypto::kes::verify_kes`. `leader_vrf_input(era, slot, eta0)` constructs the era-correct alpha; `leader_value_for` applies the era-correct range-extension. `leader_check::verify_and_evaluate_leader` consumes only public-key material + canonical inputs and fail-closes on full-enum `ExpectedVrfInput` mismatch. `EraSchedule::locate(slot)` resolves the era/epoch for a slot (consumed by the G-A epoch guard). `opcert_validate` consumes an `OperationalCert`, cold key, expected period, prev counter. |
| **MUST NOT** | (1)–(14) consensus carry-forward (no I/O, no clock, no `ChainDb` in BLUE consensus, no density in fork-choice, closed consensus enums, etc.). (15) `consensus::leader_check` (CN-FORGE-02) — MUST NOT depend on `LedgerView`/`EraSchedule`/`ChainDepState`/wall-clock/storage/any RED crate; MUST NOT observe `KesSecret`/`VrfSigningKey`/`ColdSigningKey`; `NotEligible` carries only a bounded fingerprint. (16) `consensus::vrf_cert` leader-VRF authority (CN-FORGE-04) — `leader_vrf_input` is the single era→construction authority; no path accepts both TPraos and Praos inputs for one era; Praos producer alpha = validator alpha (`ci_check_producer_praos_vrf.sh`). |
| **Inbound deps** | `ade_ledger`, `ade_runtime` (heavy), `ade_testkit`, `ade_core_interop`, `ade_node`. |
| **Outbound deps** | `ade_types`, `ade_crypto`, `minicbor`. Dev-deps: `ade_testkit`, `serde_json`, `cardano-crypto`. |
| **Entry points** | `use ade_core::consensus::{…}` aggregator; `validate_and_apply_header`, `select_best_chain`, `apply_rollback`, `apply_nonce_input`, `apply_op_cert`, `query_leader_schedule`, `verify_vrf_cert`, `tiebreaker_prefer`; `ade_core::consensus::era_schedule::EraSchedule::locate`; `ade_core::consensus::opcert_validate::opcert_validate`; `ade_core::consensus::leader_check::{verify_and_evaluate_leader, LeaderCheckVerdict, is_leader_for_vrf_output}`; `ade_core::consensus::vrf_cert::{ExpectedVrfInput, ActiveSlotsCoeff, leader_vrf_input, leader_value_for, praos_vrf_input, praos_leader_value}`. |
| **Key modules** | `consensus/{era_schedule, praos_state, events, errors, vrf_cert, kes_check, nonce, op_cert, leader_schedule, header_summary, header_validate, candidate, fork_choice, rollback, ledger_view, encoding, opcert_validate, leader_check}.rs`. |

---

### `ade_crypto`

| Attribute | Value |
|-----------|-------|
| **Purpose** | Pure cryptographic verification primitives — Blake2b hashing, Ed25519 / Byron-bootstrap signature verification, KES verification with operational certificates, VRF verification, plus closed signature-artifact types. Verification only — signing lives in RED `ade_runtime::producer::signing`. Also owns the Ade-native `Sum6KES Ed25519DSIGN` algorithm (`kes_sum/`), byte-identical to Haskell `cardano-base`. `blake2b_256` is the function the sidecar-provenance helper hashes the canonical sidecar bytes with (and the G-A `require_forge_current_pparams` hash-binds the `protocol_params_json` preimage with); `blake2b_224` derives the operator `pool_id` from the cold verification key in the N-F-F ingress. |
| **Creates** | `Blake2b224`, `Blake2b256`, `HashAlgorithm`, `Ed25519VerificationKey`, `Ed25519Signature`, `ByronExtendedVerificationKey`, `KesVerificationKey`, `KesPeriod`, `OperationalCertData`, `VrfVerificationKey`, `VrfProof`, `VrfOutput`, `CryptoError`, `KesSignature`, `SUM6_KES_SIG_LEN = 448`. **`kes_sum::`** (BLUE): `KesAlgorithm` trait, `Sum0Kes`, `Sum0SigningKey`, `Sum0Signature`, `SumKes<D>`, `SumSigningKey<D>`, `SumSignature<D>`, aliases `Sum1Kes..Sum6Kes`, closed `KesError`, closed `KesParseError`. |
| **Interprets** | Already-decoded byte slices. `Sum6Kes::raw_deserialize_signing_key_kes` interprets the canonical 608-byte expanded cardano-cli skey; `raw_deserialize_signature_kes` the 448-byte signature; `period_from_zeroed_sum6_tree_shape` infers the current period from tree shape. |
| **MUST NOT** | (1) Implement signing as a top-level `pub fn sign_*` (`ci_check_no_signing_in_blue.sh`). (2) Allocate global state. (3) Use any BLUE-forbidden pattern. (4) `unsafe` outside the allowlisted FFI in `vrf.rs`. (5) `build_opcert_signable` must produce the spec-correct concat. (6) `KesSignature` (DC-CRYPTO-04) — closed length-pinned wrapper, `from_bytes` only, redacting `Debug`. (7) `kes_sum` (DC-CRYPTO-08) — MUST NOT import `cardano_crypto::kes::*` in production paths (`#[cfg(test)]` oracle only; `ci_check_kes_sum_compatibility.sh` Guard 3). (8) `kes_sum::hash::expand_seed` — Haskell prefix bytes `0x01`/`0x02`. (9) every `*SigningKey` hand-rolls zeroizing `Drop` + redacting `Debug` + no public byte accessors. (10) `kes_sum` corpus — throwaway-fixture comment; no `.skey` files (Guards 1, 2). (11) error variants carry only non-secret primitives. |
| **Inbound deps** | `ade_core`, `ade_ledger`, `ade_plutus`, `ade_testkit`, `ade_core_interop`, `ade_runtime`, `ade_node`. |
| **Outbound deps** | `ade_types`, `blake2`, `ed25519-dalek`, `cardano-crypto` (`["vrf-draft03", "kes-sum", "dsign"]`, `default-features = false`; `kes-sum` retained for `#[cfg(test)]` oracle only). |
| **Entry points** | `ade_crypto::blake2b::*`, `ade_crypto::{blake2b_256, blake2b_224}`, `verify_ed25519`, `verify_byron_bootstrap`, `verify_kes`, `verify_opcert`, `verify_vrf`, `ade_crypto::kes::{KesSignature, SUM6_KES_SIG_LEN, KesVerificationKey}`, `ade_crypto::kes_sum::{KesAlgorithm, Sum6Kes, KesError, KesParseError, period_from_zeroed_sum6_tree_shape}`. |
| **Key modules** | `blake2b.rs`, `ed25519.rs`, `error.rs`, `kes.rs`, `kes_sum/` (`mod`, `single`, `sum`, `hash`, `errors`, `period`, `cardano_cli_corpus` `#[cfg(test)]`, `tests` `#[cfg(test)]`), `traits.rs`, `vrf.rs`. |

---
### `ade_ledger`

| Attribute | Value |
|-----------|-------|
| **Purpose** | The functional core (ledger half): stateless ledger rules for every era; B1 block-validity verdict; B2 transaction-validity verdict + mempool admission; B3 Conway value-conservation; B4/B5 closed Conway cert-state + governance-cert accumulation; live committee enactment; the single BLUE wire-ingress chokepoint `mempool::ingress::mempool_ingress`; the BLUE producer authority (`producer::{forge, self_accept, served_chain, state}`); the single canonical body-hash authority (`block_body_hash`); the BLUE receive-side bridge (`receive::*`); the BLUE rollback authority (`rollback::*`); the BLUE canonical snapshot encoder/decoder (`snapshot::*`); the BLUE Ade-native WAL replay+event authority (`wal::*`); the BLUE bootstrap-anchor + Mithril-binding types (`bootstrap_anchor::*`); the BLUE Conway-genesis→initial-state transform (`genesis_source`, N-Y); the canonical KES-signing pre-image recipe `block_validity::unsigned_header_pre_image`; the BLUE exact-`Rational` arithmetic (`rational`, consumed by the G-A `protocol_params` parser); the BLUE `ProtocolParameters` model (`pparams`, installed current at seed/import by G-A S2a); and (N-F-A) the BLUE closed `SeedEpochConsensusInputs` recovered-state record + its SOLE codec (`seed_consensus_inputs`), the recovered-surface `PoolDistrView::from_seed_epoch_consensus_inputs` projection (`consensus_view`), and the additive WAL provenance variant + `ReplayOutcome`/`RecoveredBootstrapProvenance` (`wal`). |
| **Creates** | ~177 public types. Load-bearing: `BlockValidityError`, `BlockVerdict`, `TxValidity*`, `MempoolState`, `ProducerTick`, `ForgedBlock`, `ForgeError`, `ForgeEffects`, `ServedChainSnapshot`, `ServedChainAdmitError`, `PoolDistrView`, `PoolEntry`, `LedgerState`, `ProtocolParameters` (the `pparams` model), `Rational` (the exact-rational arithmetic), `WalEntry` (closed sum: `AdmitBlock` + `SeedEpochConsensusInputsImported` at tag 3), `BlockVerdictTag`, `RecoveredBootstrapProvenance`, `ReplayOutcome`, `WalError` (incl. `DuplicateProvenance`, `ProvenanceAnchorMismatch`), `SeedEpochConsensusInputs` (closed; `anchor_fp`/`epoch_no`/`active_slots_coeff`/`total_active_stake`/`pool_distribution: BTreeMap<Hash28, PoolEntry>`; `SEED_CINPUT_SCHEMA_VERSION = 1`), `SeedConsensusInputsError` (closed: `MalformedCbor`/`UnknownVersion`/`Structural`/`NonCanonicalMapOrder`/`DuplicatePoolKey`/`TrailingBytes`), `BootstrapAnchor`, `SeedProvenance` (closed: `CardanoCliJson` / `Mithril { … }`), `SeedPoint`, `ANCHOR_SCHEMA_VERSION = 2`, `MithrilManifestReport`, `MithrilImportError`, `BootstrapAnchorError`, `ConwayGenesisConfig`, `GenesisSourceError` (closed: `NonConwayEra`), `UnsignedHeaderPreImage`, `UnsignedHeaderPreImageError`. |
| **Interprets** | Canonical decoded blocks/txs/certs/snapshots; `mempool_ingress` is the sole BLUE chokepoint from wire ingress to `admit`; `self_accept` re-validates a forged artifact; `served_chain_admit` derives the served index; `decode_bootstrap_anchor` reads the version-gated `ANCHOR_SCHEMA_VERSION = 2` CBOR framing; `verify_mithril_binding(report, anchor)` cross-checks the Mithril manifest against the anchor; `genesis_initial_state(&ConwayGenesisConfig)` produces the cold-start pair; `unsigned_header_pre_image` produces the canonical CBOR of `ShelleyHeaderBody`. `decode_seed_epoch_consensus_inputs` is the SOLE decoder of the closed sidecar record; `replay_from_anchor` folds the WAL into a `ReplayOutcome`. |
| **MUST NOT** | All carry-forward ledger prohibitions (no I/O, no clock, closed cert/governance enums, single body-hash recipe, etc.). **`producer::forge` (CN-FORGE-01/03):** emit exactly one of `ForgeSucceeded`/`ForgeNotLeader`/`ForgeFailed`; no `ForgeSucceeded` unless `self_accept` accepts; wrap output via `ade_codec::encode_block_envelope`; no parallel forge codepath. **`producer::served_chain` (CN-PROD-04):** only self-accepted blocks admitted; sole entry `served_chain_admit`. **`block_validity::unsigned_header_pre_image` (CN-KES-HEADER-01 / DC-KES-HEADER-01):** branded `UnsignedHeaderPreImage`'s only constructor is the canonical recipe; byte-identical to the validator-side extractor; pure. **`bootstrap_anchor` (CN-ANCHOR-01 / DC-ANCHOR-01):** `ANCHOR_SCHEMA_VERSION` version-gated; the `SeedProvenance` enum is closed. **`bootstrap_anchor::binding` (CN-MITHRIL-01 / DC-MITHRIL-01):** `verify_mithril_binding` is a pure fail-closed predicate, never re-verifies the STM multisig. **`genesis_source` (DC-GENESIS-SRC-01):** `genesis_initial_state` is Conway-only (fail-closed `NonConwayEra`). **`seed_consensus_inputs` (CN-CINPUT-01):** `SeedEpochConsensusInputs` is a single closed canonical type with a SOLE encoder/decoder pair; deterministic CBOR, `BTreeMap`-ordered, version-gated, byte-canonical; no `Default`, no `#[non_exhaustive]`; no I/O / clock / float. **`wal` provenance (DC-CINPUT-01):** the `SeedEpochConsensusInputsImported` variant stays distinct from the `AdmitBlock` chain; `replay_from_anchor` allows at most one provenance entry and fails closed on duplicate / anchor mismatch; remains pure. **`consensus_view::from_seed_epoch_consensus_inputs` (DC-CINPUT-02a):** a pure BLUE field-map preserving single-epoch semantics (off-epoch queries return `None`); no I/O / clock / `HashMap` — it is the *projection* the forge handoff (`node_sync::forge_one_from_recovered`) consumes. **`rational` / `pparams`:** stay pure BLUE types (no I/O, no float arithmetic in `rational` — exact integer arithmetic only). |
| **Inbound deps** | `ade_testkit`, `ade_core_interop`, `ade_runtime`, `ade_node`. |
| **Outbound deps** | `ade_types`, `ade_crypto`, `ade_codec`, `ade_plutus`, `ade_core`, `minicbor`, `num-bigint`, `num-integer`, `num-traits`. Dev-dep: `ade_testkit`. |
| **Entry points** | `ade_ledger::block_validity::{decode_block, validate_block, unsigned_header_pre_image::unsigned_header_pre_image}`, `ade_ledger::tx_validity::*`, `ade_ledger::mempool::{ingress::mempool_ingress, admit::*}`, `ade_ledger::producer::{forge::forge_block, self_accept::self_accept, served_chain::{ServedChainSnapshot, served_chain_admit}}`, `ade_ledger::block_body_hash::*`, `ade_ledger::receive::reducer::*`, `ade_ledger::rollback::*`, `ade_ledger::snapshot::*`, `ade_ledger::pparams::ProtocolParameters`, `ade_ledger::rational::Rational`, `ade_ledger::wal::{replay_from_anchor, ReplayOutcome, RecoveredBootstrapProvenance, WalEntry, WalError, encode_wal_entry, decode_wal_entry, WalStore}`, `ade_ledger::seed_consensus_inputs::{SeedEpochConsensusInputs, encode_seed_epoch_consensus_inputs, decode_seed_epoch_consensus_inputs, SeedConsensusInputsError, SEED_CINPUT_SCHEMA_VERSION}`, `ade_ledger::consensus_view::{PoolDistrView, PoolEntry}` (incl. `PoolDistrView::{new, from_seed_epoch_consensus_inputs}`), `ade_ledger::bootstrap_anchor::{encode_bootstrap_anchor, decode_bootstrap_anchor, BootstrapAnchor, SeedProvenance, verify_mithril_binding, MithrilManifestReport, MithrilImportError}`, `ade_ledger::genesis_source::{genesis_initial_state, ConwayGenesisConfig, GenesisSourceError}`. |
| **Key modules** | `block_validity/` (incl. `unsigned_header_pre_image.rs`, `header_input.rs`, `transition.rs`, `verdict.rs`), `tx_validity/`, `mempool/`, `cert_classify/`, `consensus_view.rs` (incl. `PoolDistrView::from_seed_epoch_consensus_inputs` — consumed by the N-F-C/N-F-E/N-F-F/N-F-G-A lifecycle forge handoff `node_sync::forge_one_from_recovered`), `producer/` (`forge`, `self_accept`, `served_chain`, `state`), `block_body_hash.rs`, `receive/`, `rollback/`, `snapshot/`, `pparams.rs` (the `ProtocolParameters` model), `rational.rs` (exact-rational arithmetic, consumed by `ade_runtime::consensus_inputs::protocol_params`), `wal/` (`mod`, `error`, `event`, `replay`, `store_trait`), `seed_consensus_inputs.rs`, `bootstrap_anchor/` (`mod`, `anchor`, `binding`, `error`), `genesis_source.rs`. |

> **GREEN sub-classification:** `ade_ledger::mempool::{policy, canonicalize}` are GREEN-by-content. **RED sub-classification:** `ade_ledger::consensus_input_extract` is RED-by-content. Both carry their own module banner.

---

### `ade_network` *(BLUE submodules)*

| Attribute | Value |
|-----------|-------|
| **Purpose** | Owns the Cardano Ouroboros mini-protocol authority — the closed wire grammar (CBOR codecs) and pure state machines for all N2N + N2C mini-protocols, plus the BLUE mux frame primitive and the producer-side server-role reducers. Owns the per-protocol tag-24 **composition** authorities layered over `ade_codec::{wrap_tag24, unwrap_tag24}` — `codec::block_fetch::{compose,decompose}_blockfetch_block` (era **inside** the wrap; Conway = storage index 7) and `codec::chain_sync::{compose,decompose}_rollforward_header` + `chain_sync_wire_era_index` (era_tag **outside** the wrap; CONSENSUS era index Conway = 6). |
| **Creates** | 109 BLUE canonical types: `mux/frame.rs` 5, `codec/` 38, `handshake/` 9, `chain_sync/` 11, `block_fetch/` 10, `tx_submission/` 5, `keep_alive/` 5, `peer_sharing/` 5, `n2c/` 21. `chain_sync::server::HeaderProjection` carries an `era: CardanoEra` field. |
| **Interprets** | Mini-protocol wire frames; `mux::frame::decode_frame` is the single frame decode authority; per-protocol `*_transition` reducers consume decoded messages. `decompose_blockfetch_block` / `decompose_rollforward_header` strip the per-protocol tag-24 composition (delegating the inner unwrap to `ade_codec::unwrap_tag24`). |
| **MUST NOT** | (1) `mux::frame` — single `encode_frame`/`decode_frame` pair. (2) handshake — single `n2n_transition` + single `n2c_transition`. (3) closed `AcceptedMiniProtocol` registry. (4) no socket I/O in BLUE submodules. (5) no async/tokio in BLUE submodules. (6) depend on no workspace crate beyond `ade_codec` + `ade_types`. (7) server reducers MUST NOT split headers in parallel or depend on signing. (8) tag-24 composition (CN-WIRE-08) — composers delegate byte-level wrap/strip to the single `ade_codec` authority; a served BlockFetch `MsgBlock` payload is `tag24(bytes([era, block]))` and a served ChainSync `RollForward` header is `[era_tag, tag24(bytes(header_cbor))]`; both pinned byte-identically against captured cardano-node 11.0.1 fixtures (`ci_check_tag24_wire_authority.sh`). |
| **Inbound deps** | `ade_runtime`, `ade_node`, `ade_core_interop`, `ade_testkit` (and the `ade_network` GREEN/RED submodules). |
| **Outbound deps** | `ade_types`, `ade_codec`. No external deps in BLUE submodules. |
| **Entry points** | `ade_network::mux::frame::{encode_frame, decode_frame}`, `ade_network::handshake::{n2n_transition, n2c_transition}`, `ade_network::chain_sync::{server::*, client::*}`, `ade_network::block_fetch::{server::*, client::*}`, `ade_network::codec::block_fetch::{compose,decompose}_blockfetch_block`, `ade_network::codec::chain_sync::{compose,decompose}_rollforward_header, chain_sync_wire_era_index}`, `ade_network::{tx_submission, keep_alive, peer_sharing, n2c}::*`. |
| **Key modules** | `mux/frame.rs`, `codec/` (incl. `block_fetch.rs`, `chain_sync.rs`), `handshake/`, `chain_sync/` (incl. `server.rs`), `block_fetch/` (incl. `server.rs`), `tx_submission/`, `keep_alive/`, `peer_sharing/`, `n2c/`. |

---

### `ade_plutus`

| Attribute | Value |
|-----------|-------|
| **Purpose** | Quarantine boundary between the Ade-canonical ledger and the ported UPLC evaluator from `aiken-lang/aiken` (pinned tag `v1.1.21`). |
| **Creates** | `PlutusScript`, `PlutusLanguage`, `EvalOutput`, `PlutusError`, `CostModels`, `DecoderMode`, `PerScriptResult`, `TxEvalResult`. |
| **Interprets** | UPLC scripts (Plutus V1/V2/V3) and `CostModels` CBOR; phase-two tx evaluation. `PlutusScript::from_cbor` is a named ingress chokepoint. |
| **MUST NOT** | (1) Re-export any `pallas_*` or `aiken_uplc::` type. (2) Let another BLUE crate bypass the canonical entry. (3) Activate PV11 builtins. (4) Use any BLUE-forbidden pattern. (5) Construct `PreservedCbor` outside `ade_codec`. |
| **Inbound deps** | `ade_ledger`, `ade_testkit`. |
| **Outbound deps** | `ade_types`, `ade_crypto`, `ade_codec`, `aiken_uplc` (git, tag `v1.1.21`), `pallas-primitives` (internal-only). |
| **Entry points** | `ade_plutus::eval_tx_phase_two`, `ade_plutus::tx_eval::*`, `ade_plutus::evaluator::*`, `ade_plutus::cost_model::*`. |
| **Key modules** | `evaluator.rs`, `cost_model.rs`, `script_context.rs`, `script_verdict.rs`, `tx_eval.rs`. |

---

### `ade_types`

| Attribute | Value |
|-----------|-------|
| **Purpose** | Owns the canonical Cardano domain vocabulary — primitives, era enum, address forms, era-tagged tx bodies/outputs/certificates, governance types — the lingua franca for every other crate. `CardanoEra` carries the `is_praos()` classifier (Babbage + Conway). `Hash28`/`Hash32`/`EpochNo`/`SlotNo` are the primitives the recovered-state record, the forge-slot guard, the G-A epoch guard, and the N-F-F operator `pool_id` are keyed/typed on. `ProtocolVersion` is the value the G-A `ForgeActivation` carries from the recovered current ledger view. |
| **Creates** | `CardanoEra` (with `is_praos()`), `SlotNo`, `BlockNo`, `EpochNo`, `Hash28`, `Hash32`, `Coin`, `Lovelace`, `NetworkId`, `Nonce`, `TxIn`, `RewardAccount`, `PoolId`, `Address`, `ByronAddress`, `Credential`, `StakeCredential`, `Certificate`, `PoolRegistrationCert`, `ConwayCert`, `MIRCert`, `DRep`, `GovAction`, `GovActionState`, `GovActionId`, `Anchor`, `ProposalProcedure`, `OperationalCert`, `NativeScript`, `Datum`, `MultiAsset`, `AssetName`, `CostModel`, `ExUnits`, plus per-era tx-body/tx-out/witness wrappers and `ShelleyBlock`/`ShelleyHeader`/`VrfData`/`ProtocolVersion`. |
| **Interprets** | None — produce-only. |
| **MUST NOT** | (1) Construct or decode `PreservedCbor`. (2) Use any BLUE-forbidden pattern. (3) Depend on any workspace crate. (4) Add open/extensible variants to closed enums without a versioned gate (`CardanoEra` discriminants are closed: Byron=0 … Conway=7; `is_praos()` classifies exactly {Babbage, Conway}). |
| **Inbound deps** | Every other workspace crate. |
| **Outbound deps** | None. |
| **Entry points** | `ade_types::CardanoEra` (incl. `is_praos`, `is_byron`, `as_u8`), `ade_types::tx::{Coin, TxIn, RewardAccount}`, `ade_types::{Hash32, SlotNo, Hash28, BlockNo, EpochNo}`, `ade_types::conway::{tx::ConwayTxBody, cert::*, governance::{ProposalProcedure, GovAction, GovActionId}}`, `ade_types::shelley::block::{OperationalCert, ProtocolVersion, ShelleyHeader, ShelleyBlock, VrfData}`. |
| **Key modules** | `primitives.rs`, `era.rs`, `tx.rs`, `address/`, `byron/`, `shelley/`, `allegra/`, `mary/`, `alonzo/`, `babbage/`, `conway/`. |

---
## GREEN Modules — Deterministic Glue

> Deterministic, non-authoritative. May depend on BLUE; must not affect authoritative outputs. GREEN sub-trees inside the RED `ade_runtime` and `ade_node` crates carry a `//! GREEN …` banner + the same deny attributes and are CI-gated for purity.

### `ade_runtime::consensus_inputs::protocol_params` *(GREEN by content — PHASE4-N-F-G-A S2a)*

| Attribute | Value |
|-----------|-------|
| **Purpose** | The cardano-cli `query protocol-parameters` JSON parser (`//! GREEN`). It converts the oracle's protocol-parameters JSON (the `protocol_params_json` preimage carried in the operator consensus-inputs bundle) into a canonical BLUE `ade_ledger::pparams::ProtocolParameters`, so the recovered ledger carries the **current** protocol version + modeled parameters instead of `ProtocolParameters::default()` (the stale `protocol_major = 2` the S2 PO-1 entry check exposed). It parses the forge/header-relevant fields `ProtocolParameters` models; Conway fields outside that modeled surface (`utxoCostPerByte`, `executionUnitPrices`, `maxValueSize`, …) are ignored, not denied. |
| **Creates** | Closed `ProtocolParamsParseError` (incl. `InexactRational { field: &'static str }` — a rational literal that cannot be represented exactly by integer arithmetic). Pure functions: `parse_protocol_parameters_json(bytes, network_magic) -> Result<ProtocolParameters, ProtocolParamsParseError>`; the internal exact decimal/scientific → `Rational` integer converter. Not canonical-counted (GREEN; RED `ade_runtime`). |
| **MUST NOT** | (1) **Use any float path** (hard rule) — no `f64`, no `as f64`, no serde float deserialization; rational unit-interval / non-negative-interval literals (`poolPledgeInfluence`, `monetaryExpansion`, `treasuryCut`) are preserved as `serde_json::value::RawValue` strings and converted to exact `ade_ledger::rational::Rational` by integer decimal/scientific parsing; a literal that cannot be represented exactly (non-numeric, NaN/Inf, exponent overflow) fails closed (`InexactRational`). (2) Affect authoritative outputs (it produces a parameter record the recovered-ledger install consumes; it is glue, not the `ProtocolParameters` author). (3) Introduce nondeterminism. (4) Perform I/O / hold a clock / allocate global state / `await` — pure over its byte input. |
| **Inbound deps** | `ade_runtime::consensus_inputs::canonical` (`require_forge_current_pparams` calls it on the hash-bound preimage); the G-A forge-capable seed-import callers reach it through that accessor. |
| **Outbound deps** | `ade_ledger::pparams::ProtocolParameters`, `ade_ledger::rational::Rational`, `ade_types`, `serde_json` (incl. `value::RawValue`). |
| **Entry points** | `ade_runtime::consensus_inputs::protocol_params::{parse_protocol_parameters_json, ProtocolParamsParseError}` (re-exported from `consensus_inputs::{parse_protocol_parameters_json, ProtocolParamsParseError}`). |

> **Promotable / demotable:** **promotable to BLUE on demand** (pure, deterministic over its byte input, no float, closed-error vocabulary) — it sits in RED `ade_runtime` only to live beside the consensus-inputs importer; never demotable to RED.

---

### `ade_testkit::consensus::genesis_pinning` *(GREEN by content — PHASE4-N-F-G-A S1; `#[cfg(test)]`)*

| Attribute | Value |
|-----------|-------|
| **Purpose** | The genesis-consistency pinning harness (`// GREEN`, whole module `#[cfg(test)]`). It reads the committed private-net Ade-as-leader reference fixture (S1b: `consensus-inputs.json` + `shelley-genesis.json` + `PROVENANCE.md`), builds the recovered seed-epoch surface, drives the **REAL** `ade_runtime::bootstrap::bootstrap_initial_state` warm-start, and pins Ade's recovered values + leader-eligibility inputs (eta0, ASC, stake, VRF keyhash, `praos_vrf_input`) against the genesis-derived reference. It backs CE-G-A-1 — the four S1 pinning tests. |
| **Creates** | No public types (the whole module is `#[cfg(test)] mod tests`); it exercises BLUE/RED authorities and asserts. Not canonical-counted (GREEN test infrastructure). |
| **MUST NOT** | (1) Be anything but test infrastructure — the committed fixture is **evidence input, never runtime authority**: never a production source of eta0 / stake / ASC / VRF keyhash; the in-test sidecar pre-seed (`put_seed_epoch_consensus_inputs`) is confined to this `#[cfg(test)]` module, so the CN-CINPUT-02 populate-side fence keeps production population on the verified-bootstrap path only. (2) Compare an Ade-internal-state fingerprint to a Haskell-serialized-state hash — comparisons are over observable / derived surfaces only (DC-COMPAT-01). (3) Commit secret key material in the fixture dir (no `*.skey` / `*.vkey`; gated by `ci_check_genesis_consistency_fixture_present.sh`). (4) Affect authoritative outputs / introduce nondeterminism. |
| **Inbound deps** | None at compile time (exercised by the crate's own `#[cfg(test)]` suite). |
| **Outbound deps** | `ade_core::consensus::{era_schedule, praos_state, vrf_cert, …}`, `ade_ledger::{seed_consensus_inputs, consensus_view}`, `ade_runtime::{bootstrap, chaindb}`, the S1b committed fixture files. |
| **Entry points** | None public — the four S1 pinning `#[test]`s are the surface. |

---
### `ade_node::forge_intent` *(GREEN by content — PHASE4-N-F-F S1)*

| Attribute | Value |
|-----------|-------|
| **Purpose** | The pure tri-state forge-intent classifier for `--mode node` (`//! GREEN`). It answers "may `--mode node` forge?" as a total function of which operator-key CLI flags are *present* — **never of their contents**: it reads only `Option<&Path>` presence, opens no file, parses no key, and touches no secret. The closed two-variant `ForgeIntent` (`On`/`Off`) plus the structured `ForgeIntentError::PartialKeySet` make the dangerous state — "forge with a partial / missing / fabricated key set" — **unrepresentable as a classifier result**. A partial key set is an *error*, never an *intent*. Material loading into RED custody is `operator_forge` (S2); the `ForgeActivation` / binary `Some`/`None` flip is `node_lifecycle` (S3). (G-A leaves this module unchanged.) |
| **Creates** | Closed `ForgeIntent { On(ForgePaths), Off }`; presence-validated `ForgePaths { cold, kes, vrf, opcert, genesis: PathBuf }` (paths only — no secrets, no file contents; constructed ONLY on the complete-set arm); closed `ForgeIntentError::PartialKeySet { present: Vec<&'static str>, missing: Vec<&'static str> }` (static CLI flag-name strings only). Pure function: `classify_forge_intent(cold, kes, vrf, opcert, genesis: Option<&Path>) -> Result<ForgeIntent, ForgeIntentError>` — total over all 2⁵ presence combinations, the partial arm binding the tuple by name (exhaustive without `_`). Not canonical-counted (GREEN; RED `ade_node`). |
| **MUST NOT** | (1) Observe key *contents* — it reads only `Option<&Path>` presence; MUST NOT open a file, parse a key, or touch any secret (CN-NODE-03 intent half; `ci_check_forge_intent_closed.sh`). (2) Carry a third "partial" `ForgeIntent` variant. (3) Collapse an unenumerated presence combination into `On`/`Off` via a wildcard. (4) Let `ForgeIntentError` carry a supplied path string or key bytes — only static flag-name strings (OP-OPS-04). (5) Perform I/O / hold a clock / allocate global state / `await` — pure. (6) `classify_forge_intent` is the sole classifier entry. |
| **Inbound deps** | `ade_node::node_lifecycle` (the `--mode node` arm is the sole caller); `ade_node::operator_forge` (consumes the `ForgePaths` the `On` variant carries). |
| **Outbound deps** | `std::path` (`Path`, `PathBuf`) only. |
| **Entry points** | `ade_node::forge_intent::{classify_forge_intent, ForgeIntent, ForgePaths, ForgeIntentError}`. |

> **Promotable / demotable:** firmly **promotable to BLUE on demand**; never demotable to RED.

---

### `ade_node::run_loop_planner` *(GREEN by content — PHASE4-N-F-D S1; forge step PHASE4-N-F-E S1)*

| Attribute | Value |
|-----------|-------|
| **Purpose** | The pure lifecycle decision function for the `--mode node` relay run loop (`//! GREEN`). `plan_loop_step` selects each iteration's `LoopStep` from four closed, orthogonal, lifecycle-level inputs — operator intent (`ShutdownStatus`), momentary source readiness (`SyncStatus`), forge-slot scheduling (`ForgeSlotStatus`), and structural feed liveness (`LoopState`). It owns **no authority**. The planner learns only whether a forge slot is *due* via the content-blind `ForgeSlotStatus` — never who is a leader (leadership is decided BLUE inside `forge_one_from_recovered`). The forge-slot *monotonicity* is the pure `forge_slot_status` guard — the ONLY function here that observes a `SlotNo`. (G-A leaves this module unchanged.) |
| **Creates** | `LoopState`, `SyncStatus`, `ShutdownStatus`, closed `ForgeSlotStatus { Due, NotDue }`, closed `LoopStep { SyncOnce, Idle, HaltCleanly, ForgeTick }`. Pure functions: `plan_loop_step(loop_state, sync_status, forge_slot_status, shutdown_status) -> LoopStep`, `forge_slot_status(last_forged_slot: Option<SlotNo>, current_slot: SlotNo) -> ForgeSlotStatus`. Not canonical-counted (GREEN; RED `ade_node`). |
| **MUST NOT** | (1) `plan_loop_step` MUST stay content-blind — it may NOT reference `SlotNo` (only the `forge_slot_status` guard may); step selection ranges only over the closed `ForgeSlotStatus` (DC-NODE-05; `ci_check_loop_planner_closed.sh`). (2) Return a `LoopStep` outside the four closed variants. (3) Perform I/O / hold a clock / allocate / `await` — pure. (4) Decide any authority (validity / chain selection / leadership / forge eligibility / evidence). (5) Return `ForgeTick` under `ForgeSlotStatus::NotDue`. |
| **Inbound deps** | `ade_node::node_lifecycle` (`run_relay_loop` is the sole caller). |
| **Outbound deps** | `ade_types` (`SlotNo`, in the `forge_slot_status` guard only). |
| **Entry points** | `ade_node::run_loop_planner::{plan_loop_step, forge_slot_status, LoopStep, ForgeSlotStatus, LoopState, SyncStatus, ShutdownStatus}`. |

> **Promotable / demotable:** firmly **promotable to BLUE on demand**; never demotable to RED.

---

### `ade_testkit`

| Attribute | Value |
|-----------|-------|
| **Purpose** | Test infrastructure: differential harness, oracle snapshot loader, regression corpus, genesis loader, era mapping, transcript replay, diff reporting; consensus / block-validity / tx-validity / mempool-ingress / proposal-procedures / producer test harnesses; the N-Y observable-surface snapshot→tip differential harness (`harness::sync_diff`); and (N-F-G-A S1) the `consensus::genesis_pinning` genesis-consistency pinning harness (full entry above). Supplies the `ConwayValidityCorpus` the bootstrap/warm-start/forge tests build a warm-startable chaindb + `PoolDistrView` from. |
| **Creates** | Harness + corpus types (`ConwayValidityCorpus`, differential replay drivers, snapshot loaders). **N-Y `harness::sync_diff`:** `BlockVerdict` (closed observable-surface verdict string), `ObservableBlockSurface`, `UtxoEntry`, `SyncOracleFixture`, `AdeObservedSurfaces`, `SyncRegressionFixture`, `RegressionFixtureViolation`. **N-F-G-A `consensus::genesis_pinning`:** no public types (`#[cfg(test)]`-only). Not canonical-counted (GREEN). |
| **MUST NOT** | (1) Affect authoritative outputs. (2) Introduce nondeterminism into the BLUE-under-test path. (3) Construct semantic types bypassing the canonical decoders. **`harness::sync_diff` + `consensus::genesis_pinning` (DC-COMPAT-01):** MUST NOT compare Ade's internal ledger `fingerprint` to a Haskell / serialized-state hash — compatibility is proven only on observable surfaces (`ci_check_no_haskell_fingerprint_equality.sh`). |
| **Inbound deps** | None at compile time; consumed via integration tests + dev-dep links. |
| **Outbound deps** | `ade_types`, `ade_codec`, `ade_crypto`, `ade_core`, `ade_ledger`, `ade_plutus`, `ade_runtime`, `blake2`, `flate2`, `tar`, `serde`, `serde_json`, `toml`, `cardano-crypto` (dev). |
| **Entry points** | `ade_testkit::validity::corpus::ConwayValidityCorpus`, the differential `*_replay` drivers, the snapshot loaders, the genesis loader, `ade_testkit::harness::sync_diff::{sync_differential_snapshot_to_tip, diff_observable_surfaces, parse_sync_oracle_fixture, load_committed_regression_fixtures, validate_regression_fixture}`. |
| **Key modules** | `consensus/` (incl. `genesis_pinning.rs`, N-F-G-A), `validity/`, `tx_validity/`, `mempool/`, `governance/`, `producer/`, `harness/` (incl. `sync_diff.rs`, `diff_report.rs`), differential + corpus infrastructure. |

---

### `ade_network::session` *(GREEN by content — PHASE4-N-L)*

| Attribute | Value |
|-----------|-------|
| **Purpose** | The pure session driver. Composes the BLUE authorities `mux::frame::{encode,decode}_frame`, `handshake::n2n_transition`, and per-mini-protocol state machines through `session::core::step`; owns the partial-frame buffer + `SessionState` type-state + closed `AcceptedMiniProtocol` registry + per-mini-protocol payload reassembly. |
| **Creates** | ~12 closed types across `session::{event, state, demux, core, handshake_driver}`. |
| **MUST NOT** | (1) `session::core::step` is the only pub reducer in `session/`. (2) no `tokio::*` imports in session core. (3) `AcceptedMiniProtocol` closed. (4) no unbounded queue (`ci_check_session_no_unbounded.sh`). (5) affect authoritative outputs. |
| **Inbound deps** | `ade_runtime::network::{mux_pump, n2n_dialer}`, `ade_runtime::orchestrator::keep_alive_session`. |
| **Outbound deps** | `ade_network` BLUE submodules, `ade_codec`, `ade_types`. |
| **Entry points** | `ade_network::session::core::step`, `ade_network::session::{state::SessionState, event::*, demux::*, handshake_driver::*}`. |
| **Key modules** | `session/{mod, core, event, state, demux, handshake_driver}.rs`. |

---

### `ade_runtime::seed_consensus_merge` *(GREEN by content — PHASE4-N-F-A A2)*

| Attribute | Value |
|-----------|-------|
| **Purpose** | The deterministic, no-I/O merge transform that lifts a verified-bootstrap `LiveConsensusInputsCanonical` (bootstrap-time two-map shape) plus the minted anchor fingerprint + seed epoch into the BLUE single-map `SeedEpochConsensusInputs`. `total_active_stake` is the saturating sum of every pool's `active_stake`, derived exactly as the forge-time projection does. |
| **Creates** | `SeedConsensusMergeError` (closed: `PoolMissingVrfKeyhash { pool }` / `PoolMissingStake { pool }`). Not canonical-counted (GREEN). |
| **MUST NOT** | Affect authoritative outputs. Introduce nondeterminism (`BTreeMap` only). **Default-fill on a provenance gap:** a pool present in `pool_distribution` but absent from `pool_vrf_keyhashes` (or the reverse) MUST fail closed with the structured `SeedConsensusMergeError` — never a zero-hash fill. |
| **Inbound deps** | `ade_runtime::{genesis_bootstrap, mithril_bootstrap}` (the two verified-bootstrap composers). |
| **Outbound deps** | `ade_ledger::{consensus_view::PoolEntry, seed_consensus_inputs::SeedEpochConsensusInputs}`, `ade_runtime::consensus_inputs::LiveConsensusInputsCanonical`, `ade_types`. |
| **Entry points** | `ade_runtime::seed_consensus_merge::{merge_seed_epoch_consensus_inputs, SeedConsensusMergeError}`. |

---

### `ade_runtime` GREEN-by-content sub-trees

> All carry a `//! GREEN …` banner inside the RED `ade_runtime` crate, the BLUE deny attributes, and a purity CI gate. Each is "promotable to BLUE on demand, never demotable to RED" unless noted.

| Sub-tree | Purpose | MUST NOT | CI gate |
|---|---|---|---|
| `consensus_inputs::protocol_params` *(N-F-G-A S2a)* | GREEN cardano-cli protocol-parameters JSON → BLUE `ProtocolParameters`; exact decimal/scientific → `Rational` by integer arithmetic. *(Full entry above.)* | Use any float path (`f64`/`as f64`/serde float); affect authoritative outputs; introduce nondeterminism. | `ci_check_recovered_ledger_pparams_sourced.sh` (install-side, S2a) |
| `consensus_inputs::canonical` (`require_forge_current_pparams`) *(N-F-G-A S2a)* | GREEN hash-bound current-pparams accessor — `protocol_params_json` carried OUTSIDE the frozen 15-field fingerprint; requires present + hash-bind to `protocol_params_hash` + exact parse, fail-closed `ForgeCurrentPParamsError`. | Alter the bundle fingerprint by adding the preimage to the canonical CBOR; return pparams without the `blake2b_256` hash bind. | `ci_check_recovered_ledger_pparams_sourced.sh` |
| `seed_consensus_merge` *(N-F-A A2)* | GREEN bootstrap-time merge: two-map `LiveConsensusInputsCanonical` → single-map BLUE `SeedEpochConsensusInputs`. *(Full entry above.)* | Default-fill a provenance gap (zero-hash); affect authoritative outputs; introduce nondeterminism. | `ci_check_consensus_input_provenance.sh` |
| `forward_sync::reducer` *(N-Y)* | GREEN forward-sync lifecycle reducer; the private `AdmitPlan::durable` is the sole `AdvanceTip` emitter (durable-before-tip). The RED `forward_sync::pump_block` driver gained its first production caller in N-F-C. | Emit `AdvanceTip` before `StoreBlockBytes` + `AppendWal` (DC-SYNC-01); affect authoritative outputs. | `ci_check_forward_sync_chokepoint_only.sh` |
| `producer::coordinator` *(N-Q)* | Pure coordinator for live producer-mode; hosts `CoordinatorState::kes_period_for_slot` (reused by the N-F-E forge tick AND the N-F-F operator-material activation). | Own/store private signing material (CN-PROD-02); affect authoritative outputs. | `ci_check_producer_coordinator_no_secrets.sh` |
| `producer::producer_log` *(N-Q, extended N-W)* | Closed-vocabulary producer evidence log. | Add an open/wildcard event variant; emit `BlockServed` for a block not in the served snapshot (CN-PROD-04). | `ci_check_operator_evidence_manifest_schema.sh` |
| `producer::chain_evolution` *(N-T)* | Linear `ChainEvolution` typestate. | Never mints `AcceptedBlock` (CE-T-7); advance on authority disagreement (CE-T-6b). | gated by CE-T-7; purity inherited |
| `producer::{broadcast_to_served, served_chain_lookups}` *(N-G)* | GREEN served-chain broadcast targets + read-side lookups. | Affect authoritative outputs; serve a block that failed self-accept. | `ci_check_broadcast_to_served_purity.sh` |
| `clock` (`DeterministicClock` + `Clock` trait + `millis_to_slot` + `checked_millis_to_slot`) *(N-K; checked guard N-F-G-A S3)* | GREEN clock trait + deterministic impl (DC-NODE-03) + the pure `millis_to_slot` projection. **N-F-G-A** added the closed `SlotAlignmentError` + `checked_millis_to_slot` (the forge-path guard: before-anchor fails closed instead of saturating to `start_slot`). The N-F-E/G-A `ForgeTick` now derives its slot via `checked_millis_to_slot`; the N-F-F `On` arm injects a `SystemClock` (the sole wall-clock read on the forge path). | `DeterministicClock` reads no wall-clock; `checked_millis_to_slot` uses no float, no wall-clock, and MUST NOT saturate a before-anchor tick (fail closed). | `ci_check_clock_seam.sh` (seam); the before-anchor fail-closed wiring is asserted via the S3 unit tests + the node-path tests |
| `orchestrator::{mod, event, state, core}` *(N-K)* | GREEN core reducer + closed-vocabulary event + state. | tokio imports in core; open event vocabulary. | `ci_check_orchestrator_core_purity.sh` |
| `rollback::{cadence, in_memory_cache, chaindb_block_source, persistent_cache, persistent_writer}` *(N-I/N-J/N-K)* | GREEN rollback adapter glue + snapshot cadence + persistent cache/writer (DC-NODE-02, DC-CONS-21). | Parallel snapshot cadence; affect authoritative rollback state. | `ci_check_persistent_writer_no_parallel_cadence.sh`, `ci_check_snapshot_cadence_purity.sh` |
| `receive::{events_to_state, in_memory_chain_write}` *(N-H)* | GREEN receive-side glue. | Affect authoritative receive verdict. | `ci_check_receive_replay_purity.sh` |
| `seed_import` *(N-M-A)* | Single authority converting a cardano-cli JSON UTxO dump into canonical seed entries. | Construct semantic types bypassing canonical decoders. | `ci_check_seed_import_closure.sh`, `ci_check_seed_import_full_preprod_support.sh` |
| `bootstrap_anchor` *(N-M-A)* | RED-shell composer minting `BootstrapAnchor` (the *types* + binding predicate are BLUE in `ade_ledger`). | Mint an anchor outside this composer; bind a Mithril anchor without the BLUE `verify_mithril_binding`. | `ci_check_bootstrap_anchor_closure.sh` |
| `wal` *(N-M-A)* | File-backed Ade-native WAL (append-only). | Mutate/rewrite committed WAL entries. | `ci_check_wal_append_only.sh` |
| `consensus_inputs` *(N-M-C; extended N-F-G-A)* | Operator-extracted `LiveConsensusInputs` importer; **N-F-G-A** adds the `protocol_params` parser + the `require_forge_current_pparams` hash-bound preimage accessor (rows above). | Treat the peer as runtime authority; overstate semantic truth. | `ci_check_live_consensus_inputs_closure.sh`, `ci_check_live_consensus_inputs_fingerprint.sh` |
| `admission::*` (the GREEN reducer half) *(N-M-C)* | GREEN admission verdict/agreement reducer comparing already-authoritative outputs. | Emit RED verdicts; skip reference scripts; treat `lagging` as success (DC-EVIDENCE-01). | `ci_check_admission_runner_closure.sh`, `ci_check_admission_no_red_verdicts.sh`, `ci_check_lagging_is_evidence_only.sh`, `ci_check_admit_replay_equivalence.sh` |

> **Note on `bootstrap` (the `bootstrap_initial_state` authority, N-K):** it carries a `//! GREEN` banner but at HEAD also hosts the **RED** A3b warm-start sidecar restore (a `SnapshotStore` read). It is documented in the RED `ade_runtime` entry below to keep the I/O-touching surface (`restore_seed_epoch_consensus_inputs`) visible; the cold-start / warm-start materialization composition remains GREEN over the BLUE authorities.

---
### `ade_node::ba02_evidence` *(GREEN by content — PHASE4-N-F-C L6; library, unwired)*

| Attribute | Value |
|-----------|-------|
| **Purpose** | The BA-02 peer-acceptance evidence surface: it COMPARES two already-authoritative outputs — the BLUE-minted forged-block hash (read verbatim from `ForgedBlockArtifact`, never recomputed) and an operator-captured peer-accept signal — and emits a closed verdict. GREEN evidence: it forges nothing, admits nothing, persists no node state. Reached by no binary arm at this HEAD (exercised only by its `#[cfg(test)]` suite). (G-A leaves this module unchanged.) |
| **Creates** | `AdeForgeRecord`; closed `PeerAcceptEvent` (2-variant allow-list); `PeerAcceptSource`; `NoEvidenceReason` (closed: `NoPeerAccept`/`HashMismatch`/`ChainPointMismatch`/`ConflictingPeerSignals`); `BA02Outcome` (closed: `Ba02Manifest` / `NoEvidence`); versioned `Ba02Manifest` (`BA02_MANIFEST_SCHEMA_VERSION = 1`). GREEN — not canonical-counted. |
| **MUST NOT** | (1) Affect authoritative outputs / forge / admit / persist node state. (2) Recompute the forged-block hash. (3) Parse block bytes or construct semantic types. (4) **(RO-LIVE-06 / BA-02 honesty)** treat any weaker signal as acceptance — Ade self-accept, `ForgeSucceeded`, `block_received`, a lagging/diverged agreement verdict, or conflicting peer signals are all `NoEvidence`. (5) Construct a `Ba02Manifest` anywhere but `correlate`'s exact-match arm. (6) Commit a synthetic manifest under `docs/evidence/*ba02*` (`ci_check_ba02_evidence_closed.sh`). |
| **Inbound deps** | None at HEAD — reached only by tests. (Intended caller: the operator-pass live leg under RO-LIVE-01, deferred.) |
| **Outbound deps** | `ade_runtime::producer::coordinator::ForgedBlockArtifact`, `ade_types::Hash32`, `serde`, `serde_json`. |
| **Entry points** | `ade_node::ba02_evidence::{parse_peer_accept_events, correlate, AdeForgeRecord, Ba02Manifest, BA02Outcome, PeerAcceptEvent}`. |

---

### `ade_node` GREEN-by-content sub-trees

| Sub-tree | Purpose | MUST NOT | CI gate |
|---|---|---|---|
| `forge_intent` *(N-F-F S1)* | GREEN pure tri-state forge-intent classifier. *(Full entry above.)* | Observe key contents / open a file / parse a key; carry a third "partial" variant; collapse an unenumerated subset via a wildcard; let the error carry a path/secret byte. | `ci_check_forge_intent_closed.sh` |
| `run_loop_planner` *(N-F-D S1; forge step N-F-E S1)* | GREEN pure lifecycle planner. *(Full entry above.)* | `plan_loop_step` reference `SlotNo` (only the guard may); return a non-closed `LoopStep`; decide any authority; return `ForgeTick` under `NotDue`. | `ci_check_loop_planner_closed.sh` |
| `node_sync::forge_epoch_admission` *(N-F-G-A S4)* | GREEN pure epoch-boundary guard — `forge_epoch_admission(slot, era_schedule, seed_epoch)` over the closed `ForgeEpochAdmission { WithinSeedEpoch, OffEpoch }`, derived solely via the BLUE `EraSchedule::locate`. Called BEFORE leadership / KES signing inside `forge_one_from_recovered`. | Fabricate the candidate epoch (must use `EraSchedule::locate`); run after leadership; drive a nonce promotion (DC-EPOCH-03). | `ci_check_node_forge_single_epoch_fail_closed.sh` |
| `admission_log` *(N-M-B)* | GREEN admission-mode JSONL event vocabulary + writer (closed enum). | Add open/wildcard event variant. | `ci_check_admission_log_vocabulary_closed.sh` |
| `live_log` *(N-L-LIVE)* | GREEN closed JSONL vocabulary for the wire-only live pass. | Add open event variant; overstate semantic truth. | `ci_check_wire_only_event_vocabulary_closed.sh` |
| `admission` (the GREEN half of the orchestrator) *(N-M-B)* | GREEN admission orchestrator reducer + verdict mapping. | Affect authoritative verdict; skip reference-script validation. | `ci_check_admission_no_refscript_skip.sh` |
| `ba02_evidence` *(N-F-C, library/unwired)* | GREEN BA-02 evidence correlator. *(Full entry above.)* | Treat a weaker signal as acceptance; recompute the forged hash; forge/admit/persist; commit a synthetic manifest. | `ci_check_ba02_evidence_closed.sh` |

> **Note on `node_sync` color:** `node_sync` is **RED** (it drives the apply path / owns the persistent-store reads). The `forge_epoch_admission` *function* within it carries a `GREEN — pure` banner (derived solely from `(slot, era_schedule, seed_epoch)`); it is surfaced as a GREEN sub-tree row here for the new G-A guard, while the crate entry for `node_sync` stays in the RED `ade_node` section.

---
## RED Modules — Imperative Shell

> I/O, network, storage, clocks, retries. May depend on BLUE/GREEN. Must not modify core state directly or construct semantic types unsafely.

### `ade_runtime`

| Attribute | Value |
|-----------|-------|
| **Purpose** | The imperative shell — moves bytes, owns sockets/files/clocks/keys, drives tokio tasks. Hosts: producer-mode key custody + shell composition (`producer/`), the N2N network drivers (`network/`), the node orchestrator runners (`orchestrator/`), the receive/rollback/admission shells, the seed-import/WAL/consensus-inputs importers (incl. the N-F-G-A GREEN `consensus_inputs::protocol_params` parser + the `consensus_inputs::canonical` `require_forge_current_pparams` accessor), ChainDB + recovery shells, the N-Y bootstrap/forward-sync surfaces, the N-Z `mithril_bootstrap` composition, the clock seam (`clock` — DC-NODE-03; **N-F-G-A** added the GREEN `checked_millis_to_slot` + closed `SlotAlignmentError` forge-path guard alongside the saturating `millis_to_slot` and the RED `SystemClock`), and (N-F-A) the seed-epoch consensus-input recovered-state shell. The `producer::keys` cold/VRF/KES loaders + `ProducerShell::init` + `producer::{genesis_parser, opcert_envelope}` parsers + `coordinator::CoordinatorState::kes_period_for_slot` + `coordinator::GenesisAnchor` are the RED authorities the N-F-F/N-F-G-A `ade_node::operator_forge` ingress reuses (no reimpl). |
| **Creates (RED-only)** | `KesSecret`, `VrfSigningKey`, `ColdSigningKey` custody wrappers; `KeyLoadError`; `MuxTransportHandle` consumers; `OutboundCommand` (closed enum, N-S-B); `PerPeerOutbound` map; `DispatchError`; `ServedChainHandle`; `GenesisAnchor`; `ProducerShell` (+ `ShellInitError`). **Clock (N-K; N-F-G-A):** `SystemClock`, `DeterministicClock`, closed `SlotAlignmentError` (1-variant `BeforeGenesisAnchor`, GREEN-by-content). **consensus_inputs (N-M-C; N-F-G-A):** `LiveConsensusInputsCanonical` (now carries `protocol_params_json: Option<String>` OUTSIDE the 15-field fingerprint), closed `ProtocolParamsParseError`, closed `ForgeCurrentPParamsError` (`PreimageAbsent` / `BindMismatch` / parse). **N-Y:** `forward_sync::pump::{PumpError, PumpTip, SnapshotSink, NoCheckpointSink}`; `mithril_import::{…, MithrilManifestError, MithrilProvenanceImport}`; `recovery::restart::{NodeRecoveryError, RecoveredNode}`. **N-Z:** `mithril_bootstrap::{MithrilSeedPointInputs, MithrilBootstrapOutput, MithrilBootstrapError}`. **N-F-A:** `bootstrap::{SeedEpochConsensusSource (closed: NotRequired / RequiredFromRecoveredProvenance), BootstrapState, BootstrapInputs, BootstrapError (incl. the seed-consensus fail-closed variants)}`; `genesis_bootstrap::{GenesisBootstrapError, GenesisBootstrapOutput}`. Never semantic/canonical types. |
| **Interprets** | Canonicalizes peer bytes/files for the BLUE core: `producer::keys::{load_cold_signing_key_skey, load_vrf_signing_key_skey, load_kes_signing_key_skey}`; `producer::genesis_parser::parse_shelley_genesis`; `producer::opcert_envelope::parse_opcert_envelope`; `seed_import`. **N-F-G-A:** `consensus_inputs::protocol_params::parse_protocol_parameters_json` is the SOLE cardano-cli protocol-parameters JSON parser (GREEN; exact `Rational`, no float); `consensus_inputs::canonical::require_forge_current_pparams` hash-binds the `protocol_params_json` preimage to the fingerprinted `protocol_params_hash` (via `blake2b_256`) before parsing. **N-Y:** `mithril_import::json::parse_mithril_manifest_json` is the SOLE manifest-JSON parser. **N-F-A:** `bootstrap::restore_seed_epoch_consensus_inputs` reads the anchor-fp-keyed sidecar bytes and verifies them fail-closed. |
| **MUST NOT** | (1) Modify BLUE state directly or construct semantic types from raw bytes. (2) Bypass canonical validation. (3) `producer::signing` (DC-CRYPTO-03/05) — RED-confined key custody. (4) `producer::keys` — no `KesSecret::from_*` inside `load_kes_signing_key_skey`. (5) `producer::coordinator` is GREEN and holds no secrets. (6) `network::outbound_command` (CN-OUTBOUND-RELAY-01) — `OutboundCommand` is the sole channel to MuxPump's encoder. (7) per-peer outbound map (CN-PEER-OUTBOUND-MAP-01 / DC-OUTBOUND-FIFO-01). (8) `network::n2n_server` — no signing dep. **(9) `forward_sync::pump` (DC-SYNC-01):** refuse to advance the tip before `StoreBlockBytes` + `AppendWal` return Ok (`PumpError::TipBeforeDurable`). **(10) `mithril_import` (CN-MITHRIL-01):** no semantic decision; route initial state through the single `bootstrap_initial_state` authority. **(11) `genesis_bootstrap` (CN-NODE-01 / DC-GENESIS-SRC-01 / CN-CINPUT-02):** route through the single `bootstrap_initial_state` authority; the composer WRITES the sidecar, never consumes one. **(12) `recovery::restart`:** compose the existing authorities; fail fast on `NodeRecoveryError::WalTailFingerprintMismatch`. **(13) `mithril_bootstrap` (DC-MITHRIL-02):** the anchor `seed_point` MUST be minted from `MithrilSeedPointInputs`, never the manifest; run `verify_mithril_binding` fail-closed BEFORE any `bootstrap_initial_state`. **(14) `bootstrap::restore_seed_epoch_consensus_inputs` (DC-CINPUT-01):** fail closed on a missing sidecar / hash mismatch / non-canonical decode / binding mismatch / non-byte-identical re-encode — and MUST NOT fall back to the forge-time bundle. **(15) `seed_consensus_provenance::append_seed_epoch_provenance` (CN-CINPUT-02):** MUST hash the EXACT A1 bytes the composer `put`; referenced only at the two verified-bootstrap composition sites. **(16) `chaindb` seed-sidecar surface (CN-CINPUT-02):** anchor-fp-keyed namespace disjoint from the slot-keyed snapshot space; redb `SCHEMA_VERSION = 3`. **(17) `producer::keys` + `ProducerShell` + `producer::{genesis_parser, opcert_envelope}` (CN-NODE-03 / OP-OPS-04, reused by N-F-F/N-F-G-A):** the cold/VRF/KES loaders + the opcert/genesis parsers are the SOLE key/config-parse authorities — `ade_node::operator_forge` reuses them and MUST NOT reimplement parsing; `ProducerShell` is the RED key-custody holder (no public private-byte accessor; `KeyLoadError` carries no path bytes). **(18) `clock::checked_millis_to_slot` (N-F-G-A S3):** MUST NOT saturate a before-anchor tick — it fails closed (`SlotAlignmentError::BeforeGenesisAnchor`); no float, no wall-clock; the saturating `millis_to_slot` is left intact for non-forge callers. **(19) `consensus_inputs::protocol_params` (N-F-G-A S2a):** no float path; fail closed (`InexactRational`) on a non-exact rational literal. **(20) `consensus_inputs::canonical` (N-F-G-A S2a):** `protocol_params_json` stays OUTSIDE the 15-field canonical CBOR fingerprint; `require_forge_current_pparams` fails closed without the hash bind. |
| **Inbound deps** | `ade_node`, `ade_core_interop`, `ade_testkit` (dev/integration). |
| **Outbound deps** | `ade_types`, `ade_core`, `ade_crypto`, `ade_codec`, `ade_ledger`, `ade_network`, `redb`, `serde`, `serde_json`, `bech32`, `base58`, `cardano-crypto` (`["vrf-draft03", "dsign"]`), `ed25519-dalek`, `tokio`. (`consensus_inputs::protocol_params` adds `serde_json::value::RawValue` + `ade_ledger::{pparams, rational}`.) |
| **Entry points** | `ade_runtime::producer::{producer_shell::{ProducerShell (incl. init, public_metadata, cold_vk), ShellInitError}, keys::{load_cold_signing_key_skey, load_vrf_signing_key_skey, load_kes_signing_key_skey}, coordinator::{*, ForgedBlockArtifact, CoordinatorEvent, CoordinatorState (incl. kes_period_for_slot), GenesisAnchor, coordinator_init}, served_chain_handle::{ServedChainHandle, push_atomic}, genesis_parser::parse_shelley_genesis, opcert_envelope::parse_opcert_envelope}`, `ade_runtime::network::{n2n_listener::*, n2n_dialer::*, mux_pump::*, n2n_server::*, outbound_command::OutboundCommand}`, `ade_runtime::orchestrator::*`, `ade_runtime::bootstrap::{bootstrap_initial_state, BootstrapInputs, BootstrapState, SeedEpochConsensusSource, BootstrapError}`, `ade_runtime::seed_consensus_merge::{merge_seed_epoch_consensus_inputs, SeedConsensusMergeError}`, `ade_runtime::consensus_inputs::{parse_protocol_parameters_json, ProtocolParamsParseError, LiveConsensusInputsCanonical (incl. require_forge_current_pparams), ForgeCurrentPParamsError}`, `ade_runtime::seed_consensus_provenance::append_seed_epoch_provenance`, `ade_runtime::forward_sync::{forward_sync_step, pump_block, SyncEffect, PumpError, SnapshotSink}`, `ade_runtime::mithril_import::{import_mithril_manifest, parse_mithril_manifest_json, MithrilProvenanceImport, MithrilManifestError}`, `ade_runtime::genesis_bootstrap::{bootstrap_from_conway_genesis, GenesisBootstrapError, GenesisBootstrapOutput}`, `ade_runtime::mithril_bootstrap::{bootstrap_from_mithril_snapshot, MithrilSeedPointInputs, MithrilBootstrapOutput, MithrilBootstrapError}`, `ade_runtime::recovery::restart::{recover_node_state, NodeRecoveryError}` (test-only secondary), `ade_runtime::chaindb::{ChainDb, SnapshotStore (incl. put_/get_seed_epoch_consensus_inputs, list_seed_epoch_consensus_anchor_fps), InMemoryChainDb}`, `ade_runtime::clock::{Clock, DeterministicClock, SystemClock, millis_to_slot, checked_millis_to_slot, SlotAlignmentError}`, `ade_runtime::{seed_import, wal, bootstrap_anchor}::*`. |
| **Key modules** | `producer/` (RED + GREEN-by-content; incl. `producer_shell.rs`, `keys.rs`, `genesis_parser.rs`, `opcert_envelope.rs` — the RED authorities reused by `ade_node::operator_forge`), `network/`, `orchestrator/` (RED runners + GREEN core), `receive/`, `rollback/`, `admission/`, `seed_import/`, `consensus_inputs/` (`mod`, `importer`, `json`, `canonical` — incl. the N-F-G-A `require_forge_current_pparams` accessor + `protocol_params_json` non-fingerprinted carry, `protocol_params` GREEN parser, `view`), `wal/`, `chaindb/` (`mod`, `persistent`, `in_memory`, `snapshot_contract` — redb schema v3), `forward_sync/` (`mod`, `reducer` GREEN, `pump` RED), `mithril_import/`, `seed_consensus_merge.rs` (GREEN, N-F-A), `seed_consensus_provenance.rs` (RED, N-F-A), `mithril_bootstrap.rs`, `genesis_bootstrap.rs`, `recovery/` (`mod`, `restart`), `bootstrap.rs` (GREEN composition + RED A3b restore), `bootstrap_anchor.rs`, `clock.rs` (the seam + `millis_to_slot` + the N-F-G-A `checked_millis_to_slot` / `SlotAlignmentError`). |

---
### `ade_node::operator_forge` *(RED — PHASE4-N-F-F S2/S3; real parsers PHASE4-N-F-G-A S2)*

| Attribute | Value |
|-----------|-------|
| **Purpose** | The single named `--mode node` operator-material ingress site (`//! RED`). It consumes a presence-validated `ForgePaths` (from the GREEN forge-intent classifier) and builds the RED key-custody holder by **reusing** the existing cold / VRF / KES loaders + the real cardano-cli opcert/genesis parsers — it reimplements **no** parser. `load_operator_producer_shell(&ForgePaths) -> Result<ProducerShell, OperatorForgeError>` loads cold/VRF/KES and runs `ProducerShell::init` (KES-period-vs-opcert freshness bound, CN-PROD-02). `build_operator_forge_material(&ForgePaths) -> Result<OperatorForgeMaterial, OperatorForgeError>` returns the shell + genesis anchors + the operator `pool_id` from `blake2b_224(cold_vk)` + clock-seam anchors + honest-scope `pparams`/`protocol_version` defaults. **N-F-G-A S2:** retires `parse_simple_opcert_json` / `parse_simple_genesis_json` on the node path and uses the REAL `ade_runtime::producer::opcert_envelope::parse_opcert_envelope` + `ade_runtime::producer::genesis_parser::parse_shelley_genesis` (the genesis reuse extracts clock/KES/network constants ONLY). Key custody is RED-confined. **Mithril boundary (load-bearing):** this is operator signing-material ingress, NOT bootstrap — it does NOT call Mithril, create a second bootstrap path, or re-derive initial state; `parse_shelley_genesis` is reused only to extract clock/KES anchors, NOT as a starting-state source. |
| **Creates (RED-only)** | Closed 6-variant `OperatorForgeError` (`ColdKeyLoad(KeyLoadError)` / `VrfKeyLoad(KeyLoadError)` / `KesKeyLoad(KeyLoadError)` / `OpcertParse(&'static str)` / `ShellInit(ShellInitError)` / `GenesisParse(&'static str)` — inner `KeyLoadError` carries no path bytes per OP-OPS-04); `OperatorForgeMaterial { shell: ProducerShell, genesis: GenesisAnchor, pool_id: Hash28, pparams, protocol_version, anchor_millis, start_slot, slot_length_ms }` — **deliberately NOT `Debug`/`Serialize`**. Never semantic/canonical types. |
| **Interprets** | Operator key/genesis/opcert files — but only by **delegating** to the reused `ade_runtime` loaders (`load_cold_signing_key_skey`, `load_vrf_signing_key_skey`), the `pub(crate)`-widened `produce_mode::load_kes_skey_any_format`, and the real `ade_runtime::producer::{opcert_envelope::parse_opcert_envelope, genesis_parser::parse_shelley_genesis}` parsers (N-F-G-A S2). It parses nothing itself. |
| **MUST NOT** | (1) Reimplement any key/opcert/genesis parser — it MUST reuse the existing `ade_runtime` cold/VRF/KES loaders + the real `parse_opcert_envelope` / `parse_shelley_genesis` parsers (CN-NODE-03 custody half; `ci_check_operator_forge_no_secret_leak.sh`); on the node path the `parse_simple_*` stubs are retired (`ci_check_node_forge_real_cli_ingress.sh`). (2) Introduce a new BLUE authority, a plugin/trait seam, a second forge codepath, or any BLUE crate change (CN-NODE-03). (3) Expose any private-key byte — no byte accessor, no serialization, no logging; `OperatorForgeMaterial` is NOT `Debug`/`Serialize`; `OperatorForgeError` carries no path/key bytes (OP-OPS-04). (4) Call Mithril, create a second bootstrap path, or re-derive initial state — `parse_shelley_genesis` is reused for clock/KES-anchor extraction ONLY (CN-NODE-01). (5) Fabricate `pool_id` — it is derived in this ONE named place from `blake2b_224(cold_vk)`. (6) Build a `CoordinatorState` here — that is kept caller-side (in `node_lifecycle`). |
| **Inbound deps** | `ade_node::node_lifecycle` (the `--mode node` `On` arm is the sole caller). |
| **Outbound deps** | `ade_node::forge_intent` (`ForgePaths`), `ade_node::produce_mode` (`load_kes_skey_any_format`), `ade_runtime::producer::{producer_shell::{ProducerShell, ShellInitError}, keys::{load_cold_signing_key_skey, load_vrf_signing_key_skey}, coordinator::GenesisAnchor, opcert_envelope::{parse_opcert_envelope, OpCertParseError}, genesis_parser::{parse_shelley_genesis, GenesisParseError}}` (and `KeyLoadError`), `ade_crypto::blake2b_224`, `ade_types::{Hash28, SlotNo, shelley::block::ProtocolVersion}`, `ade_ledger::pparams::ProtocolParameters`. |
| **Entry points** | `ade_node::operator_forge::{load_operator_producer_shell, build_operator_forge_material, OperatorForgeMaterial, OperatorForgeError}`. |

---

### `ade_node`

| Attribute | Value |
|-----------|-------|
| **Purpose** | The node binary + library entry. Owns argv parsing, the node lifecycle, and the mode drivers dispatched by `main()`: `--mode wire_only` (default), `--mode admission`, `--mode key_gen_kes`, `--mode produce`, and (N-F-C) `--mode node` (`node_lifecycle`). `produce_mode::run_real_forge` carries an era guard. `admission::runner` strips the served block's tag-24 envelope via `ade_codec::unwrap_tag24`. **N-F-C/D/E/F/G-A scope (the `--mode node` live surface):** `node_lifecycle` is the single `PHASE4-N-F-C-LIFECYCLE-OWNER` — it opens a `PersistentChainDb` + `FileWalStore`, classifies first-run-vs-warm-start, then (FirstRun) runs `bootstrap_from_mithril_snapshot` durably or (WarmStart) recovers the verified `BootstrapState`. **N-F-F** classifies forge intent on that single recovered state and flips `None`/`Some(ForgeActivation)`. **N-F-G-A** then: sources the `On`-arm forge constants (`protocol_version` + `pparams`) from the recovered current ledger view (S2); ingests operator config through the real `parse_opcert_envelope` + `parse_shelley_genesis` (S2, in `operator_forge`); installs the oracle-bound current `ProtocolParameters` into the recovered ledger at seed/import (S2a, in `admission::{seed_to_snapshot, bootstrap}`); derives the `ForgeTick` slot via the checked `checked_millis_to_slot` (S3 — before-anchor fails closed); and fails an off-epoch forge closed via `forge_epoch_admission` **before** leadership / KES signing (S4, in `node_sync::forge_one_from_recovered`). **`run_relay_loop`'s containment is semantically unchanged** — the new boundaries sit inside the existing fence. With the empty source the loop halts before any `ForgeTick` (forge-CAPABLE, NOT observable — RO-LIVE-01). The L6 BA-02 correlator (`ba02_evidence`) is a **library surface reached by no binary arm**. |
| **Creates (RED-only)** | `Cli`, `CliError`, `ProduceCli`, `KeyGenKesCli`, `Mode` (closed: `WireOnly`/`Admission`/`KeyGenKes`/`Produce`/`Node`), `NodeStartupInputs`, `NodeShutdownEvidence`, `NodeRunError`, exit-code constants; the `produce_mode` slot loop types; the admission runner types. **N-F-C (`node_lifecycle`, RED):** `NodeStart` (closed: `FirstRun`/`WarmStart`), `NodeLifecycleError` (closed; incl. `RelaySync` N-F-D and `ForgeKeyIngress(String)` N-F-F), exit-code constants (incl. `EXIT_NODE_FORGE_KEY_INGRESS_FAILED = 44`). **N-F-D/E (`node_lifecycle`, RED):** `run_relay_loop`; `ForgeActivation<'a>` (recovered-state forge inputs + clock seam + the per-iteration derived slot + the in-memory `hermetic_forge_outcomes` test field; **N-F-G-A** adds `protocol_version: ProtocolVersion` sourced current + `last_slot_alignment_fail: Option<SlotAlignmentError>` S3). **N-F-F (`forge_intent` GREEN / `operator_forge` RED — entries above).** **N-F-C/D (`node_sync`, RED):** `NodeBlockSource` (closed: `WirePump`/`InMemory`), `NodeSyncError`, `NodeForgeError`; **N-F-G-A (`node_sync`, GREEN-by-function):** closed `ForgeEpochAdmission { WithinSeedEpoch, OffEpoch }` + `forge_epoch_admission`. **N-F-D/E (`run_loop_planner`, GREEN — entry above).** **N-F-C (`ba02_evidence`, GREEN).** Never semantic/canonical types. |
| **Interprets** | argv (closed mode set); operator-supplied key/genesis/opcert file paths (delegated to `ade_runtime` parsers + `ade_node::operator_forge`); evidence-manifest TOML schema; the admission runner interprets a peer's tag-24-wrapped BlockFetch `MsgBlock` payload via `ade_codec::unwrap_tag24`. **N-F-G-A:** the `ForgeTick` arm interprets the clock seam's wall-clock tick into a `SlotNo` via `checked_millis_to_slot` (before-anchor fails closed); the `On`-arm constants are sourced from the recovered current ledger view; the off-epoch guard interprets `(slot, era_schedule, seed_epoch)` via the BLUE `EraSchedule::locate`. |
| **MUST NOT** | (1) Construct semantic types bypassing the canonical decoders / `ade_runtime` parsers. (2) `produce_mode` (CN-PROD-04, CN-OUTBOUND-RELAY-01) — obtain initial state only via `bootstrap_initial_state`; re-validate each broadcast block through BLUE `self_accept`; emit outbound bytes only via `OutboundCommand`. (3) No synthetic forge state (N-T). (4) No durability in the produce_mode path (N-U scope). (5) `run_real_forge` (CN-FORGE-04) — non-Praos era fail-closes. (6) `admission::runner` (CN-WIRE-08) — strip via `ade_codec::unwrap_tag24`. (7) `wire_only` — overstate semantic truth. (8) operator-evidence manifest carries the closed schema (CN-OPERATOR-EVIDENCE-01). (9) closed mode set with no wildcard dispatch (`ci_check_node_mode_closure.sh`). (10) `produce_mode` recovered-surface fence (CN-CINPUT-02) — MUST pass only `SeedEpochConsensusSource::NotRequired`. **(11) `node_lifecycle` lifecycle owner (CN-NODE-01):** exactly one `PHASE4-N-F-C-LIFECYCLE-OWNER`; initial state SOLELY via `bootstrap_initial_state`; no parallel/cold init, no fallback; the N-F-F/G-A forge-on arm reuses the SINGLE recovered `state` (NO second bootstrap, NO Mithril call) (`ci_check_lifecycle_owner_uses_bootstrap_initial_state.sh`, `ci_check_node_binary_uses_single_bootstrap.sh`). **(12) `run_relay_loop` / `node_sync` sync driver (CN-NODE-02 / DC-SYNC-02):** advance the durable tip ONLY via `run_node_sync → pump_block`; `run_node_sync` UNMODIFIED; **`run_relay_loop`'s containment stays semantically unchanged by N-F-G-A** (`ci_check_node_run_loop_containment.sh`, `ci_check_node_sync_via_pump.sh`). **(13) `run_relay_loop` forge tick (CN-NODE-02 / CE-E-4 / DC-NODE-05):** the forge is reached ONLY through exactly one fenced `forge_one_from_recovered(` (self-accept-only); MUST NOT call `run_real_forge` directly, correlate evidence, or serve/broadcast/gossip a forged block; advances no durable tip; **N-F-G-A** derives the tick slot via `checked_millis_to_slot` (before-anchor fail-closed) (`ci_check_node_run_loop_containment.sh`). **(14) `run_loop_planner` (DC-NODE-05):** `plan_loop_step` content-blind. **(15) `node_sync::forge_one_from_recovered` consume-side fence (CN-CINPUT-03 / DC-CINPUT-02b):** derive the leadership `PoolDistrView` ONLY via `from_seed_epoch_consensus_inputs(`; fabricate no `SeedEpochConsensusInputs { … }` literal. **(16) `node_sync::forge_one_from_recovered` epoch fence (DC-EPOCH-03, N-F-G-A S4):** call `forge_epoch_admission` BEFORE `query_leader_schedule`; an off-epoch / unlocatable slot fails closed before leadership / KES signing; the guard derives the epoch via the BLUE `EraSchedule::locate` (no fabricated epoch math); the forge path drives NO `NonceInput::EpochBoundary` / `CandidateFreeze` nonce promotion (`ci_check_node_forge_single_epoch_fail_closed.sh`). **(17) recovered-ledger pparams (CE-G-A-2a, N-F-G-A S2a):** `admission::{seed_to_snapshot, bootstrap}` install the oracle-bound current `ProtocolParameters` (via `require_forge_current_pparams`), never `ProtocolParameters::default()` / genesis-initial (`ci_check_recovered_ledger_pparams_sourced.sh`). **(18) `ba02_evidence` (RO-LIVE-06):** single `Ba02Manifest` constructor in `correlate`; treat no weaker signal as acceptance (`ci_check_ba02_evidence_closed.sh`). **(19) forge-intent + operator-material ingress (CN-NODE-03 / OP-OPS-04):** `forge_intent` content-blind; `operator_forge` reuses the loaders (incl. real `parse_opcert_envelope` / `parse_shelley_genesis`, no reimpl), exposes no private-key byte; the `--mode node` arm builds the activation through this single ingress, never a second forge codepath (`ci_check_forge_intent_closed.sh`, `ci_check_operator_forge_no_secret_leak.sh`, `ci_check_node_forge_real_cli_ingress.sh`). |
| **Inbound deps** | None (binary + integration tests). |
| **Outbound deps** | `ade_types`, `ade_core`, `ade_crypto`, `ade_ledger`, `ade_runtime`, `ade_network`, `ade_codec`, `serde`, `serde_json`, `tokio`. Dev-deps: `ade_testkit`, `tempfile`. |
| **Entry points** | `main()`; `ade_node::run_node_lifecycle` (`--mode node`); `ade_node::node_lifecycle::{run_relay_loop, ForgeActivation}`; `ade_node::forge_intent::{classify_forge_intent, ForgeIntent, ForgePaths}` (GREEN); `ade_node::operator_forge::{build_operator_forge_material, load_operator_producer_shell, OperatorForgeMaterial}` (RED); `ade_node::run_loop_planner::{plan_loop_step, forge_slot_status, LoopStep, ForgeSlotStatus}` (GREEN); `ade_node::node_sync::{NodeBlockSource, run_node_sync, forge_one_from_recovered, forge_epoch_admission, ForgeEpochAdmission}` (library); `ade_node::run_node_until_shutdown`; `ade_node::produce_mode::{run_real_forge, run_produce_mode, *}`; `ade_node::cli::{Cli, ProduceCli, Mode}`; `ade_node::admission::*`; `ade_node::wire_only::*`; `ade_node::key_gen::*`; `ade_node::ba02_evidence::{parse_peer_accept_events, correlate, Ba02Manifest}` (library). |
| **Key modules** | `lib.rs`, `cli.rs`, `node.rs`, `main.rs`, `node_lifecycle.rs` (the `--mode node` owner + `run_relay_loop` + `ForgeActivation` + the N-F-F forge-intent flip + the N-F-G-A S2/S3 current-constants/checked-clock wiring), `forge_intent.rs` (**GREEN**), `operator_forge.rs` (**RED** — real-parser ingress, N-F-G-A S2), `run_loop_planner.rs` (**GREEN**), `node_sync.rs` (the sync driver + recovered-forge handoff + the N-F-G-A S4 `forge_epoch_admission` guard), `ba02_evidence.rs` (**GREEN**, library/unwired), `produce_mode.rs` (incl. the `pub(crate)` `load_kes_skey_any_format` reused by `operator_forge`), `wire_only.rs`, `key_gen.rs`, `admission/` (`bootstrap`, `runner`, `seed_to_snapshot` — the N-F-G-A S2a current-pparams install, `verdict`), `admission_log/`, `live_log/`. |

> **GREEN sub-classification:** `ade_node::forge_intent`, `ade_node::run_loop_planner`, and `ade_node::ba02_evidence` are GREEN-by-content; the `node_sync::forge_epoch_admission` *function* is GREEN-by-function (full entries / rows above). `node_lifecycle` + `node_sync` + `operator_forge` are RED.

---

### `ade_core_interop`

| Attribute | Value |
|-----------|-------|
| **Purpose** | Live cardano-node interop driver. Hosts the `live_*_session` RED binaries (operator-action evidence harness) plus the N-E S4/S5 GREEN tx-submission bridges, and the chain-follow driver (`follow`). `follow` strips the peer's ChainSync `RollForward` `[era_tag, tag24(header_cbor)]` envelope via `ade_codec::unwrap_tag24`. |
| **Creates (RED-only)** | Live-session drivers and transcript types; never semantic types. |
| **MUST NOT** | (1) Construct semantic types from raw bytes. (2) Be depended on by any BLUE/GREEN crate (RED leaf). (3) Overstate semantic truth in evidence. (4) `follow` (CN-WIRE-08) — strip via `ade_codec::unwrap_tag24`. |
| **Inbound deps** | None (RED leaf — binaries). |
| **Outbound deps** | `ade_core`, `ade_codec`, `ade_crypto`, `ade_ledger`, `ade_runtime`, `ade_network`, `ade_testkit`, `ade_types`, `tokio`. |
| **Entry points** | `ade_core_interop::bin::{live_tx_submission_session, live_block_production_session, live_block_fetch_session, live_block_follow_session}`; `ade_core_interop::follow::*`; `ade_core_interop::tx_submission`, `ade_core_interop::local_tx_submission` (GREEN sub-class). |
| **Key modules** | `bin/`, `follow.rs`, `tx_submission.rs`, `local_tx_submission.rs`. |

---

### `ade_network::mux::transport` *(RED)*

| Attribute | Value |
|-----------|-------|
| **Purpose** | The only place inside `ade_network` where socket I/O happens. RED tokio shell over the BLUE mux frame primitive and the GREEN session reducer. Provides `MuxTransportHandle` + closed `TransportError` + `DuplexCapacity::DEFAULT` + `spawn_duplex`. |
| **Creates (RED-only)** | `MuxTransportHandle`, `TransportError`, `DuplexCapacity`, `MuxTransport`. |
| **MUST NOT** | (1) Construct semantic types. (2) Bypass `mux::frame` for framing. (3) Live in BLUE scope. |
| **Inbound deps** | `ade_runtime::network::{mux_pump, n2n_dialer}`. |
| **Outbound deps** | `ade_network::mux::frame` (BLUE), `ade_network::session` (GREEN), `tokio`. |
| **Entry points** | `ade_network::mux::transport::{MuxTransportHandle, spawn_duplex, open_tcp}`. |
| **Key modules** | `mux/transport.rs`. |

---

### `ade_network` *(RED capture binaries — non-session)*

| Attribute | Value |
|-----------|-------|
| **Purpose** | Operator-action capture binaries (live evidence harness) inside `ade_network`. The `ade_chain_sync_capture` binary supports `--intersect-slot` / `--intersect-hash` flags (N-X) to capture a real Conway `RollForward` golden fixture. |
| **MUST NOT** | (1) Construct semantic types from raw bytes. (2) Overstate semantic truth in captured evidence. |
| **Outbound deps** | `ade_network` BLUE/GREEN submodules, `ade_codec`, `ade_types`, `tokio`. |

---
## Cross-Module Rules (project-wide)

### Dependency direction (RED → GREEN → BLUE; never outward)

```
ade_types     → {}
ade_codec     → {ade_types}
ade_crypto    → {ade_types, blake2, ed25519-dalek, cardano-crypto[vrf-draft03,kes-sum(test-only),dsign]}
ade_core      → {ade_types, ade_crypto, minicbor}
ade_ledger    → {ade_core, ade_plutus, ade_crypto, ade_codec, ade_types, minicbor, num-*}
ade_plutus    → {ade_crypto, ade_codec, ade_types, aiken_uplc, pallas-primitives(internal)}
ade_network   → BLUE submodules → {ade_codec, ade_types}; session(GREEN) → +BLUE submodules; transport(RED) → +tokio
ade_testkit   → {ade_core, ade_ledger, ade_plutus, ade_runtime, ade_crypto, ade_codec, ade_types, cardano-crypto}   [GREEN]
ade_runtime   → {ade_core, ade_crypto, ade_codec, ade_types, ade_ledger, ade_network, redb, serde*, bech32, base58, cardano-crypto[vrf-draft03,dsign], ed25519-dalek, tokio}   [RED]
ade_node      → {ade_types, ade_core, ade_crypto, ade_ledger, ade_runtime, ade_network, ade_codec, serde, serde_json, tokio}   [RED; forge_intent + run_loop_planner + ba02_evidence + node_sync::forge_epoch_admission are GREEN-by-content/function; operator_forge + node_lifecycle + node_sync are RED]
ade_core_interop → {ade_core, ade_codec, ade_crypto, ade_ledger, ade_runtime, ade_network, ade_testkit, ade_types, tokio}   [RED leaf]
```

**Forbidden directions.** Any BLUE crate (or BLUE `ade_network` submodule) depending on `ade_runtime`, `ade_node`, `ade_core_interop`, or the RED half of `ade_network` is a CI failure (`ci_check_dependency_boundary.sh`). Any non-`ade_plutus` crate referring to `pallas_*` is a CI failure (`ci_check_pallas_quarantine.sh`). Any `cardano_crypto::kes` import outside `#[cfg(test)]` under `crates/ade_crypto/src/**` is a CI failure (`ci_check_kes_sum_compatibility.sh` Guard 3). A second block-envelope encoder or a `produce_mode` direct-transport write is a CI failure. A second `leader_vrf_input` authority, or a path accepting both TPraos and Praos VRF inputs for one era, is a CI failure (`ci_check_producer_praos_vrf.sh`). A second `wrap_tag24`/`unwrap_tag24` definition or a hand-rolled tag-24 parse in RED is a CI failure (`ci_check_tag24_wire_authority.sh`). A forward-sync `AdvanceTip` reachable before the block's durability writes (DC-SYNC-01), a Mithril/genesis bootstrap path bypassing the single `bootstrap_initial_state` authority (`ci_check_mithril_uses_bootstrap_initial_state.sh`), and an internal-ledger-fingerprint-vs-Haskell-hash equality assertion in the compatibility harness (DC-COMPAT-01) are CI failures. A registry rule citing a non-existent `crates/**.rs` / `ci/**.sh` `code_locus` is a CI failure (`ci_check_registry_code_locus_exists.sh`). A Mithril-bootstrap path that sources the anchor `seed_point` from the manifest (DC-MITHRIL-02) is a CI failure. A forge-time path that populates the seed-epoch consensus-input sidecar or appends its WAL provenance is a CI failure (CN-CINPUT-02). **N-F-C:** a second `PHASE4-N-F-C-LIFECYCLE-OWNER`, a parallel/cold storage-init, a genesis/bundle/cold/graft fallback, a `recover_node_state` overclaim, or a `RequiredFromRecoveredProvenance` construction outside {owner, `bootstrap.rs`} is a CI failure (CN-NODE-01); treating a weaker signal as BA-02 peer acceptance, or constructing a `Ba02Manifest` outside `correlate`'s exact-match arm, is a CI failure (RO-LIVE-06). **N-F-D:** a direct `pump_block`/manual tip advance/forge/evidence/verdict/follower/second-bootstrap reference in `run_relay_loop`, or a `LoopStep` outside the closed set, is a CI failure (CN-NODE-02 / DC-SYNC-02). **N-F-E:** `plan_loop_step` referencing a `SlotNo`, or `run_relay_loop` reaching the forge by any path other than exactly one fenced `forge_one_from_recovered`, is a CI failure (DC-NODE-05 / CN-NODE-02 / CE-E-4). **N-F-F:** a `forge_intent` classifier that opens a file / parses a key / observes key contents, a third "partial" `ForgeIntent` variant, a `ForgeIntentError`/`OperatorForgeError` carrying a path/key byte, an `OperatorForgeMaterial` with a `Debug`/`Serialize` derive, a reimplemented parser in `operator_forge`, a second forge codepath / second bootstrap / Mithril call on the forge-on arm, or a `ReceiveState::new` outside the owner allow-list `{node.rs, node_lifecycle.rs}` is a CI failure (CN-NODE-03 / CN-NODE-01 / OP-OPS-04). **NEW (N-F-G-A): forge fidelity is real-config, current-pparams, and fail-closed at the two boundaries** — a `parse_simple_*` parser reintroduced on the node forge path (`ci_check_node_forge_real_cli_ingress.sh`), a recovered ledger carrying `ProtocolParameters::default()` / genesis-initial pparams instead of the oracle preimage (`ci_check_recovered_ledger_pparams_sourced.sh`), a `forge_epoch_admission` guard reordered *after* `query_leader_schedule` / a fabricated candidate epoch / a `NonceInput::EpochBoundary`|`CandidateFreeze` nonce promotion on the forge path (`ci_check_node_forge_single_epoch_fail_closed.sh`), a committed S1 reference fixture missing a file / not Ade-as-leader / leaking secret key material (`ci_check_genesis_consistency_fixture_present.sh`), a `protocol_params` float path, a `checked_millis_to_slot` that saturates a before-anchor tick, or a `protocol_params_json` preimage folded INTO the 15-field canonical fingerprint is a CI failure (DC-EPOCH-03 / CE-G-A-1/2/2a/4).

### Naming convention

All crates are prefixed `ade_`. TCB color is not encoded in the crate name. The authoritative classifier is `.idd-config.json` `core_paths` + the per-file `// Core Contract:` / `//! BLUE|GREEN|RED` banner + the cluster TCB Color Maps; CI scripts hard-code their BLUE list.

### Closed enums / registries (for SEAMS cross-reference)

Closed semantic surfaces detected at HEAD: `AcceptedMiniProtocol` (`ade_network::session`), `TagEnvelopeError` (`ade_codec::cbor::tag24`), `ExpectedVrfInput` (2-variant, `ade_core::consensus::vrf_cert`), `LeaderCheckVerdict` (2-variant, `ade_core::consensus::leader_check`), `OutboundCommand` + `DispatchError` (`ade_runtime::network`), `ProducerLogEvent` + `ForgeFailureReason`, `ChainEvolutionError`, `ServedChainAdmitError`, `KesError` / `KesParseError`, the admission/wire-only/live-log JSONL vocabularies, the operator-evidence manifest TOML schema, the closed `CardanoEra` / Conway cert + governance enums. **N-Y:** `SeedProvenance` (version-gated behind `ANCHOR_SCHEMA_VERSION = 2`), `MithrilImportError`, `GenesisSourceError`, `SyncEffect` (4-variant), `MithrilManifestError`, `PumpError`, `NodeRecoveryError`, the `sync_diff` observable `BlockVerdict` + `RegressionFixtureViolation`. **N-Z:** `MithrilBootstrapError`. **N-F-A:** `SeedEpochConsensusInputs` (version-gated behind `SEED_CINPUT_SCHEMA_VERSION = 1`, SOLE codec), `SeedConsensusInputsError` (6-variant), `SeedConsensusMergeError` (2-variant), `SeedEpochConsensusSource` (2-variant), and the additive **WAL** variant `WalEntry::SeedEpochConsensusInputsImported` (tag 3). **N-F-C:** `Mode` (5-variant, no wildcard dispatch), `NodeStart` (2-variant), `NodeLifecycleError` / `NodeSyncError` / `NodeForgeError`, `NodeBlockSource` (2-variant), and the BA-02 evidence vocabulary. **N-F-D/E:** `LoopStep` (4-variant incl. `ForgeTick`) + content-blind `ForgeSlotStatus` (2-variant). **N-F-F:** `ForgeIntent` (2-variant: `On(ForgePaths)` / `Off`), `ForgeIntentError` (1-variant), `OperatorForgeError` (6-variant). **N-F-G-A:** `SlotAlignmentError` (1-variant: `BeforeGenesisAnchor`, `ade_runtime::clock`), `ProtocolParamsParseError` (incl. `InexactRational`, `ade_runtime::consensus_inputs::protocol_params`), `ForgeCurrentPParamsError` (`PreimageAbsent`/`BindMismatch`/parse, `ade_runtime::consensus_inputs::canonical`), `ForgeEpochAdmission` (2-variant: `WithinSeedEpoch` / `OffEpoch`, `ade_node::node_sync`). **Governance: `WalEntry` deliberately stays a CE-not-law surface (tags append-only); `LoopStep` is an additively-evolvable closed planner vocabulary; `ForgeIntent` / `ForgeEpochAdmission` are closed classifier vocabularies (a partial key set / an off-epoch slot is an error, never a third variant). The `protocol_params_json` preimage stays OUTSIDE the frozen 15-field `LiveConsensusInputsCanonical` fingerprint (the fingerprint commits to its hash). The redb `chaindb` `SCHEMA_VERSION` (v3) is a versioned-gate.**

### CI enforcement (116 scripts under `ci/`)

The full list is mechanically obtainable via `ls ci/ci_check_*.sh` (116 at HEAD). CI lives entirely under `ci/` (`ci_dirs = ["ci"]`); there is no `.github/workflows/` invariant gate at HEAD. New / changed / load-bearing since the prior CODEMAP HEAD (PHASE4-N-F-F close, 112 scripts):

| Script | Enforces | Cluster / commit |
|---|---|---|
| `ci_check_genesis_consistency_fixture_present.sh` | **NEW (N-F-G-A S1, CE-G-A-1)** — the three S1b fixture files are committed + well-formed + Ade-as-leader (eta0 == genesis_hash_hex, ASC, non-empty pool_distribution, a VRF keyhash per pool), and NO secret key material (`*.skey`/`*.vkey`/cardano-cli SigningKey envelope) leaked into the committed fixture dir. | N-F-G-A `b5decb3e`/`23addfbb` |
| `ci_check_recovered_ledger_pparams_sourced.sh` | **NEW (N-F-G-A S2a, CE-G-A-2a)** — the recovered ledger's `protocol_params` are sourced from the operator bundle's oracle preimage (`require_forge_current_pparams`) at the forge-capable seed import — never `ProtocolParameters::default()` / genesis-initial; scoped to `seed_to_snapshot.rs` + `bootstrap.rs` + `canonical.rs`. | N-F-G-A `3dba81db` |
| `ci_check_node_forge_real_cli_ingress.sh` | **NEW (N-F-G-A S2, CE-G-A-2)** — the `--mode node` operator-forge ingress loads config through the real `parse_opcert_envelope` + `parse_shelley_genesis`; fails closed if a `parse_simple_*` JSON parser is reintroduced on the node forge path. | N-F-G-A `11704998` |
| `ci_check_node_forge_single_epoch_fail_closed.sh` | **NEW (N-F-G-A S4, CE-G-A-4 / DC-EPOCH-03)** — `forge_one_from_recovered` calls `forge_epoch_admission` BEFORE `query_leader_schedule`; the guard derives the candidate epoch via the BLUE `EraSchedule::locate` (no fabricated epoch math); the node forge path drives NO `NonceInput::EpochBoundary` / `CandidateFreeze` nonce promotion. | N-F-G-A `80dac1f7` |
| `ci_check_loop_planner_closed.sh` | DC-NODE-05 — the GREEN planner's closed `LoopStep`/`ForgeSlotStatus` + the `SlotNo` ban scoped to `plan_loop_step`. (Unchanged in N-F-G-A.) | N-F-D; extended N-F-E (carried) |
| `ci_check_node_run_loop_containment.sh` | CN-NODE-02 / CE-E-4 — `run_relay_loop`'s tip-advance / forge / serve fence. (Unchanged in N-F-G-A — containment is semantically unchanged.) | N-F-D; extended N-F-E (carried) |
| `ci_check_forge_intent_closed.sh` | CN-NODE-03 (intent half) — closed two-variant `ForgeIntent`, content-blind classifier, no path/secret in the error. (Carried.) | N-F-F (carried) |
| `ci_check_operator_forge_no_secret_leak.sh` | CN-NODE-03 (custody half) — loader reuse (now incl. the real opcert/genesis parsers), no `Debug`/`Serialize` on `OperatorForgeMaterial`, no private-byte accessor. (Carried; extended in spirit by the S2 real-parser gate.) | N-F-F (carried) |
| `ci_check_node_binary_uses_single_bootstrap.sh` | CN-NODE-01 — `ReceiveState::new` owner allow-list `{node.rs, node_lifecycle.rs}`. (Carried.) | N-F-F (carried) |
| `ci_check_consensus_input_provenance.sh` | CN-CINPUT-02 (populate-side + forge-time fence) + guard (d) the CONSUME-side fence (CN-CINPUT-03 / DC-CINPUT-02b). (Carried.) | N-F-A; guard (d) N-F-C (carried) |

> Earlier-cluster scripts (N-A through N-Z, plus N-F-A/N-F-C/N-F-D/N-F-E/N-F-F) are present and counted in the 116 total. The per-script enforce/scope detail is in the registry's `ci_script` fields per rule.

---

## Generation notes

- Regenerated (**incremental, scoped refresh** catching up the PHASE4-N-F-G-A cluster close) at HEAD `80dac1f7` (`git rev-parse --short HEAD`). The prior on-disk CODEMAP stopped at the PHASE4-N-F-F close (`4eb7610`). PHASE4-N-F-G-A (forge fidelity on the `--mode node` spine) closes now: S1 `2684a618`/`b5decb3e` (GREEN genesis-consistency pinning harness) + S1b `50c6a5b0`/`23addfbb`, S2a `38680107`/`3dba81db` (current protocol-parameters source), S2 `a5afb013`/`11704998` (real opcert/genesis ingress + recovered-current constants), S3 `2a27d352`/`2049ec9d` (checked slot-alignment guard), S4 `5204a8d8`/`80dac1f7` (off-epoch fail-closed guard). The refresh is **scoped to the N-F-G-A `ade_runtime` + `ade_node` + `ade_testkit` surface + the mechanical counts that drifted**; every BLUE crate entry and the N-Q…N-F-F module entries are carried forward unchanged.
- **No BLUE crate was modified** by N-F-G-A (verified by recomputing every BLUE crate's `(pub )?struct|enum` count — all unchanged at `80dac1f7`: `ade_codec` 11, `ade_types` 81, `ade_crypto` 21, `ade_core` 49, `ade_ledger` 177, `ade_plutus` 8; `ade_network` BLUE submodules 109). All N-F-G-A code is in RED `ade_runtime` (the GREEN `consensus_inputs::protocol_params` parser, the `consensus_inputs::canonical` `require_forge_current_pparams` accessor, the GREEN `clock::checked_millis_to_slot` + `SlotAlignmentError`), RED `ade_node` (`operator_forge` real parsers, `node_lifecycle` S2/S3 wiring, `node_sync` S4 epoch guard, `admission::{seed_to_snapshot, bootstrap}` S2a install), and GREEN `ade_testkit` (`consensus::genesis_pinning` `#[cfg(test)]`). It uses `BTreeMap` (never `HashMap`), sorted keys, integer-only rational arithmetic (no float in `protocol_params`), and no clock/rand/float in production paths beyond the existing RED clock seam.
- **Cluster-doc location:** the N-F-G-A cluster + slice docs live under `docs/clusters/PHASE4-N-F-G-A/`; on close they archive under `docs/clusters/completed/`. All prior closed cluster docs are already under `docs/clusters/completed/`.
- Mechanical counts recomputed fresh from the tree at `80dac1f7` (not copied): **11 crates** (Δ 0), **456 canonical types** (**Δ 0** — no BLUE crate touched; all N-F-G-A types live in RED `ade_runtime`/`ade_node` and are not canonical-counted; `ProtocolParameters`/`Rational` are pre-existing BLUE types), **2213 tests** (**Δ +25** vs the prior on-disk doc's 2188 — the N-F-G-A surface across `protocol_params.rs`, `clock.rs`, `canonical.rs`, `genesis_pinning.rs`, `node_sync.rs`, `node_lifecycle.rs`, `seed_to_snapshot.rs`), **116 CI checks** (**Δ +4**: the four N-F-G-A gates), **313 registry rules** (**Δ +2** vs the prior 311: the new enforced `DC-EPOCH-03` + the new declared `DC-NODE-06`).
- **Boundary honesty (load-bearing, verbatim).** PHASE4-N-F-G-A is **forge-fidelity hardening on the relay spine** — real cardano-cli config ingress (opcert + genesis), oracle-bound **current** `ProtocolParameters` (no longer `::default()`), a before-anchor clock→slot fail-closed guard (S3), and an off-epoch forge fail-closed guard *before* leadership / KES signing (S4). It does **NOT** serve / admit / gossip / advance a durable tip. The forge stays **subordinate + self-accept-only**; `run_relay_loop`'s containment is **semantically unchanged** (the new boundaries sit inside the existing fence). On the empty binary source the loop still halts before any `ForgeTick` — the `On` arm is forge-CAPABLE but **not observable** (RO-LIVE-01 follow-on). There is **NO serve / admit / gossip / durable-tip / BA-02 / RO-LIVE acceptance claim**. **BA-02 is satisfied nowhere.** The S1 reference fixture is **evidence input, never runtime authority**.
- **DC-NODE-06 is a declared forward sketch (NOT enforced by G-A).** The registry's new `DC-NODE-06` (self-accept→serve handoff, shape B) carries `introduced_in = "PHASE4-N-F-G-B"`, `status = "declared"`, empty `tests`/`ci_script` — it is the invariant the *next* sub-cluster (G-B) will enforce. G-A enforces only `DC-EPOCH-03`. Both rule entries (and the seven `strengthened_in += "PHASE4-N-F-G-A"` bumps) are committed in the working tree (`git show HEAD:docs/ade-invariant-registry.toml` = 313 = the worktree count).
- **Stale `.idd-config.json` fields (informational).** `.idd-config.json` `_invariant_registry_doc` still reads "311 entries"; `_head_deltas_baseline` is `4eb7610` (the N-F-F close). Bump both on the N-F-G-A HEAD_DELTAS refresh — they are config annotations, not enforced counts.
- TCB color for every new/changed module verified against the on-disk `// Core Contract:` / `//!` banner: `protocol_params.rs` opens `//! GREEN cardano-cli query protocol-parameters JSON parser` → **GREEN**; `clock.rs` carries the GREEN clock-seam banner (`checked_millis_to_slot` is pure, no float, no wall-clock) → **GREEN-by-content**; `genesis_pinning.rs` opens `// GREEN — genesis-consistency pinning harness` + whole-module `#[cfg(test)]` → **GREEN**; `node_sync.rs::forge_epoch_admission` carries `GREEN — pure` (the enclosing `node_sync.rs` is `//! RED`) → **GREEN-by-function inside a RED module**; `operator_forge.rs` opens `//! RED` → **RED**; `node_lifecycle.rs` opens `//! RED \`--mode node\` Ade node lifecycle owner` → **RED**. Carried-forward colors unchanged.
- The doc is regenerated, not edited. If a value drifts, fix the source, not the doc.
