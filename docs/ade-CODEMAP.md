# Module Authority Map — Ade

> **Status:** Living architectural document. Regenerated; not hand-edited.
> Per-project instance of `~/.claude/methodology/templates/codemap.md`.

> 11 crates, 457 canonical types, 2284 tests, 126 CI checks at HEAD (`550eec3a`, PHASE4-N-F-G-J cluster close).
>
> **PHASE4-N-F-G-J delta (PrevHash null/hash32 wire authority + position-aware header rule + genesis-successor cold-start forge on the `--mode node` spine):** **ONE BLUE canonical type added** (`PrevHash = Genesis | Block(Hash32)` in `ade_types::shelley::block`, replacing the prior flat `prev_hash: Hash32` field — **456 → 457**) and **ONE NEW BLUE module** (`ade_ledger::block_validity::header_position`, adding the `check_header_position` *function* + the `BlockValidityError::HeaderPositionInvalid` *variant*, no struct/enum). The cluster makes the Shelley-and-later header `prev_hash` field the closed Cardano wire grammar `$hash32 / null` (cardano-ledger `PrevHash = GenesisHash | BlockHash`) and discharges the deferred position-aware coupling: a genesis-successor block (`block_number 0`) MUST carry `PrevHash::Genesis` (CBOR null `0xf6`); a non-genesis block (`> 0`) MUST carry `PrevHash::Block(hash32)`. Five slices. **S1** (taxonomy) adds the NEW **GREEN** closed feed/forge scheduling-event vocabulary `ade_node::live_log::{sched_event::NodeSchedEvent, sched_writer::NodeSchedLogWriter}` (emit-only diagnostic sibling of the wire-only `LiveLogEvent`; a separate closed enum + its own byte-deterministic JSONL encoder; the relay loop's `run_relay_loop_with_sched` records scheduling decisions WITHOUT changing them) + the gate `ci_check_node_sched_events_emit_only.sh` (`CN-NODE-04`). **S2** introduces the closed BLUE sum `PrevHash` + the **POSITION-BLIND** `$hash32/null` codec: `ade_codec::shelley::block::decode_prev_hash` decodes `null -> Genesis` / `hash32 -> Block` as a pure function of the CBOR token (NEVER `block_number`), the `ShelleyHeaderBody` `AdeEncode` writes `Genesis -> write_null` / `Block(h) -> write_bytes_canonical`, and the producer `prev_hash` migrates `Hash32 -> PrevHash` end to end (`ProducerTick.prev_hash`, `unsigned_header_pre_image`'s `prev_hash: PrevHash`, `ChainEvolution::prev_hash()` cold-start `-> PrevHash::Genesis` — the all-zero stand-in is **deleted at its source**) + the gate `ci_check_prevhash_single_wire_authority.sh` (`CN-WIRE-09`). **S3** (position validation + genesis forge) adds the NEW **BLUE** `ade_ledger::block_validity::header_position::check_header_position` — the SINGLE position-AWARE authority (`block_number 0 <=> Genesis`), called by `decode_block` BEFORE the header authority, surfacing `BlockValidityError::HeaderPositionInvalid` (coarse class `HeaderInvalid` — **no new `BlockRejectClass`**); `producer::forge` now forges a real genesis-successor block (block 0 + `PrevHash::Genesis`) its own validator accepts. **S4** (node-spine cold-start reachability) adds two GREEN-by-function decisions to RED `ade_node` — `node_sync::forge_header_position(Option<&ChainTip>)` (the ONE cold-start convention: `None => (0, Genesis)`, `Some => (last_block_no+1, Block)`, a tip-without-height edge fails closed as `NodeForgeError::RecoveredTipMissingBlockNo`, deleting the pre-S4 `.unwrap_or(1)` disagreement) + `node_lifecycle::may_cold_start_forge(selected_tip_present, has_recovered_lineage, feed_eligible)` (the cold-start permission gate) — so when `ChainDb::tip()` AND the recovered tip are BOTH `None`, the `LoopStep::ForgeTick` arm forges block 0 + `PrevHash::Genesis` through the SAME `run_real_forge -> self_accept -> SelfAcceptedHandoff` path S3 proved + the gate `ci_check_genesis_successor_reachability.sh` (`DC-NODE-08`). **S5** (C1 genesis rehearsal) reuses the existing BA-02 evidence + rehearsal surfaces (no new authority): `ade_node::{ba02_evidence, rehearsal_evidence::PrivateRehearsalManifest, rehearsal_pass}`, the new test `crates/ade_node/tests/node_c1_genesis_rehearsal.rs`, and the runbook `docs/evidence/phase4-n-f-g-j-genesis-rehearsal-README.md` (`CN-REHEARSAL-FIDELITY-01` strengthened). **Registry → 319 rules**: NEW `CN-WIRE-09` (`tier = derived`, `enforced`), NEW `DC-NODE-08` (`tier = derived`, `enforced`), NEW `CN-NODE-04` (the closed sched-event vocabulary). `CN-REHEARSAL-FIDELITY-01` gains `strengthened_in += "PHASE4-N-F-G-J"`. **No rule weakened.** **3 NEW CI gates (123 → 126):** `ci_check_node_sched_events_emit_only.sh` (S1) + `ci_check_prevhash_single_wire_authority.sh` (S2) + `ci_check_genesis_successor_reachability.sh` (S4). Tests **+39** by the documented `#[test]|#[tokio::test]` grep method (2245 at `main` → 2284 at HEAD). **Boundary honesty (load-bearing — do NOT soften / do NOT broaden):** the codec stays **POSITION-BLIND** (the `Genesis|Block` decision is the CBOR token's, never the `block_number`'s); the position rule lives in EXACTLY one BLUE authority (`check_header_position`), called from `decode_block`, **never in `ade_codec`** and never duplicated. No all-zero `Hash32`, no anchor fingerprint, and no Shelley genesis hash may stand in for the genesis predecessor — it is structurally CBOR null. The cold-start first-block reachability is scoped to the **hermetic cold-start execution** (NO `genesis_forged` latch); the forge engine takes **no `ChainDb` handle**, so it advances **no durable tip** — durable block-1+ progression is the durability slice (N-U). The recovered seed-epoch lineage gates **PERMISSION** to forge from the genesis-successor position; it is **NOT** the source of the prev_hash bytes. **NO RO-LIVE flip** (`RO-LIVE-01` stays `partial` / operator-gated); the eligibility signal is general (forge-configured + valid recovered base), **never a private-only / C1-only flag**.
>
> > **PHASE4-N-F-G-H delta (carried — node-spine live serve-to-peer on the `--mode node` spine):** **NO BLUE authority / canonical type added** (456 canonical types unchanged — the one BLUE touch is an additive *function*, `ade_network::handshake::version_table::n2n_supported_for_magic`, not a struct/enum). The cluster wires `--mode node` to **serve real peers READ-ONLY** from the G-B self-accepted `ServedChainView`, via a **sibling** listener/serve-dispatch task spawned **OUTSIDE** `run_relay_loop`, through the EXISTING BLUE ChainSync + BlockFetch serve reducers, with **no second serve authority or serializer**. Three slices (+ S2b). **S1** (`8b8b8139`) extracts the coordinator-free serve-dispatch core to a single shared home: NEW **RED** `ade_runtime::network::serve_dispatch` receives `dispatch_server_frame_event_to_outbound` + the per-peer-state lifecycle (`install_server_peer_state` / `remove_server_peer_state`) + `ServerPeerStates` (= `BTreeMap<PeerId, PerPeerN2nServerState>`) + `DispatchError` + the GREEN-by-content `ServedBlockEvidence` — all **moved verbatim** from `ade_node::produce_mode` (pure code-motion; `produce_mode`'s `CoordinatorState` / `coordinator_step` / evidence-writer wrapping stays in `produce_mode`, which is re-pointed at the shared core — its emitted frames are byte-identical, proven by `produce_loopback.rs`). NEW gate `ci_check_single_serve_dispatch_authority.sh`. **S2** (`8c6a6a7e`) wires the node spine: NEW **RED** `ade_node::node_lifecycle::run_node_serve_task` (the request-driven serve sibling, `tokio::spawn`ed OUTSIDE `run_relay_loop` on the `On` arm when `--listen` is supplied) + `bind_serve_listener` (binds ONCE, surfaces a bind fault) + the closed 2-variant `ServeStartError { InvalidAddr(String), Bind(io::ErrorKind) }`. It reuses the EXISTING `--listen` flag (no new `--mode node` flag) and the shared `serve_dispatch` core over the retained `ServedChainView`. **S2b** (`a8ca5e52`) makes the serve listener **magic-aware**: NEW additive BLUE **fn** `n2n_supported_for_magic(network_magic: u32) -> Vec<(u16, VersionData)>` (specializes the static `N2N_SUPPORTED` table for the configured magic — `N2N_SUPPORTED` + `n2n()` are UNCHANGED), and `ade_runtime::network::n2n_listener` changes `N2nListenerConfig.our_supported` + `PerPeerSessionConfig.our_supported` from `&'static [(u16, VersionData)]` to `Arc<[(u16, VersionData)]>` (RED; the owned magic-aware serve table). Both live serve sites (`node_lifecycle` + `produce_mode`) build the table via `n2n_supported_for_magic` from the genesis magic — so a real preprod (magic 1) / C1 (magic 42) follower's handshake is not refused on a magic mismatch. NEW gate `ci_check_serve_listener_magic_aware.sh`. **S3** (`f59b1eaa`) adds the C1 serve runbook (`docs/evidence/phase4-n-f-g-h-node-serve-README.md`) + the env-gated **RED** operator harness `crates/ade_node/tests/node_c1_serve_live.rs::node_c1_serve_live` (`ADE_LIVE_C1_SERVE=1`; skipped in CI; `NoEvidence` panics, writes nothing). **Registry → 316 rules**: NEW `DC-NODE-07` (`tier = derived`, `status = enforced` at this close; `introduced_in = "PHASE4-N-F-G-H"`; declared at S1, flipped at close mirroring the G-D convention). 5 carry-forward strengthenings (`DC-CONS-17`, `DC-CONS-18`, `DC-PROTO-07`, `DC-PROTO-08`, `CN-PROTO-06` each gain `strengthened_in += "PHASE4-N-F-G-H"`). `RO-LIVE-01` / `RO-LIVE-06` are deliberately **NOT** bumped — only `cross_ref`'d (the serve MECHANISM is enforced; the live peer-ACCEPT stays operator-gated — the decoupling). **2 NEW CI gates (121 → 123):** `ci_check_single_serve_dispatch_authority.sh` + `ci_check_serve_listener_magic_aware.sh`. Tests **+3** by the prior CODEMAP's documented `#[test]|#[tokio::test]` grep method (`n2n_supported_for_magic_produces_configured_magic` in `version_table.rs`; `node_c1_serve_live`; the carried `ba02_gate_archived_home` test from the `18085231` BA-02-gate fix in this span); the two `node_spine_serve_loopback.rs` integration tests use `#[tokio::test(flavor = "multi_thread", …)]`, which the documented grep does not match (see Generation notes). **Boundary honesty (load-bearing — do NOT soften / do NOT broaden):** G-H wires a **READ-ONLY serve OF the already-self-accepted `ServedChainView`** — it serves what the G-B handoff admitted, and nothing it serves is authoritative output it produced. The claim is **NARROW**: a follower can now **discover Ade's served tip** (ChainSync header advertisement, DC-CONS-18) and **fetch the served block by hash** (BlockFetch body, DC-CONS-17). It is **NOT** a peer-ACCEPTANCE claim (`RO-LIVE-01` stays `partial`), **NOT** BA-02 / live-evidence (`RO-LIVE-06` enforces schema + mechanics only — wire-serve success ≠ peer acceptance), and **NOT** a durable-tip / gossip / reactor change. The serve is **request-driven only** (no proactive `advance_tip` / `served_chain_view.changed()` reactor). `run_relay_loop`'s containment is **byte-/semantically unchanged** (the serve task lives outside it); there is **NO serve / `push_atomic` / served-chain mutation inside the relay loop**, **NO second `ServedChain` authority**, **NO parallel tag-24 serializer** (the single `CN-WIRE-08` envelope authority is reused), **NO new `--mode node` flag** (the existing `--listen` is reused), and **NO new BLUE authority / canonical type / variant**. The live C1 serve execution stays `blocked_until_operator_c1_serve_executed` (env-gated harness only).
>
> > **PHASE4-N-F-G-D delta (carried — private-testnet accepted-block bounty DRY-RUN harness on the `--mode node` spine):** **NO BLUE crate changed** (456 canonical types unchanged). All changes land in the RED binary/library (`ade_node`), CI, and docs. G-D adds a path-faithful, non-promotable bounty DRY-RUN **harness** — it does NOT enforce that a C1 run has succeeded (the rehearsal gate is vacuous until a real operator-produced manifest is committed). Four slices. **S1** (`d4d0f456`) proves `--mode node` accepted-block PATH FIDELITY (CN-REHEARSAL-FIDELITY-01 clause 1): the C1 private dry-run uses the SAME accepted-block path as preview/preprod (`import_live_consensus_inputs` → forge → self-accept → sibling-serve → block-fetch → peer log → correlate), with NO private-only flag/branch/bootstrap/from-genesis constructor; the only differences are operator INPUTS (a private genesis whose stake makes Ade win slots fast) + the evidence LABEL (S2). New test `node_accepted_block_consensus_inputs_via_shared_import` (`crates/ade_node/tests/node_path_fidelity.rs`) proves the shared importer is venue-agnostic (an epoch-0-shaped private bundle AND a synced-preprod-tip bundle both lift through the single `import_live_consensus_inputs_from_bytes` with NO venue branch). New gate `ci_check_node_path_fidelity.sh` — guard (a): the `cli.rs` argv flag set equals the pinned **28-flag** closed allow-list (G-D adds none; a private-only / venue flag trips it); guard (b): no from-genesis consensus-inputs constructor exists AND `node_lifecycle.rs` sources consensus inputs only via the shared `import_live_consensus_inputs`. **S2** (`459cf78d`) adds the NEW **GREEN** `ade_node::rehearsal_evidence` (`//! GREEN`): `PrivateRehearsalManifest` WRAPS a correlate-produced `Ba02Manifest` payload in a structurally distinct, NON-PROMOTABLE rehearsal envelope; closed 1-variant `RehearsalVenue` (`PrivateTestnetC1`); the SOLE constructor `from_correlate_outcome` returns `None` on `NoEvidence` (nothing to wrap, nothing to write); `to_canonical_toml` is pure and always emits `is_rehearsal = true` + `not_bounty_evidence = true` as LITERALS (the type cannot represent a non-rehearsal). Pure — no I/O / clock / rand / float / HashMap. The NEW **RED** `ade_node::rehearsal_pass` (`//! RED`) is the file I/O: `correlate_peer_log_file_into_rehearsal` REUSES `ba02_pass::correlate_peer_log_file` VERBATIM (no alternate correlator); `write_private_rehearsal_manifest` accepts ONLY a `PrivateRehearsalManifest`. New gate `ci_check_rehearsal_manifest_schema.sh` (S2; hardened in S4) — vacuous-until-committed; closed **12-field** rehearsal schema + `venue`/private-testnet + `is_rehearsal`/`not_bounty_evidence` markers + `peer_log_file_sha256` binding; THREE non-promotability barriers (distinct `docs/evidence/` home; the rehearsal markers; a cross-check that NO rehearsal marker appears in any `.toml` under a bounty home). **S3** (`076a5af5`) adds the C1 dry-run runbook (`docs/evidence/phase4-n-f-g-d-private-rehearsal-README.md` — a provable STRICT SUBSET of the G-C preprod operator-pass runbook) + the RED test file `crates/ade_node/tests/node_c1_dry_run_rehearsal.rs` (hermetic `c1_dry_run_correlate_to_rehearsal_envelope` + the env-gated `node_c1_dry_run_rehearsal_live`, `ADE_LIVE_C1_DRY_RUN` — a RED test harness, skipped in CI, NOT a runtime node mode; NoEvidence panics, never writes). NO binary wiring, NO new flag, NO new gate, NO synthetic manifest committed. **S4** (`6bd60c80`) close-surfaced security fix — the rehearsal leak gate now scans ALL real bounty homes (active `docs/clusters/PHASE4-N-F-G-C/` AND archived `docs/clusters/completed/PHASE4-N-F-G-C/`) by building the EXISTING-homes list first (no `[[ -d ]]` whole-check skip), fail-closed (a scan error on an existing home, grep rc≥2, fails closed); new durable regression test `crates/ade_node/tests/rehearsal_gate_archived_home.rs::rehearsal_gate_fails_on_archived_home_leak` (a rehearsal-marked `.toml` under the ARCHIVED home now FAILS the gate). **Registry → 315 rules**: NEW `CN-REHEARSAL-FIDELITY-01` (`tier = release`, `status = enforced` at this close; two coupled clauses — (1) path fidelity + (2) evidence non-promotability). **No rule weakened; no RO-LIVE strengthening bump** (G-D does not advance the bounty deliverable). **2 NEW CI gates (119 → 121):** `ci_check_node_path_fidelity.sh` + `ci_check_rehearsal_manifest_schema.sh`. Tests **+7** (`node_accepted_block_consensus_inputs_via_shared_import`; the 3 `rehearsal_*` unit tests in `rehearsal_pass.rs`; `c1_dry_run_correlate_to_rehearsal_envelope` + `node_c1_dry_run_rehearsal_live`; `rehearsal_gate_fails_on_archived_home_leak`). **Boundary language (load-bearing — do NOT soften / do NOT broaden):** G-D enforces that the private C1 dry-run is a **path-faithful, non-promotable rehearsal HARNESS**. It does **NOT** enforce that a C1 run has succeeded; the rehearsal gate is **vacuous until a real operator-produced manifest is committed** (only the README is committed under the rehearsal home — no `.toml` manifest). **NO RO-LIVE flip** (`RO-LIVE-01` stays `partial`, `RO-LIVE-06` stays schema-only/`enforced`); **no bounty / preview / preprod completion claim**; the live C1 execution stays `blocked_until_operator_c1_net_executed`; private C1 acceptance ≠ bounty completion (preview/preprod acceptance = the single bounty deliverable, captured separately). The G-A..G-E forge / serve / live-feed / containment surfaces are **unchanged**; the three containment / handoff / memory fences (`ci_check_node_run_loop_containment.sh`, `ci_check_served_chain_handoff_fence.sh`, `ci_check_live_feed_memory_bounds.sh`) are **byte-unchanged**.
>
> **PHASE4-N-F-G-E delta (carried — live-feed bounded memory before authoritative decode/apply on the `--mode node` spine):** **NO BLUE crate changed** (456 canonical types unchanged). One slice (S1, `6f848825`); doc commit `5a4d8c12`; close commit `da205bff`. No new module — two closed memory caps added to existing modules, each fail-closed BEFORE the BLUE `ade_codec` decode path. **GREEN `ade_network::session::core`** gains a closed const `MAX_REASSEMBLY_TAIL_BYTES = 16 * 1024 * 1024`: after `drain_protocol_items` drains every COMPLETE item, an incomplete per-mini-protocol reassembly tail `> cap` returns the additive `SessionError::ReassemblyBufferOverflow { protocol, len, cap }` (fail closed — drop the peer; no silent truncation, no partial decode). **GREEN `ade_network::session::event`** adds the closed-enum variant `SessionError::ReassemblyBufferOverflow` (no wildcard; its sole exhaustive consumer `ade_runtime::network::mux_pump::session_err_to_halt` maps it → `PeerHaltReason::ChainSyncDecodeError`). **RED `ade_node::node_sync`** gains a closed const `MAX_WIRE_PUMP_LOOKAHEAD = 256`: `pump_lookahead` stops the opportunistic `try_recv` drain at the cap so the existing bounded `mpsc` (`LIVE_WIRE_PUMP_CHANNEL_CAP = 64`) back-pressures the pump. **RED `ade_runtime::network::mux_pump`** maps the new variant to `PeerHaltReason::ChainSyncDecodeError`. **Registry → 314 rules** (at the G-E close): NEW `DC-LIVEMEM-01` (`tier = derived`, operational-hardening, `status = enforced`); no rule weakened. **One NEW CI gate (118 → 119): `ci_check_live_feed_memory_bounds.sh`** — verifies both bounds are CLOSED LITERAL constants AND not wired to CLI / env / config. `session/` is NOT one of the 9 BLUE `ade_network` submodule `core_paths`, so the new `SessionError` variant is **GREEN-by-content and NOT canonical-counted**. Tests **+4** (`session_reassembly_tail_over_cap_fails_closed`, `session_reassembly_tail_under_cap_still_drains_complete_item`, `wirepump_lookahead_stops_at_cap`, `wirepump_lookahead_cap_preserves_relay_behavior_under_normal_feed`). **Boundary honesty (load-bearing — do NOT soften / do NOT broaden):** the G-E claim is NARROW — **peer-driven memory on the live feed is bounded before authoritative decode/apply.** It is **NOT** full network DoS resistance, **NOT** peer resource fairness, **NOT** BA-02 / live-evidence readiness. Precision: the reassembly check is post-extend, so a single buffer's transient peak is `cap + one <=64 KiB mux frame` (~16.06 MiB); `ProtoBuffers` holds up to ~10 INDEPENDENT per-protocol buffers each capped separately, so the per-connection aggregate ceiling is ~10× the single-buffer cap — still O(constant) per connection (per-connection-COUNT limits / peer fairness are a SEPARATE, out-of-scope surface). **NO BLUE change**; the serve/forge/containment fences are **byte-unchanged**; **no live-evidence / BA-02 / RO-LIVE flip or claim.**
>
> **PHASE4-N-F-G-C delta (carried — live WirePump feed + BA-02 evidence wiring on the `--mode node` spine):** **NO BLUE crate changed** (456 canonical types unchanged). All changes land in the RED binary/driver (`ade_node`), CI, and docs. The cluster closes the **MECHANICAL live-feed + evidence scaffolding ONLY** — it makes the forge-capable `On` arm **live-feed-wireable** and wires the operator-pass BA-02 evidence I/O, **without** claiming peer acceptance. Two sub-themes: **S1** (`71036d10`) RED `ade_node::node_lifecycle` gains `spawn_live_wire_pump_source(peer_addrs, network_magic, recovered_tip) -> NodeBlockSource` — it builds a LIVE `NodeBlockSource::WirePump` from the operator-supplied `--peer` by **reusing** the closed `ade_runtime::admission::{dial_for_admission, run_admission_wire_pump}` VERBATIM. `NodeBlockSource` stays the **closed 2-variant** `{WirePump, InMemory}` (a fill, not a new variant); `ade_node::admission::bootstrap::build_n2n_version_table` is promoted `fn` → `pub(crate) fn` (reused, no reimpl). **S2** (`90791691`) a NEW RED module `ade_node::ba02_pass` (`//! RED`): `correlate_peer_log_file(ade, peer_log_path) -> io::Result<BA02Outcome>` + `write_ba02_manifest(&Ba02Manifest, out)` (accepts ONLY a `Ba02Manifest`); GREEN `ade_node::ba02_evidence` is **unchanged** (`correlate` stays the SOLE `Ba02Manifest` ctor). **Registry stays 313 rules** (no new rule): `RO-LIVE-01` (stays `partial`), `RO-LIVE-06` (stays `enforced`), `CN-OPERATOR-EVIDENCE-01` (stays `enforced`), `DC-NODE-06` (stays `enforced`) each gain `strengthened_in += "PHASE4-N-F-G-C"`. One NEW CI gate (117 → 118): `ci_check_ba02_evidence_manifest_schema.sh`. `ci_check_served_chain_handoff_fence.sh` was **BROADENED in place** (S1). **Boundary honesty (load-bearing — do not soften):** Peer ACCEPT stays **operator-gated** (`RO-LIVE-01` partial / `blocked_until_operator_stake_available`); there is **NO live BA-02 claim** (`RO-LIVE-06` enforces schema + mechanics only); Ade self-accept / served-block / wire success **≠ peer acceptance**.
>
> **PHASE4-N-F-G-B delta (carried — self-accept→serve handoff on the `--mode node` spine):** **NO BLUE crate changed** (456 canonical types unchanged). The cluster proves that **only a BLUE self-accepted forged artifact can be served**, via a **sibling** serve task, **without relaxing relay-loop containment**. NEW GREEN `ade_runtime::producer::self_accepted_handoff::SelfAcceptedHandoff` (a newtype over the BLUE `ade_ledger::producer::AcceptedBlock` with a private field, sole ctor `from_self_accepted`, accessors `accepted()` / `into_accepted()`). A dispatcher-spawned **sibling** served-chain admit task drains a typed `mpsc<SelfAcceptedHandoff>` and admits via the single `ServedChainHandle::push_atomic` fed ONLY by `into_accepted()`. **Registry stays 313 rules**: `DC-NODE-06` flipped **declared → enforced**; `CN-PROD-04` + `CN-CONS-07` gained `strengthened_in += "PHASE4-N-F-G-B"`. One NEW CI gate (116 → 117). **Boundary honesty:** G-B is **serve-authority only** — **NOT** a live-feed cluster; the relay-loop containment gate is **byte-/semantically unchanged**; **NO peer-acceptance / BA-02 / RO-LIVE acceptance claim**.
>
> **PHASE4-N-F-G-A delta (carried — forge fidelity on the `--mode node` spine):** **NO BLUE crate changed** (456 canonical types unchanged). The cluster makes the `--mode node` forge path source **real** forge constants instead of stubs/defaults, and fail closed on two boundaries that the prior path masked: a before-genesis-anchor clock→slot saturation (S3) and an off-epoch forge slot (S4). **Registry → 313 rules**: NEW `DC-EPOCH-03` (`enforced`) + NEW `DC-NODE-06` (`declared` — since flipped by G-B). Seven `strengthened_in += "PHASE4-N-F-G-A"` bumps. Four NEW CI gates. **Boundary honesty:** G-A is **forge-fidelity hardening on the relay spine**. It does **NOT** serve, admit, gossip, or advance a durable tip; the forge stays **subordinate + self-accept-only**. **BA-02 is satisfied nowhere.** The S1 fixture is **evidence input, never runtime authority**.

---

## Conventions

- A **module** in Ade is a Cargo workspace crate (smallest independently-buildable unit). Source: `Cargo.toml` `[workspace] members` (11 entries). One exception to crate-granularity: `ade_network` is split by *submodule color* — `.idd-config.json` `core_paths` resolves BLUE at the submodule path level (9 paths), so its BLUE / GREEN / RED submodules are documented as separate entries.
- Several BLUE crates host sub-trees that carry a *different* TCB color by their own module banner and cluster TCB Color Map; and the RED `ade_runtime` and `ade_node` crates host numerous **GREEN-by-content** sub-trees. These are surfaced as sub-classification notes inside the owning crate's entry (or, where load-bearing, as their own GREEN/RED entries below).
- Modules are listed by TCB color (BLUE → GREEN → RED), alphabetical within each color.

### TCB color sources (in order of authority)

1. `.idd-config.json` `core_paths` — substring match against absolute path. BLUE crate prefixes: `ade_codec`, `ade_types`, `ade_crypto`, `ade_core`, `ade_ledger`, `ade_plutus`. Plus the 9 BLUE `ade_network` submodule paths: `mux/frame.rs`, `codec/`, `handshake/`, `chain_sync/`, `block_fetch/`, `tx_submission/`, `keep_alive/`, `peer_sharing/`, `n2c/`.
2. `.idd-config.json` `_core_paths_doc` — `ade_runtime` is RED; `ade_testkit` is GREEN; `ade_node` is RED; `ade_network::mux::transport` + `ade_network::session` are nominally RED (but `session/` is GREEN-by-content per PHASE4-N-L). `ade_core_interop` is RED.
3. **Module doc-comment banners** — every BLUE/GREEN source file opens with the `// Core Contract:` banner followed by `//! BLUE …` / `//! GREEN …` / `//! RED …`. This is the per-file authority that distinguishes GREEN-by-content from RED inside `ade_runtime` / `ade_node`. Verified directly against the tree at HEAD for every new/changed module in this regeneration (for **PHASE4-N-F-G-D**: `rehearsal_evidence.rs` → `//! GREEN` (the `PrivateRehearsalManifest` envelope; pure — no I/O/clock/rand/float/HashMap), `rehearsal_pass.rs` → `//! RED` (the file I/O over the reused `ba02_pass::correlate_peer_log_file`); no BLUE path touched — both modules live in `ade_node`, NOT a BLUE `core_paths` entry). Carried banners verified unchanged: `ba02_pass.rs` → `//! RED`; `node_lifecycle.rs` / `node_sync.rs` → `//! RED`; `protocol_params.rs` / `genesis_pinning.rs` / `session/core.rs` + `session/event.rs` → `//! GREEN`.
4. Cluster TCB Color Maps under `docs/clusters/` and `docs/clusters/completed/` — §5 "TCB color map (FC/IS partition)" of each `cluster.md`.
5. The invariant registry `docs/ade-invariant-registry.toml` (315 rules at HEAD) — each rule's `code_locus` + `statement` is the authoritative source for the **MUST NOT** rows below.

### Active cluster at HEAD

**PHASE4-N-F-G-J — CLOSED (PrevHash null/hash32 wire authority + position-aware header rule + genesis-successor cold-start forge on the `--mode node` spine).** HEAD `550eec3a` is the close commit (branch `forge/node-empty-feed-tick-g-j`). N-F-G-J is a five-slice cluster: **S1** the closed GREEN feed/forge scheduling-event taxonomy (`ade_node::live_log::{sched_event, sched_writer}` + `ci_check_node_sched_events_emit_only.sh`, `CN-NODE-04`); **S2** the closed BLUE sum `PrevHash = Genesis | Block(Hash32)` + the **POSITION-BLIND** `$hash32/null` codec (`ade_codec::shelley::block::decode_prev_hash` + the `ShelleyHeaderBody` `AdeEncode` null/hash32 match + the producer `Hash32->PrevHash` migration + `ci_check_prevhash_single_wire_authority.sh`, `CN-WIRE-09`); **S3** the NEW BLUE single **POSITION-AWARE** authority `ade_ledger::block_validity::header_position::check_header_position` (called by `decode_block`; `BlockValidityError::HeaderPositionInvalid` -> existing `HeaderInvalid` class) + a real genesis-successor forge (block 0 + `PrevHash::Genesis`); **S4** node-spine cold-start first-block reachability (`node_sync::forge_header_position(Option<&ChainTip>)` + `node_lifecycle::may_cold_start_forge` + `ci_check_genesis_successor_reachability.sh`, `DC-NODE-08` — both-`None` tip forges block 0 through the SAME `run_real_forge -> self_accept -> SelfAcceptedHandoff` path); **S5** the C1 genesis rehearsal harness (reuse-only — `rehearsal_evidence` / `rehearsal_pass` + `node_c1_genesis_rehearsal.rs`; `CN-REHEARSAL-FIDELITY-01` strengthened). ONE NEW BLUE canonical type (`PrevHash`, **456 → 457**) + one NEW BLUE module (`header_position`, a fn + a variant, no struct/enum). Registry → **319 rules** (NEW `CN-WIRE-09` + `DC-NODE-08` + `CN-NODE-04`, all `enforced`); 123 → **126 CI** (NEW `ci_check_node_sched_events_emit_only.sh` + `ci_check_prevhash_single_wire_authority.sh` + `ci_check_genesis_successor_reachability.sh`). On the N-F-G-J close the cluster + slice docs (S1–S5) archive under `docs/clusters/completed/PHASE4-N-F-G-J/`. The predecessor **PHASE4-N-F-G-H — CLOSED (node-spine live serve-to-peer).** HEAD `f59b1eaa` was the S3 close commit (branch `serve/node-spine-peer-g-h`). G-H is a three-slice (+ S2b) cluster that wires `--mode node` to serve real peers **READ-ONLY** from the G-B self-accepted `ServedChainView`, via a sibling listener/serve-dispatch task spawned OUTSIDE `run_relay_loop`: S1 extracts the coordinator-free serve-dispatch core to the shared **NEW RED** `ade_runtime::network::serve_dispatch` (pure code-motion + `produce_mode` re-point + the single-authority gate `ci_check_single_serve_dispatch_authority.sh`); S2 wires the node spine (`run_node_serve_task` + `bind_serve_listener` + closed `ServeStartError`, reusing the existing `--listen` flag); S2b makes the serve listener magic-aware (NEW additive BLUE **fn** `n2n_supported_for_magic` + the `Arc<[..]>`-typed listener config + the gate `ci_check_serve_listener_magic_aware.sh`); S3 the C1 serve runbook + env-gated operator harness. NO new BLUE authority / canonical type (the one BLUE touch is a fn — 456 unchanged); the two NEW serve modules/fns below. Registry → **316 rules** (NEW `DC-NODE-07`, `tier = derived`, `enforced`); 121 → **123 CI** (NEW `ci_check_single_serve_dispatch_authority.sh` + `ci_check_serve_listener_magic_aware.sh`). The predecessor **PHASE4-N-F-G-D** (private-testnet accepted-block bounty DRY-RUN harness) is CLOSED at HEAD `dde78fb8` (S4 slice-span HEAD `6bd60c80`); its cluster + slice docs (S1–S4) are archived under `docs/clusters/completed/PHASE4-N-F-G-D/`. On the G-H close, the G-H cluster + slice docs (S1/S2/S2b/S3) archive under `docs/clusters/completed/PHASE4-N-F-G-H/`.

**Predecessor PHASE4-N-F-G-D (CLOSED — carried).** Added a path-faithful, non-promotable bounty DRY-RUN **harness** on the `--mode node` spine: a path-fidelity proof + fence (S1), a non-promotable rehearsal-evidence surface + gate (S2 — NEW GREEN `ade_node::rehearsal_evidence` + NEW RED `ade_node::rehearsal_pass`), a C1 dry-run runbook + operator-gated execution scaffold (S3), and a close-surfaced rehearsal-leak-gate archived-home hardening (S4). NO BLUE change. `CN-REHEARSAL-FIDELITY-01` enforced (314 → 315). 119 → 121 CI (NEW `ci_check_node_path_fidelity.sh` + `ci_check_rehearsal_manifest_schema.sh`). The predecessor **PHASE4-N-F-G-E** (live-feed bounded memory) is CLOSED at HEAD `da205bff` (S1 slice-span HEAD `6f848825`, doc `5a4d8c12`); archived under `docs/clusters/completed/PHASE4-N-F-G-E/`.

**Predecessor PHASE4-N-F-G-E (CLOSED — carried).** Bounded two pre-existing unbounded peer-driven memory surfaces that the G-C live-feed wiring EXPOSED on the `--mode node` binary path (flagged by the G-C per-cluster security review + SEAMS candidate) — a GREEN per-mini-protocol reassembly-tail cap (`ade_network::session::core`, `MAX_REASSEMBLY_TAIL_BYTES = 16 MiB`, fail-closed via the additive `SessionError::ReassemblyBufferOverflow`) and a RED WirePump lookahead-depth cap (`ade_node::node_sync`, `MAX_WIRE_PUMP_LOOKAHEAD = 256`). `DC-LIVEMEM-01` enforced (313 → 314). 118 → 119 CI (NEW `ci_check_live_feed_memory_bounds.sh`).

**Predecessor PHASE4-N-F-G-C (CLOSED — carried).** Made the forge-capable `On` arm **live-feed-wireable** from `--peer` (reusing the closed admission dial + pump) and wired the operator-pass BA-02 evidence I/O — closing the **MECHANICAL live-feed + evidence scaffolding ONLY**. HEAD `90791691` was the S2 slice-span HEAD; `351d46bc` is the close commit. G-C slices: **S1** (`71036d10` — live WirePump feed wiring), **S2** (`90791691` — BA-02 evidence wiring: the NEW RED `ade_node::ba02_pass` module + `ci_check_ba02_evidence_manifest_schema.sh`).

**Predecessor PHASE4-N-F-G-B (CLOSED — carried).** Proved that **only a BLUE self-accepted forged artifact can be served**, via a **sibling** serve task, **without relaxing relay-loop containment**. NEW GREEN `SelfAcceptedHandoff` constructor fence; the dispatcher-spawned sibling served-chain admit task → the single `push_atomic`. `DC-NODE-06` flipped declared → enforced.

**Predecessor PHASE4-N-F-G-A (CLOSED — carried).** Hardened the N-F-F forge-on path so the constants it forges with are *real*, and so two boundaries the prior path masked **fail closed** (the checked `checked_millis_to_slot` slot-alignment guard; the off-epoch `forge_epoch_admission` fail-closed guard).

**Boundary language (load-bearing — do NOT soften / do NOT broaden).** G-J makes the header `prev_hash` field the closed wire grammar `$hash32 / null` and discharges the position-aware coupling — and nothing more. The raw byte codec (`ade_codec::shelley::block::decode_prev_hash` + the `ShelleyHeaderBody` `AdeEncode` match) is **POSITION-BLIND**: it decodes `null -> Genesis` / `hash32 -> Block` as a pure function of the CBOR token, NEVER consulting `block_number`. The position-AWARE coupling (`block_number 0 <=> Genesis`) lives in EXACTLY ONE BLUE authority, `ade_ledger::block_validity::header_position::check_header_position`, called by `decode_block` BEFORE the header authority and surfacing `BlockValidityError::HeaderPositionInvalid` (coarse class `HeaderInvalid` — **no new `BlockRejectClass`**); it **MUST NOT** live in `ade_codec` and **MUST NOT** be duplicated. No all-zero `Hash32`, no anchor fingerprint, and no Shelley genesis hash may stand in for the genesis predecessor — it is structurally CBOR null (the producer `prev_hash` migrated `Hash32 -> PrevHash` end to end, deleting the all-zero stand-in at its source in `ChainEvolution::prev_hash()`). The node-spine cold-start first-block reachability (`DC-NODE-08`) is **scoped to the hermetic cold-start execution** (NO `genesis_forged` latch); the recovered seed-epoch lineage gates **PERMISSION** to forge from the genesis-successor position, it is **NOT** the source of the prev_hash bytes (which are structurally null); the forge engine takes **no `ChainDb` handle**, so it advances **no durable tip** (durable block-1+ progression is N-U). The S1 scheduling-event vocabulary is **emit-only** — it records the relay loop's feed/forge decisions WITHOUT changing them. **NO RO-LIVE flip** (`RO-LIVE-01` stays `partial` / operator-gated); the cold-start eligibility signal is general (forge-configured + valid recovered base), **never a private-only / C1-only flag**. **No rule weakened.**

**Boundary language (carried — PHASE4-N-F-G-H).** G-H wires a **READ-ONLY serve OF the already-self-accepted `ServedChainView`** — and nothing more. `--mode node` now serves real peers from the G-B self-accepted served chain (the read side of the single `ServedChainHandle` fed by the sibling self-accept→serve push task, DC-NODE-06), via a sibling listener + serve-dispatch task spawned OUTSIDE `run_relay_loop`, through the EXISTING BLUE `producer_chain_sync_serve` + `producer_block_fetch_serve` (+ `producer_chain_sync_advance_tip`) — serving BOTH the ChainSync header advertisement (a follower discovers Ade's served tip on each `ServedChainView` update, DC-CONS-18) AND the BlockFetch body (the follower fetches the served block by hash, DC-CONS-17), under the closed server-agency reply surface (CN-PROTO-06) and the deterministic/total session reducers (DC-PROTO-07, DC-PROTO-08). The claim is **NARROW**: serving = advertising + answering fetches OF already-self-accepted blocks; it is **NOT** a peer-ACCEPTANCE claim (`RO-LIVE-01` stays `partial` / operator-gated), **NOT** BA-02 / live-evidence (`RO-LIVE-06` enforces schema + mechanics only — wire-serve success ≠ peer acceptance), and **NOT** a durable-tip / gossip change. The serve is **request-driven only** (no proactive `advance_tip` / `served_chain_view.changed()` reactor). There is **NO second `ServedChain` authority**, **NO parallel tag-24 serializer** (the single `CN-WIRE-08` envelope authority is reused), **NO second serve-dispatch definition** (exactly one in `ade_runtime`, both modes call it), and **NO serve / `push_atomic` / served-chain mutation inside `run_relay_loop`** (the containment gate `ci_check_node_run_loop_containment.sh` stays SEMANTICALLY UNCHANGED, CN-NODE-02). `--mode node` reuses the EXISTING `--listen` flag (**NO new `--mode node` flag**), and there is **NO new BLUE authority / canonical type / variant** (the lone BLUE touch is the additive fn `n2n_supported_for_magic`). The live C1 serve execution stays `blocked_until_operator_c1_serve_executed`.

**Boundary language (carried — PHASE4-N-F-G-D).** G-D enforces that the private C1 dry-run is a **path-faithful, non-promotable rehearsal HARNESS** — and nothing more. (1) PATH FIDELITY: the C1 private dry-run uses the SAME `--mode node` accepted-block path as preview/preprod (`import_live_consensus_inputs` → forge → self-accept → sibling-serve → block-fetch → peer log → `correlate`), with NO private-only flag / branch / bootstrap authority / from-genesis constructor; the only differences are operator-controlled INPUTS (a private genesis whose stake allocation makes Ade win slots fast) + the evidence LABEL. No private-only helper may make the rehearsal pass if the same condition would fail on preview/preprod — such a condition is a shared-path bug to fix in the shared path, never special-cased. (2) EVIDENCE NON-PROMOTABILITY: any private-testnet manifest is clearly marked rehearsal / private-testnet, stored ONLY under the rehearsal home (`docs/evidence/phase4-n-f-g-d-private-rehearsal-*.toml`, never the bounty home), sha256-bound to a real Haskell peer log, correlate-produced (`ba02_evidence::correlate` is the sole acceptance-evidence constructor), and flips NO RO-LIVE rule. G-D does **NOT** enforce that a C1 run has succeeded — the rehearsal gate is **vacuous until a real operator-produced manifest is committed** (only the README is committed under the rehearsal home at HEAD; no `.toml` manifest). There is **NO RO-LIVE flip** (`RO-LIVE-01` stays `partial`; `RO-LIVE-06` stays schema-only/`enforced`; neither gains a G-D strengthening), **NO bounty / preview / preprod completion claim**, and the live C1 execution stays `blocked_until_operator_c1_net_executed`. Private C1 acceptance ≠ bounty completion; preview/preprod acceptance = the single bounty deliverable, captured separately. The G-A..G-E forge / serve / live-feed / containment surfaces are **unchanged**; the three containment / handoff / memory fences are **byte-unchanged**.

**Boundary language (carried — PHASE4-N-F-G-E).** G-E bounds **peer-driven memory on the live `--mode node` feed BEFORE authoritative decode/apply** — and nothing more. Two closed caps, each fail-closed in front of the BLUE `ade_codec` decode path. The claim is **NARROW**: it is **NOT** full network DoS resistance, **NOT** peer resource fairness, **NOT** BA-02 / live-evidence readiness. The bounds are **CLOSED CONSTANTS — defensive implementation bounds, NOT Cardano semantic parameters**; a future hardening slice may *tighten* them, but no runtime option (CLI / env / config) may disable them or set them unbounded. There is **NO BLUE change**, **NO new `NodeBlockSource` / `CoordinatorEvent` variant**, **NO serve / forge / containment change**, and **NO live-evidence / BA-02 / RO-LIVE flip or claim**.

**Boundary language (carried — PHASE4-N-F-G-C).** G-C closes the **MECHANICAL live-feed + evidence scaffolding ONLY**. Peer ACCEPT is **operator-gated** (`RO-LIVE-01` partial / `blocked_until_operator_stake_available`); there is **NO live BA-02 claim**; Ade self-accept / served-block / wire success **≠ peer acceptance**; `correlate` is the sole `Ba02Manifest` constructor; **no synthetic manifest is committed**.

**Boundary language (carried — PHASE4-N-F-G-B).** G-B is **serve-authority only** — it proves the serve *mechanism* on the `--mode node` spine admits **only** a BLUE self-accepted artifact. The relay-loop containment gate is **byte-/semantically unchanged**, and it makes **NO peer-acceptance / BA-02 / RO-LIVE acceptance claim**.

**Boundary language (carried — PHASE4-N-F-G-A).** G-A is **forge-fidelity hardening on the relay spine**. It does **NOT** serve / admit / gossip / advance a durable tip. The forge stays **subordinate + self-accept-only**. The S1 reference fixture is **evidence input, never runtime authority**.

**What the binary `Node` arm actually runs (precise wiring honesty):**
- `main()` `Mode::Node → run_node_lifecycle → run_node_lifecycle_inner`. The arm is **fully wired and durable for bootstrap + recovery**: a true first run runs `first_run_mithril_bootstrap` (fail-closes on `verify_mithril_binding` before any state is admitted); a non-empty store runs `warm_start_recovery` (WAL replay → verified recovered `BootstrapState`). Both arms produce the **single recovered `BootstrapState`**.
- **N-F-F forge-intent flip (carried):** the arm calls `classify_forge_intent(...)`. `Off` → `run_relay_loop(…, None)`; `On(paths)` → `build_operator_forge_material → coordinator_init → ForgeActivation → run_relay_loop(…, Some(&mut activation))`; `PartialKeySet` → `NodeLifecycleError::ForgeKeyIngress`.
- **G-A forge constants / clock guard / epoch guard (carried):** the `On` arm's `ForgeActivation` derives `protocol_version` + `pparams` from the recovered current ledger view; the operator config is ingested through the real `parse_opcert_envelope` + `parse_shelley_genesis`; the `ForgeTick` branch derives its slot via `checked_millis_to_slot` (before-anchor fails closed); `forge_one_from_recovered` calls `forge_epoch_admission(...)` **before** `query_leader_schedule` (off-epoch fails closed before leadership / KES signing).
- **G-B self-accept→serve handoff (carried):** the `On` arm spawns a **sibling** served-chain admit task draining a typed `mpsc::UnboundedSender<SelfAcceptedHandoff>` and admitting via the single `ServedChainHandle::push_atomic(handoff.into_accepted())`; the relay loop does a best-effort typed `tx.send(h)` only.
- **G-C live WirePump feed (carried):** the `On` arm wires `spawn_live_wire_pump_source(&cli.peer_addrs, network_magic, state.tip.as_ref())` when `--peer` is supplied (else `NodeBlockSource::in_memory(Vec::new())`); it reuses the closed `ade_runtime::admission::{dial_for_admission, run_admission_wire_pump}` + the `pub(crate)`-promoted `build_n2n_version_table`. **No new `NodeBlockSource` variant, no second tip-advance, no verdict.**
- **G-C BA-02 evidence I/O (carried):** the RED `ade_node::ba02_pass` module is the operator-pass evidence file I/O (`correlate_peer_log_file` + `write_ba02_manifest`). It is **reached by no binary arm** (exercised by `crates/ade_node/tests/node_operator_pass_ba02.rs`).
- **G-E live-feed memory bounds (carried):** the live `WirePump` feed is memory-bounded BEFORE authoritative decode/apply by two closed caps — the GREEN `session::core` 16 MiB reassembly-tail cap (`SessionError::ReassemblyBufferOverflow`, drop the peer) and the RED `node_sync::pump_lookahead` 256-block lookahead cap (back-pressuring the bounded mpsc). No new variant, no second tip-advance, no verdict.
- **G-D rehearsal harness (NEW):** the C1 dry-run is a **path-faithful, non-promotable rehearsal HARNESS**, not a runtime node mode and **not wired into any binary arm**. The MECHANICAL half is the path-fidelity fence (`ci_check_node_path_fidelity.sh`), the GREEN `rehearsal_evidence::PrivateRehearsalManifest` non-promotable envelope, the RED `rehearsal_pass` file I/O (which reuses `ba02_pass::correlate_peer_log_file` verbatim), the rehearsal-schema gate (`ci_check_rehearsal_manifest_schema.sh`), and a dry-run runbook that is a provable STRICT SUBSET of the G-C preprod operator-pass runbook. The OPERATOR-GATED half (the actual C1 live execution) stays `blocked_until_operator_c1_net_executed` and is exercised only by the env-gated RED test `crates/ade_node/tests/node_c1_dry_run_rehearsal.rs::node_c1_dry_run_rehearsal_live` (`ADE_LIVE_C1_DRY_RUN=1`; skipped in CI). **No binary wiring, no new `--mode node` flag, no new bootstrap / from-genesis constructor, no synthetic manifest committed (the rehearsal gate stays vacuous), no RO-LIVE flip, no BLUE change.**
- **G-H node-spine serve-to-peer (NEW):** on the `On` arm, when `--listen <addr>` is supplied, `run_node_lifecycle_inner` calls `bind_serve_listener(listen)` (fail-fast on a bad/occupied addr via the closed `ServeStartError`) and `tokio::spawn`s `run_node_serve_task(...)` **OUTSIDE `run_relay_loop`** (mirroring the G-B sibling push task). The serve task is **request-driven**: it accepts N2N connections (advertising via `n2n_supported_for_magic(network_magic)`), and per inbound server-frame event drives the shared `ade_runtime::network::serve_dispatch::dispatch_server_frame_event_to_outbound` over the retained GREEN `ServedChainView` — the BLUE `producer_chain_sync_serve` advertises the served tip + `producer_block_fetch_serve` answers the body fetch. It admits nothing, mutates no served chain, advances no durable tip, and makes **no peer-acceptance claim** (`RO-LIVE-01` partial). The relay loop is untouched (`ci_check_node_run_loop_containment.sh` byte-unchanged). With no `--listen`, no serve task is spawned. The live C1 serve is exercised only by the env-gated RED test `crates/ade_node/tests/node_c1_serve_live.rs::node_c1_serve_live` (`ADE_LIVE_C1_SERVE=1`; skipped in CI).
- **G-J PrevHash + position-aware header rule + genesis-successor cold-start forge (NEW):** the header `prev_hash` is now the closed sum `PrevHash` (`Genesis` = CBOR null, `Block(Hash32)` = hash32), decoded POSITION-BLIND by `ade_codec::shelley::block::decode_prev_hash`; the position rule (`block_number 0 <=> Genesis`) is enforced by the single BLUE `ade_ledger::block_validity::header_position::check_header_position`, called by `decode_block` ahead of the header authority. On the `On` arm, the `LoopStep::ForgeTick` branch derives `(block_number, prev_hash)` via the GREEN `node_sync::forge_header_position(selected_tip.as_ref())` (the ONE cold-start convention: `None => (0, PrevHash::Genesis)`; `Some => (last_block_no+1, PrevHash::Block(tip.hash))`; a tip-without-height edge fails closed as `NodeForgeError::RecoveredTipMissingBlockNo` — never `.unwrap_or(1)`). When `ChainDb::tip()` AND the recovered tip are BOTH `None`, the GREEN `node_lifecycle::may_cold_start_forge(selected_tip_present=false, has_recovered_lineage, feed_eligible)` permits forging block 0 + `PrevHash::Genesis` through the SAME `run_real_forge -> self_accept -> SelfAcceptedHandoff -> ServedChainView` path (DC-NODE-08), gated by the recovered seed-epoch lineage + `ForgeIntent::On` + a forge-eligible feed (`no_block_available | clean_empty`). The forge engine holds NO `ChainDb` handle and advances NO durable tip (durable block-1+ = N-U); the cold-start fires within the hermetic execution (no `genesis_forged` latch). `run_relay_loop_with_sched` additionally records the closed GREEN `NodeSchedEvent` scheduling diagnostics to stderr (emit-only; never alters scheduling). With no `--peer` the loop still halts before any `ForgeTick` unless a cold-start is permitted — observable forge stays RO-LIVE-01.
- **Empty-source honesty (no-`--peer` arm, carried).** With no `--peer`, `NodeBlockSource::in_memory(Vec::new())` is empty; the planner halts on iteration 1 BEFORE any `SyncOnce` or `ForgeTick`. The `On` arm is **forge-CAPABLE but not observable** without a live feed. An operator-witnessed accepted block is the RO-LIVE-01 follow-on.

**G-J added exactly ONE BLUE canonical type + ONE NEW BLUE module** (verified: `git diff --name-only main..HEAD` touches the BLUE `core_paths` files `crates/ade_types/src/shelley/block.rs` (the NEW `PrevHash` enum, replacing the prior flat `prev_hash: Hash32` field — `ade_types` `(pub )?(struct|enum)` count `81 -> 82`), `crates/ade_codec/src/shelley/block.rs` (the POSITION-BLIND `decode_prev_hash` fn + the `AdeEncode` null/hash32 match — no struct/enum, `ade_codec` holds at 11), `crates/ade_ledger/src/block_validity/{header_position.rs (NEW), header_input.rs, verdict.rs, mod.rs, unsigned_header_pre_image.rs}` + `crates/ade_ledger/src/producer/{forge.rs, state.rs}` (the NEW `check_header_position` fn + the `BlockValidityError::HeaderPositionInvalid` *variant* + the `ProducerTick.prev_hash: PrevHash` migration — no NEW struct/enum, `ade_ledger` holds at **177**). Net BLUE canonical-type delta: **+1** (`PrevHash`), total **456 → 457**. Every other BLUE crate count is byte-stable at both `main` and HEAD: `ade_core` 49, `ade_crypto` 21, `ade_plutus` 8; `ade_network` BLUE submodules 109. The NEW GREEN `ade_node::live_log::{sched_event, sched_writer}` modules and the GREEN-by-function `node_sync::forge_header_position` / `node_lifecycle::may_cold_start_forge` decisions live in RED `ade_node` — none is canonical-counted.)

**No BLUE authority or canonical type was added by G-H** (verified: `git diff --name-only 6bd60c80..f59b1eaa` touches exactly ONE BLUE `core_paths` file — `crates/ade_network/src/handshake/version_table.rs` — and that change adds only the public **function** `n2n_supported_for_magic` + its unit test, **no `struct`/`enum`**; every BLUE crate's `(pub )?(struct|enum)` count is byte-stable at both `6bd60c80` and `f59b1eaa`, total **456**). The NEW `ade_runtime::network::serve_dispatch` module is RED (it lives in RED `ade_runtime`); the NEW `ade_node` serve wiring (`run_node_serve_task` / `bind_serve_listener` / `ServeStartError`) is RED — none is canonical-counted. Canonical-type total holds at **456**.

**No BLUE crate was modified by G-D** (verified: `git diff --name-only da205bff..6bd60c80` matches no `.idd-config.json` `core_paths` entry — the changed `crates/` files are `ade_node/src/{lib.rs, rehearsal_evidence.rs, rehearsal_pass.rs}` and `ade_node/tests/{node_c1_dry_run_rehearsal.rs, node_path_fidelity.rs, rehearsal_gate_archived_home.rs}`, all RED-crate; the rest are CI, the registry, and docs; every BLUE crate's `(pub )?(struct|enum)` count is unchanged at both `6f848825` and `6bd60c80`). The two NEW `ade_node` modules are NOT canonical-counted (`rehearsal_evidence` is GREEN-by-content; `rehearsal_pass` is RED). Canonical-type total holds at **456**.

The cluster (PHASE4-N-F-G-D) introduces / extends:
- **GREEN (non-promotable rehearsal-evidence envelope)** `ade_node::rehearsal_evidence` (NEW module, S2 — `//! GREEN`) — `PrivateRehearsalManifest` WRAPS a correlate-produced `Ba02Manifest` payload in a structurally distinct, NON-PROMOTABLE rehearsal envelope. Closed 1-variant `RehearsalVenue { PrivateTestnetC1 }` (a non-private venue is unrepresentable). `RehearsalEnvelope` carries the venue + the committed peer-log filename + its sha256. The SOLE constructor `from_correlate_outcome(&BA02Outcome, RehearsalEnvelope) -> Option<Self>` wraps `BA02Outcome::Ba02Manifest` and returns `None` on `NoEvidence` (nothing to wrap, nothing to write) — so a rehearsal manifest is ALWAYS correlate-produced. `to_canonical_toml(&self) -> String` is pure and ALWAYS emits `is_rehearsal = true` + `not_bounty_evidence = true` as literals (the type cannot serialize a non-rehearsal). `REHEARSAL_MANIFEST_SCHEMA_VERSION = 1`. Registered `pub mod rehearsal_evidence;` in `lib.rs`. Pure — no I/O / clock / rand / float / HashMap. Backs `CN-REHEARSAL-FIDELITY-01` clause 2. *(Full entry in GREEN Modules below.)*
- **RED (rehearsal-evidence file I/O — reuse, no reimpl)** `ade_node::rehearsal_pass` (NEW module, S2 — `//! RED`) — `correlate_peer_log_file_into_rehearsal(ade, peer_log_path, envelope) -> io::Result<Option<PrivateRehearsalManifest>>` REUSES `ba02_pass::correlate_peer_log_file` VERBATIM (no alternate correlator), then wraps a correlate-produced payload in the envelope; `Ok(None)` iff `correlate` returned `NoEvidence`; a missing/unreadable file fails closed (`io::Error`). `write_private_rehearsal_manifest(&PrivateRehearsalManifest, out_path) -> io::Result<()>` accepts ONLY a `PrivateRehearsalManifest` (the argument type is the gate). Registered `pub mod rehearsal_pass;` in `lib.rs`. Constructs no evidence, synthesizes no acceptance. *(Full entry in RED Modules below.)*
- **NEW CI gate** `ci_check_node_path_fidelity.sh` (S1, CN-REHEARSAL-FIDELITY-01 clause 1) — guard (a): the `crates/ade_node/src/cli.rs` argv flag-literal set equals the pinned closed **28-flag** allow-list (G-D adds none; a private-only / venue flag — e.g. `--private-net`, `--from-genesis`, `--devnet`, `--rehearsal` — would change the set and trip the guard). guard (b): no fn whose name carries BOTH `genesis` and `consensus` exists (line comments stripped first so prose naming the forbidden construct cannot self-trip) AND `node_lifecycle.rs` sources consensus inputs via the shared `import_live_consensus_inputs`. The C1 dry-run differs from the preprod pass only in operator INPUTS + the evidence LABEL — never in code.
- **NEW CI gate** `ci_check_rehearsal_manifest_schema.sh` (S2; hardened in S4, CN-REHEARSAL-FIDELITY-01 clause 2) — when a committed `docs/evidence/phase4-n-f-g-d-private-rehearsal-*.toml` manifest is present, it verifies the closed **12-field** rehearsal schema (`schema_version`, `venue`, `is_rehearsal`, `not_bounty_evidence`, `peer_log_file`, `peer_log_file_sha256`, `forged_block_hash_hex`, `slot`, `network_magic`, `peer_accept_source`, `peer`, `matched_block_hash_hex`), `schema_version == 1`, `is_rehearsal = true` + `not_bounty_evidence = true`, a `venue` of `private-testnet*`, and that `peer_log_file_sha256` matches the actual SHA-256 of the committed peer-log fixture. When NO manifest is committed (the typical state — the C1 dry-run is `blocked_until_operator_c1_net_executed`), the gate is **vacuously satisfied**. THREE non-promotability barriers: the distinct `docs/evidence/` home; the explicit rehearsal markers; and a cross-check (S4 hardening) that NO rehearsal marker (`^is_rehearsal` / `^not_bounty_evidence`) appears in any `.toml` under EITHER bounty home (active `docs/clusters/PHASE4-N-F-G-C/` AND archived `docs/clusters/completed/PHASE4-N-F-G-C/`) — built from the EXISTING-homes list first (no `[[ -d ]]` whole-check skip), fail-closed on a scan error (grep rc≥2) over an existing home.
- **NEW runbook** `docs/evidence/phase4-n-f-g-d-private-rehearsal-README.md` (S3) — the C1 dry-run execution runbook, a provable STRICT SUBSET of the G-C preprod operator-pass runbook (same `--mode node` path / `--peer` feed / operator-key flow / peer-log capture / `correlate` / NoEvidence fail-closed; ONLY `venue=private-testnet-c1` + operator genesis stake + the `PrivateRehearsalManifest` envelope differ — the strict-subset property is ANCHORED by the S1 path-fidelity fence).
- **NEW test files** `crates/ade_node/tests/node_path_fidelity.rs` (S1 — `node_accepted_block_consensus_inputs_via_shared_import`), `crates/ade_node/tests/node_c1_dry_run_rehearsal.rs` (S3 — `c1_dry_run_correlate_to_rehearsal_envelope` + env-gated `node_c1_dry_run_rehearsal_live`), `crates/ade_node/tests/rehearsal_gate_archived_home.rs` (S4 — `rehearsal_gate_fails_on_archived_home_leak`). Plus the 3 `rehearsal_*` unit tests inside `crates/ade_node/src/rehearsal_pass.rs`.
- **Registry:** 314 → **315**. NEW `CN-REHEARSAL-FIDELITY-01` (`tier = release`, `status = enforced` at this close; `introduced_in = "PHASE4-N-F-G-D"`; `strengthened_in = []`; two coupled clauses — path fidelity + evidence non-promotability; `ci_script = ci/ci_check_node_path_fidelity.sh, ci/ci_check_rehearsal_manifest_schema.sh`; 7 tests; `cross_ref` = RO-LIVE-01 / RO-LIVE-06 / CN-OPERATOR-EVIDENCE-01 / DC-NODE-06 / DC-EPOCH-03 / CN-NODE-02 / CN-CINPUT-03 / DC-CINPUT-02b). **No rule weakened; no RO-LIVE strengthening bump.**

**Governance note (PHASE4-N-F-G-D).** G-D upholds the load-bearing structural lines. **A HARNESS, not an acceptance claim:** G-D ships a path-fidelity fence + a non-promotable rehearsal-evidence surface + a dry-run runbook + an operator-gated execution scaffold, but **claims no peer acceptance and no bounty completion** — `RO-LIVE-01` stays `partial`, `RO-LIVE-06` enforces schema + mechanics only, and the rehearsal gate is vacuous-until-committed. **Path fidelity is mechanically fenced:** `ci_check_node_path_fidelity.sh` forbids any new `--mode node` flag (28-flag pinned allow-list) and any from-genesis consensus-inputs constructor — the C1 dry-run differs from preprod only in operator inputs + the evidence label, never in code. **Evidence is non-promotable:** `correlate` stays the sole `Ba02Manifest` ctor; the `PrivateRehearsalManifest` type cannot represent a non-rehearsal; three independent barriers (distinct home, explicit markers, bounty-home leak cross-check) keep a rehearsal manifest out of the bounty path. **Closed surfaces stay closed:** `RehearsalVenue` is a closed 1-variant enum; `NodeBlockSource` / `CoordinatorEvent` are unchanged; no new BLUE authority / canonical type / argv flag / bootstrap. **Containment is untouched:** the relay-loop containment gate, the served-chain handoff fence, and the live-feed memory bounds are byte-unchanged. **No overclaim:** private C1 acceptance ≠ bounty completion; the live C1 execution stays `blocked_until_operator_c1_net_executed`.

**Governance note (PHASE4-N-F-G-H).** Read-only serve, not an authority change. The serve task serves the *already-self-accepted* `ServedChainView` (it produces no authoritative output); it reuses the EXISTING BLUE serve reducers + the single `CN-WIRE-08` tag-24 serializer (no parallel serializer). The single-serve-dispatch authority is **mechanically unique** — exactly one `dispatch_server_frame_event_to_outbound` in `ade_runtime`, both modes call it, `ci_check_single_serve_dispatch_authority.sh` forbids a duplicate. Containment is untouched (the serve task is OUTSIDE `run_relay_loop`; the relay-loop containment gate is byte-unchanged). `DC-NODE-07` is `tier = derived` (NOT BLUE consensus law). No overclaim — serving ≠ peer acceptance ≠ BA-02; no `RO-LIVE` flip; the live C1 serve stays operator-gated. The lone BLUE touch is an additive fn (`n2n_supported_for_magic`); version negotiation stays closed + enumerated (only `network_magic` is parameterized).

**Governance note (carried — PHASE4-N-F-G-E).** Operational hardening, not an authority change: `DC-LIVEMEM-01` is `tier = derived` (explicitly NOT BLUE consensus law). The cap fires before the BLUE decode path. Closed source/enum surfaces stay closed (the reassembly cap fails closed via an additive closed-enum variant; both bounds are closed literal constants with no runtime escape hatch). Containment is untouched. No overclaim — bounded memory before authoritative decode/apply, NOT network DoS resistance, NOT peer fairness; no `RO-LIVE` flip.

**Governance note (carried — PHASE4-N-F-G-C).** Mechanical scaffolding, not an acceptance claim. Reuse, not reimplementation (the live feed reuses `dial_for_admission` + `run_admission_wire_pump` verbatim). Closed source stays closed (`NodeBlockSource` is still the 2-variant `{WirePump, InMemory}`). `correlate` is the sole `Ba02Manifest` ctor; no synthetic manifest is committed.

**Governance note (carried — PHASE4-N-F-G-B).** Serve authority, not a relaxation. Only self-accepted artifacts serve. Single serve mutation via the single `push_atomic`. No new BLUE authority, no new `CoordinatorEvent` variant, no live feed, no peer-acceptance / BA-02 / RO-LIVE claim.

**Governance note (carried — PHASE4-N-F-G-A).** The forge stays subordinate; the single recovered `BootstrapState` is the forge base (no second bootstrap); the forge advances no tip and is self-accept-only. The two new boundaries are fail-closed walls, not silent saturations. The S1 fixture is evidence input, never runtime authority. `protocol_params` carries no float path.

**Carried-forward (PHASE4-N-F-G-H, node-spine serve-to-peer).** NEW RED `ade_runtime::network::serve_dispatch` (the single shared `dispatch_server_frame_event_to_outbound` + `install_/remove_server_peer_state` + `ServerPeerStates` + `DispatchError` + GREEN `ServedBlockEvidence`, moved from `produce_mode`); NEW additive BLUE fn `ade_network::handshake::version_table::n2n_supported_for_magic`; RED `ade_runtime::network::n2n_listener` `our_supported: Arc<[(u16, VersionData)]>`; NEW RED `ade_node::node_lifecycle::{run_node_serve_task, bind_serve_listener, ServeStartError}`; `ade_node::produce_mode` re-pointed at the shared serve core + magic-aware listener. `DC-NODE-07` enforced. **G-H is the active surface; everything below it (the G-A..G-E forge / handoff / live-feed / memory surfaces) is byte-unchanged by G-H** (the serve is read-only over the view).

**Carried-forward (PHASE4-N-F-G-E, live-feed bounded memory).** GREEN `ade_network::session::core` closed `MAX_REASSEMBLY_TAIL_BYTES = 16 MiB` reassembly-tail cap + the additive closed `SessionError::ReassemblyBufferOverflow` (GREEN `ade_network::session::event`); RED `ade_node::node_sync` closed `MAX_WIRE_PUMP_LOOKAHEAD = 256` lookahead cap; RED `ade_runtime::network::mux_pump` variant→`PeerHaltReason::ChainSyncDecodeError` mapping. `DC-LIVEMEM-01` enforced. **G-D does not alter the G-E live-feed memory bounds — they are byte-unchanged.**

**Carried-forward (PHASE4-N-F-G-C, live WirePump feed + BA-02 evidence).** RED `ade_node::node_lifecycle::spawn_live_wire_pump_source` + the `On`-arm `--peer` wire; the `pub(crate)`-promoted `admission::bootstrap::build_n2n_version_table`; the NEW RED `ade_node::ba02_pass` module (`correlate_peer_log_file` + `write_ba02_manifest`). `RO-LIVE-01` partial / `RO-LIVE-06` enforced. **G-D reuses `ba02_pass::correlate_peer_log_file` verbatim in `rehearsal_pass` — it does not alter the G-C live feed or BA-02 I/O.**

**Carried-forward (PHASE4-N-F-G-B, self-accept→serve handoff).** GREEN `ade_runtime::producer::self_accepted_handoff::SelfAcceptedHandoff`; RED `ade_node::produce_mode::run_real_forge` return-shape widening; RED `ade_node::node_sync::forge_one_from_recovered` handoff wrap; RED `ade_node::node_lifecycle` sibling served-chain admit task. DC-NODE-06 enforced. **G-D does not alter the serve mechanism.**

**Carried-forward (PHASE4-N-F-G-A, forge fidelity).** GREEN `ade_runtime::consensus_inputs::protocol_params`; GREEN `ade_runtime::clock::checked_millis_to_slot` + closed `SlotAlignmentError`; GREEN `ade_runtime::consensus_inputs::canonical::require_forge_current_pparams`; GREEN `ade_testkit::consensus::genesis_pinning` (`#[cfg(test)]`); GREEN-by-function `ade_node::node_sync::forge_epoch_admission` + closed `ForgeEpochAdmission` (DC-EPOCH-03 enforced); RED `ade_node::operator_forge` real parsers; RED `ade_node::admission::{seed_to_snapshot, bootstrap}` current-pparams install. **G-D does not alter any G-A surface** — it pins the shared `import_live_consensus_inputs` path the G-A forge constants ride.

**Carried-forward (PHASE4-N-F-F, operator-key ingress + the forge-on flip).** GREEN `ade_node::forge_intent`; RED `ade_node::operator_forge`; RED `ade_node::node_lifecycle` (`--mode node` arm classifies forge intent). CN-NODE-03 enforced. **G-D does not alter the intent classifier or the custody-confined ingress surface.**

**Carried-forward (PHASE4-N-F-E / N-F-D, the forge-tick + run-loop spine).** GREEN `ade_node::run_loop_planner` (closed 4-variant `LoopStep` incl. `ForgeTick`; content-blind `ForgeSlotStatus`); RED `ade_node::node_lifecycle::run_relay_loop`; RED `ade_node::node_sync`'s content-blind `NodeBlockSource` readiness. `run_node_sync` is UNMODIFIED. DC-NODE-05 / CN-NODE-02 enforced. **G-D leaves `run_relay_loop`'s containment byte-unchanged.**

**Carried-forward (PHASE4-N-F-C, the node lifecycle).** RED `ade_node::node_lifecycle` (the single `PHASE4-N-F-C-LIFECYCLE-OWNER`), RED `ade_node::node_sync` (closed `NodeBlockSource`; `run_node_sync`; `forge_one_from_recovered`), GREEN `ade_node::ba02_evidence` (the BA-02 evidence correlator — consumed by `ba02_pass` and, transitively, by the G-D `rehearsal_pass`), the `Mode::Node` variant. CN-NODE-01 / DC-SYNC-01 / RO-LIVE-06.

**Carried-forward (PHASE4-N-F-A, still load-bearing below).** The recovered seed-epoch consensus-input CAPABILITY surface — **BLUE** `ade_ledger::seed_consensus_inputs` (closed `SeedEpochConsensusInputs` + SOLE codec, `SEED_CINPUT_SCHEMA_VERSION = 1`), the `PoolDistrView::from_seed_epoch_consensus_inputs` projection (`consensus_view`), the additive `WalEntry::SeedEpochConsensusInputsImported` (tag 3) + `ReplayOutcome`/`RecoveredBootstrapProvenance` (`wal`), **GREEN** `ade_runtime::seed_consensus_merge`, **RED** `ade_runtime::{seed_consensus_provenance, bootstrap, genesis_bootstrap, mithril_bootstrap}`, and the anchor-fp-keyed `chaindb` sidecar surface (redb `SCHEMA_VERSION = 3`) — is inventoried unchanged. **Carried-forward (PHASE4-N-Z / N-Y).** The Mithril production-bootstrap composition, forward-sync (`forward_sync::{reducer, pump}`), and the bootstrap-anchor / genesis-source BLUE types are unchanged in shape.

**Cluster-doc location.** On the G-H close, the PHASE4-N-F-G-H cluster doc + slice docs (S1/S2/S2b/S3) archive under `docs/clusters/completed/PHASE4-N-F-G-H/`. The predecessor PHASE4-N-F-G-D cluster doc + slice docs (S1–S4) are archived under `docs/clusters/completed/PHASE4-N-F-G-D/`. The predecessor **PHASE4-N-F-G-E** set (cluster + S1) is archived under `docs/clusters/completed/PHASE4-N-F-G-E/`; the **PHASE4-N-F-G-C** set (cluster + S1/S2) under `docs/clusters/completed/PHASE4-N-F-G-C/`; the **PHASE4-N-F-G-B** set under `docs/clusters/completed/PHASE4-N-F-G-B/`; the **PHASE4-N-F-G-A** set under `docs/clusters/completed/PHASE4-N-F-G-A/`. Every prior closed cluster doc — the **PHASE4-N-F-A / N-F-C / N-F-D / N-F-E / N-F-F** sets, the entire **PHASE4-N-Q / N-R-\* / N-S-\*** set, the **N-M-\*** sub-trees, **N-O**, **N-P**, **N-T**, **N-V**, **N-W**, **N-X**, **N-Y**, **N-Z** — is archived under `docs/clusters/completed/`.

This regeneration is an **incremental, scoped refresh** of the prior full inventory (on-disk doc at the N-F-G-D close, HEAD `6bd60c80`, 121 CI checks, 315 rules) to catch up through **PHASE4-N-F-G-H** (node-spine live serve-to-peer, closing now at `f59b1eaa`), plus the mechanical counts that drifted. The refresh is **scoped to the N-F-G-H surface — the NEW RED `ade_runtime::network::serve_dispatch` shared serve-dispatch core + the additive BLUE fn `n2n_supported_for_magic` + the `Arc<[..]>`-typed `n2n_listener` config + the NEW RED node-spine serve wiring (`run_node_serve_task` / `bind_serve_listener` / `ServeStartError`) + the two NEW serve gates — plus the mechanical counts that drifted**; all unrelated module entries (every BLUE crate, and the N-Q/N-R/N-S/N-W/N-X/N-Y/N-Z/N-F-A…N-F-G-D surfaces) are carried forward unchanged.

### Counts (mechanical, with sources)

| Count | Value | Source / command |
|---|---|---|
| Crates | **11** | `grep -cE '"crates/' Cargo.toml` (`[workspace] members`). **Δ vs prior (11): 0.** G-J added no crate. |
| Canonical types | **457** | `grep -rhE "^(pub )?(struct\|enum) " --include='*.rs'` over the 6 BLUE crate `src/` trees + the 9 BLUE `ade_network` submodule paths, summed at the carried baseline. Breakdown (carried frozen snapshot): `ade_codec` 11, `ade_types` 81, `ade_crypto` 21, `ade_core` 49, `ade_ledger` **177**, `ade_plutus` 8 (= 347) + `ade_network` BLUE submodules 109 (`mux/frame.rs` 5, `codec/` 38, `handshake/` 9, `chain_sync/` 11, `block_fetch/` 10, `tx_submission/` 5, `keep_alive/` 5, `peer_sharing/` 5, `n2c/` 21). `canonical_type_registry: null`, so the structural grep is authoritative. **Δ vs prior CODEMAP (456): 0** — `git diff --name-only 6bd60c80..f59b1eaa` touches exactly ONE BLUE `core_paths` file (`crates/ade_network/src/handshake/version_table.rs`), and that change adds only the public **function** `n2n_supported_for_magic` (+ its unit test) — NO new `struct`/`enum` (the `handshake` BLUE submodule count holds at 9). The other changed `crates/` files are RED (`ade_runtime/src/network/{mod.rs, n2n_listener.rs, serve_dispatch.rs}`, `ade_node/src/{node_lifecycle.rs, produce_mode.rs, admission/bootstrap.rs}`, `ade_node/tests/*`). The NEW `ade_runtime::network::serve_dispatch` is RED; the NEW `ade_node` serve wiring is RED — neither is canonical-counted. Every BLUE crate count was re-verified byte-stable at both `6bd60c80` and `f59b1eaa`. **N-F-G-J Δ: +1** — `git grep -hE "^(pub )?(struct|enum) "` over `crates/ade_types/src` goes `81 -> 82` (the NEW `PrevHash = Genesis | Block(Hash32)` enum, replacing the prior flat `prev_hash: Hash32` field); `ade_ledger` holds at **177** (the NEW `header_position` module adds the `check_header_position` *function* + the `BlockValidityError::HeaderPositionInvalid` *variant*, no struct/enum) and `ade_codec` at 11 (the `decode_prev_hash` fn + the null/hash32 `AdeEncode` match, no struct/enum); `ade_core` 49 / `ade_crypto` 21 / `ade_plutus` 8 / `ade_network` BLUE 109 all byte-stable `main`↔HEAD. Updated breakdown: `ade_codec` 11, `ade_types` **82**, `ade_crypto` 21, `ade_core` 49, `ade_ledger` 177, `ade_plutus` 8 (= 348) + `ade_network` BLUE 109 = **457**. |
| Tests | **2284** | `git grep -hE "#\[test\]\|#\[tokio::test\]" -- 'crates/**/*.rs' \| wc -l` at `550eec3a` (the prior CODEMAP's method). Approximate per the template fallback rule (count of attributes, not a runner enumeration). **Δ vs prior CODEMAP (2241): +3** by this method — `ade_network/src/handshake/version_table.rs` +1 (`n2n_supported_for_magic_produces_configured_magic`), `ade_node/tests/node_c1_serve_live.rs` +1 (`node_c1_serve_live`), `ade_node/tests/ba02_gate_archived_home.rs` +1 (`ba02_gate_*` — the `18085231` BA-02-gate archived-home fix that landed in the `6bd60c80..f59b1eaa` span). **Method caveat:** the two G-H serve-loopback integration tests in `ade_node/tests/node_spine_serve_loopback.rs` use `#[tokio::test(flavor = "multi_thread", …)]`, which the documented `#[test]\|#[tokio::test]` grep does NOT match — so the *true* new-test count for the span is 5; the header tracks the documented method for consistency with the carried 2241 baseline (see Generation notes). Verified via `git grep`: 2241 at `6bd60c80` → 2244 at `f59b1eaa`. **N-F-G-J Δ: +39** by this method (`git grep` 2245 at `main` → **2284** at HEAD `550eec3a`) — the bulk are the PrevHash codec/round-trip + position-rule + cold-start tests across `ade_codec/src/shelley/block.rs`, `ade_ledger/src/block_validity/{header_position,header_input,unsigned_header_pre_image}.rs`, `ade_ledger/src/producer/{forge,state}.rs`, `ade_runtime/src/producer/chain_evolution.rs`, the GREEN `ade_node/src/live_log/{sched_event,sched_writer}.rs` sched-vocabulary tests, and `ade_node/src/{node_sync,node_lifecycle}.rs` cold-start gate tests, plus the corpus + integration test files (`ade_codec/tests/{shelley,allegra_mary,full_corpus}_round_trip.rs`, `ade_node/tests/{forge_succeeds,forge_handler_variants,node_c1_genesis_rehearsal}.rs`). |
| CI checks | **126** | `ls ci/ci_check_*.sh \| wc -l` = 126 at `550eec3a` (committed; cross-checked against `git ls-tree -r HEAD ci/`). CI lives entirely under `ci/` (`ci_dirs = ["ci"]`); there is no `.github/workflows/` invariant gate at HEAD. **Δ vs prior CODEMAP (121): +2** — the two **G-H** gates `ci_check_single_serve_dispatch_authority.sh` (S1) + `ci_check_serve_listener_magic_aware.sh` (S2b), both `DC-NODE-07`. `ci_check_node_run_loop_containment.sh`, `ci_check_served_chain_handoff_fence.sh`, and `ci_check_live_feed_memory_bounds.sh` are all **byte-unchanged** by G-H. **N-F-G-J Δ: +3** (123 → 126) — the three G-J gates `ci_check_node_sched_events_emit_only.sh` (S1, `CN-NODE-04`) + `ci_check_prevhash_single_wire_authority.sh` (S2, `CN-WIRE-09`) + `ci_check_genesis_successor_reachability.sh` (S4, `DC-NODE-08`); `ci_check_rehearsal_manifest_schema.sh` was extended in place by S5 (no count change). |
| Registry rules | **319** | `grep -cE "^id = " docs/ade-invariant-registry.toml` = 319. Reference only — not a header count. **Δ vs prior CODEMAP (316): +3** — NEW `CN-WIRE-09` (`tier = derived`, `enforced`, `introduced_in = "PHASE4-N-F-G-J"`; `ci_script = ci/ci_check_prevhash_single_wire_authority.sh`; the PrevHash null/hash32 wire grammar + POSITION-BLIND codec), NEW `DC-NODE-08` (`tier = derived`, `enforced`; `ci_script = ci/ci_check_genesis_successor_reachability.sh`; the node-spine genesis-successor cold-start forge allowance; `cross_ref` = DC-NODE-05 / DC-NODE-06 / DC-NODE-07 / DC-EPOCH-03 / CN-CINPUT-03 / DC-CINPUT-02b / DC-FORGE-01 / CN-FORGE-01 / CN-NODE-04 / CN-WIRE-09 / RO-LIVE-01), NEW `CN-NODE-04` (the closed feed/forge sched-event vocabulary; `ci_script = ci/ci_check_node_sched_events_emit_only.sh`). `CN-REHEARSAL-FIDELITY-01` gains `strengthened_in += "PHASE4-N-F-G-J"` (S5 genesis rehearsal). `RO-LIVE-01` / `RO-LIVE-06` are **NOT** bumped (the live ACCEPT stays operator-gated). **No rule weakened.** |

---

## BLUE Modules — Pure Functional Core

> **Shared header (applies to every BLUE entry below).** Every `.rs` source file begins with the `// Core Contract:` banner and the following deny attributes are present in each crate's `lib.rs` (or, for `ade_network`, at the crate root — BLUE submodules inherit them):
> `#![deny(unsafe_code)]`, `#![deny(clippy::unwrap_used)]`, `#![deny(clippy::expect_used)]`, `#![deny(clippy::panic)]`, `#![deny(clippy::float_arithmetic)]`.
>
> Cross-cutting BLUE CI gates (all scope the full BLUE set — 6 BLUE crates + 9 BLUE `ade_network` submodule paths):
> - `ci_check_module_headers.sh` — banner first-line check.
> - `ci_check_forbidden_patterns.sh` — `HashMap`/`HashSet`/`IndexMap`/`indexmap::`, `SystemTime`, `Instant`, `std::fs`, `std::net`, `tokio`, `async fn`, `f32`/`f64`, `anyhow`, `rand::thread_rng`, `thread::spawn`, `unsafe` outside an allowlist.
> - `ci_check_dependency_boundary.sh` — no BLUE crate depends on a RED crate.
> - `ci_check_no_signing_in_blue.sh` — signing operations forbidden in BLUE.
> - `ci_check_no_semantic_cfg.sh` — `#[cfg(feature = …)]` / `cfg!(feature = …)` forbidden in BLUE.
> - `ci_check_hash_uses_wire_bytes.sh` — no hashing of re-encoded bytes in BLUE.
> - `ci_check_ingress_chokepoints.sh` — only named `decode_*` chokepoints construct `PreservedCbor`.
> - `ci_check_pallas_quarantine.sh` — `pallas-*` confined to `ade_plutus`.
> - `ci_check_no_async_in_blue.sh` — async constructs forbidden in BLUE (DC-CORE-01).
>
> Narrow BLUE gates: `ci_check_no_chaindb_in_consensus_blue.sh`, `ci_check_no_float_in_consensus.sh`, `ci_check_consensus_closed_enums.sh`, `ci_check_no_density_in_fork_choice.sh` (DC-CONS-03), `ci_check_deposit_param_authority.sh` (DC-TXV-07), `ci_check_gov_cert_accumulation_closed.sh` (DC-LEDGER-09), `ci_check_credential_discriminant_closed.sh` (DC-LEDGER-10), `ci_check_proposal_procedures_closed.sh` (DC-LEDGER-11), `ci_check_conway_cert_classification_closed.sh`, the N-C producer-authority gate set, the N-G server-role gate set, the N-H receive gate set, the N-I/N-J rollback+snapshot gate set, the mempool-ingress gate set, the N-P KES gate, the N-R/N-S/N-V BLUE gates, the N-W gate `ci_check_producer_praos_vrf.sh` (CN-FORGE-04), the N-X gate `ci_check_tag24_wire_authority.sh` (CN-WIRE-08), the N-Y gates `ci_check_forward_sync_chokepoint_only.sh` (DC-SYNC-01), `ci_check_mithril_uses_bootstrap_initial_state.sh` (CN-MITHRIL-01), `ci_check_no_haskell_fingerprint_equality.sh` (DC-COMPAT-01), the workspace-wide `ci_check_registry_code_locus_exists.sh`, the N-Z `ci_check_mithril_seed_point_independence.sh` (DC-MITHRIL-02), the N-F-A `ci_check_consensus_input_provenance.sh` (CN-CINPUT-02), the N-F-C node-lifecycle gates `ci_check_lifecycle_owner_uses_bootstrap_initial_state.sh` (CN-NODE-01), `ci_check_node_sync_via_pump.sh` (DC-SYNC-01), `ci_check_ba02_evidence_closed.sh` (RO-LIVE-06), the N-F-D run-loop gates `ci_check_loop_planner_closed.sh` + `ci_check_node_run_loop_containment.sh` (extended in place by N-F-E; DC-NODE-05 / CN-NODE-02 / CE-E-4), and the N-F-F operator-key-ingress gates `ci_check_forge_intent_closed.sh` + `ci_check_operator_forge_no_secret_leak.sh` (CN-NODE-03) with `ci_check_node_binary_uses_single_bootstrap.sh` (CN-NODE-01, `ReceiveState::new` owner allow-list). **N-F-G-A added** four forge-fidelity gates: `ci_check_genesis_consistency_fixture_present.sh` (S1, CE-G-A-1), `ci_check_recovered_ledger_pparams_sourced.sh` (S2a, CE-G-A-2a), `ci_check_node_forge_real_cli_ingress.sh` (S2, CE-G-A-2), `ci_check_node_forge_single_epoch_fail_closed.sh` (S4, CE-G-A-4 / DC-EPOCH-03). **N-F-G-B added** the served-chain handoff gate `ci_check_served_chain_handoff_fence.sh` (S3, DC-NODE-06 — **broadened in place by N-F-G-C S1**). **N-F-G-C added** the BA-02 evidence manifest gate `ci_check_ba02_evidence_manifest_schema.sh` (S2, RO-LIVE-06). **N-F-G-E added** the live-feed memory-bounds gate `ci_check_live_feed_memory_bounds.sh` (S1, DC-LIVEMEM-01). **N-F-G-D added** the path-fidelity gate `ci_check_node_path_fidelity.sh` (S1) + the rehearsal-manifest-schema gate `ci_check_rehearsal_manifest_schema.sh` (S2/S4) — both CN-REHEARSAL-FIDELITY-01; neither is a BLUE-set gate (the `--mode node` rehearsal harness is RED/GREEN in `ade_node`). **N-F-G-H added** the single-serve-dispatch-authority gate `ci_check_single_serve_dispatch_authority.sh` (S1) + the magic-aware-serve-listener gate `ci_check_serve_listener_magic_aware.sh` (S2b) — both DC-NODE-07; neither is a BLUE-set gate (the serve-dispatch core + listener are RED in `ade_runtime` / `ade_node`). The lone G-H BLUE touch is the additive **fn** `ade_network::handshake::version_table::n2n_supported_for_magic` (no struct/enum; the BLUE deny-attribute + closed-enum gates above apply to it unchanged).

---

### `ade_codec`

| Attribute | Value |
|-----------|-------|
| **Purpose** | Owns Cardano-canonical CBOR ingress — the only place in the workspace that turns raw bytes into typed semantic values, with wire-byte preservation for every hash-bearing structure. Owns the standalone opcert byte authority and the canonical Conway-tx preserved-byte splitter, the canonical block-envelope encoder `cbor::envelope::encode_block_envelope`, and (N-X) the single workspace **tag-24 CBOR-in-CBOR wire-envelope authority** `cbor::tag24::{wrap_tag24, unwrap_tag24}` + closed `TagEnvelopeError`. Also exposes the shared canonical CBOR read/write primitives (`cbor::{read_array_header, read_map_header, read_uint, read_bytes, write_*_canonical, canonical_width, ContainerEncoding, IntWidth}`) the BLUE codecs in `ade_ledger` (incl. the `SeedEpochConsensusInputs` + WAL codecs, and the `LiveConsensusInputsCanonical` 15-field fingerprint encoder in `ade_runtime`) build on. |
| **Creates** | `PreservedCbor<T>`, `RawCbor`, `BlockEnvelope`, `ByronDecodedBlock`, `CodecContext`, `CodecError` (incl. `UnknownCertTag`, `DuplicateMapKey`, `TrailingBytes`, `InvalidCborStructure`), `ContainerEncoding`, `IntWidth`, era-tagged block/tx wrappers, `OpCertCodecError`, `TxComponents<'a>`, `TagEnvelopeError` (closed: `NotTag24`, `NotByteString`, `Truncated`, `TrailingBytes`). Functions: `conway::cert::{decode_conway_certs, decode_drep}`, `conway::withdrawals::{decode_withdrawals, withdrawals_sum}`, `conway::governance::{decode,encode}_proposal_procedures`, `shelley::opcert::{encode_opcert, decode_opcert}`, `shelley::tx_components::split_conway_tx_components`, `cbor::envelope::{decode_block_envelope, encode_block_envelope}`, `cbor::tag24::{wrap_tag24, unwrap_tag24}`. |
| **Interprets** | All canonical Cardano CBOR — block envelopes (`[era, block]`), era-specific blocks, tx bodies/outs, certificates, addresses. `unwrap_tag24` is the single workspace authority that strips a tag-24 (`0xd8 0x18`) envelope, returning a zero-copy borrow of the verbatim inner bytes (no re-encode) and failing closed with a typed `TagEnvelopeError`. `cbor::mod::{read_bytes, read_text, skip_item}` fail-close (no panic) on an overflowing declared length via `checked_add`. **N-F-G-J** `shelley::block::decode_prev_hash` decodes the header `prev_hash` field POSITION-BLIND — `null (0xf6) -> PrevHash::Genesis` / `hash32 -> PrevHash::Block` as a pure function of the CBOR token, NEVER `block_number`; the matching `ShelleyHeaderBody` `AdeEncode` writes `Genesis -> write_null` / `Block(h) -> write_bytes_canonical` (CN-WIRE-09). |
| **MUST NOT** | (1) Construct `PreservedCbor` outside `ade_codec` (`pub(crate)` + `ci_check_ingress_chokepoints.sh`). (2) Re-encode wire bytes when computing hashes. (3) Use any forbidden BLUE pattern. (4) Depend on any workspace crate except `ade_types`. (5) `conway::cert` (DC-LEDGER-08) — no unknown-tag swallow. (6) `conway::withdrawals` — no last-wins on duplicate `RewardAccount`. (7) `decode_stake_credential` (DC-LEDGER-10) — must not erase the credential tag. (8) `conway::governance` (DC-LEDGER-11) — no silent skip on unknown `GovAction`. (9) `shelley::opcert` (DC-CONS-11/12). (10) `shelley::tx_components` (DC-CONS-13/16). (11) `encode_block_envelope` (CN-FORGE-03) — the workspace's single block-envelope encoder; no parallel serializer. (12) `cbor::tag24` (CN-WIRE-08) — `wrap_tag24`/`unwrap_tag24` defined exactly once; `unwrap_tag24` fails closed (typed `TagEnvelopeError`, never panic); inner bytes copied verbatim / zero-copy borrowed; `read_bytes`/`read_text`/`skip_item` reject an overflowing declared length without panic (`ci_check_tag24_wire_authority.sh`). (13) `shelley::block::decode_prev_hash` / the `ShelleyHeaderBody` `prev_hash` `AdeEncode` (CN-WIRE-09) — the codec is the byte representation only and MUST stay POSITION-BLIND: it decides `Genesis | Block` from the CBOR token (null vs hash32) and MUST NOT consult `block_number`; the position-aware coupling (`block_number 0 <=> Genesis`) lives in the validator (`ade_ledger::block_validity::header_position`), never here (`ci_check_prevhash_single_wire_authority.sh`). |
| **Inbound deps** | `ade_ledger` (heavy), `ade_plutus`, `ade_testkit`, `ade_network`, `ade_runtime`, `ade_core_interop`, `ade_node`. |
| **Outbound deps** | `ade_types`. std-only; dev-deps `serde_json`, `toml`. |
| **Entry points** | `ade_codec::cbor::envelope::{decode_block_envelope, encode_block_envelope}`, `ade_codec::{wrap_tag24, unwrap_tag24, TagEnvelopeError}` (crate-root re-exports), `ade_codec::cbor::{read_array_header, read_map_header, read_uint, read_bytes, write_uint_canonical, write_bytes_canonical, write_array_header, write_map_header, canonical_width, ContainerEncoding, IntWidth}`, `ade_codec::traits::AdeEncode`, `ade_codec::CodecContext`, per-era `decode_*_block`, `ade_codec::address::decode_address`, `ade_codec::conway::tx::decode_conway_tx_body`, `ade_codec::conway::cert::decode_conway_certs`, `ade_codec::conway::governance::{decode,encode}_proposal_procedures`, `ade_codec::shelley::opcert::{encode_opcert, decode_opcert}`, `ade_codec::shelley::tx_components::split_conway_tx_components`. |
| **Key modules** | `cbor/` (incl. `envelope.rs`, `tag24.rs`, `mod.rs`), `byron/`, `shelley/` (incl. `cert.rs`, `opcert.rs`, `tx_components.rs`), `allegra/`, `mary/`, `alonzo/`, `babbage/`, `conway/`, `address/`, `preserved.rs`, `traits.rs`, `primitives.rs`, `error.rs`. |

---

### `ade_core`

| Attribute | Value |
|-----------|-------|
| **Purpose** | BLUE authoritative Praos consensus core. Owns canonical types and pure state-transitions deciding which header/chain Ade accepts: HFC era schedule + slot↔era↔time (incl. `EraSchedule::locate` — the BLUE epoch locator the G-A `forge_epoch_admission` guard derives the candidate epoch from), Praos chain-dep state, VRF cert verification + leader-eligibility, KES + op-cert period verification, header validation, fork-choice, header-level rollback, leader-schedule query, canonical encodings, producer-side opcert acceptance, the closed BLUE leader-check evaluator (`consensus::leader_check`, N-R-A), and the single era→leader-VRF-input authority `consensus::vrf_cert::{ExpectedVrfInput, leader_vrf_input, leader_value_for}` (N-W). The `consensus::nonce` epoch-boundary transitions (`CandidateFreeze` / `EpochBoundary`) exist but are **undriven on the `--mode node` forge path** (DC-EPOCH-03). |
| **Creates** | **Schedule:** `BootstrapAnchorHash`, `EraSchedule`, `EraSummary`, `EraLocation`. **State:** `PraosChainDepState`, `OpCertCounterMap`, `Nonce`. **Events/points:** `Point`, `ChainHash`, `BlockDistance`, `SecurityParam`, `ChainEvent`, `ChainSelectionReject`. **Errors:** `HFCError`, `SlotTimeError`, `HeaderValidationError`, `VrfCertError`, `OpCertCounterError`, `NonceEvolutionError`, `LeaderScheduleError`, `OpCertError`. **Header surface:** `HeaderInput`, `HeaderVrf`, `HeaderKes`, `ValidatedHeaderSummary`, `HeaderApplied`. **Fork-choice:** `TiebreakerView`, `CandidateFragment`, `ChainSelectorState`, `ForkChoiceError`. **VRF/leader:** `VrfRole`, `VerifiedVrf`, `StakeFraction`, `ActiveSlotsCoeff`, `LeaderScheduleQuery`, `LeaderScheduleAnswer`, `ExpectedVrfInput` (closed 2-variant era-discriminated leader-VRF-input enum). **Boundary:** `LedgerView` trait. **Rollback:** `RollBackRequest`, `RollBackApplied`. **`leader_check`:** `LeaderCheckVerdict`, `LeaderCheckError`, `VrfOutputFingerprint`, `LeaderProofFingerprint`. 49 public types. |
| **Interprets** | Canonical inputs from the `ade_runtime` shell. KES check verifies the hot-KES signature via `ade_crypto::kes::verify_kes`. `leader_vrf_input(era, slot, eta0)` constructs the era-correct alpha; `leader_value_for` applies the era-correct range-extension. `leader_check::verify_and_evaluate_leader` consumes only public-key material + canonical inputs and fail-closes on full-enum `ExpectedVrfInput` mismatch. `EraSchedule::locate(slot)` resolves the era/epoch for a slot (consumed by the G-A epoch guard). `opcert_validate` consumes an `OperationalCert`, cold key, expected period, prev counter. |
| **MUST NOT** | (1)–(14) consensus carry-forward (no I/O, no clock, no `ChainDb` in BLUE consensus, no density in fork-choice, closed consensus enums, etc.). (15) `consensus::leader_check` (CN-FORGE-02) — MUST NOT depend on `LedgerView`/`EraSchedule`/`ChainDepState`/wall-clock/storage/any RED crate; MUST NOT observe `KesSecret`/`VrfSigningKey`/`ColdSigningKey`; `NotEligible` carries only a bounded fingerprint. (16) `consensus::vrf_cert` leader-VRF authority (CN-FORGE-04) — `leader_vrf_input` is the single era→construction authority; no path accepts both TPraos and Praos inputs for one era; Praos producer alpha = validator alpha (`ci_check_producer_praos_vrf.sh`). |
| **Inbound deps** | `ade_ledger`, `ade_runtime` (heavy), `ade_testkit`, `ade_core_interop`, `ade_node`. |
| **Outbound deps** | `ade_types`, `ade_crypto`, `minicbor`. Dev-deps: `ade_testkit`, `serde_json`, `cardano-crypto`. |
| **Entry points** | `use ade_core::consensus::{…}` aggregator; `validate_and_apply_header`, `select_best_chain`, `apply_rollback`, `apply_nonce_input`, `apply_op_cert`, `query_leader_schedule`, `verify_vrf_cert`, `tiebreaker_prefer`; `ade_core::consensus::era_schedule::EraSchedule::locate`; `ade_core::consensus::opcert_validate::opcert_validate`; `ade_core::consensus::leader_check::{verify_and_evaluate_leader, LeaderCheckVerdict, is_leader_for_vrf_output}`; `ade_core::consensus::vrf_cert::{ExpectedVrfInput, ActiveSlotsCoeff, leader_vrf_input, leader_value_for, praos_vrf_input, praos_leader_value}`. |
| **Key modules** | `consensus/{era_schedule, praos_state, events, errors, vrf_cert, kes_check, nonce, op_cert, leader_schedule, header_summary, header_validate, candidate, fork_choice, rollback, ledger_view, encoding, opcert_validate, leader_check}.rs`. |

---

### `ade_crypto`

| Attribute | Value |
|-----------|-------|
| **Purpose** | Pure cryptographic verification primitives — Blake2b hashing, Ed25519 / Byron-bootstrap signature verification, KES verification with operational certificates, VRF verification, plus closed signature-artifact types. Verification only — signing lives in RED `ade_runtime::producer::signing`. Also owns the Ade-native `Sum6KES Ed25519DSIGN` algorithm (`kes_sum/`), byte-identical to Haskell `cardano-base`. `blake2b_256` is the function the sidecar-provenance helper hashes the canonical sidecar bytes with (and the G-A `require_forge_current_pparams` hash-binds the `protocol_params_json` preimage with); `blake2b_224` derives the operator `pool_id` from the cold verification key in the N-F-F ingress. |
| **Creates** | `Blake2b224`, `Blake2b256`, `HashAlgorithm`, `Ed25519VerificationKey`, `Ed25519Signature`, `ByronExtendedVerificationKey`, `KesVerificationKey`, `KesPeriod`, `OperationalCertData`, `VrfVerificationKey`, `VrfProof`, `VrfOutput`, `CryptoError`, `KesSignature`, `SUM6_KES_SIG_LEN = 448`. **`kes_sum::`** (BLUE): `KesAlgorithm` trait, `Sum0Kes`, `Sum0SigningKey`, `Sum0Signature`, `SumKes<D>`, `SumSigningKey<D>`, `SumSignature<D>`, aliases `Sum1Kes..Sum6Kes`, closed `KesError`, closed `KesParseError`. |
| **Interprets** | Already-decoded byte slices. `Sum6Kes::raw_deserialize_signing_key_kes` interprets the canonical 608-byte expanded cardano-cli skey; `raw_deserialize_signature_kes` the 448-byte signature; `period_from_zeroed_sum6_tree_shape` infers the current period from tree shape. |
| **MUST NOT** | (1) Implement signing as a top-level `pub fn sign_*` (`ci_check_no_signing_in_blue.sh`). (2) Allocate global state. (3) Use any BLUE-forbidden pattern. (4) `unsafe` outside the allowlisted FFI in `vrf.rs`. (5) `build_opcert_signable` must produce the spec-correct concat. (6) `KesSignature` (DC-CRYPTO-04) — closed length-pinned wrapper, `from_bytes` only, redacting `Debug`. (7) `kes_sum` (DC-CRYPTO-08) — MUST NOT import `cardano_crypto::kes::*` in production paths (`#[cfg(test)]` oracle only; `ci_check_kes_sum_compatibility.sh` Guard 3). (8) `kes_sum::hash::expand_seed` — Haskell prefix bytes `0x01`/`0x02`. (9) every `*SigningKey` hand-rolls zeroizing `Drop` + redacting `Debug` + no public byte accessors. (10) `kes_sum` corpus — throwaway-fixture comment; no `.skey` files (Guards 1, 2). (11) error variants carry only non-secret primitives. |
| **Inbound deps** | `ade_core`, `ade_ledger`, `ade_plutus`, `ade_testkit`, `ade_core_interop`, `ade_runtime`, `ade_node`. |
| **Outbound deps** | `ade_types`, `blake2`, `ed25519-dalek`, `cardano-crypto` (`["vrf-draft03", "kes-sum", "dsign"]`, `default-features = false`; `kes-sum` retained for `#[cfg(test)]` oracle only). |
| **Entry points** | `ade_crypto::blake2b::*`, `ade_crypto::{blake2b_256, blake2b_224}`, `verify_ed25519`, `verify_byron_bootstrap`, `verify_kes`, `verify_opcert`, `verify_vrf`, `ade_crypto::kes::{KesSignature, SUM6_KES_SIG_LEN, KesVerificationKey}`, `ade_crypto::kes_sum::{KesAlgorithm, Sum6Kes, KesError, KesParseError, period_from_zeroed_sum6_tree_shape}`. |
| **Key modules** | `blake2b.rs`, `ed25519.rs`, `error.rs`, `kes.rs`, `kes_sum/` (`mod`, `single`, `sum`, `hash`, `errors`, `period`, `cardano_cli_corpus` `#[cfg(test)]`, `tests` `#[cfg(test)]`), `traits.rs`, `vrf.rs`. |

---
### `ade_ledger`

| Attribute | Value |
|-----------|-------|
| **Purpose** | The functional core (ledger half): stateless ledger rules for every era; B1 block-validity verdict; B2 transaction-validity verdict + mempool admission; B3 Conway value-conservation; B4/B5 closed Conway cert-state + governance-cert accumulation; live committee enactment; the single BLUE wire-ingress chokepoint `mempool::ingress::mempool_ingress`; the BLUE producer authority (`producer::{forge, self_accept, served_chain, state}`); the single canonical body-hash authority (`block_body_hash`); the BLUE receive-side bridge (`receive::*`); the BLUE rollback authority (`rollback::*`); the BLUE canonical snapshot encoder/decoder (`snapshot::*`); the BLUE Ade-native WAL replay+event authority (`wal::*`); the BLUE bootstrap-anchor + Mithril-binding types (`bootstrap_anchor::*`); the BLUE Conway-genesis→initial-state transform (`genesis_source`, N-Y); the canonical KES-signing pre-image recipe `block_validity::unsigned_header_pre_image`; the BLUE exact-`Rational` arithmetic (`rational`, consumed by the G-A `protocol_params` parser); the BLUE `ProtocolParameters` model (`pparams`, installed current at seed/import by G-A S2a); and (N-F-A) the BLUE closed `SeedEpochConsensusInputs` recovered-state record + its SOLE codec (`seed_consensus_inputs`), the recovered-surface `PoolDistrView::from_seed_epoch_consensus_inputs` projection (`consensus_view`), and the additive WAL provenance variant + `ReplayOutcome`/`RecoveredBootstrapProvenance` (`wal`). The `producer::AcceptedBlock` (returned only by `self_accept` on `Ok`) is the BLUE token the N-F-G-B GREEN `SelfAcceptedHandoff` carrier wraps. **N-F-G-J** adds the single POSITION-AWARE header authority `block_validity::header_position::check_header_position` (the `block_number 0 <=> PrevHash::Genesis` coupling, called by `decode_block` before the header authority; CN-WIRE-09) + the additive `BlockValidityError::HeaderPositionInvalid` variant (coarse class `HeaderInvalid`); the producer (`producer::{forge, state}`) + the KES pre-image (`unsigned_header_pre_image`) carry `prev_hash: PrevHash` directly (Genesis for the genesis-successor block 0, byte-identical Block path otherwise). |
| **Creates** | ~177 public types. Load-bearing: `BlockValidityError`, `BlockVerdict`, `TxValidity*`, `MempoolState`, `ProducerTick`, `ForgedBlock`, `ForgeError`, `ForgeEffects`, `AcceptedBlock` (the BLUE self-accepted token; field private to `producer::self_accept`), `ServedChainSnapshot`, `ServedChainAdmitError`, `PoolDistrView`, `PoolEntry`, `LedgerState`, `ProtocolParameters` (the `pparams` model), `Rational` (the exact-rational arithmetic), `WalEntry` (closed sum: `AdmitBlock` + `SeedEpochConsensusInputsImported` at tag 3), `BlockVerdictTag`, `RecoveredBootstrapProvenance`, `ReplayOutcome`, `WalError` (incl. `DuplicateProvenance`, `ProvenanceAnchorMismatch`), `SeedEpochConsensusInputs` (closed; `anchor_fp`/`epoch_no`/`active_slots_coeff`/`total_active_stake`/`pool_distribution: BTreeMap<Hash28, PoolEntry>`; `SEED_CINPUT_SCHEMA_VERSION = 1`), `SeedConsensusInputsError` (closed: `MalformedCbor`/`UnknownVersion`/`Structural`/`NonCanonicalMapOrder`/`DuplicatePoolKey`/`TrailingBytes`), `BootstrapAnchor`, `SeedProvenance` (closed: `CardanoCliJson` / `Mithril { … }`), `SeedPoint`, `ANCHOR_SCHEMA_VERSION = 2`, `MithrilManifestReport`, `MithrilImportError`, `BootstrapAnchorError`, `ConwayGenesisConfig`, `GenesisSourceError` (closed: `NonConwayEra`), `UnsignedHeaderPreImage`, `UnsignedHeaderPreImageError`. |
| **Interprets** | Canonical decoded blocks/txs/certs/snapshots; `mempool_ingress` is the sole BLUE chokepoint from wire ingress to `admit`; `self_accept` re-validates a forged artifact (returning `AcceptedBlock` only on `Ok`); `served_chain_admit` derives the served index; `decode_bootstrap_anchor` reads the version-gated `ANCHOR_SCHEMA_VERSION = 2` CBOR framing; `verify_mithril_binding(report, anchor)` cross-checks the Mithril manifest against the anchor; `genesis_initial_state(&ConwayGenesisConfig)` produces the cold-start pair; `unsigned_header_pre_image` produces the canonical CBOR of `ShelleyHeaderBody` (now carrying `prev_hash: PrevHash` — `Genesis` as CBOR null for block 0). `decode_block` calls `check_header_position(block_number, prev_hash)` before the header authority, rejecting a position-illegal pair (block 0 without `Genesis`, or `> 0` with `Genesis`) as `HeaderPositionInvalid`. `decode_seed_epoch_consensus_inputs` is the SOLE decoder of the closed sidecar record; `replay_from_anchor` folds the WAL into a `ReplayOutcome`. |
| **MUST NOT** | All carry-forward ledger prohibitions (no I/O, no clock, closed cert/governance enums, single body-hash recipe, etc.). **`producer::forge` (CN-FORGE-01/03):** emit exactly one of `ForgeSucceeded`/`ForgeNotLeader`/`ForgeFailed`; no `ForgeSucceeded` unless `self_accept` accepts; wrap output via `ade_codec::encode_block_envelope`; no parallel forge codepath. **`producer::self_accept` (CN-FORGE-01):** `AcceptedBlock` is minted ONLY here on a successful re-validation; its field is private to this module (so the N-F-G-B `SelfAcceptedHandoff` can only wrap a genuine BLUE-accepted token). **`producer::served_chain` (CN-PROD-04):** only self-accepted blocks admitted; sole entry `served_chain_admit`. **`block_validity::unsigned_header_pre_image` (CN-KES-HEADER-01 / DC-KES-HEADER-01):** branded `UnsignedHeaderPreImage`'s only constructor is the canonical recipe; byte-identical to the validator-side extractor; pure (its `prev_hash: PrevHash` emits `Genesis` as CBOR null for block 0). **`block_validity::header_position` (CN-WIRE-09 position clause):** `check_header_position` is the SINGLE position-aware authority for the `block_number`/`prev_hash` coupling, called from `decode_block`; it MUST NOT live in `ade_codec` or be duplicated; it folds its reject into the existing `HeaderInvalid` class (no new `BlockRejectClass`); pure, total, no `String` payload (`ci_check_prevhash_single_wire_authority.sh`). **`bootstrap_anchor` (CN-ANCHOR-01 / DC-ANCHOR-01):** `ANCHOR_SCHEMA_VERSION` version-gated; the `SeedProvenance` enum is closed. **`bootstrap_anchor::binding` (CN-MITHRIL-01 / DC-MITHRIL-01):** `verify_mithril_binding` is a pure fail-closed predicate, never re-verifies the STM multisig. **`genesis_source` (DC-GENESIS-SRC-01):** `genesis_initial_state` is Conway-only (fail-closed `NonConwayEra`). **`seed_consensus_inputs` (CN-CINPUT-01):** `SeedEpochConsensusInputs` is a single closed canonical type with a SOLE encoder/decoder pair; deterministic CBOR, `BTreeMap`-ordered, version-gated, byte-canonical; no `Default`, no `#[non_exhaustive]`; no I/O / clock / float. **`wal` provenance (DC-CINPUT-01):** the `SeedEpochConsensusInputsImported` variant stays distinct from the `AdmitBlock` chain; `replay_from_anchor` allows at most one provenance entry and fails closed on duplicate / anchor mismatch; remains pure. **`consensus_view::from_seed_epoch_consensus_inputs` (DC-CINPUT-02a):** a pure BLUE field-map preserving single-epoch semantics (off-epoch queries return `None`); no I/O / clock / `HashMap` — it is the *projection* the forge handoff (`node_sync::forge_one_from_recovered`) consumes. **`rational` / `pparams`:** stay pure BLUE types (no I/O, no float arithmetic in `rational` — exact integer arithmetic only). |
| **Inbound deps** | `ade_testkit`, `ade_core_interop`, `ade_runtime`, `ade_node`. |
| **Outbound deps** | `ade_types`, `ade_crypto`, `ade_codec`, `ade_plutus`, `ade_core`, `minicbor`, `num-bigint`, `num-integer`, `num-traits`. Dev-dep: `ade_testkit`. |
| **Entry points** | `ade_ledger::block_validity::{decode_block, validate_block, check_header_position, unsigned_header_pre_image::unsigned_header_pre_image}`, `ade_ledger::tx_validity::*`, `ade_ledger::mempool::{ingress::mempool_ingress, admit::*}`, `ade_ledger::producer::{forge::forge_block, self_accept::{self_accept, AcceptedBlock}, served_chain::{ServedChainSnapshot, served_chain_admit}}`, `ade_ledger::block_body_hash::*`, `ade_ledger::receive::reducer::*`, `ade_ledger::rollback::*`, `ade_ledger::snapshot::*`, `ade_ledger::pparams::ProtocolParameters`, `ade_ledger::rational::Rational`, `ade_ledger::wal::{replay_from_anchor, ReplayOutcome, RecoveredBootstrapProvenance, WalEntry, WalError, encode_wal_entry, decode_wal_entry, WalStore}`, `ade_ledger::seed_consensus_inputs::{SeedEpochConsensusInputs, encode_seed_epoch_consensus_inputs, decode_seed_epoch_consensus_inputs, SeedConsensusInputsError, SEED_CINPUT_SCHEMA_VERSION}`, `ade_ledger::consensus_view::{PoolDistrView, PoolEntry}` (incl. `PoolDistrView::{new, from_seed_epoch_consensus_inputs}`), `ade_ledger::bootstrap_anchor::{encode_bootstrap_anchor, decode_bootstrap_anchor, BootstrapAnchor, SeedProvenance, verify_mithril_binding, MithrilManifestReport, MithrilImportError}`, `ade_ledger::genesis_source::{genesis_initial_state, ConwayGenesisConfig, GenesisSourceError}`. |
| **Key modules** | `block_validity/` (incl. `unsigned_header_pre_image.rs`, `header_input.rs`, `header_position.rs` — the N-F-G-J single position-aware authority, `transition.rs`, `verdict.rs`), `tx_validity/`, `mempool/`, `cert_classify/`, `consensus_view.rs` (incl. `PoolDistrView::from_seed_epoch_consensus_inputs` — consumed by the N-F-C/N-F-E/N-F-F/N-F-G-A lifecycle forge handoff `node_sync::forge_one_from_recovered`), `producer/` (`forge`, `self_accept` — mints `AcceptedBlock`, `served_chain`, `state`), `block_body_hash.rs`, `receive/`, `rollback/`, `snapshot/`, `pparams.rs` (the `ProtocolParameters` model), `rational.rs` (exact-rational arithmetic, consumed by `ade_runtime::consensus_inputs::protocol_params`), `wal/` (`mod`, `error`, `event`, `replay`, `store_trait`), `seed_consensus_inputs.rs`, `bootstrap_anchor/` (`mod`, `anchor`, `binding`, `error`), `genesis_source.rs`. |

> **GREEN sub-classification:** `ade_ledger::mempool::{policy, canonicalize}` are GREEN-by-content. **RED sub-classification:** `ade_ledger::consensus_input_extract` is RED-by-content. Both carry their own module banner.

---

### `ade_network` *(BLUE submodules)*

| Attribute | Value |
|-----------|-------|
| **Purpose** | Owns the Cardano Ouroboros mini-protocol authority — the closed wire grammar (CBOR codecs) and pure state machines for all N2N + N2C mini-protocols, plus the BLUE mux frame primitive and the producer-side server-role reducers. Owns the per-protocol tag-24 **composition** authorities layered over `ade_codec::{wrap_tag24, unwrap_tag24}` — `codec::block_fetch::{compose,decompose}_blockfetch_block` (era **inside** the wrap; Conway = storage index 7) and `codec::chain_sync::{compose,decompose}_rollforward_header` + `chain_sync_wire_era_index` (era_tag **outside** the wrap; CONSENSUS era index Conway = 6). |
| **Creates** | 109 BLUE canonical types: `mux/frame.rs` 5, `codec/` 38, `handshake/` 9, `chain_sync/` 11, `block_fetch/` 10, `tx_submission/` 5, `keep_alive/` 5, `peer_sharing/` 5, `n2c/` 21. `chain_sync::server::HeaderProjection` carries an `era: CardanoEra` field. **N-F-G-H S2b** added the additive BLUE **function** `handshake::version_table::n2n_supported_for_magic(network_magic: u32) -> Vec<(u16, VersionData)>` — **no new canonical type** (the count holds at 9 for `handshake/`, 109 total): it specializes the existing closed `N2N_SUPPORTED` version SET (V11..=V16) for a configured magic; `N2N_SUPPORTED` + the `n2n()`/`n2c()` builders are unchanged. |
| **Interprets** | Mini-protocol wire frames; `mux::frame::decode_frame` is the single frame decode authority; per-protocol `*_transition` reducers consume decoded messages. `decompose_blockfetch_block` / `decompose_rollforward_header` strip the per-protocol tag-24 composition (delegating the inner unwrap to `ade_codec::unwrap_tag24`). |
| **MUST NOT** | (1) `mux::frame` — single `encode_frame`/`decode_frame` pair. (2) handshake — single `n2n_transition` + single `n2c_transition`. (3) closed `AcceptedMiniProtocol` registry. (4) no socket I/O in BLUE submodules. (5) no async/tokio in BLUE submodules. (6) depend on no workspace crate beyond `ade_codec` + `ade_types`. (7) server reducers MUST NOT split headers in parallel or depend on signing. (8b) `n2n_supported_for_magic` (DC-NODE-07, N-F-G-H) — MUST NOT widen the version SET or change the `VersionData` shape; it parameterizes ONLY `network_magic` off the configured identity (the version set stays exactly `N2N_SUPPORTED`'s); additive + pure + deterministic; adds no canonical type (`ci_check_serve_listener_magic_aware.sh` pins that live serve sites build their table via this fn, never the static `N2N_SUPPORTED`). (8) tag-24 composition (CN-WIRE-08) — composers delegate byte-level wrap/strip to the single `ade_codec` authority; a served BlockFetch `MsgBlock` payload is `tag24(bytes([era, block]))` and a served ChainSync `RollForward` header is `[era_tag, tag24(bytes(header_cbor))]`; both pinned byte-identically against captured cardano-node 11.0.1 fixtures (`ci_check_tag24_wire_authority.sh`). |
| **Inbound deps** | `ade_runtime`, `ade_node`, `ade_core_interop`, `ade_testkit` (and the `ade_network` GREEN/RED submodules). |
| **Outbound deps** | `ade_types`, `ade_codec`. No external deps in BLUE submodules. |
| **Entry points** | `ade_network::mux::frame::{encode_frame, decode_frame}`, `ade_network::handshake::{n2n_transition, n2c_transition}`, `ade_network::handshake::version_table::{N2N_SUPPORTED, n2n_supported_for_magic}` (the latter NEW, N-F-G-H S2b), `ade_network::chain_sync::{server::*, client::*}`, `ade_network::block_fetch::{server::*, client::*}`, `ade_network::codec::block_fetch::{compose,decompose}_blockfetch_block`, `ade_network::codec::chain_sync::{compose,decompose}_rollforward_header, chain_sync_wire_era_index}`, `ade_network::{tx_submission, keep_alive, peer_sharing, n2c}::*`. |
| **Key modules** | `mux/frame.rs`, `codec/` (incl. `block_fetch.rs`, `chain_sync.rs`), `handshake/`, `chain_sync/` (incl. `server.rs`), `block_fetch/` (incl. `server.rs`), `tx_submission/`, `keep_alive/`, `peer_sharing/`, `n2c/`. |

---

### `ade_plutus`

| Attribute | Value |
|-----------|-------|
| **Purpose** | Quarantine boundary between the Ade-canonical ledger and the ported UPLC evaluator from `aiken-lang/aiken` (pinned tag `v1.1.21`). |
| **Creates** | `PlutusScript`, `PlutusLanguage`, `EvalOutput`, `PlutusError`, `CostModels`, `DecoderMode`, `PerScriptResult`, `TxEvalResult`. |
| **Interprets** | UPLC scripts (Plutus V1/V2/V3) and `CostModels` CBOR; phase-two tx evaluation. `PlutusScript::from_cbor` is a named ingress chokepoint. |
| **MUST NOT** | (1) Re-export any `pallas_*` or `aiken_uplc::` type. (2) Let another BLUE crate bypass the canonical entry. (3) Activate PV11 builtins. (4) Use any BLUE-forbidden pattern. (5) Construct `PreservedCbor` outside `ade_codec`. |
| **Inbound deps** | `ade_ledger`, `ade_testkit`. |
| **Outbound deps** | `ade_types`, `ade_crypto`, `ade_codec`, `aiken_uplc` (git, tag `v1.1.21`), `pallas-primitives` (internal-only). |
| **Entry points** | `ade_plutus::eval_tx_phase_two`, `ade_plutus::tx_eval::*`, `ade_plutus::evaluator::*`, `ade_plutus::cost_model::*`. |
| **Key modules** | `evaluator.rs`, `cost_model.rs`, `script_context.rs`, `script_verdict.rs`, `tx_eval.rs`. |

---

### `ade_types`

| Attribute | Value |
|-----------|-------|
| **Purpose** | Owns the canonical Cardano domain vocabulary — primitives, era enum, address forms, era-tagged tx bodies/outputs/certificates, governance types — the lingua franca for every other crate. `CardanoEra` carries the `is_praos()` classifier (Babbage + Conway). `Hash28`/`Hash32`/`EpochNo`/`SlotNo` are the primitives the recovered-state record, the forge-slot guard, the G-A epoch guard, and the N-F-F operator `pool_id` are keyed/typed on. `ProtocolVersion` is the value the G-A `ForgeActivation` carries from the recovered current ledger view. **N-F-G-J** added the closed sum `PrevHash = Genesis | Block(Hash32)` (with `block_hash() -> Option<&Hash32>`) — the header `prev_hash` field's closed wire grammar `$hash32 / null`, replacing the prior flat `prev_hash: Hash32` (the lone new canonical type this cluster). |
| **Creates** | `CardanoEra` (with `is_praos()`), `SlotNo`, `BlockNo`, `EpochNo`, `Hash28`, `Hash32`, `Coin`, `Lovelace`, `NetworkId`, `Nonce`, `TxIn`, `RewardAccount`, `PoolId`, `Address`, `ByronAddress`, `Credential`, `StakeCredential`, `Certificate`, `PoolRegistrationCert`, `ConwayCert`, `MIRCert`, `DRep`, `GovAction`, `GovActionState`, `GovActionId`, `Anchor`, `ProposalProcedure`, `OperationalCert`, `NativeScript`, `Datum`, `MultiAsset`, `AssetName`, `CostModel`, `ExUnits`, plus per-era tx-body/tx-out/witness wrappers and `ShelleyBlock`/`ShelleyHeader`/`VrfData`/`ProtocolVersion`/`PrevHash` (the N-F-G-J closed `Genesis | Block(Hash32)` sum). |
| **Interprets** | None — produce-only. |
| **MUST NOT** | (1) Construct or decode `PreservedCbor`. (2) Use any BLUE-forbidden pattern. (3) Depend on any workspace crate. (4) Add open/extensible variants to closed enums without a versioned gate (`CardanoEra` discriminants are closed: Byron=0 … Conway=7; `is_praos()` classifies exactly {Babbage, Conway}; `PrevHash` is a closed 2-variant `Genesis | Block(Hash32)` sum — the header predecessor is `Genesis`/null XOR a `Block` hash, never a flat `Hash32`, N-F-G-J/CN-WIRE-09). |
| **Inbound deps** | Every other workspace crate. |
| **Outbound deps** | None. |
| **Entry points** | `ade_types::CardanoEra` (incl. `is_praos`, `is_byron`, `as_u8`), `ade_types::tx::{Coin, TxIn, RewardAccount}`, `ade_types::{Hash32, SlotNo, Hash28, BlockNo, EpochNo}`, `ade_types::conway::{tx::ConwayTxBody, cert::*, governance::{ProposalProcedure, GovAction, GovActionId}}`, `ade_types::shelley::block::{OperationalCert, ProtocolVersion, ShelleyHeader, ShelleyBlock, VrfData, PrevHash}`. |
| **Key modules** | `primitives.rs`, `era.rs`, `tx.rs`, `address/`, `byron/`, `shelley/`, `allegra/`, `mary/`, `alonzo/`, `babbage/`, `conway/`. |

---
## GREEN Modules — Deterministic Glue

> Deterministic, non-authoritative. May depend on BLUE; must not affect authoritative outputs. GREEN sub-trees inside the RED `ade_runtime` and `ade_node` crates carry a `//! GREEN …` banner + the same deny attributes and are CI-gated for purity.

### `ade_runtime::producer::self_accepted_handoff` *(GREEN by content — PHASE4-N-F-G-B S1)*

| Attribute | Value |
|-----------|-------|
| **Purpose** | The typed self-accepted-artifact handoff fence (`//! GREEN`). `SelfAcceptedHandoff` is the constructor-fenced carrier that moves a BLUE self-accepted forged block from the `--mode node` forge path toward the (S2) sibling served-chain admit task. Its SOLE constructor takes the BLUE `ade_ledger::producer::AcceptedBlock` — itself producible only by BLUE `self_accept` returning `Ok` (CN-FORGE-01; `AcceptedBlock`'s field is private to `ade_ledger::producer::self_accept`). There is **no** constructor from raw `Vec<u8>`, a `ForgedBlockArtifact`, a `CoordinatorEvent`, a self-declared acceptance flag, or a peer verdict — so "hand an artifact that was not BLUE self-accepted to the serve task" is type-unrepresentable. It is the GREEN fence backing `DC-NODE-06`. Pure / deterministic: no I/O, clock, rand, or float; the carrier wraps the original token verbatim and never re-validates or re-derives it (same `AcceptedBlock` ⇒ same carrier). Registered in `producer/mod.rs`. |
| **Creates** | `SelfAcceptedHandoff` (`#[derive(Debug, Clone, PartialEq)]`; private field `accepted: AcceptedBlock`). Sole constructor `from_self_accepted(accepted: AcceptedBlock) -> Self`; accessors `accepted(&self) -> &AcceptedBlock` and `into_accepted(self) -> AcceptedBlock` (the `push_atomic` input for S2). Not canonical-counted (GREEN; RED `ade_runtime`). |
| **Interprets** | None — it is a verbatim carrier. It never decodes, re-validates, or re-derives the carried block; `accepted()` / `into_accepted()` return the original BLUE token. |
| **MUST NOT** | (1) Carry an artifact that was not BLUE self-accepted — there is NO constructor from raw `Vec<u8>` / `ForgedBlockArtifact` / `CoordinatorEvent` / a self-declared flag / a peer verdict; the private field + the sole `from_self_accepted` constructor make "serve an unaccepted artifact" unrepresentable (CN-FORGE-01 / DC-NODE-06; `ci_check_served_chain_handoff_fence.sh` guard (3) pins the channel type). (2) Re-derive the token from `artifact.bytes` — the carrier holds the ORIGINAL `AcceptedBlock` from `self_accept`. (3) Re-validate / decode / mutate the carried block. (4) Perform I/O / hold a clock / allocate global state / `await` — pure. |
| **Inbound deps** | `ade_node::node_sync` (`forge_one_from_recovered` wraps the surfaced token via `from_self_accepted`); `ade_node::node_lifecycle` (the relay loop sends it over the typed channel; the sibling admit task drains it and calls `into_accepted()`). |
| **Outbound deps** | `ade_ledger::producer::AcceptedBlock` (the carried BLUE token) only. |
| **Entry points** | `ade_runtime::producer::self_accepted_handoff::SelfAcceptedHandoff::{from_self_accepted, accepted, into_accepted}`. |

> **Promotable / demotable:** firmly **promotable to BLUE on demand** (a pure constructor-fenced newtype over a BLUE type, no float / clock / I/O); it sits in RED `ade_runtime` only to live beside the producer surface. Never demotable to RED.

---

### `ade_runtime::consensus_inputs::protocol_params` *(GREEN by content — PHASE4-N-F-G-A S2a)*

| Attribute | Value |
|-----------|-------|
| **Purpose** | The cardano-cli `query protocol-parameters` JSON parser (`//! GREEN`). It converts the oracle's protocol-parameters JSON (the `protocol_params_json` preimage carried in the operator consensus-inputs bundle) into a canonical BLUE `ade_ledger::pparams::ProtocolParameters`, so the recovered ledger carries the **current** protocol version + modeled parameters instead of `ProtocolParameters::default()` (the stale `protocol_major = 2` the S2 PO-1 entry check exposed). It parses the forge/header-relevant fields `ProtocolParameters` models; Conway fields outside that modeled surface (`utxoCostPerByte`, `executionUnitPrices`, `maxValueSize`, …) are ignored, not denied. |
| **Creates** | Closed `ProtocolParamsParseError` (incl. `InexactRational { field: &'static str }` — a rational literal that cannot be represented exactly by integer arithmetic). Pure functions: `parse_protocol_parameters_json(bytes, network_magic) -> Result<ProtocolParameters, ProtocolParamsParseError>`; the internal exact decimal/scientific → `Rational` integer converter. Not canonical-counted (GREEN; RED `ade_runtime`). |
| **MUST NOT** | (1) **Use any float path** (hard rule) — no `f64`, no `as f64`, no serde float deserialization; rational unit-interval / non-negative-interval literals (`poolPledgeInfluence`, `monetaryExpansion`, `treasuryCut`) are preserved as `serde_json::value::RawValue` strings and converted to exact `ade_ledger::rational::Rational` by integer decimal/scientific parsing; a literal that cannot be represented exactly (non-numeric, NaN/Inf, exponent overflow) fails closed (`InexactRational`). (2) Affect authoritative outputs (it produces a parameter record the recovered-ledger install consumes; it is glue, not the `ProtocolParameters` author). (3) Introduce nondeterminism. (4) Perform I/O / hold a clock / allocate global state / `await` — pure over its byte input. |
| **Inbound deps** | `ade_runtime::consensus_inputs::canonical` (`require_forge_current_pparams` calls it on the hash-bound preimage); the G-A forge-capable seed-import callers reach it through that accessor. |
| **Outbound deps** | `ade_ledger::pparams::ProtocolParameters`, `ade_ledger::rational::Rational`, `ade_types`, `serde_json` (incl. `value::RawValue`). |
| **Entry points** | `ade_runtime::consensus_inputs::protocol_params::{parse_protocol_parameters_json, ProtocolParamsParseError}` (re-exported from `consensus_inputs::{parse_protocol_parameters_json, ProtocolParamsParseError}`). |

> **Promotable / demotable:** **promotable to BLUE on demand** (pure, deterministic over its byte input, no float, closed-error vocabulary) — it sits in RED `ade_runtime` only to live beside the consensus-inputs importer; never demotable to RED.

---

### `ade_testkit::consensus::genesis_pinning` *(GREEN by content — PHASE4-N-F-G-A S1; `#[cfg(test)]`)*

| Attribute | Value |
|-----------|-------|
| **Purpose** | The genesis-consistency pinning harness (`// GREEN`, whole module `#[cfg(test)]`). It reads the committed private-net Ade-as-leader reference fixture (S1b: `consensus-inputs.json` + `shelley-genesis.json` + `PROVENANCE.md`), builds the recovered seed-epoch surface, drives the **REAL** `ade_runtime::bootstrap::bootstrap_initial_state` warm-start, and pins Ade's recovered values + leader-eligibility inputs (eta0, ASC, stake, VRF keyhash, `praos_vrf_input`) against the genesis-derived reference. It backs CE-G-A-1 — the four S1 pinning tests. |
| **Creates** | No public types (the whole module is `#[cfg(test)] mod tests`); it exercises BLUE/RED authorities and asserts. Not canonical-counted (GREEN test infrastructure). |
| **MUST NOT** | (1) Be anything but test infrastructure — the committed fixture is **evidence input, never runtime authority**: never a production source of eta0 / stake / ASC / VRF keyhash; the in-test sidecar pre-seed (`put_seed_epoch_consensus_inputs`) is confined to this `#[cfg(test)]` module, so the CN-CINPUT-02 populate-side fence keeps production population on the verified-bootstrap path only. (2) Compare an Ade-internal-state fingerprint to a Haskell-serialized-state hash — comparisons are over observable / derived surfaces only (DC-COMPAT-01). (3) Commit secret key material in the fixture dir (no `*.skey` / `*.vkey`; gated by `ci_check_genesis_consistency_fixture_present.sh`). (4) Affect authoritative outputs / introduce nondeterminism. |
| **Inbound deps** | None at compile time (exercised by the crate's own `#[cfg(test)]` suite). |
| **Outbound deps** | `ade_core::consensus::{era_schedule, praos_state, vrf_cert, …}`, `ade_ledger::{seed_consensus_inputs, consensus_view}`, `ade_runtime::{bootstrap, chaindb}`, the S1b committed fixture files. |
| **Entry points** | None public — the four S1 pinning `#[test]`s are the surface. |

---
### `ade_node::forge_intent` *(GREEN by content — PHASE4-N-F-F S1)*

| Attribute | Value |
|-----------|-------|
| **Purpose** | The pure tri-state forge-intent classifier for `--mode node` (`//! GREEN`). It answers "may `--mode node` forge?" as a total function of which operator-key CLI flags are *present* — **never of their contents**: it reads only `Option<&Path>` presence, opens no file, parses no key, and touches no secret. The closed two-variant `ForgeIntent` (`On`/`Off`) plus the structured `ForgeIntentError::PartialKeySet` make the dangerous state — "forge with a partial / missing / fabricated key set" — **unrepresentable as a classifier result**. A partial key set is an *error*, never an *intent*. Material loading into RED custody is `operator_forge` (S2); the `ForgeActivation` / binary `Some`/`None` flip is `node_lifecycle` (S3). (G-A/G-B/G-C leave this module unchanged.) |
| **Creates** | Closed `ForgeIntent { On(ForgePaths), Off }`; presence-validated `ForgePaths { cold, kes, vrf, opcert, genesis: PathBuf }` (paths only — no secrets, no file contents; constructed ONLY on the complete-set arm); closed `ForgeIntentError::PartialKeySet { present: Vec<&'static str>, missing: Vec<&'static str> }` (static CLI flag-name strings only). Pure function: `classify_forge_intent(cold, kes, vrf, opcert, genesis: Option<&Path>) -> Result<ForgeIntent, ForgeIntentError>` — total over all 2⁵ presence combinations, the partial arm binding the tuple by name (exhaustive without `_`). Not canonical-counted (GREEN; RED `ade_node`). |
| **MUST NOT** | (1) Observe key *contents* — it reads only `Option<&Path>` presence; MUST NOT open a file, parse a key, or touch any secret (CN-NODE-03 intent half; `ci_check_forge_intent_closed.sh`). (2) Carry a third "partial" `ForgeIntent` variant. (3) Collapse an unenumerated presence combination into `On`/`Off` via a wildcard. (4) Let `ForgeIntentError` carry a supplied path string or key bytes — only static flag-name strings (OP-OPS-04). (5) Perform I/O / hold a clock / allocate global state / `await` — pure. (6) `classify_forge_intent` is the sole classifier entry. |
| **Inbound deps** | `ade_node::node_lifecycle` (the `--mode node` arm is the sole caller); `ade_node::operator_forge` (consumes the `ForgePaths` the `On` variant carries). |
| **Outbound deps** | `std::path` (`Path`, `PathBuf`) only. |
| **Entry points** | `ade_node::forge_intent::{classify_forge_intent, ForgeIntent, ForgePaths, ForgeIntentError}`. |

> **Promotable / demotable:** firmly **promotable to BLUE on demand**; never demotable to RED.

---

### `ade_node::run_loop_planner` *(GREEN by content — PHASE4-N-F-D S1; forge step PHASE4-N-F-E S1)*

| Attribute | Value |
|-----------|-------|
| **Purpose** | The pure lifecycle decision function for the `--mode node` relay run loop (`//! GREEN`). `plan_loop_step` selects each iteration's `LoopStep` from four closed, orthogonal, lifecycle-level inputs — operator intent (`ShutdownStatus`), momentary source readiness (`SyncStatus`), forge-slot scheduling (`ForgeSlotStatus`), and structural feed liveness (`LoopState`). It owns **no authority**. The planner learns only whether a forge slot is *due* via the content-blind `ForgeSlotStatus` — never who is a leader (leadership is decided BLUE inside `forge_one_from_recovered`). The forge-slot *monotonicity* is the pure `forge_slot_status` guard — the ONLY function here that observes a `SlotNo`. (G-A/G-B/G-C leave this module unchanged.) |
| **Creates** | `LoopState`, `SyncStatus`, `ShutdownStatus`, closed `ForgeSlotStatus { Due, NotDue }`, closed `LoopStep { SyncOnce, Idle, HaltCleanly, ForgeTick }`. Pure functions: `plan_loop_step(loop_state, sync_status, forge_slot_status, shutdown_status) -> LoopStep`, `forge_slot_status(last_forged_slot: Option<SlotNo>, current_slot: SlotNo) -> ForgeSlotStatus`. Not canonical-counted (GREEN; RED `ade_node`). |
| **MUST NOT** | (1) `plan_loop_step` MUST stay content-blind — it may NOT reference `SlotNo` (only the `forge_slot_status` guard may); step selection ranges only over the closed `ForgeSlotStatus` (DC-NODE-05; `ci_check_loop_planner_closed.sh`). (2) Return a `LoopStep` outside the four closed variants. (3) Perform I/O / hold a clock / allocate / `await` — pure. (4) Decide any authority (validity / chain selection / leadership / forge eligibility / evidence). (5) Return `ForgeTick` under `ForgeSlotStatus::NotDue`. |
| **Inbound deps** | `ade_node::node_lifecycle` (`run_relay_loop` is the sole caller). |
| **Outbound deps** | `ade_types` (`SlotNo`, in the `forge_slot_status` guard only). |
| **Entry points** | `ade_node::run_loop_planner::{plan_loop_step, forge_slot_status, LoopStep, ForgeSlotStatus, LoopState, SyncStatus, ShutdownStatus}`. |

> **Promotable / demotable:** firmly **promotable to BLUE on demand**; never demotable to RED.

---

### `ade_testkit`

| Attribute | Value |
|-----------|-------|
| **Purpose** | Test infrastructure: differential harness, oracle snapshot loader, regression corpus, genesis loader, era mapping, transcript replay, diff reporting; consensus / block-validity / tx-validity / mempool-ingress / proposal-procedures / producer test harnesses; the N-Y observable-surface snapshot→tip differential harness (`harness::sync_diff`); and (N-F-G-A S1) the `consensus::genesis_pinning` genesis-consistency pinning harness (full entry above). Supplies the `ConwayValidityCorpus` the bootstrap/warm-start/forge tests build a warm-startable chaindb + `PoolDistrView` from. (G-C leaves this crate unchanged.) |
| **Creates** | Harness + corpus types (`ConwayValidityCorpus`, differential replay drivers, snapshot loaders). **N-Y `harness::sync_diff`:** `BlockVerdict` (closed observable-surface verdict string), `ObservableBlockSurface`, `UtxoEntry`, `SyncOracleFixture`, `AdeObservedSurfaces`, `SyncRegressionFixture`, `RegressionFixtureViolation`. **N-F-G-A `consensus::genesis_pinning`:** no public types (`#[cfg(test)]`-only). Not canonical-counted (GREEN). |
| **MUST NOT** | (1) Affect authoritative outputs. (2) Introduce nondeterminism into the BLUE-under-test path. (3) Construct semantic types bypassing the canonical decoders. **`harness::sync_diff` + `consensus::genesis_pinning` (DC-COMPAT-01):** MUST NOT compare Ade's internal ledger `fingerprint` to a Haskell / serialized-state hash — compatibility is proven only on observable surfaces (`ci_check_no_haskell_fingerprint_equality.sh`). |
| **Inbound deps** | None at compile time; consumed via integration tests + dev-dep links. |
| **Outbound deps** | `ade_types`, `ade_codec`, `ade_crypto`, `ade_core`, `ade_ledger`, `ade_plutus`, `ade_runtime`, `blake2`, `flate2`, `tar`, `serde`, `serde_json`, `toml`, `cardano-crypto` (dev). |
| **Entry points** | `ade_testkit::validity::corpus::ConwayValidityCorpus`, the differential `*_replay` drivers, the snapshot loaders, the genesis loader, `ade_testkit::harness::sync_diff::{sync_differential_snapshot_to_tip, diff_observable_surfaces, parse_sync_oracle_fixture, load_committed_regression_fixtures, validate_regression_fixture}`. |
| **Key modules** | `consensus/` (incl. `genesis_pinning.rs`, N-F-G-A), `validity/`, `tx_validity/`, `mempool/`, `governance/`, `producer/`, `harness/` (incl. `sync_diff.rs`, `diff_report.rs`), differential + corpus infrastructure. |

---

### `ade_network::session` *(GREEN by content — PHASE4-N-L)*

| Attribute | Value |
|-----------|-------|
| **Purpose** | The pure session driver. Composes the BLUE authorities `mux::frame::{encode,decode}_frame`, `handshake::n2n_transition`, and per-mini-protocol state machines through `session::core::step`; owns the partial-frame buffer + `SessionState` type-state + closed `AcceptedMiniProtocol` registry + per-mini-protocol payload reassembly. **N-F-G-E:** `session::core` gains the closed const `MAX_REASSEMBLY_TAIL_BYTES = 16 MiB` — after `drain_protocol_items` drains every COMPLETE item, an incomplete reassembly tail over the cap fails closed with the additive closed-enum variant `SessionError::ReassemblyBufferOverflow` (drop the peer; bounds peer-driven memory before the BLUE `ade_codec` decode path). |
| **Creates** | ~12 closed types across `session::{event, state, demux, core, handshake_driver}`. **N-F-G-E** adds the closed `SessionError::ReassemblyBufferOverflow { protocol: u16, len: usize, cap: usize }` variant (additive; no new type) + the closed const `MAX_REASSEMBLY_TAIL_BYTES = 16 * 1024 * 1024` in `session::core`. GREEN — not canonical-counted (`session/` is not a BLUE `ade_network` submodule path). |
| **MUST NOT** | (1) `session::core::step` is the only pub reducer in `session/`. (2) no `tokio::*` imports in session core. (3) `AcceptedMiniProtocol` closed. (4) no unbounded queue (`ci_check_session_no_unbounded.sh`). (5) affect authoritative outputs. **(6) reassembly-tail bound (DC-LIVEMEM-01, N-F-G-E):** after draining every COMPLETE item, an incomplete per-mini-protocol reassembly tail over `MAX_REASSEMBLY_TAIL_BYTES` MUST fail closed (`SessionError::ReassemblyBufferOverflow`, peer-fatal) — never grow unbounded, never silently truncate, never partial-decode; `MAX_REASSEMBLY_TAIL_BYTES` is a closed literal const, NOT a Cardano semantic parameter, and MUST NOT be wired to CLI / env / config (`ci_check_live_feed_memory_bounds.sh` guards 1 + 3); the cap fires BEFORE the BLUE decode path; the GREEN no-clock/rand/float/HashMap purity contract is preserved (the check is a pure `buf.len()` compare). The new variant carries only non-secret primitives (`protocol`/`len`/`cap`) and has exactly one exhaustive consumer (`ade_runtime::network::mux_pump::session_err_to_halt` → `PeerHaltReason::ChainSyncDecodeError`); no wildcard match. |
| **Inbound deps** | `ade_runtime::network::{mux_pump, n2n_dialer}`, `ade_runtime::orchestrator::keep_alive_session`. The N-F-G-C live WirePump feed reaches this driver transitively via the reused `ade_runtime::admission::{dial_for_admission, run_admission_wire_pump}`. |
| **Outbound deps** | `ade_network` BLUE submodules, `ade_codec`, `ade_types`. |
| **Entry points** | `ade_network::session::core::step`, `ade_network::session::{state::SessionState, event::*, demux::*, handshake_driver::*}`. |
| **Key modules** | `session/{mod, core, event, state, demux, handshake_driver}.rs`. The N-F-G-E reassembly-tail cap (`MAX_REASSEMBLY_TAIL_BYTES` + the `SessionError::ReassemblyBufferOverflow` variant) lives in `session/core.rs` + `session/event.rs`. |

---

### `ade_runtime::seed_consensus_merge` *(GREEN by content — PHASE4-N-F-A A2)*

| Attribute | Value |
|-----------|-------|
| **Purpose** | The deterministic, no-I/O merge transform that lifts a verified-bootstrap `LiveConsensusInputsCanonical` (bootstrap-time two-map shape) plus the minted anchor fingerprint + seed epoch into the BLUE single-map `SeedEpochConsensusInputs`. `total_active_stake` is the saturating sum of every pool's `active_stake`, derived exactly as the forge-time projection does. |
| **Creates** | `SeedConsensusMergeError` (closed: `PoolMissingVrfKeyhash { pool }` / `PoolMissingStake { pool }`). Not canonical-counted (GREEN). |
| **MUST NOT** | Affect authoritative outputs. Introduce nondeterminism (`BTreeMap` only). **Default-fill on a provenance gap:** a pool present in `pool_distribution` but absent from `pool_vrf_keyhashes` (or the reverse) MUST fail closed with the structured `SeedConsensusMergeError` — never a zero-hash fill. |
| **Inbound deps** | `ade_runtime::{genesis_bootstrap, mithril_bootstrap}` (the two verified-bootstrap composers). |
| **Outbound deps** | `ade_ledger::{consensus_view::PoolEntry, seed_consensus_inputs::SeedEpochConsensusInputs}`, `ade_runtime::consensus_inputs::LiveConsensusInputsCanonical`, `ade_types`. |
| **Entry points** | `ade_runtime::seed_consensus_merge::{merge_seed_epoch_consensus_inputs, SeedConsensusMergeError}`. |

---

### `ade_runtime` GREEN-by-content sub-trees

> All carry a `//! GREEN …` banner inside the RED `ade_runtime` crate, the BLUE deny attributes, and a purity CI gate. Each is "promotable to BLUE on demand, never demotable to RED" unless noted.

| Sub-tree | Purpose | MUST NOT | CI gate |
|---|---|---|---|
| `producer::self_accepted_handoff` *(N-F-G-B S1)* | GREEN typed constructor-fenced carrier for a BLUE self-accepted forged block (sole ctor `from_self_accepted(AcceptedBlock)`; accessors `accepted()` / `into_accepted()`). *(Full entry above.)* | Carry an artifact that was not BLUE self-accepted (no raw-bytes / artifact / event / flag / verdict ctor); re-derive the token from `artifact.bytes`; re-validate / mutate; affect authoritative outputs. | `ci_check_served_chain_handoff_fence.sh` (serve-ingress; broadened N-F-G-C S1) |
| `consensus_inputs::protocol_params` *(N-F-G-A S2a)* | GREEN cardano-cli protocol-parameters JSON → BLUE `ProtocolParameters`; exact decimal/scientific → `Rational` by integer arithmetic. *(Full entry above.)* | Use any float path (`f64`/`as f64`/serde float); affect authoritative outputs; introduce nondeterminism. | `ci_check_recovered_ledger_pparams_sourced.sh` (install-side, S2a) |
| `consensus_inputs::canonical` (`require_forge_current_pparams`) *(N-F-G-A S2a)* | GREEN hash-bound current-pparams accessor — `protocol_params_json` carried OUTSIDE the frozen 15-field fingerprint; requires present + hash-bind to `protocol_params_hash` + exact parse, fail-closed `ForgeCurrentPParamsError`. | Alter the bundle fingerprint by adding the preimage to the canonical CBOR; return pparams without the `blake2b_256` hash bind. | `ci_check_recovered_ledger_pparams_sourced.sh` |
| `seed_consensus_merge` *(N-F-A A2)* | GREEN bootstrap-time merge: two-map `LiveConsensusInputsCanonical` → single-map BLUE `SeedEpochConsensusInputs`. *(Full entry above.)* | Default-fill a provenance gap (zero-hash); affect authoritative outputs; introduce nondeterminism. | `ci_check_consensus_input_provenance.sh` |
| `forward_sync::reducer` *(N-Y)* | GREEN forward-sync lifecycle reducer; the private `AdmitPlan::durable` is the sole `AdvanceTip` emitter (durable-before-tip). The RED `forward_sync::pump_block` driver gained its first production caller in N-F-C. | Emit `AdvanceTip` before `StoreBlockBytes` + `AppendWal` (DC-SYNC-01); affect authoritative outputs. | `ci_check_forward_sync_chokepoint_only.sh` |
| `producer::coordinator` *(N-Q)* | Pure coordinator for live producer-mode; hosts `CoordinatorState::kes_period_for_slot` (reused by the N-F-E forge tick AND the N-F-F operator-material activation). | Own/store private signing material (CN-PROD-02); affect authoritative outputs. | `ci_check_producer_coordinator_no_secrets.sh` |
| `producer::producer_log` *(N-Q, extended N-W)* | Closed-vocabulary producer evidence log. | Add an open/wildcard event variant; emit `BlockServed` for a block not in the served snapshot (CN-PROD-04). | `ci_check_operator_evidence_manifest_schema.sh` |
| `producer::chain_evolution` *(N-T; N-F-G-J)* | Linear `ChainEvolution` typestate. **N-F-G-J** migrated `prev_hash() -> PrevHash` — cold-start (`tip == None`) proposes `PrevHash::Genesis`, never an all-zero `Hash32` stand-in (the stand-in is **deleted at its source**); `Some(tip)` proposes `PrevHash::Block`. The proposal is GREEN over already-authoritative tip state; the BLUE `decode_block -> check_header_position` is the final position authority. | Never mints `AcceptedBlock` (CE-T-7); advance on authority disagreement (CE-T-6b); propose an all-zero `Hash32` for the genesis predecessor (CN-WIRE-09 — it is `PrevHash::Genesis`/null). | gated by CE-T-7; purity inherited; `ci_check_prevhash_single_wire_authority.sh` (cold-start -> Genesis) |
| `producer::{broadcast_to_served, served_chain_lookups}` *(N-G)* | GREEN served-chain broadcast targets + read-side lookups. | Affect authoritative outputs; serve a block that failed self-accept. | `ci_check_broadcast_to_served_purity.sh` |
| `clock` (`DeterministicClock` + `Clock` trait + `millis_to_slot` + `checked_millis_to_slot`) *(N-K; checked guard N-F-G-A S3)* | GREEN clock trait + deterministic impl (DC-NODE-03) + the pure `millis_to_slot` projection. **N-F-G-A** added the closed `SlotAlignmentError` + `checked_millis_to_slot` (the forge-path guard: before-anchor fails closed instead of saturating to `start_slot`). The N-F-E/G-A `ForgeTick` derives its slot via `checked_millis_to_slot`; the N-F-F `On` arm injects a `SystemClock` (the sole wall-clock read on the forge path). | `DeterministicClock` reads no wall-clock; `checked_millis_to_slot` uses no float, no wall-clock, and MUST NOT saturate a before-anchor tick (fail closed). | `ci_check_clock_seam.sh` (seam); the before-anchor fail-closed wiring is asserted via the S3 unit tests + the node-path tests |
| `orchestrator::{mod, event, state, core}` *(N-K)* | GREEN core reducer + closed-vocabulary event + state. | tokio imports in core; open event vocabulary. | `ci_check_orchestrator_core_purity.sh` |
| `rollback::{cadence, in_memory_cache, chaindb_block_source, persistent_cache, persistent_writer}` *(N-I/N-J/N-K)* | GREEN rollback adapter glue + snapshot cadence + persistent cache/writer (DC-NODE-02, DC-CONS-21). | Parallel snapshot cadence; affect authoritative rollback state. | `ci_check_persistent_writer_no_parallel_cadence.sh`, `ci_check_snapshot_cadence_purity.sh` |
| `receive::{events_to_state, in_memory_chain_write}` *(N-H)* | GREEN receive-side glue. | Affect authoritative receive verdict. | `ci_check_receive_replay_purity.sh` |
| `seed_import` *(N-M-A)* | Single authority converting a cardano-cli JSON UTxO dump into canonical seed entries. | Construct semantic types bypassing canonical decoders. | `ci_check_seed_import_closure.sh`, `ci_check_seed_import_full_preprod_support.sh` |
| `bootstrap_anchor` *(N-M-A)* | RED-shell composer minting `BootstrapAnchor` (the *types* + binding predicate are BLUE in `ade_ledger`). | Mint an anchor outside this composer; bind a Mithril anchor without the BLUE `verify_mithril_binding`. | `ci_check_bootstrap_anchor_closure.sh` |
| `wal` *(N-M-A)* | File-backed Ade-native WAL (append-only). | Mutate/rewrite committed WAL entries. | `ci_check_wal_append_only.sh` |
| `consensus_inputs` *(N-M-C; extended N-F-G-A)* | Operator-extracted `LiveConsensusInputs` importer; **N-F-G-A** adds the `protocol_params` parser + the `require_forge_current_pparams` hash-bound preimage accessor (rows above). | Treat the peer as runtime authority; overstate semantic truth. | `ci_check_live_consensus_inputs_closure.sh`, `ci_check_live_consensus_inputs_fingerprint.sh` |
| `admission::*` (the GREEN reducer half + the reused dial/pump) *(N-M-C; reused N-F-G-C)* | GREEN admission verdict/agreement reducer comparing already-authoritative outputs. The RED `admission::{dial_for_admission, run_admission_wire_pump}` dial + pump are **reused verbatim** by the N-F-G-C `node_lifecycle::spawn_live_wire_pump_source` (no reimpl, no new wire authority) to build the LIVE `NodeBlockSource::WirePump` feed. | Emit RED verdicts; skip reference scripts; treat `lagging` as success (DC-EVIDENCE-01). | `ci_check_admission_runner_closure.sh`, `ci_check_admission_no_red_verdicts.sh`, `ci_check_lagging_is_evidence_only.sh`, `ci_check_admit_replay_equivalence.sh` |

> **Note on `bootstrap` (the `bootstrap_initial_state` authority, N-K):** it carries a `//! GREEN` banner but at HEAD also hosts the **RED** A3b warm-start sidecar restore (a `SnapshotStore` read). It is documented in the RED `ade_runtime` entry below to keep the I/O-touching surface (`restore_seed_epoch_consensus_inputs`) visible; the cold-start / warm-start materialization composition remains GREEN over the BLUE authorities.

---
### `ade_node::ba02_evidence` *(GREEN by content — PHASE4-N-F-C L6; library; now consumed by `ba02_pass`)*

| Attribute | Value |
|-----------|-------|
| **Purpose** | The BA-02 peer-acceptance evidence surface: it COMPARES two already-authoritative outputs — the BLUE-minted forged-block hash (read verbatim from `ForgedBlockArtifact`, never recomputed) and an operator-captured peer-accept signal — and emits a closed verdict. GREEN evidence: it forges nothing, admits nothing, persists no node state. **N-F-G-C:** still reached by no binary arm, but its `parse_peer_accept_events` + `correlate` are now *consumed* by the NEW RED `ade_node::ba02_pass` file-I/O wrapper (S2) — `correlate` stays the SOLE `Ba02Manifest` constructor and this module is **byte-unchanged**. |
| **Creates** | `AdeForgeRecord`; closed `PeerAcceptEvent` (2-variant allow-list); `PeerAcceptSource`; `NoEvidenceReason` (closed: `NoPeerAccept`/`HashMismatch`/`ChainPointMismatch`/`ConflictingPeerSignals`); `BA02Outcome` (closed: `Ba02Manifest` / `NoEvidence`); versioned `Ba02Manifest` (`BA02_MANIFEST_SCHEMA_VERSION = 1`). GREEN — not canonical-counted. |
| **MUST NOT** | (1) Affect authoritative outputs / forge / admit / persist node state. (2) Recompute the forged-block hash. (3) Parse block bytes or construct semantic types. (4) **(RO-LIVE-06 / BA-02 honesty)** treat any weaker signal as acceptance — Ade self-accept, `ForgeSucceeded`, `block_received`, a lagging/diverged agreement verdict, or conflicting peer signals are all `NoEvidence`. (5) Construct a `Ba02Manifest` anywhere but `correlate`'s exact-match arm (the RED `ba02_pass::write_ba02_manifest` only *writes* an already-`correlate`-produced `Ba02Manifest`; it constructs none). (6) Commit a synthetic manifest under `docs/evidence/*ba02*` or `docs/clusters/PHASE4-N-F-G-C/CE-G-C-LIVE_*.toml` (`ci_check_ba02_evidence_closed.sh`, `ci_check_ba02_evidence_manifest_schema.sh`). |
| **Inbound deps** | `ade_node::ba02_pass` (the RED file-I/O wrapper, N-F-G-C — the SOLE in-tree caller). **N-F-G-D:** also reached transitively via the RED `ade_node::rehearsal_pass`, which calls `ba02_pass::correlate_peer_log_file` verbatim. Reached otherwise only by tests. (Intended runtime caller: the operator-pass live leg under RO-LIVE-01, still deferred.) |
| **Outbound deps** | `ade_runtime::producer::coordinator::ForgedBlockArtifact`, `ade_types::Hash32`, `serde`, `serde_json`. |
| **Entry points** | `ade_node::ba02_evidence::{parse_peer_accept_events, correlate, AdeForgeRecord, Ba02Manifest, BA02Outcome, PeerAcceptEvent}`. |

---

### `ade_node::rehearsal_evidence` *(GREEN by content — PHASE4-N-F-G-D S2)*

| Attribute | Value |
|-----------|-------|
| **Purpose** | The non-promotable private-testnet rehearsal-evidence envelope (`//! GREEN`). `PrivateRehearsalManifest` WRAPS a correlate-produced `ba02_evidence::Ba02Manifest` payload (the SAME proof the bounty BA-02 path produces) in a structurally distinct, NON-PROMOTABLE rehearsal envelope. It records ONLY that the exact `--mode node` accepted-block path was exercised end-to-end against a real Haskell peer on a C1 private testnet, as a bounty DRY-RUN. Pure / deterministic: no I/O, no clock, no rand, no float, no `HashMap`. **Honesty line (load-bearing):** Ade self-accept / served bytes / wire success are NOT acceptance — only the Haskell peer log through `ba02_evidence::correlate` is (the allow-list is inherited verbatim). C1 acceptance ≠ bounty completion. |
| **Creates** | `PrivateRehearsalManifest` (`ba02: Ba02Manifest` + `venue` + `peer_log_file` + `peer_log_file_sha256`); closed 1-variant `RehearsalVenue { PrivateTestnetC1 }` (a non-private venue is unrepresentable — a rehearsal is never preprod / preview); `RehearsalEnvelope` (the venue + committed peer-log filename + its sha256, supplied by the RED I/O caller); `REHEARSAL_MANIFEST_SCHEMA_VERSION = 1`. GREEN — not canonical-counted. |
| **MUST NOT** | (1) Affect authoritative outputs / forge / admit / persist node state. (2) Construct a `PrivateRehearsalManifest` from anything but a correlate-produced `BA02Outcome::Ba02Manifest` — the SOLE ctor `from_correlate_outcome` returns `None` on `BA02Outcome::NoEvidence` (nothing to wrap, nothing to write), so a rehearsal manifest is ALWAYS correlate-produced; there is no path from raw operator input or a `NoEvidence` outcome to a manifest (CN-REHEARSAL-FIDELITY-01 clause 2). (3) Serialize a non-rehearsal — `to_canonical_toml` ALWAYS emits `is_rehearsal = true` + `not_bounty_evidence = true` as LITERALS; the type cannot represent a non-rehearsal. (4) Live under the bounty home or be referenced by the `CE-G-C-LIVE_*` / `ci_check_ba02_evidence_manifest_schema.sh` gate — the rehearsal manifest lives ONLY under `docs/evidence/phase4-n-f-g-d-private-rehearsal-*.toml` (`ci_check_rehearsal_manifest_schema.sh`). (5) Flip any RO-LIVE rule (C1 acceptance ≠ bounty completion). |
| **Inbound deps** | `ade_node::rehearsal_pass` (the RED file-I/O wrapper, N-F-G-D — the SOLE in-tree caller; reached otherwise only by the in-module unit tests + `crates/ade_node/tests/node_c1_dry_run_rehearsal.rs`). |
| **Outbound deps** | `ade_node::ba02_evidence::{Ba02Manifest, BA02Outcome, PeerAcceptSource}`. (Std-only; no I/O.) |
| **Entry points** | `ade_node::rehearsal_evidence::{PrivateRehearsalManifest (incl. from_correlate_outcome, to_canonical_toml), RehearsalEnvelope, RehearsalVenue, REHEARSAL_MANIFEST_SCHEMA_VERSION}`. |

---

### `ade_node::live_log::{sched_event, sched_writer}` *(GREEN by content — PHASE4-N-F-G-J S1)*

| Attribute | Value |
|-----------|-------|
| **Purpose** | The closed `--mode node` feed/forge scheduling-event vocabulary + its byte-deterministic JSONL writer (`//! GREEN`, `CN-NODE-04`). `NodeSchedEvent` is a CLOSED, fail-closed-on-unknown, **emit-only** diagnostic surface that makes the relay loop's feed/forge scheduling decision observable WITHOUT changing it. It is a SIBLING of the wire-only `live_log::event::LiveLogEvent` — a separate closed enum with its own hand-rolled JSON serializer (`NodeSchedLogWriter`, one object per line, flushed after every emit so a SIGINT mid-emit cannot produce a partial line; no serde-derive). `run_relay_loop_with_sched` records `ForgeTickConsidered` / `ForgeAttempted` / `ForgeResult` / `ForgeTickSkipped` / `FeedUnavailable` through an `Option<&mut dyn NodeSchedSink>` that is **best-effort and NEVER alters the loop's scheduling / control flow**. Pure / deterministic: no I/O beyond the injected writer sink, no clock, no rand, no float, no `HashMap`. |
| **Creates** | `NodeSchedEvent` (closed: `ForgeTickConsidered`, `ForgeAttempted`, `ForgeResult { outcome }`, `ForgeTickSkipped { reason }`, `FeedUnavailable { reason }`), `ForgeOutcome` + `FeedReason` (closed scheduling-diagnostic enums), `NodeSchedLogWriter<W>`, the `NodeSchedSink` trait. GREEN — not canonical-counted (lives in RED `ade_node`). |
| **MUST NOT** | (1) Alter the relay loop's feed/forge scheduling or control flow — the sink is best-effort and emit-only; the planner NEVER reads a sched event (`ci_check_node_sched_events_emit_only.sh`, `CN-NODE-04`). (2) Carry an open / wildcard variant — a new `NodeSchedEvent` variant is a compile error in the exhaustive per-variant writer match (no `_`). (3) Overstate semantic truth — a sched event is a scheduling diagnostic, never an acceptance / verdict / authority signal. (4) Perform I/O beyond the injected writer / hold a clock / allocate global state / `await`. |
| **Inbound deps** | `ade_node::node_lifecycle` (`run_relay_loop_with_sched` records through the `NodeSchedSink`); reached otherwise only by the in-crate `#[tokio::test]` suite. |
| **Outbound deps** | `std::io` (the writer `W`); `ade_types` (scheduling primitives). No serde-derive. |
| **Entry points** | `ade_node::live_log::{NodeSchedEvent, NodeSchedLogWriter, NodeSchedSink, ForgeOutcome, FeedReason}`. |

---

### `ade_node` GREEN-by-content sub-trees

| Sub-tree | Purpose | MUST NOT | CI gate |
|---|---|---|---|
| `forge_intent` *(N-F-F S1)* | GREEN pure tri-state forge-intent classifier. *(Full entry above.)* | Observe key contents / open a file / parse a key; carry a third "partial" variant; collapse an unenumerated subset via a wildcard; let the error carry a path/secret byte. | `ci_check_forge_intent_closed.sh` |
| `run_loop_planner` *(N-F-D S1; forge step N-F-E S1)* | GREEN pure lifecycle planner. *(Full entry above.)* | `plan_loop_step` reference `SlotNo` (only the guard may); return a non-closed `LoopStep`; decide any authority; return `ForgeTick` under `NotDue`. | `ci_check_loop_planner_closed.sh` |
| `node_sync::forge_epoch_admission` *(N-F-G-A S4)* | GREEN pure epoch-boundary guard — `forge_epoch_admission(slot, era_schedule, seed_epoch)` over the closed `ForgeEpochAdmission { WithinSeedEpoch, OffEpoch }`, derived solely via the BLUE `EraSchedule::locate`. Called BEFORE leadership / KES signing inside `forge_one_from_recovered`. | Fabricate the candidate epoch (must use `EraSchedule::locate`); run after leadership; drive a nonce promotion (DC-EPOCH-03). | `ci_check_node_forge_single_epoch_fail_closed.sh` |
| `admission_log` *(N-M-B)* | GREEN admission-mode JSONL event vocabulary + writer (closed enum). | Add open/wildcard event variant. | `ci_check_admission_log_vocabulary_closed.sh` |
| `live_log` *(N-L-LIVE)* | GREEN closed JSONL vocabulary for the wire-only live pass. | Add open event variant; overstate semantic truth. | `ci_check_wire_only_event_vocabulary_closed.sh` |
| `live_log::{sched_event, sched_writer}` *(N-F-G-J S1)* | GREEN closed **emit-only** feed/forge scheduling-event vocabulary `NodeSchedEvent` + its byte-deterministic JSONL writer; a sibling of the wire-only `LiveLogEvent`. *(Full entry above.)* | Alter the relay loop's scheduling / control flow (best-effort emit-only sink; the planner never reads it); add an open/wildcard variant; overstate semantic truth (a diagnostic, never acceptance/verdict). | `ci_check_node_sched_events_emit_only.sh` |
| `admission` (the GREEN half of the orchestrator) *(N-M-B)* | GREEN admission orchestrator reducer + verdict mapping. | Affect authoritative verdict; skip reference-script validation. | `ci_check_admission_no_refscript_skip.sh` |
| `ba02_evidence` *(N-F-C, library; now consumed by `ba02_pass` N-F-G-C)* | GREEN BA-02 evidence correlator. *(Full entry above.)* | Treat a weaker signal as acceptance; recompute the forged hash; forge/admit/persist; construct a `Ba02Manifest` outside `correlate`. | `ci_check_ba02_evidence_closed.sh`, `ci_check_ba02_evidence_manifest_schema.sh` |
| `rehearsal_evidence` *(N-F-G-D S2)* | GREEN non-promotable private-testnet rehearsal-evidence envelope. *(Full entry above.)* | Construct a `PrivateRehearsalManifest` from anything but a correlate-produced payload (`from_correlate_outcome` returns `None` on `NoEvidence`); serialize a non-rehearsal (`to_canonical_toml` always emits `is_rehearsal`/`not_bounty_evidence` literals); live under / be referenced by the bounty home; flip any RO-LIVE rule. | `ci_check_rehearsal_manifest_schema.sh` |

> **Note on `node_sync` color:** `node_sync` is **RED** (it drives the apply path / owns the persistent-store reads + the live-feed forge wrap). The `forge_epoch_admission` *function* within it carries a `GREEN — pure` banner (derived solely from `(slot, era_schedule, seed_epoch)`); it is surfaced as a GREEN sub-tree row here, while the crate entry for `node_sync` stays in the RED `ade_node` section. **Note on `ba02_pass` color (N-F-G-C):** the `ba02_pass` module is **RED** (file I/O — `fs::read_to_string` / `fs::write`); it is documented in the RED `ade_node` section below. It wraps the GREEN `ba02_evidence` correlator but performs the I/O the correlator must not. **Note on the N-F-G-D rehearsal split:** `rehearsal_evidence` is **GREEN** (the pure `PrivateRehearsalManifest` envelope, full entry above); `rehearsal_pass` is **RED** (file I/O that reuses `ba02_pass::correlate_peer_log_file` verbatim and wraps a correlate-produced payload), documented in the RED `ade_node` section below. **Note on the N-F-G-J sched vocabulary:** `live_log::{sched_event, sched_writer}` are **GREEN** (the closed emit-only `NodeSchedEvent` + its byte-deterministic JSONL writer — full entry above); the two GREEN-by-function cold-start decisions `node_sync::forge_header_position(Option<&ChainTip>)` and `node_lifecycle::may_cold_start_forge(...)` carry GREEN-pure semantics within their RED host modules (documented in the RED `ade_node` row below).

---
## RED Modules — Imperative Shell

> I/O, network, storage, clocks, retries. May depend on BLUE/GREEN. Must not modify core state directly or construct semantic types unsafely.

### `ade_runtime::network::serve_dispatch` *(RED — PHASE4-N-F-G-H S1)*

| Attribute | Value |
|-----------|-------|
| **Purpose** | The single shared, **coordinator-free** server-side serve-dispatch authority (`//! RED`). `dispatch_server_frame_event_to_outbound` maps an inbound N2N server-frame event over a borrowed GREEN `ServedChainView` through the BLUE `ade_network::{chain_sync,block_fetch}::server` serve reducers (`producer_chain_sync_serve` / `producer_block_fetch_serve` / `producer_chain_sync_advance_tip`) and relays the typed reply as an `OutboundCommand` over the per-peer outbound channel — returning `(frames_sent, Vec<ServedBlockEvidence>)`. It is the ONE serve-dispatch core both modes call: extracted verbatim from `ade_node::produce_mode` in S1 (its `CoordinatorState` / `coordinator_step` / producer-evidence-writer wrapping stays in `produce_mode`, which is re-pointed here), then wired to the node spine in S2 via `ade_node::node_lifecycle::run_node_serve_task`. It is **request-driven only** — it reacts to server-frame events; it installs no proactive `advance_tip` / `served_chain_view.changed()` reactor. Backs `DC-NODE-07`'s "no second serve authority / serializer" clause. Registered `pub mod serve_dispatch;` in `network/mod.rs`. |
| **Creates (RED-only)** | Closed 4-variant `DispatchError { UnknownPeer { peer_id }, PeerOutboundMissing { peer_id }, SendFailure { peer_id }, ReducerError }` (non-secret primitives only); `ServerPeerStates` (type alias `BTreeMap<PeerId, PerPeerN2nServerState>`); the **GREEN-by-content** `ServedBlockEvidence { peer_id, slot, hash: [u8;32], bytes_len }` (a pure observation of a block present in the served snapshot, collected into an owned `Vec` BEFORE any `.await` — it OBSERVES, never re-decides what the BLUE reducer serves, never fabricated). Never semantic/canonical types — all three are **moved verbatim** from `produce_mode` (no field changes; not BLUE; the 456-count is unaffected). |
| **Interprets** | An inbound N2N server-frame `OrchestratorEvent` (`PeerN2nServer*Frame`): it decodes the frame via the BLUE session/codec authorities (delegated), drives the BLUE serve reducer over the borrowed `ServedChainView`, and emits a typed `OutboundCommand`. It parses no block bytes and constructs no semantic type. `install_server_peer_state` / `remove_server_peer_state` are the per-peer-state lifecycle helpers (install `PerPeerN2nServerState::new(..)` on a downstream-server `PeerConnected`; remove + drop the per-peer outbound sender on `PeerDisconnected`) — coordinator-free. |
| **MUST NOT** | (1) **Be defined more than once** — exactly ONE `fn dispatch_server_frame_event_to_outbound` in the workspace, under `crates/ade_runtime/`, zero in `crates/ade_node/`; `node_lifecycle.rs` defines no parallel serve-dispatch; `produce_mode.rs` references this shared core (no private copy) (DC-NODE-07; `ci_check_single_serve_dispatch_authority.sh`). (2) Hold a `CoordinatorState` / call `coordinator_step` / write producer evidence — that entanglement stays in `produce_mode`'s `handle_listener_event`; this core is coordinator-free. (3) Re-decide / re-derive what the BLUE serve reducer serves, or fabricate a `ServedBlockEvidence` — `(slot, hash, bytes_len)` are READ from the served snapshot. (4) Mutate the served chain (no `push_atomic`, no `served_chain_admit`) — it borrows the view read-only. (5) Add a proactive `advance_tip` / `changed()` reactor (request-driven only). (6) Emit outbound bytes by any path but a typed `OutboundCommand` (CN-OUTBOUND-RELAY-01) — no `Vec<u8>` byte tunnel. (7) Hold a `watch::Ref` across an `.await` (the evidence `Vec` is collected first). |
| **Inbound deps** | `ade_node::produce_mode` (`handle_listener_event`'s server-frame arms call it, S1 re-point); `ade_node::node_lifecycle::run_node_serve_task` (the node-spine serve sibling, S2). |
| **Outbound deps** | `ade_network::{chain_sync,block_fetch}::server` (BLUE serve reducers), `ade_runtime::producer::served_chain_handle::ServedChainView` (GREEN, borrowed), `ade_runtime::network::outbound_command::OutboundCommand`, `ade_runtime::network::PerPeerOutbound`, `ade_runtime::orchestrator::OrchestratorEvent`, `tokio` (channels). |
| **Entry points** | `ade_runtime::network::serve_dispatch::{dispatch_server_frame_event_to_outbound, install_server_peer_state, remove_server_peer_state, ServerPeerStates, DispatchError, ServedBlockEvidence}` (all re-exported from `ade_runtime::network`). |
| **Key modules** | `network/serve_dispatch.rs`. |

> **Sub-classification:** the module carries a `//! RED` banner (the dispatch adapter performs channel I/O); the `ServedBlockEvidence` struct inside it is **GREEN-by-content** (a pure observation, no I/O) — promotable on demand, never authoritative. The serve *reducers* it drives stay BLUE (untouched). The single-authority gate `ci_check_single_serve_dispatch_authority.sh` makes "one serve-dispatch core" durable, not point-in-time.

---

### `ade_runtime`

| Attribute | Value |
|-----------|-------|
| **Purpose** | The imperative shell — moves bytes, owns sockets/files/clocks/keys, drives tokio tasks. Hosts: producer-mode key custody + shell composition (`producer/` — incl. the **N-F-G-B** GREEN `self_accepted_handoff::SelfAcceptedHandoff` typed handoff carrier), the N2N network drivers (`network/` — incl. `mux_pump::session_err_to_halt`, which **N-F-G-E** extends to map the new GREEN `SessionError::ReassemblyBufferOverflow` → `PeerHaltReason::ChainSyncDecodeError`, dropping the peer; and the **N-F-G-H** NEW `network::serve_dispatch` shared coordinator-free serve-dispatch core (full entry above) + the `n2n_listener` whose `our_supported` is now `Arc<[(u16, VersionData)]>` for the owned magic-aware serve table), the node orchestrator runners (`orchestrator/`), the receive/rollback/admission shells (incl. the `admission::{dial_for_admission, run_admission_wire_pump}` dial + pump **reused verbatim** by the N-F-G-C `ade_node` live WirePump feed), the seed-import/WAL/consensus-inputs importers (incl. the N-F-G-A GREEN `consensus_inputs::protocol_params` parser + the `consensus_inputs::canonical` `require_forge_current_pparams` accessor), ChainDB + recovery shells, the N-Y bootstrap/forward-sync surfaces, the N-Z `mithril_bootstrap` composition, the clock seam (`clock` — DC-NODE-03; **N-F-G-A** added the GREEN `checked_millis_to_slot` + closed `SlotAlignmentError` forge-path guard alongside the saturating `millis_to_slot` and the RED `SystemClock`), and (N-F-A) the seed-epoch consensus-input recovered-state shell. The `producer::keys` cold/VRF/KES loaders + `ProducerShell::init` + `producer::{genesis_parser, opcert_envelope}` parsers + `coordinator::CoordinatorState::kes_period_for_slot` + `coordinator::GenesisAnchor` are the RED authorities the N-F-F/N-F-G-A `ade_node::operator_forge` ingress reuses (no reimpl). |
| **Creates (RED-only)** | `KesSecret`, `VrfSigningKey`, `ColdSigningKey` custody wrappers; `KeyLoadError`; `MuxTransportHandle` consumers; `OutboundCommand` (closed enum, N-S-B); `PerPeerOutbound` map; `DispatchError` (closed 4-variant — **N-F-G-H** moved it + `ServerPeerStates` + the GREEN-by-content `ServedBlockEvidence` into `network::serve_dispatch`); `ServedChainHandle`; `GenesisAnchor`; `ProducerShell` (+ `ShellInitError`); `AdmissionPeerEvent` (the dial/pump output the N-F-G-C live feed forwards into the `WirePump` arm). **Clock (N-K; N-F-G-A):** `SystemClock`, `DeterministicClock`, closed `SlotAlignmentError` (1-variant `BeforeGenesisAnchor`, GREEN-by-content). **consensus_inputs (N-M-C; N-F-G-A):** `LiveConsensusInputsCanonical` (now carries `protocol_params_json: Option<String>` OUTSIDE the 15-field fingerprint), closed `ProtocolParamsParseError`, closed `ForgeCurrentPParamsError` (`PreimageAbsent` / `BindMismatch` / parse). **N-Y:** `forward_sync::pump::{PumpError, PumpTip, SnapshotSink, NoCheckpointSink}`; `mithril_import::{…, MithrilManifestError, MithrilProvenanceImport}`; `recovery::restart::{NodeRecoveryError, RecoveredNode}`. **N-Z:** `mithril_bootstrap::{MithrilSeedPointInputs, MithrilBootstrapOutput, MithrilBootstrapError}`. **N-F-A:** `bootstrap::{SeedEpochConsensusSource (closed: NotRequired / RequiredFromRecoveredProvenance), BootstrapState, BootstrapInputs, BootstrapError (incl. the seed-consensus fail-closed variants)}`; `genesis_bootstrap::{GenesisBootstrapError, GenesisBootstrapOutput}`. Never semantic/canonical types. |
| **Interprets** | Canonicalizes peer bytes/files for the BLUE core: `producer::keys::{load_cold_signing_key_skey, load_vrf_signing_key_skey, load_kes_signing_key_skey}`; `producer::genesis_parser::parse_shelley_genesis`; `producer::opcert_envelope::parse_opcert_envelope`; `seed_import`; `admission::{dial_for_admission, run_admission_wire_pump}` (the N2N dial + chain-sync/block-fetch pump producing `AdmissionPeerEvent` — reused by the N-F-G-C live feed). **N-F-G-A:** `consensus_inputs::protocol_params::parse_protocol_parameters_json` is the SOLE cardano-cli protocol-parameters JSON parser (GREEN; exact `Rational`, no float); `consensus_inputs::canonical::require_forge_current_pparams` hash-binds the `protocol_params_json` preimage to the fingerprinted `protocol_params_hash` (via `blake2b_256`) before parsing. **N-Y:** `mithril_import::json::parse_mithril_manifest_json` is the SOLE manifest-JSON parser. **N-F-A:** `bootstrap::restore_seed_epoch_consensus_inputs` reads the anchor-fp-keyed sidecar bytes and verifies them fail-closed. |
| **MUST NOT** | (1) Modify BLUE state directly or construct semantic types from raw bytes. (2) Bypass canonical validation. (3) `producer::signing` (DC-CRYPTO-03/05) — RED-confined key custody. (4) `producer::keys` — no `KesSecret::from_*` inside `load_kes_signing_key_skey`. (5) `producer::coordinator` is GREEN and holds no secrets. (6) `network::outbound_command` (CN-OUTBOUND-RELAY-01) — `OutboundCommand` is the sole channel to MuxPump's encoder. (7) per-peer outbound map (CN-PEER-OUTBOUND-MAP-01 / DC-OUTBOUND-FIFO-01). **(7b) `network::serve_dispatch` (DC-NODE-07, N-F-G-H):** the single `dispatch_server_frame_event_to_outbound` serve-dispatch authority — defined exactly once (in `ade_runtime`), coordinator-free, request-driven, read-only over the borrowed `ServedChainView`, relaying via a typed `OutboundCommand`; no second serve-dispatch definition, no proactive `advance_tip`/`changed()` reactor, no served-chain mutation (`ci_check_single_serve_dispatch_authority.sh`). **(7c) `network::n2n_listener` magic-aware serve table (DC-NODE-07, N-F-G-H):** live serve sites build `our_supported` (now `Arc<[(u16, VersionData)]>`) via `n2n_supported_for_magic(<configured magic>)`, never the static mainnet `N2N_SUPPORTED` (`ci_check_serve_listener_magic_aware.sh`). (8) `network::n2n_server` — no signing dep. **(9) `forward_sync::pump` (DC-SYNC-01):** refuse to advance the tip before `StoreBlockBytes` + `AppendWal` return Ok (`PumpError::TipBeforeDurable`). **(10) `mithril_import` (CN-MITHRIL-01):** no semantic decision; route initial state through the single `bootstrap_initial_state` authority. **(11) `genesis_bootstrap` (CN-NODE-01 / DC-GENESIS-SRC-01 / CN-CINPUT-02):** route through the single `bootstrap_initial_state` authority; the composer WRITES the sidecar, never consumes one. **(12) `recovery::restart`:** compose the existing authorities; fail fast on `NodeRecoveryError::WalTailFingerprintMismatch`. **(13) `mithril_bootstrap` (DC-MITHRIL-02):** the anchor `seed_point` MUST be minted from `MithrilSeedPointInputs`, never the manifest; run `verify_mithril_binding` fail-closed BEFORE any `bootstrap_initial_state`. **(14) `bootstrap::restore_seed_epoch_consensus_inputs` (DC-CINPUT-01):** fail closed on a missing sidecar / hash mismatch / non-canonical decode / binding mismatch / non-byte-identical re-encode — and MUST NOT fall back to the forge-time bundle. **(15) `seed_consensus_provenance::append_seed_epoch_provenance` (CN-CINPUT-02):** MUST hash the EXACT A1 bytes the composer `put`; referenced only at the two verified-bootstrap composition sites. **(16) `chaindb` seed-sidecar surface (CN-CINPUT-02):** anchor-fp-keyed namespace disjoint from the slot-keyed snapshot space; redb `SCHEMA_VERSION = 3`. **(17) `producer::keys` + `ProducerShell` + `producer::{genesis_parser, opcert_envelope}` (CN-NODE-03 / OP-OPS-04, reused by N-F-F/N-F-G-A):** the cold/VRF/KES loaders + the opcert/genesis parsers are the SOLE key/config-parse authorities — `ade_node::operator_forge` reuses them and MUST NOT reimplement parsing; `ProducerShell` is the RED key-custody holder (no public private-byte accessor; `KeyLoadError` carries no path bytes). **(18) `admission::{dial_for_admission, run_admission_wire_pump}` (reused by N-F-G-C):** the SOLE N2N admission dial + pump — the live `--mode node` feed reuses them verbatim and MUST NOT reimplement the dial/handshake/pump or invent a parallel wire authority; they emit `AdmissionPeerEvent`, never a semantic verdict or a tip. **(19) `clock::checked_millis_to_slot` (N-F-G-A S3):** MUST NOT saturate a before-anchor tick — it fails closed (`SlotAlignmentError::BeforeGenesisAnchor`); no float, no wall-clock; the saturating `millis_to_slot` is left intact for non-forge callers. **(20) `consensus_inputs::protocol_params` (N-F-G-A S2a):** no float path; fail closed (`InexactRational`) on a non-exact rational literal. **(21) `consensus_inputs::canonical` (N-F-G-A S2a):** `protocol_params_json` stays OUTSIDE the 15-field canonical CBOR fingerprint; `require_forge_current_pparams` fails closed without the hash bind. |
| **Inbound deps** | `ade_node`, `ade_core_interop`, `ade_testkit` (dev/integration). |
| **Outbound deps** | `ade_types`, `ade_core`, `ade_crypto`, `ade_codec`, `ade_ledger`, `ade_network`, `redb`, `serde`, `serde_json`, `bech32`, `base58`, `cardano-crypto` (`["vrf-draft03", "dsign"]`), `ed25519-dalek`, `tokio`. (`consensus_inputs::protocol_params` adds `serde_json::value::RawValue` + `ade_ledger::{pparams, rational}`.) |
| **Entry points** | `ade_runtime::producer::{producer_shell::{ProducerShell (incl. init, public_metadata, cold_vk), ShellInitError}, keys::{load_cold_signing_key_skey, load_vrf_signing_key_skey, load_kes_signing_key_skey}, coordinator::{*, ForgedBlockArtifact, CoordinatorEvent, CoordinatorState (incl. kes_period_for_slot), GenesisAnchor, coordinator_init}, served_chain_handle::{ServedChainHandle, push_atomic}, genesis_parser::parse_shelley_genesis, opcert_envelope::parse_opcert_envelope, self_accepted_handoff::SelfAcceptedHandoff}`, `ade_runtime::admission::{dial_for_admission, run_admission_wire_pump, AdmissionPeerEvent}` (reused by N-F-G-C), `ade_runtime::network::{n2n_listener::* (incl. `N2nListenerConfig`/`PerPeerSessionConfig` with `our_supported: Arc<[(u16, VersionData)]>`), n2n_dialer::*, mux_pump::*, n2n_server::*, outbound_command::OutboundCommand, serve_dispatch::{dispatch_server_frame_event_to_outbound, install_server_peer_state, remove_server_peer_state, ServerPeerStates, DispatchError, ServedBlockEvidence}}`, `ade_runtime::orchestrator::*`, `ade_runtime::bootstrap::{bootstrap_initial_state, BootstrapInputs, BootstrapState, SeedEpochConsensusSource, BootstrapError}`, `ade_runtime::seed_consensus_merge::{merge_seed_epoch_consensus_inputs, SeedConsensusMergeError}`, `ade_runtime::consensus_inputs::{parse_protocol_parameters_json, ProtocolParamsParseError, LiveConsensusInputsCanonical (incl. require_forge_current_pparams), ForgeCurrentPParamsError}`, `ade_runtime::seed_consensus_provenance::append_seed_epoch_provenance`, `ade_runtime::forward_sync::{forward_sync_step, pump_block, SyncEffect, PumpError, SnapshotSink}`, `ade_runtime::mithril_import::{import_mithril_manifest, parse_mithril_manifest_json, MithrilProvenanceImport, MithrilManifestError}`, `ade_runtime::genesis_bootstrap::{bootstrap_from_conway_genesis, GenesisBootstrapError, GenesisBootstrapOutput}`, `ade_runtime::mithril_bootstrap::{bootstrap_from_mithril_snapshot, MithrilSeedPointInputs, MithrilBootstrapOutput, MithrilBootstrapError}`, `ade_runtime::recovery::restart::{recover_node_state, NodeRecoveryError}` (test-only secondary), `ade_runtime::chaindb::{ChainDb, SnapshotStore (incl. put_/get_seed_epoch_consensus_inputs, list_seed_epoch_consensus_anchor_fps), InMemoryChainDb}`, `ade_runtime::clock::{Clock, DeterministicClock, SystemClock, millis_to_slot, checked_millis_to_slot, SlotAlignmentError}`, `ade_runtime::{seed_import, wal, bootstrap_anchor}::*`. |
| **Key modules** | `producer/` (RED + GREEN-by-content; incl. `producer_shell.rs`, `keys.rs`, `genesis_parser.rs`, `opcert_envelope.rs` — the RED authorities reused by `ade_node::operator_forge` — and `self_accepted_handoff.rs` GREEN, N-F-G-B), `network/` (incl. the **N-F-G-H** `serve_dispatch.rs` shared serve-dispatch core + the `n2n_listener.rs` `Arc<[..]>`-typed `our_supported`), `orchestrator/` (RED runners + GREEN core), `admission/` (incl. `dial_for_admission` + `run_admission_wire_pump` reused by N-F-G-C), `receive/`, `rollback/`, `seed_import/`, `consensus_inputs/` (`mod`, `importer`, `json`, `canonical` — incl. the N-F-G-A `require_forge_current_pparams` accessor + `protocol_params_json` non-fingerprinted carry, `protocol_params` GREEN parser, `view`), `wal/`, `chaindb/` (`mod`, `persistent`, `in_memory`, `snapshot_contract` — redb schema v3), `forward_sync/` (`mod`, `reducer` GREEN, `pump` RED), `mithril_import/`, `seed_consensus_merge.rs` (GREEN, N-F-A), `seed_consensus_provenance.rs` (RED, N-F-A), `mithril_bootstrap.rs`, `genesis_bootstrap.rs`, `recovery/` (`mod`, `restart`), `bootstrap.rs` (GREEN composition + RED A3b restore), `bootstrap_anchor.rs`, `clock.rs` (the seam + `millis_to_slot` + the N-F-G-A `checked_millis_to_slot` / `SlotAlignmentError`). |

---
### `ade_node::operator_forge` *(RED — PHASE4-N-F-F S2/S3; real parsers PHASE4-N-F-G-A S2)*

| Attribute | Value |
|-----------|-------|
| **Purpose** | The single named `--mode node` operator-material ingress site (`//! RED`). It consumes a presence-validated `ForgePaths` (from the GREEN forge-intent classifier) and builds the RED key-custody holder by **reusing** the existing cold / VRF / KES loaders + the real cardano-cli opcert/genesis parsers — it reimplements **no** parser. `load_operator_producer_shell(&ForgePaths) -> Result<ProducerShell, OperatorForgeError>` loads cold/VRF/KES and runs `ProducerShell::init` (KES-period-vs-opcert freshness bound, CN-PROD-02). `build_operator_forge_material(&ForgePaths) -> Result<OperatorForgeMaterial, OperatorForgeError>` returns the shell + genesis anchors + the operator `pool_id` from `blake2b_224(cold_vk)` + clock-seam anchors + honest-scope `pparams`/`protocol_version` defaults. **N-F-G-A S2:** retires `parse_simple_opcert_json` / `parse_simple_genesis_json` on the node path and uses the REAL `ade_runtime::producer::opcert_envelope::parse_opcert_envelope` + `ade_runtime::producer::genesis_parser::parse_shelley_genesis` (the genesis reuse extracts clock/KES/network constants ONLY). Key custody is RED-confined. **Mithril boundary (load-bearing):** this is operator signing-material ingress, NOT bootstrap — it does NOT call Mithril, create a second bootstrap path, or re-derive initial state; `parse_shelley_genesis` is reused only to extract clock/KES anchors, NOT as a starting-state source. (G-B/G-C leave this module unchanged.) |
| **Creates (RED-only)** | Closed 6-variant `OperatorForgeError` (`ColdKeyLoad(KeyLoadError)` / `VrfKeyLoad(KeyLoadError)` / `KesKeyLoad(KeyLoadError)` / `OpcertParse(&'static str)` / `ShellInit(ShellInitError)` / `GenesisParse(&'static str)` — inner `KeyLoadError` carries no path bytes per OP-OPS-04); `OperatorForgeMaterial { shell: ProducerShell, genesis: GenesisAnchor, pool_id: Hash28, pparams, protocol_version, anchor_millis, start_slot, slot_length_ms }` — **deliberately NOT `Debug`/`Serialize`**. Never semantic/canonical types. |
| **Interprets** | Operator key/genesis/opcert files — but only by **delegating** to the reused `ade_runtime` loaders (`load_cold_signing_key_skey`, `load_vrf_signing_key_skey`), the `pub(crate)`-widened `produce_mode::load_kes_skey_any_format`, and the real `ade_runtime::producer::{opcert_envelope::parse_opcert_envelope, genesis_parser::parse_shelley_genesis}` parsers (N-F-G-A S2). It parses nothing itself. |
| **MUST NOT** | (1) Reimplement any key/opcert/genesis parser — it MUST reuse the existing `ade_runtime` cold/VRF/KES loaders + the real `parse_opcert_envelope` / `parse_shelley_genesis` parsers (CN-NODE-03 custody half; `ci_check_operator_forge_no_secret_leak.sh`); on the node path the `parse_simple_*` stubs are retired (`ci_check_node_forge_real_cli_ingress.sh`). (2) Introduce a new BLUE authority, a plugin/trait seam, a second forge codepath, or any BLUE crate change (CN-NODE-03). (3) Expose any private-key byte — no byte accessor, no serialization, no logging; `OperatorForgeMaterial` is NOT `Debug`/`Serialize`; `OperatorForgeError` carries no path/key bytes (OP-OPS-04). (4) Call Mithril, create a second bootstrap path, or re-derive initial state — `parse_shelley_genesis` is reused for clock/KES-anchor extraction ONLY (CN-NODE-01). (5) Fabricate `pool_id` — it is derived in this ONE named place from `blake2b_224(cold_vk)`. (6) Build a `CoordinatorState` here — that is kept caller-side (in `node_lifecycle`). |
| **Inbound deps** | `ade_node::node_lifecycle` (the `--mode node` `On` arm is the sole caller). |
| **Outbound deps** | `ade_node::forge_intent` (`ForgePaths`), `ade_node::produce_mode` (`load_kes_skey_any_format`), `ade_runtime::producer::{producer_shell::{ProducerShell, ShellInitError}, keys::{load_cold_signing_key_skey, load_vrf_signing_key_skey}, coordinator::GenesisAnchor, opcert_envelope::{parse_opcert_envelope, OpCertParseError}, genesis_parser::{parse_shelley_genesis, GenesisParseError}}` (and `KeyLoadError`), `ade_crypto::blake2b_224`, `ade_types::{Hash28, SlotNo, shelley::block::ProtocolVersion}`, `ade_ledger::pparams::ProtocolParameters`. |
| **Entry points** | `ade_node::operator_forge::{load_operator_producer_shell, build_operator_forge_material, OperatorForgeMaterial, OperatorForgeError}`. |

---

### `ade_node::ba02_pass` *(RED — PHASE4-N-F-G-C S2)*

| Attribute | Value |
|-----------|-------|
| **Purpose** | The operator-pass BA-02 evidence **file I/O** (`//! RED`). It reads the operator-captured peer-accept JSONL log from disk and runs it through the GREEN `ade_node::ba02_evidence` reducer (`parse_peer_accept_events` + `correlate` — the SOLE `Ba02Manifest` constructor), and writes a `correlate`-produced manifest. RED file I/O ONLY: it constructs no evidence, derives no acceptance, and never coerces a non-acceptance line. `correlate` stays the sole authority; a `Ba02Manifest` is a CLAIM ABOUT authority, not authority. **Honesty line (load-bearing):** Ade self-accept ≠ peer acceptance; a served block ≠ peer acceptance; wire success ≠ peer acceptance. The ONLY input that can yield a manifest is a real operator-captured peer log naming the EXACT forged hash, through `correlate`. A missing/unreadable peer-log file fails closed (`io::Error`), never a synthesized acceptance. Reached by no binary arm at this HEAD — exercised by `crates/ade_node/tests/node_operator_pass_ba02.rs` (incl. the env-gated `node_operator_pass_ba02_live`). |
| **Creates (RED-only)** | No types — two pure-glue file-I/O functions. Never semantic/canonical types, never a `Ba02Manifest` (it only *reads through* `correlate` and *writes* an already-`correlate`-produced manifest). |
| **Interprets** | An operator-captured peer-accept JSONL file: `correlate_peer_log_file` reads the file bytes → `ba02_evidence::parse_peer_accept_events` (allow-list) → `ba02_evidence::correlate`. It parses no block bytes and constructs no semantic type. |
| **MUST NOT** | (1) Construct a `Ba02Manifest` — `correlate`'s exact-match arm is the SOLE constructor; `write_ba02_manifest`'s argument type *is* the gate (only an already-`correlate`-produced `Ba02Manifest` is writable), so a written manifest is ALWAYS correlate-produced (RO-LIVE-06; `ci_check_ba02_evidence_manifest_schema.sh`). (2) Synthesize / coerce acceptance — a missing/unreadable peer-log file is an `io::Error` (fail-closed), never a `NoEvidence`-to-manifest upgrade, never a fabricated accept line. (3) Treat Ade self-accept / `ForgeSucceeded` / a served block / wire success as evidence. (4) Recompute the forged-block hash or re-validate the carried block (it delegates entirely to the GREEN correlator). (5) Commit a synthetic manifest with no real sha256-bound peer-log fixture (`ci_check_ba02_evidence_manifest_schema.sh` — vacuous-until-committed + 8-field schema + `peer_log_file_sha256` cross-check). |
| **Inbound deps** | None at HEAD — reached only by `crates/ade_node/tests/node_operator_pass_ba02.rs`. (Intended runtime caller: the operator-pass live leg under RO-LIVE-01, still deferred / `blocked_until_operator_stake_available`.) |
| **Outbound deps** | `ade_node::ba02_evidence::{correlate, parse_peer_accept_events, AdeForgeRecord, BA02Outcome, Ba02Manifest}`, `std::{fs, io, path::Path}`. |
| **Entry points** | `ade_node::ba02_pass::{correlate_peer_log_file, write_ba02_manifest}`. |

---

### `ade_node::rehearsal_pass` *(RED — PHASE4-N-F-G-D S2)*

| Attribute | Value |
|-----------|-------|
| **Purpose** | The private-testnet rehearsal-evidence **file I/O** (`//! RED`). It reads the operator-captured peer-accept JSONL log and runs it through the GREEN `ba02_evidence::correlate` (via `ba02_pass::correlate_peer_log_file` — the SOLE `Ba02Manifest` constructor, **reused verbatim**, no alternate correlator), then wraps a correlate-produced payload in a `rehearsal_evidence::PrivateRehearsalManifest`. RED file I/O ONLY: it constructs no evidence, synthesizes no acceptance, and uses no alternate correlator. **Honesty line (load-bearing):** Ade self-accept / a served block / wire success ≠ peer acceptance; the ONLY input that can yield a manifest is a real operator-captured peer log naming the EXACT forged hash, through `correlate`. A `NoEvidence` outcome yields `None` (writes nothing); a missing/unreadable peer-log file fails closed (`io::Error`) — never a synthesized manifest. Reached by no binary arm at this HEAD — exercised by `crates/ade_node/tests/node_c1_dry_run_rehearsal.rs` (incl. the env-gated `node_c1_dry_run_rehearsal_live`, `ADE_LIVE_C1_DRY_RUN`) + the in-module unit tests. |
| **Creates (RED-only)** | No types — two pure-glue file-I/O functions over the GREEN `PrivateRehearsalManifest`. Never semantic/canonical types; never a `Ba02Manifest` (it reads through `correlate`) and never a `PrivateRehearsalManifest` outside the GREEN `from_correlate_outcome` arm (it only *writes* an already-constructed one). |
| **Interprets** | An operator-captured peer-accept JSONL file: `correlate_peer_log_file_into_rehearsal` reads the file bytes → `ba02_pass::correlate_peer_log_file` (→ `ba02_evidence::parse_peer_accept_events` allow-list → `ba02_evidence::correlate`) → `PrivateRehearsalManifest::from_correlate_outcome`. It parses no block bytes and constructs no semantic type. |
| **MUST NOT** | (1) Use an alternate correlator — it MUST reuse `ba02_pass::correlate_peer_log_file` verbatim (CN-REHEARSAL-FIDELITY-01 clause 2). (2) Construct a `PrivateRehearsalManifest` — `from_correlate_outcome`'s `Ba02Manifest` arm is the SOLE constructor; `write_private_rehearsal_manifest`'s argument type *is* the gate (only an already-constructed `PrivateRehearsalManifest` is writable), so a written rehearsal manifest is ALWAYS correlate-produced. (3) Synthesize / coerce acceptance — a missing/unreadable peer-log file is an `io::Error` (fail-closed), never a `NoEvidence`-to-manifest upgrade, never a fabricated accept line; `NoEvidence` writes nothing. (4) Treat Ade self-accept / `ForgeSucceeded` / a served block / wire success as evidence. (5) Write outside the rehearsal home or with a non-rehearsal label, or commit a synthetic manifest with no real sha256-bound peer-log fixture (`ci_check_rehearsal_manifest_schema.sh` — vacuous-until-committed + 12-field schema + `peer_log_file_sha256` cross-check + the no-bounty-home-leak cross-check). (6) Flip any RO-LIVE rule (private C1 acceptance ≠ bounty completion). |
| **Inbound deps** | None at HEAD — reached only by `crates/ade_node/tests/node_c1_dry_run_rehearsal.rs` + the in-module unit tests. (Intended runtime caller: the operator-gated C1 dry-run under `blocked_until_operator_c1_net_executed`.) |
| **Outbound deps** | `ade_node::ba02_evidence::AdeForgeRecord`, `ade_node::ba02_pass::correlate_peer_log_file`, `ade_node::rehearsal_evidence::{PrivateRehearsalManifest, RehearsalEnvelope}`, `std::{fs, io, path::Path}`. |
| **Entry points** | `ade_node::rehearsal_pass::{correlate_peer_log_file_into_rehearsal, write_private_rehearsal_manifest}`. |

---

### `ade_node`

| Attribute | Value |
|-----------|-------|
| **Purpose** | The node binary + library entry. Owns argv parsing, the node lifecycle, and the mode drivers dispatched by `main()`: `--mode wire_only` (default), `--mode admission`, `--mode key_gen_kes`, `--mode produce`, and (N-F-C) `--mode node` (`node_lifecycle`). `produce_mode::run_real_forge` carries an era guard and (N-F-G-B S1) returns `(CoordinatorEvent, Option<AcceptedBlock>)` — surfacing the original BLUE self-accepted token on the success path. `admission::runner` strips the served block's tag-24 envelope via `ade_codec::unwrap_tag24`. **N-F-C/D/E/F/G-A/G-B/G-C scope (the `--mode node` live surface):** `node_lifecycle` is the single `PHASE4-N-F-C-LIFECYCLE-OWNER` — it opens a `PersistentChainDb` + `FileWalStore`, classifies first-run-vs-warm-start, then (FirstRun) runs `bootstrap_from_mithril_snapshot` durably or (WarmStart) recovers the verified `BootstrapState`. **N-F-F** classifies forge intent on that single recovered state and flips `None`/`Some(ForgeActivation)`. **N-F-G-A** sources the `On`-arm forge constants from the recovered current ledger view (S2), ingests operator config through real parsers (S2), installs current pparams (S2a), derives the `ForgeTick` slot via `checked_millis_to_slot` (S3), and fails an off-epoch forge closed via `forge_epoch_admission` before leadership/KES (S4). **N-F-G-B** wires the self-accept→serve handoff (`run_real_forge` surfaces the BLUE token; `node_sync::forge_one_from_recovered` wraps it into a typed `SelfAcceptedHandoff`; `run_node_lifecycle_inner`'s `On` arm spawns a sibling served-chain admit task → the single `push_atomic`; the relay loop forwards a typed channel send only). **N-F-G-C** then wires the **live WirePump feed**: when `--peer` is supplied, the `On` arm builds a LIVE `NodeBlockSource::WirePump` via `spawn_live_wire_pump_source` (reusing `ade_runtime::admission::{dial_for_admission, run_admission_wire_pump}` + the `pub(crate)`-promoted `admission::bootstrap::build_n2n_version_table`); else it keeps the prior empty `in_memory` source. With a live feed the forge is observable when the feed is Continuing and a due leader slot is reached; **peer ACCEPT is NOT claimed** (operator-gated, RO-LIVE-01 partial). **N-F-G-C** also adds the RED `ba02_pass` operator-pass BA-02 evidence file-I/O wrapper (`correlate_peer_log_file` + `write_ba02_manifest`). **N-F-G-E** then bounds peer-driven memory on that live feed BEFORE authoritative decode/apply: `node_sync::pump_lookahead` caps the content-blind WirePump lookahead at `MAX_WIRE_PUMP_LOOKAHEAD = 256` (back-pressuring the bounded mpsc), and the GREEN session reducer it drains through caps each reassembly tail at 16 MiB (fail-closed `SessionError::ReassemblyBufferOverflow`). No new module, no new `NodeBlockSource` variant, no BLUE change. **N-F-G-D** then adds the private-testnet accepted-block bounty DRY-RUN **harness** on this same spine — NOT a runtime node mode and **not wired into any binary arm**: the path-fidelity fence (`ci_check_node_path_fidelity.sh` — no new `--mode node` flag vs the pinned 28-flag allow-list, no from-genesis consensus-inputs constructor), the NEW GREEN `rehearsal_evidence::PrivateRehearsalManifest` non-promotable envelope, the NEW RED `rehearsal_pass` file I/O (which reuses `ba02_pass::correlate_peer_log_file` verbatim), the rehearsal-schema gate (`ci_check_rehearsal_manifest_schema.sh`), and a dry-run runbook that is a provable strict subset of the G-C preprod operator-pass runbook. The live C1 execution stays `blocked_until_operator_c1_net_executed` (exercised only by the env-gated RED test `node_c1_dry_run_rehearsal_live`); **no RO-LIVE flip, no bounty/preview/preprod completion claim, no BLUE change**. **N-F-G-H** then wires `--mode node` to **serve real peers READ-ONLY** from the G-B self-accepted `ServedChainView`: the `On` arm calls `bind_serve_listener(--listen)` (fail-fast via the closed `ServeStartError`) and `tokio::spawn`s `run_node_serve_task` **OUTSIDE `run_relay_loop`** (mirroring the G-B sibling push task), driving the shared `ade_runtime::network::serve_dispatch::dispatch_server_frame_event_to_outbound` over the retained view (advertising via `n2n_supported_for_magic(network_magic)`). `produce_mode` is re-pointed at the same shared serve core (S1). It serves the already-self-accepted chain (ChainSync header advertisement + BlockFetch body), admits nothing, advances no durable tip, and makes **no peer-acceptance claim** (`RO-LIVE-01` partial); it reuses the EXISTING `--listen` flag (no new `--mode node` flag). The live C1 serve is exercised only by the env-gated RED test `node_c1_serve_live` (`ADE_LIVE_C1_SERVE=1`). **`run_relay_loop`'s containment is byte-/semantically unchanged** by every G-* delta — the new boundaries (G-A) sit inside the existing fence, the G-B handoff is a typed channel send, and the G-C change is purely which `NodeBlockSource` is handed in. With the empty source (no `--peer`) the loop halts before any `ForgeTick` (forge-CAPABLE, NOT observable — RO-LIVE-01). The L6 BA-02 correlator (`ba02_evidence`) is a **library surface reached by no binary arm** (now consumed by the RED `ba02_pass` I/O wrapper + tests). **N-F-G-J** then adds genesis-successor cold-start forge on this spine: the `ForgeTick` arm derives `(block_number, prev_hash)` via the GREEN `node_sync::forge_header_position(selected_tip.as_ref())` (the ONE cold-start convention — `None => block 0 + PrevHash::Genesis`, `Some => last_block_no+1 + Block`, a tip-without-height edge fails closed as `NodeForgeError::RecoveredTipMissingBlockNo`), and when `ChainDb::tip()` AND the recovered tip are BOTH `None` the GREEN `node_lifecycle::may_cold_start_forge` permits forging block 0 + `PrevHash::Genesis` through the SAME `run_real_forge -> self_accept -> SelfAcceptedHandoff -> ServedChainView` path (DC-NODE-08), gated by the recovered seed-epoch lineage + `ForgeIntent::On` + a forge-eligible feed; the forge engine holds NO `ChainDb` handle and advances NO durable tip (N-U). `run_relay_loop_with_sched` additionally emits the closed GREEN `NodeSchedEvent` scheduling diagnostics to stderr (emit-only, `CN-NODE-04`; never alters scheduling). The `admission::runner` now maps `BlockValidityError::HeaderPositionInvalid` to `InvalidAdmitReason::Header` (CN-WIRE-09 position reject on the admit path). |
| **Creates (RED-only)** | `Cli` (incl. `peer_addrs: Vec<String>` + `network_magic: Option<u32>` consumed by the N-F-G-C live feed), `CliError`, `ProduceCli`, `KeyGenKesCli`, `Mode` (closed: `WireOnly`/`Admission`/`KeyGenKes`/`Produce`/`Node`), `NodeStartupInputs`, `NodeShutdownEvidence`, `NodeRunError`, exit-code constants; the `produce_mode` slot loop types; the admission runner types. **N-F-C (`node_lifecycle`, RED):** `NodeStart` (closed: `FirstRun`/`WarmStart`), `NodeLifecycleError` (closed; incl. `RelaySync` N-F-D, `ForgeKeyIngress(String)` N-F-F, and `MissingFlag(&'static str)` for the N-F-G-C `--network-magic` requirement when `--peer` is supplied), exit-code constants (incl. `EXIT_NODE_FORGE_KEY_INGRESS_FAILED = 44`). **N-F-D/E (`node_lifecycle`, RED):** `run_relay_loop`; `ForgeActivation<'a>` (recovered-state forge inputs + clock seam + the per-iteration derived slot + the in-memory `hermetic_forge_outcomes` test field; **N-F-G-A** adds `protocol_version` + `last_slot_alignment_fail`; **N-F-G-B** adds a private `handoff_tx: Option<mpsc::UnboundedSender<SelfAcceptedHandoff>>` + the `with_handoff_sender` builder). **N-F-G-C (`node_lifecycle`, RED):** `spawn_live_wire_pump_source(peer_addrs, network_magic, recovered_tip) -> NodeBlockSource` + `const LIVE_WIRE_PUMP_CHANNEL_CAP = 64` (the bounded live-feed channel capacity). **N-F-F (`forge_intent` GREEN / `operator_forge` RED — entries above).** **N-F-G-C (`admission::bootstrap`, RED):** `build_n2n_version_table` promoted `fn` → `pub(crate) fn` (reused, no reimpl). **N-F-C/D (`node_sync`, RED):** `NodeBlockSource` (closed 2-variant: `WirePump`/`InMemory`; the N-F-G-C live feed fills the `WirePump` arm via `from_wire_pump` — no new variant), `NodeSyncError`, `NodeForgeError` (**N-F-G-B:** `forge_one_from_recovered` returns `(CoordinatorEvent, Option<SelfAcceptedHandoff>)`); **N-F-G-A (`node_sync`, GREEN-by-function):** closed `ForgeEpochAdmission { WithinSeedEpoch, OffEpoch }` + `forge_epoch_admission`. **N-F-G-E (`node_sync`, RED):** `const MAX_WIRE_PUMP_LOOKAHEAD = 256` — `pump_lookahead` stops the opportunistic `try_recv` drain at the cap so the bounded `LIVE_WIRE_PUMP_CHANNEL_CAP = 64` mpsc back-pressures the pump (no new type, no new variant). **N-F-G-H (`node_lifecycle`, RED):** `run_node_serve_task` (the node-spine serve sibling — request-driven serve of the G-B `ServedChainView`, `tokio::spawn`ed OUTSIDE `run_relay_loop`) + `bind_serve_listener(listen_addr) -> Result<TcpListener, ServeStartError>` (binds ONCE) + the closed 2-variant `ServeStartError { InvalidAddr(String), Bind(std::io::ErrorKind) }`. No new `--mode node` flag (reuses `--listen`); no new canonical type. **N-F-G-C (`ba02_pass`, RED — entry above).** **N-F-G-J (`node_sync`, GREEN-by-function):** `forge_header_position(selected_tip: Option<&ChainTip>) -> Result<(u64, PrevHash), NodeForgeError>` + the additive `NodeForgeError::RecoveredTipMissingBlockNo` (a recovered tip without a recorded block height fails closed; `forge_one_from_recovered` takes `Option<&ChainTip>`). **N-F-G-J (`node_lifecycle`, GREEN-by-function):** `may_cold_start_forge(selected_tip_present, has_recovered_lineage, feed_eligible) -> bool` (the cold-start permission gate) + `run_relay_loop_with_sched(.., sched: Option<&mut dyn NodeSchedSink>)` (the emit-only sched-instrumented relay loop; `run_relay_loop` delegates with `sched = None`). **N-F-G-J (`live_log`, GREEN — entry above):** the closed `NodeSchedEvent` vocabulary + `NodeSchedLogWriter` + `NodeSchedSink`. **N-F-D/E (`run_loop_planner`, GREEN — entry above).** **N-F-C (`ba02_evidence`, GREEN).** Never semantic/canonical types. |
| **Interprets** | argv (closed mode set); operator-supplied key/genesis/opcert file paths (delegated to `ade_runtime` parsers + `ade_node::operator_forge`); evidence-manifest TOML schema; the admission runner interprets a peer's tag-24-wrapped BlockFetch `MsgBlock` payload via `ade_codec::unwrap_tag24`. **N-F-G-A:** the `ForgeTick` arm interprets the clock seam's wall-clock tick into a `SlotNo` via `checked_millis_to_slot` (before-anchor fails closed). **N-F-G-C:** the `On` arm interprets `--peer` addrs (parsed to `SocketAddr`, unparseable ones logged-and-dropped) into a live `NodeBlockSource::WirePump` via the reused admission dial + pump (the runtime `AdmissionPeerEvent` feeds the `WirePump` arm directly); the `ba02_pass` wrapper interprets an operator-captured peer-accept JSONL file through the GREEN `correlate`. **N-F-G-J:** `admission::runner` maps a BLUE `BlockValidityError::HeaderPositionInvalid` (the position-illegal `prev_hash`) into `InvalidAdmitReason::Header` on the admit path. |
| **MUST NOT** | (1) Construct semantic types bypassing the canonical decoders / `ade_runtime` parsers. (2) `produce_mode` (CN-PROD-04, CN-OUTBOUND-RELAY-01) — obtain initial state only via `bootstrap_initial_state`; re-validate each broadcast block through BLUE `self_accept`; emit outbound bytes only via `OutboundCommand`. (3) No synthetic forge state (N-T). (4) No durability in the produce_mode path (N-U scope). (5) `run_real_forge` (CN-FORGE-04) — non-Praos era fail-closes. (6) `admission::runner` (CN-WIRE-08) — strip via `ade_codec::unwrap_tag24`. (7) `wire_only` — overstate semantic truth. (8) operator-evidence manifest carries the closed schema (CN-OPERATOR-EVIDENCE-01). (9) closed mode set with no wildcard dispatch (`ci_check_node_mode_closure.sh`). (10) `produce_mode` recovered-surface fence (CN-CINPUT-02) — MUST pass only `SeedEpochConsensusSource::NotRequired`. **(11) `node_lifecycle` lifecycle owner (CN-NODE-01):** exactly one `PHASE4-N-F-C-LIFECYCLE-OWNER`; initial state SOLELY via `bootstrap_initial_state`; no parallel/cold init, no fallback; the N-F-F/G-A/G-B/G-C forge-on arm reuses the SINGLE recovered `state` (NO second bootstrap, NO Mithril call — the N-F-G-C live feed advances NO durable tip and triggers NO bootstrap) (`ci_check_lifecycle_owner_uses_bootstrap_initial_state.sh`, `ci_check_node_binary_uses_single_bootstrap.sh`). **(12) `run_relay_loop` / `node_sync` sync driver (CN-NODE-02 / DC-SYNC-02):** advance the durable tip ONLY via `run_node_sync → pump_block`; `run_node_sync` UNMODIFIED; **`run_relay_loop`'s containment stays byte-/semantically unchanged by N-F-G-A/B/C** (`ci_check_node_run_loop_containment.sh`, `ci_check_node_sync_via_pump.sh`). **(13) `run_relay_loop` forge tick (CN-NODE-02 / CE-E-4 / DC-NODE-05):** the forge is reached ONLY through exactly one fenced `forge_one_from_recovered(` (self-accept-only); MUST NOT call `run_real_forge` directly, correlate evidence, or serve/broadcast/gossip a forged block in the loop body; advances no durable tip; **N-F-G-B** forwards each surfaced `SelfAcceptedHandoff` as a best-effort typed `tx.send(h)` ONLY (the loop body holds no `ServedChainHandle` and calls no `push_atomic`) (`ci_check_node_run_loop_containment.sh`). **(14) `run_loop_planner` (DC-NODE-05):** `plan_loop_step` content-blind. **(15) `node_sync::forge_one_from_recovered` consume-side fence (CN-CINPUT-03 / DC-CINPUT-02b):** derive the leadership `PoolDistrView` ONLY via `from_seed_epoch_consensus_inputs(`; fabricate no `SeedEpochConsensusInputs { … }` literal. **(16) `node_sync::forge_one_from_recovered` epoch fence (DC-EPOCH-03, N-F-G-A S4):** call `forge_epoch_admission` BEFORE `query_leader_schedule`; an off-epoch / unlocatable slot fails closed before leadership / KES signing; the guard derives the epoch via the BLUE `EraSchedule::locate`; the forge path drives NO nonce promotion (`ci_check_node_forge_single_epoch_fail_closed.sh`). **(17) recovered-ledger pparams (CE-G-A-2a, N-F-G-A S2a):** `admission::{seed_to_snapshot, bootstrap}` install the oracle-bound current `ProtocolParameters` (via `require_forge_current_pparams`), never `ProtocolParameters::default()` / genesis-initial (`ci_check_recovered_ledger_pparams_sourced.sh`). **(18) `ba02_evidence` + `ba02_pass` (RO-LIVE-06):** single `Ba02Manifest` constructor in `correlate`; `ba02_pass::write_ba02_manifest` accepts only an already-`correlate`-produced `Ba02Manifest`; treat no weaker signal (self-accept / served-block / wire success) as acceptance; a missing peer-log file fails closed; commit no synthetic manifest (`ci_check_ba02_evidence_closed.sh`, `ci_check_ba02_evidence_manifest_schema.sh`). **(19) forge-intent + operator-material ingress (CN-NODE-03 / OP-OPS-04):** `forge_intent` content-blind; `operator_forge` reuses the loaders (incl. real `parse_opcert_envelope` / `parse_shelley_genesis`, no reimpl), exposes no private-key byte; the `--mode node` arm builds the activation through this single ingress, never a second forge codepath (`ci_check_forge_intent_closed.sh`, `ci_check_operator_forge_no_secret_leak.sh`, `ci_check_node_forge_real_cli_ingress.sh`). **(20) self-accept→serve handoff (DC-NODE-06, N-F-G-B; serve-ingress broadened N-F-G-C):** only a BLUE self-accepted artifact enters the sibling serve task — carried as a typed `SelfAcceptedHandoff` (`Some` iff `ForgeSucceeded`; the ORIGINAL `self_accept` token, never re-derived from `artifact.bytes`); the node-spine served chain is mutated ONLY via the single `ServedChainHandle::push_atomic` fed by `into_accepted()`, in a sibling task — never `served_chain_admit(` directly, never a `push_atomic` in the relay-loop body; every node-spine unbounded handoff channel is typed `UnboundedSender<SelfAcceptedHandoff>` (allow-list; never `<Vec<u8>>` / `<ForgedBlockArtifact>` / `<bool>`); owners `{node_lifecycle.rs, node_sync.rs}`; the relay-loop containment gate stays byte-unchanged (`ci_check_served_chain_handoff_fence.sh`, `ci_check_node_run_loop_containment.sh`). **(21) live WirePump feed (N-F-G-C S1):** the `--mode node` live feed MUST reuse the closed `ade_runtime::admission::{dial_for_admission, run_admission_wire_pump}` verbatim (no reimplemented dial/handshake/pump, no parallel wire authority); it fills the existing `NodeBlockSource::WirePump` arm (NO new `NodeBlockSource` variant); it advances NO durable tip and triggers NO bootstrap; dial/parse failures are logged-and-dropped (C3 honest-scope), never fatal and never a fabricated address or silent tip graft; and it makes **no peer-acceptance claim** (RO-LIVE-01 stays partial / operator-gated). **(22) live-feed lookahead bound (DC-LIVEMEM-01, N-F-G-E S1):** `node_sync::pump_lookahead` MUST stop opportunistic draining at `MAX_WIRE_PUMP_LOOKAHEAD` (a closed literal const) so the existing bounded mpsc back-pressures the pump — no unbounded `VecDeque` growth from a fast / hostile peer, never a silent drop (queued bytes stay in the bounded channel and drain once below the cap); `MAX_WIRE_PUMP_LOOKAHEAD` MUST NOT be wired to CLI / env / config (`ci_check_live_feed_memory_bounds.sh` guards 2 + 3); the cap is content-blind (RED scheduling only) and leaves the verdict-decoupled `NodeBlockSource` contract + the peer's arrival order unchanged. **(23) accepted-block path fidelity + non-promotable rehearsal evidence (CN-REHEARSAL-FIDELITY-01, N-F-G-D):** the `--mode node` accepted-block path MUST add NO private-only argv flag (the `cli.rs` flag set equals the pinned 28-flag closed allow-list) and NO from-genesis consensus-inputs constructor — the C1 private dry-run uses the SAME path as preview/preprod, sourcing consensus inputs via the shared `import_live_consensus_inputs`; a condition that would fail on preprod is a shared-path bug, never special-cased (`ci_check_node_path_fidelity.sh`, clause 1). A private-testnet rehearsal manifest MUST be correlate-produced (the GREEN `rehearsal_evidence::PrivateRehearsalManifest` sole ctor `from_correlate_outcome` returns `None` on `NoEvidence`; `rehearsal_pass::write_private_rehearsal_manifest` accepts only that type), marked rehearsal (`is_rehearsal`/`not_bounty_evidence` literals), stored ONLY under `docs/evidence/phase4-n-f-g-d-private-rehearsal-*.toml`, sha256-bound to a real Haskell peer log, never under / referenced by a bounty home, and flip NO RO-LIVE rule (`ci_check_rehearsal_manifest_schema.sh`, clause 2 — vacuous-until-committed). **(24) node-spine serve-to-peer (DC-NODE-07, N-F-G-H):** `--mode node` serves real peers ONLY from the G-B self-accepted `ServedChainView` (read side of the single `ServedChainHandle`, DC-NODE-06), via the sibling `run_node_serve_task` spawned OUTSIDE `run_relay_loop`, through the EXISTING BLUE `producer_chain_sync_serve` + `producer_block_fetch_serve` + the SINGLE shared `dispatch_server_frame_event_to_outbound`; it MUST mutate no served chain (no `push_atomic` / `served_chain_admit` in the serve task), advance no durable tip, add no proactive `advance_tip`/`changed()` reactor, define no second serve-dispatch (`ci_check_single_serve_dispatch_authority.sh`), use no parallel tag-24 serializer (CN-WIRE-08), and add no new `--mode node` argv flag (it reuses `--listen`; the S1 path-fidelity fence `ci_check_node_path_fidelity.sh` stays green); the live serve listener advertises via `n2n_supported_for_magic(<configured magic>)`, never the static `N2N_SUPPORTED` (`ci_check_serve_listener_magic_aware.sh`); `run_relay_loop`'s containment gate `ci_check_node_run_loop_containment.sh` stays byte-/semantically unchanged (the serve task is outside it); serving makes NO peer-acceptance claim (acceptance is proven ONLY by the peer's validation log through `ba02_evidence::correlate`, RO-LIVE-06; `RO-LIVE-01`/`RO-LIVE-06` do NOT flip). **(25) genesis-successor cold-start forge + PrevHash position rule (DC-NODE-08 / CN-WIRE-09, N-F-G-J):** the `ForgeTick` arm MUST derive `(block_number, prev_hash)` via the SINGLE `node_sync::forge_header_position(Option<&ChainTip>)` — ONE cold-start convention (`None => block 0 + PrevHash::Genesis`; `Some => last_block_no+1 + Block(tip.hash)`); a tip without a recorded height MUST fail closed (`NodeForgeError::RecoveredTipMissingBlockNo`), NEVER a magic `.unwrap_or(1)` default. The both-`None` genesis forge is permitted ONLY when `may_cold_start_forge` holds (recovered seed-epoch lineage present + `ForgeIntent::On` + a forge-eligible feed under the CN-NODE-04 closed split), routes through the SAME `run_real_forge -> self_accept -> SelfAcceptedHandoff` path, advances NO durable tip (no `ChainDb` handle), fires within the hermetic cold-start (no `genesis_forged` latch), and flips NO RO-LIVE rule; the recovered lineage gates PERMISSION, never the prev_hash bytes (`ci_check_genesis_successor_reachability.sh`). The `NodeSchedEvent` sched sink is best-effort emit-only and MUST NOT alter scheduling (`ci_check_node_sched_events_emit_only.sh`, CN-NODE-04). | 
| **Inbound deps** | None (binary + integration tests). |
| **Outbound deps** | `ade_types`, `ade_core`, `ade_crypto`, `ade_ledger`, `ade_runtime` (incl. `admission::{dial_for_admission, run_admission_wire_pump, AdmissionPeerEvent}` reused by the N-F-G-C live feed), `ade_network` (incl. `codec::chain_sync::Point`), `ade_codec`, `serde`, `serde_json`, `tokio`. Dev-deps: `ade_testkit`, `tempfile`. |
| **Entry points** | `main()`; `ade_node::run_node_lifecycle` (`--mode node`); `ade_node::node_lifecycle::{run_relay_loop, ForgeActivation (incl. with_handoff_sender), spawn_live_wire_pump_source, run_node_serve_task, bind_serve_listener, ServeStartError}` (the last three NEW, N-F-G-H); `ade_runtime::producer::self_accepted_handoff::SelfAcceptedHandoff` (the typed handoff carrier, consumed here); `ade_node::ba02_pass::{correlate_peer_log_file, write_ba02_manifest}` (RED — operator-pass evidence I/O); `ade_node::forge_intent::{classify_forge_intent, ForgeIntent, ForgePaths}` (GREEN); `ade_node::operator_forge::{build_operator_forge_material, load_operator_producer_shell, OperatorForgeMaterial}` (RED); `ade_node::run_loop_planner::{plan_loop_step, forge_slot_status, LoopStep, ForgeSlotStatus}` (GREEN); `ade_node::node_sync::{NodeBlockSource (incl. from_wire_pump, in_memory), run_node_sync, forge_one_from_recovered, forge_epoch_admission, ForgeEpochAdmission, forge_header_position, NodeForgeError}` (library; the last two NEW, N-F-G-J); `ade_node::node_lifecycle::{run_relay_loop_with_sched, may_cold_start_forge}` + `ade_node::live_log::{NodeSchedEvent, NodeSchedLogWriter, NodeSchedSink}` (NEW, N-F-G-J); `ade_node::run_node_until_shutdown`; `ade_node::produce_mode::{run_real_forge, run_produce_mode, *}`; `ade_node::cli::{Cli, ProduceCli, Mode}`; `ade_node::admission::*` (incl. the `pub(crate)` `bootstrap::build_n2n_version_table`); `ade_node::wire_only::*`; `ade_node::key_gen::*`; `ade_node::ba02_evidence::{parse_peer_accept_events, correlate, Ba02Manifest}` (GREEN library). |
| **Key modules** | `lib.rs` (incl. `pub mod ba02_pass;` N-F-G-C, `pub mod rehearsal_evidence;` + `pub mod rehearsal_pass;` N-F-G-D), `cli.rs`, `node.rs`, `main.rs`, `node_lifecycle.rs` (the `--mode node` owner + `run_relay_loop` + `ForgeActivation` + the N-F-F forge-intent flip + the N-F-G-A S2/S3 wiring + the N-F-G-B sibling served-chain admit task + the **N-F-G-C** `spawn_live_wire_pump_source` live-feed wire + `On`-arm `--peer` source selection + the **N-F-G-H** `run_node_serve_task` / `bind_serve_listener` / closed `ServeStartError` node-spine serve sibling spawned OUTSIDE `run_relay_loop` on `--listen` + the **N-F-G-J** GREEN `may_cold_start_forge` cold-start permission gate + `run_relay_loop_with_sched` emit-only sched-instrumented loop + the `ForgeTick` cold-start arm), `forge_intent.rs` (**GREEN**), `operator_forge.rs` (**RED** — real-parser ingress, N-F-G-A S2), `run_loop_planner.rs` (**GREEN**), `node_sync.rs` (the sync driver + recovered-forge handoff — **N-F-G-B** `forge_one_from_recovered` returns `(CoordinatorEvent, Option<SelfAcceptedHandoff>)` — + the N-F-G-A S4 `forge_epoch_admission` guard + the **N-F-G-E** `MAX_WIRE_PUMP_LOOKAHEAD = 256` `pump_lookahead` cap; the **N-F-G-J** GREEN-by-function `forge_header_position(Option<&ChainTip>)` cold-start convention + `NodeForgeError::RecoveredTipMissingBlockNo` + `forge_one_from_recovered(Option<&ChainTip>)`; the closed 2-variant `NodeBlockSource` lives here), `ba02_evidence.rs` (**GREEN**, library; now consumed by `ba02_pass`), `live_log/` (incl. **N-F-G-J** `sched_event.rs` + `sched_writer.rs` — the **GREEN** closed emit-only `NodeSchedEvent` vocabulary + byte-deterministic JSONL writer), `ba02_pass.rs` (**RED**, N-F-G-C — operator-pass evidence file I/O), `rehearsal_evidence.rs` (**GREEN**, N-F-G-D — the non-promotable `PrivateRehearsalManifest` envelope), `rehearsal_pass.rs` (**RED**, N-F-G-D — rehearsal-evidence file I/O reusing `ba02_pass::correlate_peer_log_file`), `produce_mode.rs` (incl. the `pub(crate)` `load_kes_skey_any_format` reused by `operator_forge`), `wire_only.rs`, `key_gen.rs`, `admission/` (`bootstrap` — the N-F-G-C `pub(crate)` `build_n2n_version_table`, `runner`, `seed_to_snapshot` — the N-F-G-A S2a current-pparams install, `verdict`), `admission_log/`, `live_log/`. |

> **GREEN sub-classification:** `ade_node::forge_intent`, `ade_node::run_loop_planner`, and `ade_node::ba02_evidence` are GREEN-by-content; the `node_sync::forge_epoch_admission` *function* is GREEN-by-function (full entries / rows above). `node_lifecycle` + `node_sync` + `operator_forge` + **`ba02_pass` (N-F-G-C)** + **`rehearsal_pass` (N-F-G-D)** are RED; the **N-F-G-D** `rehearsal_evidence` module is **GREEN-by-content** (the pure `PrivateRehearsalManifest` envelope — full entry in the GREEN section). The **N-F-G-E** `node_sync::pump_lookahead` cap is RED content-blind scheduling (it bounds a buffer depth; it inspects no block content).

---

### `ade_core_interop`

| Attribute | Value |
|-----------|-------|
| **Purpose** | Live cardano-node interop driver. Hosts the `live_*_session` RED binaries (operator-action evidence harness) plus the N-E S4/S5 GREEN tx-submission bridges, and the chain-follow driver (`follow`). `follow` strips the peer's ChainSync `RollForward` `[era_tag, tag24(header_cbor)]` envelope via `ade_codec::unwrap_tag24`. (G-C leaves this crate unchanged.) |
| **Creates (RED-only)** | Live-session drivers and transcript types; never semantic types. |
| **MUST NOT** | (1) Construct semantic types from raw bytes. (2) Be depended on by any BLUE/GREEN crate (RED leaf). (3) Overstate semantic truth in evidence. (4) `follow` (CN-WIRE-08) — strip via `ade_codec::unwrap_tag24`. |
| **Inbound deps** | None (RED leaf — binaries). |
| **Outbound deps** | `ade_core`, `ade_codec`, `ade_crypto`, `ade_ledger`, `ade_runtime`, `ade_network`, `ade_testkit`, `ade_types`, `tokio`. |
| **Entry points** | `ade_core_interop::bin::{live_tx_submission_session, live_block_production_session, live_block_fetch_session, live_block_follow_session}`; `ade_core_interop::follow::*`; `ade_core_interop::tx_submission`, `ade_core_interop::local_tx_submission` (GREEN sub-class). |
| **Key modules** | `bin/`, `follow.rs`, `tx_submission.rs`, `local_tx_submission.rs`. |

---

### `ade_network::mux::transport` *(RED)*

| Attribute | Value |
|-----------|-------|
| **Purpose** | The only place inside `ade_network` where socket I/O happens. RED tokio shell over the BLUE mux frame primitive and the GREEN session reducer. Provides `MuxTransportHandle` + closed `TransportError` + `DuplexCapacity::DEFAULT` + `spawn_duplex`. |
| **Creates (RED-only)** | `MuxTransportHandle`, `TransportError`, `DuplexCapacity`, `MuxTransport`. |
| **MUST NOT** | (1) Construct semantic types. (2) Bypass `mux::frame` for framing. (3) Live in BLUE scope. |
| **Inbound deps** | `ade_runtime::network::{mux_pump, n2n_dialer}`. |
| **Outbound deps** | `ade_network::mux::frame` (BLUE), `ade_network::session` (GREEN), `tokio`. |
| **Entry points** | `ade_network::mux::transport::{MuxTransportHandle, spawn_duplex, open_tcp}`. |
| **Key modules** | `mux/transport.rs`. |

---

### `ade_network` *(RED capture binaries — non-session)*

| Attribute | Value |
|-----------|-------|
| **Purpose** | Operator-action capture binaries (live evidence harness) inside `ade_network`. The `ade_chain_sync_capture` binary supports `--intersect-slot` / `--intersect-hash` flags (N-X) to capture a real Conway `RollForward` golden fixture. |
| **MUST NOT** | (1) Construct semantic types from raw bytes. (2) Overstate semantic truth in captured evidence. |
| **Outbound deps** | `ade_network` BLUE/GREEN submodules, `ade_codec`, `ade_types`, `tokio`. |

---
## Cross-Module Rules (project-wide)

### Dependency direction (RED → GREEN → BLUE; never outward)

```
ade_types     → {}
ade_codec     → {ade_types}
ade_crypto    → {ade_types, blake2, ed25519-dalek, cardano-crypto[vrf-draft03,kes-sum(test-only),dsign]}
ade_core      → {ade_types, ade_crypto, minicbor}
ade_ledger    → {ade_core, ade_plutus, ade_crypto, ade_codec, ade_types, minicbor, num-*}
ade_plutus    → {ade_crypto, ade_codec, ade_types, aiken_uplc, pallas-primitives(internal)}
ade_network   → BLUE submodules → {ade_codec, ade_types}; session(GREEN, incl. the N-F-G-E MAX_REASSEMBLY_TAIL_BYTES cap + SessionError::ReassemblyBufferOverflow) → +BLUE submodules; transport(RED) → +tokio
ade_testkit   → {ade_core, ade_ledger, ade_plutus, ade_runtime, ade_crypto, ade_codec, ade_types, cardano-crypto}   [GREEN]
ade_runtime   → {ade_core, ade_crypto, ade_codec, ade_types, ade_ledger, ade_network, redb, serde*, bech32, base58, cardano-crypto[vrf-draft03,dsign], ed25519-dalek, tokio}   [RED; network::serve_dispatch (N-F-G-H) is the single shared serve-dispatch core (RED adapter; the ServedBlockEvidence struct inside it is GREEN-by-content); n2n_listener our_supported is now Arc<[(u16,VersionData)]>]
ade_node      → {ade_types, ade_core, ade_crypto, ade_ledger, ade_runtime, ade_network, ade_codec, serde, serde_json, tokio}   [RED; forge_intent + run_loop_planner + ba02_evidence + node_sync::forge_epoch_admission are GREEN-by-content/function; operator_forge + node_lifecycle (incl. the N-F-G-H run_node_serve_task / bind_serve_listener / ServeStartError serve sibling + the N-F-G-J may_cold_start_forge / run_relay_loop_with_sched cold-start path) + node_sync (incl. the N-F-G-E MAX_WIRE_PUMP_LOOKAHEAD cap + the N-F-G-J forge_header_position cold-start convention) + ba02_pass + rehearsal_pass (N-F-G-D) are RED; rehearsal_evidence (N-F-G-D) + live_log::{sched_event,sched_writer} (N-F-G-J, closed emit-only NodeSchedEvent) are GREEN-by-content; node_sync::forge_header_position + node_lifecycle::may_cold_start_forge (N-F-G-J) are GREEN-by-function]
ade_core_interop → {ade_core, ade_codec, ade_crypto, ade_ledger, ade_runtime, ade_network, ade_testkit, ade_types, tokio}   [RED leaf]
```

**Forbidden directions.** Any BLUE crate (or BLUE `ade_network` submodule) depending on `ade_runtime`, `ade_node`, `ade_core_interop`, or the RED half of `ade_network` is a CI failure (`ci_check_dependency_boundary.sh`). Any non-`ade_plutus` crate referring to `pallas_*` is a CI failure (`ci_check_pallas_quarantine.sh`). Any `cardano_crypto::kes` import outside `#[cfg(test)]` under `crates/ade_crypto/src/**` is a CI failure (`ci_check_kes_sum_compatibility.sh` Guard 3). A second block-envelope encoder or a `produce_mode` direct-transport write is a CI failure. A second `leader_vrf_input` authority, or a path accepting both TPraos and Praos VRF inputs for one era, is a CI failure (`ci_check_producer_praos_vrf.sh`). A second `wrap_tag24`/`unwrap_tag24` definition or a hand-rolled tag-24 parse in RED is a CI failure (`ci_check_tag24_wire_authority.sh`). A forward-sync `AdvanceTip` reachable before the block's durability writes (DC-SYNC-01), a Mithril/genesis bootstrap path bypassing the single `bootstrap_initial_state` authority (`ci_check_mithril_uses_bootstrap_initial_state.sh`), and an internal-ledger-fingerprint-vs-Haskell-hash equality assertion in the compatibility harness (DC-COMPAT-01) are CI failures. A registry rule citing a non-existent `crates/**.rs` / `ci/**.sh` `code_locus` is a CI failure (`ci_check_registry_code_locus_exists.sh`). A Mithril-bootstrap path that sources the anchor `seed_point` from the manifest (DC-MITHRIL-02) is a CI failure. A forge-time path that populates the seed-epoch consensus-input sidecar or appends its WAL provenance is a CI failure (CN-CINPUT-02). **N-F-C:** a second `PHASE4-N-F-C-LIFECYCLE-OWNER`, a parallel/cold storage-init, a genesis/bundle/cold/graft fallback, a `recover_node_state` overclaim, or a `RequiredFromRecoveredProvenance` construction outside {owner, `bootstrap.rs`} is a CI failure (CN-NODE-01); treating a weaker signal as BA-02 peer acceptance, or constructing a `Ba02Manifest` outside `correlate`'s exact-match arm, is a CI failure (RO-LIVE-06). **N-F-D:** a direct `pump_block`/manual tip advance/forge/evidence/verdict/follower/second-bootstrap reference in `run_relay_loop`, or a `LoopStep` outside the closed set, is a CI failure (CN-NODE-02 / DC-SYNC-02). **N-F-E:** `plan_loop_step` referencing a `SlotNo`, or `run_relay_loop` reaching the forge by any path other than exactly one fenced `forge_one_from_recovered`, is a CI failure (DC-NODE-05 / CN-NODE-02 / CE-E-4). **N-F-F:** a `forge_intent` classifier that opens a file / parses a key / observes key contents, a third "partial" `ForgeIntent` variant, a `ForgeIntentError`/`OperatorForgeError` carrying a path/key byte, an `OperatorForgeMaterial` with a `Debug`/`Serialize` derive, a reimplemented parser in `operator_forge`, a second forge codepath / second bootstrap / Mithril call on the forge-on arm, or a `ReceiveState::new` outside the owner allow-list `{node.rs, node_lifecycle.rs}` is a CI failure (CN-NODE-03 / CN-NODE-01 / OP-OPS-04). **N-F-G-A: forge fidelity is real-config, current-pparams, and fail-closed at the two boundaries** — a `parse_simple_*` parser reintroduced on the node forge path (`ci_check_node_forge_real_cli_ingress.sh`), a recovered ledger carrying `ProtocolParameters::default()` / genesis-initial pparams instead of the oracle preimage (`ci_check_recovered_ledger_pparams_sourced.sh`), a `forge_epoch_admission` guard reordered *after* `query_leader_schedule` / a fabricated candidate epoch / a nonce promotion on the forge path (`ci_check_node_forge_single_epoch_fail_closed.sh`), a committed S1 reference fixture missing a file / not Ade-as-leader / leaking secret key material (`ci_check_genesis_consistency_fixture_present.sh`), a `protocol_params` float path, a `checked_millis_to_slot` that saturates a before-anchor tick, or a `protocol_params_json` preimage folded INTO the 15-field canonical fingerprint is a CI failure (DC-EPOCH-03 / CE-G-A-1/2/2a/4). **N-F-G-B: only self-accepted artifacts serve, via the single `push_atomic`, without relaxing containment** — a node-spine `push_atomic(` not fed by `into_accepted()`, a direct `served_chain_admit(` on the node spine, or a handoff channel typed anything but `UnboundedSender<SelfAcceptedHandoff>` is a CI failure (`ci_check_served_chain_handoff_fence.sh`, DC-NODE-06); a `SelfAcceptedHandoff` constructed from anything but a BLUE `AcceptedBlock` is type-impossible (private field, sole `from_self_accepted` ctor, CN-FORGE-01); the relay-loop containment gate `ci_check_node_run_loop_containment.sh` stays byte-/semantically unchanged. **NEW (N-F-G-C): live feed reuses the closed admission dial/pump, fills the existing source arm, and claims no acceptance** — the `ci_check_served_chain_handoff_fence.sh` fence is **broadened in place** to owners `{node_lifecycle.rs, node_sync.rs}` with guard (3) now an ALLOW-LIST (every node-spine unbounded handoff channel must carry `SelfAcceptedHandoff`); a committed `docs/clusters/PHASE4-N-F-G-C/CE-G-C-LIVE_*.toml` BA-02 manifest that is missing a schema field, sets `schema_version != 1`, or whose `peer_log_file_sha256` does not match the committed peer-log fixture is a CI failure (`ci_check_ba02_evidence_manifest_schema.sh`, RO-LIVE-06) — and with no committed manifest the gate is vacuously satisfied (no synthetic-manifest claim). A live `--mode node` feed that reimplements the N2N dial/handshake/pump instead of reusing `ade_runtime::admission::{dial_for_admission, run_admission_wire_pump}`, that adds a new `NodeBlockSource` variant, that advances a durable tip or triggers a bootstrap from the feed, or that claims peer acceptance is out of bounds (RO-LIVE-01 stays partial / operator-gated). `ci_check_node_run_loop_containment.sh` is byte-unchanged by N-F-G-C. **NEW (N-F-G-E): peer-driven live-feed memory is bounded before authoritative decode/apply, via closed constants with no runtime escape hatch** — a `MAX_REASSEMBLY_TAIL_BYTES` or `MAX_WIRE_PUMP_LOOKAHEAD` bound that is removed, that is not a closed literal const, or that is wired to CLI / env / config (a runtime override path) is a CI failure (`ci_check_live_feed_memory_bounds.sh`, DC-LIVEMEM-01); an incomplete `ade_network::session` reassembly tail past the 16 MiB cap MUST fail closed (`SessionError::ReassemblyBufferOverflow`, drop the peer) rather than grow unbounded or partial-decode, and the `node_sync` WirePump lookahead MUST stop at 256 so the bounded mpsc back-pressures the pump. The cap fires BEFORE the BLUE `ade_codec` decode path; `ci_check_node_run_loop_containment.sh` and `ci_check_served_chain_handoff_fence.sh` are both **byte-unchanged** by N-F-G-E; no `RO-LIVE` flip / no BA-02 claim. The claim is NARROW — bounded memory before decode/apply, NOT network DoS resistance, NOT peer fairness (per-connection-COUNT limits are a separate out-of-scope surface). **NEW (N-F-G-D): the C1 private dry-run is a path-faithful, non-promotable bounty rehearsal HARNESS** — a new `--mode node` argv flag (the `cli.rs` flag set must equal the pinned 28-flag closed allow-list) or a from-genesis consensus-inputs constructor (a fn naming both `genesis` and `consensus`) is a CI failure (`ci_check_node_path_fidelity.sh`, CN-REHEARSAL-FIDELITY-01 clause 1); a committed `docs/evidence/phase4-n-f-g-d-private-rehearsal-*.toml` rehearsal manifest that is missing a schema field, sets `schema_version != 1`, omits the `is_rehearsal`/`not_bounty_evidence` markers, uses a non-private `venue`, or whose `peer_log_file_sha256` does not match the committed peer-log fixture is a CI failure — and a rehearsal marker (`^is_rehearsal` / `^not_bounty_evidence`) found in ANY `.toml` under EITHER bounty home (active `docs/clusters/PHASE4-N-F-G-C/` or archived `docs/clusters/completed/PHASE4-N-F-G-C/`) is a CI failure (`ci_check_rehearsal_manifest_schema.sh`, clause 2 — vacuous-until-committed, fail-closed on a scan error over an existing home). With no committed manifest the rehearsal gate is vacuously satisfied (no bounty/preview/preprod completion claim; the live C1 execution stays `blocked_until_operator_c1_net_executed`; no RO-LIVE flip). The three containment / handoff / memory fences (`ci_check_node_run_loop_containment.sh`, `ci_check_served_chain_handoff_fence.sh`, `ci_check_live_feed_memory_bounds.sh`) are all byte-unchanged by N-F-G-D. **NEW (N-F-G-H): the serve-dispatch authority is unique and the node spine serves the already-self-accepted view READ-ONLY, outside the relay loop, with no new flag and no new serializer** — a second `fn dispatch_server_frame_event_to_outbound` anywhere (or one outside `crates/ade_runtime/`, or a parallel serve-dispatch in `node_lifecycle.rs`, or `produce_mode.rs` holding a private copy) is a CI failure (`ci_check_single_serve_dispatch_authority.sh`, DC-NODE-07); a live serve listener (`node_lifecycle.rs` / `produce_mode.rs`) that sets `our_supported` to the static `N2N_SUPPORTED` instead of building it via `n2n_supported_for_magic` is a CI failure (`ci_check_serve_listener_magic_aware.sh`, DC-NODE-07); a `serve` / `push_atomic` / served-chain mutation inside `run_relay_loop`, a proactive `advance_tip`/`changed()` serve reactor, a second `ServedChain` authority, a parallel tag-24 serializer, or a new `--mode node` argv flag for the serve is out of bounds (the containment + path-fidelity fences `ci_check_node_run_loop_containment.sh` + `ci_check_node_path_fidelity.sh` stay byte-/semantically unchanged, CN-NODE-02 / CN-REHEARSAL-FIDELITY-01). Serving makes no peer-acceptance claim (acceptance only via `ba02_evidence::correlate`, RO-LIVE-06); `RO-LIVE-01` stays `partial` and the live C1 serve stays `blocked_until_operator_c1_serve_executed`. The lone BLUE touch is the additive fn `n2n_supported_for_magic` — no version-set widening, no new canonical type. **NEW (N-F-G-J): the header `prev_hash` is the closed `$hash32 / null` wire grammar with a POSITION-BLIND codec + a SINGLE position-aware authority, and the node spine may cold-start-forge the genesis-successor block from the recovered base** — a second position-rule authority, or a `block_number 0 <=> Genesis` check living in `ade_codec` (the byte codec must stay POSITION-BLIND), is a CI failure (`ci_check_prevhash_single_wire_authority.sh`, CN-WIRE-09); a producer-path `prev_hash: Hash32` field or a `ChainEvolution` cold-start emitting an all-zero `Hash32` stand-in instead of `PrevHash::Genesis` is a CI failure (same gate, guards (c)/(d)). A `ForgeTick` arm deriving the cold-start `(block_number, prev_hash)` by any path other than the single `node_sync::forge_header_position(Option<&ChainTip>)`, a `.unwrap_or(1)` block-height default, a both-`None` genesis forge not gated by `may_cold_start_forge` (recovered seed-epoch lineage + ForgeIntent::On + a forge-eligible feed), a cold-start forge that takes a `ChainDb` handle / advances a durable tip / relies on a `genesis_forged` latch, or a forge from a raw/unanchored/from-genesis-file base is out of bounds (`ci_check_genesis_successor_reachability.sh`, DC-NODE-08; RO-LIVE-01 stays partial / operator-gated). A `NodeSchedEvent` sched sink that alters the relay loop's scheduling / control flow, or that the planner reads, is a CI failure (`ci_check_node_sched_events_emit_only.sh`, CN-NODE-04).

### Naming convention

All crates are prefixed `ade_`. TCB color is not encoded in the crate name. The authoritative classifier is `.idd-config.json` `core_paths` + the per-file `// Core Contract:` / `//! BLUE|GREEN|RED` banner + the cluster TCB Color Maps; CI scripts hard-code their BLUE list.

### Closed enums / registries (for SEAMS cross-reference)

Closed semantic surfaces detected at HEAD: `AcceptedMiniProtocol` (`ade_network::session`), `TagEnvelopeError` (`ade_codec::cbor::tag24`), `ExpectedVrfInput` (2-variant, `ade_core::consensus::vrf_cert`), `LeaderCheckVerdict` (2-variant, `ade_core::consensus::leader_check`), `OutboundCommand` + `DispatchError` (`ade_runtime::network`), `ProducerLogEvent` + `ForgeFailureReason`, `ChainEvolutionError`, `ServedChainAdmitError`, `KesError` / `KesParseError`, the admission/wire-only/live-log JSONL vocabularies, the operator-evidence manifest TOML schema, the closed `CardanoEra` / Conway cert + governance enums. **N-Y:** `SeedProvenance` (version-gated behind `ANCHOR_SCHEMA_VERSION = 2`), `MithrilImportError`, `GenesisSourceError`, `SyncEffect` (4-variant), `MithrilManifestError`, `PumpError`, `NodeRecoveryError`, the `sync_diff` observable `BlockVerdict` + `RegressionFixtureViolation`. **N-Z:** `MithrilBootstrapError`. **N-F-A:** `SeedEpochConsensusInputs` (version-gated behind `SEED_CINPUT_SCHEMA_VERSION = 1`, SOLE codec), `SeedConsensusInputsError` (6-variant), `SeedConsensusMergeError` (2-variant), `SeedEpochConsensusSource` (2-variant), and the additive **WAL** variant `WalEntry::SeedEpochConsensusInputsImported` (tag 3). **N-F-C:** `Mode` (5-variant, no wildcard dispatch), `NodeStart` (2-variant), `NodeLifecycleError` / `NodeSyncError` / `NodeForgeError`, `NodeBlockSource` (2-variant), and the BA-02 evidence vocabulary (`BA02Outcome` / `PeerAcceptEvent` / `NoEvidenceReason` / versioned `Ba02Manifest`). **N-F-D/E:** `LoopStep` (4-variant incl. `ForgeTick`) + content-blind `ForgeSlotStatus` (2-variant). **N-F-F:** `ForgeIntent` (2-variant: `On(ForgePaths)` / `Off`), `ForgeIntentError` (1-variant), `OperatorForgeError` (6-variant). **N-F-G-A:** `SlotAlignmentError` (1-variant: `BeforeGenesisAnchor`, `ade_runtime::clock`), `ProtocolParamsParseError` (incl. `InexactRational`, `ade_runtime::consensus_inputs::protocol_params`), `ForgeCurrentPParamsError` (`PreimageAbsent`/`BindMismatch`/parse, `ade_runtime::consensus_inputs::canonical`), `ForgeEpochAdmission` (2-variant: `WithinSeedEpoch` / `OffEpoch`, `ade_node::node_sync`). **N-F-G-B:** `SelfAcceptedHandoff` (`ade_runtime::producer::self_accepted_handoff`) — a constructor-fenced newtype over the BLUE `AcceptedBlock` (private field; sole ctor `from_self_accepted`; no raw-bytes / artifact / event / flag / verdict ctor), the GREEN fence backing `DC-NODE-06`; `run_real_forge` / `forge_one_from_recovered` widen to a `(CoordinatorEvent, Option<…>)` return with **no new `CoordinatorEvent` variant**. **N-F-G-C:** the closed 2-variant `NodeBlockSource { WirePump, InMemory }` is **unchanged** — the live operator feed is a *fill* of the existing `WirePump` arm (via `from_wire_pump`), **not** a new variant; `Mode` (5-variant), `NodeLifecycleError` (the `MissingFlag` arm covers the `--network-magic`-required-with-`--peer` case — already part of the closed enum), and the BA-02 evidence vocabulary stay closed; the NEW RED `ba02_pass` adds **no** type (two file-I/O fns over the existing closed `Ba02Manifest` / `BA02Outcome`). **N-F-G-E:** the closed `SessionError` (`ade_network::session::event`) gains the **additive** variant `ReassemblyBufferOverflow { protocol, len, cap }` (no wildcard; one exhaustive consumer `mux_pump::session_err_to_halt`); `NodeBlockSource` stays the 2-variant `{WirePump, InMemory}` (unchanged); the two memory bounds (`MAX_REASSEMBLY_TAIL_BYTES = 16 MiB`, `MAX_WIRE_PUMP_LOOKAHEAD = 256`) are closed literal constants with no runtime override path. **N-F-G-D:** NEW closed 1-variant `RehearsalVenue { PrivateTestnetC1 }` (`ade_node::rehearsal_evidence`) — a non-private rehearsal venue is unrepresentable; `PrivateRehearsalManifest` is a constructor-fenced wrapper over the existing closed `Ba02Manifest` (sole ctor `from_correlate_outcome`, `None` on `NoEvidence`; `to_canonical_toml` always emits the `is_rehearsal`/`not_bounty_evidence` literals); `NodeBlockSource` / `CoordinatorEvent` / `Mode` (5-variant) / the BA-02 evidence vocabulary all stay **unchanged** (G-D adds no new `--mode node` flag and no new variant; `rehearsal_pass` adds two file-I/O fns over the existing closed types). **N-F-G-J:** the NEW closed BLUE sum `PrevHash { Genesis, Block(Hash32) }` (`ade_types::shelley::block`) — the header `prev_hash` field's closed wire grammar `$hash32 / null`, replacing the prior flat `Hash32` (the lone new canonical type); the additive `BlockValidityError::HeaderPositionInvalid` variant (`ade_ledger::block_validity::verdict`) folds into the existing closed `BlockRejectClass::HeaderInvalid` (NO new `BlockRejectClass`); the NEW closed GREEN `NodeSchedEvent` feed/forge scheduling-event vocabulary + `ForgeOutcome` / `FeedReason` (`ade_node::live_log::sched_event`, emit-only, `CN-NODE-04`); the additive `NodeForgeError::RecoveredTipMissingBlockNo` (`ade_node::node_sync`); the closed `ForgeEpochAdmission` / `NodeBlockSource` / `Mode` (5-variant) all stay **unchanged** (G-J adds no `--mode node` flag and no new `NodeBlockSource` variant — the cold-start is a new code path through the existing closed source, never a new variant). **N-F-G-H:** the moved closed 4-variant `DispatchError` (`ade_runtime::network::serve_dispatch`: `UnknownPeer`/`PeerOutboundMissing`/`SendFailure`/`ReducerError`) + `ServerPeerStates` (type alias) + the GREEN-by-content `ServedBlockEvidence` relocate from `produce_mode` **unchanged**; the closed 2-variant `ServeStartError { InvalidAddr(String), Bind(io::ErrorKind) }` is NEW (`ade_node::node_lifecycle`); `n2n_supported_for_magic` is an additive BLUE **fn** over the unchanged closed `N2N_SUPPORTED` version SET (no new type, no version widening); `Mode` (5-variant) / `NodeBlockSource` (2-variant) / `CoordinatorEvent` all stay **unchanged** (G-H adds no `--mode node` flag and no new variant — the serve reuses `--listen`). **Governance: `WalEntry` deliberately stays a CE-not-law surface (tags append-only); `LoopStep` is an additively-evolvable closed planner vocabulary; `ForgeIntent` / `ForgeEpochAdmission` / `NodeBlockSource` are closed classifier/source vocabularies (a partial key set / an off-epoch slot / a live feed are an error or a fill, never a new variant). The `protocol_params_json` preimage stays OUTSIDE the frozen 15-field `LiveConsensusInputsCanonical` fingerprint (the fingerprint commits to its hash). The redb `chaindb` `SCHEMA_VERSION` (v3) is a versioned-gate. The N-F-G-E live-feed memory bounds are deliberately CLOSED CONSTANTS (defensive implementation bounds, NOT Cardano semantic parameters) — `DC-LIVEMEM-01` is `tier = derived` (operational-hardening, explicitly NOT BLUE consensus law); a future hardening slice may *tighten* a bound, but no runtime option (CLI / env / config) may disable it or set it unbounded. The N-F-G-D rehearsal-evidence surface is deliberately a HARNESS-only contract: `CN-REHEARSAL-FIDELITY-01` (`tier = release`) enforces path fidelity + evidence non-promotability for the C1 dry-run, NOT that a C1 run has succeeded — the rehearsal gate is vacuous-until-committed and flips no RO-LIVE rule (private C1 acceptance ≠ bounty completion; preview/preprod acceptance is the single bounty deliverable, captured separately). The N-F-G-H serve surface is deliberately a READ-ONLY view-serve: `DC-NODE-07` (`tier = derived`, NOT BLUE consensus law) enforces single-serve-dispatch-authority + serve-from-the-self-accepted-view-outside-the-relay-loop + magic-aware advertisement, NOT peer acceptance — `RO-LIVE-01`/`RO-LIVE-06` do not flip (serving ≠ acceptance ≠ BA-02). The N-F-G-J `PrevHash` surface is the closed Cardano wire grammar `$hash32 / null` (`CN-WIRE-09`, `tier = derived`) with a STRICT byte-codec / position-rule split: the byte codec is POSITION-BLIND, the single position-aware authority is `check_header_position` (called by `decode_block`, never in `ade_codec`, never duplicated). The N-F-G-J node-spine cold-start forge allowance `DC-NODE-08` (`tier = derived`, NOT BLUE consensus law) is a forge PERMISSION gate (recovered lineage + ForgeIntent::On + forge-eligible feed), NOT a durable-tip advance and NOT an RO-LIVE flip — the recovered lineage gates permission, never the structurally-null prev_hash bytes; the `NodeSchedEvent` vocabulary is an emit-only diagnostic (`CN-NODE-04`), never an authority.**

### CI enforcement (126 scripts under `ci/`)

The full list is mechanically obtainable via `ls ci/ci_check_*.sh` (126 at HEAD `550eec3a`). CI lives entirely under `ci/` (`ci_dirs = ["ci"]`); there is no `.github/workflows/` invariant gate at HEAD. New / changed / load-bearing since the prior CODEMAP HEAD (PHASE4-N-F-G-H close, 123 scripts):

| Script | Enforces | Cluster / commit |
|---|---|---|
| `ci_check_prevhash_single_wire_authority.sh` | **NEW (N-F-G-J S2, CN-WIRE-09)** — the single PrevHash wire authority. Guards: (a) `PrevHash::Genesis` round-trips as CBOR null / `Block` as hash32 through ONE shared BLUE `ade_codec` authority; (b) the codec is POSITION-BLIND (`decode_prev_hash` takes no `block_number`); (c) the SINGLE position-rule authority (`check_header_position`) is referenced by `decode_block` and NEVER appears in `ade_codec`; (d) no `prev_hash: Hash32` field survives on the producer path AND `ChainEvolution` cold-start proposes `PrevHash::Genesis` (no all-zero stand-in). | N-F-G-J S2 |
| `ci_check_genesis_successor_reachability.sh` | **NEW (N-F-G-J S4, DC-NODE-08)** — node-spine genesis-successor cold-start first-block reachability. Enforces: the `forge_one_from_recovered` `Option<&ChainTip>` signature; the cold-start `PrevHash::Genesis` emission at block 0; the ONE convention (no `.unwrap_or(1)` block-height default); the lineage + eligibility gating (`may_cold_start_forge`); and the no-`ChainDb`-handle structural no-durable-tip (the forge engine advances no durable tip). RO-LIVE-01 stays partial / operator-gated. | N-F-G-J S4 |
| `ci_check_node_sched_events_emit_only.sh` | **NEW (N-F-G-J S1, CN-NODE-04)** — the closed `NodeSchedEvent` feed/forge scheduling-event vocabulary is **emit-only**: the sched sink (`Option<&mut dyn NodeSchedSink>` in `run_relay_loop_with_sched`) is best-effort and MUST NOT alter the relay loop's scheduling / control flow, and the planner MUST NOT read a sched event; the per-variant JSONL writer match is exhaustive (a new variant is a compile error, no `_`). | N-F-G-J S1 |
| `ci_check_single_serve_dispatch_authority.sh` | **NEW (N-F-G-H S1, DC-NODE-07)** — single serve-dispatch authority. Guard (a): exactly one `fn dispatch_server_frame_event_to_outbound` in the workspace, and it is under `crates/ade_runtime/`. Guard (b): `crates/ade_node/src/node_lifecycle.rs` defines NO parallel serve-dispatch of its own. Guard (c): `crates/ade_node/src/produce_mode.rs` references the shared core (it was re-pointed, not left holding a private copy). Fail-closed-smoke-verified against an injected second `fn dispatch_server_frame_event_to_outbound` in `node_lifecycle.rs`. Makes "no second serve authority" durable, not point-in-time. | N-F-G-H `8b8b8139` |
| `ci_check_serve_listener_magic_aware.sh` | **NEW (N-F-G-H S2b, DC-NODE-07)** — the live serve listeners (node-spine + producer) MUST advertise N2N versions using the CONFIGURED network magic, built via `ade_network::handshake::version_table::n2n_supported_for_magic(<magic>)`, NOT the static mainnet `N2N_SUPPORTED`. For each live serve site (`produce_mode.rs`, `node_lifecycle.rs`): guard (a) no `our_supported:` set to the static `N2N_SUPPORTED`; guard (b) the site builds its serve version table via `n2n_supported_for_magic`. Scope: only the live serve binaries (the dialer's `N2N_SUPPORTED` use to ENUMERATE version numbers + test listeners are out of scope). Otherwise a real preprod (magic 1) / C1 (magic 42) follower's handshake is refused on a magic mismatch. | N-F-G-H `a8ca5e52` |
| `ci_check_node_path_fidelity.sh` | **NEW (N-F-G-D S1, CN-REHEARSAL-FIDELITY-01 clause 1)** — `--mode node` accepted-block PATH FIDELITY. Guard (a): the `crates/ade_node/src/cli.rs` argv flag-literal set equals the pinned closed **28-flag** allow-list (G-D adds none; a private-only / venue flag — `--private-net` / `--from-genesis` / `--devnet` / `--rehearsal` — trips it). Guard (b): no fn naming BOTH `genesis` and `consensus` (a from-genesis consensus-inputs constructor; line comments stripped first so prose cannot self-trip) AND `node_lifecycle.rs` sources consensus inputs via the shared `import_live_consensus_inputs`. Fail-closed-smoke-verified against an injected `--private-net` flag + a `build_consensus_inputs_from_genesis` ctor. | N-F-G-D `d4d0f456` |
| `ci_check_rehearsal_manifest_schema.sh` | **NEW (N-F-G-D S2; hardened S4, CN-REHEARSAL-FIDELITY-01 clause 2)** — when a committed `docs/evidence/phase4-n-f-g-d-private-rehearsal-*.toml` manifest is present, verifies the closed **12-field** rehearsal schema, `schema_version == 1`, `is_rehearsal = true` + `not_bounty_evidence = true`, a `private-testnet*` `venue`, and that `peer_log_file_sha256` matches the committed peer-log fixture. Vacuous-until-committed (the C1 dry-run is `blocked_until_operator_c1_net_executed`). THREE non-promotability barriers: distinct `docs/evidence/` home; the rehearsal markers; and (S4) a cross-check that NO rehearsal marker appears in any `.toml` under EITHER bounty home (active + archived `docs/clusters/{,completed/}PHASE4-N-F-G-C/`) — built from the EXISTING-homes list first (no `[[ -d ]]` whole-check skip), fail-closed on a scan error (grep rc≥2). The no-synthetic / no-promotion enforcer. | N-F-G-D `459cf78d`; hardened `6bd60c80` |
| `ci_check_live_feed_memory_bounds.sh` | **NEW (N-F-G-E S1, DC-LIVEMEM-01)** — the no-escape-hatch guard for the two live-feed memory bounds. Guard (1): `MAX_REASSEMBLY_TAIL_BYTES` is the closed `16 * 1024 * 1024` literal const in `ade_network/src/session/core.rs` (GREEN reassembly-tail cap). Guard (2): `MAX_WIRE_PUMP_LOOKAHEAD` is the closed `256` literal const in `ade_node/src/node_sync.rs` (RED lookahead-depth cap). Guard (3): no line referencing either bound also reads CLI / env / config (`env::` / `std::env` / `::var(` / `from_env` / `getenv` / `[Cc]li` / `clap` / `from_config` / `config.`) — line comments are stripped first so the doc-comments naming "CLI / env / config" do not self-trip. The bounds are defensive implementation constants, tightenable but never disableable at runtime. | N-F-G-E `6f848825` |
| `ci_check_ba02_evidence_manifest_schema.sh` (carried) | **N-F-G-C S2 (RO-LIVE-06 BA-02 evidence; mirrors `ci_check_operator_evidence_manifest_schema.sh`)** — when a committed `docs/clusters/PHASE4-N-F-G-C/CE-G-C-LIVE_*.toml` manifest is present, it verifies the closed 8-field schema (`schema_version`, `block_hash`, `slot`, `peer_log_file`, `peer_log_file_sha256`, `peer_log_capture_command`, `peer_log_filter`, `accept_event_kind`), `schema_version == 1`, and that `peer_log_file_sha256` matches the actual SHA-256 of the committed peer-log fixture. With NO manifest committed (the typical state — live ACCEPT is `blocked_until_operator_stake_available`) the gate is **vacuously satisfied**. The no-synthetic-manifest enforcer. **In-span change (not G-H proper): `18085231` broadened this gate to ALSO scan the ARCHIVED G-C home (`docs/clusters/completed/PHASE4-N-F-G-C/`), with the regression test `crates/ade_node/tests/ba02_gate_archived_home.rs` (counted in the +3 above).** | N-F-G-C `90791691`; archived-home scan `18085231` |
| `ci_check_served_chain_handoff_fence.sh` (BROADENED in place) | DC-NODE-06 serve-ingress clause — **N-F-G-C S1** broadened the owner set `{node_lifecycle.rs}` → `{node_lifecycle.rs, node_sync.rs}` and flipped guard (3) from a 3-name **deny-list** to an **ALLOW-LIST** (every node-spine unbounded handoff channel must carry `SelfAcceptedHandoff`; ≥1 `UnboundedSender<SelfAcceptedHandoff>` present). Guards (1) every `push_atomic(` fed by `into_accepted()` and (2) no direct `served_chain_admit(` on the node spine are unchanged. NOT a new file — no CI count change for this script. | N-F-G-B `738dddd1`; broadened N-F-G-C `71036d10`; byte-unchanged N-F-G-E |
| `ci_check_node_run_loop_containment.sh` (carried, BYTE-UNCHANGED) | CN-NODE-02 / CE-E-4 / DC-NODE-06 — `run_relay_loop`'s tip-advance / forge / serve fence. **Byte-unchanged by N-F-G-C and N-F-G-E** (the live feed only changes which `NodeBlockSource` is handed in; the G-E memory caps sit inside `session::core` + `node_sync::pump_lookahead`; the loop body is untouched). | N-F-D; extended N-F-E (carried, unchanged) |
| `ci_check_node_sync_via_pump.sh` (carried) | DC-SYNC-02 / CN-NODE-02 — the durable tip advances ONLY via `run_node_sync → pump_block`; `run_node_sync` UNMODIFIED. The N-F-G-C live feed advances no durable tip (the `WirePump` source still drains through the same pump path). | N-F-C; carried (unchanged) |
| `ci_check_ba02_evidence_closed.sh` (carried) | RO-LIVE-06 — `correlate` is the SOLE `Ba02Manifest` ctor; no weaker signal is acceptance; no synthetic manifest under `docs/evidence/*ba02*`. The N-F-G-C RED `ba02_pass` writer only *writes* a `correlate`-produced manifest (it constructs none). | N-F-C; carried (unchanged) |
| `ci_check_genesis_consistency_fixture_present.sh` | N-F-G-A S1, CE-G-A-1 — committed Ade-as-leader fixture, no leaked secret. (Carried.) | N-F-G-A `b5decb3e`/`23addfbb` |
| `ci_check_recovered_ledger_pparams_sourced.sh` | N-F-G-A S2a, CE-G-A-2a — recovered-ledger pparams sourced from the oracle preimage, never defaulted. (Carried.) | N-F-G-A `3dba81db` |
| `ci_check_node_forge_real_cli_ingress.sh` | N-F-G-A S2, CE-G-A-2 — real `parse_opcert_envelope` + `parse_shelley_genesis` on the node path; `parse_simple_*` retired. (Carried.) | N-F-G-A `11704998` |
| `ci_check_node_forge_single_epoch_fail_closed.sh` | N-F-G-A S4, CE-G-A-4 / DC-EPOCH-03 — off-epoch fail-closed before leadership, no nonce promotion. (Carried.) | N-F-G-A `80dac1f7` |
| `ci_check_loop_planner_closed.sh` | DC-NODE-05 — the GREEN planner's closed `LoopStep`/`ForgeSlotStatus` + the `SlotNo` ban scoped to `plan_loop_step`. (Unchanged.) | N-F-D; extended N-F-E (carried) |
| `ci_check_forge_intent_closed.sh` | CN-NODE-03 (intent half). (Carried.) | N-F-F (carried) |
| `ci_check_operator_forge_no_secret_leak.sh` | CN-NODE-03 (custody half). (Carried.) | N-F-F (carried) |
| `ci_check_node_binary_uses_single_bootstrap.sh` | CN-NODE-01 — `ReceiveState::new` owner allow-list `{node.rs, node_lifecycle.rs}`. (Carried; the N-F-G-C live feed triggers no second bootstrap.) | N-F-F (carried) |
| `ci_check_consensus_input_provenance.sh` | CN-CINPUT-02 (populate-side + forge-time fence) + guard (d) the CONSUME-side fence (CN-CINPUT-03 / DC-CINPUT-02b). (Carried.) | N-F-A; guard (d) N-F-C (carried) |

> Earlier-cluster scripts (N-A through N-Z, plus N-F-A/N-F-C/N-F-D/N-F-E/N-F-F/N-F-G-A/N-F-G-B/N-F-G-C/N-F-G-D/N-F-G-E/N-F-G-H) are present and counted in the 126 total. The per-script enforce/scope detail is in the registry's `ci_script` fields per rule.

---


## Generation notes

- Regenerated (**fast incremental delta-refresh** at the PHASE4-N-F-G-J cluster close) at HEAD `550eec3a` (`git rev-parse --short HEAD`; branch `forge/node-empty-feed-tick-g-j`). The prior on-disk CODEMAP stopped at the PHASE4-N-F-G-H close (`f59b1eaa`, 456 canonical types, 123 CI checks, 316 rules). The span measured is `main..HEAD` (`main` HEAD `550eec3a`'s merge-base baseline at 456/123/316). PHASE4-N-F-G-J (PrevHash null/hash32 wire authority + position-aware header rule + genesis-successor cold-start forge on the `--mode node` spine) closes in five slices: **S1** feed/forge sched-event taxonomy (NEW GREEN `ade_node::live_log::{sched_event, sched_writer}` + `ci_check_node_sched_events_emit_only.sh`, `CN-NODE-04`); **S2** the closed BLUE sum `PrevHash` + the POSITION-BLIND `$hash32/null` codec + the producer `Hash32->PrevHash` migration + `ci_check_prevhash_single_wire_authority.sh` (`CN-WIRE-09`); **S3** the NEW BLUE position-aware authority `ade_ledger::block_validity::header_position::check_header_position` + a real genesis-successor forge; **S4** node-spine cold-start reachability (GREEN `node_sync::forge_header_position` + `node_lifecycle::may_cold_start_forge` + `ci_check_genesis_successor_reachability.sh`, `DC-NODE-08`); **S5** the C1 genesis rehearsal harness (reuse-only; `CN-REHEARSAL-FIDELITY-01` strengthened). The refresh is **scoped to the N-F-G-J surface (the NEW BLUE `PrevHash` type + `header_position` authority + the codec/producer/pre-image PrevHash migration + the NEW GREEN sched-event vocabulary + the GREEN-by-function cold-start decisions + the mechanical counts that drifted)**; every BLUE crate entry and the N-Q…N-F-G-H module entries unrelated to G-J are carried forward verbatim.
- **G-J added exactly ONE BLUE canonical type (`PrevHash`) + ONE NEW BLUE module (`header_position`)** (verified two ways: (a) `git diff --name-only main..HEAD` touches the BLUE `core_paths` files `crates/ade_types/src/shelley/block.rs`, `crates/ade_codec/src/shelley/block.rs`, and `crates/ade_ledger/src/block_validity/{header_position.rs (NEW), header_input.rs, verdict.rs, mod.rs, unsigned_header_pre_image.rs}` + `crates/ade_ledger/src/producer/{forge.rs, state.rs}`; (b) recomputing every BLUE crate's `(pub )?(struct|enum)` count at BOTH `main` and HEAD — `ade_types` `81 -> 82` (the NEW `PrevHash` enum), `ade_ledger` 177 (the NEW `check_header_position` is a *function* + `HeaderPositionInvalid` is a *variant*, no struct/enum), `ade_codec` 11 (`decode_prev_hash` is a fn + the `AdeEncode` match, no struct/enum), `ade_core` 49, `ade_crypto` 21, `ade_plutus` 8, `ade_network` BLUE 109 — all byte-stable except `ade_types +1`; total **456 -> 457**). The NEW GREEN `ade_node::live_log::{sched_event, sched_writer}` and the GREEN-by-function `node_sync::forge_header_position` / `node_lifecycle::may_cold_start_forge` live in RED `ade_node` — none is canonical-counted.
- Mechanical counts recomputed fresh from the tree at `550eec3a` (not copied): **11 crates** (Δ 0), **457 canonical types** (Δ +1 — the lone new BLUE type `PrevHash`), **2284 tests** by the documented `#[test]|#[tokio::test]` grep (Δ +39 vs the 2245 at `main`; `git grep` 2245 at `main` → 2284 at `550eec3a`), **126 CI checks** (`ls ci/ci_check_*.sh | wc -l`; cross-checked `git ls-tree -r HEAD ci/`; Δ +3: the three G-J gates), **319 registry rules** (`grep -cE '^id = ' docs/ade-invariant-registry.toml`; Δ +3 vs 316 — NEW `CN-WIRE-09` + `DC-NODE-08` + `CN-NODE-04`, all `enforced`; `CN-REHEARSAL-FIDELITY-01` strengthened; no rule weakened).
- **Cluster-doc location:** at this regen the N-F-G-J cluster + slice docs live under `docs/clusters/PHASE4-N-F-G-J/` (`cluster.md`, `S1-feed-forge-events-taxonomy.md`, `S2-prevhash-type-codec-authority.md`, `S3-position-validation-genesis-forge.md`, `S4-node-spine-cold-start-reachability.md`, `S5-c1-genesis-rehearsal.md`) + the planning docs `docs/planning/phase4-n-f-g-j-{invariants,cluster-slice-plan,genesis-successor-prevhash-invariants,rescope-cluster-slice-plan}.md` + the runbook `docs/evidence/phase4-n-f-g-j-genesis-rehearsal-README.md`; on the cluster close they archive under `docs/clusters/completed/PHASE4-N-F-G-J/` (the parent `/cluster-close` performs the move).
- TCB color for every new/changed module verified against the on-disk `// Core Contract:` / `//!` banner at `550eec3a`: `header_position.rs` opens `//! BLUE header-position legality` → **BLUE** (`ade_ledger`, a `core_paths` crate); `sched_event.rs` + `sched_writer.rs` open `//! GREEN` → **GREEN** (closed emit-only sched vocabulary inside RED `ade_node`); `node_sync.rs` → `//! RED` (the `forge_header_position` fn within it is GREEN-by-function — a pure `Option<&ChainTip> -> (u64, PrevHash)` decision); `node_lifecycle.rs` → `//! RED` (the `may_cold_start_forge` fn within it is GREEN-by-function); `chain_evolution.rs` → `//! GREEN` (unchanged banner; `prev_hash()` migrated to `PrevHash`); `ade_types`/`ade_codec` `shelley/block.rs` carry the BLUE `// Core Contract:` banner (no color change — additive type/fn). Carried-forward colors unchanged.
- Regenerated (**incremental, scoped refresh** catching up the PHASE4-N-F-G-H cluster close, carried) at HEAD `f59b1eaa` (`git rev-parse --short HEAD`; branch `serve/node-spine-peer-g-h`). The prior on-disk CODEMAP stopped at the PHASE4-N-F-G-D close (`6bd60c80`, 121 CI checks, 315 rules). PHASE4-N-F-G-H (node-spine live serve-to-peer on the `--mode node` spine) closes in three slices + S2b: **S1** extract shared serve-dispatch authority (`dce150b4` doc, `8b8b8139` impl), **S2** node-spine serve wiring (`03fe740c` doc, `8c6a6a7e` impl), **S2b** magic-aware serve listener (`917f5606` doc, `a8ca5e52` impl), **S3** C1 serve runbook + operator-gated harness (`75dcc2a4` doc, `f59b1eaa` impl). The span `6bd60c80..f59b1eaa` (the G-D close `dde78fb8` + the `18085231` BA-02-gate archived-home fix + the PHASE4-N-F-G-H cluster) touches: 1 BLUE `core_paths` file (`crates/ade_network/src/handshake/version_table.rs` — the additive fn `n2n_supported_for_magic` + its unit test ONLY), 1 NEW RED module + its registration (`crates/ade_runtime/src/network/serve_dispatch.rs`, `crates/ade_runtime/src/network/mod.rs`), the RED `crates/ade_runtime/src/network/n2n_listener.rs` (`Arc<[..]>`-typed `our_supported`), the RED `crates/ade_node/src/{node_lifecycle.rs, produce_mode.rs}` (serve wiring + re-point) + `crates/ade_node/src/admission/bootstrap.rs`, 3 new test files (`crates/ade_node/tests/{node_c1_serve_live.rs, node_spine_serve_loopback.rs, ba02_gate_archived_home.rs}`), 2 new CI gates (`ci/ci_check_single_serve_dispatch_authority.sh`, `ci/ci_check_serve_listener_magic_aware.sh`) + the broadened `ci/ci_check_ba02_evidence_manifest_schema.sh`, the registry, the four grounding docs, and the G-H cluster/planning/evidence docs. The refresh is **scoped to the N-F-G-H surface (the NEW RED `serve_dispatch` core + the additive BLUE fn `n2n_supported_for_magic` + the `Arc<[..]>`-typed listener config + the NEW RED node-spine serve wiring) + the mechanical counts that drifted**; every BLUE crate entry and the N-Q…N-F-G-D module entries are carried forward unchanged.
- **No BLUE authority or canonical type was added** by N-F-G-H (verified two ways: (a) `git diff --name-only 6bd60c80..f59b1eaa` touches exactly ONE `.idd-config.json` `core_paths` (BLUE) file — `crates/ade_network/src/handshake/version_table.rs` — and that diff adds only the public **function** `n2n_supported_for_magic` (+ its unit test), no `struct`/`enum` (`grep -nE 'pub fn n2n_supported_for_magic|pub (struct|enum)'` confirms the handshake BLUE submodule count holds at 9); (b) recomputing every BLUE crate's `(pub )?(struct|enum)` count at BOTH `6bd60c80` and `f59b1eaa` — all byte-stable: `ade_codec` 11, `ade_types` 81, `ade_crypto` 21, `ade_core` 49, `ade_ledger` 177, `ade_plutus` 8; `ade_network` BLUE submodules 109; total **456**). The NEW `ade_runtime::network::serve_dispatch` is RED (it lives in RED `ade_runtime`; the `ServedBlockEvidence` struct inside it is GREEN-by-content); the NEW `ade_node` serve wiring (`run_node_serve_task` / `bind_serve_listener` / `ServeStartError`) is RED — none lives on a BLUE `core_paths` path, so none is canonical-counted. The serve is read-only over the GREEN `ServedChainView`.
- **Cluster-doc location:** at this regen the N-F-G-H cluster + slice docs live under `docs/clusters/PHASE4-N-F-G-H/` (`cluster.md`, `S1-extract-shared-serve-dispatch.md`, `S2-node-spine-serve-wiring.md`, `S2b-magic-aware-serve-listener.md`, `S3-c1-serve-runbook-operator-gated.md`) + the planning docs `docs/planning/phase4-n-f-g-h-{invariants,cluster-slice-plan}.md` + the runbook `docs/evidence/phase4-n-f-g-h-node-serve-README.md`; on the cluster close they archive under `docs/clusters/completed/PHASE4-N-F-G-H/` (the parent `/cluster-close` performs the move). The predecessor N-F-G-D set (cluster + S1–S4) is already archived under `docs/clusters/completed/PHASE4-N-F-G-D/`; the N-F-G-E / N-F-G-C / N-F-G-B / N-F-G-A sets likewise; all prior closed cluster docs are under `docs/clusters/completed/`.
- Mechanical counts recomputed fresh from the tree at `f59b1eaa` (not copied): **11 crates** (`grep -cE '"crates/' Cargo.toml`; Δ 0), **456 canonical types** (**Δ 0** — the one BLUE-file change adds a fn, no struct/enum; the NEW `serve_dispatch` + serve wiring are RED / GREEN-by-content and not canonical-counted), **2244 tests** by the prior CODEMAP's documented `#[test]|#[tokio::test]` grep (**Δ +3** vs the prior 2241 — `n2n_supported_for_magic_produces_configured_magic`, `node_c1_serve_live`, `ba02_gate_archived_home`; verified `git grep` 2241 at `6bd60c80` → 2244 at `f59b1eaa`), **123 CI checks** (`ls ci/ci_check_*.sh | wc -l`; cross-checked `git ls-tree -r HEAD ci/`; **Δ +2**: the two N-F-G-H gates; the three containment / handoff / memory fences byte-unchanged), **316 registry rules** (`grep -cE '^id = ' docs/ade-invariant-registry.toml`; **Δ +1** vs the prior 315 — NEW `DC-NODE-07`, `tier = derived`, `enforced`, `introduced_in = PHASE4-N-F-G-H`; no rule weakened).
- **Test-count method caveat (load-bearing — flagged for the user).** The documented test-count method (`git grep -hE "#\[test\]|#\[tokio::test\]"`) matches `#[test]` and bare `#[tokio::test]` but **NOT** `#[tokio::test(flavor = "multi_thread", …)]`. The two G-H serve-loopback integration tests in `crates/ade_node/tests/node_spine_serve_loopback.rs` (`node_spine_serve_loopback_follower_fetches_self_accepted_block`, `node_serve_start_failure_is_surfaced_not_silent` — both `DC-NODE-07` tests) use the multi-thread flavor and are therefore NOT captured by the documented grep. The *true* number of new tests for the G-H span is **5**; the header tracks the documented method (**+3 → 2244**) for consistency with the carried 2241 baseline and the prior CODEMAP's stated command. (A broader pattern `#\[(tokio::)?test(\(|\])` returns 2265 at HEAD — a different baseline; not adopted, to avoid a spurious method-change drift unrelated to this cluster.)
- **Note on the canonical-type breakdown.** The per-crate figures in the Counts table (`ade_ledger` 177, `ade_core` 49, `ade_codec` 11, …; summing to 456) are a **carried frozen snapshot** from the baseline that first established the 456 total. A raw `git grep -E '^(pub )?(struct|enum) '` over the same BLUE trees at HEAD returns higher per-crate numbers (e.g. `ade_ledger` 181, `ade_core` 50, `ade_codec` 12) — the doc's figures exclude test-module / non-canonical declarations by a stricter accounting than a bare structural grep. Because the only BLUE `core_paths` change across `6bd60c80..f59b1eaa` is the addition of one *function* (no struct/enum), the 456 total and its sub-figures are byte-stable by construction and are carried forward verbatim; re-deriving them with a different counting method would introduce spurious drift unrelated to this cluster. The total `456` is the load-bearing header figure; the sub-figures are reference only.
- **Boundary honesty (load-bearing, verbatim).** PHASE4-N-F-G-H wires a **READ-ONLY serve OF the already-self-accepted `ServedChainView`** — and nothing more. `--mode node` now serves real peers (ChainSync header advertisement, DC-CONS-18; BlockFetch body by hash, DC-CONS-17) from the G-B self-accepted served chain, via a sibling listener + serve-dispatch task spawned OUTSIDE `run_relay_loop`, through the EXISTING BLUE serve reducers + the single shared `dispatch_server_frame_event_to_outbound` + the single `CN-WIRE-08` tag-24 serializer, request-driven (no proactive `advance_tip`/`changed()` reactor). It is **NOT** a peer-acceptance claim (acceptance is proven ONLY by the peer's validation log through `ba02_evidence::correlate`, RO-LIVE-06), **NOT** BA-02 / live-evidence, and **NOT** a durable-tip / gossip change. There is **NO RO-LIVE flip** (`RO-LIVE-01` stays `partial`; `RO-LIVE-06` stays schema-only/`enforced`; neither flips and neither is bumped — G-H `cross_ref`s them, recording the decoupling), **NO second `ServedChain` authority**, **NO second serve-dispatch definition**, **NO parallel tag-24 serializer**, **NO serve / `push_atomic` / served-chain mutation inside `run_relay_loop`** (the containment gate is byte-/semantically unchanged), **NO new `--mode node` argv flag** (the existing `--listen` is reused; the S1 path-fidelity fence stays green), and **NO new BLUE authority / canonical type / variant** (the lone BLUE touch is the additive fn `n2n_supported_for_magic`). The live C1 serve execution stays `blocked_until_operator_c1_serve_executed`.
- **Registry change at this close.** One NEW rule `DC-NODE-07` (count 315 → 316), `tier = derived`, `status = enforced`, `introduced_in = "PHASE4-N-F-G-H"`, `strengthened_in = []`; `ci_script = ci/ci_check_single_serve_dispatch_authority.sh, ci/ci_check_serve_listener_magic_aware.sh`; `tests = [node_spine_serve_loopback_follower_fetches_self_accepted_block, node_serve_start_failure_is_surfaced_not_silent, n2n_supported_for_magic_produces_configured_magic, node_c1_serve_live]`; `cross_ref = [DC-NODE-06, CN-NODE-02, DC-CONS-17, DC-CONS-18, CN-PROTO-06, DC-PROTO-07, DC-PROTO-08, CN-WIRE-08, CN-FORGE-01, RO-LIVE-01, RO-LIVE-06, CN-REHEARSAL-FIDELITY-01]`. Five carry-forward strengthenings (`DC-CONS-17`, `DC-CONS-18`, `DC-PROTO-07`, `DC-PROTO-08`, `CN-PROTO-06` gain `strengthened_in += "PHASE4-N-F-G-H"`). `RO-LIVE-01` / `RO-LIVE-06` are **NOT** bumped — only `cross_ref`'d (the decoupling). **No rule weakened.** The registry edit is **staged in the working tree** at this regen (not yet committed — it is part of the `M docs/ade-invariant-registry.toml` working-tree change the cluster close commits).
- **`.idd-config.json` annotations (informational).** At this regen `head_deltas_baseline` is still `6bd60c80` (the N-F-G-D close baseline) and `_invariant_registry_doc` still reads "315 entries" — both are config annotations the G-D refresh set; the G-H `/head-deltas` step (a sibling grounding-doc refresh) bumps them. They are config annotations, not enforced counts.
- TCB color for every new/changed module verified against the on-disk `// Core Contract:` / `//!` banner at `f59b1eaa`: `serve_dispatch.rs` opens `//! RED shared serve-dispatch authority for the producer-side server` → **RED** module (the `ServedBlockEvidence` struct inside it is documented GREEN-by-content per its own doc-comment — a pure observation, never re-deciding the BLUE reducer's output); `node_lifecycle.rs` → `//! RED` (unchanged) with the NEW `run_node_serve_task` / `bind_serve_listener` / `ServeStartError`; `version_table.rs` is a BLUE `ade_network::handshake` submodule (`core_paths`), and its addition is a pure additive fn (no banner/color change). Carried-forward colors unchanged.
- The doc is regenerated, not edited. If a value drifts, fix the source, not the doc.
