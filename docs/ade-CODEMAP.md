# Module Authority Map — Ade

> **Status:** Living architectural document. Regenerated; not hand-edited.
> Per-project instance of `~/.claude/methodology/templates/codemap.md`.

> 11 crates, 462 canonical types, 2542 tests, 173 CI checks at HEAD (`862cd2cb`, PHASE4-N-AO close — live multi-candidate fork-choice SELECT + adopt (rung-2): the live participant `NeedsForkChoice` arm now routes an aggregated candidate SET to the SINGLE existing BLUE `select_best_chain`, fetches + prevalidates the winner's bodies as a complete branch, and adopts via the existing rollback/WAL/`pump_block` authorities — **prove-before-commit**, never abandoning the durable chain until the replacement is validated. The entire SELECT was wired in `ade_node` (RED/GREEN); the BLUE consensus core (`select_best_chain`, `validate_and_apply_header`, `materialize_rolled_back_state`, the `WalEntry::RollBack{ForkChoiceWin}` marker) is **reused byte-unchanged** — **ZERO new BLUE canonical type** (the cluster hypothesis held mechanically: `ade_core` 49→49, `ade_ledger` 181→181, no production diff in either BLUE crate). +11 registry rules (`DC-NODE-34…41`, `DC-EVIDENCE-04/05`, `DC-PUMP-04`), +12 CI gates, `CN-CONS-03` flipped `declared → enforced` on the committed natural two-producer CE-AO-6 transcript).

---

## Conventions

- A **module** in Ade is a Cargo workspace crate (smallest independently-buildable unit). Source: `Cargo.toml` `[workspace] members` (11 entries). One exception to crate-granularity: `ade_network` is split by *submodule color* — `.idd-config.json` `core_paths` resolves BLUE at the submodule path level (9 paths), so its BLUE / GREEN / RED submodules are documented as separate entries.
- Several BLUE crates host sub-trees that carry a *different* TCB color by their own module banner; the RED `ade_runtime` and `ade_node` crates host numerous **GREEN-by-content** sub-trees. These are surfaced as sub-classification notes inside the owning crate's entry (or, where load-bearing, as their own GREEN/RED entries below). **PHASE4-N-AO added six new `ade_node` source modules + one bin**, all RED/GREEN-by-content inside the RED `ade_node` crate (full per-module entries below): the GREEN `candidate_aggregator` / `lca_walk` / `selector_state` / `post_switch_continuity`, the GREEN-prove-core-plus-RED-seam `fork_switch`, and the RED `fair_merge`.
- Modules are listed by TCB color (BLUE → GREEN → RED), alphabetical within each color.

### TCB color sources (in order of authority)

1. `.idd-config.json` `core_paths` — substring match against absolute path. BLUE crate prefixes: `ade_codec`, `ade_types`, `ade_crypto`, `ade_core`, `ade_ledger`, `ade_plutus`. Plus the 9 BLUE `ade_network` submodule paths: `mux/frame.rs`, `codec/`, `handshake/`, `chain_sync/`, `block_fetch/`, `tx_submission/`, `keep_alive/`, `peer_sharing/`, `n2c/`.
2. `.idd-config.json` `_core_paths_doc` — `ade_runtime` is RED; `ade_testkit` is GREEN; `ade_node` is RED; `ade_network::mux::transport` + `ade_network::session` are nominally RED (but `session/` is GREEN-by-content per PHASE4-N-L). `ade_core_interop` is RED.
3. **Module doc-comment banners** — every BLUE/GREEN source file opens with the `// Core Contract:` banner and/or a `//! BLUE …` / `//! GREEN …` / `//! RED …` line. This is the per-file authority that distinguishes GREEN-by-content from RED inside `ade_runtime` / `ade_node`. **The `b8860b16..HEAD` span (PHASE4-N-AO) added SEVEN production source files** (six modules + one bin), all inside the RED `ade_node` crate, NONE recoloring an existing module and NONE touching a BLUE crate: the GREEN-banner `candidate_aggregator.rs` (`//! GREEN — BLUE-safe candidate construction`), `selector_state.rs` (`//! GREEN — selector-state projection …`), `post_switch_continuity.rs` (`//! GREEN replayable post-switch branch-continuity verdict`), the deterministic-pure `lca_walk.rs` + `fork_switch.rs` (`// Core Contract:` deterministic banner; `fork_switch::prevalidate_branch` is GREEN/BLUE-reused-and-pure, while its `BranchBodySource` seam + `Null`/`Prefetched` impls are RED), and the RED-scheduling `fair_merge.rs` (`// Core Contract: RED scheduling discipline ONLY …`). The seven BLUE/GREEN/RED files from the prior N-AM/N-AN span retain their banners verbatim. **No BLUE crate source changed in this span** — verified `git diff --name-status b8860b16..HEAD -- 'crates/ade_core/src/**/*.rs' 'crates/ade_ledger/src/**/*.rs'` is empty (excluding tests).
4. Cluster TCB Color Maps under `docs/clusters/` and `docs/clusters/completed/` — §7 "TCB Color Map" of `docs/clusters/PHASE4-N-AO/cluster.md`: **BLUE (reused unchanged)** = `ade_core::consensus::{fork_choice::select_best_chain, header_validate::validate_and_apply_header, candidate, rollback}` + `ade_ledger::rollback::materialize_rolled_back_state` + `receive::reducer` + `wal::event::{WalEntry::RollBack, RollbackReason::ForkChoiceWin}`; **GREEN** = the per-peer candidate aggregator + ordering + the reconciliation/continuity projection (OQ-AO-6 *resolved GREEN* by the S2 proof); **RED** = `run_participant_sync` dispatch + fork-switch driver, the per-peer feed + selected-range fetch, the `NodeSyncItem` peer threading, convergence-evidence emission.
5. The invariant registry `docs/ade-invariant-registry.toml` (372 rules at HEAD) — each rule's `code_locus` + `statement` is the authoritative source for the **MUST NOT** rows below.

### Active cluster at HEAD

**PHASE4-N-AO (live multi-candidate fork-choice SELECT + adopt, rung-2 — primary `DC-NODE-37`) — CLOSED.** HEAD `862cd2cb` is the cluster-close (it flips `CN-CONS-03` `declared → enforced` on the committed natural two-producer CE-AO-6 transcript + refines `ci_check_convergence_evidence_vocabulary_closed.sh`). The cluster is **RED+GREEN, with the BLUE consensus core reused byte-unchanged** — it adds **NO new BLUE canonical type** (the load-bearing §1/§8/§9 hypothesis, proven mechanically). It shipped fourteen slices (S1–S14) wiring the SELECT half that PHASE4-N-AI's single-best-peer FOLLOW deliberately did not do:

- **The single selector authority is unchanged (BLUE, `ade_core`):** `consensus::fork_choice::select_best_chain` remains the **sole `DC-CONS-03` chain-selector** — multi-candidate, k-bounded, density-free, arrival-order-independent (`CN-CONS-01`). PHASE4-N-AO **routes the live candidate set TO it** (the `NeedsForkChoice` arm); it does NOT add a second selector, a parallel preference, a density ordering, or an operator heuristic. `git diff b8860b16..HEAD -- crates/ade_core/src/consensus/fork_choice.rs` is empty.
- **BLUE-safe candidate construction (GREEN, `ade_node::candidate_aggregator`, S2 `DC-NODE-35`):** `build_candidate_fragment` assembles a `CandidateFragment` for the selector **only from `validate_and_apply_header` output** — every `ValidatedHeaderSummary` in a fragment is Ade's own BLUE-validated header, NEVER a peer-minted summary (the forbidden `ade_core_interop::follow` shape) and NEVER a raw followed-peer tip. `assemble_candidate_set` orders the set by a deterministic total key (tip block-no, then `TiebreakerView` field order) — a canonicalisation only, NOT the Praos preference. The S2 5-point entry gate (peer identity preserved; hash-critical paths use preserved wire bytes / internal proof surfaces use canonical bytes; fragments derived from Ade validation; ordering deterministic; malformed/missing fails closed) is the cluster's load-bearing gate, and **resolved OQ-AO-6 → GREEN** (pure relabel/group/project of already-validated data; no store read, no materialize, no selection, no mutation).
- **Live multi-block fork-anchor discovery (GREEN, `ade_node::lca_walk`, S7 `DC-NODE-38`):** the live-geometry gap CE-AO-6 surfaced is that a live competing branch is multi-block, so the competing block's immediate parent is an intermediate block Ade never stored. `walk_to_durable_lca` walks the competing branch's preserved parent links back to the durable **last-common-ancestor** — a `ChainDb`-stored block confirmed on **slot AND hash** (`DC-NODE-29`) — k-bounded by **block depth** (traversed-header count, never slot distance). The per-peer branch cache is NOT authority — only an indexed memory of received preserved headers; each `CachedHeader` self-binds (`block_hash == map key`) or the branch fails closed.
- **Fork-switch prove-before-commit (GREEN-pure prove + RED driver, S4 `DC-NODE-37` — the security-critical slice):** `fork_switch::prevalidate_branch` is PURE — given the already-fetched bodies + the read-only-materialized anchor state it (1) binds each body to its S3-selected `ValidatedHeaderSummary`, (2) links `prev_hash` from the durable fork anchor, and (3) folds BLUE `block_validity` over the branch; **any failure leaves ChainDb/ledger/chain_dep byte-unchanged** (the H-1 class at fork-choice scale). The RED driver `node_lifecycle::apply_fork_switch` proves FIRST (no durable mutation in `prove_fork_switch`), and only on a proven branch adopts via the existing `apply_chain_event` authorities (`RolledBack(fork_anchor)` + `ChainSelected(body)×N`, recorded as the durable append-only `WalEntry::RollBack{ForkChoiceWin}`). A failed/lying/incomplete winner retires the decision as a structured `ProofFailed` and **HOLDS** the `DC-NODE-28` forge fence.
- **Reselection replay-equivalence + fence resolution (BLUE-reused + GREEN/RED, S5):** a `ForkChoiceWin` reselection replays byte-identically (multi-peer corpus, strengthening `DC-NODE-27`); `selector.current_tip == ChainDb::tip` after every decision; `fork_switch::fork_switch_fence_resolved` clears the forge fence ONLY on a fully-resolved state (no pending decision, no unresolved missing bridge, AND caught up).
- **Multi-peer wire-pump fairness (RED, `ade_node::fair_merge`, S8 `DC-PUMP-04`):** the S7 live retry surfaced that fanning all peers into one shared bounded channel lets a continuously-producing peer starve the others, so a competing branch never reaches dispatch. Each peer's pump now feeds its OWN bounded lane; `fair_merge` drains them with a deterministic configured-order round-robin (one item per peer per round; rotating cursor; closed lane retired in place). It is **RED scheduling discipline ONLY** — it changes delivery *opportunity*, never fork-choice (`select_best_chain` stays arrival-order-independent). *(Note: the MEMORY record shows S8 was later found to NOT be the live blocker — the live multi-peer delivery was an evidence-attribution artifact, fixed by the S1 per-block `block_received` peer labelling, commit `6846d252`; S8 stays committed + hermetically proven as the cleaner per-peer-lane architecture.)*
- **Closed fork-choice convergence evidence (GREEN, `ade_node::admission_log` + `convergence_evidence`, S9 `DC-EVIDENCE-04`):** the SELECT middle is now a CLOSED, observe-only convergence-evidence sequence (`needs_fork_choice → lca_discovered → candidate_fragment_built → fork_choice_selected → branch_fetch_started → branch_fetch_completed → branch_prevalidated → fork_switch_applied | fork_switch_failed | fork_switch_superseded`) — 10 fork-choice discriminators EQUAL the emit-only allow-list; every field is bounded + typed (the `fork_switch_id` is a deterministic `blake2b` hex-prefix; the failure code is a closed enum). Every `fork_choice_selected{win}` pairs to EXACTLY ONE terminal per `fork_switch_id` (`fork_switch_superseded` was added for the per-tip-id supersession case). The evidence OBSERVES already-computed authority outcomes — never consumed by selection/walk/apply/the fence.
- **Replayable post-switch branch-continuity verdict (GREEN, `ade_node::post_switch_continuity`, S10 `DC-EVIDENCE-05`):** a pure, total, deterministic reducer over the closed transcript classifies Ade's OWN validated admitted-block lineage after a `ForkChoiceWin` adoption at tip X into a closed `PostSwitchContinuity` verdict (`ContinuesSelectedBranch | Diverged | BrokenLineage | DanglingForkChoiceWin | InsufficientEvidence`). It reads ONLY Ade's own admitted lineage (peer tip is NEVER an input). It is the SINGLE implementation behind both the hermetic replay test and the live CE-AO-6 release gate (no Rust/Python drift).
- **Post-`ForkChoiceWin` forward-follow continuity (RED dispatch + GREEN reducer, S11 `DC-NODE-39`):** after adopting at X, a descendant whose parent link Ade has not validated is NEVER admitted; a genuinely missing bridge emits a closed `MissingBridge` discriminant, PRESERVES the durable chain byte-unchanged, and HOLDS the forge fence (`pending_missing_bridge.is_none()` is a fence-resolve precondition) — replacing the pre-S11 silent no-op. The hold is HOLD-until-progress.
- **Rolled-back branch evidence retention (GREEN, `ade_node::lca_walk` + `node_lifecycle`, S13 `DC-NODE-40`):** the blocks Ade itself rolled back during a `ForkChoiceWin` adoption (admitted `LinearExtend`, so never in the S7 competing-only branch cache) are retained as walk-visible EVIDENCE so a later competing branch descending through them stays evaluable — closing the S12-harness Fault-1 `MissingBridge` over-fire (the LCA-walk over-fire the LCA walk hit). Retention is k-bounded, hash-keyed, self-binding; it NEVER becomes durable authority, a rollback target, or the LCA anchor.
- **Missing-bridge range re-fetch (RED, `ade_node::fork_switch` + `node_lifecycle`, S14 `DC-NODE-41`):** the `DC-NODE-39` passive floor cannot recover a bridge ChainSync already streamed past (each block is delivered once). When a post-`ForkChoiceWin` **winning** peer presents a descendant whose parent chain is missing, Ade actively re-fetches the range (`from_tip+1 .. descendant`) from that peer ONLY, admits it in parent-link order through `pump_block`, and clears the hold ONLY on real admitted progress. Byte-only (a lying/short range leaves the structured hold), winning-peer-only (no fetch spam on loser orphans), bounded-retry (`MAX_RANGE_REFETCH_ATTEMPTS = 3`).

It flips ELEVEN rules → `enforced` (`DC-NODE-34/35/36/37/38/39/40/41`, `DC-EVIDENCE-04/05`, `DC-PUMP-04`) backed by twelve new gates, and flips **`CN-CONS-03` `declared → enforced`** (`evidence_notes`: a NATURAL two-producer multi-candidate SELECT pass — both Haskell producers live throughout, no loser-freeze, the SELECT decision entirely Ade's — `ContinuesSelectedBranch` + terminal `AgreedAtSwitchTip{slot 391}`, 25 admitted descendants chained, 0 diverged, every fork-choice win terminal; transcript `ao-CN-CONS-03-FLIP-natural-conv.jsonl`, sha256 `6713efe9…`, preserved OUTSIDE the repo per competition-secrecy). **SCOPE (honest):** proves convergence for the EXERCISED two-producer partition-and-reconverge venue (S1–S14); NOT an unbounded multi-peer Cardano ChainSel claim; post-switch endless-flip-flop survival is out of scope. `RO-LIVE-01` (preprod rung-3) stays operator-gated. The PHASE4-N-AO cluster + slice docs live under `docs/clusters/PHASE4-N-AO/` (S1–S14); its invariants under `docs/planning/phase4-select-multicandidate-fork-choice-{invariants,cluster-slice-plan}.md` and `docs/planning/phase4-n-ao-ce-ao-6-live-gap.md`.

**Carried: PHASE4-N-AN (rollback-materialization preserves recovered eta0 — `T-REC-06`) — CLOSED + ARCHIVED** at `b8860b16`. The BLUE change was one new method `PraosChainDepState::overlay_recovered_eta0` (the SINGLE eta0-overlay authority, shared by WarmStart bootstrap + rollback materialize) + a `recovered_eta0: Option<&Nonce>` param on `materialize_rolled_back_state` (the SOLE rolled-back-state authority) — no new canonical type. **PHASE4-N-AO reuses both unchanged** — `fork_switch::prove_fork_switch` threads `state.recovered_eta0` into its read-only `materialize_rolled_back_state` at the fork anchor (so the fork-switch prevalidation replays the branch against the same eta0 live admit uses). **Carried: PHASE4-N-AM (keep-alive wire-pump client — `DC-PUMP-03`) — CLOSED + ARCHIVED** at `b8860b16`: RED-only, the N2N keep-alive CLIENT in `ade_runtime::admission::wire_pump` (reused unchanged by `--mode node`). **Carried: PHASE4-N-AL/AK/AJ/AI … N-U** (participant recovered-anchor rollback no-op; recovered-anchor live-follow start + the +2 BLUE `RecoveredAnchorPoint` types; participant-path convergence-evidence emission; live fork-choice rollback-FOLLOW wiring incl. the BLUE `WalEntry::RollBack` durable marker + `RollbackReason::ForkChoiceWin`; local-durable forge + KES evolve-then-sign + bounded serve) — all CLOSED + archived. Every prior closed cluster doc is archived under `docs/clusters/completed/`.

**What the binary `Node` arm actually runs (precise wiring honesty):** `--mode node` (`ade_node::node_lifecycle`) bootstraps via the single `bootstrap_initial_state` authority (WarmStart restores the seed-epoch sidecar + overlays the recovered eta0; resolves the live-follow start tip via `resolve_live_follow_start`), spawns the live WirePump feed — **PHASE4-N-AO S8: now via per-peer bounded lanes drained by the deterministic `fair_merge`**, the keep-alive client sustaining each link — drains it through the sole `forward_sync::pump_block` durable-admit chokepoint, and serves a read-only projection of the durable ChainDb to peers. On the **participant** path (`run_participant_sync`), a competing Participant block classified `NeedsForkChoice` is **no longer fail-closed** (the pre-AO `Err(UnexpectedRollback)`): `dispatch_competing_fork_choice` caches the competing header (self-bound), walks `walk_to_durable_lca` to the durable LCA (consulting the S13 rollback-retention on a cache miss), builds the candidate fragment from the LCA via `build_candidate_fragment` (read-only `materialize_rolled_back_state` at the durable anchor for the fork-point chain_dep), and routes the candidate SET to the single `select_best_chain`. A win emits a provisional `PendingForkSwitch` + sets the forge fence; the relay loop drives `prefetch_branch_bodies` (S6 BlockFetch RequestRange, byte-only) → `apply_fork_switch`, which proves-then-commits (`RolledBack + ChainSelected×N`, `WalEntry::RollBack{ForkChoiceWin}`). A walk failure for a winning-peer descendant ahead of the durable tip sets an eligible `pending_range_refetch` (S14) ALONGSIDE the `DC-NODE-39` floor hold; the range re-fetch drives `recover_missing_range` (the `pump_block` sole admit), clearing the hold ONLY on `RangeRefetchOutcome::Admitted`. Every decide/apply step emits closed observe-only convergence evidence. The single-producer `run_node_sync` path is unchanged (it never reaches SELECT — `SingleProducer`/`Unknown` venues fail closed on a competing block). `RO-LIVE-01` stays operator-gated.

### Counts (mechanical, with sources)

| Count | Value | Source / command |
|---|---|---|
| Crates | **11** | `grep -cE '"crates/' Cargo.toml` (`[workspace] members`). **Δ vs prior on-disk CODEMAP (11): 0** — `git diff --name-status b8860b16..HEAD -- Cargo.toml` shows no member added; PHASE4-N-AO is six new modules + one bin **inside the existing `ade_node` crate**. |
| Canonical types | **462** | `git grep -hE "^(pub )?(struct\|enum) "` over the 6 BLUE crate `src/` trees + the 9 BLUE `ade_network` submodule paths, summed at HEAD `862cd2cb`. **Δ vs prior on-disk CODEMAP (462): 0 — the cluster's load-bearing hypothesis (`§1`/`§8.9`/`§9`: zero new BLUE canonical type) held mechanically.** Breakdown (byte-stable across the span; NO production source changed in any BLUE crate): `ade_codec` 11, `ade_types` 82, `ade_crypto` 21, `ade_core` **49** (49 at both `b8860b16` and `862cd2cb`), `ade_ledger` **181** (181 at both), `ade_plutus` 8 (= 352) + `ade_network` BLUE submodules **110** (`mux/frame.rs` 5, `codec/` 39, `handshake/` 9, `chain_sync/` 11, `block_fetch/` 10, `tx_submission/` 5, `keep_alive/` 5, `peer_sharing/` 5, `n2c/` 21). The AO modules import PRE-EXISTING BLUE types (`CandidateFragment` / `TiebreakerView` / `ValidatedHeaderSummary` / `Point` from `ade_core::consensus::{candidate, header_summary, events}`; `RollbackReason::ForkChoiceWin` from `ade_ledger::wal::event`) — all present at `b8860b16`. `canonical_type_registry: null`, so the structural grep is authoritative. The AO modules' OWN closed types (`MissingBridgeReason`, `RangeRefetchOutcome`, `ForkSwitchOutcome`, `CandidateBuildError`, `LcaError`, `BranchProofError`, `PostSwitchContinuity`, the `AdmissionLogEvent` fork-choice variants, …) live in the RED `ade_node` crate and are NOT canonical-counted. |
| Tests | **2542** | `git grep -hE "#\[test\]\|#\[tokio::test\]" -- 'crates/**/*.rs' \| wc -l` = 2542 at `862cd2cb`. Approximate per the template fallback rule (count of attributes, not a runner enumeration). **Δ vs the same grep at the prior baseline `b8860b16` (2455): +87** — the S1–S14 hermetic test suites (the per-module unit tests in `fork_switch.rs` / `lca_walk.rs` / `candidate_aggregator.rs` / `fair_merge.rs` / `selector_state.rs` / `post_switch_continuity.rs` + the AO integration suites under `crates/ade_node/tests/`, incl. the S12 bridge-gap fault-injection harness). |
| CI checks | **173** | `ls ci/ci_check_*.sh \| wc -l` = 173 at `862cd2cb` (cross-checked `git ls-tree -r HEAD ci/`). CI lives entirely under `ci/` (`ci_dirs = ["ci"]`); there is no `.github/workflows/` invariant gate at HEAD. **Δ vs prior on-disk CODEMAP (161): +12** — verified `git diff --name-status b8860b16..HEAD -- 'ci/ci_check_*.sh'`: **12 `A`** (`ci_check_peer_identity_preserved.sh`, `ci_check_candidate_construction_validated.sh`, `ci_check_live_selector_dispatch.sh`, `ci_check_fork_switch_never_abandons.sh`, `ci_check_live_blockfetch_byte_only.sh`, `ci_check_lca_anchor_walk.sh`, `ci_check_wire_pump_fairness.sh`, `ci_check_fork_choice_evidence_closed.sh`, `ci_check_post_switch_convergence_window.sh`, `ci_check_missing_bridge_fail_closed.sh`, `ci_check_rollback_retention_evidence.sh`, `ci_check_missing_bridge_refetch.sh`), **6 `M`** (reused/extended: `ci_check_convergence_evidence_vocabulary_closed.sh`, `ci_check_live_fork_choice_apply.sh`, `ci_check_live_fork_choice_wiring.sh`, `ci_check_node_path_fidelity.sh`, `ci_check_wal_rollback_replay_equiv.sh`, `ci_check_wire_rollback_signal_preserved.sh`), **0 `D`**. |
| Registry rules | **372** | `grep -cE "^id = " docs/ade-invariant-registry.toml` = 372. Reference only — not a header count. **Status breakdown:** 239 `enforced` / 19 `partial` / 113 `declared` / 1 `enforced_scaffolding`. **Δ vs prior on-disk CODEMAP (361): +11** — the net-new rules are `DC-NODE-34/35/36/37/38/39/40/41`, `DC-EVIDENCE-04`, `DC-EVIDENCE-05`, `DC-PUMP-04` (all `enforced` at close). **One flip:** `CN-CONS-03` `declared → enforced` (on the committed natural CE-AO-6 transcript, `tier = derived`). **No rule weakened.** |

---

## BLUE Modules — Pure Functional Core

> **Shared header (applies to every BLUE entry below).** Every `.rs` source file begins with the `// Core Contract:` banner, and the following deny attributes are present in each crate's `lib.rs` (or, for `ade_network`, at the crate root — BLUE submodules inherit them):
> `#![deny(unsafe_code)]`, `#![deny(clippy::unwrap_used)]`, `#![deny(clippy::expect_used)]`, `#![deny(clippy::panic)]`, `#![deny(clippy::float_arithmetic)]`.
>
> Cross-cutting BLUE CI gates (all scope the full BLUE set — 6 BLUE crates + 9 BLUE `ade_network` submodule paths): `ci_check_module_headers.sh`, `ci_check_forbidden_patterns.sh`, `ci_check_dependency_boundary.sh`, `ci_check_no_signing_in_blue.sh`, `ci_check_no_semantic_cfg.sh`, `ci_check_hash_uses_wire_bytes.sh`, `ci_check_ingress_chokepoints.sh`, `ci_check_pallas_quarantine.sh`, `ci_check_no_async_in_blue.sh`.
>
> Narrow BLUE / consensus-authority gates carried + extended by PHASE4-N-AO: the standing `ci_check_no_density_in_fork_choice.sh` + `ci_check_consensus_closed_enums.sh` + `ci_check_chain_selection_arrival_order_independent.sh` (the `#[cfg(test)]` arrival-order proof over the UNCHANGED `select_best_chain`, `CN-CONS-01`) + `ci_check_rollback_materialize_closure.sh` / `ci_check_rollback_materialize_eta0.sh` (`CN-STORE-07` / `T-REC-06`) + `ci_check_receive_reducer_closure.sh` (`DC-CONS-20`) all STAY GREEN under PHASE4-N-AO (the cluster routes-to + reuses these BLUE authorities, never duplicates them). **The N-AO gate `ci_check_wal_rollback_replay_equiv.sh` was extended for the `ForkChoiceWin` reselection** (`DC-NODE-27` strengthened to multi-peer reselection). **No N-AO gate scopes a BLUE crate source change** — every N-AO gate scopes the RED/GREEN `ade_node` SELECT wiring; the BLUE deny / closed-enum / canonical-codec gates apply to the unchanged BLUE files.

---

### `ade_codec`

| Attribute | Value |
|-----------|-------|
| **Purpose** | Owns Cardano-canonical CBOR ingress — the only place in the workspace that turns raw bytes into typed semantic values, with wire-byte preservation for every hash-bearing structure. Owns the standalone opcert byte authority and the canonical Conway-tx preserved-byte splitter, the canonical block-envelope encoder `cbor::envelope::encode_block_envelope`, and (N-X) the single workspace **tag-24 CBOR-in-CBOR wire-envelope authority** `cbor::tag24::{wrap_tag24, unwrap_tag24}` + closed `TagEnvelopeError`. Also exposes the shared canonical CBOR read/write primitives the BLUE codecs in `ade_ledger` + `ade_network` build on. **N-F-G-J** owns the POSITION-BLIND header `prev_hash` codec `shelley::block::decode_prev_hash` (`null -> Genesis` / `hash32 -> Block`; CN-WIRE-09) — the parse the PHASE4-N-AO LCA walk + fork-switch link checks consume via the decoded `PrevHash`. (Unchanged across the PHASE4-N-AO span.) |
| **Creates** | `PreservedCbor<T>`, `RawCbor`, `BlockEnvelope`, `ByronDecodedBlock`, `CodecContext`, `CodecError`, `ContainerEncoding`, `IntWidth`, era-tagged block/tx wrappers, `OpCertCodecError`, `TxComponents<'a>`, `TagEnvelopeError` (closed). 11 public types (byte-stable across the G-K…N-AO span). |
| **Interprets** | All canonical Cardano CBOR. `unwrap_tag24` is the single workspace authority that strips a tag-24 (`0xd8 0x18`) envelope (zero-copy borrow, fail-closed `TagEnvelopeError`). `shelley::block::decode_prev_hash` decodes the header `prev_hash` POSITION-BLIND (CN-WIRE-09). |
| **MUST NOT** | (1) Construct `PreservedCbor` outside `ade_codec`. (2) Re-encode wire bytes when computing hashes. (3) Use any forbidden BLUE pattern. (4) Depend on any workspace crate except `ade_types`. (5)–(11) per-construct prohibitions (Conway cert/withdrawals/governance, opcert, tx_components, single block-envelope encoder). (12) `cbor::tag24` (CN-WIRE-08) — `wrap_tag24`/`unwrap_tag24` defined exactly once; `unwrap_tag24` fails closed (never panic); no second/hand-rolled tag-24 parse anywhere. (13) `shelley::block::decode_prev_hash` (CN-WIRE-09) — the codec is the byte representation only and MUST stay POSITION-BLIND (no `block_number`); the position-aware coupling lives in `ade_ledger::block_validity::header_position`, never here. |
| **Inbound deps** | `ade_ledger` (heavy), `ade_plutus`, `ade_testkit`, `ade_network`, `ade_runtime`, `ade_core_interop`, `ade_node`. |
| **Outbound deps** | `ade_types`. std-only; dev-deps `serde_json`, `toml`. |
| **Entry points** | `ade_codec::cbor::envelope::{decode_block_envelope, encode_block_envelope}`, `ade_codec::{wrap_tag24, unwrap_tag24, TagEnvelopeError}`, the `cbor::{read_*, write_*_canonical, canonical_width, ContainerEncoding, IntWidth}` primitives, `ade_codec::traits::AdeEncode`, per-era `decode_*_block`, `ade_codec::shelley::{opcert::{encode_opcert, decode_opcert}, tx_components::split_conway_tx_components, block::decode_prev_hash}`. |
| **Key modules** | `cbor/` (incl. `envelope.rs`, `tag24.rs`), `byron/`, `shelley/` (incl. `block.rs`, `cert.rs`, `opcert.rs`, `tx_components.rs`), `allegra/`, `mary/`, `alonzo/`, `babbage/`, `conway/`, `address/`, `preserved.rs`, `traits.rs`, `primitives.rs`, `error.rs`. |

---

### `ade_core`

| Attribute | Value |
|-----------|-------|
| **Purpose** | The functional core (consensus half): Praos VRF leader-check + the expected-VRF-input recipe; **the chain-selection authority `consensus::fork_choice::select_best_chain` (the sole `DC-CONS-03` selector)**; the BLUE candidate/header-validation authorities the PHASE4-N-AO SELECT path reuses — `consensus::header_validate::validate_and_apply_header` (the sole candidate-summary source), `consensus::candidate::{CandidateFragment, TiebreakerView}`, `consensus::header_summary::{HeaderInput, HeaderVrf, ValidatedHeaderSummary}`, `consensus::events::Point`, `consensus::rollback`, `consensus::praos_leader_value`; the `PraosChainDepState` / `Nonce` evolution (incl. the N-AN `overlay_recovered_eta0` single eta0-overlay authority); the HFC era-schedule `EraSchedule`; the closed `LeaderCheckVerdict`. **PHASE4-N-AO (UNCHANGED — the load-bearing reuse):** `select_best_chain` + `validate_and_apply_header` + `candidate` + `rollback` are **reused byte-unchanged** as the SELECT mechanism (the cluster routes the live candidate set TO `select_best_chain`, adds no second selector). `git diff b8860b16..HEAD -- crates/ade_core/src/consensus/` is empty; `ade_core` is 49 → 49. |
| **Creates** | `PraosChainDepState`, `Nonce`, `EraSchedule`, `ExpectedVrfInput`, `LeaderCheckVerdict` (closed), `CandidateFragment`, `TiebreakerView`, `ValidatedHeaderSummary`, `HeaderInput`, `HeaderVrf`, `Point`, `BlockDistance`, candidate/VRF-input/leader-threshold types. 49 public types (byte-stable across the N-AO span). |
| **Interprets** | Praos header VRF/leader inputs; candidate chain summaries for `select_best_chain`. `validate_and_apply_header` validates one `HeaderInput` against a chain-dep + ledger view and emits a `ValidatedHeaderSummary` — the SOLE source the GREEN `ade_node::candidate_aggregator` is permitted to build a `CandidateFragment` from. `consensus::praos_state` evolves the nonce + overlays the recovered eta0. |
| **MUST NOT** | (1) Perform I/O / read a clock / use float / `HashMap`. **(2) Be the home of a SECOND chain selector — `select_best_chain` is the sole `DC-CONS-03` fork-choice authority; PHASE4-N-AO MUST route-to it, never duplicate it (no parallel selector / density ordering / operator heuristic / live re-implementation in `ade_node`); arrival-order-independent (`CN-CONS-01`). `validate_and_apply_header` is the sole candidate-summary source — a RED-minted `ValidatedHeaderSummary` (the `ade_core_interop::follow` shape) MUST NOT cross into this crate or reach `select_best_chain` (`DC-NODE-35`).** (3) Construct semantic types from raw bytes. (4) Depend on any RED/GREEN crate (only `ade_types`, `ade_crypto`, `minicbor`). (5) Add a SECOND `leader_vrf_input` / expected-VRF-input authority. (6) `PraosChainDepState::overlay_recovered_eta0` (T-REC-06) — the SINGLE eta0-overlay authority; no second nonce-substitution site; never weaken/skip/bypass header VRF; stays pure. |
| **Inbound deps** | `ade_ledger`, `ade_testkit`, `ade_core_interop`, `ade_runtime`, `ade_node` (incl. the PHASE4-N-AO SELECT consumers: `candidate_aggregator` / `selector_state` / `fork_switch` / `lca_walk` import `consensus::{candidate, header_validate, header_summary, events, praos_leader_value, praos_state, era_schedule, ledger_view}`; `node_lifecycle` calls `select_best_chain`). |
| **Outbound deps** | `ade_types`, `ade_crypto`, `minicbor`. Dev-dep: `ade_testkit`. |
| **Entry points** | `ade_core::consensus::{fork_choice::select_best_chain, header_validate::validate_and_apply_header, leader_check::*, praos_state::{PraosChainDepState (incl. overlay_recovered_eta0), Nonce}, candidate::{CandidateFragment, TiebreakerView}, header_summary::{HeaderInput, HeaderVrf, ValidatedHeaderSummary}, events::{Point, BlockDistance}, praos_leader_value}`, `ade_core::era_schedule::EraSchedule`. |
| **Key modules** | `consensus/` (`fork_choice.rs`, `header_validate.rs`, `candidate.rs`, `header_summary.rs`, `events.rs`, `rollback.rs`, `leader_check.rs`, `praos_state.rs`, `nonce.rs`, `vrf_cert.rs`, `ledger_view.rs`, `era_schedule.rs`). |

---

### `ade_crypto`

| Attribute | Value |
|-----------|-------|
| **Purpose** | Pure cryptographic verification — Praos VRF (draft-03) verify, the Ade-owned BLUE Sum6KES algorithm (`kes_sum/`), Ed25519 DSIGN verify, blake2b hashing. The PHASE4-N-AO `fork_switch_id` is a `blake2b` hex-prefix of `(winning_peer ‖ fork_anchor ‖ winner_tip)` — over the `ade_crypto::blake2b` hashing reused unchanged. (Unchanged across the N-AO span.) |
| **Creates** | `KesError`, `KesParseError`, VRF/KES/DSIGN verify types, `Sum6` key/signature representations. 21 public types. |
| **Interprets** | VRF proofs, KES signatures + the expanded `Sum6KES` envelope, Ed25519 signatures; `blake2b_256` / `blake2b_224`. |
| **MUST NOT** | (1) **Sign** anything — verification only; signing material custody lives in the RED `ade_runtime::producer` shell (`T-KEY-01` / `ci_check_no_signing_in_blue.sh`). (2) Perform I/O / read a clock / use float. (3) Re-implement Sum6KES divergently from Haskell `cardano-base` (the cardano-crypto Rust crate is a `#[cfg(test)]` oracle only). (4) Depend on any workspace crate except `ade_types`. |
| **Inbound deps** | `ade_core`, `ade_ledger`, `ade_plutus`, `ade_runtime`, `ade_node`, `ade_core_interop`, `ade_testkit`. |
| **Outbound deps** | `ade_types`, `blake2`, `ed25519-dalek`, `cardano-crypto` (`["vrf-draft03", "kes-sum"(test-only), "dsign"]`). |
| **Entry points** | `ade_crypto::vrf::*`, `ade_crypto::kes_sum::{kes_update, kes_verify, *}`, `ade_crypto::dsign::*`, `ade_crypto::hash::{blake2b_256, blake2b_224}` (`ade_crypto::blake2b::blake2b_256`). |
| **Key modules** | `vrf/`, `kes_sum/`, `dsign/`, `hash.rs`. |

---

### `ade_ledger`

| Attribute | Value |
|-----------|-------|
| **Purpose** | The functional core (ledger half): stateless ledger rules for every era; **B1 block-validity verdict `block_validity::block_validity` (the BLUE ledger fold the PHASE4-N-AO `fork_switch::prevalidate_branch` reuses to validate a fetched branch before any commit)**; B2 tx-validity + mempool admission; the single BLUE wire-ingress chokepoint `mempool::ingress::mempool_ingress`; the BLUE producer authority; the single body-hash authority; **the receive/rollback authorities the PHASE4-N-AO fork-switch adopt reuses byte-unchanged — `rollback::materialize::materialize_rolled_back_state` (the SOLE rolled-back-state authority, `CN-STORE-07`, with the N-AN `recovered_eta0` overlay), the lockstep `receive::reducer` (`DC-CONS-20`), and the closed append-only `wal::event::{WalEntry::RollBack, RollbackReason::ForkChoiceWin}` durable marker (`DC-NODE-27`)**; the snapshot/bootstrap-anchor/genesis/seed-epoch authorities; the KES-signing pre-image recipe; (N-AK) the closed `recovered_anchor_point::RecoveredAnchorPoint` record. **PHASE4-N-AO (UNCHANGED — the load-bearing reuse):** `block_validity`, `materialize_rolled_back_state`, `receive::reducer`, and the `WalEntry::RollBack{ForkChoiceWin}` marker are reused byte-unchanged as the prove + adopt authorities; `RollbackReason::ForkChoiceWin` (defined + replay-tested at N-AI) is now constructed LIVE for the first time. `git diff b8860b16..HEAD -- crates/ade_ledger/src/` is empty; `ade_ledger` is 181 → 181. |
| **Creates** | **181** public types (byte-stable across the N-AO span). Load-bearing for PHASE4-N-AO: `BlockValidityError`, `BlockVerdict` / `BlockValidityVerdict` (`Valid { tip, .. } \| Invalid`), `LedgerState`, `WalEntry` (closed sum: `AdmitBlock` + `SeedEpochConsensusInputsImported` tag 3 + `RollBack` tag 1), `RollbackPoint` / `RollbackReason` (incl. `ForkChoiceWin`). Plus: `TxValidity*`, `MempoolState`, `ProducerTick`, `ForgedBlock`, `ServedChainSnapshot`, `PoolDistrView`, `ProtocolParameters`, `Rational`, `SeedEpochConsensusInputs` (closed; `epoch_nonce`; `SEED_CINPUT_SCHEMA_VERSION = 2`), `BootstrapAnchor`, `RecoveredAnchorPoint` (closed; `RECOVERED_ANCHOR_POINT_SCHEMA_VERSION = 1`). `PrevHash` is owned by `ade_types`. |
| **Interprets** | Canonical decoded blocks/txs/certs/snapshots; `block_validity` produces the per-block verdict + post-state the PHASE4-N-AO branch fold consumes; `materialize_rolled_back_state` interprets the recovered `nearest_le` snapshot chain_dep (overlaying eta0) — reused read-only at the durable fork anchor by `fork_switch::prove_fork_switch`; `mempool_ingress` is the sole BLUE chokepoint; `decode_block` (re-exported through `block_validity`) is the PHASE4-N-AO body-decode + body-hash + `prev_hash` source. |
| **MUST NOT** | All carry-forward ledger prohibitions (no I/O, no clock, closed cert/governance enums, single body-hash recipe). `producer::*` fences. **`rollback::materialize::materialize_rolled_back_state` (T-REC-06 / CN-STORE-07) MUST stay the SOLE rolled-back-state authority — PHASE4-N-AO reuses it, never re-implements a parallel rollback (the fork-switch adopt MUST go through `materialize` + `commit_rollback` + `pump_block`, recorded as `WalEntry::RollBack{ForkChoiceWin}`, NOT a second rollback implementation, `DC-NODE-37`).** `wal::{event, replay}` rollback marker (DC-NODE-27 / CN-WAL-01) — `WalEntry::RollBack` is a DURABLE MARKER, append-only (tag 1 reserved); `RollbackReason::ForkChoiceWin` is a closed variant, NOT widened by PHASE4-N-AO. `block_validity` stays the single block verdict authority — PHASE4-N-AO's branch prevalidation folds it, never bypasses it. `recovered_anchor_point` / `seed_consensus_inputs` / `bootstrap_anchor` / `genesis_source` provenance fences (carried). |
| **Inbound deps** | `ade_testkit`, `ade_core_interop`, `ade_runtime`, `ade_node` (incl. the PHASE4-N-AO consumers: `fork_switch` imports `block_validity::{block_validity, decode_block, BlockValidityVerdict, DecodedBlock}` + `state::LedgerState`; `node_lifecycle` calls `materialize_rolled_back_state` + `pump_block` + the `WalEntry::RollBack{ForkChoiceWin}` apply via `apply_chain_event`). |
| **Outbound deps** | `ade_types`, `ade_crypto`, `ade_codec`, `ade_plutus`, `ade_core` (incl. `consensus::praos_state::{Nonce, PraosChainDepState::overlay_recovered_eta0}`), `minicbor`, `num-bigint`, `num-integer`, `num-traits`. Dev-dep: `ade_testkit`. |
| **Entry points** | `ade_ledger::block_validity::{decode_block, validate_block, block_validity, BlockValidityVerdict, DecodedBlock, check_header_position, unsigned_header_pre_image::unsigned_header_pre_image}`, `ade_ledger::state::LedgerState`, `ade_ledger::tx_validity::*`, `ade_ledger::mempool::{ingress::mempool_ingress, admit::*}`, `ade_ledger::producer::*`, `ade_ledger::block_body_hash::*`, `ade_ledger::{receive, rollback, snapshot}::*` (incl. `rollback::materialize::materialize_rolled_back_state`), `ade_ledger::wal::*` (incl. `wal::event::{WalEntry::RollBack, RollbackPoint, RollbackReason::ForkChoiceWin}`), `ade_ledger::pparams::ProtocolParameters`, `ade_ledger::rational::Rational`, `ade_ledger::seed_consensus_inputs::*`, `ade_ledger::consensus_view::{PoolDistrView, PoolEntry}`, `ade_ledger::bootstrap_anchor::*`, `ade_ledger::genesis_source::*`, `ade_ledger::recovered_anchor_point::*`. |
| **Key modules** | `block_validity/` (incl. `verdict.rs`, `unsigned_header_pre_image.rs`, `header_position.rs`), `tx_validity/`, `mempool/`, `cert_classify/`, `consensus_view.rs`, `producer/`, `block_body_hash.rs`, `receive/` (incl. `reducer.rs`), `rollback/` (incl. `materialize.rs`), `snapshot/`, `pparams.rs`, `rational.rs`, `wal/` (incl. `event.rs` — `WalEntry::RollBack` + `RollbackReason::ForkChoiceWin`; `replay.rs`), `seed_consensus_inputs.rs`, `bootstrap_anchor/`, `genesis_source.rs`, `recovered_anchor_point.rs`, `state.rs`. |

> **GREEN sub-classification:** `ade_ledger::mempool::{policy, canonicalize}` are GREEN-by-content. **RED sub-classification:** `ade_ledger::consensus_input_extract` is RED-by-content. Both carry their own module banner.

---

### `ade_network` *(BLUE submodules)*

| Attribute | Value |
|-----------|-------|
| **Purpose** | Owns the Cardano Ouroboros mini-protocol authority — the closed wire grammar (CBOR codecs) and pure state machines for all N2N + N2C mini-protocols, plus the BLUE mux frame primitive and the producer-side server-role reducers. Owns the per-protocol tag-24 composition authorities and (mini-protocol 2) the **`block_fetch/` grammar `block_fetch::{client, server}` + `codec::block_fetch::{compose,decompose}_blockfetch_block`** the PHASE4-N-AO selected-range fetch (BlockFetch `RequestRange` anchor→tip) rides on. **`keep_alive/` (mini-protocol 8)** owns the BLUE keep-alive grammar reused by the N-AM client. (No `ade_network` change in the PHASE4-N-AO span — the SELECT path reuses the existing BlockFetch + ChainSync grammar.) |
| **Creates** | **110** BLUE canonical types: `mux/frame.rs` 5, `codec/` 39 (incl. the closed `ArrayHead = Definite(u64) \| Indefinite`, CN-WIRE-11), `handshake/` 9, `chain_sync/` 11, `block_fetch/` 10, `tx_submission/` 5, `keep_alive/` 5, `peer_sharing/` 5, `n2c/` 21. |
| **Interprets** | Mini-protocol wire frames; `mux::frame::decode_frame` is the single frame decode authority; per-protocol `*_transition` reducers consume decoded messages. `decompose_blockfetch_block` / `decompose_rollforward_header` strip the per-protocol tag-24 composition. `decode_find_intersect_points` decodes the `FindIntersect` points list (SCOPED to FindIntersect — CN-WIRE-11). `keep_alive_transition` consumes a decoded `KeepAliveMessage` (reused by the N-AM RED client). |
| **MUST NOT** | (1) `mux::frame` — single `encode_frame`/`decode_frame` pair. (2) handshake — single `n2n_transition` + single `n2c_transition`. (3) closed `AcceptedMiniProtocol` registry. (4) no socket I/O in BLUE submodules. (5) no async/tokio in BLUE submodules. (6) depend on no workspace crate beyond `ade_codec` + `ade_types`. (7) server reducers MUST NOT split headers in parallel or depend on signing. (8) tag-24 composition (CN-WIRE-08) — composers delegate byte-level wrap/strip to the single `ade_codec` authority. (8b) `n2n_supported_for_magic` (DC-NODE-07). (8c) `encode_n2n_version_params` (CN-WIRE-10). (8d) `decode_array_head_two_form` / `ArrayHead` (CN-WIRE-11). (8e) `producer_chain_sync_serve` FindIntersect cursor (DC-PROTO-10). (8f) `keep_alive` grammar (CN-PUMP-01 / DC-PUMP-03) — the SINGLE keep-alive grammar, reused not redefined. |
| **Inbound deps** | `ade_runtime` (incl. the N-AM keep-alive-client consumer + the PHASE4-N-AO BlockFetch `RequestRange` consumer of `block_fetch::client`), `ade_node`, `ade_core_interop`, `ade_testkit`. |
| **Outbound deps** | `ade_types`, `ade_codec`. No external deps in BLUE submodules. |
| **Entry points** | `ade_network::mux::frame::{encode_frame, decode_frame}`, `ade_network::handshake::{n2n_transition, n2c_transition}`, `ade_network::handshake::version_table::{N2N_SUPPORTED, n2n_supported_for_magic, encode_n2n_version_params}`, `ade_network::chain_sync::{server::*, client::*}`, `ade_network::block_fetch::{server::*, client::*}`, `ade_network::keep_alive::{keep_alive_transition, KeepAliveState, KeepAliveEvent, KeepAliveOutput, KeepAliveError}`, `ade_network::codec::{block_fetch::{compose,decompose}_blockfetch_block, chain_sync::{{compose,decompose}_rollforward_header, Point}, keep_alive::*, primitives::{decode_array_head_two_form, ArrayHead}}`, `ade_network::{tx_submission, peer_sharing, n2c}::*`. |
| **Key modules** | `mux/frame.rs`, `codec/` (incl. `block_fetch.rs`, `chain_sync.rs`, `keep_alive.rs`, `primitives.rs`), `handshake/` (incl. `version_table.rs`), `chain_sync/` (incl. `server.rs`), `block_fetch/` (incl. `server.rs`), `tx_submission/`, `keep_alive/` (incl. `transition.rs`), `peer_sharing/`, `n2c/`. |

---

### `ade_plutus`

| Attribute | Value |
|-----------|-------|
| **Purpose** | Quarantine boundary between the Ade-canonical ledger and the ported UPLC evaluator from `aiken-lang/aiken` (pinned tag `v1.1.21`). (Unchanged across the N-AO span.) |
| **Creates** | `PlutusScript`, `PlutusLanguage`, `EvalOutput`, `PlutusError`, `CostModels`, `DecoderMode`, `PerScriptResult`, `TxEvalResult`. 8 public types. |
| **Interprets** | UPLC scripts (Plutus V1/V2/V3) and `CostModels` CBOR; phase-two tx evaluation. `PlutusScript::from_cbor` is a named ingress chokepoint. |
| **MUST NOT** | (1) Re-export any `pallas_*` or `aiken_uplc::` type. (2) Let another BLUE crate bypass the canonical entry. (3) Activate PV11 builtins. (4) Use any BLUE-forbidden pattern. (5) Construct `PreservedCbor` outside `ade_codec`. |
| **Inbound deps** | `ade_ledger`, `ade_testkit`. |
| **Outbound deps** | `ade_types`, `ade_crypto`, `ade_codec`, `aiken_uplc` (git, tag `v1.1.21`), `pallas-primitives` (internal-only). |
| **Entry points** | `ade_plutus::eval_tx_phase_two`, `ade_plutus::tx_eval::*`, `ade_plutus::evaluator::*`, `ade_plutus::cost_model::*`. |
| **Key modules** | `evaluator.rs`, `cost_model.rs`, `script_context.rs`, `script_verdict.rs`, `tx_eval.rs`. |

---

### `ade_types`

| Attribute | Value |
|-----------|-------|
| **Purpose** | Owns the canonical Cardano domain vocabulary — primitives, era enum, address forms, era-tagged tx bodies/outputs/certificates, governance types — the lingua franca for every other crate. `CardanoEra` carries `is_praos()`. **N-F-G-J (carried)** owns the closed sum `PrevHash = Genesis | Block(Hash32)` — the parent-link type the PHASE4-N-AO LCA walk + fork-switch link checks pattern-match (`PrevHash::Block(h) if *h == expected_prev`). `Hash32` / `SlotNo` / `BlockNo` are the field types of the AO `ForkAnchor` / `Point` / `RangeRefetch` records. (No `ade_types` change in the N-AO span — 82 byte-stable.) |
| **Creates** | `CardanoEra` (with `is_praos()`), `SlotNo`, `BlockNo`, `EpochNo`, `Hash28`, `Hash32`, `Coin`, `Lovelace`, `NetworkId`, `Nonce`, `TxIn`, `RewardAccount`, `PoolId`, `Address`, `Credential`, `Certificate`, `ConwayCert`, `DRep`, `GovAction`, `OperationalCert`, per-era tx-body/tx-out/witness wrappers, `ShelleyBlock`/`ShelleyHeader`/`VrfData`/`ProtocolVersion`/`PrevHash`. 82 public types. |
| **Interprets** | None — produce-only. |
| **MUST NOT** | (1) Construct or decode `PreservedCbor`. (2) Use any BLUE-forbidden pattern. (3) Depend on any workspace crate. (4) Add open/extensible variants to closed enums without a versioned gate (`CardanoEra` discriminants closed: Byron=0 … Conway=7; `PrevHash` is a closed 2-variant `Genesis | Block(Hash32)` sum, never a flat `Hash32`). |
| **Inbound deps** | Every other workspace crate. |
| **Outbound deps** | None. |
| **Entry points** | `ade_types::CardanoEra` (incl. `is_praos`), `ade_types::{Hash32, SlotNo, Hash28, BlockNo, EpochNo, Nonce}`, `ade_types::conway::{tx::ConwayTxBody, cert::*, governance::*}`, `ade_types::shelley::block::{OperationalCert, ProtocolVersion, ShelleyHeader, ShelleyBlock, VrfData, PrevHash}`. |
| **Key modules** | `primitives.rs`, `era.rs`, `tx.rs`, `address/`, `byron/`, `shelley/`, `allegra/`, `mary/`, `alonzo/`, `babbage/`, `conway/`. |

---

## GREEN Modules — Deterministic Glue

> Deterministic, non-authoritative. May depend on BLUE; must not affect authoritative outputs. GREEN sub-trees inside the RED `ade_runtime` and `ade_node` crates carry a `//! GREEN …` banner (or the deterministic `// Core Contract:` banner) + the same deny attributes and are CI-gated for purity. **PHASE4-N-AO added four GREEN-by-content `ade_node` modules** (`candidate_aggregator`, `lca_walk`, `selector_state`, `post_switch_continuity`) + the GREEN-pure prove core `fork_switch::prevalidate_branch` (full per-module entries below). The aggregator's TCB color (OQ-AO-6) was **resolved GREEN** by the S2 proof — it is a pure relabel/group/project of already-BLUE-validated data, with no store read, no materialize, no selection, no mutation.

### `ade_node::candidate_aggregator` *(GREEN by content — PHASE4-N-AO S2, `DC-NODE-35`)*

| Attribute | Value |
|-----------|-------|
| **Purpose** | The BLUE-safe candidate construction core: a PURE projection that, given a fork anchor + the chain-dep AT that anchor + a peer's candidate header inputs, validates each header through the BLUE `validate_and_apply_header` authority and assembles a `CandidateFragment` for `select_best_chain` (consumed live in S3). The cluster's load-bearing entry-gate module — it keeps the SELECT from becoming "look over the peer's shoulder." |
| **Creates** | `CandidateBuildError` (closed: `HeaderInvalid { index, error } \| EmptyHeaders`). `build_candidate_fragment` / `assemble_candidate_set` produce / order the BLUE-owned `CandidateFragment` (it CREATES no canonical type — `CandidateFragment` / `TiebreakerView` are `ade_core` BLUE types). Not canonical-counted (GREEN). |
| **MUST NOT** | Affect authoritative outputs; introduce nondeterminism (no wall-clock / rand / float / HashMap; the candidate-set order is a total deterministic sort key, NOT the Praos preference — that lives in `select_best_chain`). **`DC-NODE-35`:** mint a `ValidatedHeaderSummary` — every summary in a fragment MUST be `validate_and_apply_header` output, NEVER a peer-trusted minted summary (the forbidden `ade_core_interop::follow` shape) and NEVER a raw followed-peer tip; a header that does not fully validate produces NO fragment (`HeaderInvalid` fails closed); zero headers fail closed (`EmptyHeaders`). Perform NO store read, NO materialize, NO selection, NO block-fetch, NO WAL, NO durable mutation (the GREEN boundary — the RED S3 driver supplies `anchor` + `anchor_chain_dep` from a read-only `materialize_rolled_back_state` at Ade's durable stored fork anchor; peer-supplied fork state MUST never reach this core). |
| **Inbound deps** | `ade_node::node_lifecycle` (`dispatch_competing_fork_choice` calls `build_candidate_fragment` + `assemble_candidate_set`). |
| **Outbound deps** | BLUE `ade_core::consensus::{candidate::{CandidateFragment, TiebreakerView}, header_validate::validate_and_apply_header, header_summary::HeaderInput, events::{BlockDistance, Point}, praos_state::PraosChainDepState, ledger_view::LedgerView, era_schedule::EraSchedule, errors::HeaderValidationError}`; `ade_types::BlockNo`. No tokio (GREEN). |
| **Entry points** | `ade_node::candidate_aggregator::{build_candidate_fragment, assemble_candidate_set, CandidateBuildError}`. |

> **GREEN classification (load-bearing, OQ-AO-6 resolved):** `ade_node::candidate_aggregator` is **promotable to BLUE on demand, never demotable to RED** — it is BLUE-adjacent pure semantic projection that the cluster deliberately kept GREEN because it constructs nothing the BLUE authority did not already validate.

### `ade_node::lca_walk` *(GREEN by content — PHASE4-N-AO S7, `DC-NODE-38`; retention S13 `DC-NODE-40`)*

| Attribute | Value |
|-----------|-------|
| **Purpose** | The last-common-ancestor fork-anchor walk: `walk_to_durable_lca` walks a live competing branch's preserved parent links back to the durable LCA (a `ChainDb`-stored block) so a multi-block competing branch is eligible for SELECT. PURE-deterministic over the per-peer branch cache + the S13 rollback-retention + read-only `ChainDb` lookups. The live-geometry gap CE-AO-6 surfaced (a competing block's immediate parent is an intermediate block Ade never stored). |
| **Creates** | `CachedHeader { header: HeaderInput, prev_hash: PrevHash, block_hash: Hash32 }` (a received competing-branch header preserved for the walk; `block_hash` is the re-derived hash, the self-binding map key), `LcaError` (closed: `NoDurableAncestorWithinK \| BranchGap \| ExceededK \| CacheSelfBindingViolation`), `LcaResult { anchor_slot, anchor_hash, headers }`. Not canonical-counted (GREEN). |
| **MUST NOT** | Affect authoritative outputs; introduce nondeterminism (no wall-clock / rand / float; `BTreeMap`-keyed, so arrival-order-independent — the live analog of `CN-CONS-01`); mutate any durable state (read-only `ChainDb` lookups only). **`DC-NODE-38`:** treat the branch cache as authority — it is an indexed memory of received preserved headers ONLY; each entry MUST self-bind (`block_hash == map key`) or fail closed (`CacheSelfBindingViolation`); the LCA is authoritative ONLY when `ChainDb` confirms **slot AND hash** (`DC-NODE-29`), never the competing block's immediate parent, never peer-supplied, never slot-only/hash-only; the walk MUST be k-bounded by BLOCK DEPTH (traversed-header count), never slot distance; over-k / branch-gap / no-durable-LCA / cache-self-binding-violation / lying-parent-link-to-genesis all fail closed with no durable mutation. **`DC-NODE-40` (S13):** a retained rolled-back block is walk-visible EVIDENCE only — it provides intermediate HOPS, NEVER the LCA anchor (the anchor stays `ChainDb`-durable only — `retained_blocks_are_not_anchors`), NEVER durable authority / a rollback target, and is k-bounded + self-binding identically; retention MUST NOT paper over a genuine gap (a bridge in neither cache nor retention nor durable still returns `BranchGap`). |
| **Inbound deps** | `ade_node::node_lifecycle` (`dispatch_competing_fork_choice` calls `walk_to_durable_lca`); `ade_node::fork_switch` (imports `LcaError` for `map_lca_error`). |
| **Outbound deps** | `ade_core::consensus::header_summary::HeaderInput`; `ade_runtime::chaindb::ChainDb` (read-only `get_block_by_hash`); `ade_types::shelley::block::PrevHash`; `ade_types::{Hash32, SlotNo}`; `std::collections::BTreeMap`. No tokio (GREEN). |
| **Entry points** | `ade_node::lca_walk::{walk_to_durable_lca, CachedHeader, LcaError, LcaResult}`. |

> **GREEN classification:** `ade_node::lca_walk` is **promotable to BLUE on demand, never demotable to RED** — it is pure deterministic geometry over self-bound evidence + the durable store, performing read-only lookups only.

### `ade_node::selector_state` *(GREEN by content — PHASE4-N-AO S3, `DC-NODE-36`; winner_tip S6)*

| Attribute | Value |
|-----------|-------|
| **Purpose** | The selector-state projection for live fork-choice dispatch: `project_tiebreaker` derives a block's `TiebreakerView` **from Ade's own already-admitted durable tip bytes** (local durable authority — not peer-derived, not revalidated, not minted) so the current tip and the S2-validated candidates compare on the same `praos_leader_value` basis `validate_and_apply_header` records. Also owns the provisional fork-choice carrier types S3 emits toward S4. PURE. |
| **Creates** | `SelectorProjectError` (closed: `UnsupportedTpraosTip`), `ForkAnchor { slot, hash, block_no }` (the durable-bound fork point — bound to Ade's durable STORED `(slot, hash, block_no)` by the RED driver via `get_block_by_hash`, never peer-supplied, `DC-NODE-29`), `PendingForkSwitch { fork_anchor, winning_peer, winning_candidate: CandidateFragment, winner_tip: Point }` (the PROVISIONAL S3 decision consumed by S4 — `winner_tip` is the BlockFetch `RequestRange` upper endpoint ONLY, NOT adoption authority). Not canonical-counted (GREEN). |
| **MUST NOT** | Affect authoritative outputs; introduce nondeterminism (no wall-clock / rand / float / HashMap). **`DC-NODE-36`:** a `PendingForkSwitch` is the provisional decision S3 emits — S3 MUST set it + the `DC-NODE-28` forge fence but **apply NOTHING** (no body-fetch, no rollback-commit, no `WalEntry::RollBack`); the current durable chain is preserved until S4 fetches + validates the replacement branch (FC-6); `winner_tip` is a fetch endpoint ONLY — the fetched body MUST still bind to the S3-selected `ValidatedHeaderSummary` and pass S4 `prevalidate_branch` before any rollback (a peer that serves a different body for the endpoint is rejected by S4 `BodyHeaderMismatch`, not adopted); `project_tiebreaker` MUST project from Ade's OWN durable tip bytes (local authority), and a legacy TPraos tip fails closed (`UnsupportedTpraosTip`) rather than project an inconsistent tiebreaker; the `ForkAnchor` MUST bind Ade's durable STORED slot+hash (`DC-NODE-29`), never peer-supplied. |
| **Inbound deps** | `ade_node::node_lifecycle` (`dispatch_competing_fork_choice` projects the tip + emits a `PendingForkSwitch`); `ade_node::fork_switch` (imports `ForkAnchor` / `PendingForkSwitch`). |
| **Outbound deps** | BLUE `ade_core::consensus::{candidate::{CandidateFragment, TiebreakerView}, events::Point, header_summary::{HeaderInput, HeaderVrf}, praos_leader_value}`; `ade_types::{BlockNo, Hash32, SlotNo}`. No tokio (GREEN). |
| **Entry points** | `ade_node::selector_state::{project_tiebreaker, ForkAnchor, PendingForkSwitch, SelectorProjectError}`. |

### `ade_node::post_switch_continuity` *(GREEN by content — PHASE4-N-AO S10, `DC-EVIDENCE-05`)*

| Attribute | Value |
|-----------|-------|
| **Purpose** | The replayable post-switch branch-continuity verdict: a pure, total, deterministic reducer over the closed convergence-evidence transcript that, after a `ForkChoiceWin` adoption at tip X, classifies Ade's OWN validated admitted-block lineage into a closed `PostSwitchContinuity` verdict. The SINGLE implementation behind both the hermetic replay test and the live CE-AO-6 release gate (`ci/ci_check_post_switch_convergence_window.sh` invokes the `post_switch_continuity` bin) — no Rust/Python drift. `evaluate_release_window` layers the DC-EVIDENCE-04 bounded-window acceptance terminal on top. |
| **Creates** | `EventView` (one parsed transcript event — only the continuity-relevant fields), `PostSwitchContinuity` (closed: `ContinuesSelectedBranch { admitted_after_switch, tip_slot, tip_hash } \| Diverged \| BrokenLineage \| DanglingForkChoiceWin \| InsufficientEvidence`). Not canonical-counted (GREEN). |
| **MUST NOT** | Affect authoritative outputs; introduce nondeterminism (no wall-clock / rand / float / HashMap; replay-equivalent — same events ⇒ byte-identical verdict). **`DC-EVIDENCE-05`:** be consumed by `select_best_chain` / `walk_to_durable_lca` / `apply_fork_switch` / `pump_block` / the forge fence — it is OBSERVE-ONLY (CI + `/cluster-close` use it as the release gate; the node never uses it to select chains); read anything but Ade's OWN admitted-block lineage — the peer's tip is NEVER an input to the verdict (`prev_hash` is the admitted block's VALIDATED header field, the same parent link S7's walk consumes, never peer-supplied); emit a free-form-string verdict (closed sum). `ContinuesSelectedBranch` REQUIRES unbroken `prev_hash` lineage from X across every post-X `block_admitted`, no `diverged` after X, and every `fork_choice_selected{win}` paired to a terminal. |
| **Inbound deps** | `ade_node::bin::post_switch_continuity` (the thin transcript→verdict bin); CI (`ci_check_post_switch_convergence_window.sh`); the hermetic replay tests. |
| **Outbound deps** | the closed `admission_log` event vocabulary (for the discriminator strings); `std` only. No tokio (GREEN). |
| **Entry points** | `ade_node::post_switch_continuity::{derive_post_switch_continuity, evaluate_release_window, PostSwitchContinuity, EventView}`; the bin `ade_node::bin::post_switch_continuity`. |

### `ade_node::fork_switch::prevalidate_branch` *(GREEN-pure prove core — PHASE4-N-AO S4, `DC-NODE-37`)*

> The `fork_switch` module is **split-color**: the `prevalidate_branch` prove core (+ the closed outcome/error types + `fork_switch_fence_resolved` + `map_lca_error` + `range_refetch_should_retry`) is GREEN-pure and BLUE-reused; its `BranchBodySource` RED seam + the `NullBranchBodySource` / `PrefetchedBranchBodies` impls are RED (documented in the RED `ade_node` entry). The whole module is summarized here once.

| Attribute | Value |
|-----------|-------|
| **Purpose** | The fork-switch prove core. `prevalidate_branch` is PURE (no I/O, no store reads, no durable mutation): given the already-fetched bodies + the already-materialized read-only anchor state, it (1) BINDS each body's re-derived header to its S3-selected `ValidatedHeaderSummary` (field-match + recomputed body-hash; trusts nothing peer-asserted), (2) LINKS `body[0].prev_hash == fork_anchor.hash` and `body[i].prev_hash == hash(body[i-1])`, and (3) folds BLUE `block_validity` over the bodies from the materialized anchor — three proofs, all STRICTLY BEFORE the RED driver's irreversible `commit_rollback`. Also hosts the closed S11/S14 outcome types + the fence-resolution predicate. |
| **Creates** | `BranchProofError` (closed: `EmptyBranch \| BodyUnavailable{slot} \| BodyHeaderMismatch{index} \| BrokenParentLink{index} \| BodyInvalid{index} \| AnchorUnreachable`), `ProvenBlock { bytes, tip }`, `ProvenBranch { blocks }`, `ForkSwitchOutcome` (closed: `Adopted { new_tip, new_tip_prev } \| ProofFailed { error }`), `MissingBridgeReason` (closed S11: `BranchGap \| NoDurableAncestorWithinK \| ExceededK \| CacheSelfBindingViolation \| LcaUnreachable` + `as_str`), `RangeRefetch { peer, from_tip, to_descendant, fork_switch_id, reason }`, `RangeRefetchOutcome` (closed S14: `Admitted \| Unavailable \| ShortRange \| BodyHeaderMismatch \| ParentLinkMismatch \| ValidationFailed` + `as_str` + `is_admitted`), `PostSwitchFollow { winning_peer, adopted_tip, fork_switch_id }`, `MAX_RANGE_REFETCH_ATTEMPTS = 3`. Not canonical-counted (RED `ade_node` types). |
| **MUST NOT** | (the GREEN-pure prove core) Affect authoritative outputs except via the BLUE `block_validity` fold it reuses; introduce nondeterminism; perform I/O, read the store, or mutate any durable state (`prevalidate_branch` is PURE over its inputs — `// prevalidate_branch is PURE: no I/O, no store reads, no durable mutation`). **`DC-NODE-37` (the H-1 class at fork-choice scale):** the prove MUST STRICTLY PRECEDE the caller's `commit_rollback`; any non-`Valid` `block_validity` verdict, any body↔header mismatch, any broken parent link, an empty branch, a short branch (fewer bodies than selected headers), or an unreachable anchor MUST fail closed leaving ChainDb + LedgerState + PraosChainDepState **byte-unchanged**; trust NOTHING peer-asserted (re-derive the header + recompute the body hash; S3's summary carries no block hash). `fork_switch_fence_resolved` MUST clear the forge fence ONLY on a fully-resolved state (no pending decision, no unresolved `MissingBridge`, AND caught up — a proof failure HOLDS the fence). `MissingBridgeReason` / `RangeRefetchOutcome` are closed discriminants with NO free-form strings. |
| **Inbound deps** | `ade_node::node_lifecycle` (`prove_fork_switch` / `apply_fork_switch` call `prevalidate_branch`; the relay loop reads `ForkSwitchOutcome` / `MissingBridgeReason` / `RangeRefetch` / `PostSwitchFollow` / `range_refetch_should_retry` / `fork_switch_fence_resolved`). |
| **Outbound deps** | BLUE `ade_core::consensus::{candidate::CandidateFragment, era_schedule::EraSchedule, events::Point, header_summary::{HeaderVrf, ValidatedHeaderSummary}, ledger_view::LedgerView, praos_leader_value, praos_state::PraosChainDepState}`; `ade_crypto::vrf::VrfOutput`; BLUE `ade_ledger::block_validity::{block_validity, decode_block, BlockValidityVerdict, DecodedBlock}` + `state::LedgerState`; `ade_types::shelley::block::PrevHash`; `ade_types::{Hash32, SlotNo}`; the GREEN `ade_node::lca_walk::LcaError` + `selector_state::{ForkAnchor, PendingForkSwitch}`. (The RED seam impls add `std::collections::BTreeMap`.) No tokio in the prove core. |
| **Entry points** | `ade_node::fork_switch::{prevalidate_branch, ProvenBranch, ProvenBlock, BranchProofError, ForkSwitchOutcome, MissingBridgeReason, map_lca_error, RangeRefetch, RangeRefetchOutcome, PostSwitchFollow, MAX_RANGE_REFETCH_ATTEMPTS, range_refetch_should_retry, fork_switch_fence_resolved, BranchBodySource, NullBranchBodySource, PrefetchedBranchBodies, FetchError}`. |

### `ade_node::convergence_evidence` *(GREEN by content — PHASE4-N-AJ; fork-choice + range-refetch evidence PHASE4-N-AO S9/S14)*

| Attribute | Value |
|-----------|-------|
| **Purpose** | The GREEN closed-vocabulary convergence-evidence sink for the live Participant rollback-follow + SELECT path. `ConvergenceEvidenceSink` writes the closed `AgreementVerdict` vocabulary (`block_received` / `block_admitted` / `agreement_verdict`) + (PHASE4-N-AO) the closed fork-choice + range-refetch event sequence to a dedicated `--convergence-evidence-path` JSONL sink as an emit-only side-output. `ConvergenceEvidence` is the higher-level emitter (fingerprint-bound) the relay loop drives. **PHASE4-N-AO S1 fix (`6846d252`):** `emit_block_received(peer, slot, hash)` threads the PER-BLOCK origin peer (the evidence-attribution artifact that previously mislabelled every block to the first peer, masking working multi-peer SELECT). **PHASE4-N-AO S9/S14:** adds the observe-only `emit_needs_fork_choice` / `emit_lca_discovered` / `emit_candidate_fragment_built` / `emit_fork_choice_selected` / `emit_branch_fetch_{started,completed}` / `emit_branch_prevalidated` / `emit_fork_switch_{applied,failed,superseded}` / `emit_missing_bridge` / `emit_range_refetch_{started,completed}` taps. |
| **Creates** | `ConvergenceEvidenceSink` (over a `Box<dyn Write>` — RED file I/O underneath, no raw-writer accessor), `ConvergenceEvidence` (the fingerprint-bound emitter), `EvidenceEmitResult { Written \| Disabled \| FailedAndPoisoned }` (closed). The closed event vocabulary + `ForkChoiceResult` + `ForkChoiceEvidenceFailure` live in `admission_log::event`. Not canonical-counted (GREEN). |
| **MUST NOT** | Affect authoritative outputs; halt authority on a write failure (a write-failure poisons + marks incomplete via `FailedAndPoisoned`, NEVER swallowed, NEVER halts the authority path); **`DC-EVIDENCE-04`/`DC-NODE-41`:** add an open/wildcard event variant to the closed vocabulary (the 10 fork-choice discriminators + the 2 range-refetch discriminators EQUAL the emit-only allow-list); emit a free-form error string (`failure_code` is the closed `ForkChoiceEvidenceFailure`; the `MissingBridgeReason` / `RangeRefetchOutcome` reason is a closed discriminator `as_str`; the `fork_switch_id` is a bounded deterministic `blake2b` hex-prefix); be CONSUMED by `select_best_chain` / `walk_to_durable_lca` / `apply_fork_switch` / the forge fence (observe-only — the SELECT/apply decisions are made on Ade's authority outputs, never on the evidence); overstate semantic truth (`lagging` is evidence-only, never success); change the byte output when `--convergence-evidence-path` is absent; expose a raw-writer accessor. |
| **Inbound deps** | `ade_node::node_lifecycle` (the dispatch decide-half + the relay-loop apply-half emit taps); `ade_node::admission::runner`. |
| **Outbound deps** | the closed `ade_node::admission_log::{AdmissionLogEvent, AdmissionLogWriter, ForkChoiceResult, ForkChoiceEvidenceFailure}` vocabulary; the reused `AgreementVerdict` verdict vocabulary + `verdict::derive`; `ade_types::Hash32`; `std::io::Write`. |
| **Entry points** | `ade_node::convergence_evidence::{ConvergenceEvidenceSink, ConvergenceEvidence, EvidenceEmitResult}`. |

### `ade_network::session` *(GREEN by content — PHASE4-N-L; outbound segmentation N-AB)*

| Attribute | Value |
|-----------|-------|
| **Purpose** | The deterministic GREEN session reducer for the Cardano Ouroboros mux — `step` is the SOLE pure fn reducing `(SessionState, ByteChunkIn) -> Result<(SessionState', Vec<SessionEffect>), SessionError>` (CN-SESS-03), composing the BLUE `mux::frame` + `handshake::n2n_transition` + the closed `AcceptedMiniProtocol` registry + per-mini-protocol frame demux/reassembly (INBOUND one `DeliverPeerFrame` per COMPLETE CBOR item; OUTBOUND `handle_outbound` SEGMENTS a payload > `MAX_PAYLOAD`, fail-closed above `MAX_OUTBOUND_PAYLOAD_BYTES = 16 MiB`). GREEN-by-content — every file pure / deterministic / no tokio / SystemTime / Instant / rand. (Unchanged across the N-AO span.) |
| **Creates** | `SessionState` (type-state), `ConnectedState` (closed-sum-indexed `proto_buffers` — no HashMap), `HandshakeProgress`, `SessionEffect` (closed), `SessionError` (closed — incl. `ReassemblyBufferOverflow` + `OutboundPayloadTooLarge`), `AcceptedMiniProtocol` (the CLOSED registry — GREEN), `ByteChunkIn`, `HandshakeRole`, `FrameBuffer`. None canonical-counted. |
| **MUST NOT** | Affect authoritative outputs; introduce nondeterminism (no wall-clock / rand / float / HashMap). **CN-SESS-03:** `step` is the SOLE reduction entry. **CN-SESS-01/02:** frame encode/decode through the single BLUE `mux::frame`; handshake through the single `n2n_transition`. **DC-SESS-01:** a `Handshaking` session MUST NOT deliver any non-handshake frame. **CN-SESS-04:** one accumulating buffer per `AcceptedMiniProtocol`; one `DeliverPeerFrame` per COMPLETE CBOR item. **CN-SESS-05:** an outbound payload > `MAX_PAYLOAD` MUST be SEGMENTED (no truncate/drop/reorder; `concat(segments) == payload`; fail closed above the FIXED `MAX_OUTBOUND_PAYLOAD_BYTES = 16 MiB`). |
| **Inbound deps** | `ade_runtime::network::{mux_pump, n2n_dialer, n2n_listener}`, `ade_runtime::admission::wire_pump`, `ade_node::wire_only`, `ade_network::mux::transport`. |
| **Outbound deps** | BLUE `ade_network::{mux::frame, handshake}`; `ade_codec`; `ade_types`. No tokio (GREEN). |
| **Entry points** | `ade_network::session::{step, FrameBuffer, SessionState, ConnectedState, HandshakeProgress, SessionEffect, SessionError, AcceptedMiniProtocol, ByteChunkIn, HandshakeRole}`, `ade_network::session::handshake_driver::{run_n2n_handshake_initiator, run_n2n_handshake_responder}`. |
| **Key modules** | `session/core.rs`, `session/state.rs`, `session/demux.rs`, `session/event.rs`, `session/handshake_driver.rs`. |

> **GREEN classification (load-bearing):** `ade_network::session` is **promotable to BLUE on demand, never demotable to RED**.

### `ade_runtime::seed_consensus_merge` *(GREEN by content — PHASE4-N-F-A; eta0 threaded N-F-G-N)*

| Attribute | Value |
|-----------|-------|
| **Purpose** | The deterministic, no-I/O merge transform lifting a verified-bootstrap `LiveConsensusInputsCanonical` plus the minted anchor fingerprint + seed epoch into the BLUE single-map `SeedEpochConsensusInputs`, carrying the recovered Praos `epoch_nonce` (eta0) into the BLUE record's `epoch_nonce` field (T-REC-04). (Unchanged across the N-AO span.) |
| **Creates** | `SeedConsensusMergeError` (closed: `PoolMissingVrfKeyhash` / `PoolMissingStake`). Not canonical-counted (GREEN). |
| **MUST NOT** | Affect authoritative outputs. Introduce nondeterminism (`BTreeMap` only). Default-fill on a provenance gap. Carry the recovered eta0 as anything but verbatim (DC-CINPUT-03 — never a zero-eta0 fallback). |
| **Inbound deps** | `ade_runtime::{genesis_bootstrap, mithril_bootstrap}`. |
| **Outbound deps** | `ade_ledger::{consensus_view::PoolEntry, seed_consensus_inputs::SeedEpochConsensusInputs}`, `ade_runtime::consensus_inputs::LiveConsensusInputsCanonical`, `ade_core::consensus::praos_state::Nonce`, `ade_types`. |
| **Entry points** | `ade_runtime::seed_consensus_merge::{merge_seed_epoch_consensus_inputs, SeedConsensusMergeError}`. |

---

### `ade_runtime` GREEN-by-content sub-trees (carried)

> All carry a `//! GREEN …` banner inside the RED `ade_runtime` crate, the BLUE deny attributes, and a purity CI gate. Each is "promotable to BLUE on demand, never demotable to RED" unless noted.

| Sub-tree | Purpose | MUST NOT | CI gate |
|---|---|---|---|
| `producer::self_accepted_handoff` *(N-F-G-B)* | GREEN constructor-fenced carrier for a BLUE self-accepted forged block. | Carry an artifact that was not BLUE self-accepted; re-derive; affect authoritative outputs. | `ci_check_served_chain_handoff_fence.sh` |
| `consensus_inputs::protocol_params` *(N-F-G-A)* | GREEN cardano-cli pparams JSON → BLUE `ProtocolParameters` by integer arithmetic. | Use any float path; affect authoritative outputs; introduce nondeterminism. | `ci_check_recovered_ledger_pparams_sourced.sh` |
| `forward_sync::reducer` *(N-Y; recovered_anchor N-AK; recovered_eta0 N-AN)* | GREEN forward-sync lifecycle reducer; `AdmitPlan::durable` is the sole `AdvanceTip` emitter. Carries `ForwardSyncState.{recovered_anchor, recovered_eta0}`. The `recovered_eta0` is threaded into the PHASE4-N-AO `fork_switch::prove_fork_switch` read-only materialize too. | Emit `AdvanceTip` before durability (DC-SYNC-01); affect authoritative outputs; carry `recovered_eta0` as anything but the verbatim recovered eta0. | `ci_check_forward_sync_chokepoint_only.sh`, `ci_check_rollback_materialize_eta0.sh` |
| `producer::coordinator` *(N-Q)* | Pure coordinator for live producer-mode. | Own/store private signing material (CN-PROD-02); affect authoritative outputs. | `ci_check_producer_coordinator_no_secrets.sh` |
| `producer::producer_log` *(N-Q, ext. N-W)* | Closed-vocabulary producer evidence log. | Add an open variant; emit `BlockServed` for a non-served block (CN-PROD-04). | `ci_check_operator_evidence_manifest_schema.sh` |
| `producer::chain_evolution` *(N-T; N-F-G-J)* | Linear `ChainEvolution` typestate; `prev_hash() -> PrevHash`. | Mint `AcceptedBlock`; advance on disagreement; propose an all-zero genesis `Hash32` (CN-WIRE-09). | `ci_check_prevhash_single_wire_authority.sh` |
| `clock` *(N-K; N-F-G-A)* | GREEN clock trait + deterministic impl + pure slot projections. | `DeterministicClock` reads no wall-clock; `checked_millis_to_slot` uses no float / wall-clock. | `ci_check_clock_seam.sh` |
| `orchestrator::{mod, event, state, core}` *(N-K)* | GREEN core reducer + closed-vocabulary event + state. | tokio imports in core; open event vocabulary. | `ci_check_orchestrator_core_purity.sh` |
| `rollback::{cadence, …}` *(N-I/N-J/N-K)* | GREEN rollback adapter glue + snapshot cadence + persistent cache/writer. | Parallel snapshot cadence; affect authoritative rollback state. | `ci_check_persistent_writer_no_parallel_cadence.sh`, `ci_check_snapshot_cadence_purity.sh` |
| `seed_import` *(N-M-A)* | Single cardano-cli JSON UTxO dump → canonical seed entries. | Construct semantic types bypassing canonical decoders. | `ci_check_seed_import_closure.sh`, `ci_check_seed_import_full_preprod_support.sh` |
| `consensus_inputs` *(N-M-C; ext. N-F-G-A)* | Operator-extracted `LiveConsensusInputs` importer + pparams parser. | Treat the peer as runtime authority; overstate semantic truth. | `ci_check_live_consensus_inputs_closure.sh`, `ci_check_live_consensus_inputs_fingerprint.sh` |
| `admission::*` (GREEN reducer half + reused dial/pump) *(N-M-C; reused N-F-G-C; tag-24 N-F-G-O; rollback preserve N-AI; keep-alive N-AM)* | GREEN admission verdict/agreement reducer; the RED `admission::{dial_for_admission, run_admission_wire_pump}` reused by the live `--mode node` feed (incl. the keep-alive client). | Emit RED verdicts; skip reference scripts; treat `lagging` as success; emit any `AdmissionPeerEvent` from the keep-alive client. | `ci_check_admission_runner_closure.sh`, `ci_check_admission_no_red_verdicts.sh`, `ci_check_lagging_is_evidence_only.sh`, `ci_check_admit_replay_equivalence.sh`, `ci_check_keep_alive_wire_only.sh` |

> **Note on `bootstrap` (the `bootstrap_initial_state` authority, N-K; eta0 N-F-G-N; live-follow start N-AK; restore-before-materialize reorder N-AN):** it carries a `//! GREEN single bootstrap authority` banner but also hosts the **RED** A3b warm-start sidecar restore. Documented in the RED `ade_runtime` entry below. Unchanged by PHASE4-N-AO (the SELECT path is `ade_node`-side).

---

### `ade_node` GREEN-by-content sub-trees (carried)

| Sub-tree | Purpose | MUST NOT | CI gate |
|---|---|---|---|
| `forge_intent` *(N-F-F)* | GREEN pure tri-state forge-intent classifier. | Observe key contents; carry a third variant; carry a secret byte. | `ci_check_forge_intent_closed.sh` |
| `run_loop_planner` *(N-F-D; venue policy N-AG)* | GREEN pure lifecycle planner (closed `LoopStep`/`ForgeSlotStatus`/`VenuePolicy`). | Reference `SlotNo`; return a non-closed `LoopStep`; decide any authority; add an open `VenuePolicy` arm. | `ci_check_loop_planner_closed.sh`, `ci_check_single_producer_loop_continuation.sh` |
| `node_sync::forge_epoch_admission` *(N-F-G-A)* | GREEN pure epoch-boundary guard over the closed `ForgeEpochAdmission`. | Fabricate the candidate epoch; run after leadership; drive a nonce promotion. | `ci_check_node_forge_single_epoch_fail_closed.sh` |
| `live_log::{sched_event, sched_writer}` *(N-F-G-J; forge-base N-AH)* | GREEN closed emit-only feed/forge scheduling-event vocabulary `NodeSchedEvent`. | Alter control flow; add an open variant; be READ by any authority path. | `ci_check_node_sched_events_emit_only.sh`, `ci_check_live_transcript_forge_base.sh` |
| `ba02_evidence` *(N-F-C)* | GREEN BA-02 evidence correlator. | Treat a weaker signal as acceptance; recompute the forged hash; forge/admit/persist. | `ci_check_ba02_evidence_closed.sh`, `ci_check_ba02_evidence_manifest_schema.sh` |
| `convergence_evidence` *(N-AJ; fork-choice + range-refetch evidence N-AO S9/S14)* | GREEN closed-vocabulary convergence-evidence sink (`ConvergenceEvidenceSink` / `ConvergenceEvidence`). *(Full entry above.)* | Affect authoritative outputs; halt authority on a write failure; add an open variant; emit a free-form error string; be consumed by selection/walk/apply/fence. | `ci_check_convergence_evidence_vocabulary_closed.sh`, `ci_check_convergence_evidence_emit_only.sh`, `ci_check_fork_choice_evidence_closed.sh` |
| `candidate_aggregator` *(N-AO S2)* | GREEN BLUE-safe candidate construction (`build_candidate_fragment` / `assemble_candidate_set`). *(Full entry above.)* | Mint a `ValidatedHeaderSummary`; perform a store read / materialize / selection / mutation; non-deterministic ordering (DC-NODE-35). | `ci_check_candidate_construction_validated.sh` |
| `lca_walk` *(N-AO S7; retention S13)* | GREEN LCA fork-anchor walk (`walk_to_durable_lca` + `CachedHeader`). *(Full entry above.)* | Treat the cache/retention as authority; anchor on a non-durable block; exceed the block-depth k; mutate durable state (DC-NODE-38/40). | `ci_check_lca_anchor_walk.sh`, `ci_check_rollback_retention_evidence.sh` |
| `selector_state` *(N-AO S3)* | GREEN selector-state projection (`project_tiebreaker` + `ForkAnchor`/`PendingForkSwitch`). *(Full entry above.)* | Apply a `PendingForkSwitch` in S3; bind the `ForkAnchor` to peer data; project from non-local bytes (DC-NODE-36). | `ci_check_live_selector_dispatch.sh` |
| `fork_switch::prevalidate_branch` *(N-AO S4; S11/S14 types)* | GREEN-pure fork-switch prove core + closed outcome/error types + the fence-resolve predicate. *(Full entry above; RED seam below.)* | Commit before prove; abandon the durable chain on a failed/incomplete branch; trust peer-asserted fields; free-form discriminants (DC-NODE-37/39/41). | `ci_check_fork_switch_never_abandons.sh`, `ci_check_missing_bridge_fail_closed.sh`, `ci_check_missing_bridge_refetch.sh` |
| `post_switch_continuity` *(N-AO S10)* | GREEN replayable post-switch branch-continuity reducer + closed verdict. *(Full entry above.)* | Be consumed by any authority path; read the peer tip; emit a free-form verdict (DC-EVIDENCE-05). | `ci_check_post_switch_convergence_window.sh` |
| `admission_log` *(N-M-B; fork-choice + range-refetch vocab N-AO S9/S14)* | GREEN admission-mode JSONL event vocabulary + writer (closed enum; the 10 fork-choice + 2 range-refetch + `ForkChoiceResult` + `ForkChoiceEvidenceFailure` variants). | Add an open/wildcard event variant; emit a free-form error string. | `ci_check_admission_log_vocabulary_closed.sh`, `ci_check_fork_choice_evidence_closed.sh` |
| `live_log` *(N-L-LIVE)* | GREEN closed JSONL vocabulary for the wire-only live pass. | Add an open variant; overstate semantic truth. | `ci_check_wire_only_event_vocabulary_closed.sh` |

> **Note on `node_sync` / `node_lifecycle` color (carried + N-AO):** `node_sync` + `node_lifecycle` are **RED** (they drive the apply path / own the persistent-store reads + the live-feed forge wrap + the serve sibling + the PHASE4-N-AO fork-switch DRIVER). Their GREEN-by-function decisions (`forge_epoch_admission`, `forge_header_position`, `may_cold_start_forge`, `single_producer_forge_decision`, `classify_receive`, `resolve_disposition`, `pending_reselection_forge_refusal`, `durable_tip_matches`, …) carry GREEN-pure semantics within the RED host modules. **PHASE4-N-AO:** `dispatch_competing_fork_choice` / `prove_fork_switch` / `apply_fork_switch` / `recover_missing_range` / `prefetch_branch_bodies` are the RED drivers (documented in the RED `ade_node` entry); they CALL the GREEN `candidate_aggregator` / `lca_walk` / `selector_state` / `fork_switch::prevalidate_branch` cores and the BLUE `select_best_chain` / `materialize_rolled_back_state` / `pump_block` authorities. `ade_testkit::consensus::genesis_pinning` is GREEN `#[cfg(test)]` test infrastructure. |

---
### `ade_testkit` *(GREEN — deterministic test/corpus infrastructure; dev-dep / `#[cfg(test)]`-scoped)*

| Attribute | Value |
|-----------|-------|
| **Purpose** | GREEN deterministic TEST / CORPUS infrastructure for the workspace — the oracle/differential harness, the validity & tx-validity corpora, the consensus & governance replay corpora, the producer reference vectors, the snapshot/genesis/ledger diff loaders, and the `consensus::ledger_view_stub::LedgerViewStub` the PHASE4-N-AO `fork_switch` / `candidate_aggregator` hermetic tests exercise. The home of `replay_cmd = cargo test -p ade_testkit`. (Unchanged across the N-AO span.) |
| **Creates** | GREEN test/corpus value types — NONE canonical-counted: `ConwayValidityCorpus` + loaders, `BlockReplay` / `TxReplay`, the adversarial mutation enums, `OracleManifest` / `RegressionCorpus` + validators, `DifferentialReport`, `Transcript`, `LedgerViewStub` / `EpochStakeFixture` / `PoolFixture`, `VrfReferenceVector` / `KesReferenceVector`. All GREEN. |
| **Interprets** | Committed reference corpora + oracle dumps, for the test harness only. Evidence inputs, never runtime authority. |
| **MUST NOT** | (1) **Be a production runtime dependency.** GREEN dev-dep / `#[cfg(test)]`-scoped — an INBOUND `[dev-dependencies]` of the BLUE/RED crates, NEVER in a production crate's plain `[dependencies]` (its sole plain edge is the RED-leaf `ade_core_interop`); MUST NOT be promoted onto a hashed/persisted/consensus path. (2) Affect authoritative outputs or introduce nondeterminism. (3) Construct semantic types bypassing canonical decoders. (4) Compare an Ade-internal-state fingerprint to a Haskell-serialized-state hash (DC-COMPAT-01). (5) Commit secret key material. (6) Overstate semantic truth. |
| **Inbound deps** | `ade_core` (dev), `ade_ledger` (dev), `ade_network` (dev), `ade_runtime` (dev), `ade_node` (dev), `ade_core_interop` (plain — the RED-leaf interop/test driver). |
| **Outbound deps** | `ade_core`, `ade_ledger`, `ade_plutus`, `ade_crypto`, `ade_codec`, `ade_types`, `ade_runtime`, `cardano-crypto`, `blake2`, `ed25519-dalek`, `serde`, `serde_json`, `toml`, `flate2`, `tar`. |
| **Entry points** | `ade_testkit::validity::*`, `ade_testkit::tx_validity::*`, `ade_testkit::consensus::{corpus::*, stream_replay::*, ledger_view_stub::{LedgerViewStub, EpochStakeFixture, PoolFixture}}`, `ade_testkit::governance::*`, `ade_testkit::harness::*`, `ade_testkit::mempool::ingress_replay::*`. |
| **Key modules** | `consensus/` (`corpus.rs`, `stream_replay.rs`, `genesis_pinning.rs`, `ledger_view_stub.rs`), `governance/`, `harness/`, `mempool/ingress_replay.rs`, `producer/`, `tx_validity/`, `validity/`. |

> **GREEN classification (load-bearing):** `ade_testkit` is GREEN test/corpus infrastructure, **never promotable onto a production runtime path**.

---

## RED Modules — Imperative Shell

> I/O, network, storage, clocks, retries. May depend on BLUE/GREEN. Must not modify core state directly or construct semantic types unsafely.

### `ade_node`

| Attribute | Value |
|-----------|-------|
| **Purpose** | The node binary + library entry. Owns argv parsing, the node lifecycle, and the mode drivers dispatched by `main()`: `--mode wire_only`, `--mode admission`, `--mode key_gen_kes`, `--mode produce`, and `--mode node` (`node_lifecycle`, the single `PHASE4-N-F-C-LIFECYCLE-OWNER`). Carries the full N-F-C…N-AL `--mode node` live surface. **PHASE4-N-AO (NEW — the live multi-candidate SELECT driver):** `node_lifecycle` gains the RED fork-switch drivers — `dispatch_competing_fork_choice` (caches the competing header self-bound, walks `walk_to_durable_lca` consulting the S13 `rollback_retention`, builds the candidate fragment via a read-only `materialize_rolled_back_state` at the durable anchor, routes the candidate SET to `select_best_chain`, emits a provisional `PendingForkSwitch` on a win + emits the S9 observe-only decide-half evidence, and on a walk failure sets the S11 `pending_missing_bridge` floor hold + an S14 `pending_range_refetch` for an eligible winning-peer descendant), `prove_fork_switch` (RED fetch of the winner's bodies via the `BranchBodySource` seam + a read-only `materialize_rolled_back_state` at the fork anchor + the GREEN `prevalidate_branch` — PURE, no durable mutation), `apply_fork_switch` (proves FIRST; adopts a proven branch via `apply_chain_event` `RolledBack + ChainSelected×N` recorded as `WalEntry::RollBack{ForkChoiceWin}`, captures the rolled-back blocks into `rollback_retention` BEFORE the rollback, then clears the fence LAST; a proof failure HOLDS the fence), `recover_missing_range` (the S14 `pump_block` sole-admit range-recovery loop, per-block `block_admitted` evidence), and `prefetch_branch_bodies` (the S6 async `BlockFetch RequestRange` byte-only prefetch into `PrefetchedBranchBodies`). `ForgeActivation` gains the fork-switch lifecycle state `pending_missing_bridge` / `post_switch_follow` / `pending_range_refetch` (alongside `pending_fork_switch` / `pending_reselection`) + the `rollback_retention` cross-iteration cache. `spawn_live_wire_pump_source` is rewired for the S8 per-peer-lane `fair_merge` feed. **NO new BLUE type, NO new CLI flag** — the SELECT is wired entirely in RED/GREEN reusing the BLUE core. |
| **Creates (RED-only)** | `Cli` (incl. `peer_addrs`, `network_magic`), `CliError`, `ProduceCli`, `KeyGenKesCli`, `Mode` (closed: `WireOnly`/`Admission`/`KeyGenKes`/`Produce`/`Node`), `NodeStartupInputs`, `NodeShutdownEvidence`, `NodeRunError`, exit-code constants. `node_lifecycle` (RED): `NodeStart` (closed), `NodeLifecycleError` (closed), `ForgeActivation<'a>` (incl. `last_forge_refused`, `pending_reselection`, **and the PHASE4-N-AO fork-switch lifecycle state `pending_fork_switch: Option<PendingForkSwitch>`, `pending_missing_bridge: Option<MissingBridgeReason>`, `post_switch_follow: Option<PostSwitchFollow>`, `pending_range_refetch: Option<RangeRefetch>`, `last_fork_switch_failure: Option<BranchProofError>`**), `run_relay_loop` / `run_relay_loop_with_sched`, `spawn_live_wire_pump_source` (S8: per-peer lanes + `fair_merge`), `wire_pump_start_point`, `run_node_serve_task`, `bind_serve_listener`, `apply_chain_event` / `run_participant_sync` / `declare_participant_venue` + `AppliedTip` / `ApplyError`, **the PHASE4-N-AO RED drivers `dispatch_competing_fork_choice`, `prove_fork_switch`, `apply_fork_switch`, `recover_missing_range`, `prefetch_branch_bodies` + `map_branch_proof_failure`**. `node_sync` (RED): closed `NodeBlockSource { WirePump, InMemory }`, the closed `NodeSyncItem { Block { peer, bytes } \| RollBack(Point) }` (**PHASE4-N-AO S1: `Block` now carries the origin `peer` — `DC-NODE-34`**), `NodeSyncError`, the GREEN-by-function classifiers, `ForgeMode` / `VenueRole` / `ReceiveClass` / `ReceiveDisposition` / `CandidateSummary` / `ForgeRefused` / `NodeForgeOutcome`, `MAX_WIRE_PUMP_LOOKAHEAD`. **PHASE4-N-AO new modules (RED `ade_node`, NOT canonical-counted):** the GREEN `candidate_aggregator` / `lca_walk` / `selector_state` / `post_switch_continuity` (+ bin) + the split-color `fork_switch` (GREEN prove core + the **RED `BranchBodySource` seam, `NullBranchBodySource`, `PrefetchedBranchBodies`**) + the RED `fair_merge`. The closed `admission_log` fork-choice + range-refetch vocabulary (`AdmissionLogEvent` variants + `ForkChoiceResult` + `ForkChoiceEvidenceFailure`). |
| **Interprets** | argv (closed mode set); operator key/genesis/opcert file paths; evidence-manifest TOML schema; a peer's tag-24-wrapped BlockFetch `MsgBlock` via `ade_codec::unwrap_tag24`; the live receive path interprets each peer item as a `NodeSyncItem` (now peer-tagged) and routes a Participant `NeedsForkChoice` into `dispatch_competing_fork_choice`. **PHASE4-N-AO:** `prove_fork_switch` interprets the winning peer's fetched branch bodies (decode + body-hash + `prev_hash`) against the S3-selected summaries + the materialized anchor state; `recover_missing_range` interprets a re-fetched range in parent-link order; the `post_switch_continuity` bin interprets the closed convergence-evidence JSONL into a `PostSwitchContinuity` verdict. |
| **MUST NOT** | All carry-forward `--mode node` prohibitions (closed mode set, single lifecycle owner via `bootstrap_initial_state`, durable-tip ONLY via `pump_block`, content-blind planner, the serve-as-projection / forge-on-followed-tip / receive-idempotency / recovered-anchor DC-NODE-31/32/33 fences). **`run_node_serve_task` lifetime (DC-NODE-09 — PRESERVED):** the serve sibling stays up past feed-end. **`bootstrap_initial_state` is the single lifecycle owner (CN-NODE-01 — PRESERVED):** `node_lifecycle` MUST construct initial state ONLY via `bootstrap_initial_state` and MUST NOT call `materialize_rolled_back_state` for INITIAL-STATE construction — the ONLY permitted `materialize_rolled_back_state` calls in `node_lifecycle` are the N-AI live rollback-follow inside `apply_chain_event` AND **(PHASE4-N-AO) the read-only fork-point/fork-anchor materialize inside `dispatch_competing_fork_choice` / `prove_fork_switch` (a read-only materialize for the candidate chain_dep + the prevalidation anchor state, never an initial-state construction)**. **(PHASE4-N-AO) the SELECT driver fences:** **`DC-NODE-34` (S1)** — peer identity preserved end-to-end (`NodeSyncItem::Block.peer`); a single-peer FOLLOW run MUST replay byte-identically to the pre-S1 baseline (no selection/admission/rollback/verdict change). **`DC-NODE-36` (S3)** — the `NeedsForkChoice` arm MUST route the aggregated candidate SET to the SINGLE `select_best_chain` (no second selector / parallel preference / density ordering / operator heuristic); only `validate_and_apply_header`-derived summaries reach it; a `TiebreakerLossKeepCurrent` makes NO durable change; arrival-order-independent over the live multi-peer set. **`DC-NODE-37` (S4 — the H-1 class):** NEVER commit a rollback of the current durable chain until the replacement branch's bodies are FETCHED, LINKED, and VALIDATED as a complete candidate branch; a failed/lying/Byzantine/incomplete winner leaves ChainDb + LedgerState + PraosChainDepState UNCHANGED; adoption proceeds ONLY via `materialize_rolled_back_state` (`CN-STORE-07`) + lockstep `receive::reducer` (`DC-CONS-20`) + `pump_block` (`DC-NODE-05/12`), the fork anchor bound to Ade's durable STORED slot+hash (`DC-NODE-29`), recorded as the durable append-only `WalEntry::RollBack{ForkChoiceWin}` — NOT a second rollback implementation; no forge across the pending reselection (`DC-NODE-28`). **`DC-NODE-38` (S7)** — the fork anchor is the durable LCA on slot AND hash, k-bounded by block depth; the branch cache is evidence, not authority. **`DC-NODE-39` (S11)** — a descendant whose parent link Ade has not validated is NEVER admitted; a genuine missing bridge emits a closed `MissingBridge` discriminant, preserves the durable chain byte-unchanged, HOLDS the forge fence (`pending_missing_bridge.is_none()` is a fence-resolve precondition), and refuses the silent no-op; `MissingBridge` is NEVER a rollback target / candidate anchor / fence-clear reason / reason to skip the missing parent or trust the later block. **`DC-NODE-40` (S13)** — `rollback_retention` is walk-visible evidence only (k-bounded, hash-keyed, self-binding); NEVER durable authority / a rollback target / the LCA anchor / an S2-S4 bypass; the durable LCA stays `ChainDb`-stored only. **`DC-NODE-41` (S14)** — the range re-fetch is byte-only (a lying/short/unservable range leaves the structured hold, no admit, no mutation), targets ONLY the winning peer (no fetch spam on loser orphans), is bounded-retry (`MAX_RANGE_REFETCH_ATTEMPTS`, no spin), admits IN PARENT-LINK ORDER through `pump_block` (the sole admit), and clears the hold + fence ONLY on real admitted progress (`RangeRefetchOutcome::Admitted`); S14 is RECOVERY, never selection (S3 already decided the winner). **`DC-PUMP-04` (S8)** — never fan all peer pumps into one shared bounded channel (the pre-S8 starvation shape); never drop a peer's fork-choice-relevant block because another is hot; the merge order is RED scheduling discipline ONLY and MUST NOT decide fork-choice (`select_best_chain` stays arrival-order-independent); never let wall-clock / rand / scheduler timing define peer priority. **`DC-EVIDENCE-04/05`** — the convergence + continuity evidence is OBSERVE-ONLY, never consumed by selection/walk/apply/the fence; closed vocabulary, no free-form strings. |
| **Inbound deps** | None (binary + integration tests). |
| **Outbound deps** | `ade_types`, `ade_core` (incl. `consensus::{fork_choice::select_best_chain, header_validate::validate_and_apply_header, candidate, header_summary, events, praos_leader_value, praos_state, era_schedule, ledger_view}`), `ade_crypto` (incl. `vrf` + `blake2b` for the `fork_switch_id`), `ade_ledger` (incl. `block_validity::{block_validity, decode_block, BlockValidityVerdict, DecodedBlock}`, `state::LedgerState`, `rollback::materialize::materialize_rolled_back_state`, `wal::event::{WalEntry::RollBack, RollbackReason::ForkChoiceWin}`, `pump_block` via `forward_sync`), `ade_runtime` (incl. `chaindb::ChainDb` for the LCA walk + the fork-anchor `get_block_by_hash`; `admission::{dial_for_admission, run_admission_wire_pump, AdmissionPeerEvent}`; the BlockFetch `RequestRange` shells), `ade_network` (incl. `codec::chain_sync::Point`, `block_fetch::client`), `ade_codec`, `serde`, `serde_json`, `tokio`. Dev-deps: `ade_testkit` (incl. `consensus::ledger_view_stub::LedgerViewStub`), `tempfile`. |
| **Entry points** | `main()`; `ade_node::run_node_lifecycle` (`--mode node`); `ade_node::node_lifecycle::{run_relay_loop, run_relay_loop_with_sched, ForgeActivation, spawn_live_wire_pump_source, run_node_serve_task, bind_serve_listener, apply_chain_event, run_participant_sync, declare_participant_venue, AppliedTip, ApplyError, wire_pump_start_point, dispatch_competing_fork_choice, prove_fork_switch, apply_fork_switch, recover_missing_range, prefetch_branch_bodies}`; `ade_node::node_sync::{NodeBlockSource, NodeSyncItem, run_node_sync, classify_receive, resolve_disposition, ReceiveClass, ReceiveDisposition, NodeSyncError, ForgeRefused, NodeForgeOutcome}`; the PHASE4-N-AO GREEN cores `ade_node::{candidate_aggregator, lca_walk, selector_state, post_switch_continuity}::*` + the split-color `ade_node::fork_switch::*` + the RED `ade_node::fair_merge::{fair_merge, fair_recv, per_peer_delivered_counts, PER_PEER_LANE_CAP}`; `ade_node::convergence_evidence::{ConvergenceEvidenceSink, ConvergenceEvidence, EvidenceEmitResult}` (GREEN); `ade_node::admission_log::*`; `ade_node::run_loop_planner::*` / `live_log::*` / `forge_intent::*` (GREEN); `ade_node::operator_forge::*` / `ba02_pass::*` / `rehearsal_pass::*` / `produce_mode::*` (RED); `ade_node::cli::{Cli, ProduceCli, Mode}`; `ade_node::{admission, wire_only, key_gen}::*`. |
| **Key modules** | `lib.rs`, `cli.rs`, `node.rs`, `main.rs`, **`node_lifecycle.rs`** (the `--mode node` owner + `run_relay_loop` + the carried N-F-C…N-AL wiring + **PHASE4-N-AO: `dispatch_competing_fork_choice` / `prove_fork_switch` / `apply_fork_switch` / `recover_missing_range` / `prefetch_branch_bodies` + the `ForgeActivation` fork-switch lifecycle state + the relay-loop S4/S14 drive + the S8 `spawn_live_wire_pump_source` per-peer-lane feed**), `node_sync.rs` (+ **PHASE4-N-AO S1: `NodeSyncItem::Block.peer`**), **`candidate_aggregator.rs`** (GREEN, S2), **`lca_walk.rs`** (GREEN, S7/S13), **`selector_state.rs`** (GREEN, S3/S6), **`fork_switch.rs`** (GREEN prove core + RED seam, S4/S11/S14), **`fair_merge.rs`** (RED, S8), **`post_switch_continuity.rs`** + **`bin/post_switch_continuity.rs`** (GREEN, S10), `convergence_evidence.rs` (GREEN, N-AJ + N-AO evidence taps), `admission_log/` (GREEN, + N-AO fork-choice + range-refetch vocab), `forge_intent.rs` / `run_loop_planner.rs` / `ba02_evidence.rs` / `live_log/` (GREEN), `operator_forge.rs` / `ba02_pass.rs` / `rehearsal_pass.rs` / `produce_mode.rs` / `wire_only.rs` / `key_gen.rs` (RED), `admission/`. |

> **GREEN sub-classification (carried + N-AO):** `forge_intent`, `run_loop_planner`, `ba02_evidence`, `live_log::{sched_event,sched_writer}`, `convergence_evidence`, `rehearsal_evidence`, **`candidate_aggregator`, `lca_walk`, `selector_state`, `post_switch_continuity`, `fork_switch::prevalidate_branch`** are GREEN-by-content; `node_sync::{forge_epoch_admission, classify_receive, resolve_disposition, …}` + `node_lifecycle::may_cold_start_forge` are GREEN-by-function. `node_lifecycle` + `node_sync` + `operator_forge` + `ba02_pass` + `rehearsal_pass` are RED; the PHASE4-N-AO `fork_switch::{BranchBodySource, NullBranchBodySource, PrefetchedBranchBodies}` seam impls + `fair_merge` + the `node_lifecycle` fork-switch DRIVERS are RED.

### `ade_runtime`

| Attribute | Value |
|-----------|-------|
| **Purpose** | The imperative shell — moves bytes, owns sockets/files/clocks/keys, drives tokio tasks. Hosts: producer-mode key custody + shell composition; the N2N network drivers (incl. the **BlockFetch `RequestRange` shells the PHASE4-N-AO selected-range / range-refetch fetch rides on**); the node orchestrator runners; the receive/rollback/admission shells (incl. `admission::{dial_for_admission, run_admission_wire_pump}` reused by the live `--mode node` feed, with the N-AM keep-alive client); the seed-import/WAL/consensus-inputs importers; ChainDB (incl. the read-only `get_block_by_hash` the PHASE4-N-AO LCA walk + fork-anchor binding consume) + recovery shells; the durable-chain serve projection (`network::served_chain_projection`, bounded at `MAX_SERVE_RANGE_BLOCKS = 256`); the N-AE.F `pump_block` hash-exact idempotency gate (**reused as the PHASE4-N-AO sole admit for branch adoption + range recovery**); the recovered-anchor load + store surface. (No `ade_runtime` production source change in the PHASE4-N-AO span — the SELECT path is `ade_node`-side and reuses the existing `ade_runtime` shells; `git diff b8860b16..HEAD -- crates/ade_runtime/src/` is empty apart from any test files.) |
| **Creates (RED-only)** | `KesSecret`, `VrfSigningKey`, `ColdSigningKey` custody wrappers; `KeyLoadError`; `MuxTransportHandle` consumers; `OutboundCommand` (closed); `PerPeerOutbound`; `DispatchError` (closed); `ServerPeerStates`; `ServedChainHandle`; `ProducerShell`; `AdmissionPeerEvent` (closed — `Block { peer, .. } \| TipUpdate { peer, .. } \| RollBackward { peer, point, tip } \| Disconnected { peer }`; the per-variant `peer` label is the carrier the PHASE4-N-AO S1 `NodeSyncItem::Block.peer` threading preserves end-to-end); `AdmissionWirePumpError` (closed — incl. `KeepAlive(KeepAliveError)`); `KEEP_ALIVE_CADENCE`. Clock: `SystemClock`, `DeterministicClock`, `SlotAlignmentError`. consensus_inputs / bootstrap / serve plumbing: `LiveConsensusInputsCanonical`, `BootstrapState`, `BootstrapInputs`, `BootstrapError`, `ChainDbServedSource<'a>`, `ServedChainSource<'a>`, `CappedSlotRange`, `ServeRangeOutcome`, `MAX_SERVE_RANGE_BLOCKS = 256`. No new RED type in the PHASE4-N-AO span. |
| **Interprets** | Canonicalizes peer bytes/files for the BLUE core: the cold/VRF/KES loaders; `genesis_parser::parse_shelley_genesis`; `seed_import`; `admission::{dial_for_admission, run_admission_wire_pump}` (the N2N dial + chain-sync/block-fetch pump producing `AdmissionPeerEvent`, incl. the preserved `RollBackward` point + the keep-alive cookie). `bootstrap::restore_seed_epoch_consensus_inputs` reads the anchor-fp-keyed sidecar; `recovered_anchor::load_recovered_anchor_point` reads the recovered-anchor-point record. The BlockFetch `RequestRange` shells canonicalize a fetched range's bytes for the PHASE4-N-AO `prove_fork_switch` / `recover_missing_range` (which validate them via the BLUE authorities before any admit). |
| **MUST NOT** | (1) Modify BLUE state directly or construct semantic types from raw bytes. (2) Bypass canonical validation. (3) `producer::signing` (DC-CRYPTO-03/05) — RED-confined custody. (3b) `kes_sign_header_advancing` (DC-CRYPTO-10) — evolve before sign, fail-closed. (4)–(7) `producer::keys` / `coordinator` / `outbound_command` / per-peer outbound fences. (7b–7d) `network::serve_dispatch` / `n2n_listener` / `served_chain_projection::ChainDbServedSource` (DC-NODE-07 / DC-NODE-13/14 / DC-SERVEMEM-01) — READ-ONLY, bounded per-request. (8) `admission::wire_pump` keep-alive client (DC-PUMP-03) — reuse the BLUE grammar, wire-only, no `AdmissionPeerEvent`. **(9) `forward_sync::pump` / `pump_block` (DC-SYNC-01 / DC-NODE-16) — refuse to advance the tip before `StoreBlockBytes` + `AppendWal`; the already-have skip is HASH-keyed; it is the SOLE durable admit — PHASE4-N-AO's branch adoption + range recovery MUST go through it, never a parallel admit.** (10)–(16d) `mithril_import` / `genesis_bootstrap` / `recovery::restart` / `bootstrap::restore_seed_epoch_consensus_inputs` / `seed_consensus_provenance` / `chaindb` recovered-anchor surface / `recovered_anchor::load_recovered_anchor_point` / `resolve_live_follow_start` fences. (17)–(22) `producer::keys` / `admission::{dial,pump}` / feed tag-24 unwrap / `clock::checked_millis_to_slot` / `consensus_inputs` / rollback-signal-preservation fences. |
| **Inbound deps** | `ade_node`, `ade_core_interop`, `ade_testkit` (dev/integration). |
| **Outbound deps** | `ade_types`, `ade_core` (incl. `consensus::praos_state::{Nonce, PraosChainDepState::overlay_recovered_eta0}` transitively via `ade_ledger`), `ade_crypto`, `ade_codec` (incl. `unwrap_tag24`), `ade_ledger` (incl. `rollback::materialize::materialize_rolled_back_state`, `recovered_anchor_point::*`), `ade_network` (incl. `keep_alive` + `codec::keep_alive` + `block_fetch::client`), `redb`, `serde`, `serde_json`, `bech32`, `base58`, `cardano-crypto`, `ed25519-dalek`, `tokio` (dev-dep: `tokio` `["test-util"]`, `tempfile`, `ade_testkit`). |
| **Entry points** | `ade_runtime::producer::*`, `ade_runtime::admission::{dial_for_admission, run_admission_wire_pump, AdmissionPeerEvent, AdmissionWirePumpError}`, `ade_runtime::network::{n2n_listener::*, n2n_dialer::*, mux_pump::*, n2n_server::*, outbound_command::*, serve_dispatch::*, served_chain_projection::{ChainDbServedSource, ServeRangeOutcome, MAX_SERVE_RANGE_BLOCKS}, block_fetch shells}`, `ade_runtime::orchestrator::*`, `ade_runtime::bootstrap::{bootstrap_initial_state, BootstrapInputs, BootstrapState, BootstrapError}`, `ade_runtime::recovered_anchor::load_recovered_anchor_point`, `ade_runtime::seed_consensus_merge::*`, `ade_runtime::consensus_inputs::*`, `ade_runtime::{forward_sync (incl. pump::pump_block), mithril_import, genesis_bootstrap, mithril_bootstrap, recovery::restart, chaindb (incl. ChainDb::get_block_by_hash), clock, seed_import, wal, bootstrap_anchor, seed_epoch_lineage}::*`. |
| **Key modules** | `producer/`, `network/` (incl. `served_chain_projection.rs`, `serve_dispatch.rs`, `n2n_listener.rs`, the block-fetch shells), `orchestrator/`, `admission/` (incl. `wire_pump.rs` — the feed tag-24 unwrap + the N-AI `RollBackward` + the N-AM keep-alive client; `dial_for_admission` + `run_admission_wire_pump`), `receive/`, `rollback/`, `seed_import/`, `consensus_inputs/`, `wal/`, `chaindb/` (redb schema v3; the bounded serve primitives + `get_block_by_hash`), `forward_sync/` (incl. `pump.rs` — the DC-NODE-16 idempotency gate, the PHASE4-N-AO sole admit; `reducer.rs`), `mithril_import/`, `seed_consensus_merge.rs`, `seed_epoch_lineage.rs`, `mithril_bootstrap.rs`, `genesis_bootstrap.rs`, `recovery/`, `bootstrap.rs`, `recovered_anchor.rs`, `clock.rs`. |

> **RED sub-entries carried** (unchanged in shape across the N-AO span): `ade_runtime::network::serve_dispatch` *(RED — N-F-G-H)* and `ade_runtime::network::served_chain_projection` *(RED — N-U S3 / DC-NODE-13)*. The PHASE4-N-AO BlockFetch `RequestRange` / range-refetch fetch reuses the existing RED block-fetch shells; it adds no new serve-path surface.

---

### `ade_core_interop`

| Attribute | Value |
|-----------|-------|
| **Purpose** | Live cardano-node interop driver. Hosts the `live_*_session` RED binaries plus the N-E S4/S5 GREEN tx-submission bridges and the chain-follow driver (`follow`). `follow` strips the peer's ChainSync `RollForward` envelope via `ade_codec::unwrap_tag24`. **`follow.rs` is the FORBIDDEN minted-`ValidatedHeaderSummary` shape the PHASE4-N-AO `DC-NODE-35` gate forbids from reaching `select_best_chain`** (the cluster's candidate summaries come ONLY from `validate_and_apply_header`). (Unchanged across the N-AO span.) |
| **Creates (RED-only)** | Live-session drivers and transcript types; never semantic types. |
| **MUST NOT** | (1) Construct semantic types from raw bytes. (2) Be depended on by any BLUE/GREEN crate (RED leaf). (3) Overstate semantic truth in evidence. (4) `follow` (CN-WIRE-08) — strip via `ade_codec::unwrap_tag24`. **(5, cross-ref DC-NODE-35) the `follow.rs` minted summary shape MUST NOT cross into BLUE or any authority path / reach `select_best_chain`.** |
| **Inbound deps** | None (RED leaf — binaries). |
| **Outbound deps** | `ade_core`, `ade_codec`, `ade_crypto`, `ade_ledger`, `ade_runtime`, `ade_network`, `ade_testkit`, `ade_types`, `tokio`. |
| **Entry points** | `ade_core_interop::bin::{live_tx_submission_session, live_block_production_session, live_block_fetch_session, live_block_follow_session}`; `ade_core_interop::follow::*`; `ade_core_interop::{tx_submission, local_tx_submission}`. |
| **Key modules** | `bin/`, `follow.rs`, `tx_submission.rs`, `local_tx_submission.rs`. |

---

### `ade_network::mux::transport` *(RED)*

| Attribute | Value |
|-----------|-------|
| **Purpose** | The only place inside `ade_network` where socket I/O happens. RED tokio shell over the BLUE mux frame primitive and the GREEN session reducer. Provides `MuxTransportHandle` + closed `TransportError` + `DuplexCapacity::DEFAULT` + `spawn_duplex`. (Unchanged across the span.) |
| **Creates (RED-only)** | `MuxTransportHandle`, `TransportError`, `DuplexCapacity`, `MuxTransport`. |
| **MUST NOT** | (1) Construct semantic types. (2) Bypass `mux::frame` for framing. (3) Live in BLUE scope. |
| **Inbound deps** | `ade_runtime::network::{mux_pump, n2n_dialer}`. |
| **Outbound deps** | `ade_network::mux::frame` (BLUE), `ade_network::session` (GREEN), `tokio`. |
| **Entry points** | `ade_network::mux::transport::{MuxTransportHandle, spawn_duplex, open_tcp}`. |
| **Key modules** | `mux/transport.rs`. |

---

### `ade_network` *(RED capture binaries — non-session)*

| Attribute | Value |
|-----------|-------|
| **Purpose** | Operator-action capture binaries (live evidence harness). `ade_chain_sync_capture` (`--intersect-slot`/`--intersect-hash`) captures a real Conway `RollForward` golden fixture; `ade_keep_alive_capture` captures real keep-alive traffic. |
| **MUST NOT** | (1) Construct semantic types from raw bytes. (2) Overstate semantic truth in captured evidence. |
| **Outbound deps** | `ade_network` BLUE/GREEN submodules, `ade_codec`, `ade_types`, `tokio`. |

---
## Cross-Module Rules (project-wide)

### Dependency direction (RED → GREEN → BLUE; never outward)

```
ade_types     → {}
ade_codec     → {ade_types}
ade_crypto    → {ade_types, blake2, ed25519-dalek, cardano-crypto[vrf-draft03,kes-sum(test-only),dsign]}
ade_core      → {ade_types, ade_crypto, minicbor}   [BLUE; the SOLE chain-selector select_best_chain + validate_and_apply_header + candidate + rollback REUSED BYTE-UNCHANGED by PHASE4-N-AO (routed-to, never duplicated); 49→49 — ZERO new BLUE canonical type]
ade_ledger    → {ade_core, ade_plutus, ade_crypto, ade_codec, ade_types, minicbor, num-*}   [BLUE; block_validity + materialize_rolled_back_state (CN-STORE-07) + receive::reducer (DC-CONS-20) + the WalEntry::RollBack{ForkChoiceWin} marker (DC-NODE-27) REUSED BYTE-UNCHANGED by PHASE4-N-AO (prove + adopt authorities); RollbackReason::ForkChoiceWin now constructed live; 181→181 — ZERO new BLUE canonical type]
ade_plutus    → {ade_crypto, ade_codec, ade_types, aiken_uplc, pallas-primitives(internal)}
ade_network   → BLUE submodules → {ade_codec, ade_types}; session(GREEN) → +BLUE submodules; transport(RED) → +tokio   [no ade_network change in the N-AO span — the SELECT path reuses the existing BlockFetch + ChainSync grammar for RequestRange]
ade_testkit   → {ade_core, ade_ledger, ade_plutus, ade_runtime, ade_crypto, ade_codec, ade_types, cardano-crypto}   [GREEN]
ade_runtime   → {ade_core, ade_crypto, ade_codec, ade_types, ade_ledger, ade_network, redb, serde*, bech32, base58, cardano-crypto[vrf-draft03,dsign], ed25519-dalek, tokio}   [RED; no production source change in the N-AO span — the SELECT driver lives in ade_node and reuses ade_runtime's chaindb::get_block_by_hash, pump_block (the sole admit), and the BlockFetch RequestRange shells]
ade_node      → {ade_types, ade_core, ade_crypto, ade_ledger, ade_runtime, ade_network, ade_codec, serde, serde_json, tokio}   [RED; PHASE4-N-AO wires the ENTIRE live multi-candidate SELECT here — the GREEN candidate_aggregator / lca_walk / selector_state / post_switch_continuity + the split-color fork_switch (GREEN prove core + RED BranchBodySource seam) + the RED fair_merge + the node_lifecycle RED drivers (dispatch_competing_fork_choice / prove_fork_switch / apply_fork_switch / recover_missing_range / prefetch_branch_bodies) + the ForgeActivation fork-switch lifecycle state — all reusing the BLUE core. NodeSyncItem::Block now carries peer (DC-NODE-34). NO new BLUE/external dep]
ade_core_interop → {ade_core, ade_codec, ade_crypto, ade_ledger, ade_runtime, ade_network, ade_testkit, ade_types, tokio}   [RED leaf; follow.rs is the FORBIDDEN minted-summary shape — DC-NODE-35]
```

**Forbidden directions (carried + the N-AO span).** Any BLUE crate (or BLUE `ade_network` submodule) depending on `ade_runtime`, `ade_node`, `ade_core_interop`, or the RED half of `ade_network` is a CI failure (`ci_check_dependency_boundary.sh`). Any non-`ade_plutus` crate referring to `pallas_*` is a CI failure. A second block-envelope encoder, a second `leader_vrf_input` authority, a second `wrap_tag24`/`unwrap_tag24` definition, a forward-sync `AdvanceTip` before durability, a Mithril/genesis bootstrap path bypassing the single `bootstrap_initial_state`, an internal-fingerprint-vs-Haskell-hash equality assertion, a `WalEntry::RollBack` that implements a second rollback (N-AI), a second/hand-rolled `RecoveredAnchorPoint` codec (N-AK), a wire-pump keep-alive client that redefines the BLUE keep-alive grammar / emits any `AdmissionPeerEvent` (N-AM), a second eta0-overlay authority / a rollback-replay VRF bypass (N-AN), **and (PHASE4-N-AO): a SECOND chain selector or any live re-implementation / parallel preference / density ordering / operator heuristic alongside `select_best_chain` (`DC-NODE-36`); a RED-minted `ValidatedHeaderSummary` (the `follow.rs` shape) reaching `select_best_chain` (`DC-NODE-35`); a rollback of the current durable chain committed BEFORE the replacement branch's bodies are fetched + linked + validated (`DC-NODE-37` — the H-1 class); a fork anchor bound to a peer-supplied / slot-only / hash-only / non-durable point rather than Ade's durable stored slot+hash (`DC-NODE-29`/`38`); a second rollback implementation for the fork-switch adopt rather than reusing `materialize_rolled_back_state` + `commit_rollback` + the `WalEntry::RollBack{ForkChoiceWin}` marker (`DC-NODE-37`); admitting a post-switch descendant whose parent link Ade has not validated, or a silent post-switch stall instead of a structured `MissingBridge` hold (`DC-NODE-39`); a retained rolled-back block becoming durable authority / a rollback target / the LCA anchor (`DC-NODE-40`); a range re-fetch that admits out of parent-link order / bypasses `pump_block` / targets a non-winning peer / spins unbounded / clears the hold on a non-admitted outcome (`DC-NODE-41`); a multi-peer wire-pump that fans all peers into one shared bounded channel or lets scheduler/wall-clock/rand timing decide peer priority or affect any BLUE selection (`DC-PUMP-04`); a convergence/continuity-evidence event consumed by selection/walk/apply/the forge fence, or an open/free-form-string evidence variant (`DC-EVIDENCE-04`/`05`)** are all CI failures (the carried N-A…N-AN fences + the twelve N-AO gates).

**NEW (PHASE4-N-AO — live multi-candidate fork-choice SELECT + adopt):**
- **N-AO (`DC-NODE-37`, primary):** when `select_best_chain` picks a winner that forks BELOW Ade's current durable tip, Ade MUST NEVER commit a rollback of its current durable chain until the replacement branch's bodies are FETCHED (BlockFetch `RequestRange` anchor→tip from the winning peer), LINKED (`prev_hash` from the durable fork anchor, anchor bound to Ade's stored slot+hash per `DC-NODE-29`), and VALIDATED (the BLUE `block_validity` fold) as a complete candidate branch — the prove STRICTLY precedes the irreversible `commit_rollback`; a failed/lying/Byzantine/incomplete winner leaves ChainDb + LedgerState + PraosChainDepState UNCHANGED. Adoption proceeds ONLY via the already-enforced `materialize_rolled_back_state` (`CN-STORE-07`) + lockstep `receive::reducer` (`DC-CONS-20`) + `pump_block` (`DC-NODE-05/12`), recorded as the durable append-only `WalEntry::RollBack{ForkChoiceWin}` (NOT a second rollback) and replay-equivalent (`DC-NODE-27`, extended to multi-peer reselection); no forge across the pending reselection (`DC-NODE-28`). Supporting chain: **`DC-NODE-34`** peer identity preserved end-to-end (`NodeSyncItem::Block.peer`); **`DC-NODE-35`** candidate fragments derived ONLY from `validate_and_apply_header` (never the `follow.rs` mint); **`DC-NODE-36`** the `NeedsForkChoice` arm routes the candidate SET to the SINGLE `select_best_chain` (arrival-order-independent); **`DC-NODE-38`** the fork anchor is the durable LCA (slot AND hash, block-depth-k-bounded); **`DC-NODE-39`** a missing post-switch bridge is a structured fail-closed `MissingBridge` hold, never a silent stall or unvalidated admit; **`DC-NODE-40`** rolled-back blocks are retained as walk-visible evidence only (never durable authority / the LCA anchor); **`DC-NODE-41`** a winner-descendant whose bridge ChainSync streamed past is actively range-re-fetched (byte-only, winning-peer-only, bounded-retry, `pump_block` sole admit) or remains fail-closed; **`DC-PUMP-04`** each peer's pump feeds its own bounded lane drained by a deterministic configured-order `fair_merge` (RED scheduling discipline only, never deciding fork-choice); **`DC-EVIDENCE-04`** the SELECT middle is a closed observe-only convergence-evidence sequence (every win paired to exactly one terminal per `fork_switch_id`); **`DC-EVIDENCE-05`** a GREEN replayable post-switch branch-continuity reducer (reads only Ade's own admitted lineage, observe-only). **`CN-CONS-03` FLIPPED `declared → enforced`** on the committed natural two-producer CE-AO-6 transcript (`ContinuesSelectedBranch`, terminal `AgreedAtSwitchTip{slot 391}`, 0 diverged, every win terminal) — proving the EXERCISED two-producer partition-and-reconverge venue, NOT unbounded Cardano ChainSel. The twelve gates: `ci_check_peer_identity_preserved.sh`, `ci_check_candidate_construction_validated.sh`, `ci_check_live_selector_dispatch.sh`, `ci_check_fork_switch_never_abandons.sh`, `ci_check_live_blockfetch_byte_only.sh`, `ci_check_lca_anchor_walk.sh`, `ci_check_wire_pump_fairness.sh`, `ci_check_fork_choice_evidence_closed.sh`, `ci_check_post_switch_convergence_window.sh`, `ci_check_missing_bridge_fail_closed.sh`, `ci_check_rollback_retention_evidence.sh`, `ci_check_missing_bridge_refetch.sh` (+ the 6 reused/extended). `RO-LIVE-01` (preprod rung-3) stays operator-gated.

**Carried (compressed) earlier-cluster fences — full detail in the registry's `ci_script` fields:** PHASE4-N-AN (`T-REC-06` — rollback-materialize eta0), PHASE4-N-AM (`DC-PUMP-03` — keep-alive wire-pump client), PHASE4-N-AL (`DC-NODE-33`), PHASE4-N-AK (`DC-NODE-31`/`32` + the +2 BLUE `RecoveredAnchorPoint` types), PHASE4-N-AJ (`DC-NODE-30`), PHASE4-N-AI (`DC-NODE-23`…`29` + `CN-CONS-01` — the live fork-choice rollback-FOLLOW + the `WalEntry::RollBack` marker + `RollbackReason::ForkChoiceWin`, the precedents PHASE4-N-AO reuses live), PHASE4-N-AH (`DC-NODE-20`/`21`/`22`), PHASE4-N-AE/.F (`DC-NODE-14`/`15`/`16`/`DC-CONS-24`/`DC-PROTO-10`), PHASE4-N-AC (`DC-CRYPTO-10`), PHASE4-N-AB (`CN-SESS-05`), PHASE4-N-AA (`DC-SERVEMEM-01`), PHASE4-N-U (forged-block durability), and the N-F-G-K…G-R wire/serve/feed/forge fences.

### Naming convention

All crates are prefixed `ade_`. TCB color is not encoded in the crate name. The authoritative classifier is `.idd-config.json` `core_paths` + the per-file `// Core Contract:` / `//! BLUE|GREEN|RED` banner + the cluster TCB Color Maps; CI scripts hard-code their BLUE list.

### Closed enums / registries (for SEAMS cross-reference)

Closed semantic surfaces detected at HEAD (carried + the N-AO span): `AcceptedMiniProtocol` (`ade_network::session`), `TagEnvelopeError` (`ade_codec::cbor::tag24`), `ExpectedVrfInput` / `LeaderCheckVerdict` (`ade_core::consensus`), `CandidateFragment` / `TiebreakerView` / `ValidatedHeaderSummary` / `HeaderInput` / `HeaderVrf` / `Point` (`ade_core::consensus::{candidate, header_summary, events}` — REUSED by the PHASE4-N-AO SELECT path, byte-unchanged), `KeepAliveMessage` / `KeepAliveState` (`ade_network::keep_alive`), `OutboundCommand` + `DispatchError` + `AdmissionPeerEvent` (the per-variant `peer` label PHASE4-N-AO threads) + `AdmissionWirePumpError` (`ade_runtime`), `KesError` / `KesParseError`, the admission/wire-only/live-log JSONL vocabularies (incl. the N-AJ `AgreementVerdict` convergence-evidence vocabulary), the operator-evidence manifest TOML schema, the closed `CardanoEra` / Conway cert + governance enums, `PrevHash` (`ade_types::shelley::block`), the closed BLUE `ArrayHead` (`ade_network::codec::primitives`), `SeedEpochConsensusInputs` + `WalEntry::{SeedEpochConsensusInputsImported, RollBack}` + `RollbackReason::ForkChoiceWin` + `RecoveredAnchorPoint` (`ade_ledger`). **PHASE4-N-AO adds these closed surfaces (all in the RED `ade_node` crate — NOT canonical-counted):** the GREEN `CandidateBuildError` (`candidate_aggregator`); `LcaError` (`lca_walk`); `SelectorProjectError` (`selector_state`); `BranchProofError` / `ForkSwitchOutcome` / `MissingBridgeReason` / `RangeRefetchOutcome` / the `BranchBodySource` seam (`fork_switch`); `PostSwitchContinuity` (`post_switch_continuity`); the closed `AdmissionLogEvent` fork-choice + range-refetch vocabulary (`needs_fork_choice` / `lca_discovered` / `candidate_fragment_built` / `fork_choice_selected` / `branch_fetch_started` / `branch_fetch_completed` / `branch_prevalidated` / `fork_switch_applied` / `fork_switch_failed` / `fork_switch_superseded` / `missing_bridge` / `range_refetch_started` / `range_refetch_completed`) + `ForkChoiceResult` (`Win | Loss`) + `ForkChoiceEvidenceFailure` (`admission_log::event`); the closed `NodeSyncItem` is extended (`Block` gains `peer`). **Every `DC-NODE-34…41` / `DC-EVIDENCE-04/05` / `DC-PUMP-04` rule is `enforced`; `CN-CONS-03` is now `enforced` (scoped to the exercised two-producer venue).** **Governance note (N-AO):** the SELECT mechanism is CE (mechanically gated by the twelve gates), reusing the existing BLUE consensus closed surfaces — NO new law/closed BLUE type, NO version bump (the hypothesis held: zero new BLUE canonical type).

### CI enforcement (173 scripts under `ci/`)

The full list is mechanically obtainable via `ls ci/ci_check_*.sh` (173 at HEAD `862cd2cb`). CI lives entirely under `ci/` (`ci_dirs = ["ci"]`); there is no `.github/workflows/` invariant gate at HEAD. **The `b8860b16..HEAD` span (PHASE4-N-AO) added +12 CI scripts and modified 6 in place** — verified `git diff --name-status b8860b16..HEAD -- 'ci/ci_check_*.sh'`: **12 `A`**, **6 `M`**, **0 `D`**. The twelve NEW gates:

| Script | Enforces | Slice |
|---|---|---|
| `ci_check_peer_identity_preserved.sh` | **DC-NODE-34** — `NodeSyncItem::Block` carries the origin `peer` end-to-end; a single-peer FOLLOW run replays byte-identically to the pre-S1 baseline. | S1 |
| `ci_check_candidate_construction_validated.sh` | **DC-NODE-35** — candidate fragments are built from `validate_and_apply_header` output only (the `follow.rs` mint is mechanically absent from the SELECT path); ordering deterministic; malformed/missing fails closed. | S2 |
| `ci_check_live_selector_dispatch.sh` | **DC-NODE-36** — the `NeedsForkChoice` arm routes the candidate SET to the SINGLE `select_best_chain`; no second selector; a `TiebreakerLoss` makes no durable change. | S3 |
| `ci_check_fork_switch_never_abandons.sh` | **DC-NODE-37** — the durable chain is not abandoned until the replacement branch is fetched + linked + validated; an invalid/incomplete winner leaves ChainDb/ledger/chain_dep unchanged; adoption via `RolledBack + ChainSelected×N` with `ForkChoiceWin`; the fork anchor binds the stored slot+hash. | S4 |
| `ci_check_live_blockfetch_byte_only.sh` | **DC-NODE-37 (S6)** — the live BlockFetch `RequestRange` bridge carries BYTES only (`PrefetchedBranchBodies` is a byte carrier, no verdict/selection/fence); a lying/short fetch is rejected by `prevalidate_branch` before any commit. | S6 |
| `ci_check_lca_anchor_walk.sh` | **DC-NODE-38** — `walk_to_durable_lca` anchors on the durable LCA (slot AND hash), block-depth-k-bounded; the branch cache self-binds; over-k / branch-gap / lying-parent / self-binding-violation fail closed. | S7 |
| `ci_check_wire_pump_fairness.sh` | **DC-PUMP-04** — per-peer bounded lanes + a deterministic configured-order `fair_merge` (no shared channel, no hot-peer starvation, closed lane retired in place, peer identity preserved, no wall-clock/rand priority). | S8 |
| `ci_check_fork_choice_evidence_closed.sh` | **DC-EVIDENCE-04** — the 10 fork-choice (+2 range-refetch) discriminators EQUAL the emit-only allow-list; closed typed fields; every `fork_choice_selected{win}` paired to exactly one terminal per `fork_switch_id`; observe-only containment. | S9/S14 |
| `ci_check_post_switch_convergence_window.sh` | **DC-EVIDENCE-05** — invokes the `post_switch_continuity` bin: `ContinuesSelectedBranch` within the bounded window (unbroken `prev_hash` lineage from X, no diverged, every win terminal) + the convergence terminal; reads only Ade's own lineage; the SINGLE impl behind the test + the live gate. | S10 |
| `ci_check_missing_bridge_fail_closed.sh` | **DC-NODE-39** — a missing post-switch bridge emits a closed `MissingBridge`, preserves the durable chain byte-unchanged, HOLDS the forge fence, refuses the silent no-op; never a rollback target / candidate anchor / fence-clear reason. | S11 |
| `ci_check_rollback_retention_evidence.sh` | **DC-NODE-40** — `rollback_retention` is k-bounded, hash-keyed, self-binding walk-visible evidence; never durable authority / a rollback target / the LCA anchor / an S2-S4 bypass; the durable LCA stays `ChainDb`-stored only. | S13 |
| `ci_check_missing_bridge_refetch.sh` | **DC-NODE-41** — the winner-descendant range re-fetch is byte-only, winning-peer-only, bounded-retry, admits in parent-link order via `pump_block`, clears the hold only on `Admitted`; S14 is recovery, never selection. | S14 |

> **The 6 modified gates this span are reused/extended (no rule weakened):** `ci_check_wal_rollback_replay_equiv.sh` (extended for the `ForkChoiceWin` reselection replay-equivalence, `DC-NODE-27`), `ci_check_live_fork_choice_apply.sh` + `ci_check_live_fork_choice_wiring.sh` (extended for the live SELECT apply + the `pending_reselection` fence), `ci_check_node_path_fidelity.sh` (CLI allow-list), `ci_check_wire_rollback_signal_preserved.sh` (the per-peer `RollBackward` signal under the S8 fair-merge), `ci_check_convergence_evidence_vocabulary_closed.sh` (refined at the cluster-close for the full fork-choice + range-refetch vocabulary).
>
> **PHASE4-N-AO is test-backed beyond its gates:** the per-module hermetic unit tests in `fork_switch.rs` (`empty_branch_fails_closed_before_any_apply`, `fence_resolved_only_when_no_pending_and_caught_up`, `missing_bridge_reason_maps_lca_error_to_closed_discriminant`, …), `lca_walk.rs` (1-deep / multi-block / missing-intermediate / over-k-by-block-depth / lying-parent / cache-self-binding / arrival-order-permutation + the S13 retention tests `rollback_retains_removed_blocks_for_lca_walk` / `retained_blocks_are_not_anchors` / `retained_blocks_are_k_bounded` / `genuine_gap_still_missing_bridge`), `candidate_aggregator.rs` (validated-only / reject-invalid-fails-closed / empty-fails-closed / two-runs-byte-identical / arrival-independent-ordering), `selector_state.rs` (praos-matches-leader-value / deterministic / tpraos-fails-closed), `fair_merge.rs` (hot-peer-no-starve / per-peer-backpressure / peer-identity-through-merge / deterministic-order / closed-lane-retired / single-peer-unchanged), `post_switch_continuity.rs` (continuity-OK / broken-parent / post-X-diverged / win-without-terminal / ignores-peer-tip / replays-byte-identical), plus the AO integration suites under `crates/ade_node/tests/` (incl. the S12 bridge-gap fault-injection harness).

---

## Generation notes

- Regenerated (**PHASE4-N-AO cluster-close refresh** — live multi-candidate fork-choice SELECT + adopt, rung-2; primary `DC-NODE-37`; `CN-CONS-03` flipped) at HEAD `862cd2cb` (`git rev-parse --short HEAD`). The prior on-disk CODEMAP was pinned at the PHASE4-N-AN/AM close (`b8860b16`); this regeneration folds the fully-enumerable `b8860b16..HEAD` delta: ELEVEN new rules + one flip, +12 CI gates, +6 in-place gate refinements, six new `ade_node` source modules + one bin, ZERO new BLUE canonical type.
- **The cluster's load-bearing hypothesis (zero new BLUE canonical type) held mechanically.** Verified: `git diff --name-status b8860b16..HEAD -- 'crates/ade_core/src/**/*.rs' 'crates/ade_ledger/src/**/*.rs'` (excluding tests) is **empty** — NO BLUE crate source changed; `git grep -hE "^(pub )?(struct|enum) "` over the BLUE `core_paths` trees is **462** at both `b8860b16` and `862cd2cb` (`ade_core` 49→49, `ade_ledger` 181→181). The AO modules import PRE-EXISTING BLUE types (`CandidateFragment` / `ValidatedHeaderSummary` / `TiebreakerView` / `Point` from `ade_core::consensus`, present at `b8860b16`; `RollbackReason::ForkChoiceWin` from `ade_ledger::wal::event`); their OWN closed types live in the RED `ade_node` crate and are NOT canonical-counted. The BLUE `select_best_chain` / `validate_and_apply_header` / `materialize_rolled_back_state` / `block_validity` / `WalEntry::RollBack` authorities are reused byte-unchanged — `git diff b8860b16..HEAD -- crates/ade_core/src/consensus/fork_choice.rs` is empty.
- Mechanical counts recomputed fresh from the tree at `862cd2cb` (not copied): **11 crates** (`grep -cE '"crates/' Cargo.toml`; Δ 0), **462 canonical types** (Δ 0 — the BLUE core is reused, not extended), **2542 tests** (`git grep -hE "#\[test\]|#\[tokio::test\]" -- 'crates/**/*.rs' | wc -l`; Δ +87 vs `b8860b16`'s 2455 — the S1–S14 hermetic suites), **173 CI checks** (`ls ci/ci_check_*.sh | wc -l`; cross-checked `git ls-tree -r HEAD ci/`; Δ +12 — `git diff --name-status b8860b16..HEAD -- 'ci/ci_check_*.sh'` = 12 `A` + 6 `M` + 0 `D`), **372 registry rules** (`grep -cE '^id = ' docs/ade-invariant-registry.toml`; Δ +11; status 239 enforced / 19 partial / 113 declared / 1 enforced_scaffolding).
- **Registry rule reconciliation (N-AO span):** the eleven net-new rules vs the prior on-disk 361 are `DC-NODE-34/35/36/37/38/39/40/41`, `DC-EVIDENCE-04`, `DC-EVIDENCE-05`, `DC-PUMP-04` (all `enforced` at close). **One flip:** `CN-CONS-03` `declared → enforced` (on the committed natural CE-AO-6 transcript `ao-CN-CONS-03-FLIP-natural-conv.jsonl`, sha256 `6713efe9…`, preserved OUTSIDE the repo per competition-secrecy; `tier = derived`; scope = the exercised two-producer partition-and-reconverge venue, NOT unbounded Cardano ChainSel). **No rule weakened.**
- **TCB color for the SEVEN new source files verified against the on-disk banners at `862cd2cb`:** all six modules + the bin are inside the RED `ade_node` crate; NONE recolors an existing module and NONE touches a BLUE crate. GREEN-by-content: `candidate_aggregator.rs` / `selector_state.rs` / `post_switch_continuity.rs` (explicit `//! GREEN …` banners), `lca_walk.rs` + `fork_switch::prevalidate_branch` (pure deterministic `// Core Contract:` banner). RED-by-content: `fair_merge.rs` (`// Core Contract: RED scheduling discipline ONLY …`) + the `fork_switch::{BranchBodySource, NullBranchBodySource, PrefetchedBranchBodies}` seam + the `node_lifecycle` fork-switch DRIVERS. The TCB color resolution of the aggregator (OQ-AO-6) was decided GREEN by the S2 proof, not by convenience. No module was added, moved, or recolored elsewhere, so the BLUE/GREEN/RED banner survey for the rest of the workspace is carried verbatim from the N-AN refresh and remains byte-accurate at `862cd2cb`.
- **Cluster-doc location at this regen:** the PHASE4-N-AO cluster + slice docs (S1–S14) live under `docs/clusters/PHASE4-N-AO/` (NOT yet archived — the cluster-close commit `862cd2cb` flipped `CN-CONS-03` + refined one gate but did NOT run the grounding-doc regen or the archive step; this `/codemap` run is that refresh). Its invariants live under `docs/planning/phase4-select-multicandidate-fork-choice-{invariants,cluster-slice-plan}.md` + `docs/planning/phase4-n-ao-ce-ao-6-live-gap.md`. The CE-AO-6 transcripts are OUTSIDE-REPO (competition-secrecy + no-credential-leak discipline; only scrubbed in-repo notes reference them). Every prior closed cluster doc remains archived under `docs/clusters/completed/`.
- **`.idd-config.json` annotations are STALE at this regen (informational; this run does NOT modify the config).** At this HEAD the config's `head_deltas_baseline` is still `b8860b16` (the PHASE4-N-AN close); the next `/head-deltas` run owns the bump to `862cd2cb`. This `/codemap` run does NOT touch `.idd-config.json` or any source file.
- **Carried delta-stacks compressed, not deleted.** The full per-cluster N-AN / N-AM / N-AL … N-U / G-K…G-R delta narratives that prior on-disk CODEMAPs carried at length are compressed into the "Active cluster at HEAD" + the "Carried (compressed)" CI/forbidden-direction sections + the per-module entries; PHASE4-N-AO (this span) is documented in full. The load-bearing MUST NOT content for each surface is preserved (in the per-module tables + the forbidden-directions list). The registry's per-rule `ci_script(s)` / `statement` / `code_locus` fields are the authoritative source for any compressed detail.
- **Test-count method caveat (load-bearing — carried).** The documented method (`git grep -hE "#\[test\]|#\[tokio::test\]"`) matches `#[test]` and bare `#[tokio::test]` (incl. `#[tokio::test(start_paused)]`) but NOT `#[tokio::test(flavor = "multi_thread", …)]`. The AO suites are `#[test]` (the pure cores) + bare `#[tokio::test]` (the `fair_merge` async tests) — all captured by the grep; the +87 net is the mechanically-verified delta of the documented grep across the seven changed `.rs` files + their integration suites (`2455 → 2542`). The AO invariants are mechanically enforced by the twelve dedicated gates AND the named tests.
- The doc is regenerated, not edited. If a value drifts, fix the source, not the doc.
