# Module Authority Map — Ade

> **Status:** Living architectural document. Regenerated; not hand-edited.
> Per-project instance of `~/.claude/methodology/templates/codemap.md`.

> 11 crates, 462 canonical types, 2455 tests, 161 CI checks at HEAD (`b8860b16`, PHASE4-N-AN close — rollback-materialization preserves the recovered eta0 (`T-REC-06`), with PHASE4-N-AM (keep-alive wire-pump client, `DC-PUMP-03`) and a CI-script-only stale-gate triage carried in the same `e87e8a43..HEAD` span; the BLUE change is the single new method `PraosChainDepState::overlay_recovered_eta0` (`ade_core`) + a `recovered_eta0: Option<&Nonce>` param on `materialize_rolled_back_state` (`ade_ledger`) — NO new canonical type, NO new crate; +2 CI gates, +2 registry rules).

---

## Conventions

- A **module** in Ade is a Cargo workspace crate (smallest independently-buildable unit). Source: `Cargo.toml` `[workspace] members` (11 entries). One exception to crate-granularity: `ade_network` is split by *submodule color* — `.idd-config.json` `core_paths` resolves BLUE at the submodule path level (9 paths), so its BLUE / GREEN / RED submodules are documented as separate entries.
- Several BLUE crates host sub-trees that carry a *different* TCB color by their own module banner; the RED `ade_runtime` and `ade_node` crates host numerous **GREEN-by-content** sub-trees. These are surfaced as sub-classification notes inside the owning crate's entry (or, where load-bearing, as their own GREEN/RED entries below).
- Modules are listed by TCB color (BLUE → GREEN → RED), alphabetical within each color.

### TCB color sources (in order of authority)

1. `.idd-config.json` `core_paths` — substring match against absolute path. BLUE crate prefixes: `ade_codec`, `ade_types`, `ade_crypto`, `ade_core`, `ade_ledger`, `ade_plutus`. Plus the 9 BLUE `ade_network` submodule paths: `mux/frame.rs`, `codec/`, `handshake/`, `chain_sync/`, `block_fetch/`, `tx_submission/`, `keep_alive/`, `peer_sharing/`, `n2c/`.
2. `.idd-config.json` `_core_paths_doc` — `ade_runtime` is RED; `ade_testkit` is GREEN; `ade_node` is RED; `ade_network::mux::transport` + `ade_network::session` are nominally RED (but `session/` is GREEN-by-content per PHASE4-N-L). `ade_core_interop` is RED.
3. **Module doc-comment banners** — every BLUE/GREEN source file opens with the `// Core Contract:` banner and/or a `//! BLUE …` / `//! GREEN …` / `//! RED …` line. This is the per-file authority that distinguishes GREEN-by-content from RED inside `ade_runtime` / `ade_node`. **The `e87e8a43..HEAD` span (N-AM + N-AN + triage) touched SEVEN production source files**, NONE of which is a new or recolored module: the BLUE `ade_core::consensus::praos_state` (`//! BLUE Praos chain-dep-state evolution`) + `ade_ledger::rollback::materialize` (BLUE, the sole `materialize_rolled_back_state` authority); the GREEN `ade_runtime::forward_sync::reducer` (`//! GREEN forward-sync lifecycle reducer`); the RED `ade_runtime::admission::wire_pump` (`//! RED admission wire pump`), `ade_runtime::bootstrap` (`//! GREEN single bootstrap authority`, hosting the RED A3b restore), `ade_ledger::receive::reducer` (BLUE — its `RollbackContext.recovered_eta0` addition is on the `#[cfg(test)]` receive-driver path), and `ade_node::node_lifecycle` (`//! RED `--mode node``). The banner survey is otherwise carried verbatim from the N-AL refresh. Carried-stable BLUE files from prior spans (verified at HEAD): `ade_ledger::recovered_anchor_point.rs` (`//! BLUE RecoveredAnchorPoint type + canonical CBOR codec`, N-AK) and `ade_runtime::recovered_anchor.rs` (`//! BLUE recovered anchor-point load + fail-closed verify`, N-AK).
4. Cluster TCB Color Maps under `docs/clusters/` and `docs/clusters/completed/` — §5 "TCB color map (FC/IS partition)" of each `cluster.md`.
5. The invariant registry `docs/ade-invariant-registry.toml` (361 rules at HEAD) — each rule's `code_locus` + `statement` is the authoritative source for the **MUST NOT** rows below.

### Active cluster at HEAD

**PHASE4-N-AN (rollback-materialization preserves recovered eta0 — `T-REC-06`) — CLOSED + ARCHIVED.** HEAD `b8860b16` is the close (it archives both the N-AM and N-AN cluster docs to `docs/clusters/completed/`). N-AN is **BLUE+GREEN+RED**, but adds **NO new canonical type** — its BLUE change is one new *method* and one new fn *param*:

- **The single eta0-overlay authority (BLUE, `ade_core`):** the NEW `pub fn PraosChainDepState::overlay_recovered_eta0(&mut self, eta0: &Nonce)` (sets both `epoch_nonce` and `evolving_nonce` to the recovered eta0) is the SINGLE eta0-overlay authority, shared by BOTH WarmStart bootstrap (the live-admit chain_dep) AND rollback materialization (the replay chain_dep) — so live admit and rollback replay validate the header VRF against the SAME nonce (replay-equivalence by construction).
- **`materialize_rolled_back_state` gains `recovered_eta0` (BLUE, `ade_ledger`):** the SOLE rolled-back-state authority (`CN-STORE-07`) gains a `recovered_eta0: Option<&Nonce>` param and overlays it onto the `nearest_le` snapshot chain_dep (via `overlay_recovered_eta0`) BEFORE the degenerate return AND the replay-forward `block_validity` fold. The persisted snapshot carries the `Nonce::ZERO` placeholder; eta0 is the recovered canonical input (the seed-epoch `SeedEpochConsensusInputs.epoch_nonce` sidecar). `None` (cold-start / no recovered sidecar) keeps the snapshot nonce as-is. **VRF strength is UNCHANGED** — overlaying the correct nonce is not a bypass; a block whose VRF verifies against NEITHER eta0 nor the snapshot nonce still fails closed (a WRONG eta0 still fails the header VRF).
- **Threading (GREEN/RED):** the GREEN `ForwardSyncState` gains the additive `recovered_eta0: Option<Nonce>` field (`ade_runtime::forward_sync::reducer`; replay-derivable, default `None`), set on the recover arm and threaded into `apply_chain_event` (`ade_node::node_lifecycle`) — the live rollback-follow path the CE-AI-6 reorg hit. The test-only receive path adds `RollbackContext.recovered_eta0: Option<&Nonce>` (`ade_ledger::receive::reducer`, `#[cfg(test)]`-driver). `bootstrap.rs` was REORDERED so the sidecar restore (`restore_seed_epoch_consensus_inputs`) runs BEFORE `materialize`, and now passes eta0 into the WarmStart materialize call (fixing warm-start replay from a non-bare store too — the SAME root cause); the existing post-materialize eta0 overlay (the SOLE `ci_check_warmstart_eta0_overlay.sh` site, `T-REC-04`) is now idempotent after the materialize overlay.

It flips ONE rule → `enforced` (`T-REC-06`, `tier = true`), backed by a new gate `ci_check_rollback_materialize_eta0.sh` (guards: the single overlay authority exists; materialize takes the param AND applies it; NO VRF bypass). The CE-AI-6 reorg (CE-AN-LIVE) is the live evidence — a fresh hermetic 2-pool bridge venue (magic 42): bare-anchor recover @ slot 162 → follow → induced peer reorg → Ade received the `RollBackward` and FOLLOWED it (strict slot REGRESSION admit 371 → 361) → re-converged to `agreement_verdict{agreed}` @ slot 383 (our_hash == peer_hash, the reorged tip), 16 admits / 2 agreed / 0 diverged / 0 `VrfCert`/`ReplayFailedAt` — the eta0 overlay HELD through the live rollback (WITHOUT AN-S2 Ade died here with `ReplayFailedAt VrfCert`). The transcript is OUTSIDE-REPO (competition-secrecy + no-credential-leak discipline; scrubbed in-repo note only). SCOPE: the recovered seed epoch (no epoch-boundary crossing in the follow window — eta0 is the constant epoch nonce); a multi-epoch rollback nonce-evolution is a named out-of-scope follow-on. The N-AN cluster + slice docs are archived under `docs/clusters/completed/PHASE4-N-AN/`; its invariants under `docs/planning/phase4-n-an-rollback-materialize-eta0-invariants.md`.

**Carried (same span): PHASE4-N-AM (keep-alive wire-pump client — `DC-PUMP-03`) — CLOSED + ARCHIVED.** N-AM is **RED-only, ZERO BLUE change, ZERO new canonical type**. The sole production code change is in the RED `ade_runtime::admission::wire_pump` (`run_admission_wire_pump` — the SOLE per-peer pump, `CN-PUMP-01`): a `tokio::select!` cadence arm (`tokio::time::interval(KEEP_ALIVE_CADENCE)`, `KEEP_ALIVE_CADENCE = 20s`, missed-tick `Delay`) sends a proto-8 `KeepAliveMessage::KeepAlive(cookie)` (Initiator) on a ~20s cadence during inbound quiescence via the EXISTING outbound `OutboundFrame` path, advancing the REUSED BLUE `ade_network::keep_alive` state machine (`ClientIdle → ServerHasAgency{cookie}`); a `handle_keep_alive` path validates the echoed cookie on the inbound `DeliverPeerFrame{KeepAlive}` (`ServerHasAgency{cookie} + Server → ClientIdle`, `cookie' == cookie`). It is **WIRE-ONLY** — the keep-alive client produces NO canonical input, NO WAL entry, and NO `AdmissionPeerEvent` (the `DC-PUMP-01` emit-set stays unwidened); a grammar violation fails closed via the new `AdmissionWirePumpError::KeepAlive` variant (drop the peer). The cookie is a monotonic `u16` counter (no rand / wall-clock); it is a CLIENT only (the responder is a CE-AM-LIVE-gated follow-on). It flips ONE rule → `enforced` (`DC-PUMP-03`, `tier = derived`), backed by the new gate `ci_check_keep_alive_wire_only.sh` (reuse-not-redefine + wire-only fence). It adds a dev-only `tokio` `["test-util"]` dependency to `ade_runtime/Cargo.toml` (deterministic virtual-time `#[tokio::test(start_paused)]` for the cadence test — never in production builds). CE-AM-LIVE PASSED (2026-06-11, frozen c2-testnet relay, magic 42): a bare-anchor recover @ slot 202 caught up admitting 5 blocks → `agreement_verdict{agreed}` @ slot 241 (our_hash == peer_hash) → then IDLE at the frozen tip while the keep-alive client SUSTAINED the link 152s (vs the prior ~96s EOF): 7 `MsgKeepAlive` on the ~20s cadence, 7 `MsgResponseKeepAlive` cookies VALIDATED, 0 grammar failures, 0 idle-EOF — making the CE-AI-6 induced-reorg convergence capture (the N-AN live evidence) runnable. NOT a CE-AI-6 claim itself. The N-AM cluster + slice docs are archived under `docs/clusters/completed/PHASE4-N-AM/`; its invariants under `docs/planning/phase4-n-am-wire-pump-keepalive-sustain-invariants.md`.

**Carried (same span): stale-gate triage (CI-script-only, NO source change).** Three pre-existing red gates were classified stale and repaired in-place (no rule weakened, no new rule): **(1)** `ci_check_admission_wire_pump_closure.sh` — the `DC-PUMP-02` guard was refined so the chain-sync `RollBackward` arm is checked for the DISTINCT `AdmissionPeerEvent::RollBackward` (the AI-S4a fork-choice signal), not a generic `tip_update` / `TipUpdate` ("a rollback is NEVER a TipUpdate only"); `DC-PUMP-02`'s registry `statement` was refined to match, `strengthened_in += "PHASE4-N-AN"`. **(2)** `ci_check_node_path_fidelity.sh` — the CLI allow-list was extended with `--participant-venue` (N-AI) + `--convergence-evidence-path` (N-AJ, an emit-only evidence sink). **(3)** `ci_check_lifecycle_owner_uses_bootstrap_initial_state.sh` — dropped `pipefail` (a `grep -q` SIGPIPE was producing a false-negative) and SCOPED the `materialize_rolled_back_state` forbidden-call check to EXCLUDE the legitimate `apply_chain_event` rollback-follow region (a parallel/cold INITIAL-STATE materialize still trips; `CN-NODE-01`).

**Carried: PHASE4-N-AL (participant recovered-anchor rollback no-op — `DC-NODE-33`) — CLOSED** at `e87e8a43`. N-AL is the participant MIRROR of N-AK's single-producer `DC-NODE-32`: RED-only, ZERO BLUE change, ZERO new canonical type, ZERO new CI gate; the sole code change is ~17 added lines in the RED `ade_node::node_lifecycle::run_participant_sync` RollBack handler (a peer `RollBackward` binding EXACTLY slot AND hash to the persisted recovered anchor, carried in `ForwardSyncState.recovered_anchor`, is an idempotent no-op evaluated BEFORE the `DC-NODE-29` stored-block resolution). **Carried: PHASE4-N-AK (recovered-anchor live-follow start + recovered-anchor rollback no-op — `DC-NODE-31` / `DC-NODE-32`) — CLOSED** at `b4c0983d`: the lone +2-BLUE-type cluster in the recent window (the closed `RecoveredAnchorPoint` record + `RecoveredAnchorPointError` + its SOLE canonical CBOR codec in the NEW `ade_ledger::recovered_anchor_point` module; 460 → 462) + the BLUE load+verify fn `ade_runtime::recovered_anchor::load_recovered_anchor_point` + the RED anchor-fp-keyed `SnapshotStore::{put,get}_recovered_anchor_point` surface. **Carried: PHASE4-N-AJ (participant-path convergence evidence emission — `DC-NODE-30`) — CLOSED** at `b1bed361` (evidence-only; the GREEN `ade_node::convergence_evidence` sink + the closed `AgreementVerdict` vocabulary on the `--convergence-evidence-path` file). **Carried: PHASE4-N-AI (live fork-choice rollback-follow wiring — single-best-peer FOLLOW, NOT full ChainSel; `DC-NODE-23`…`DC-NODE-29`) — CLOSED** at `5ec841c8` (the BLUE `WalEntry::RollBack` durable MARKER + its `RollbackPoint` / `RollbackReason` payload types, 458 → 460; a per-cluster security review caught HIGH H-1 → AI-S6 `DC-NODE-29` pre-commit canonical binding). **Carried: PHASE4-N-AH … N-U** (local-durable forge base + cert evidence-only + warm-start re-entry; KES evolve-then-sign; outbound mux segmentation; bounded serve range; forged-block durability) — all CLOSED. Every prior closed cluster doc — the entire PHASE4-N-F-A…N-F-G-J set, the N-Q/N-R-*/N-S-* set, the N-M-* sub-trees, N-O…N-Z — is archived under `docs/clusters/completed/`.

**What the binary `Node` arm actually runs (precise wiring honesty):** `--mode node` (`ade_node::node_lifecycle`) bootstraps via the single `bootstrap_initial_state` authority (WarmStart restores the seed-epoch sidecar, then overlays the recovered eta0 onto the WarmStart materialize — **PHASE4-N-AN** — AND onto the live-admit chain_dep, and — **PHASE4-N-AK** — resolves the live-follow start tip via `resolve_live_follow_start(chaindb.tip(), recovered_anchor)`), spawns the live WirePump feed (FindIntersect at the resolved start point via `wire_pump_start_point`, never blind Origin for a bare-anchor recovery; **PHASE4-N-AM** the pump now also runs the N2N keep-alive CLIENT on a ~20s cadence during inbound quiescence, sustaining the link past the peer's ~97s keep-alive deadline), drains it through the sole `forward_sync::pump_block` durable-admit chokepoint, serves a read-only projection of the durable ChainDb to peers (the serve listener's lifetime independent of feed-end), and — for a declared single-producer venue — forges on its local durable spine head. On the rollback-follow paths (single-producer `run_node_sync` / participant `run_participant_sync`), a peer `RollBackward` binding EXACTLY slot AND hash to the persisted recovered anchor is an idempotent no-op (`DC-NODE-32` / `DC-NODE-33`); a real stored-block rollback materializes the rolled-back state via `materialize_rolled_back_state` — and **PHASE4-N-AN** that materialization now overlays the recovered eta0 so the replay validates each block's header VRF against the SAME nonce as live admit (`T-REC-06`), the fix that let Ade survive the CE-AI-6 live reorg instead of dying at `ReplayFailedAt VrfCert`. `RO-LIVE-01` stays operator-gated.

### Counts (mechanical, with sources)

| Count | Value | Source / command |
|---|---|---|
| Crates | **11** | `grep -cE '"crates/' Cargo.toml` (`[workspace] members`). **Δ vs prior on-disk CODEMAP (11): 0** — the `e87e8a43..HEAD` span (N-AM + N-AN + triage) added no crate (`git diff e87e8a43..HEAD -- Cargo.toml` shows only the `ade_runtime/Cargo.toml` dev-dep addition; `git diff e87e8a43..HEAD -- Cargo.toml` at the workspace root is empty). |
| Canonical types | **462** | `git grep -hE "^(pub )?(struct\|enum) "` over the 6 BLUE crate `src/` trees + the 9 BLUE `ade_network` submodule paths, summed at HEAD `b8860b16`. **Δ vs prior on-disk CODEMAP (462): 0** — N-AN's BLUE change is a new METHOD (`PraosChainDepState::overlay_recovered_eta0`) + a new fn PARAM (`materialize_rolled_back_state`'s `recovered_eta0`), neither a type; N-AM is RED-only. Breakdown (byte-stable across the span; the two BLUE files touched were `ade_core::consensus::praos_state` + `ade_ledger::rollback::materialize`): `ade_codec` 11, `ade_types` 82, `ade_crypto` 21, **`ade_core` 49** (49 at both `e87e8a43` and `b8860b16` — `overlay_recovered_eta0` is a method), `ade_ledger` 181 (181 at both — `materialize_rolled_back_state` gains a param), `ade_plutus` 8 (= 352) + `ade_network` BLUE submodules **110** (`mux/frame.rs` 5, `codec/` 39, `handshake/` 9, `chain_sync/` 11, `block_fetch/` 10, `tx_submission/` 5, `keep_alive/` 5, `peer_sharing/` 5, `n2c/` 21). The `ade_ledger` 181 still carries the N-AK +2 (`recovered_anchor_point::{RecoveredAnchorPoint, RecoveredAnchorPointError}`). `canonical_type_registry: null`, so the structural grep is authoritative. |
| Tests | **2455** | `git grep -hE "#\[test\]\|#\[tokio::test\]" -- 'crates/**/*.rs' \| wc -l` = 2455 at `b8860b16`. Approximate per the template fallback rule (count of attributes, not a runner enumeration). **Δ vs the same grep at the prior baseline `e87e8a43` (2451): +4** — the N-AM keep-alive tests (`wire_pump_sends_keep_alive_on_quiescent_cadence`, `wire_pump_keep_alive_response_validates_cookie_no_event`, `wire_pump_keep_alive_cookie_mismatch_fails_closed`, all bare `#[tokio::test]` / `#[tokio::test(start_paused)]` — captured by the grep) + the N-AN materialize tests (`rollback_materialize_overlays_recovered_eta0_replay_equivalent`, `rollback_materialize_does_not_bypass_vrf_on_wrong_eta0`, bare `#[test]`) net +4 over the span (the AN-S1 repro-first reproduction test in `wal_rollback_ai_s1.rs` was superseded/folded by AN-S2 — the +4 is the mechanically-verified net of the documented grep across the seven changed `.rs` files + their integration suites). |
| CI checks | **161** | `ls ci/ci_check_*.sh \| wc -l` = 161 at `b8860b16` (cross-checked `git ls-tree -r HEAD ci/`). CI lives entirely under `ci/` (`ci_dirs = ["ci"]`); there is no `.github/workflows/` invariant gate at HEAD. **Δ vs prior on-disk CODEMAP (159): +2** — verified mechanically via `git diff --name-status e87e8a43..HEAD -- 'ci/ci_check_*.sh'`: **2 `A`** (`ci_check_keep_alive_wire_only.sh` — N-AM `DC-PUMP-03`; `ci_check_rollback_materialize_eta0.sh` — N-AN `T-REC-06`), **3 `M`** (the stale-gate triage: `ci_check_admission_wire_pump_closure.sh`, `ci_check_node_path_fidelity.sh`, `ci_check_lifecycle_owner_uses_bootstrap_initial_state.sh` — repaired in place, NOT new), **0 `D`**. |
| Registry rules | **361** | `grep -cE "^id = " docs/ade-invariant-registry.toml` = 361. Reference only — not a header count. **Status breakdown:** 227 `enforced` / 19 `partial` / 114 `declared` / 1 `enforced_scaffolding`. **Δ vs prior on-disk CODEMAP (359): +2** — the two net-new rules are `DC-PUMP-03` (N-AM keep-alive wire-pump client; `enforced`; `ci_scripts = ["ci/ci_check_keep_alive_wire_only.sh"]`; `introduced_in = "PHASE4-N-AM"`, `tier = derived`) and `T-REC-06` (N-AN rollback-materialization replay-equivalence; `enforced`; `ci_scripts = ["ci/ci_check_rollback_materialize_eta0.sh"]`; `introduced_in = "PHASE4-N-AN"`, `tier = true`). **One strengthening:** `DC-PUMP-02` `strengthened_in += "PHASE4-N-AN"` (statement refined for the AI-S4a `RollBackward` event in the gate triage — a CI/registry-statement refinement, not a code change). **No rule weakened.** `CN-CONS-03` stays `declared` (single-best-peer venue only; full multi-peer ChainSel out of scope). |

---

## BLUE Modules — Pure Functional Core

> **Shared header (applies to every BLUE entry below).** Every `.rs` source file begins with the `// Core Contract:` banner, and the following deny attributes are present in each crate's `lib.rs` (or, for `ade_network`, at the crate root — BLUE submodules inherit them):
> `#![deny(unsafe_code)]`, `#![deny(clippy::unwrap_used)]`, `#![deny(clippy::expect_used)]`, `#![deny(clippy::panic)]`, `#![deny(clippy::float_arithmetic)]`.
>
> Cross-cutting BLUE CI gates (all scope the full BLUE set — 6 BLUE crates + 9 BLUE `ade_network` submodule paths): `ci_check_module_headers.sh`, `ci_check_forbidden_patterns.sh`, `ci_check_dependency_boundary.sh`, `ci_check_no_signing_in_blue.sh`, `ci_check_no_semantic_cfg.sh`, `ci_check_hash_uses_wire_bytes.sh`, `ci_check_ingress_chokepoints.sh`, `ci_check_pallas_quarantine.sh`, `ci_check_no_async_in_blue.sh`.
>
> Narrow BLUE / wire / forge / recovered-surface gates carried through prior spans: the N-W `ci_check_producer_praos_vrf.sh` (CN-FORGE-04), the N-X `ci_check_tag24_wire_authority.sh` (CN-WIRE-08), the N-Y `ci_check_forward_sync_chokepoint_only.sh` (DC-SYNC-01) / `ci_check_mithril_uses_bootstrap_initial_state.sh` (CN-MITHRIL-01) / `ci_check_no_haskell_fingerprint_equality.sh` (DC-COMPAT-01), the workspace-wide `ci_check_registry_code_locus_exists.sh`, the N-F-A `ci_check_consensus_input_provenance.sh` (CN-CINPUT-02), the N-F-G-J `ci_check_prevhash_single_wire_authority.sh` (CN-WIRE-09), the G-L/G-M/G-N/G-O wire-authority family (`ci_check_n2n_handshake_versiondata_authority.sh`, `ci_check_chainsync_findintersect_compat.sh`, `ci_check_warmstart_eta0_overlay.sh`, `ci_check_feed_tag24_unwrap.sh`), and the N-AI `ci_check_wal_rollback_replay_equiv.sh` (DC-NODE-27 — the `WalEntry::RollBack` marker + the replay re-anchor) / `ci_check_chain_selection_arrival_order_independent.sh` (CN-CONS-01 — the `#[cfg(test)]` arrival-order proof over the UNCHANGED `select_best_chain`). **PHASE4-N-AN added ONE BLUE-set CI gate** — `ci_check_rollback_materialize_eta0.sh` (T-REC-06): it guards (A) the SINGLE eta0-overlay authority exists on `PraosChainDepState`, (B) `materialize_rolled_back_state` takes the `recovered_eta0` param AND applies the overlay before the replay fold, and (C) NO VRF bypass (the replay still runs `block_validity`; no skip/unchecked). The standing BLUE deny / closed-enum / canonical-codec gates apply to the touched BLUE files (`praos_state`, `materialize`) unchanged. **PHASE4-N-AM added NO BLUE-set CI gate** (it is RED-only; `ci_check_keep_alive_wire_only.sh` scopes the RED `ade_runtime::admission::wire_pump`). The carried N-AK BLUE module `ade_ledger::recovered_anchor_point` + the BLUE fn `ade_runtime::recovered_anchor::load_recovered_anchor_point` stay TEST-ENFORCED (`DC-NODE-31.ci_script = ""`).

---

### `ade_codec`

| Attribute | Value |
|-----------|-------|
| **Purpose** | Owns Cardano-canonical CBOR ingress — the only place in the workspace that turns raw bytes into typed semantic values, with wire-byte preservation for every hash-bearing structure. Owns the standalone opcert byte authority and the canonical Conway-tx preserved-byte splitter, the canonical block-envelope encoder `cbor::envelope::encode_block_envelope`, and (N-X) the single workspace **tag-24 CBOR-in-CBOR wire-envelope authority** `cbor::tag24::{wrap_tag24, unwrap_tag24}` + closed `TagEnvelopeError`. Also exposes the shared canonical CBOR read/write primitives the BLUE codecs in `ade_ledger` + `ade_network` build on — incl. the `canonical_width` / `read_array_header` / `read_bytes` / `read_uint` / `write_array_header` / `write_bytes_canonical` / `write_uint_canonical` primitives the (N-AK) `RecoveredAnchorPoint` codec in `ade_ledger` consumes. **N-F-G-J** owns the POSITION-BLIND header `prev_hash` codec `shelley::block::decode_prev_hash` (`null -> Genesis` / `hash32 -> Block`; CN-WIRE-09). (Unchanged across the N-AM/N-AN span.) |
| **Creates** | `PreservedCbor<T>`, `RawCbor`, `BlockEnvelope`, `ByronDecodedBlock`, `CodecContext`, `CodecError`, `ContainerEncoding`, `IntWidth`, era-tagged block/tx wrappers, `OpCertCodecError`, `TxComponents<'a>`, `TagEnvelopeError` (closed). 11 public types (byte-stable across the G-K…N-AN span). |
| **Interprets** | All canonical Cardano CBOR. `unwrap_tag24` is the single workspace authority that strips a tag-24 (`0xd8 0x18`) envelope (zero-copy borrow, fail-closed `TagEnvelopeError`). The feed-side RED `ade_runtime::admission::wire_pump` calls `unwrap_tag24` before the BLUE decode (N-F-G-O) — `ade_codec` stays the single tag-24 authority. `shelley::block::decode_prev_hash` decodes the header `prev_hash` POSITION-BLIND (CN-WIRE-09). |
| **MUST NOT** | (1) Construct `PreservedCbor` outside `ade_codec`. (2) Re-encode wire bytes when computing hashes. (3) Use any forbidden BLUE pattern. (4) Depend on any workspace crate except `ade_types`. (5)–(11) per-construct prohibitions (Conway cert/withdrawals/governance, opcert, tx_components, single block-envelope encoder). (12) `cbor::tag24` (CN-WIRE-08) — `wrap_tag24`/`unwrap_tag24` defined exactly once; `unwrap_tag24` fails closed (never panic); no second/hand-rolled tag-24 parse anywhere (incl. the feed-side WirePump, which MUST reuse this authority — CN-WIRE-12). (13) `shelley::block::decode_prev_hash` (CN-WIRE-09) — the codec is the byte representation only and MUST stay POSITION-BLIND (no `block_number`); the position-aware coupling lives in `ade_ledger::block_validity::header_position`, never here. |
| **Inbound deps** | `ade_ledger` (heavy — incl. the N-AK `recovered_anchor_point` codec via the canonical primitives), `ade_plutus`, `ade_testkit`, `ade_network`, `ade_runtime`, `ade_core_interop`, `ade_node`. |
| **Outbound deps** | `ade_types`. std-only; dev-deps `serde_json`, `toml`. |
| **Entry points** | `ade_codec::cbor::envelope::{decode_block_envelope, encode_block_envelope}`, `ade_codec::{wrap_tag24, unwrap_tag24, TagEnvelopeError}`, the `cbor::{read_*, write_*_canonical, canonical_width, ContainerEncoding, IntWidth}` primitives, `ade_codec::traits::AdeEncode`, per-era `decode_*_block`, `ade_codec::shelley::{opcert::{encode_opcert, decode_opcert}, tx_components::split_conway_tx_components, block::decode_prev_hash}`. |
| **Key modules** | `cbor/` (incl. `envelope.rs`, `tag24.rs`), `byron/`, `shelley/` (incl. `block.rs`, `cert.rs`, `opcert.rs`, `tx_components.rs`), `allegra/`, `mary/`, `alonzo/`, `babbage/`, `conway/`, `address/`, `preserved.rs`, `traits.rs`, `primitives.rs`, `error.rs`. |

---

### `ade_core`

| Attribute | Value |
|-----------|-------|
| **Purpose** | The functional core (consensus half): Praos VRF leader-check + the expected-VRF-input recipe; the chain-selection authority `consensus::fork_choice::select_best_chain` (the sole DC-CONS-03 selector); the `PraosChainDepState` / `Nonce` evolution; the HFC era-schedule `EraSchedule`; the closed `LeaderCheckVerdict`. **PHASE4-N-AN (CHANGED — the BLUE change this cluster):** `consensus::praos_state` gains the NEW `pub fn PraosChainDepState::overlay_recovered_eta0(&mut self, eta0: &Nonce)` — the SINGLE eta0-overlay authority (sets `epoch_nonce` = `evolving_nonce` = the recovered seed-epoch eta0), shared by BOTH WarmStart bootstrap (the live-admit chain_dep) AND rollback materialization (the replay chain_dep), so live admit and rollback replay validate the header VRF against the SAME nonce (replay-equivalence by construction; reconstructs `genesis(eta0)`'s nonce basis at the seed epoch). It is a method, NOT a new canonical type (49 → 49). The production `select_best_chain` is byte-unchanged (the N-AI `fork_choice.rs` touch was a `#[cfg(test)]`-only arrival-order proof for CN-CONS-01). |
| **Creates** | `PraosChainDepState`, `Nonce`, `EraSchedule`, `ExpectedVrfInput`, `LeaderCheckVerdict` (closed), `ChainSelection` / candidate types, VRF-input + leader-threshold types. 49 public types (49 at both `e87e8a43` and `b8860b16` — N-AN added a method, not a type). |
| **Interprets** | Praos header VRF/leader inputs; candidate chain summaries for `select_best_chain`. `consensus::praos_state` evolves the nonce + (N-AN) overlays the recovered eta0 via `overlay_recovered_eta0`. |
| **MUST NOT** | (1) Perform I/O / read a clock / use float / `HashMap`. (2) Be the home of a SECOND chain selector — `select_best_chain` is the sole DC-CONS-03 fork-choice authority (no parallel selector; arrival-order-independent — CN-CONS-01). (3) Construct semantic types from raw bytes. (4) Depend on any RED/GREEN crate (only `ade_types`, `ade_crypto`, `minicbor`). (5) Add a SECOND `leader_vrf_input` / expected-VRF-input authority. **(6) `PraosChainDepState::overlay_recovered_eta0` (T-REC-06, N-AN):** it MUST be the SINGLE eta0-overlay authority (no second/parallel nonce-substitution site — BOTH WarmStart bootstrap and rollback materialize MUST call it, never hand-roll); it sets the recovered eta0 as both the epoch and evolving nonce and MUST NOT weaken / skip / bypass any subsequent header VRF verification (overlaying the correct nonce is not a bypass — a block whose VRF verifies against NEITHER eta0 nor the snapshot nonce still fails closed); it MUST stay pure (no I/O / clock / float / `HashMap`). |
| **Inbound deps** | `ade_ledger` (incl. the N-AN `overlay_recovered_eta0` consumer `rollback::materialize`), `ade_testkit`, `ade_core_interop`, `ade_runtime`, `ade_node`. |
| **Outbound deps** | `ade_types`, `ade_crypto`, `minicbor`. Dev-dep: `ade_testkit`. |
| **Entry points** | `ade_core::consensus::{fork_choice::select_best_chain, leader_check::*, praos_state::{PraosChainDepState (incl. `overlay_recovered_eta0`), Nonce}}`, `ade_core::era_schedule::EraSchedule`. |
| **Key modules** | `consensus/` (`fork_choice.rs`, `leader_check.rs`, `praos_state.rs` — incl. the N-AN `overlay_recovered_eta0` single eta0-overlay authority), `era_schedule.rs`. |

---

### `ade_crypto`

| Attribute | Value |
|-----------|-------|
| **Purpose** | Pure cryptographic verification — Praos VRF (draft-03) verify, the Ade-owned BLUE Sum6KES algorithm (`kes_sum/` — matches Haskell `cardano-base` byte-for-byte; the `kes_update` deterministic evolution + the KES verifier), Ed25519 DSIGN verify, blake2b hashing. (Unchanged across the N-AM/N-AN span.) |
| **Creates** | `KesError`, `KesParseError`, VRF/KES/DSIGN verify types, `Sum6` key/signature representations. 21 public types. |
| **Interprets** | VRF proofs, KES signatures + the expanded `Sum6KES` envelope, Ed25519 signatures; `blake2b_256` / `blake2b_224`. |
| **MUST NOT** | (1) **Sign** anything — verification only; signing material custody lives in the RED `ade_runtime::producer` shell (`T-KEY-01` / `ci_check_no_signing_in_blue.sh`). (2) Perform I/O / read a clock / use float. (3) Re-implement Sum6KES divergently from Haskell `cardano-base` (the cardano-crypto Rust crate is a `#[cfg(test)]` oracle only). (4) Depend on any workspace crate except `ade_types`. |
| **Inbound deps** | `ade_core`, `ade_ledger`, `ade_plutus`, `ade_runtime`, `ade_node`, `ade_core_interop`, `ade_testkit`. |
| **Outbound deps** | `ade_types`, `blake2`, `ed25519-dalek`, `cardano-crypto` (`["vrf-draft03", "kes-sum"(test-only), "dsign"]`). |
| **Entry points** | `ade_crypto::vrf::*`, `ade_crypto::kes_sum::{kes_update, kes_verify, *}`, `ade_crypto::dsign::*`, `ade_crypto::hash::{blake2b_256, blake2b_224}`. |
| **Key modules** | `vrf/`, `kes_sum/`, `dsign/`, `hash.rs`. |

---

### `ade_ledger`

| Attribute | Value |
|-----------|-------|
| **Purpose** | The functional core (ledger half): stateless ledger rules for every era; B1 block-validity verdict; B2 tx-validity verdict + mempool admission; B3 Conway value-conservation; B4/B5 closed Conway cert-state + governance-cert accumulation; live committee enactment; the single BLUE wire-ingress chokepoint `mempool::ingress::mempool_ingress`; the BLUE producer authority (`producer::{forge, self_accept, served_chain, state}`); the single body-hash authority (`block_body_hash`); the receive/rollback/snapshot/WAL/bootstrap-anchor/genesis-source authorities; the KES-signing pre-image recipe `block_validity::unsigned_header_pre_image`; the exact-`Rational` arithmetic; the `ProtocolParameters` model; (N-F-A) the closed `SeedEpochConsensusInputs` recovered-state record + its SOLE codec (`epoch_nonce`/eta0 field, `SEED_CINPUT_SCHEMA_VERSION = 2`); (N-F-G-J) the single POSITION-AWARE header authority `block_validity::header_position::check_header_position`; (N-AI) the BLUE `wal::event::WalEntry::RollBack` durable MARKER + its `RollbackPoint` / `RollbackReason` payload types + the `wal::replay` rollback re-anchor arm; (N-AK) the NEW module `recovered_anchor_point` owning the closed version-gated `RecoveredAnchorPoint` record + `RecoveredAnchorPointError` + their SOLE canonical CBOR codec. **PHASE4-N-AN (CHANGED — the BLUE rollback-replay authority this cluster):** `rollback::materialize::materialize_rolled_back_state` (the SOLE rolled-back-state authority, CN-STORE-07) gains a `recovered_eta0: Option<&Nonce>` param and overlays it (via the single `ade_core` `overlay_recovered_eta0` authority) onto the `nearest_le` snapshot chain_dep BEFORE the degenerate return AND the replay-forward `block_validity` fold — so rollback replay validates each block's header VRF against the recovered eta0, NOT the snapshot's `Nonce::ZERO` placeholder (replay-equivalence with live admit; T-REC-06). It is a fn-param change, NOT a new canonical type (181 → 181). VRF strength is UNCHANGED (a WRONG eta0 still fails the header VRF; `None`/cold-start keeps the snapshot nonce). The `receive::reducer` `RollbackContext` also gains a `recovered_eta0: Option<&Nonce>` field — but that field + its threading is on the `#[cfg(test)]` receive-driver path (the production rollback-follow threads eta0 through `apply_chain_event`, not the receive reducer). |
| **Creates** | **181** public types (181 at both `e87e8a43` and `b8860b16` — N-AN added a fn param + a test-path field, not a type; the N-AK +2 `recovered_anchor_point::{RecoveredAnchorPoint, RecoveredAnchorPointError}` are still carried). Load-bearing: `BlockValidityError` (incl. `HeaderPositionInvalid`), `BlockVerdict`, `TxValidity*`, `MempoolState`, `ProducerTick`, `ForgedBlock`, `ForgeError`, `AcceptedBlock` (field private to `producer::self_accept`), `ServedChainSnapshot`, `PoolDistrView`, `PoolEntry`, `LedgerState`, `ProtocolParameters`, `Rational`, `WalEntry` (closed sum: `AdmitBlock` + `SeedEpochConsensusInputsImported` at tag 3 + `RollBack` at tag 1, N-AI), `RollbackPoint` / `RollbackReason` (N-AI payload types), `RecoveredBootstrapProvenance`, `ReplayOutcome`, `SeedEpochConsensusInputs` (closed; carries `epoch_nonce: Nonce`; `SEED_CINPUT_SCHEMA_VERSION = 2`), `SeedConsensusInputsError` (closed), `BootstrapAnchor`, `SeedProvenance` (closed), `SeedPoint`, `ANCHOR_SCHEMA_VERSION = 2`, `MithrilManifestReport`, `BootstrapAnchorError`, `ConwayGenesisConfig`, `GenesisSourceError`, `UnsignedHeaderPreImage`, `RecoveredAnchorPoint` (closed; bound to `anchor_fp`; `RECOVERED_ANCHOR_POINT_SCHEMA_VERSION = 1`) + `RecoveredAnchorPointError` (closed). `PrevHash` is owned by `ade_types`. |
| **Interprets** | Canonical decoded blocks/txs/certs/snapshots; `mempool_ingress` is the sole BLUE chokepoint; `self_accept` re-validates a forged artifact; `decode_bootstrap_anchor` reads the version-gated framing; `verify_mithril_binding` cross-checks the Mithril manifest; `genesis_initial_state` produces the cold-start pair; `unsigned_header_pre_image` produces the canonical CBOR of `ShelleyHeaderBody`; `decode_block` calls `check_header_position` before the header authority; `decode_seed_epoch_consensus_inputs` (the SOLE sidecar decoder) reads the 7-field v2 record; `decode_wal_entry` decodes the tag-1 `WalEntry::RollBack` payload (N-AI); `decode_recovered_anchor_point` (the SOLE recovered-anchor-point decoder) reads the `array(4)` record fail-closed (N-AK). **PHASE4-N-AN:** `materialize_rolled_back_state` interprets the recovered `nearest_le` snapshot chain_dep, overlays the recovered eta0, and replays the rolled-back blocks against it (each block's header VRF verified against eta0). |
| **MUST NOT** | All carry-forward ledger prohibitions (no I/O, no clock, closed cert/governance enums, single body-hash recipe). `producer::{forge, self_accept, served_chain}` fences (CN-FORGE-01/03, CN-PROD-04). `unsigned_header_pre_image` (CN-KES-HEADER-01). `block_validity::header_position` (CN-WIRE-09 position clause). `bootstrap_anchor` / `genesis_source` / `wal` provenance fences. `seed_consensus_inputs` (CN-CINPUT-01, strengthened by T-REC-04 / DC-CINPUT-03) — a single closed canonical type with a SOLE codec pair; deterministic `BTreeMap`-ordered CBOR, version-gated; no `Default`/`#[non_exhaustive]`; no I/O. `block_validity::header_input` (DC-CONS-18). `wal::{event, replay}` rollback marker (DC-NODE-27 / CN-WAL-01 / CN-STORE-07 / DC-CONS-20, N-AI) — `WalEntry::RollBack` is a DURABLE MARKER; replay re-invokes the EXISTING `materialize_rolled_back_state` + lockstep `commit_rollback`; append-only (tag 1 reserved, tag 2 stays reserved). `recovered_anchor_point` (CN-CINPUT-01 analog, DC-NODE-31, N-AK) — a single closed canonical type + SOLE codec, version-gated, separate additive record from `SeedEpochConsensusInputs`. **`rollback::materialize::materialize_rolled_back_state` (T-REC-06 / CN-STORE-07, N-AN):** it MUST stay the SOLE rolled-back-state authority; it MUST reconstruct the replay chain_dep with the SAME recovered eta0 the live-admit path uses (overlaid via the single `ade_core::PraosChainDepState::overlay_recovered_eta0` authority, applied to the `nearest_le` snapshot chain_dep BEFORE both the degenerate-snapshot return AND the replay-forward `block_validity` fold); the persisted snapshot's placeholder / genesis `epoch_nonce` MUST NOT reach VRF verification on the rollback-replay path; the eta0 MUST be the recovered canonical input (the seed-epoch `SeedEpochConsensusInputs.epoch_nonce` sidecar) — NEVER peer data, wall-clock, CLI re-supply, or a re-query; VRF validation strength MUST be UNCHANGED on the rollback path (no bypass / skip / loosening — a WRONG eta0 still fails the header VRF, and `None` keeps the snapshot nonce as-is for cold-start); the overlay MUST go through the single `overlay_recovered_eta0` authority (no hand-rolled nonce substitution). SCOPE: the recovered seed epoch (no epoch-boundary crossing within the follow window); a multi-epoch rollback nonce-evolution is a named out-of-scope follow-on (`ci_check_rollback_materialize_eta0.sh`). |
| **Inbound deps** | `ade_testkit`, `ade_core_interop`, `ade_runtime` (incl. the N-AK `recovered_anchor_point` codec consumers + the N-AN `materialize_rolled_back_state` caller via `bootstrap`), `ade_node` (incl. the N-AN `apply_chain_event` eta0-threading caller). |
| **Outbound deps** | `ade_types`, `ade_crypto`, `ade_codec` (incl. the canonical CBOR primitives the N-AK `recovered_anchor_point` codec uses), `ade_plutus`, `ade_core` (incl. `consensus::praos_state::{Nonce, PraosChainDepState::overlay_recovered_eta0}` — N-AN), `minicbor`, `num-bigint`, `num-integer`, `num-traits`. Dev-dep: `ade_testkit`. |
| **Entry points** | `ade_ledger::block_validity::{decode_block, validate_block, check_header_position, block_header_bytes, accepted_block_header_bytes, unsigned_header_pre_image::unsigned_header_pre_image}`, `ade_ledger::tx_validity::*`, `ade_ledger::mempool::{ingress::mempool_ingress, admit::*}`, `ade_ledger::producer::{forge::forge_block, self_accept::{self_accept, AcceptedBlock}, served_chain::*}`, `ade_ledger::block_body_hash::*`, `ade_ledger::{receive, rollback, snapshot}::*` (incl. **`rollback::materialize::materialize_rolled_back_state`** — N-AN: the `recovered_eta0: Option<&Nonce>` param), `ade_ledger::pparams::ProtocolParameters`, `ade_ledger::rational::Rational`, `ade_ledger::wal::*` (incl. `wal::event::{WalEntry::RollBack, RollbackPoint, RollbackReason}`, `wal::replay::compute_superseded`), `ade_ledger::seed_consensus_inputs::{SeedEpochConsensusInputs, encode_seed_epoch_consensus_inputs, decode_seed_epoch_consensus_inputs, SeedConsensusInputsError, SEED_CINPUT_SCHEMA_VERSION}`, `ade_ledger::consensus_view::{PoolDistrView, PoolEntry}`, `ade_ledger::bootstrap_anchor::*`, `ade_ledger::genesis_source::*`, `ade_ledger::recovered_anchor_point::{RecoveredAnchorPoint, RecoveredAnchorPointError, encode_recovered_anchor_point, decode_recovered_anchor_point, RECOVERED_ANCHOR_POINT_SCHEMA_VERSION}` (N-AK). |
| **Key modules** | `block_validity/` (incl. `unsigned_header_pre_image.rs`, `header_input.rs`, `header_position.rs`, `verdict.rs`), `tx_validity/`, `mempool/`, `cert_classify/`, `consensus_view.rs`, `producer/`, `block_body_hash.rs`, `receive/` (incl. `reducer.rs` — the N-AN `#[cfg(test)]`-path `RollbackContext.recovered_eta0` field), `rollback/` (incl. **`materialize.rs`** — N-AN: the `recovered_eta0` param + the overlay-before-fold), `snapshot/`, `pparams.rs`, `rational.rs`, `wal/` (incl. `event.rs` — the `WalEntry::RollBack` variant at tag 1; `replay.rs` — the rollback re-anchor; `error.rs` — `WalError::RollbackTargetNotInChain`), `seed_consensus_inputs.rs` (`epoch_nonce` field + `SEED_CINPUT_SCHEMA_VERSION = 2`), `bootstrap_anchor/`, `genesis_source.rs`, `recovered_anchor_point.rs` (N-AK: the closed `RecoveredAnchorPoint` record + `RecoveredAnchorPointError` + the sole canonical codec). |

> **GREEN sub-classification:** `ade_ledger::mempool::{policy, canonicalize}` are GREEN-by-content. **RED sub-classification:** `ade_ledger::consensus_input_extract` is RED-by-content. Both carry their own module banner.

---

### `ade_network` *(BLUE submodules)*

| Attribute | Value |
|-----------|-------|
| **Purpose** | Owns the Cardano Ouroboros mini-protocol authority — the closed wire grammar (CBOR codecs) and pure state machines for all N2N + N2C mini-protocols, plus the BLUE mux frame primitive and the producer-side server-role reducers. Owns the per-protocol tag-24 composition authorities. **`keep_alive/` (mini-protocol 8):** owns the BLUE keep-alive grammar `keep_alive::keep_alive_transition` (the pure `ClientIdle ↔ ServerHasAgency{cookie}` state machine + agency) + the `codec::keep_alive::{encode,decode}_keep_alive_message` over the closed `KeepAliveMessage` + `KeepAliveCookie`. **PHASE4-N-AM REUSES `keep_alive/` UNCHANGED** — the new keep-alive CLIENT lives in the RED `ade_runtime::admission::wire_pump`; it advances this BLUE state machine and validates the echoed cookie, but adds NO `ade_network` change (the grammar is byte-untouched). **`handshake/` (G-L):** the single per-version N2N `versionData` wire encoding `handshake::version_table::encode_n2n_version_params` (CN-WIRE-10). **`chain_sync/` + `codec/` (G-M):** the scoped two-form ChainSync `FindIntersect` decode + the `Origin → IntersectFound[Origin]` reply (CN-WIRE-11). **`chain_sync/server.rs` (N-AE):** the `FindIntersect` cursor sets `last_announced` to the resolved intersect (`DC-PROTO-10`). (No `ade_network` change in the N-AM/N-AN span.) |
| **Creates** | **110** BLUE canonical types: `mux/frame.rs` 5, `codec/` 39 (incl. the closed `ArrayHead = Definite(u64) \| Indefinite`, CN-WIRE-11), `handshake/` 9, `chain_sync/` 11, `block_fetch/` 10, `tx_submission/` 5, `keep_alive/` 5 (incl. `KeepAliveMessage`, `KeepAliveCookie`, `KeepAliveState`, the agency/output types), `peer_sharing/` 5, `n2c/` 21. |
| **Interprets** | Mini-protocol wire frames; `mux::frame::decode_frame` is the single frame decode authority; per-protocol `*_transition` reducers consume decoded messages. `decompose_blockfetch_block` / `decompose_rollforward_header` strip the per-protocol tag-24 composition. `decode_find_intersect_points` decodes the `FindIntersect` points list via `decode_array_head_two_form` (SCOPED to FindIntersect — CN-WIRE-11). `keep_alive_transition` consumes a decoded `KeepAliveMessage` + the current agency (REUSED by the N-AM RED client). |
| **MUST NOT** | (1) `mux::frame` — single `encode_frame`/`decode_frame` pair. (2) handshake — single `n2n_transition` + single `n2c_transition`. (3) closed `AcceptedMiniProtocol` registry. (4) no socket I/O in BLUE submodules. (5) no async/tokio in BLUE submodules. (6) depend on no workspace crate beyond `ade_codec` + `ade_types`. (7) server reducers MUST NOT split headers in parallel or depend on signing. (8) tag-24 composition (CN-WIRE-08) — composers delegate byte-level wrap/strip to the single `ade_codec` authority. (8b) `n2n_supported_for_magic` (DC-NODE-07) — parameterizes ONLY `network_magic`. (8c) `encode_n2n_version_params` (CN-WIRE-10). (8d) `decode_array_head_two_form` / `ArrayHead` + the `Origin` reply (CN-WIRE-11). (8e) `producer_chain_sync_serve` FindIntersect cursor (DC-PROTO-10). **(8f) `keep_alive` grammar (CN-PUMP-01 cross-ref / DC-PUMP-03, N-AM context):** the keep-alive state machine + codec MUST stay the SINGLE keep-alive grammar — the RED `ade_runtime::admission::wire_pump` keep-alive client MUST REUSE `keep_alive_transition` + `codec::keep_alive`, never redefine the cookie/agency rules; `keep_alive/` stays BLUE (no socket I/O, no async). |
| **Inbound deps** | `ade_runtime` (incl. the N-AM keep-alive-client consumer of `keep_alive` + `codec::keep_alive`), `ade_node`, `ade_core_interop`, `ade_testkit` (and the `ade_network` GREEN/RED submodules). |
| **Outbound deps** | `ade_types`, `ade_codec`. No external deps in BLUE submodules. |
| **Entry points** | `ade_network::mux::frame::{encode_frame, decode_frame}`, `ade_network::handshake::{n2n_transition, n2c_transition}`, `ade_network::handshake::version_table::{N2N_SUPPORTED, n2n_supported_for_magic, encode_n2n_version_params}`, `ade_network::chain_sync::{server::*, client::*}`, `ade_network::block_fetch::{server::*, client::*}`, `ade_network::keep_alive::{keep_alive_transition, KeepAliveState, KeepAliveAgency, KeepAliveEvent, KeepAliveOutput, KeepAliveError}`, `ade_network::codec::{block_fetch::{compose,decompose}_blockfetch_block, chain_sync::{{compose,decompose}_rollforward_header, Point}, keep_alive::{encode_keep_alive_message, decode_keep_alive_message, KeepAliveMessage, KeepAliveCookie}, primitives::{decode_array_head_two_form, ArrayHead}}`, `ade_network::{tx_submission, peer_sharing, n2c}::*`. |
| **Key modules** | `mux/frame.rs`, `codec/` (incl. `block_fetch.rs`, `chain_sync.rs`, `keep_alive.rs`, `primitives.rs`), `handshake/` (incl. `version_table.rs`), `chain_sync/` (incl. `server.rs`), `block_fetch/` (incl. `server.rs`), `tx_submission/`, `keep_alive/` (incl. `transition.rs` — the BLUE `keep_alive_transition` REUSED by the N-AM client), `peer_sharing/`, `n2c/`. |

---

### `ade_plutus`

| Attribute | Value |
|-----------|-------|
| **Purpose** | Quarantine boundary between the Ade-canonical ledger and the ported UPLC evaluator from `aiken-lang/aiken` (pinned tag `v1.1.21`). (Unchanged across the N-AM/N-AN span.) |
| **Creates** | `PlutusScript`, `PlutusLanguage`, `EvalOutput`, `PlutusError`, `CostModels`, `DecoderMode`, `PerScriptResult`, `TxEvalResult`. 8 public types. |
| **Interprets** | UPLC scripts (Plutus V1/V2/V3) and `CostModels` CBOR; phase-two tx evaluation. `PlutusScript::from_cbor` is a named ingress chokepoint. |
| **MUST NOT** | (1) Re-export any `pallas_*` or `aiken_uplc::` type. (2) Let another BLUE crate bypass the canonical entry. (3) Activate PV11 builtins. (4) Use any BLUE-forbidden pattern. (5) Construct `PreservedCbor` outside `ade_codec`. |
| **Inbound deps** | `ade_ledger`, `ade_testkit`. |
| **Outbound deps** | `ade_types`, `ade_crypto`, `ade_codec`, `aiken_uplc` (git, tag `v1.1.21`), `pallas-primitives` (internal-only). |
| **Entry points** | `ade_plutus::eval_tx_phase_two`, `ade_plutus::tx_eval::*`, `ade_plutus::evaluator::*`, `ade_plutus::cost_model::*`. |
| **Key modules** | `evaluator.rs`, `cost_model.rs`, `script_context.rs`, `script_verdict.rs`, `tx_eval.rs`. |

---

### `ade_types`

| Attribute | Value |
|-----------|-------|
| **Purpose** | Owns the canonical Cardano domain vocabulary — primitives, era enum, address forms, era-tagged tx bodies/outputs/certificates, governance types — the lingua franca for every other crate. `CardanoEra` carries `is_praos()`. **N-F-G-J (carried)** owns the closed sum `PrevHash = Genesis | Block(Hash32)`. `Nonce` (the recovered eta0 type overlaid by N-AN's `overlay_recovered_eta0`) is created here / `ade_core` (re-exported); `Hash32` / `SlotNo` are the field types of the (N-AK) `RecoveredAnchorPoint` record. (No `ade_types` change in the N-AM/N-AN span — `82` byte-stable.) |
| **Creates** | `CardanoEra` (with `is_praos()`), `SlotNo`, `BlockNo`, `EpochNo`, `Hash28`, `Hash32`, `Coin`, `Lovelace`, `NetworkId`, `Nonce`, `TxIn`, `RewardAccount`, `PoolId`, `Address`, `ByronAddress`, `Credential`, `StakeCredential`, `Certificate`, `ConwayCert`, `DRep`, `GovAction`, `GovActionId`, `Anchor`, `ProposalProcedure`, `OperationalCert`, `NativeScript`, `Datum`, `MultiAsset`, `AssetName`, `CostModel`, `ExUnits`, per-era tx-body/tx-out/witness wrappers, `ShelleyBlock`/`ShelleyHeader`/`VrfData`/`ProtocolVersion`/`PrevHash`. 82 public types. |
| **Interprets** | None — produce-only. |
| **MUST NOT** | (1) Construct or decode `PreservedCbor`. (2) Use any BLUE-forbidden pattern. (3) Depend on any workspace crate. (4) Add open/extensible variants to closed enums without a versioned gate (`CardanoEra` discriminants closed: Byron=0 … Conway=7; `PrevHash` is a closed 2-variant `Genesis | Block(Hash32)` sum, never a flat `Hash32`). |
| **Inbound deps** | Every other workspace crate. |
| **Outbound deps** | None. |
| **Entry points** | `ade_types::CardanoEra` (incl. `is_praos`), `ade_types::tx::{Coin, TxIn, RewardAccount}`, `ade_types::{Hash32, SlotNo, Hash28, BlockNo, EpochNo, Nonce}`, `ade_types::conway::{tx::ConwayTxBody, cert::*, governance::*}`, `ade_types::shelley::block::{OperationalCert, ProtocolVersion, ShelleyHeader, ShelleyBlock, VrfData, PrevHash}`. |
| **Key modules** | `primitives.rs`, `era.rs`, `tx.rs`, `address/`, `byron/`, `shelley/`, `allegra/`, `mary/`, `alonzo/`, `babbage/`, `conway/`. |

---

## GREEN Modules — Deterministic Glue

> Deterministic, non-authoritative. May depend on BLUE; must not affect authoritative outputs. GREEN sub-trees inside the RED `ade_runtime` and `ade_node` crates carry a `//! GREEN …` banner + the same deny attributes and are CI-gated for purity. **PHASE4-N-AN touched ONE nominally-GREEN host (`ade_runtime::forward_sync::reducer`)** — adding the `ForwardSyncState.recovered_eta0: Option<Nonce>` field (replay-derivable; default `None`), the threaded recovered-eta0 the live rollback-follow path (`apply_chain_event`) overlays onto the materialize replay chain_dep (T-REC-06). **PHASE4-N-AM touched NO GREEN host** (its sole change is the RED `ade_runtime::admission::wire_pump` keep-alive client). The full per-module GREEN entries below are carried forward.

### `ade_node::convergence_evidence` *(GREEN by content — PHASE4-N-AJ)*

| Attribute | Value |
|-----------|-------|
| **Purpose** | The GREEN closed-vocabulary convergence-evidence sink for the live Participant rollback-follow path. `ConvergenceEvidenceSink` writes the reused closed `AgreementVerdict` vocabulary (`block_received` / `block_admitted` / `agreement_verdict` via `verdict::derive`) to a dedicated `--convergence-evidence-path` JSONL sink as an emit-only side-output — the CE-AI-6 bridge that closed the gap where the rollback-follow (node) path emitted no agreement evidence. (Unchanged in shape across the N-AM/N-AN span; the N-AN CE-AI-6 reorg capture is emitted through this sink.) |
| **Creates** | `ConvergenceEvidenceSink` (over a `Box<dyn Write>` — RED file I/O underneath, no raw-writer accessor), `EvidenceEmitResult { Written \| Disabled \| FailedAndPoisoned }` (closed). Not canonical-counted (GREEN). |
| **MUST NOT** | Affect authoritative outputs; halt authority on a write failure (a write-failure surfaces + poisons + marks incomplete via `EvidenceEmitResult::FailedAndPoisoned`, NEVER swallowed, NEVER halts the authority path); add an open/wildcard event variant to the closed `AgreementVerdict` vocabulary (DC-ADMIT-04); emit on a non-Diverged path that overstates semantic truth (emit-only on `Diverged`); change the byte output when `--convergence-evidence-path` is absent; expose a raw-writer accessor. |
| **Inbound deps** | `ade_node::node_lifecycle`. |
| **Outbound deps** | the reused `AgreementVerdict` verdict vocabulary + `verdict::derive`; `std::io::Write`. |
| **Entry points** | `ade_node::convergence_evidence::{ConvergenceEvidenceSink, EvidenceEmitResult}`. |

### `ade_network::session` *(GREEN by content — PHASE4-N-L; outbound segmentation N-AB)*

| Attribute | Value |
|-----------|-------|
| **Purpose** | The deterministic GREEN session reducer for the Cardano Ouroboros mux — `step` is the SOLE pure fn reducing `(SessionState, ByteChunkIn) -> Result<(SessionState', Vec<SessionEffect>), SessionError>` (CN-SESS-03), composing the BLUE `mux::frame::{encode,decode}_frame` + `handshake::n2n_transition` + the closed `AcceptedMiniProtocol` registry + per-mini-protocol frame demux/reassembly. It owns the type-state (`Handshaking` → `Connected`; DC-SESS-01) and the symmetric per-mini-protocol payload assembly: INBOUND it emits one `DeliverPeerFrame` per COMPLETE CBOR item (CN-SESS-04); OUTBOUND `handle_outbound` SEGMENTS an outbound payload larger than `MAX_PAYLOAD` into ordered `<= MAX_PAYLOAD` mux frames, fail-closed above `MAX_OUTBOUND_PAYLOAD_BYTES = 16 MiB` (CN-SESS-05). GREEN-by-content — every file is pure / deterministic / no tokio / SystemTime / Instant / rand. (Unchanged across the N-AM/N-AN span.) |
| **Creates** | `SessionState` (type-state), `ConnectedState` (closed-sum-indexed `proto_buffers` — no HashMap), `HandshakeProgress`, `SessionEffect` (closed), `SessionError` (closed — incl. `ReassemblyBufferOverflow` + `OutboundPayloadTooLarge`), `AcceptedMiniProtocol` (the CLOSED registry — GREEN), `ByteChunkIn`, `HandshakeRole`, `FrameBuffer`. The const `MAX_OUTBOUND_PAYLOAD_BYTES = 16 MiB` — a GREEN constant, NOT a canonical type. None canonical-counted. |
| **MUST NOT** | Affect authoritative outputs; introduce nondeterminism (no wall-clock / rand / float / HashMap). **CN-SESS-03:** `step` is the SOLE reduction entry. **CN-SESS-01/02:** frame encode/decode through the single BLUE `mux::frame`; handshake through the single `n2n_transition`. **DC-SESS-01:** a `Handshaking` session MUST NOT deliver any non-handshake frame. **CN-SESS-04:** one accumulating buffer per `AcceptedMiniProtocol`; one `DeliverPeerFrame` per COMPLETE CBOR item. **CN-SESS-05:** an outbound payload > `MAX_PAYLOAD` MUST be SEGMENTED (no truncate/drop/reorder; `concat(segments) == payload`; same id/mode/timestamp; fail closed above the FIXED `MAX_OUTBOUND_PAYLOAD_BYTES = 16 MiB`). |
| **Inbound deps** | `ade_runtime::network::{mux_pump, n2n_dialer, n2n_listener}`, `ade_runtime::admission::wire_pump`, `ade_node::wire_only`, `ade_network::mux::transport`. |
| **Outbound deps** | BLUE `ade_network::{mux::frame, handshake}`; `ade_codec`; `ade_types`. No tokio (GREEN). |
| **Entry points** | `ade_network::session::{step, FrameBuffer, SessionState, ConnectedState, HandshakeProgress, SessionEffect, SessionError, AcceptedMiniProtocol, ByteChunkIn, HandshakeRole}`, `ade_network::session::handshake_driver::{run_n2n_handshake_initiator, run_n2n_handshake_responder}`. |
| **Key modules** | `session/core.rs` (`step` + inbound reassembly + outbound segmentation), `session/state.rs`, `session/demux.rs`, `session/event.rs`, `session/handshake_driver.rs`. |

> **GREEN classification (load-bearing):** `ade_network::session` is **promotable to BLUE on demand, never demotable to RED**.

### `ade_runtime::seed_consensus_merge` *(GREEN by content — PHASE4-N-F-A A2; eta0 threaded N-F-G-N)*

| Attribute | Value |
|-----------|-------|
| **Purpose** | The deterministic, no-I/O merge transform lifting a verified-bootstrap `LiveConsensusInputsCanonical` plus the minted anchor fingerprint + seed epoch into the BLUE single-map `SeedEpochConsensusInputs`, carrying the recovered Praos `epoch_nonce` (eta0) into the BLUE record's `epoch_nonce` field (T-REC-04). (Unchanged in shape across the N-AM/N-AN span.) |
| **Creates** | `SeedConsensusMergeError` (closed: `PoolMissingVrfKeyhash` / `PoolMissingStake`). Not canonical-counted (GREEN). |
| **MUST NOT** | Affect authoritative outputs. Introduce nondeterminism (`BTreeMap` only). Default-fill on a provenance gap. Carry the recovered eta0 as anything but verbatim (DC-CINPUT-03 — never a zero-eta0 fallback). |
| **Inbound deps** | `ade_runtime::{genesis_bootstrap, mithril_bootstrap}`. |
| **Outbound deps** | `ade_ledger::{consensus_view::PoolEntry, seed_consensus_inputs::SeedEpochConsensusInputs}`, `ade_runtime::consensus_inputs::LiveConsensusInputsCanonical`, `ade_core::consensus::praos_state::Nonce`, `ade_types`. |
| **Entry points** | `ade_runtime::seed_consensus_merge::{merge_seed_epoch_consensus_inputs, SeedConsensusMergeError}`. |

---

### `ade_runtime` GREEN-by-content sub-trees (carried)

> All carry a `//! GREEN …` banner inside the RED `ade_runtime` crate, the BLUE deny attributes, and a purity CI gate. Each is "promotable to BLUE on demand, never demotable to RED" unless noted.

| Sub-tree | Purpose | MUST NOT | CI gate |
|---|---|---|---|
| `producer::self_accepted_handoff` *(N-F-G-B)* | GREEN constructor-fenced carrier for a BLUE self-accepted forged block (sole ctor `from_self_accepted(AcceptedBlock)`). | Carry an artifact that was not BLUE self-accepted; re-derive / re-validate; affect authoritative outputs. | `ci_check_served_chain_handoff_fence.sh` |
| `consensus_inputs::protocol_params` *(N-F-G-A)* | GREEN cardano-cli protocol-parameters JSON → BLUE `ProtocolParameters`; exact decimal/scientific → `Rational` by integer arithmetic. | Use any float path; affect authoritative outputs; introduce nondeterminism. | `ci_check_recovered_ledger_pparams_sourced.sh` |
| `consensus_inputs::canonical` (`require_forge_current_pparams`) *(N-F-G-A)* | GREEN hash-bound current-pparams accessor (`protocol_params_json` OUTSIDE the 15-field fingerprint). | Alter the bundle fingerprint; return pparams without the `blake2b_256` hash bind. | `ci_check_recovered_ledger_pparams_sourced.sh` |
| `seed_consensus_merge` *(N-F-A; eta0 N-F-G-N)* | GREEN bootstrap-time merge → BLUE `SeedEpochConsensusInputs` (carrying recovered eta0). *(Full entry above.)* | Default-fill a provenance gap / zero-eta0; affect authoritative outputs. | `ci_check_consensus_input_provenance.sh`, `ci_check_warmstart_eta0_overlay.sh` |
| `forward_sync::reducer` *(N-Y; recovered_anchor field N-AK; recovered_eta0 field N-AN)* | GREEN forward-sync lifecycle reducer; the private `AdmitPlan::durable` is the sole `AdvanceTip` emitter (durable-before-tip). Carries `ForwardSyncState.recovered_anchor: Option<ChainTip>` (N-AK — the threaded recovered-anchor authority the single-producer + participant RollBack handlers read; DC-NODE-32/33). **N-AN (CHANGED):** `ForwardSyncState` ALSO gains the additive field `recovered_eta0: Option<Nonce>` (replay-derivable; default `None`) — the recovered seed-epoch eta0 the live rollback-follow path (`apply_chain_event`) overlays onto the materialize replay chain_dep so rollback replay validates the header VRF against the SAME nonce as live admit (T-REC-06). It is NOT consensus authority — the overlay + the VRF verification happen in the BLUE `materialize_rolled_back_state`; this field only carries the recovered value forward. | Emit `AdvanceTip` before `StoreBlockBytes` + `AppendWal` (DC-SYNC-01); affect authoritative outputs; materialize `recovered_anchor` into a servable block (N-AK); **(N-AN) carry `recovered_eta0` as anything but the verbatim recovered seed-epoch eta0, or default-fill / zero it on a provenance gap (the overlay + the VRF check are the BLUE materialize's job — the field MUST NOT itself weaken any VRF verification).** | `ci_check_forward_sync_chokepoint_only.sh`, `ci_check_rollback_materialize_eta0.sh` |
| `producer::coordinator` *(N-Q)* | Pure coordinator for live producer-mode; hosts `CoordinatorState::kes_period_for_slot`. | Own/store private signing material (CN-PROD-02); affect authoritative outputs. | `ci_check_producer_coordinator_no_secrets.sh` |
| `producer::producer_log` *(N-Q, extended N-W)* | Closed-vocabulary producer evidence log. | Add an open/wildcard event variant; emit `BlockServed` for a block not in the served snapshot (CN-PROD-04). | `ci_check_operator_evidence_manifest_schema.sh` |
| `producer::chain_evolution` *(N-T; N-F-G-J)* | Linear `ChainEvolution` typestate; `prev_hash() -> PrevHash`. | Never mints `AcceptedBlock`; advance on authority disagreement; propose an all-zero `Hash32` for the genesis predecessor (CN-WIRE-09). | `ci_check_prevhash_single_wire_authority.sh` |
| `producer::{broadcast_to_served, served_chain_lookups}` *(N-G)* | GREEN served-chain broadcast targets + read-side lookups. | Affect authoritative outputs; serve a block that failed self-accept. | `ci_check_broadcast_to_served_purity.sh` |
| `clock` (`DeterministicClock` + `Clock` + `millis_to_slot` + `checked_millis_to_slot`) *(N-K; N-F-G-A)* | GREEN clock trait + deterministic impl + the pure slot projections. | `DeterministicClock` reads no wall-clock; `checked_millis_to_slot` uses no float/wall-clock and MUST NOT saturate a before-anchor tick. | `ci_check_clock_seam.sh` |
| `orchestrator::{mod, event, state, core}` *(N-K)* | GREEN core reducer + closed-vocabulary event + state. | tokio imports in core; open event vocabulary. | `ci_check_orchestrator_core_purity.sh` |
| `rollback::{cadence, in_memory_cache, chaindb_block_source, persistent_cache, persistent_writer}` *(N-I/N-J/N-K)* | GREEN rollback adapter glue + snapshot cadence + persistent cache/writer. | Parallel snapshot cadence; affect authoritative rollback state. | `ci_check_persistent_writer_no_parallel_cadence.sh`, `ci_check_snapshot_cadence_purity.sh` |
| `receive::{events_to_state, in_memory_chain_write}` *(N-H)* | GREEN receive-side glue. | Affect authoritative receive verdict. | `ci_check_receive_replay_purity.sh` |
| `seed_import` *(N-M-A)* | Single authority converting a cardano-cli JSON UTxO dump into canonical seed entries. | Construct semantic types bypassing canonical decoders. | `ci_check_seed_import_closure.sh`, `ci_check_seed_import_full_preprod_support.sh` |
| `consensus_inputs` *(N-M-C; extended N-F-G-A)* | Operator-extracted `LiveConsensusInputs` importer + the `protocol_params` parser + `require_forge_current_pparams`. | Treat the peer as runtime authority; overstate semantic truth. | `ci_check_live_consensus_inputs_closure.sh`, `ci_check_live_consensus_inputs_fingerprint.sh` |
| `admission::*` (GREEN reducer half + the reused dial/pump) *(N-M-C; reused N-F-G-C; tag-24 unwrap N-F-G-O; rollback preserve N-AI; keep-alive client N-AM)* | GREEN admission verdict/agreement reducer; the RED `admission::{dial_for_admission, run_admission_wire_pump}` are reused verbatim by the live `--mode node` feed. The feed-side tag-24 unwrap, the `AdmissionPeerEvent::RollBackward` rollback-signal preservation, and (N-AM) the keep-alive CLIENT are RED (full notes in the RED `ade_runtime` entry). | Emit RED verdicts; skip reference scripts; treat `lagging` as success (DC-EVIDENCE-01); (N-AM) emit any `AdmissionPeerEvent` from the keep-alive client (wire-only, DC-PUMP-03). | `ci_check_admission_runner_closure.sh`, `ci_check_admission_no_red_verdicts.sh`, `ci_check_lagging_is_evidence_only.sh`, `ci_check_admit_replay_equivalence.sh`, `ci_check_keep_alive_wire_only.sh` |

> **Note on `bootstrap` (the `bootstrap_initial_state` authority, N-K; eta0 N-F-G-N; live-follow start N-AK; restore-before-materialize reorder N-AN):** it carries a `//! GREEN single bootstrap authority` banner but also hosts the **RED** A3b warm-start sidecar restore (`restore_seed_epoch_consensus_inputs`, a `SnapshotStore` read). It is documented in the RED `ade_runtime` entry below. **PHASE4-N-AN:** `bootstrap_initial_state` was REORDERED so the sidecar restore runs BEFORE the WarmStart `materialize_rolled_back_state` call, and now passes the recovered eta0 INTO that materialize call (so the recovered eta0 overlays the WarmStart replay chain_dep too — fixing warm-start replay from a non-bare store, the SAME root cause as the live rollback-follow); the existing post-materialize eta0 overlay (the SOLE `ci_check_warmstart_eta0_overlay.sh` site, T-REC-04) is idempotent after the materialize overlay. **PHASE4-N-AK:** `bootstrap_initial_state` resolves the live-follow start tip via the private pure `resolve_live_follow_start` (module-private, sole caller `bootstrap_initial_state`, to preserve the CN-NODE-01 single-`pub fn` bootstrap authority).

---

### `ade_node` GREEN-by-content sub-trees (carried)

| Sub-tree | Purpose | MUST NOT | CI gate |
|---|---|---|---|
| `forge_intent` *(N-F-F S1)* | GREEN pure tri-state forge-intent classifier. | Observe key contents; carry a third "partial" variant; carry a path/secret byte. | `ci_check_forge_intent_closed.sh` |
| `run_loop_planner` *(N-F-D S1; forge step N-F-E S1; venue policy N-AG)* | GREEN pure lifecycle planner (closed `LoopStep`/`ForgeSlotStatus`/`VenuePolicy`). | `plan_loop_step` reference `SlotNo`; return a non-closed `LoopStep`; decide any authority; add an open/wildcard `VenuePolicy` arm. | `ci_check_loop_planner_closed.sh`, `ci_check_single_producer_loop_continuation.sh` |
| `node_sync::forge_epoch_admission` *(N-F-G-A S4)* | GREEN pure epoch-boundary guard over the closed `ForgeEpochAdmission`, via the BLUE `EraSchedule::locate`. | Fabricate the candidate epoch; run after leadership; drive a nonce promotion (DC-EPOCH-03). | `ci_check_node_forge_single_epoch_fail_closed.sh` |
| `live_log::{sched_event, sched_writer}` *(N-F-G-J S1; forge-base evidence N-AH S4a)* | GREEN closed **emit-only** feed/forge scheduling-event vocabulary `NodeSchedEvent` (incl. `ForgeBaseSelected` + `ForgeBaseSource::LocalChaindbTip`) + a hand-rolled byte-deterministic JSONL writer; operational/diagnostic tier ONLY (CN-NODE-04). | Alter the relay loop's control flow; add an open/wildcard variant; be READ by the planner / any authority path; carry a `ForgeBaseSource` other than `LocalChaindbTip` (DC-NODE-20/21). | `ci_check_node_sched_events_emit_only.sh`, `ci_check_live_transcript_forge_base.sh` |
| `ba02_evidence` *(N-F-C, library; consumed by `ba02_pass`)* | GREEN BA-02 evidence correlator (`correlate` is the sole `Ba02Manifest` ctor). | Treat a weaker signal as acceptance; recompute the forged hash; forge/admit/persist. | `ci_check_ba02_evidence_closed.sh`, `ci_check_ba02_evidence_manifest_schema.sh` |
| `convergence_evidence` *(N-AJ)* | GREEN closed `AgreementVerdict`-vocabulary convergence-evidence sink (`ConvergenceEvidenceSink`). *(Full entry above.)* | Affect authoritative outputs; halt authority on a write failure; add an open variant; emit on non-Diverged; change bytes when the path is absent. | `ci_check_convergence_evidence_vocabulary_closed.sh`, `ci_check_convergence_evidence_emit_only.sh` |
| `rehearsal_evidence` *(N-F-G-D S2)* | GREEN non-promotable private-testnet rehearsal-evidence envelope. | Construct from anything but a correlate-produced payload; live under / be referenced by the bounty home; flip any RO-LIVE rule. | `ci_check_rehearsal_manifest_schema.sh` |
| `admission_log` *(N-M-B)* | GREEN admission-mode JSONL event vocabulary + writer (closed enum). | Add open/wildcard event variant. | `ci_check_admission_log_vocabulary_closed.sh` |
| `live_log` *(N-L-LIVE)* | GREEN closed JSONL vocabulary for the wire-only live pass. | Add open event variant; overstate semantic truth. | `ci_check_wire_only_event_vocabulary_closed.sh` |

> **Note on `node_sync` / `node_lifecycle` color (carried + N-AF…N-AN):** `node_sync` + `node_lifecycle` are **RED** (they drive the apply path / own the persistent-store reads + the live-feed forge wrap + the serve sibling). Their GREEN-by-function decisions (`forge_epoch_admission`, `forge_header_position`, `may_cold_start_forge`, `forge_followed_tip_admission`, `single_producer_forge_decision`, `venue_policy`, `warm_start_forge_mode`, `forge_mode_after_admit`, `classify_receive`, `resolve_disposition`, `pending_reselection_forge_refusal`, `durable_tip_matches`) carry GREEN-pure semantics within the RED host modules. The single-producer + participant RollBack handlers (the recovered-anchor idempotent-no-op-vs-fail-closed decision; N-AK/N-AL) read the threaded `ForwardSyncState.recovered_anchor` (the SINGLE anchor authority, never re-read from the store inside the loop). **PHASE4-N-AN:** `apply_chain_event` threads the recovered seed-epoch eta0 (`state.seed_epoch_consensus_inputs`'s `epoch_nonce`, carried via `ForwardSyncState.recovered_eta0`) into the BLUE `materialize_rolled_back_state` call on the live rollback-follow path — RED loop wiring of a BLUE-authoritative input (the overlay + the VRF verification are the BLUE materialize's job). `ade_testkit::consensus::genesis_pinning` is GREEN `#[cfg(test)]` test infrastructure. |

---
### `ade_testkit` *(GREEN — deterministic test/corpus infrastructure; dev-dep / `#[cfg(test)]`-scoped)*

| Attribute | Value |
|-----------|-------|
| **Purpose** | GREEN deterministic TEST / CORPUS infrastructure for the workspace — the oracle/differential harness, the validity & tx-validity corpora (incl. the Conway `ConwayValidityCorpus` loader), the consensus & governance replay corpora, the producer reference vectors, and the snapshot/genesis/ledger diff loaders. The crate `lib.rs` opens the `// Core Contract:` banner (deterministic; no wall-clock / rand / HashMap / float) and re-exports seven sub-trees: `consensus` (the `corpus`/`stream_replay` HFC + nonce/leader/fork-choice/rollback corpus-path drivers + the `#[cfg(test)]` `genesis_pinning` harness + the `ledger_view_stub`), `governance`, `harness` (the oracle/diff toolkit — per-era block-field decoders, manifest/corpus/provenance parsers + validators, differential reports, snapshot/shelley/genesis loaders, transcript, address-extractor, era-mapping), `mempool`, `producer`, `tx_validity`, and `validity`. It exercises the REAL BLUE/RED authorities and asserts against committed reference corpora — the home of `replay_cmd = cargo test -p ade_testkit`. (Unchanged across the N-AM/N-AN span.) |
| **Creates** | GREEN test/corpus value types — NONE canonical-counted: `ConwayValidityCorpus` + `CorpusRatio` / `CorpusPool` / `CorpusLoadError`; `BlockReplay` / `TxReplay` + `BlockRejectClass` / `TxRejectClass`; the adversarial mutation enums `Mutation` / `WitnessMutation` / `SyntheticMutation` + `ExtractedTx` / `ExtractError`; `OracleManifest` / `RegressionCorpus` + validators + `HarnessError`; `DifferentialReport`, `Transcript` / `TranscriptMessage` / `Direction`, `ShelleyOracleUtxo` / `SnapshotHeader`, `EpochStakeFixture` / `PoolFixture`, `CorpusEntry`; `VrfReferenceVector` / `KesReferenceVector`. All GREEN. |
| **Interprets** | Committed reference corpora + oracle dumps, for the test harness only: per-era raw block CBOR; cardano-node LedgerDB snapshot tarballs; Shelley genesis UTxO dumps; oracle/regression-corpus TOML; corpus tx/block bytes. Evidence inputs, never runtime authority. |
| **MUST NOT** | (1) **Be a production runtime dependency.** It is GREEN dev-dep / `#[cfg(test)]`-scoped — an INBOUND `[dev-dependencies]` of the BLUE crates (`ade_core`, `ade_ledger`) and the RED crates (`ade_network`, `ade_runtime`, `ade_node`), NEVER in any production crate's plain `[dependencies]` (its sole plain-`[dependencies]` edge is the RED-leaf `ade_core_interop`). It MUST NOT be promoted onto a hashed/persisted/consensus path. (2) Affect authoritative outputs or introduce nondeterminism. (3) Construct semantic types bypassing the canonical decoders. (4) Compare an Ade-internal-state fingerprint to a Haskell-serialized-state hash (DC-COMPAT-01; enforced for `consensus::genesis_pinning`). (5) Commit secret key material in any corpus/fixture dir. (6) Overstate semantic truth. |
| **Inbound deps** | `ade_core` (dev), `ade_ledger` (dev), `ade_network` (dev), `ade_runtime` (dev), `ade_node` (dev), `ade_core_interop` (plain — the RED-leaf interop/test driver). |
| **Outbound deps** | `ade_core`, `ade_ledger`, `ade_plutus`, `ade_crypto`, `ade_codec`, `ade_types`, `ade_runtime` (it drives the real BLUE/RED authorities — incl. `ade_runtime::{bootstrap, chaindb}` for the `genesis_pinning` warm-start), `cardano-crypto` (`["vrf-draft03", "kes-sum", "dsign"]`), `blake2`, `ed25519-dalek`, `serde`, `serde_json`, `toml`, `flate2`, `tar`. |
| **Entry points** | `ade_testkit::validity::{corpus::ConwayValidityCorpus, replay::*, adversarial::Mutation}`, `ade_testkit::tx_validity::{replay_tx_validity, extract::*, adversarial::*, valid_synthetic::*}`, `ade_testkit::consensus::{corpus::*, stream_replay::*}`, `ade_testkit::governance::*`, `ade_testkit::producer::{reference_vectors::*, cross_impl_adapter::*}`, `ade_testkit::harness::{oracle_manifest::*, regression_corpus::*, snapshot_loader::*, shelley_loader::*, genesis_loader::*, transcript::*, adapters::*}`, `ade_testkit::mempool::ingress_replay::*`. |
| **Key modules** | `consensus/` (`corpus.rs`, `stream_replay.rs`, `genesis_pinning.rs` *(`#[cfg(test)]`)*, `ledger_view_stub.rs`), `governance/`, `harness/` (`adapters/`, `oracle_manifest.rs`, `regression_corpus.rs`, `provenance.rs`, `*_diff.rs`, `snapshot_loader.rs`, `shelley_loader.rs`, `genesis_loader.rs`, `transcript.rs`, `address_extractor.rs`, `era_mapping.rs`), `mempool/ingress_replay.rs`, `producer/`, `tx_validity/`, `validity/`. |

> **GREEN classification (load-bearing):** `ade_testkit` is GREEN test/corpus infrastructure, **never promotable onto a production runtime path**. Its `consensus::genesis_pinning` sub-module is the `#[cfg(test)]` genesis-consistency pinning harness that drives the REAL `ade_runtime::bootstrap::bootstrap_initial_state` warm-start and pins Ade's recovered eta0 / ASC / stake / VRF-keyhash / `praos_vrf_input` against the genesis-derived reference.

---

## RED Modules — Imperative Shell

> I/O, network, storage, clocks, retries. May depend on BLUE/GREEN. Must not modify core state directly or construct semantic types unsafely.

### `ade_runtime`

| Attribute | Value |
|-----------|-------|
| **Purpose** | The imperative shell — moves bytes, owns sockets/files/clocks/keys, drives tokio tasks. Hosts: producer-mode key custody + shell composition; the N2N network drivers; the node orchestrator runners; the receive/rollback/admission shells (incl. the `admission::{dial_for_admission, run_admission_wire_pump}` reused by the live `--mode node` feed); the seed-import/WAL/consensus-inputs importers; ChainDB + recovery shells; the N-Y bootstrap/forward-sync surfaces; the N-Z `mithril_bootstrap` composition; the clock seam; the N-F-A seed-epoch consensus-input recovered-state shell; the durable-chain serve projection (`network::served_chain_projection`, bounded per-request at `MAX_SERVE_RANGE_BLOCKS = 256`); the N-AE.F `pump_block` hash-exact already-have idempotency gate; the N-AI `admission::wire_pump` `AdmissionPeerEvent::RollBackward` rollback-signal preservation; the N-AC RED producer `kes_sign_header_advancing` evolve-then-sign method; the N-AK `recovered_anchor::load_recovered_anchor_point` load + the `bootstrap`/`chaindb`/`seed_epoch_lineage` recovered-anchor-point store surface. **PHASE4-N-AM (NEW — RED keep-alive client):** `admission::wire_pump::run_admission_wire_pump` (the SOLE per-peer pump, CN-PUMP-01) now runs the N2N keep-alive CLIENT (mini-protocol 8): a `tokio::select!` cadence arm (`tokio::time::interval(KEEP_ALIVE_CADENCE)`, `KEEP_ALIVE_CADENCE = Duration::from_secs(20)`, missed-tick `Delay`, fires after one full cadence not at startup) sends a `KeepAliveMessage::KeepAlive(cookie)` (Initiator) on a ~20s cadence during inbound quiescence via the EXISTING outbound `OutboundFrame` path, advancing the REUSED BLUE `ade_network::keep_alive` state machine (`ClientIdle → ServerHasAgency{cookie}`); a `handle_keep_alive` path validates the echoed cookie on the inbound `DeliverPeerFrame{KeepAlive}` (`cookie' == cookie`, `ServerHasAgency{cookie} + Server → ClientIdle`). WIRE-ONLY — it produces NO `AdmissionPeerEvent`, NO canonical input, NO WAL entry; the cookie is a monotonic `u16` counter (no rand/wall-clock); CLIENT only (no responder); a grammar violation (cookie mismatch, unsolicited ClientIdle response, undecodable payload, agency violation) fails closed via the NEW closed `AdmissionWirePumpError::KeepAlive(KeepAliveError)` variant (drop the peer). **PHASE4-N-AN (CHANGED — bootstrap reorder + eta0 into materialize):** `bootstrap.rs` was REORDERED so the A3b sidecar restore (`restore_seed_epoch_consensus_inputs`) runs BEFORE the WarmStart `materialize_rolled_back_state` call, and now passes the recovered eta0 INTO that materialize call (the recovered eta0 overlays the WarmStart replay chain_dep — fixing warm-start replay from a non-bare store, the SAME root cause as the live rollback-follow); the existing post-materialize eta0 overlay (the SOLE `ci_check_warmstart_eta0_overlay.sh` site, T-REC-04) is idempotent after it. No new type, no new I/O surface. |
| **Creates (RED-only)** | `KesSecret`, `VrfSigningKey`, `ColdSigningKey` custody wrappers; `KeyLoadError`; `MuxTransportHandle` consumers; `OutboundCommand` (closed); `PerPeerOutbound`; `DispatchError` (closed 4-variant); `ServerPeerStates`; `ServedBlockEvidence`; `ServedChainHandle`; `GenesisAnchor`; `ProducerShell` (+ `ShellInitError`); `AdmissionPeerEvent` (closed — incl. the N-AI `RollBackward {peer, point, tip}` variant). **PHASE4-N-AM:** the closed `AdmissionWirePumpError` gains the `KeepAlive(KeepAliveError)` variant (a RED pump error wrapping the BLUE `KeepAliveError`, NOT canonical-counted); the `KEEP_ALIVE_CADENCE` const (RED timing, not a type). Clock: `SystemClock`, `DeterministicClock`, closed `SlotAlignmentError`. consensus_inputs: `LiveConsensusInputsCanonical`, closed `ProtocolParamsParseError`, `ForgeCurrentPParamsError`. N-Y/N-Z/N-F-A: `forward_sync::pump::*`, `mithril_import::*`, `recovery::restart::*`, `mithril_bootstrap::*`, `genesis_bootstrap::*`. `bootstrap::{SeedEpochConsensusSource (closed), BootstrapState, BootstrapInputs (incl. the N-AK `recovered_anchor: Option<ChainTip>`), BootstrapError (incl. the 3 N-AK `RecoveredAnchorPoint*` variants)}`. The serve plumbing types `ChainDbServedSource<'a>`, the closed `ServedChainSource<'a> { Snapshot \| DurableChainDb }`, the `CappedSlotRange` value type, the closed `ServeRangeOutcome` + `MAX_SERVE_RANGE_BLOCKS = 256` (all RED serve plumbing). The N-AK store plumbing `SnapshotStore::{put,get}_recovered_anchor_point` (methods) + the `in_memory` `recovered_anchor_points` map + the `persistent` redb table `recovered_anchor_point_by_anchor_fp`. No new `WalEntry` variant, no new `NodeBlockSource` variant in this span. |
| **Interprets** | Canonicalizes peer bytes/files for the BLUE core: the cold/VRF/KES loaders; `genesis_parser::parse_shelley_genesis`; `opcert_envelope::parse_opcert_envelope`; `seed_import`; `admission::{dial_for_admission, run_admission_wire_pump}` (the N2N dial + chain-sync/block-fetch pump producing `AdmissionPeerEvent`, incl. the preserved `RollBackward` point). `admission::wire_pump` strips the peer's BlockFetch payload tag-24 envelope via `ade_codec::unwrap_tag24` before the BLUE decode (CN-WIRE-12) AND (N-AM) consumes inbound `DeliverPeerFrame{KeepAlive}` frames through the BLUE `keep_alive_transition` to validate the echoed cookie. `bootstrap::restore_seed_epoch_consensus_inputs` reads the anchor-fp-keyed sidecar (the v2 record incl. `epoch_nonce`) and verifies it fail-closed; `recovered_anchor::load_recovered_anchor_point` reads the anchor-fp-keyed recovered-anchor-point record (N-AK). |
| **MUST NOT** | (1) Modify BLUE state directly or construct semantic types from raw bytes. (2) Bypass canonical validation. (3) `producer::signing` (DC-CRYPTO-03/05) — RED-confined custody. (3b) `producer::producer_shell::kes_sign_header_advancing` (DC-CRYPTO-10) — evolve the KES key to the requested period BEFORE signing, fail-closed; signing stays RED (`ci_check_kes_evolution_before_sign.sh`). (4)–(7) `producer::keys` / `coordinator` / `outbound_command` (CN-OUTBOUND-RELAY-01) / per-peer outbound map fences. (7b) `network::serve_dispatch` (DC-NODE-07). (7c) `network::n2n_listener` magic-aware serve table (DC-NODE-07). (7d) `network::served_chain_projection::ChainDbServedSource` (DC-NODE-13 / DC-NODE-14 / DC-SERVEMEM-01) — READ-ONLY, bounded per-request via `range_bytes_capped` / `last_block_bytes`, FindIntersect-only forge-parent `intersect` clause serves NO bytes (`ci_check_served_chain_projection.sh` + `ci_check_serve_range_bounded.sh` + `ci_check_recovered_anchor_intersectable.sh`). **(8) `admission::wire_pump` keep-alive client (DC-PUMP-03, PHASE4-N-AM):** the keep-alive client MUST REUSE the BLUE `ade_network::keep_alive` + `::codec::keep_alive` grammar (no redefined cookie/agency rules); it MUST use a cadence STRICTLY under the peer keep-alive timeout (`KEEP_ALIVE_CADENCE = 20s` ≪ ~97s); it MUST respect agency (no new `MsgKeepAlive` while one is in flight — `ServerHasAgency`); it MUST NOT block / starve / reorder the chain-sync or block-fetch flow; it MUST NOT dispatch a keep-alive frame as a chain-sync/block-fetch event (closed match over `AcceptedMiniProtocol`); it MUST NOT emit ANY `AdmissionPeerEvent` (Block / TipUpdate / RollBackward / Disconnected) — keep-alive is transport liveness only, the `DC-PUMP-01` emit-set stays UNWIDENED; it MUST fail closed on a grammar violation (`AdmissionWirePumpError::KeepAlive` — drop the peer, never silently swallow); the cookie MUST be a monotonic `u16` counter (no rand / wall-clock); it MUST be a CLIENT only (no keep-alive SERVER/responder — that is a CE-AM-LIVE-gated follow-on). It MUST NOT broaden `CN-PUMP-01` / `DC-PUMP-01` / `DC-PUMP-02` (cross-refs, preserved) and MUST NOT flip `CN-CONS-03` (`ci_check_keep_alive_wire_only.sh`). **(9) `forward_sync::pump` (DC-SYNC-01 / DC-NODE-16):** refuse to advance the tip before `StoreBlockBytes` + `AppendWal`; the already-have skip is HASH-keyed before the BLUE reducer (`ci_check_receive_idempotency.sh`). (10)–(13) `mithril_import` / `genesis_bootstrap` / `recovery::restart` / `mithril_bootstrap` fences. **(14) `bootstrap::restore_seed_epoch_consensus_inputs` (DC-CINPUT-01; T-REC-04 / DC-CINPUT-03; reorder T-REC-06, N-AN):** fail closed on a missing sidecar / hash mismatch / non-canonical decode / binding mismatch; the recovered eta0 MUST be read from the v2 sidecar; **(N-AN) the restore MUST run BEFORE the WarmStart `materialize_rolled_back_state` call so the recovered eta0 can be passed into materialize (overlaying the WarmStart replay chain_dep) — the reorder changes ORDER only, not outcome (the restore is independent of the materialize result), and the post-materialize overlay (the SOLE `ci_check_warmstart_eta0_overlay.sh` site) stays the explicit eta0 authority and is idempotent after the materialize overlay** (`ci_check_warmstart_eta0_overlay.sh`, `ci_check_rollback_materialize_eta0.sh`). (15)–(16) `seed_consensus_provenance` / `chaindb` seed-sidecar surface (CN-CINPUT-02). (16b) `chaindb` recovered-anchor-point surface (DC-NODE-31, N-AK) — DISJOINT anchor-fp-keyed namespace, canonical-encoded bytes, idempotent same-bytes put / `InvalidOperation` on conflict, additive redb table (no `SCHEMA_VERSION` bump). (16c) `recovered_anchor::load_recovered_anchor_point` (DC-NODE-31, N-AK) — SOLE codec, binding check, fail-closed on missing/decode/binding-mismatch, never a silent Origin fallback, kept OUT of `bootstrap.rs` (CN-NODE-01). (16d) `bootstrap::resolve_live_follow_start` (DC-NODE-31, N-AK) — pure module-private resolver, servable `ChainDb::tip()` wins → non-Origin recovered anchor → None; never synthesize a servable block. (17)–(22) `producer::keys` / `admission::{dial,pump}` / feed tag-24 unwrap (CN-WIRE-12) / `clock::checked_millis_to_slot` / `consensus_inputs` / `admission::wire_pump` rollback signal preservation (DC-PUMP-01) fences. |
| **Inbound deps** | `ade_node`, `ade_core_interop`, `ade_testkit` (dev/integration). |
| **Outbound deps** | `ade_types`, `ade_core` (incl. `consensus::praos_state::{Nonce, PraosChainDepState::overlay_recovered_eta0}` transitively via `ade_ledger` on the N-AN materialize path), `ade_crypto`, `ade_codec` (incl. `unwrap_tag24`), `ade_ledger` (incl. `rollback::materialize::materialize_rolled_back_state` with the N-AN `recovered_eta0` param; `recovered_anchor_point::*`, N-AK), `ade_network` (incl. `keep_alive::keep_alive_transition` + `codec::keep_alive::*` — the N-AM keep-alive client), `redb`, `serde`, `serde_json`, `bech32`, `base58`, `cardano-crypto` (`["vrf-draft03", "dsign"]`), `ed25519-dalek`, `tokio` (dev-dep: `tokio` `["test-util"]` — N-AM virtual-time cadence test; `tempfile`, `ade_testkit`). |
| **Entry points** | `ade_runtime::producer::{producer_shell::* (incl. `kes_sign_header_advancing`), keys::*, coordinator::*, served_chain_handle::*, genesis_parser::*, opcert_envelope::*, self_accepted_handoff::*}`, `ade_runtime::admission::{dial_for_admission, run_admission_wire_pump (incl. the N-AM keep-alive client + `KEEP_ALIVE_CADENCE`), AdmissionPeerEvent, AdmissionWirePumpError}`, `ade_runtime::network::{n2n_listener::*, n2n_dialer::*, mux_pump::*, n2n_server::*, outbound_command::*, serve_dispatch::{ServedChainSource, *}, served_chain_projection::{ChainDbServedSource, ServeRangeOutcome, MAX_SERVE_RANGE_BLOCKS}}`, `ade_runtime::orchestrator::*`, `ade_runtime::bootstrap::{bootstrap_initial_state, BootstrapInputs, BootstrapState, SeedEpochConsensusSource, BootstrapError}` (N-AK: `recovered_anchor` + the 3 `RecoveredAnchorPoint*` error variants; N-AN: the restore-before-materialize reorder + eta0 into materialize; `resolve_live_follow_start` module-private), `ade_runtime::recovered_anchor::load_recovered_anchor_point` (N-AK), `ade_runtime::seed_consensus_merge::*`, `ade_runtime::consensus_inputs::*`, `ade_runtime::{forward_sync, mithril_import, genesis_bootstrap, mithril_bootstrap, recovery::restart, chaindb, clock, seed_import, wal, bootstrap_anchor, seed_epoch_lineage}::*` (incl. the N-AA bounded serve primitives + the N-AK `chaindb::SnapshotStore::{put,get}_recovered_anchor_point`). |
| **Key modules** | `producer/` (incl. `producer_shell.rs`), `network/` (incl. `served_chain_projection.rs`, `serve_dispatch.rs`, `n2n_listener.rs`), `orchestrator/`, `admission/` (incl. **`wire_pump.rs`** — the feed tag-24 unwrap + the N-AI `AdmissionPeerEvent::RollBackward` + **PHASE4-N-AM: the keep-alive CLIENT (`tokio::select!` cadence + `handle_keep_alive` cookie validation + `AdmissionWirePumpError::KeepAlive`)**; `dial_for_admission` + `run_admission_wire_pump`), `receive/`, `rollback/`, `seed_import/`, `consensus_inputs/`, `wal/`, `chaindb/` (redb schema v3; the bounded serve primitives; the N-AK `put/get_recovered_anchor_point` surface), `forward_sync/` (incl. `pump.rs` — the DC-NODE-16 idempotency gate; **`reducer.rs`** — the GREEN `ForwardSyncState.{recovered_anchor (N-AK), recovered_eta0 (N-AN)}` fields), `mithril_import/`, `seed_consensus_merge.rs`, `seed_consensus_provenance.rs`, `seed_epoch_lineage.rs` (N-AK anchor-point write), `mithril_bootstrap.rs`, `genesis_bootstrap.rs`, `recovery/`, **`bootstrap.rs`** (GREEN composition + RED A3b restore; N-AK `resolve_live_follow_start` + `recovered_anchor`; **PHASE4-N-AN: the restore-before-materialize reorder + eta0 passed into the WarmStart materialize**), `recovered_anchor.rs` (N-AK), `bootstrap_anchor.rs`, `clock.rs`. |

> **RED sub-entries carried** (unchanged in shape across the N-AM/N-AN span): `ade_runtime::network::serve_dispatch` *(RED — N-F-G-H; the single shared `dispatch_server_frame_event_to_outbound`)* and `ade_runtime::network::served_chain_projection` *(RED — N-U S3 / DC-NODE-13; the read-only durable-ChainDb serve projection bounded by N-AA `range_bytes_capped` / `MAX_SERVE_RANGE_BLOCKS = 256` + the N-AE FindIntersect-only forge-parent `intersect` clause)*. The N-AK recovered-anchor store surface (`chaindb` + `recovered_anchor`) is not a serve-path change; the N-AM keep-alive client + the N-AN eta0 reorder are not serve-path changes.

---

### `ade_node`

| Attribute | Value |
|-----------|-------|
| **Purpose** | The node binary + library entry. Owns argv parsing, the node lifecycle, and the mode drivers dispatched by `main()`: `--mode wire_only` (default), `--mode admission`, `--mode key_gen_kes`, `--mode produce`, and (N-F-C) `--mode node` (`node_lifecycle`). `node_lifecycle` is the single `PHASE4-N-F-C-LIFECYCLE-OWNER`. Carries the full N-F-C…N-AL `--mode node` live surface (bootstrap/recovery, forge-intent flip, real forge constants + fail-closed boundaries, the self-accept→serve handoff, the live WirePump feed, the bounded-memory + bounded-serve caps, the rehearsal harness, the serve-to-peer sibling, the genesis-successor cold-start forge, the forged-block durable admit via `pump_block`, the forge-on-followed-tip gate, the receive idempotency, the local-durable single-producer forge base + warm-start re-entry, the live fork-choice rollback-follow wiring (single-producer + participant), the N-AJ convergence-evidence emission, and the N-AK/N-AL recovered-anchor live-follow-start + rollback-no-op). **PHASE4-N-AN (CHANGED — eta0 threading on the live rollback-follow):** `node_lifecycle::apply_chain_event` (the live rollback-follow apply path the CE-AI-6 reorg hit) now threads the recovered seed-epoch eta0 (`state.seed_epoch_consensus_inputs`'s `epoch_nonce`, carried via `ForwardSyncState.recovered_eta0`, set on the recover arm) into the BLUE `materialize_rolled_back_state` call — so a live rollback materializes the replay chain_dep against the SAME nonce live admit used (T-REC-06). RED loop wiring of a BLUE-authoritative input; the overlay + the VRF verification are the BLUE materialize's job. NO new type, NO new CLI flag. **PHASE4-N-AM is NOT in `ade_node`** — its keep-alive client is in `ade_runtime::admission::wire_pump` (reused by `--mode node` via the existing pump). |
| **Creates (RED-only)** | `Cli` (incl. `peer_addrs`, `network_magic`), `CliError`, `ProduceCli`, `KeyGenKesCli`, `Mode` (closed: `WireOnly`/`Admission`/`KeyGenKes`/`Produce`/`Node`), `NodeStartupInputs`, `NodeShutdownEvidence`, `NodeRunError`, exit-code constants. `node_lifecycle` (RED): `NodeStart` (closed), `NodeLifecycleError` (closed), `ForgeActivation<'a>` (incl. `last_forge_refused` + `pending_reselection`), `run_relay_loop` / `run_relay_loop_with_sched`, `spawn_live_wire_pump_source`, `wire_pump_start_point` (N-AK), `run_node_serve_task`, `bind_serve_listener`, closed `ServeStartError`, the GREEN-by-function `may_cold_start_forge`, the apply-driver `apply_chain_event` / `run_participant_sync` / `declare_participant_venue` + `AppliedTip` / `ApplyError` (N-AI; N-AL participant RollBack handler; **N-AN: `apply_chain_event` threads `recovered_eta0` into the materialize call**). `node_sync` (RED): closed 2-variant `NodeBlockSource { WirePump, InMemory }`, the closed `NodeSyncItem { Block \| RollBack(Point) }` (N-AI), `NodeSyncError { Pump \| Capture \| UnexpectedRollback \| RollbackPointSlotMismatch }` (N-AI), `NodeForgeError`, the GREEN-by-function classifiers, the closed `ForgeMode` (3-state) / `VenueRole` / `ForgeFollowedTipAdmission` / `NotCaughtUpReason` / `ReceiveClass` / `ReceiveDisposition` / `CandidateSummary` / `ForgeRefused` / `NodeForgeOutcome`, `FollowedPeerTipSignal`, the `MAX_WIRE_PUMP_LOOKAHEAD = 256` cap. GREEN sub-trees `forge_intent` / `run_loop_planner` / `live_log::{sched_event,sched_writer}` / `ba02_evidence` / `convergence_evidence` / `rehearsal_evidence`. RED `operator_forge` / `ba02_pass` / `rehearsal_pass`. (N-AN adds no new type — `apply_chain_event`'s eta0 thread reuses the existing `ForwardSyncState.recovered_eta0` field + the existing `materialize_rolled_back_state`.) |
| **Interprets** | argv (closed mode set); operator key/genesis/opcert file paths; evidence-manifest TOML schema; the admission runner interprets a peer's tag-24-wrapped BlockFetch `MsgBlock` via `ade_codec::unwrap_tag24`; the feed's header-validation interprets the recovered consensus surface's leader-threshold view; the successor forge interprets the evolved admitted spine state; `warm_start_recovery` interprets the recovered seed-epoch sidecar (N-AK: + the persisted recovered anchor-point record) to reconstruct the recovery `era_schedule` + `ledger_view`; the `--mode node` served view reads the durable ChainDb; the live receive path interprets each peer item as a `NodeSyncItem` and routes a Participant `NeedsForkChoice` into the existing `chain_selector`. **PHASE4-N-AN:** `apply_chain_event` interprets the recovered seed-epoch `epoch_nonce` as the eta0 to thread into the rollback materialize (so the rolled-back replay validates each block's VRF against eta0). |
| **MUST NOT** | All carry-forward `--mode node` prohibitions (closed mode set, single lifecycle owner via `bootstrap_initial_state`, durable-tip ONLY via `run_node_sync → pump_block`, `run_relay_loop` containment byte-unchanged, content-blind planner, the self-accept→serve handoff fence, the live-feed reuse + lookahead bound, the path-fidelity + rehearsal-evidence fences, the serve-to-peer DC-NODE-07 fence, the genesis-successor cold-start + PrevHash position fence, the serve-as-projection DC-NODE-13 fence, the forge-on-followed-tip DC-NODE-15 / DC-CONS-24 fence, the receive-idempotency DC-NODE-16 fence, the local-durable forge-base DC-NODE-20 / cert-evidence-only DC-NODE-21 / warm-start-re-entry DC-NODE-22 fences, the live fork-choice DC-NODE-23…29 fences, the recovered-anchor DC-NODE-31/32/33 fences). **`run_node_serve_task` lifetime (DC-NODE-09 — PRESERVED):** the serve sibling is spawned OUTSIDE `run_relay_loop` and stays up past feed-end (`ci_check_node_serve_lifetime.sh`). **`bootstrap_initial_state` is the single lifecycle owner (CN-NODE-01 — PRESERVED + N-AN gate refinement):** `node_lifecycle` MUST construct initial state ONLY via `bootstrap_initial_state` and MUST NOT call `materialize_rolled_back_state` for INITIAL-STATE construction — the ONLY permitted `materialize_rolled_back_state` call in `node_lifecycle` is the N-AI/N-AN live rollback-follow inside `apply_chain_event` (a parallel/cold initial-state materialize still trips the gate; the N-AN triage scoped `ci_check_lifecycle_owner_uses_bootstrap_initial_state.sh` to exclude the `apply_chain_event` region) (`ci_check_lifecycle_owner_uses_bootstrap_initial_state.sh`). **(carried) recovered-anchor live-follow start (DC-NODE-31) / single-producer rollback boundary (DC-NODE-32) / participant rollback boundary (DC-NODE-33):** the rollback no-op binds on slot AND hash to the persisted anchor (`ForwardSyncState.recovered_anchor`, never peer-supplied alone, never re-read from the store inside the loop); the participant branch is evaluated BEFORE the `DC-NODE-29` stored-block resolution; every non-anchor / non-Origin / slot-only / hash-only rollback still fails closed (single-producer) or routes through the unchanged `DC-NODE-29` authority (participant) — a real stored-block rollback still applies; `DC-NODE-32` stays scoped to `run_node_sync` (NOT broadened to the participant path). Test-enforced (`ci_script = ""`). **(N-AN) `apply_chain_event` eta0 threading (T-REC-06):** the live rollback-follow MUST pass the recovered seed-epoch eta0 (via `ForwardSyncState.recovered_eta0`) into `materialize_rolled_back_state` so the replay validates each block's header VRF against eta0, NOT the snapshot placeholder; it MUST NOT weaken / skip the VRF verification (the overlay is the BLUE materialize's job — a WRONG eta0 still fails closed) (`ci_check_rollback_materialize_eta0.sh`). |
| **Inbound deps** | None (binary + integration tests). |
| **Outbound deps** | `ade_types`, `ade_core`, `ade_crypto`, `ade_ledger` (incl. `rollback::materialize::materialize_rolled_back_state` via `apply_chain_event`, N-AN; `recovered_anchor_point::*` in tests, N-AK), `ade_runtime` (incl. `recovered_anchor::load_recovered_anchor_point` + `bootstrap::BootstrapInputs.recovered_anchor`, N-AK; `admission::{dial_for_admission, run_admission_wire_pump, AdmissionPeerEvent}`), `ade_network` (incl. `codec::chain_sync::Point`), `ade_codec`, `serde`, `serde_json`, `tokio`. Dev-deps: `ade_testkit`, `tempfile`. |
| **Entry points** | `main()`; `ade_node::run_node_lifecycle` (`--mode node`); `ade_node::node_lifecycle::{run_relay_loop, run_relay_loop_with_sched, ForgeActivation, spawn_live_wire_pump_source, run_node_serve_task, bind_serve_listener, ServeStartError, may_cold_start_forge, warm_start_recovery, apply_chain_event (N-AN: threads `recovered_eta0` into the materialize), run_participant_sync, declare_participant_venue, AppliedTip, ApplyError, wire_pump_start_point}`; `ade_node::node_sync::{NodeBlockSource, run_node_sync, forge_one_from_recovered, admit_forged_block_durably, forge_epoch_admission, forge_header_position, forge_followed_tip_admission, NodeSyncItem, classify_receive, resolve_disposition, ReceiveClass, ReceiveDisposition, NodeSyncError, ForgeRefused, NodeForgeOutcome, FollowedPeerTipSignal, NodeForgeError}`; `ade_node::run_loop_planner::*` (GREEN); `ade_node::live_log::{NodeSchedEvent, NodeSchedLogWriter, NodeSchedSink}` (GREEN); `ade_node::convergence_evidence::{ConvergenceEvidenceSink, EvidenceEmitResult}` (GREEN, N-AJ); `ade_node::forge_intent::*` (GREEN); `ade_node::operator_forge::*` (RED); `ade_node::ba02_pass::*` + `ade_node::ba02_evidence::*`; `ade_node::rehearsal_pass::*` + `ade_node::rehearsal_evidence::*`; `ade_node::produce_mode::*`; `ade_node::cli::{Cli, ProduceCli, Mode}`; `ade_node::{admission, wire_only, key_gen}::*`. |
| **Key modules** | `lib.rs`, `cli.rs`, `node.rs`, `main.rs`, **`node_lifecycle.rs`** (the `--mode node` owner + `run_relay_loop` + the carried N-F-C…N-AL wiring incl. the N-AK `warm_start_recovery` anchor-point load + `wire_pump_start_point` + the N-AL `run_participant_sync` RollBack handler + **PHASE4-N-AN: `apply_chain_event` threads `recovered_eta0` into the rollback `materialize_rolled_back_state` call**), `node_sync.rs` (the sync driver + the N-AI detector/resolver/apply plumbing + the N-AK `run_node_sync` recovered-anchor RollBack handler), `forge_intent.rs` (GREEN), `operator_forge.rs` (RED), `run_loop_planner.rs` (GREEN), `ba02_evidence.rs` (GREEN), `convergence_evidence.rs` (GREEN, N-AJ), `live_log/` (GREEN sched vocabulary), `ba02_pass.rs` (RED), `rehearsal_evidence.rs` (GREEN), `rehearsal_pass.rs` (RED), `produce_mode.rs`, `wire_only.rs`, `key_gen.rs`, `admission/`, `admission_log/`. |

> **GREEN sub-classification (carried):** `forge_intent`, `run_loop_planner`, `ba02_evidence`, `live_log::{sched_event,sched_writer}`, `convergence_evidence`, `rehearsal_evidence` are GREEN-by-content; `node_sync::{forge_epoch_admission, forge_header_position, forge_followed_tip_admission, single_producer_forge_decision, venue_policy, warm_start_forge_mode, forge_mode_after_admit, classify_receive, resolve_disposition, pending_reselection_forge_refusal, durable_tip_matches}` + `node_lifecycle::may_cold_start_forge` are GREEN-by-function. `node_lifecycle` + `node_sync` + `operator_forge` + `ba02_pass` + `rehearsal_pass` are RED. **PHASE4-N-AN:** `apply_chain_event`'s eta0-thread into the rollback materialize is RED `node_lifecycle` loop wiring of a BLUE-authoritative input (the recovered eta0) — it reuses the existing GREEN `ForwardSyncState.recovered_eta0` field + the BLUE `materialize_rolled_back_state`; it adds no new type and overlays/verifies nothing itself.

---

### `ade_core_interop`

| Attribute | Value |
|-----------|-------|
| **Purpose** | Live cardano-node interop driver. Hosts the `live_*_session` RED binaries plus the N-E S4/S5 GREEN tx-submission bridges and the chain-follow driver (`follow`). `follow` strips the peer's ChainSync `RollForward` `[era_tag, tag24(header_cbor)]` envelope via `ade_codec::unwrap_tag24`. (Unchanged across the N-AM/N-AN span.) |
| **Creates (RED-only)** | Live-session drivers and transcript types; never semantic types. |
| **MUST NOT** | (1) Construct semantic types from raw bytes. (2) Be depended on by any BLUE/GREEN crate (RED leaf). (3) Overstate semantic truth in evidence. (4) `follow` (CN-WIRE-08) — strip via `ade_codec::unwrap_tag24`. |
| **Inbound deps** | None (RED leaf — binaries). |
| **Outbound deps** | `ade_core`, `ade_codec`, `ade_crypto`, `ade_ledger`, `ade_runtime`, `ade_network`, `ade_testkit`, `ade_types`, `tokio`. |
| **Entry points** | `ade_core_interop::bin::{live_tx_submission_session, live_block_production_session, live_block_fetch_session, live_block_follow_session}`; `ade_core_interop::follow::*`; `ade_core_interop::{tx_submission, local_tx_submission}`. |
| **Key modules** | `bin/`, `follow.rs`, `tx_submission.rs`, `local_tx_submission.rs`. |

---

### `ade_network::mux::transport` *(RED)*

| Attribute | Value |
|-----------|-------|
| **Purpose** | The only place inside `ade_network` where socket I/O happens. RED tokio shell over the BLUE mux frame primitive and the GREEN session reducer. Provides `MuxTransportHandle` + closed `TransportError` + `DuplexCapacity::DEFAULT` + `spawn_duplex`. (Unchanged across the span.) |
| **Creates (RED-only)** | `MuxTransportHandle`, `TransportError`, `DuplexCapacity`, `MuxTransport`. |
| **MUST NOT** | (1) Construct semantic types. (2) Bypass `mux::frame` for framing. (3) Live in BLUE scope. |
| **Inbound deps** | `ade_runtime::network::{mux_pump, n2n_dialer}`. |
| **Outbound deps** | `ade_network::mux::frame` (BLUE), `ade_network::session` (GREEN), `tokio`. |
| **Entry points** | `ade_network::mux::transport::{MuxTransportHandle, spawn_duplex, open_tcp}`. |
| **Key modules** | `mux/transport.rs`. |

---

### `ade_network` *(RED capture binaries — non-session)*

| Attribute | Value |
|-----------|-------|
| **Purpose** | Operator-action capture binaries (live evidence harness) inside `ade_network`. The `ade_chain_sync_capture` binary supports `--intersect-slot` / `--intersect-hash` flags to capture a real Conway `RollForward` golden fixture; the `ade_keep_alive_capture` binary captures real keep-alive traffic (the byte source for the N-AM client's fixtures). |
| **MUST NOT** | (1) Construct semantic types from raw bytes. (2) Overstate semantic truth in captured evidence. |
| **Outbound deps** | `ade_network` BLUE/GREEN submodules, `ade_codec`, `ade_types`, `tokio`. |

---
## Cross-Module Rules (project-wide)

### Dependency direction (RED → GREEN → BLUE; never outward)

```
ade_types     → {}
ade_codec     → {ade_types}
ade_crypto    → {ade_types, blake2, ed25519-dalek, cardano-crypto[vrf-draft03,kes-sum(test-only),dsign]}
ade_core      → {ade_types, ade_crypto, minicbor}   [BLUE; production select_best_chain byte-unchanged; PHASE4-N-AN adds the NEW method PraosChainDepState::overlay_recovered_eta0 — the SINGLE eta0-overlay authority (sets epoch_nonce = evolving_nonce = recovered eta0), shared by WarmStart bootstrap + rollback materialize; a method, not a type — 49→49]
ade_ledger    → {ade_core, ade_plutus, ade_crypto, ade_codec, ade_types, minicbor, num-*}   [BLUE; seed_consensus_inputs carries epoch_nonce: Nonce + SEED_CINPUT_SCHEMA_VERSION 2 (N-F-G-N); wal::event carries WalEntry::RollBack at tag 1 + RollbackPoint/RollbackReason (N-AI); recovered_anchor_point owns the closed RecoveredAnchorPoint + sole codec (N-AK, +2 BLUE types, 179→181); PHASE4-N-AN: rollback::materialize::materialize_rolled_back_state gains a recovered_eta0: Option<&Nonce> param and overlays it (via ade_core::overlay_recovered_eta0) onto the nearest_le snapshot chain_dep before the replay fold — VRF strength unchanged, T-REC-06; a fn-param change, not a type — 181→181; receive::reducer adds RollbackContext.recovered_eta0 on the #[cfg(test)] path]
ade_plutus    → {ade_crypto, ade_codec, ade_types, aiken_uplc, pallas-primitives(internal)}
ade_network   → BLUE submodules → {ade_codec, ade_types}; session(GREEN) → +BLUE submodules; transport(RED) → +tokio   [no ade_network change in the N-AM/N-AN span — the N-AM keep-alive client REUSES the BLUE keep_alive grammar from ade_runtime; the grammar is byte-untouched]
ade_testkit   → {ade_core, ade_ledger, ade_plutus, ade_runtime, ade_crypto, ade_codec, ade_types, cardano-crypto}   [GREEN]
ade_runtime   → {ade_core, ade_crypto, ade_codec, ade_types, ade_ledger, ade_network, redb, serde*, bech32, base58, cardano-crypto[vrf-draft03,dsign], ed25519-dalek, tokio}   [RED; admission::wire_pump calls ade_codec::unwrap_tag24 on the feed side (N-F-G-O) + preserves the RollBackward point (N-AI) + PHASE4-N-AM runs the N2N keep-alive CLIENT (a tokio::select! ~20s cadence sends MsgKeepAlive Initiator + handle_keep_alive validates the echoed cookie, REUSING the BLUE ade_network::keep_alive grammar; wire-only — no AdmissionPeerEvent, DC-PUMP-03; new dev-dep tokio[test-util] for the virtual-time cadence test); network::served_chain_projection is the read-only bounded serve projection (DC-SERVEMEM-01/DC-NODE-14); producer::producer_shell::kes_sign_header_advancing (DC-CRYPTO-10); forward_sync::pump::pump_block hash-exact idempotency (DC-NODE-16); recovered_anchor::load_recovered_anchor_point + the disjoint anchor-fp-keyed chaindb surface (N-AK); PHASE4-N-AN: bootstrap.rs reordered the sidecar restore BEFORE the WarmStart materialize_rolled_back_state and passes the recovered eta0 into it (fixes warm-start replay too, T-REC-06); the post-materialize warmstart-eta0 overlay (ci_check_warmstart_eta0_overlay.sh) stays the explicit eta0 authority + is idempotent after. NO new BLUE/external dep]
ade_node      → {ade_types, ade_core, ade_crypto, ade_ledger, ade_runtime, ade_network, ade_codec, serde, serde_json, tokio}   [RED; node_lifecycle carries the N-F-G-K…N-AL live serve/feed/forge/rollback-follow/convergence-evidence/recovered-anchor wiring; PHASE4-N-AN: node_lifecycle::apply_chain_event threads the recovered seed-epoch eta0 (ForwardSyncState.recovered_eta0, set on the recover arm) into the BLUE materialize_rolled_back_state on the live rollback-follow path so a live rollback replays each block's VRF against eta0 (T-REC-06) — RED loop wiring of a BLUE-authoritative input, no new type/flag. PHASE4-N-AM is NOT in ade_node (its keep-alive client is in ade_runtime). NO new BLUE/external dep]
ade_core_interop → {ade_core, ade_codec, ade_crypto, ade_ledger, ade_runtime, ade_network, ade_testkit, ade_types, tokio}   [RED leaf]
```

**Forbidden directions (carried + the N-AM/N-AN span).** Any BLUE crate (or BLUE `ade_network` submodule) depending on `ade_runtime`, `ade_node`, `ade_core_interop`, or the RED half of `ade_network` is a CI failure (`ci_check_dependency_boundary.sh`). Any non-`ade_plutus` crate referring to `pallas_*` is a CI failure. A second block-envelope encoder, a second `leader_vrf_input` authority, a second `wrap_tag24`/`unwrap_tag24` definition or a hand-rolled tag-24 parse in RED, a forward-sync `AdvanceTip` before durability, a Mithril/genesis bootstrap path bypassing the single `bootstrap_initial_state`, an internal-fingerprint-vs-Haskell-hash equality assertion, a registry rule citing a non-existent `code_locus`, a Mithril-bootstrap path sourcing the anchor `seed_point` from the manifest, a `WalEntry::RollBack` that implements a second rollback (N-AI), a second/hand-rolled `RecoveredAnchorPoint` codec or a recovered-anchor-point store surface colliding with the slot-keyed snapshot / seed-epoch sidecar namespaces (N-AK), a recovered-anchor rollback no-op that binds on slot-alone / hash-alone / a peer-supplied point rather than the persisted `BootstrapState.tip` on both slot AND hash (N-AK/N-AL), **and (PHASE4-N-AM) a wire-pump keep-alive client that redefines the BLUE `ade_network::keep_alive` grammar, uses a cadence ≥ the peer keep-alive timeout, sends a new `MsgKeepAlive` while one is in flight, dispatches a keep-alive frame as a chain-sync/block-fetch event, or emits ANY `AdmissionPeerEvent` from the keep-alive path (the `DC-PUMP-01` emit-set MUST stay unwidened), and (PHASE4-N-AN) a SECOND eta0-overlay authority (any nonce-substitution site other than `PraosChainDepState::overlay_recovered_eta0`), a `materialize_rolled_back_state` that lets the snapshot placeholder / genesis `epoch_nonce` reach VRF verification on the rollback-replay path, an eta0 sourced from peer data / wall-clock / CLI re-supply / a re-query rather than the recovered `SeedEpochConsensusInputs.epoch_nonce` sidecar, OR a rollback-replay VRF bypass / skip / loosening (the overlay is not a bypass — a WRONG eta0 still fails closed)** are all CI failures (the carried N-A…N-AL fences + the N-AM/N-AN gates).

**NEW (PHASE4-N-AN — rollback-materialization preserves recovered eta0):**
- **N-AN (T-REC-06):** a block that validates during live admit (against the eta0-overlaid chain_dep, T-REC-04) MUST NOT fail rollback-materialize replay because materialization substituted a different nonce source. `materialize_rolled_back_state` (the SOLE rolled-back-state authority, `CN-STORE-07`) MUST reconstruct the replay chain_dep with the SAME recovered eta0 the live-admit path uses — overlaid via the SINGLE `ade_core::PraosChainDepState::overlay_recovered_eta0` authority onto the `nearest_le` snapshot chain_dep BEFORE both the degenerate-snapshot return AND the replay-forward `block_validity` fold; the persisted snapshot's placeholder / genesis `epoch_nonce` MUST NOT reach VRF verification on the rollback-replay path. Same recovered store + same ordered WAL/feed ⇒ same chain_dep inputs ⇒ same `block_validity` result on the live-admit and rollback paths. The eta0 is the recovered canonical input (the seed-epoch `SeedEpochConsensusInputs.epoch_nonce` sidecar) — NEVER peer data, wall-clock, CLI re-supply, or a re-query. VRF validation strength is UNCHANGED on the rollback path (no bypass / skip / loosening — a WRONG eta0 still fails the header VRF; `None` keeps the snapshot nonce for cold-start). The same overlay authority is applied to the WarmStart bootstrap materialize too (`bootstrap.rs` reorders the sidecar restore ahead of materialize + passes eta0 — fixing warm-start replay from a non-bare store, the SAME root cause). SCOPE: the recovered seed epoch (no epoch-boundary crossing in the follow window — eta0 is the constant epoch nonce); a multi-epoch rollback nonce-evolution is a named out-of-scope follow-on. Surfaced by the CE-AI-6 reorg (the rollback-follow died at `ReplayFailedAt VrfCert`); unblocks CE-AI-6. **`ci_check_rollback_materialize_eta0.sh`** (guards: the single overlay authority exists on `PraosChainDepState`; materialize takes the param AND applies the overlay before the fold; NO VRF bypass). CE-AN-LIVE PASSED (2026-06-11, fresh hermetic 2-pool bridge venue, magic 42): bare-anchor recover @ slot 162 → induced peer reorg → Ade FOLLOWED the `RollBackward` (strict slot REGRESSION admit 371 → 361) → re-converged to `agreement_verdict{agreed}` @ slot 383 (our_hash == peer_hash), 0 `VrfCert`/`ReplayFailedAt` — the eta0 overlay HELD through the live rollback (without AN-S2 Ade died here). Transcript OUTSIDE-REPO (scrubbed note only). `RO-LIVE-01` stays operator-gated.

**NEW (PHASE4-N-AM — keep-alive wire-pump client):**
- **N-AM (DC-PUMP-03):** the admission wire pump (`run_admission_wire_pump` — the SOLE per-peer pump, `CN-PUMP-01`) MUST run the N2N keep-alive CLIENT (mini-protocol 8) by REUSING the BLUE `ade_network::keep_alive` grammar: on a cadence STRICTLY under the peer's keep-alive timeout (`KEEP_ALIVE_CADENCE = 20s` ≪ ~97s) it sends `MsgKeepAlive(cookie)` (Initiator) via the EXISTING outbound `OutboundFrame` path (advancing `keep_alive_transition: ClientIdle → ServerHasAgency{cookie}`), and on the inbound `MsgResponseKeepAlive(cookie')` it advances the SAME state machine validating `cookie' == cookie`. WIRE-ONLY: the keep-alive client produces NO canonical input, NO WAL entry, and NO `AdmissionPeerEvent` (the `DC-PUMP-01` emit-set stays unwidened — it never affects admission, the durable chain, fork-choice, the convergence-evidence vocabulary, replay-equivalence, or any BLUE state). It MUST NOT redefine the BLUE keep-alive grammar; use a cadence ≥ the peer timeout; send a new `MsgKeepAlive` while one is in flight (respect `ServerHasAgency`); block / starve / reorder the chain-sync or block-fetch flow; dispatch a keep-alive frame as a chain-sync/block-fetch event; silently swallow a grammar violation (fail closed via `AdmissionWirePumpError::KeepAlive` — drop the peer); use rand / wall-clock for the cookie (monotonic `u16` counter); implement a keep-alive SERVER/responder (CLIENT only — the responder is a CE-AM-LIVE-gated follow-on). With the client running, a live participant AND single-producer follow sustains past the ~97s keep-alive deadline — the prerequisite that makes the CE-AI-6 induced-reorg convergence capture (the N-AN live evidence) runnable. SCOPE: the keep-alive client ONLY; it MUST NOT add multi-peer ChainSel, MUST NOT flip `CN-CONS-03`, and MUST NOT broaden `CN-PUMP-01` / `DC-PUMP-01` / `DC-PUMP-02` (cross-refs, preserved). **`ci_check_keep_alive_wire_only.sh`** (reuse-not-redefine + wire-only fence). CE-AM-LIVE PASSED (2026-06-11, frozen c2-testnet relay, magic 42): caught up → `agreement_verdict{agreed}` @ slot 241 → IDLE at the frozen tip while the keep-alive client SUSTAINED the link 152s (7 `MsgKeepAlive`, 7 `MsgResponseKeepAlive` cookies VALIDATED, 0 grammar failures, 0 idle-EOF). NOT a CE-AI-6 claim itself.

**NEW (PHASE4-N-AN stale-gate triage — CI-script-only, NO source change, NO rule weakened):**
- **`ci_check_admission_wire_pump_closure.sh`** — the `DC-PUMP-02` guard now checks the chain-sync `RollBackward` arm for the DISTINCT `AdmissionPeerEvent::RollBackward` (the AI-S4a fork-choice signal), not a generic `tip_update` / `TipUpdate` ("a rollback is NEVER a TipUpdate only"). `DC-PUMP-02`'s registry `statement` refined to match; `DC-PUMP-02.strengthened_in += "PHASE4-N-AN"`. The "closed event per reply" invariant is PRESERVED (the RollBackward reply's closed event is its own variant).
- **`ci_check_node_path_fidelity.sh`** — the CLI allow-list extended with `--participant-venue` (N-AI sigma=0 participant role) + `--convergence-evidence-path` (N-AJ emit-only evidence sink, like `--evidence-log`).
- **`ci_check_lifecycle_owner_uses_bootstrap_initial_state.sh`** — dropped `pipefail` (a `grep -q` SIGPIPE produced a false-negative under `pipefail`) AND scoped the `materialize_rolled_back_state` forbidden-call check to EXCLUDE the legitimate `apply_chain_event` rollback-follow region (a parallel/cold INITIAL-STATE materialize still trips; `CN-NODE-01`).

**Carried (compressed) earlier-cluster fences — full detail in the registry's `ci_script` fields:** PHASE4-N-AL (`DC-NODE-33` — participant recovered-anchor rollback no-op; test-enforced), PHASE4-N-AK (`DC-NODE-31`/`32` — recovered-anchor live-follow start + single-producer rollback no-op; test-enforced; the +2 BLUE `RecoveredAnchorPoint` types), PHASE4-N-AJ (`DC-NODE-30` — convergence-evidence emission; `ci_check_convergence_evidence_{vocabulary_closed,emit_only}.sh`), PHASE4-N-AI (`DC-NODE-23`…`29` + `CN-CONS-01` — live fork-choice rollback-follow; the `ci_check_{wal_rollback_replay_equiv,receive_detector_venue_split,live_fork_choice_apply,live_fork_choice_wiring,wire_rollback_signal_preserved,participant_venue_inert,rollback_target_canonical_binding,chain_selection_arrival_order_independent,convergence_evidence_schema}.sh` family), PHASE4-N-AH (`DC-NODE-20`/`21`/`22`), PHASE4-N-AF (`DC-NODE-18`, cert mechanism SUPERSEDED by N-AH), PHASE4-N-AE / N-AE.F (`DC-NODE-14`/`15`/`DC-CONS-24`/`DC-PROTO-10`/`DC-NODE-16`), PHASE4-N-AC (`DC-CRYPTO-10` — KES evolve-then-sign; `ci_check_kes_evolution_before_sign.sh`), PHASE4-N-AB (`CN-SESS-05` — outbound mux segmentation), PHASE4-N-AA (`DC-SERVEMEM-01` — bounded peer-driven serve), PHASE4-N-U (`DC-NODE-12`/`DC-CONS-23`/`DC-WAL-04`/`T-REC-05`/`DC-NODE-13` — forged-block durability), and the N-F-G-K…G-R wire/serve/feed/forge fences.

### Naming convention

All crates are prefixed `ade_`. TCB color is not encoded in the crate name. The authoritative classifier is `.idd-config.json` `core_paths` + the per-file `// Core Contract:` / `//! BLUE|GREEN|RED` banner + the cluster TCB Color Maps; CI scripts hard-code their BLUE list.

### Closed enums / registries (for SEAMS cross-reference)

Closed semantic surfaces detected at HEAD (carried + the N-AM/N-AN span): `AcceptedMiniProtocol` (`ade_network::session`), `TagEnvelopeError` (`ade_codec::cbor::tag24`), `ExpectedVrfInput` / `LeaderCheckVerdict` (`ade_core::consensus`), `KeepAliveMessage` / `KeepAliveCookie` / `KeepAliveState` + the agency/output/error types (`ade_network::keep_alive` + `::codec::keep_alive`, the closed mini-protocol-8 grammar REUSED by the N-AM RED client), `OutboundCommand` + `DispatchError` (`ade_runtime::network`), `KesError` / `KesParseError`, the admission/wire-only/live-log JSONL vocabularies (incl. the emit-only `NodeSchedEvent` + the N-AJ `AgreementVerdict` convergence-evidence vocabulary), the operator-evidence manifest TOML schema, the closed `CardanoEra` / Conway cert + governance enums, `PrevHash` (`ade_types::shelley::block`, the closed `Genesis | Block(Hash32)` wire grammar), the closed BLUE `ArrayHead { Definite(u64), Indefinite }` (`ade_network::codec::primitives`, SCOPED to FindIntersect — CN-WIRE-11). **N-F-A:** `SeedEpochConsensusInputs` (version-gated; the SOLE codec; carries `epoch_nonce` under `SEED_CINPUT_SCHEMA_VERSION = 2`) + `SeedConsensusInputsError` / `SeedConsensusMergeError` / `SeedEpochConsensusSource` + `WalEntry::SeedEpochConsensusInputsImported`. **N-F-C…G-H:** `Mode` (5-variant), `NodeStart`, `NodeBlockSource` (2-variant), the BA-02 vocabulary, `LoopStep`/`ForgeSlotStatus`, `ForgeIntent`, `SlotAlignmentError`/`ProtocolParamsParseError`/`ForgeCurrentPParamsError`/`ForgeEpochAdmission`, `SelfAcceptedHandoff`, `RehearsalVenue`/`PrivateRehearsalManifest`, `DispatchError` (4-variant)/`ServeStartError` (2-variant). **N-U/N-AA:** `ServedChainSource { Snapshot \| DurableChainDb }`, `ServeRangeOutcome { Served \| Empty \| CapExceeded \| ReadError }` + `CappedSlotRange` + `MAX_SERVE_RANGE_BLOCKS = 256`. **N-AE:** `ForgeFollowedTipAdmission` + `NotCaughtUpReason` + `ForgeRefused` + `NodeForgeOutcome` + `FollowedPeerTipSignal`. **N-AI:** the BLUE `WalEntry::RollBack` (tag 1, append-only) + `RollbackReason` + `RollbackPoint` + `WalError::RollbackTargetNotInChain`; the RED `NodeSyncItem` / `ReceiveClass` / `ReceiveDisposition` + the extended `NodeSyncError` + `ForgeRefused::ReselectionPending` + `AdmissionPeerEvent::RollBackward` + `AdmissionWirePumpError::UnsupportedRollbackPoint`. **N-AJ:** the GREEN closed `EvidenceEmitResult` + the reused closed `AgreementVerdict` vocabulary on the convergence-evidence file. **N-AK:** the BLUE closed record `RecoveredAnchorPoint {anchor_fp, slot, block_hash}` (version-gated `RECOVERED_ANCHOR_POINT_SCHEMA_VERSION = 1`; SOLE codec) + the closed `RecoveredAnchorPointError` (+2 BLUE types) + the RED closed `BootstrapError` additions. **PHASE4-N-AM adds ONE closed RED surface:** the `AdmissionWirePumpError::KeepAlive(KeepAliveError)` variant (a RED pump-error wrapper over the BLUE closed `KeepAliveError` — NOT canonical-counted; the keep-alive client reuses the EXISTING closed BLUE `KeepAliveMessage` / `KeepAliveState` grammar, adds no new BLUE closed surface); the sole N-AM rule `DC-PUMP-03` is `tier = derived` + `enforced`, and does NOT flip `CN-CONS-03`. **PHASE4-N-AN adds NO new closed surface** — its BLUE change is the new method `PraosChainDepState::overlay_recovered_eta0` (the SINGLE eta0-overlay authority — a method, not an enum/registry) + the `recovered_eta0: Option<&Nonce>` param on `materialize_rolled_back_state`; it reuses the existing `Nonce` / `PraosChainDepState` / `SeedEpochConsensusInputs.epoch_nonce` surfaces; the sole N-AN rule `T-REC-06` is `tier = true` + `enforced`. **Governance note (N-AN):** the single-overlay-authority + materialize-param shape are CE (mechanically gated by `ci_check_rollback_materialize_eta0.sh`), NOT a new law/closed-type; no version bump (the recovered eta0 is the existing `SeedEpochConsensusInputs.epoch_nonce` v2 sidecar field).

### CI enforcement (161 scripts under `ci/`)

The full list is mechanically obtainable via `ls ci/ci_check_*.sh` (161 at HEAD `b8860b16`). CI lives entirely under `ci/` (`ci_dirs = ["ci"]`); there is no `.github/workflows/` invariant gate at HEAD. **The `e87e8a43..HEAD` span (N-AM + N-AN + triage) added +2 CI scripts and modified 3 in place** — verified mechanically via `git diff --name-status e87e8a43..HEAD -- 'ci/ci_check_*.sh'`: **2 `A`** (`ci_check_keep_alive_wire_only.sh`, `ci_check_rollback_materialize_eta0.sh`), **3 `M`** (`ci_check_admission_wire_pump_closure.sh`, `ci_check_node_path_fidelity.sh`, `ci_check_lifecycle_owner_uses_bootstrap_initial_state.sh`), **0 `D`**. The 161 total also still carries the +2 PHASE4-N-AJ gates and the N-AI gate family from prior spans. The two NEW gates this span:

| Script | Enforces | Cluster / commit |
|---|---|---|
| `ci_check_keep_alive_wire_only.sh` | **NEW (PHASE4-N-AM, DC-PUMP-03)** — the admission wire pump runs the N2N keep-alive CLIENT (mini-protocol 8) by REUSING the BLUE `ade_network::keep_alive` grammar (reuse-not-redefine), and the keep-alive code paths NEVER synthesize a semantic `AdmissionPeerEvent` (Block / TipUpdate / RollBackward) — keep-alive is transport liveness only, so the `DC-PUMP-01` emit-set stays unwidened. | PHASE4-N-AM |
| `ci_check_rollback_materialize_eta0.sh` | **NEW (PHASE4-N-AN, T-REC-06)** — `materialize_rolled_back_state` overlays the recovered seed-epoch eta0 onto the nearest-snapshot chain_dep BEFORE the replay-forward fold so rollback replay validates each block's header VRF against eta0 (the SAME nonce live admit used), NOT the snapshot's `Nonce::ZERO` placeholder. Guards: (A) the SINGLE eta0-overlay authority (`PraosChainDepState::overlay_recovered_eta0`) exists; (B) materialize takes the `recovered_eta0` param AND applies the overlay; (C) NO VRF bypass — the replay still runs `block_validity`, no skip/unchecked. | PHASE4-N-AN |

> **The 3 modified gates this span are CI-script-only (no rule weakened):** `ci_check_admission_wire_pump_closure.sh` (DC-PUMP-02 `RollBackward` arm refined to the distinct `AdmissionPeerEvent::RollBackward`), `ci_check_node_path_fidelity.sh` (CLI allow-list + `--participant-venue` / `--convergence-evidence-path`), `ci_check_lifecycle_owner_uses_bootstrap_initial_state.sh` (dropped `pipefail` SIGPIPE false-negative + scoped the materialize check to exclude `apply_chain_event`).
>
> **PHASE4-N-AM is also test-backed** beyond its gate: `wire_pump_sends_keep_alive_on_quiescent_cadence` (`#[tokio::test(start_paused)]` virtual-time — the production `run_admission_wire_pump` `select!` emits a real proto-8 `MsgKeepAlive` over a loopback mux during inbound quiescence, no `AdmissionPeerEvent`), `wire_pump_keep_alive_response_validates_cookie_no_event`, `wire_pump_keep_alive_cookie_mismatch_fails_closed`. **PHASE4-N-AN is test-backed** by `rollback_materialize_overlays_recovered_eta0_replay_equivalent` (`None` ⇒ `VrfCert`; `Some(eta0)` ⇒ Valid + materialized `epoch_nonce == eta0`) + `rollback_materialize_does_not_bypass_vrf_on_wrong_eta0` (a WRONG eta0 still fails the header VRF — the overlay is not a bypass).
>
> **(Carried) PHASE4-N-AL / N-AK added no CI gate.** `DC-NODE-31` / `DC-NODE-32` / `DC-NODE-33` are TEST-ENFORCED (`ci_script = ""`), via the suites named in their registry `tests` arrays (`ade_node/tests/live_fork_choice_ai_s4bii.rs` + the `ade_ledger::recovered_anchor_point` / `ade_runtime::bootstrap` / `ade_runtime::seed_epoch_lineage` round-trip + warm-start suites). No dedicated `ci_check_*.sh` gate scans their structural shape — surfaced as a carried gap below.

---

## Generation notes

- Regenerated (**PHASE4-N-AN cluster-close refresh** — rollback-materialization preserves recovered eta0, `T-REC-06`; with PHASE4-N-AM keep-alive client `DC-PUMP-03` + a CI-script-only stale-gate triage in the same `e87e8a43..HEAD` span) at HEAD `b8860b16` (`git rev-parse --short HEAD`). The prior on-disk CODEMAP was pinned at the PHASE4-N-AL close (`e87e8a43`); this regeneration folds the fully-enumerable `e87e8a43..HEAD` delta: TWO new rules (`DC-PUMP-03`, `T-REC-06`), +2 CI gates, +3 in-place gate repairs, one BLUE method + one fn param (NO new canonical type), one new dev-dep.
- **The BLUE change this span is a method + a fn param, NOT a canonical type.** Verified mechanically: `git diff --name-status e87e8a43..HEAD -- 'crates/**/*.rs'` lists seven production files — the BLUE `ade_core::consensus::praos_state` (the NEW `overlay_recovered_eta0` method) + `ade_ledger::rollback::materialize` (the NEW `recovered_eta0: Option<&Nonce>` param) + `ade_ledger::receive::reducer` (a `#[cfg(test)]`-path `RollbackContext.recovered_eta0` field), the GREEN `ade_runtime::forward_sync::reducer` (the `ForwardSyncState.recovered_eta0` field), and the RED `ade_runtime::admission::wire_pump` (the N-AM keep-alive client) + `ade_runtime::bootstrap` (the N-AN restore-before-materialize reorder) + `ade_node::node_lifecycle` (the N-AN `apply_chain_event` eta0-thread). `git grep -hE "^(pub )?(struct|enum) "` over the BLUE `core_paths` trees is **462** at both `e87e8a43` and `b8860b16` (unchanged: `ade_core` 49→49, `ade_ledger` 181→181 — a new method and a new fn param add no type). The BLUE `select_best_chain` chain-selection authority (DC-CONS-03) is byte-UNCHANGED.
- Mechanical counts recomputed fresh from the tree at `b8860b16` (not copied): **11 crates** (`grep -cE '"crates/' Cargo.toml`; Δ 0), **462 canonical types** (Δ 0 vs the on-disk 462 — the BLUE change is a method + a param; `git grep` over the BLUE `core_paths` trees = 462 at both SHAs), **2455 tests** by the documented `#[test]|#[tokio::test]` grep (`git grep -hE "#\[test\]|#\[tokio::test\]" -- 'crates/**/*.rs' | wc -l`; Δ +4 vs the SAME grep at `e87e8a43` = 2451 — the N-AM keep-alive + N-AN materialize tests net +4 across the span), **161 CI checks** (`ls ci/ci_check_*.sh | wc -l`; cross-checked `git ls-tree -r HEAD ci/`; Δ +2 vs the on-disk 159 — `git diff --name-status e87e8a43..HEAD -- 'ci/ci_check_*.sh'` = 2 `A` + 3 `M` + 0 `D`), **361 registry rules** (`grep -cE '^id = ' docs/ade-invariant-registry.toml`; Δ +2 vs 359 — `DC-PUMP-03` + `T-REC-06`; status 227 enforced / 19 partial / 114 declared / 1 enforced_scaffolding; no rule weakened).
- **Registry rule reconciliation (N-AM/N-AN span):** the two net-new rules vs the prior on-disk 359 are `DC-PUMP-03` (N-AM keep-alive wire-pump client; `enforced`; `ci_scripts = ["ci/ci_check_keep_alive_wire_only.sh"]`; `cross_ref = [CN-PUMP-01, DC-PUMP-01, DC-PUMP-02, DC-NODE-30, CN-CONS-03]`; `introduced_in = "PHASE4-N-AM"`, `tier = derived`) and `T-REC-06` (N-AN rollback-materialization replay-equivalence; `enforced`; `ci_scripts = ["ci/ci_check_rollback_materialize_eta0.sh"]`; `cross_ref = [T-REC-04, DC-CINPUT-03, CN-STORE-07, DC-NODE-27, DC-CONS-20]`; `introduced_in = "PHASE4-N-AN"`, `tier = true`). **One strengthening:** `DC-PUMP-02.strengthened_in += "PHASE4-N-AN"` (the gate-triage statement refinement for the AI-S4a `RollBackward` event — a CI/registry-statement change, no code). **No rule weakened; `CN-CONS-03` NOT flipped** (it stays `declared` — N-AM/N-AN make CE-AI-6 RUNNABLE + the live reorg capture exists, but the multi-peer-ChainSel flip is a separate follow-on).
- **TCB color for the SEVEN changed source files verified against the on-disk `//!` banners at `b8860b16`:** the BLUE `praos_state.rs` (`//! BLUE …`) + `materialize.rs` (BLUE) — the N-AN BLUE change (a method + a param) sits where it belongs (the eta0 overlay + the VRF verification are BLUE authority); the BLUE `receive/reducer.rs` adds its `recovered_eta0` field only on the `#[cfg(test)]` receive-driver path. The GREEN `forward_sync/reducer.rs` (`//! GREEN …`) gains a carrier field. The RED `wire_pump.rs` / `bootstrap.rs` / `node_lifecycle.rs` host the wire-client / reorder / loop-wiring. No module was added, moved, or recolored, so the full banner survey from the N-AL refresh is carried verbatim and remains byte-accurate at `b8860b16`.
- **Cluster-doc location at this regen:** the just-closed PHASE4-N-AM + PHASE4-N-AN cluster docs were ARCHIVED to `docs/clusters/completed/PHASE4-N-AM/` + `docs/clusters/completed/PHASE4-N-AN/` by the close commit `b8860b16`; their invariants live under `docs/planning/phase4-n-am-wire-pump-keepalive-sustain-invariants.md` + `docs/planning/phase4-n-an-rollback-materialize-eta0-invariants.md`. The CE-AM-LIVE + CE-AN-LIVE (CE-AI-6 reorg) transcripts are OUTSIDE-REPO (competition-secrecy + no-credential-leak discipline; only scrubbed in-repo notes reference them). Every prior closed cluster doc remains archived under `docs/clusters/completed/`.
- **`.idd-config.json` annotations are slightly STALE at this regen (informational; this run does NOT modify the config).** At this HEAD the config's `head_deltas_baseline` is still `e87e8a43` (the PHASE4-N-AL close) and `_invariant_registry_doc` still reads "359 entries"; the next `/head-deltas` run owns the bump to `b8860b16` / 361 rules. This `/codemap` run does NOT touch `.idd-config.json` or any source file.
- **Carried delta-stacks compressed, not deleted.** The full per-cluster N-AL / N-AK / N-AJ / N-AI / N-AH … N-U / G-K…G-R delta narratives that prior on-disk CODEMAPs carried at length are compressed into the "Active cluster at HEAD" + the "Carried (compressed)" CI/forbidden-direction sections + the per-module entries; N-AM + N-AN (this span) are documented in full because they are small and fully enumerable. The load-bearing MUST NOT content for each surface is preserved (in the per-module tables + the forbidden-directions list). The registry's per-rule `ci_script(s)` / `statement` / `code_locus` fields are the authoritative source for any compressed detail.
- **Test-count method caveat (load-bearing — carried).** The documented method (`git grep -hE "#\[test\]|#\[tokio::test\]"`) matches `#[test]` and bare `#[tokio::test]` (incl. `#[tokio::test(start_paused)]`) but NOT `#[tokio::test(flavor = "multi_thread", …)]`. For the N-AM/N-AN span the caveat does NOT bite — the new keep-alive cadence test is `#[tokio::test(start_paused)]` (captured) and the materialize tests are bare `#[test]` (captured); the net +4 is the mechanically-verified delta of the documented grep across the seven changed `.rs` files + their integration suites (`2451 → 2455`). The N-AM / N-AN invariants are mechanically enforced by their dedicated gates (`ci_check_keep_alive_wire_only.sh` / `ci_check_rollback_materialize_eta0.sh`) AND the named tests.
- The doc is regenerated, not edited. If a value drifts, fix the source, not the doc.
