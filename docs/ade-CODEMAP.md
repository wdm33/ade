# Module Authority Map — Ade

> **Status:** Living architectural document. Regenerated; not hand-edited.
> Per-project instance of `~/.claude/methodology/templates/codemap.md`.

> 11 crates, 404 canonical types, 1508 tests, 47 CI checks at HEAD (`a280954`).

---

## Conventions

- A **module** in Ade is a Cargo workspace crate (smallest independently-buildable unit). One exception: `ade_network` is split by *submodule color* — its BLUE submodules and its RED submodules are documented as two separate entries below, because `.idd-config.json` `core_paths` resolves BLUE at the submodule path level rather than crate-wide. The `ade_core::consensus` submodule sits *inside* the BLUE `ade_core` crate and is covered by that entry. The `ade_ledger::block_validity` / `ade_ledger::consensus_view` / `ade_ledger::tx_validity` / `ade_ledger::mempool::admit` / `ade_ledger::mempool::ingress` / `ade_ledger::cert_classify` modules sit inside the BLUE `ade_ledger` crate and are covered by that entry; the `ade_ledger::producer` sub-tree (PHASE4-N-C — `forge`, `self_accept`, `state`; **PHASE4-N-G — `served_chain`**) and the `ade_ledger::block_body_hash` top-level module (PHASE4-N-C S4 single body-hash authority) are likewise BLUE under the already-BLUE `ade_ledger` crate prefix. The RED `ade_ledger::consensus_input_extract`, the GREEN `ade_ledger::mempool::policy`, and the GREEN `ade_ledger::mempool::canonicalize` sit inside the BLUE `ade_ledger` crate but carry a different color by their own module doc-comment and the cluster TCB Color Maps — they are surfaced as sub-classification notes inside the `ade_ledger` entry. The `ade_testkit::mempool` sub-tree (PHASE4-N-E S2), the `ade_testkit::governance` sub-tree (PROPOSAL-PROCEDURES-DECODE PP-S2), and the `ade_testkit::producer` sub-tree (PHASE4-N-C) sit inside the GREEN `ade_testkit` crate, classified GREEN by their own module doc-comments and the cluster TCB Color Maps. **NEW — PHASE4-N-G — `ade_runtime::network` sub-tree** sits inside the RED `ade_runtime` crate and hosts the per-peer N2N server session driver (RED).
- Modules are listed by TCB color (BLUE → GREEN → RED), alphabetical within each color.
- TCB color sources, in order of authority:
  1. `.idd-config.json` `core_paths` — substring match against absolute path. BLUE matches: `ade_codec`, `ade_types`, `ade_crypto`, `ade_core`, `ade_ledger`, `ade_plutus`, and the 9 `ade_network` submodule paths (`mux/frame.rs`, `codec/`, `handshake/`, `chain_sync/`, `block_fetch/`, `tx_submission/`, `keep_alive/`, `peer_sharing/`, `n2c/`).
  2. `.idd-config.json` `_core_paths_doc` — `ade_runtime` is RED; `ade_testkit` is GREEN; `ade_node` is RED; `ade_network::mux::transport` and `ade_network::session` are RED; `ade_network::lib` and `ade_network::mux::mod` are GREEN barrels. `ade_core_interop` is **RED** per its `Cargo.toml` header comment and the PHASE4-N-B TCB Color Map; the `ade_core_interop::tx_submission` (S4) and `ade_core_interop::local_tx_submission` (S5) modules carry a GREEN sub-classification by their own module doc-comments and the PHASE4-N-E TCB Color Map. The RED binaries `ade_core_interop::bin::live_tx_submission_session` (N-E S6), `ade_core_interop::bin::live_block_production_session` (N-C S7), and **`ade_core_interop::bin::live_block_fetch_session` (PHASE4-N-G S7, NEW)** are the operator-action live evidence pattern (RED by their own file-header comments and the established `live_consensus_session` precedent).
  3. `docs/clusters/completed/PHASE4-N-B/cluster.md` § "TCB Color Map" — `era_schedule`, `praos_state`, `vrf_cert`, `nonce`, `op_cert`, `leader_schedule`, `header_validate`, `fork_choice`, `rollback` are BLUE; `chain_selector`, `candidate_fragment` are GREEN; `genesis_parser` is RED; `ade_testkit::consensus` is GREEN; `ade_core_interop` is RED.
  4. `docs/clusters/completed/PHASE4-B1/cluster.md` § "TCB Color Map" — `ade_ledger::consensus_view`, `ade_ledger::block_validity` are BLUE; `ade_core::consensus::{header_validate, kes_check}` extensions are BLUE; `ade_testkit::validity` is GREEN; `ade_ledger::consensus_input_extract` is RED.
  5. PHASE4-B2 (tx-validity agreement) — `ade_ledger::tx_validity::*` and `ade_ledger::mempool::admit` are BLUE; `ade_ledger::mempool::policy` is GREEN (Tier-5); `ade_testkit::tx_validity` is GREEN.
  6. PHASE4-B3/B5 (tx value-conservation + Conway governance-cert accumulation) — all new/changed modules are BLUE under the already-BLUE crate prefixes.
  7. PHASE4-N-E (mempool wire ingress) — `ade_ledger::mempool::ingress` is BLUE (S1); `ade_ledger::mempool::canonicalize` is GREEN (S3); `ade_testkit::mempool::ingress_replay` is GREEN (S2); `ade_core_interop::tx_submission` is GREEN (S4); `ade_core_interop::local_tx_submission` is GREEN (S5); `ade_core_interop::bin::live_tx_submission_session` is RED (S6).
  8. PROPOSAL-PROCEDURES-DECODE — `ade_codec::conway::governance` is BLUE; the new `ProposalProcedure` canonical type in `ade_types::conway::governance` is BLUE; `ade_testkit::governance::proposal_procedures_replay` is GREEN.
  9. PHASE4-N-C — `ade_ledger::producer::{forge, self_accept, state}` BLUE; `ade_ledger::block_body_hash` BLUE; `ade_codec::shelley::{opcert, tx_components}` BLUE; `ade_core::consensus::opcert_validate` BLUE; `ade_crypto::kes::KesSignature` BLUE; `ade_runtime::producer::{signing, keys, scheduler, broadcast}` RED; `ade_runtime::producer::tick_assembler` GREEN; `ade_testkit::producer::*` GREEN; `ade_core_interop::bin::live_block_production_session` RED.
  10. **NEW — `docs/clusters/completed/PHASE4-N-G/cluster.md` § "TCB Color Map"** — `ade_network::chain_sync::server` (S1, S3) and `ade_network::block_fetch::server` (S1, S4) are **BLUE** (under the already-BLUE `ade_network` chain_sync/block_fetch submodule paths and per the per-file `// Core Contract:` banners); `ade_ledger::producer::served_chain` (S2) is **BLUE** (under the already-BLUE `ade_ledger` crate prefix); `ade_ledger::block_validity::header_input` (S1, *existing*) is **BLUE** (gained the new public `accepted_block_header_bytes` accessor — single canonical header projection per DC-CONS-18); `ade_runtime::producer::{broadcast_to_served, served_chain_lookups}` (S5) are **GREEN** (deterministic glue; pure-fn closure mechanically enforced by `ci/ci_check_broadcast_to_served_purity.sh`); `ade_runtime::network::n2n_server` (S6) is **RED** (per-peer N2N session driver, file-header RED banner); `ade_core_interop::bin::live_block_fetch_session` (S7) is **RED** (operator-action live evidence binary, matches the `live_block_production_session` + `live_tx_submission_session` precedent).

- **Active cluster at HEAD (none).** PHASE4-N-G (block-fetch + chain-sync server-role response paths) is **closed at HEAD `a280954`** and archived under `docs/clusters/completed/PHASE4-N-G/`. The cluster's 7 new registry rules (`DC-CONS-17`, `DC-CONS-18`, `DC-PROTO-07`, `DC-PROTO-08`, `CN-PROTO-06`, `RO-LIVE-01` — plus strengthenings to `DC-PROTO-06` and `CN-CONS-07`) are at `status = "enforced"` except `RO-LIVE-01` which carries `status = "partial"` + `open_obligation = "blocked_until_operator_peer_available"` (CE-N-G-8's live half). PHASE4-N-C's `CN-CONS-06` also remains `status = "enforced"` with its existing `open_obligation` on the operator-action live half (carried). `docs/clusters/` currently contains only the archive of the PHASE4-N-B follow-on (CE-N-B-6 manual evidence still pending operator log) and the closed `completed/` set.

- **Delta since prior CODEMAP HEAD `694dd74` (this regeneration — PHASE4-N-G cluster, 7 slices S1–S7 over commits `8cd17c9..a280954`).**

  **Structural deltas summarized:**
  - **NEW BLUE module:** `ade_ledger::producer::served_chain` (S2). Holds `ServedChainSnapshot` (BTreeMap-keyed `(SlotNo, Hash32) -> AcceptedBlock`, append-only), `served_chain_admit(snap, accepted) -> Result<ServedChainSnapshot, ServedChainAdmitError>`, the `block_at` / `block_bytes` / `range_bytes` / `iter` / `iter_accepted` accessors, and the deterministic `fingerprint()`. The only entry path is `served_chain_admit`, which derives the `(slot, hash)` key from the AcceptedBlock bytes via `decode_block` — no caller-supplied "asserted hash" can mismatch the bytes. **CN-CONS-07 strengthened in N-G:** every byte that ever leaves this index originated as `AcceptedBlock.as_bytes()`, which only `self_accept` returning `Ok(...)` produces.
  - **NEW BLUE accessor (existing module):** `accepted_block_header_bytes(&AcceptedBlock) -> Result<&[u8], BlockValidityError>` added to `ade_ledger::block_validity::header_input` (S1). The single canonical header-projection authority for the producer-side server pump. Reuses the existing `header_cbor_slice` walker — no parallel splitter (DC-CONS-18). Enforced repo-wide by `ci/ci_check_no_parallel_header_splitter.sh`.
  - **NEW BLUE modules:** `ade_network::chain_sync::server` (S1, S3) and `ade_network::block_fetch::server` (S1, S4). Each holds (a) a **closed `ServerReply` wrapper** with a private inner enum that contains only server-agency-legal variants — compile-time impossibility of constructing a client-agency reply (CN-PROTO-06); (b) per-protocol `Producer*ServerState`; (c) the pure, total, deterministic reducer (`producer_chain_sync_serve` + `producer_chain_sync_advance_tip` for chain-sync; `producer_block_fetch_serve` for block-fetch); (d) the read-side trait the GREEN adapter implements (`ServedHeaderLookup` + `HeaderProjection` for chain-sync; `ServedRangeLookup` for block-fetch).
  - **NEW GREEN sub-tree (inside RED `ade_runtime`):** `ade_runtime::producer::{broadcast_to_served, served_chain_lookups}` (S5). `broadcast_to_served::drain_and_admit` is the pure adapter that drains `BroadcastQueue` and admits each `AcceptedBlock` into `ServedChainSnapshot` via `served_chain_admit`; `served_chain_lookups::ServedChainLookups<'a>` implements both `ServedHeaderLookup` and `ServedRangeLookup` over a borrowed `ServedChainSnapshot` (orphan-rule reason: lives in the only crate that depends on both `ade_ledger` and `ade_network`). The `next_after`/`tip` impls source the projected header bytes via the canonical `accepted_block_header_bytes` (no parallel splitter).
  - **NEW RED sub-tree:** `ade_runtime::network` + `ade_runtime::network::n2n_server` (S6). `PerPeerN2nServerState` holds per-peer state for both chain-sync and block-fetch reducers + the handshake-negotiated versions. `dispatch_chain_sync_frame` / `dispatch_block_fetch_frame` decode an inbound mini-protocol frame, run the BLUE reducer through the GREEN `ServedChainLookups` adapter, and encode the outgoing frame(s). `poll_chain_sync_advance` is the deferred-RollForward poller called after each broadcast admission. `DispatchError` is the closed sum. The dispatcher is a pure state-machine driver — *no socket I/O lives here*; the binary layer (`live_block_fetch_session`) is the only place that plugs sockets in (and at HEAD that plug is honest-scope stubbed pending operator-action). Key-boundary doctrine: `ade_runtime::network` MUST NOT import from `ade_runtime::producer::signing` (enforced by `ci/ci_check_n2n_server_no_signing_dep.sh`).
  - **NEW RED operator-action live binary:** `ade_core_interop::bin::live_block_fetch_session` (S7) — CE-N-G-8 evidence-capture pass. Default hermetic `main` prints readiness + exits; `--connect` is the explicit operator-action flag. The actual tokio socket wiring from `n2n_server` to the wire is honest-scope stubbed at HEAD; the binary prints the wiring stub plan and exits in `--connect` mode pending a private cardano-node peer being provisioned. Writes `docs/clusters/PHASE4-N-G/CE-N-G-LIVE_<date>.log` once the operator-action pass runs.
  - **NEW integration tests under `ade_runtime/tests/`:** `cross_impl_server_pipeline.rs` (S7 — CE-N-G-7 mechanical adapter binding N-C's `(ProducerTick, AcceptedBlock)` corpus to N-G's server pipeline + Ade's own decoder; 2 tests), `server_paths_transcript_replay.rs` (S5 — synthetic session-transcript replay: `session_transcript_replay_byte_identical`, `session_transcript_served_block_bytes_equal_admitted_accepted_block_bytes`, `session_transcript_announced_header_matches_served_body_recipe`), `n2n_server_two_peer_determinism.rs` (S6 — OQ-4 multi-peer determinism: per-session transcripts independent under parallel drive). The cluster doc named these "under `ade_testkit::server_paths::*`"; they shipped under `ade_runtime::tests::*` instead (the orphan-rule + dev-dep direction made `ade_runtime::tests/` the natural host since they exercise the full RED dispatcher → GREEN adapter → BLUE reducer pipeline).
  - **NEW closed canonical types (+13):** `ServedChainSnapshot`, `ServedChainAdmitError` (in `ade_ledger::producer::served_chain`); `ServerReply` + private `ServerVariant` + `HeaderProjection` + `ProducerChainSyncServerState` + `ProducerServerError` + `ServerStep` (in `ade_network::chain_sync::server`); `ServerReply` + private `ServerVariant` + `ProducerBlockFetchServerState` + `ProducerBlockFetchServerError` + `BlockFetchServerStep` (in `ade_network::block_fetch::server`). **Canonical type count: 391 → 404 (+13).** Per-BLUE-crate breakdown at HEAD: `ade_codec` 10, `ade_types` 81, `ade_crypto` 13, `ade_core` 44, `ade_ledger` **139 (+2)**, `ade_plutus` 8, BLUE `ade_network` **109 (+11)** = 404.
  - **NEW CI gates (+7):** `ci/ci_check_no_parallel_header_splitter.sh` (S1; DC-CONS-16 strengthening + DC-CONS-18 entry; repo-wide grep — only `accepted_block_header_bytes` in `block_validity/header_input.rs` may project header bytes), `ci/ci_check_served_chain_closure.sh` (S2; CN-CONS-07 strengthening + CE-N-G-2; scopes `crates/ade_ledger/src/producer/served_chain.rs` — forbids `HashMap`/`HashSet`, asserts admit single-argument), `ci/ci_check_chain_sync_server_closure.sh` (S3; DC-PROTO-08; scopes `crates/ade_network/src/chain_sync/server.rs` — no wall-clock/rand/async/HashMap, no pub fn returns raw `ChainSyncMessage`, presence of the two reducers), `ci/ci_check_block_fetch_server_closure.sh` (S4; DC-CONS-17 enforcement foundation; scopes `crates/ade_network/src/block_fetch/server.rs` — same shape + asserts `ServerReply::block(` is fed from `ServedRangeLookup`), `ci/ci_check_broadcast_to_served_purity.sh` (S5; CE-N-G-5; scopes `crates/ade_runtime/src/producer/{broadcast_to_served,served_chain_lookups}.rs` — pure adapter shape + positive `accepted_block_header_bytes` import), `ci/ci_check_n2n_server_no_signing_dep.sh` (S6; key-boundary doctrine; scopes `crates/ade_runtime/src/network/` — forbids any import path to `crate::producer::signing`), `ci/ci_check_server_paths_corpus_present.sh` (S7; CE-N-G-7 + binary presence; checks the two integration tests exist with their named test functions, the binary src + Cargo.toml `[[bin]]` entry exist, the procedure doc exists). **CI script count: 40 → 47 (+7).**
  - **NEW registry rules / strengthenings (+8 entries — 4 net additions plus 2 strengthenings; total registry rules 188 → 196):** `DC-CONS-17` (block-fetch served bytes byte-identical to `AcceptedBlock.as_bytes()`, enforced), `DC-CONS-18` (chain-sync `RollForward.header` bytes = `accepted_block_header_bytes`; recipe binding to `block_body_hash` on the served body, enforced), `DC-PROTO-07` (per-session orchestrator deterministic across replays, enforced), `DC-PROTO-08` (deterministic resolution at server-agency states — no silent wait, enforced), `CN-PROTO-06` (type-level closed `ServerReply<M>` wrapper; client-agency messages from server pump unrepresentable, enforced), `RO-LIVE-01` (release: Haskell peer accepts Ade-forged block via RequestRange, partial — `blocked_until_operator_peer_available`). Strengthens `DC-PROTO-06` (`PHASE4-N-A` → `PHASE4-N-A, PHASE4-N-G`) and `CN-CONS-07` (`[]` → `PHASE4-N-G`; broadcast gate preserved across the network seam — bytes that reach the wire must trace through `AcceptedBlock` → `ServedChainSnapshot`).
  - **NEW test inventory (+55).** Per-crate at HEAD: `ade_codec` 162 (unchanged), `ade_types` 23 (unchanged), `ade_crypto` 51 (unchanged), `ade_core` 126 (unchanged), `ade_ledger` 492 → **502 (+10)** (8 inline tests in `served_chain.rs` + 2 inline tests in `block_validity/header_input.rs` for `accepted_block_header_bytes`), `ade_plutus` 28 (unchanged), `ade_testkit` 312 (unchanged), `ade_runtime` 58 → **78 (+20)** (10 inline tests in `network/n2n_server.rs` + 3 inline tests in `producer/broadcast_to_served.rs` + 3 inline tests in `producer/served_chain_lookups.rs` + 4 integration tests across the three new `tests/*.rs` files), `ade_network` 176 → **200 (+24)** (large inline test blocks in `chain_sync/server.rs` and `block_fetch/server.rs` covering reducer totality, byte-identicality, deterministic resolution), `ade_node` 0, `ade_core_interop` 25 → **26 (+1)** (the binary's hermetic-mode smoke test). **Test inventory 1453 → 1508 (+55)**, reported approximate per the template's fallback rule (test runner not executed).
  - **New crate-level edges (+2):** `ade_runtime → ade_network` is now a **non-dev** dependency (S6 — the `network/n2n_server.rs` driver and S5 `served_chain_lookups.rs` trait impls live in `ade_runtime` and need `ade_network`'s server reducer + trait surface). This is a legal RED → BLUE direction (BLUE submodules of `ade_network`) and conformant with `ci/ci_check_dependency_boundary.sh`. `ade_network` gained **dev-deps** on `ade_ledger`, `ade_testkit`, `ade_crypto`, `ade_core` (for the corpus-backed reducer tests inside `chain_sync/server.rs` and `block_fetch/server.rs` that build `ServedChainSnapshot` over the Conway-576 corpus). Dev-dep direction is BLUE-test-only-on-BLUE — also legal.

- Counts:
  - Crates: 11, from `Cargo.toml` `[workspace] members`. Unchanged.
  - Canonical types: 404, from `grep -rE "^(pub )?(struct|enum) "` across the full BLUE scope. Breakdown: `ade_codec` 10, `ade_types` 81, `ade_crypto` 13, `ade_core` 44, `ade_ledger` **139 (+2)**, `ade_plutus` 8, plus the 9 BLUE `ade_network` submodule paths **109 (+11)**. Registry `canonical_type_registry: null`, so a structural count is used. **+13 since `694dd74`** — N-G server-role types listed above.
  - Tests: 1508 — count of `#[test]` / `#[tokio::test]` attributes across `crates/`. Reported as approximate per the template's fallback rule. **+55 since `694dd74`**, breakdown above.
  - CI checks: 47 — file count under `ci/ci_check_*.sh`. **+7 since `694dd74`** — `ci_check_no_parallel_header_splitter.sh`, `ci_check_served_chain_closure.sh`, `ci_check_chain_sync_server_closure.sh`, `ci_check_block_fetch_server_closure.sh`, `ci_check_broadcast_to_served_purity.sh`, `ci_check_n2n_server_no_signing_dep.sh`, `ci_check_server_paths_corpus_present.sh` (all PHASE4-N-G). Full inventory at HEAD: `block_fetch_server_closure` *(NEW)*, `broadcast_to_served_purity` *(NEW)*, `cbor_round_trip`, `ce_n_a_5_proof`, `chaindb_contract`, `chaindb_crash_safety`, `chain_sync_server_closure` *(NEW)*, `consensus_closed_enums`, `constitution_coverage`, `conway_cert_classification_closed`, `credential_discriminant_closed`, `crypto_vectors`, `dependency_boundary`, `deposit_param_authority`, `differential_divergence`, `forbidden_patterns`, `forge_purity`, `gov_cert_accumulation_closed`, `hash_uses_wire_bytes`, `hfc_translation`, `ingress_chokepoints`, `ledger_determinism`, `mempool_ingress_closure`, `mempool_ingress_replay`, `module_headers`, `n2n_server_no_signing_dep` *(NEW)*, `no_async_in_blue`, `no_chaindb_in_consensus_blue`, `no_density_in_fork_choice`, `no_float_in_consensus`, `no_parallel_header_splitter` *(NEW)*, `no_private_keys_in_corpus`, `no_producer_body_encoder`, `no_secrets`, `no_semantic_cfg`, `no_signing_in_blue`, `opcert_closed`, `pallas_quarantine`, `private_key_custody`, `producer_corpus_present`, `proposal_procedures_closed`, `recovery_contract`, `ref_provenance`, `scheduler_closure`, `self_accept_gate`, `served_chain_closure` *(NEW)*, `server_paths_corpus_present` *(NEW)*. The forward-looking `ci/ci_check_no_fail_open_in_validation.sh` (the `if len == K { check } else { skip }` grep gate, DC-VAL-06 / DC-TXV) is still **not** shipped. No `.github/workflows/` yet.

---

## BLUE Modules — Pure Functional Core

> **Shared header (applies to every BLUE entry below).** Every `.rs` source file begins with the contract banner
> `// Core Contract:` and the following deny attributes are present in each crate's `lib.rs` (or, for `ade_network`,
> at the crate root — the BLUE submodules inherit them):
> `#![deny(unsafe_code)]`, `#![deny(clippy::unwrap_used)]`, `#![deny(clippy::expect_used)]`,
> `#![deny(clippy::panic)]`, `#![deny(clippy::float_arithmetic)]`.
> CI scripts that enforce the shared rules (all 7 scope the full BLUE set — the 6 BLUE crates plus the 9 BLUE `ade_network` submodule paths declared in `.idd-config.json` `core_paths`):
> - `ci/ci_check_module_headers.sh` — banner first-line check.
> - `ci/ci_check_forbidden_patterns.sh` — `HashMap`, `HashSet`, `IndexMap`/`IndexSet`/`indexmap::`, `SystemTime`, `Instant`, `std::fs`, `std::net`, `tokio`, `async fn`, `f32`/`f64`, `anyhow`, `rand::thread_rng`, `thread::spawn`, plus `unsafe` outside a documented allowlist.
> - `ci/ci_check_dependency_boundary.sh` — no BLUE crate depends on a RED crate.
> - `ci/ci_check_no_signing_in_blue.sh` — `SigningKey`/`SecretKey`/`PrivateKey`/`private_key`/`sign_message`/`sign_block` forbidden in BLUE.
> - `ci/ci_check_no_semantic_cfg.sh` — `#[cfg(feature = ...)]` and `cfg!(feature = ...)` forbidden in BLUE.
> - `ci/ci_check_hash_uses_wire_bytes.sh` — no hashing of `canonical_bytes` / re-encoded bytes in BLUE.
> - `ci/ci_check_ingress_chokepoints.sh` — only named `decode_*` chokepoints construct `PreservedCbor`.
> - `ci/ci_check_pallas_quarantine.sh` — `pallas-*` references confined to `ade_plutus`.
> - `ci/ci_check_no_async_in_blue.sh` *(PHASE4-N-A, S-A1)* — async constructs forbidden anywhere in the BLUE scope. Enforces DC-CORE-01.
>
> Three additional CI scripts narrow the shared header to the `ade_core::consensus` tree (PHASE4-N-B): `ci/ci_check_no_chaindb_in_consensus_blue.sh`, `ci/ci_check_no_float_in_consensus.sh`, `ci/ci_check_consensus_closed_enums.sh` (TARGETS scope: `crates/ade_core/src/consensus/`, `crates/ade_ledger/src/block_validity/`, `crates/ade_ledger/src/tx_validity/`, `crates/ade_ledger/src/mempool/`).
> A fourth narrow check enforces a single fork-choice rule: `ci/ci_check_no_density_in_fork_choice.sh` (DC-CONS-03).
> A fifth narrow check (PHASE4-B3) enforces deposit-parameter authority: `ci/ci_check_deposit_param_authority.sh` (DC-TXV-07).
> A sixth narrow check (PHASE4-B5) enforces gov-cert accumulation closure: `ci/ci_check_gov_cert_accumulation_closed.sh` (DC-LEDGER-09).
> A seventh narrow check (OQ5/COMMITTEE/DREP/ENACTMENT-COMMITTEE-FIDELITY + ENACTMENT-COMMITTEE-WRITEBACK) enforces credential-discriminant closure: `ci/ci_check_credential_discriminant_closed.sh` (DC-LEDGER-10).
> An eighth narrow check (PROPOSAL-PROCEDURES-DECODE, PP-S1) enforces proposal_procedures closure: `ci/ci_check_proposal_procedures_closed.sh` (DC-LEDGER-11).
>
> **Eight narrow checks added by PHASE4-N-C (the producer-authority gate set):**
> - `ci/ci_check_forge_purity.sh` (DC-CONS-13/14/15) — forge body purity.
> - `ci/ci_check_no_producer_body_encoder.sh` (DC-CONS-16) — single body-hash authority, repo-wide.
> - `ci/ci_check_no_private_keys_in_corpus.sh` (DC-CRYPTO-03/04/05 corpus discipline) — producer + (N-G S5 extension) server-paths corpora private-key-free.
> - `ci/ci_check_opcert_closed.sh` (DC-CONS-11/12) — closed opcert encoders + acceptance authority.
> - `ci/ci_check_private_key_custody.sh` (DC-CRYPTO-03/04/05 + OP-OPS-04) — signing primitives RED-confined.
> - `ci/ci_check_producer_corpus_present.sh` (CN-CONS-06 mechanical half) — fixture-presence guard.
> - `ci/ci_check_scheduler_closure.sh` (DC-CONS-13 + DC-MEM-03 + OP-OPS-05) — RED scheduler purity + BroadcastQueue value-take.
> - `ci/ci_check_self_accept_gate.sh` (CN-CONS-07) — type-level broadcast-gate guards.
>
> **NEW — Seven narrow checks added by PHASE4-N-G (the producer-side server-role gate set):**
> - `ci/ci_check_no_parallel_header_splitter.sh` (DC-CONS-16 strengthening + DC-CONS-18, S1) — repo-wide grep gate: the only `pub fn .*header_bytes` site is `accepted_block_header_bytes` in `crates/ade_ledger/src/block_validity/header_input.rs`. No new `pub fn .*split_header(...)` or `pub fn .*split_block_envelope(...)` anywhere.
> - `ci/ci_check_served_chain_closure.sh` (CN-CONS-07 strengthening + CE-N-G-2, S2) — scopes `crates/ade_ledger/src/producer/served_chain.rs`: no `HashMap`/`HashSet`/`std::collections::Hash*`; `served_chain_admit` is single-argument (no caller-supplied "asserted hash"); `ServedChainSnapshot.blocks` private (no struct-literal back-door).
> - `ci/ci_check_chain_sync_server_closure.sh` (DC-PROTO-08, S3) — scopes `crates/ade_network/src/chain_sync/server.rs`: no wall-clock/rand/async/HashMap in production code; no `pub fn` returns raw `ChainSyncMessage` (outgoing replies go via `ServerReply::into_message()`); positive presence of `producer_chain_sync_serve` + `producer_chain_sync_advance_tip`.
> - `ci/ci_check_block_fetch_server_closure.sh` (DC-CONS-17 foundation, S4) — scopes `crates/ade_network/src/block_fetch/server.rs`: same shape + asserts `ServerReply::block(` is fed from the `ServedRangeLookup` iterator output (positive grep that the `block` constructor's argument is sourced from `served.range_bytes`).
> - `ci/ci_check_broadcast_to_served_purity.sh` (CE-N-G-5, S5) — scopes `crates/ade_runtime/src/producer/{broadcast_to_served,served_chain_lookups}.rs`: no `std::time`/`tokio`/`rand`/`HashMap` imports; `drain_and_admit` signature pinned to `(ServedChainSnapshot, BroadcastQueue) -> (ServedChainSnapshot, BroadcastQueue, Vec<AcceptedBlock>)`; `ServedChainLookups` impls both trait seams; positive `accepted_block_header_bytes` import (no parallel splitter in the GREEN adapter).
> - `ci/ci_check_n2n_server_no_signing_dep.sh` (S6, key-boundary doctrine) — scopes `crates/ade_runtime/src/network/`: forbids any `use crate::producer::signing` / `producer::signing::` reference. Private-key custody stays RED-confined to `producer::signing`; the server pump handles only AcceptedBlock-derived bytes.
> - `ci/ci_check_server_paths_corpus_present.sh` (CE-N-G-7 + CE-N-G-8 binary presence, S7) — checks the two integration tests exist with their named test functions at `crates/ade_runtime/tests/cross_impl_server_pipeline.rs` and `crates/ade_runtime/tests/server_paths_transcript_replay.rs`; checks the live binary src + Cargo.toml `[[bin]]` entry exist; checks the procedure doc `docs/clusters/PHASE4-N-G/CE-N-G-8_PROCEDURE.md` exists.
>
> Two checks narrow the shared header to the wire-ingress path of `ade_ledger::mempool` (PHASE4-N-E):
> - `ci/ci_check_mempool_ingress_closure.sh` (DC-MEM-03, S1).
> - `ci/ci_check_mempool_ingress_replay.sh` (DC-MEM-04, S2 + S3).
>
> A BLUE crate or BLUE `ade_network` submodule that adds a feature flag, an async function, a `HashMap`, or a RED dep fails CI on push.
> The 3 RED-scope CI scripts (`ci_check_chaindb_contract.sh`, `ci_check_recovery_contract.sh`, `ci_check_chaindb_crash_safety.sh`) and the 1 evidence script `ci_check_ce_n_a_5_proof.sh` are not part of this shared header — they are documented in the cross-module CI matrix at the bottom.

---

### `ade_codec`

> **Status carried from prior CODEMAP — unchanged at HEAD `a280954`.** PHASE4-N-G did not touch `ade_codec`. The N-C-introduced `shelley::opcert` + `shelley::tx_components` modules carry forward unchanged.

| Attribute | Value |
|-----------|-------|
| **Purpose** | Owns Cardano-canonical CBOR ingress: the only place in the workspace that turns raw bytes into typed semantic values, with wire-byte preservation for every hash-bearing structure. Also owns the standalone opcert byte authority (`shelley::opcert::{encode_opcert, decode_opcert}`) and the canonical Conway-tx preserved-byte splitter (`shelley::tx_components::split_conway_tx_components` — reused by `ade_ledger::producer::forge` and the body-hash recomputation path). |
| **Creates** | `PreservedCbor<T>`, `RawCbor`, `BlockEnvelope`, `ByronDecodedBlock`, `CodecContext`, `CodecError` (incl. `UnknownCertTag`, `DuplicateMapKey`, `TrailingBytes`, `InvalidCborStructure`), `ContainerEncoding`, `IntWidth`, plus era-tagged block/tx wrappers under `byron/`, `shelley/`, `allegra/`, `mary/`, `alonzo/`, `babbage/`, `conway/`. Functions: `conway::cert::{decode_conway_certs, decode_drep}`, `conway::withdrawals::{decode_withdrawals, withdrawals_sum}`, `conway::governance::{decode_proposal_procedures, encode_proposal_procedures}`, `shelley::cert::read_pool_registration_cert`, both era `decode_stake_credential`, `shelley::opcert::{encode_opcert, decode_opcert, write_opcert_fields_into}`, `shelley::tx_components::split_conway_tx_components`. Types: `OpCertCodecError`, `TxComponents<'a>`. |
| **Interprets** | All canonical Cardano CBOR — block envelopes, era-specific blocks, tx bodies, tx outs, certificates, addresses. Conway certificate array (closed over CDDL tags 0..18, owner-complete) and Conway withdrawals map (deduplicated). Both era credential decoders preserve the key/script tag. Sole authority for `PreservedCbor::new` (constructor is `pub(crate)`). CIP-1694 `proposal_procedure` array. Standalone cardano-cli `OperationalCertificate` 4-tuple. Conway transaction 4-tuple (preserved-byte slicing only). |
| **MUST NOT** | (1) Construct `PreservedCbor` outside `ade_codec` (`pub(crate)` + `ci_check_ingress_chokepoints.sh`). (2) Re-encode wire bytes when computing hashes (`ci_check_hash_uses_wire_bytes.sh`). (3) Use any forbidden BLUE pattern. (4) Depend on any other workspace crate except `ade_types`. (5) `conway::cert` (DC-LEDGER-08) — no unknown-tag swallow; owner-complete; no catch-all. (6) `conway::withdrawals` — no last-wins on duplicate `RewardAccount`. (7) `decode_stake_credential` (DC-LEDGER-10) — must not erase the credential tag. (8) `conway::governance` (DC-LEDGER-11) — no silent skip on unknown `GovAction`; no opaque pass-through at body codec key 20. (9) `shelley::opcert` (DC-CONS-11/12) — cardano-byte-identical 4-tuple; dedicated `OpCertCodecError` variant per shape failure; not a second header-embedded opcert encoder (enforced by `ci_check_opcert_closed.sh`). (10) `shelley::tx_components` (DC-CONS-13/16) — preserved-byte slices that alias the input buffer (no `to_vec` / clones); reject non-4-tuple shapes; not re-encode the boolean validity flag or the auxiliary-data null. |
| **Inbound deps** | `ade_ledger`, `ade_plutus`, `ade_testkit`, `ade_network`, `ade_runtime`, `ade_core_interop`. No new inbound crate edge in PHASE4-N-G. |
| **Outbound deps** | `ade_types`. No external dependencies; std-only. Dev-deps: `serde_json`, `toml`. |
| **Entry points** | `ade_codec::cbor::envelope::decode_block_envelope`, `ade_codec::cbor`, `ade_codec::traits::AdeEncode`, `ade_codec::CodecContext`, per-era `decode_*_block`, `ade_codec::address::decode_address`. B2: `ade_codec::conway::tx::decode_conway_tx_body`. B3: `ade_codec::conway::cert::decode_conway_certs` and `ade_codec::conway::withdrawals::{decode_withdrawals, withdrawals_sum}`. PP-S1: `ade_codec::conway::governance::{decode_proposal_procedures, encode_proposal_procedures}`. N-C: `ade_codec::shelley::opcert::{encode_opcert, decode_opcert, OpCertCodecError}`, `ade_codec::shelley::tx_components::{split_conway_tx_components, TxComponents}`. **N-G NEW heavy use site:** `decode_block_envelope` is now also consumed by the N-G integration test `cross_impl_server_pipeline.rs` to decode every served `Block { bytes }` payload — a structural cross-impl assertion that the bytes Ade serves are Ade-decodable independent of the network. |
| **Key modules** | `cbor/`, `byron/`, `shelley/` (incl. `cert.rs`, `opcert.rs`, `tx_components.rs`), `allegra/`, `mary/`, `alonzo/`, `babbage/`, `conway/`, `address/`, `preserved.rs`, `traits.rs`, `primitives.rs`, `error.rs`. |

---

### `ade_core`

> **Status carried from prior CODEMAP — unchanged at HEAD `a280954`.** PHASE4-N-G did not touch `ade_core`. The N-C-introduced `consensus::opcert_validate.rs` module carries forward unchanged.

| Attribute | Value |
|-----------|-------|
| **Purpose** | BLUE authoritative Praos consensus core. Owns canonical types and pure state-transitions that decide which header / chain Ade accepts: HFC era schedule + slot↔era↔time translation, Praos chain-dep state (nonce / op-cert counters), VRF cert verification + leader-eligibility predicate, KES signature + op-cert period verification wired into header admission, header validation pipeline, fork-choice, rollback authority, leader-schedule query, canonical encodings of all chain-dep state and chain events. Owns the producer-side opcert acceptance authority (`opcert_validate`). |
| **Creates** | **Schedule:** `BootstrapAnchorHash`, `EraSchedule`, `EraSummary`, `EraLocation`. **State:** `PraosChainDepState`, `OpCertCounterMap`, `Nonce`. **Events/points:** `Point`, `ChainHash`, `BlockDistance`, `SecurityParam`, `ChainEvent`, `ChainSelectionReject`. **Errors:** `HFCError`, `SlotTimeError`, `OutsideForecastRange`, `HeaderValidationError`, `FieldError`, `FieldKind`, `VrfCertError`, `OpCertCounterError`, `NonceEvolutionError`, `LeaderScheduleError`, `OpCertError`. **Header surface:** `HeaderInput`, `HeaderVrf`, `HeaderKes`, `ValidatedHeaderSummary`, `HeaderApplied`. **Fork-choice:** `TiebreakerView`, `CandidateFragment`, `ChainSelectorState`, `ForkChoiceError`. **Op-cert/nonce:** `OpCertObservation`, `NonceInput`. **VRF:** `VrfRole`, `VerifiedVrf`, `StakeFraction`, `ActiveSlotsCoeff`. **Leader schedule:** `LeaderScheduleQuery`, `LeaderScheduleAnswer`. **Ledger view boundary:** `LedgerView` trait. **Rollback:** `RollBackRequest`, `RollBackApplied`. **Encoding:** `DecodeError`. 44 public types. |
| **Interprets** | Canonical inputs from the `ade_runtime` shell — `HeaderInput` projections, `EraSchedule` materialized once from genesis JSON, `LedgerView` snapshots from `ade_ledger::consensus_view::PoolDistrView`, ordered `StreamInput` events. KES check verifies hot KES key signature over the header body CBOR bytes. `opcert_validate` consumes an `OperationalCert`, cold key, expected period, and prev counter. |
| **MUST NOT** | (1) Take or accept a `&ChainDb` reference (`ci_check_no_chaindb_in_consensus_blue.sh`). (2) Use any `f32`/`f64` (`ci_check_no_float_in_consensus.sh`). (3) Add `#[non_exhaustive]` to any consensus enum, open-tail `Other`/`Unknown`, owned `String` fields, or `Box<dyn ...>` (`ci_check_consensus_closed_enums.sh`). (4) Reference `density` in `fork_choice.rs`/`candidate.rs` outside a `// no-density:` annotation. (5) Read wall-clock. (6) Use `HashMap`/`HashSet`; `OpCertCounterMap` uses `BTreeMap`. (7) `async fn`, `.await`, `tokio`. (8) Construct a `PreservedCbor`. (9) Re-derive stake snapshots (DC-CONSENSUS-02). (10) Bypass the canonical `validate_and_apply_header` pipeline. (11) B1 fail-closed (DC-VAL-06). (12) B1 KES rule (DC-CRYPTO-01). (13) All shared-header BLUE rules. (14) `consensus::opcert_validate` (DC-CONS-11/12) — `OpCertError` closed; sub-check order pinned; `prev_counter == None` only first-opcert acceptance path; no external counter store. |
| **Inbound deps** | `ade_ledger` (B1 + N-C — `block_validity` calls `validate_and_apply_header`; `producer::forge` calls `opcert_validate` + `is_leader_for_vrf_output`), `ade_runtime`, `ade_testkit`, `ade_core_interop`. No new inbound crate edge in PHASE4-N-G — the N-G integration tests under `ade_runtime/tests/*` and `ade_network/src/{chain_sync,block_fetch}/server.rs` test modules reuse the established `ade_runtime → ade_core` and dev-dep `ade_network → ade_core` edges. |
| **Outbound deps** | `ade_types`, `ade_crypto`. Dev-deps: `ade_testkit`, `serde_json`, `cardano-crypto`. |
| **Entry points** | `use ade_core::consensus::{...}` aggregator, `ade_core::consensus::ledger_view::LedgerView`, `ade_core::consensus::vrf_cert::{vrf_input, verify_praos_vrf, ...}`, `ade_core::consensus::kes_check::{verify_header_kes, expect_size}`, `ade_core::consensus::praos_state::*`, `ade_core::consensus::header_summary::*`. Top-level transitions: `validate_and_apply_header`, `select_best_chain`, `apply_rollback`, `apply_nonce_input`, `apply_op_cert`, `query_leader_schedule` + `is_leader_for_vrf_output`, `verify_vrf_cert` + `verify_praos_vrf` + `check_leader_claim` + `is_leader`, `tiebreaker_prefer`, `encode/decode_chain_dep_state`, `encode/decode_chain_event`. `ade_core::consensus::opcert_validate::{opcert_validate, OpCertError}`. |
| **Key modules** | `consensus/era_schedule.rs`, `consensus/praos_state.rs`, `consensus/events.rs`, `consensus/errors.rs`, `consensus/vrf_cert.rs`, `consensus/kes_check.rs`, `consensus/nonce.rs`, `consensus/op_cert.rs`, `consensus/leader_schedule.rs`, `consensus/header_summary.rs` + `consensus/header_validate.rs`, `consensus/candidate.rs`, `consensus/fork_choice.rs`, `consensus/rollback.rs`, `consensus/ledger_view.rs`, `consensus/encoding.rs`, `consensus/opcert_validate.rs`. |

---

### `ade_crypto`

> **Status carried from prior CODEMAP — unchanged at HEAD `a280954`.** PHASE4-N-G did not touch `ade_crypto`.

| Attribute | Value |
|-----------|-------|
| **Purpose** | Owns pure cryptographic verification primitives — Blake2b hashing, Ed25519 / Byron-bootstrap signature verification, KES verification with operational certificates, VRF verification, plus the closed signature-artifact types (`KesSignature`, `VrfProof`, `VrfOutput`, `Ed25519Signature`) that BLUE consumes across the RED→BLUE boundary. Verification only — signing lives in `ade_runtime::producer::signing`. |
| **Creates** | `Blake2b224`, `Blake2b256`, `HashAlgorithm` trait, `Ed25519VerificationKey`, `Ed25519Signature`, `ByronExtendedVerificationKey`, `KesVerificationKey`, `KesPeriod`, `OperationalCertData`, `VrfVerificationKey`, `VrfProof`, `VrfOutput`, `CryptoError`, `KesSignature(pub [u8; SUM6_KES_SIG_LEN])`, `SUM6_KES_SIG_LEN: usize = 448`. |
| **Interprets** | Verification key / signature / proof byte structures. Not a CBOR parser — accepts already-decoded byte slices. |
| **MUST NOT** | (1) Implement signing (`ci_check_no_signing_in_blue.sh`). (2) Allocate global state. (3) Use any BLUE forbidden pattern. (4) Use `unsafe` outside the allowlisted FFI in `src/vrf.rs`. (5) `build_opcert_signable` must produce the spec-correct raw concatenation. (6) `KesSignature` (DC-CRYPTO-04) — closed length-pinned wrapper; only `from_bytes` construction; custom redacting `Debug`; no `PartialOrd`/`Ord`/`Hash` derives. (7) No `Drop` impl that zeroizes — `KesSignature` is BLUE and not secret. |
| **Inbound deps** | `ade_core`, `ade_ledger`, `ade_plutus`, `ade_testkit`, `ade_core_interop`, `ade_runtime`. No new inbound crate edge in PHASE4-N-G. |
| **Outbound deps** | `ade_types`, `blake2`, `ed25519-dalek`, `cardano-crypto` (vrf-draft03 + kes-sum + dsign features, `default-features = false`). |
| **Entry points** | `ade_crypto::blake2b::blake2b_256`, `blake2b_224`, `blake2b_256`, `block_header_hash`, `transaction_id`, `script_hash`, `credential_hash`, `verify_ed25519`, `verify_byron_bootstrap`, `verify_kes`, `verify_opcert`, `verify_vrf`, `ade_crypto::kes::{KesSignature, SUM6_KES_SIG_LEN}`. |

---

### `ade_ledger`

> **Status change at HEAD `a280954` (PHASE4-N-G S1 + S2 — one new BLUE sub-module + one new public accessor on an existing BLUE module).** Between `694dd74` and HEAD: (a) the new `ade_ledger::producer::served_chain` module (S2) hosts the BLUE producer-side served-chain index — `ServedChainSnapshot` (`BTreeMap<(SlotNo, Hash32), AcceptedBlock>`-backed, append-only), `served_chain_admit(snap, accepted)` (the only entry path; derives `(slot, hash)` from the AcceptedBlock bytes via `decode_block` — no caller-supplied "asserted hash"), and the read accessors `block_at` / `block_bytes` / `range_bytes` / `iter` / `iter_accepted` / `fingerprint`. **CN-CONS-07 strengthened in N-G:** the broadcast gate is preserved across the network seam — bytes that ever reach the wire trace through `AcceptedBlock` → `ServedChainSnapshot`. (b) The existing `ade_ledger::block_validity::header_input.rs` gained the public accessor `accepted_block_header_bytes(&AcceptedBlock) -> Result<&[u8], BlockValidityError>` (S1). This is the **single canonical header-projection authority** for the producer-side server pump (DC-CONS-18) — reuses the existing `header_cbor_slice` walker; no parallel splitter. All prior B-track / N-E / PROPOSAL-PROCEDURES-DECODE / N-C narrative carries forward.

| Attribute | Value |
|-----------|-------|
| **Purpose** | The functional core (ledger half): stateless ledger rules for every Cardano era; the B1 top-level block-validity verdict; the B2 top-level transaction-validity verdict + mempool admission; the B3 full Conway value-conservation accounting; the B4 closed Conway cert-state accumulation; the B5 closed Conway governance-cert accumulation; the live committee-enactment write-back; the single BLUE chokepoint `mempool::ingress::mempool_ingress` from wire ingress into `admit`; the BLUE producer authority (`forge_block`, `self_accept`, `AcceptedBlock` broadcast token, `block_body_hash` recipe). **NEW — PHASE4-N-G:** the BLUE producer-side served-chain index (`producer::served_chain::{ServedChainSnapshot, served_chain_admit}` — the only path AcceptedBlock bytes enter a network-readable store; CN-CONS-07 strengthening) and the single canonical header-projection authority (`block_validity::header_input::accepted_block_header_bytes` — DC-CONS-18; the chain-sync `RollForward.header` bytes are this function's output verbatim). |
| **Creates** | Carry-forward: `LedgerState`, `EpochState`, `UTxOState`, `TxOut`, `Value`, `MultiAsset`, `AssetName`, `DelegationState`, `CertState`, `PoolParams`, `ConwayGovState`, `GovActionState`, `RatificationResult`, `EnactmentEffects`, `ProtocolParameters`, `BlockApplyResult`, `BlockVerdict`, `TxVerdict`, `ScriptPosture`, `EpochBoundaryResult`, `LedgerFingerprint`, `Rational`, `WitnessInfo`, layered `LedgerError`. **B1:** `PoolDistrView`, `PoolEntry`, `BlockValidityOutcome`, `DecodedBlock`, `BlockValidityVerdict`, `BlockRejectClass`, `BlockValidityError`, `FieldError`, `FieldKind`, `MissingInput`, `VerdictSurface`, `SurfaceDecodeError`. **B2:** `TxValidityVerdict`, `TxRejectClass`, `TxValidityError`, `TxValidityOutcome`, `TxVerdictSurface`, `TxSurfaceDecodeError`, `DecodedTx`, `VKeyWitnessRef`, `WitnessClosureError`, `WitnessField`, `SignerSource`, `RequiredSigners`, `RequiredSignerError`, `RequiredSignerField`, `ResolvedInputs`, `ResolvedOutput`, `MempoolState`, `AdmitOutcome` (BLUE); `OrderPolicy` (GREEN). **B3:** `ConwayOnlyDepositParams`, `ConwayDepositParams`, `ValidationEnvironmentError`, `UnsupportedStateDependentDepositAccounting`, `EraInvalidCertificateError`. **B4:** `ConwayCertAction`, `GovernanceOwner`, `GovernanceCertEffect`, `OwnerTaggedEffect`, `ConwayCertOutcome`, `ConwayCertEnv`. **B5:** `GovCertEnv`. **PHASE4-N-E:** `IngressSource`, `IngressEvent`, `PeerId`, `PeerSubmissionQueue`. **PHASE4-N-C:** `ProducerTick`, `ForgedBlock`, `ForgeError`, `ForgeEffects`, `AcceptedBlock`, `SelfAcceptError`. Functions: `producer::forge::forge_block`, `producer::self_accept::self_accept`, `block_body_hash::{block_body_hash, block_body_hash_from_buckets}`. **NEW — PHASE4-N-G:** `ServedChainSnapshot { blocks: BTreeMap<(SlotNo, Hash32), AcceptedBlock> }` (private `blocks` field — no struct-literal back-door); `ServedChainAdmitError` (closed sum: `Decode(BlockValidityError)`, `KeyByteConflict { slot, hash }`); function `served_chain_admit(snap, accepted) -> Result<ServedChainSnapshot, ServedChainAdmitError>`; accessor `accepted_block_header_bytes(accepted) -> Result<&[u8], BlockValidityError>` on `block_validity::header_input`. |
| **Interprets** | Decoded canonical types from `ade_codec`. Hosts per-era validation composers and the late-era witness decoder. B1: `block_validity::header_input::decode_block`. B2: `tx_validity::phase1::decode_tx`. B3: `conway::check_conway_coin_conservation`. B4: `rules::accumulate_tx_certs`. B5: feeds owner-complete `ConwayCert` into `gov_cert::apply_conway_gov_cert`. PHASE4-N-E S1: `mempool_ingress` is a pure pass-through. PHASE4-N-C: `producer::forge::forge_block` interprets the canonical `ProducerTick` only; `producer::self_accept::self_accept` interprets the forged byte slice exactly once by handing it to `block_validity::transition::block_validity`. **NEW — PHASE4-N-G:** `producer::served_chain::served_chain_admit` interprets the `AcceptedBlock` bytes exactly once via `decode_block` to derive the `(slot, hash)` index key — no caller-supplied "asserted hash" can mismatch the bytes (the admit is single-argument by design). `block_validity::header_input::accepted_block_header_bytes` interprets the AcceptedBlock envelope to produce the canonical header sub-slice via the existing `decode_block_envelope` + `header_cbor_slice` walker — single authority for the producer-side server pump's `RollForward.header` projection. |
| **MUST NOT** | (1) Perform I/O. (2) Use `HashMap`, floats, or any other BLUE-forbidden pattern. (3) Hash anything other than wire bytes. (4) Construct `PreservedCbor` outside named decoders. (5) Depend on `ade_runtime`. (6) Use signing primitives or `#[cfg(feature)]` gates. (7) Omit `// Core Contract:` banner. (8–30) B1/B2/B3/B4/B5/N-E/PP-S1 carry-forward (see prior CODEMAP for detail). (31) `producer::forge::forge_block` (DC-CONS-13/14/15 + DC-LEDGER-12) — pure function of `&ProducerTick`. (32) `producer::self_accept` (CN-CONS-07) — type-level broadcast gate; `AcceptedBlock` constructor private to `self_accept.rs`. (33) `producer::state::ProducerTick` (DC-CONS-13/14) — closed struct; no private-key fields. (34) `block_body_hash` (DC-CONS-16) — single body-hash authority workspace-wide. **(35) NEW — `producer::served_chain` (CN-CONS-07 strengthening, S2):** must NOT use `HashMap`/`HashSet`/`std::collections::Hash*` — `BTreeMap` is the only iteration order (enforced by `ci/ci_check_served_chain_closure.sh`). `ServedChainSnapshot.blocks` MUST remain a private field — no struct-literal construction outside `served_chain.rs` (same script). `served_chain_admit` MUST remain single-argument — no caller-supplied "asserted hash" parameter exists; the `(slot, hash)` key is derived inside the function from the AcceptedBlock bytes via `decode_block` (same script). `ServedChainAdmitError` MUST remain closed (no `#[non_exhaustive]`, no `String`-bearing variant). The only construction path for entries in `blocks` is `served_chain_admit(snap, accepted)`; the only public field on the snapshot is none — all access goes through accessors that yield `AcceptedBlock` references or `&[u8]` slices of `AcceptedBlock.as_bytes()`. **(36) NEW — `block_validity::header_input::accepted_block_header_bytes` (DC-CONS-16 strengthening + DC-CONS-18, S1):** must remain the **single** `pub fn .*header_bytes` site in the workspace — no parallel splitter (enforced by `ci/ci_check_no_parallel_header_splitter.sh`; the script's allowlist contains exactly one path). Must reuse the existing `header_cbor_slice` walker — no re-implementation of the inner header sub-slice walker. Must return a slice that aliases the input `accepted.as_bytes()` (positional projection via slice arithmetic). On malformed envelope must surface a typed `BlockValidityError::Codec(_)` — no panic. |
| **Inbound deps** | `ade_testkit`, `ade_core_interop`, `ade_runtime`. **NEW heavy consumption sites in PHASE4-N-G** — `ade_runtime::producer::{broadcast_to_served, served_chain_lookups}` consume `ade_ledger::producer::{ServedChainSnapshot, ServedChainAdmitError, served_chain_admit, AcceptedBlock}` + `ade_ledger::block_validity::accepted_block_header_bytes`. `ade_network::{chain_sync, block_fetch}::server`'s **test modules** now consume `ade_ledger::producer::ServedChainSnapshot` + `ade_ledger::producer::self_accept` (corpus-backed reducer tests) — this is the new dev-dep edge `ade_network → ade_ledger`. `ade_runtime::network::n2n_server` (RED) consumes `ade_ledger::producer::ServedChainSnapshot` (read-only reference). |
| **Outbound deps** | `ade_types`, `ade_crypto`, `ade_codec`, `ade_plutus`, `ade_core`, `minicbor`, `num-bigint`, `num-integer`, `num-traits`. Dev-dep: `ade_testkit`. No new outbound crate edge in PHASE4-N-G — the new `served_chain` module reuses the established `ade_ledger → {ade_crypto, ade_codec, ade_types}` edges (uses `ade_crypto::blake2b::blake2b_256` for fingerprint, `ade_codec::cbor::envelope::decode_block_envelope` indirectly via `decode_block`, and `ade_types::{Hash32, SlotNo}`). |
| **Entry points** | Carry-forward: `ade_ledger::rules::apply_block` (8 import sites), `apply_block_classified`, `apply_block_with_accounting`, `apply_block_with_verdicts`, `apply_conway_tx_to_utxo` (B2), `apply_epoch_boundary_full`, `ade_ledger::state::{LedgerState, EpochState}`, `ade_ledger::pparams::*`, `ade_ledger::utxo::UTxOState`, `ade_ledger::hfc::translate_era`, `ade_ledger::fingerprint::fingerprint`, `ade_ledger::governance::*`, `ade_ledger::delegation::*`, `ade_ledger::epoch::*`, `ade_ledger::witness::*`. B1: `ade_ledger::block_validity::*`, `ade_ledger::consensus_view::{PoolDistrView, PoolEntry}`. B2: `ade_ledger::tx_validity::{...}`, `ade_ledger::mempool::{admit, MempoolState, AdmitOutcome, ...}`. PHASE4-N-E: `ade_ledger::mempool::{mempool_ingress, IngressEvent, IngressSource, canonicalize_peer_streams, PeerId, PeerSubmissionQueue}`. PHASE4-N-C: `ade_ledger::producer::{forge::{forge_block, ForgeError, ForgeEffects, ForgedBlock}, self_accept::{self_accept, AcceptedBlock, SelfAcceptError}, state::ProducerTick}`; `ade_ledger::block_body_hash::{block_body_hash, block_body_hash_from_buckets}`. **NEW — PHASE4-N-G:** `ade_ledger::producer::{served_chain::{ServedChainSnapshot, ServedChainAdmitError, served_chain_admit}}` (re-exported at `ade_ledger::producer::*`); `ade_ledger::block_validity::accepted_block_header_bytes`. Both surfaces are consumed by `ade_runtime::producer::{broadcast_to_served, served_chain_lookups}` (the GREEN adapter pair) and by `ade_runtime::network::n2n_server` (read-only). |
| **Key modules** | `byron.rs`, `shelley.rs`, `mary.rs`, `alonzo.rs`, `babbage.rs`, `conway.rs`, `rules.rs`, `state.rs`, `utxo.rs`, `delegation.rs`, `epoch.rs`, `governance.rs`, `hfc.rs`, `late_era_validation.rs`, `phase.rs`, `plutus_eval.rs`, `pparams.rs`, `rational.rs`, `scripts.rs`, `value.rs`, `witness.rs`, `fingerprint.rs`, `error.rs`, `gov_cert.rs` (B5), `consensus_view.rs` (B1), `block_validity/` (B1; **N-G addition: `header_input.rs` gained `pub fn accepted_block_header_bytes`** + 2 inline tests for the projection), `consensus_input_extract.rs` (B1, **RED**), `tx_validity/` (B2), `mempool/` (B2 + N-E), `cert_classify.rs` (B3), `block_body_hash.rs` (N-C S4), `producer/` (N-C S3/S5 + **NEW — N-G S2**): `producer/mod.rs` (24 LOC barrel; re-exports `served_chain::{served_chain_admit, ServedChainAdmitError, ServedChainSnapshot}` alongside the N-C re-exports), `producer/state.rs`, `producer/forge.rs`, `producer/self_accept.rs`, **NEW — `producer/served_chain.rs`** (PHASE4-N-G S2; 171 LOC + 8 inline tests — `ServedChainSnapshot`, `served_chain_admit`, `ServedChainAdmitError`, `block_at`, `block_bytes`, `range_bytes`, `iter`, `iter_accepted`, `fingerprint`). |

> **GREEN sub-classification (`ade_ledger::mempool::policy`, `ade_ledger::mempool::canonicalize`).** Carry-forward.

> **RED sub-classification (`ade_ledger::consensus_input_extract`).** Carry-forward.

> **NEW — `ade_ledger::producer::served_chain` BLUE classification rests on three layers:** (a) the crate prefix is BLUE under `.idd-config.json` `core_paths`; (b) the file begins with the `// Core Contract:` banner (enforced by `ci_check_module_headers.sh`); (c) the narrow CI gate `ci_check_served_chain_closure.sh` pins the BLUE shape mechanically (BTreeMap-only iteration; admit single-argument; `blocks` field private). The new `accepted_block_header_bytes` accessor on `block_validity::header_input` is additionally guarded repo-wide by `ci_check_no_parallel_header_splitter.sh` — the only sanctioned site for *any* `pub fn .*header_bytes` symbol in the workspace.

> **All prior closure narratives unchanged at HEAD.** B-track + N-E + PROPOSAL-PROCEDURES-DECODE + N-C all carry forward.

---

### `ade_network` *(BLUE submodules)*

> **Status change at HEAD `a280954` (PHASE4-N-G S1 + S3 + S4 — two new BLUE server-role sub-modules).** Between `694dd74` and HEAD, the BLUE half of `ade_network` gained `chain_sync/server.rs` (S1, S3 — `ServerReply` closed wrapper + `ProducerChainSyncServerState` + `producer_chain_sync_serve` + `producer_chain_sync_advance_tip` + `ServedHeaderLookup` trait + `HeaderProjection`) and `block_fetch/server.rs` (S1, S4 — `ServerReply` closed wrapper + `ProducerBlockFetchServerState` + `producer_block_fetch_serve` + `ServedRangeLookup` trait). Both `server.rs` files compose the existing PHASE4-N-A `*_transition` state machines (unchanged) for grammar validation; they do not re-implement the state graph. The two `ServerReply` wrappers carry a **private inner enum** containing only Server-agency-legal variants — compile-time impossibility of constructing a client-agency reply (`CN-PROTO-06`). PHASE4-N-G did not touch any of the 11 pre-existing protocol codec / transition files. All prior PHASE4-N-A narrative carries forward.

| Attribute | Value |
|-----------|-------|
| **Purpose** | Owns the Cardano Ouroboros mini-protocol authority — the closed wire grammar (CBOR codecs) and pure state machines for all 11 N2N + N2C mini-protocols, plus the BLUE Ouroboros mux frame primitive. **NEW — PHASE4-N-G:** also owns the producer-side server-role reducer surface (`chain_sync::server` + `block_fetch::server`) — pure, total, deterministic transitions composing the existing BLUE state machines for grammar validation with the producer-side decision logic, and the closed `ServerReply<M>` wrappers that make client-agency replies from the server pump unrepresentable. |
| **Creates** | **Wire grammar (`codec/`):** `HandshakeMessage`, `N2cHandshakeMessage`, `ChainSyncMessage`, `BlockFetchMessage`, `TxSubmission2Message`, `KeepAliveMessage`, `PeerSharingMessage`, `LocalChainSyncMessage`, `LocalTxSubmissionMessage`, `LocalStateQueryMessage`, `LocalTxMonitorMessage`; closed payload types; structured `CodecError` + `ProtocolKind`. **State machines:** one closed `*State` / `*Agency` / `*Output` / `*Error` quad per protocol. **Mux primitives (`mux/frame.rs`):** `MuxFrame`, `MuxHeader`, `MuxMode`, `MiniProtocolId`, `MuxError`. **NEW — PHASE4-N-G (`chain_sync/server.rs`):** `ServerReply(ServerVariant)` (closed wrapper; private inner enum carries only `RollForward`, `RollBackward`, `AwaitReply`, `IntersectFound`, `IntersectNotFound` — server-agency-legal only), `HeaderProjection { slot, hash, block_no, header_bytes }`, `ProducerChainSyncServerState { state, last_announced }`, `ProducerServerError::Grammar(ChainSyncError)`, `ServerStep::{Done, Reply(ServerReply)}`, trait `ServedHeaderLookup { fn next_after, fn intersect, fn tip }`. Functions: `producer_chain_sync_serve(state, in_msg, &dyn ServedHeaderLookup, version) -> Result<(ProducerChainSyncServerState, ServerStep), ProducerServerError>`, `producer_chain_sync_advance_tip(state, &dyn ServedHeaderLookup) -> Result<(ProducerChainSyncServerState, Option<ServerReply>), ProducerServerError>`. **NEW — PHASE4-N-G (`block_fetch/server.rs`):** `ServerReply(ServerVariant)` (closed wrapper; private inner enum carries only `StartBatch`, `NoBlocks`, `Block { bytes }`, `BatchDone`), `ProducerBlockFetchServerState { state }`, `ProducerBlockFetchServerError::Grammar(BlockFetchError)`, `BlockFetchServerStep::{Done, Replies(Vec<ServerReply>)}`, trait `ServedRangeLookup { fn range_bytes }`. Function: `producer_block_fetch_serve(state, in_msg, &dyn ServedRangeLookup, version) -> Result<(ProducerBlockFetchServerState, BlockFetchServerStep), ProducerBlockFetchServerError>`. |
| **Interprets** | The on-wire CBOR of every Ouroboros mini-protocol message (closed grammar) and the 8-byte Ouroboros mux frame header. Block/header/tx-body bytes inside RollForward / Block / Tx are opaque pass-through (DC-PROTO-06); LSQ Query/Result payloads also opaque. **NEW — PHASE4-N-G:** the server reducers interpret already-decoded `ChainSyncMessage` / `BlockFetchMessage` values (the dispatcher in `ade_runtime::network::n2n_server` does the decode); they emit `ServerReply` values that the dispatcher subsequently encodes via the existing per-protocol `encode_*_message`. The header byte slice carried by `ServerReply::RollForward { header, .. }` is **not interpreted here** — it is sourced verbatim from `ServedHeaderLookup::next_after`'s `header_bytes`, which the GREEN adapter populates from the canonical `accepted_block_header_bytes` (DC-CONS-18 single authority). The block byte slice carried by `ServerReply::Block { bytes }` is similarly verbatim from `ServedRangeLookup::range_bytes`. |
| **MUST NOT** | (1) `async fn`, `.await`, `tokio`, `async_std`, `futures::`, async-runtime spawn, or async timers in any file under the 9 BLUE paths. (2) Decode block / header / tx-body bytes. (3) Construct a `PreservedCbor` outside `ade_codec`'s named chokepoints. (4) Add `#[non_exhaustive]` to any wire-message enum or introduce a generic `Codec<P>` trait. (5) Define a generic `Agency<P>` wrapper. (6) Read a selected version from ambient session state. (7) Redefine `TxId` / `SlotNo` / `Hash32` / `CardanoEra`. (8) Depend on `pallas-network`. (9) Hold `String` in any error variant. (10) Use any other BLUE forbidden pattern. **(11) NEW — `chain_sync::server` (DC-PROTO-08 + CN-PROTO-06, S1/S3):** must NOT import wall-clock (`SystemTime`/`Instant`), randomness, async runtime, or `HashMap`/`HashSet` (`ci/ci_check_chain_sync_server_closure.sh`). Must NOT have any `pub fn` returning a raw `ChainSyncMessage` — outgoing replies go via `ServerReply::into_message()` (same script). The `ServerReply` inner enum field must remain private — no public constructor exists for the client-agency variants `RequestNext` / `FindIntersect` / `Done` (compile-time `CN-PROTO-06`). `producer_chain_sync_serve` must remain **total** over (server_state, in_msg) — every pair returns `Ok` with a `ServerStep` or an `Err(ProducerServerError::Grammar(_))`; no panic, no `unimplemented!`, no silent wait (`DC-PROTO-08`). The reducer must NOT silently return `Ok((MustReply, AwaitReply))` followed by an unbounded suspend — the `MustReply` parking is paired with `producer_chain_sync_advance_tip` polled by the orchestrator after each broadcast admission. **(12) NEW — `block_fetch::server` (DC-CONS-17 + CN-PROTO-06, S1/S4):** same shape rules as `chain_sync::server` (`ci/ci_check_block_fetch_server_closure.sh`). The `ServerReply::block(bytes)` constructor's argument must be sourced from the `ServedRangeLookup::range_bytes` iterator — every byte the server emits as `Block { bytes }` traces to `AcceptedBlock.as_bytes()` (DC-CONS-17; mechanical positive grep). The `ServerReply` inner enum must remain private — no public constructor for the client-agency variants `RequestRange` / `ClientDone`. `producer_block_fetch_serve` must remain **total** and deterministic; the multi-reply non-empty-range case returns `[StartBatch, Block(_)*, BatchDone]` (curated sequence shape, test-pinned). Origin-endpoint ranges return `[NoBlocks]` (narrow-scope OQ-1 lock). **(13) NEW — both `server.rs` files (CN-CONS-07 strengthening, S1):** the `RollForward.header` and `Block { bytes }` byte slices must be sourced **only** through the trait-bound seam — no `decode_*_block` call inside production code in either server file; the byte authority for both surfaces is the `ServedChainSnapshot` accessed via `ServedHeaderLookup` / `ServedRangeLookup`. |
| **Inbound deps** | `ade_core_interop` (live consensus session binary, N-E S4/S5 bridges, S6 `live_tx_submission_session` binary, N-C S7 `live_block_production_session` binary, **NEW — N-G S7 `live_block_fetch_session` binary — all RED → BLUE**). **NEW — non-dev edge: `ade_runtime → ade_network`** introduced by PHASE4-N-G S5/S6 — `ade_runtime::network::n2n_server` and `ade_runtime::producer::served_chain_lookups` consume the BLUE server-role reducer types and traits. Legal RED → BLUE direction; `ci/ci_check_dependency_boundary.sh` forbids BLUE → RED, not RED → BLUE. **NEW — dev-deps: `ade_network → {ade_ledger, ade_testkit, ade_crypto, ade_core}`** — the corpus-backed reducer tests inside `chain_sync/server.rs` and `block_fetch/server.rs` build `ServedChainSnapshot` over the Conway-576 corpus via `self_accept`, so the test modules need the full BLUE stack as `[dev-dependencies]`. |
| **Outbound deps** | `ade_types`, `ade_codec`. No external deps in the BLUE submodules. The two new `server.rs` files add no outbound crate edges; their imports stay within the crate (`crate::chain_sync::{agency, state, transition}`, `crate::codec::*`) and `ade_types`. |
| **Entry points** | Codec entry points per protocol; `ade_network::codec::primitives::*`; `ade_network::codec::version::*`; `ade_network::codec::{CodecError, ProtocolKind}`. State-machine `*_transition` per protocol. Mux: `ade_network::mux::frame::{encode_frame, decode_frame, ...}`. **NEW — PHASE4-N-G:** `ade_network::chain_sync::server::{ServerReply, HeaderProjection, ProducerChainSyncServerState, ProducerServerError, ServerStep, ServedHeaderLookup, producer_chain_sync_serve, producer_chain_sync_advance_tip}`; `ade_network::block_fetch::server::{ServerReply, ProducerBlockFetchServerState, ProducerBlockFetchServerError, BlockFetchServerStep, ServedRangeLookup, producer_block_fetch_serve}`. Consumed by `ade_runtime::network::n2n_server` (via the `ServedChainLookups` adapter) and by the integration tests under `ade_runtime/tests/`. |
| **Key modules** | `codec/` (11 protocols + `primitives.rs` + `version.rs` + `error.rs`); `handshake/`, `chain_sync/` (NEW — `server.rs`, 804 LOC + large inline test suite), `block_fetch/` (NEW — `server.rs`, 596 LOC + large inline test suite), `tx_submission/`, `keep_alive/`, `peer_sharing/`; `n2c/`; `mux/frame.rs`. |

---

### `ade_plutus`

> **Status carried from prior CODEMAP — unchanged at HEAD `a280954`.** PHASE4-N-G did not touch `ade_plutus`.

| Attribute | Value |
|-----------|-------|
| **Purpose** | Quarantine boundary between the Ade-canonical ledger and the ported UPLC evaluator from `aiken-lang/aiken` (pinned to tag `v1.1.21`). Exposes Ade-canonical types only; aiken- and pallas-originated types are strictly internal. |
| **Creates** | `PlutusScript`, `PlutusLanguage`, `EvalOutput`, `PlutusError`, `CostModels`, `DecoderMode`, `PerScriptResult`, `TxEvalResult`. |
| **Interprets** | UPLC scripts (Plutus V1/V2/V3) and `CostModels` CBOR. Phase-two transaction evaluation. `PlutusScript::from_cbor` is a named ingress chokepoint. |
| **MUST NOT** | (1) Re-export any `pallas_*` or `aiken_uplc::` type. (2) Allow another BLUE crate to bypass the canonical entry. (3) Activate PV11 builtins. (4) Use any BLUE-forbidden pattern. (5) Construct `PreservedCbor` outside `ade_codec`. |
| **Inbound deps** | `ade_ledger`, `ade_testkit`. |
| **Outbound deps** | `ade_types`, `ade_crypto`, `ade_codec`, `aiken_uplc` (git, tag `v1.1.21`), `pallas-primitives` (internal-only). |
| **Entry points** | `ade_plutus::eval_tx_phase_two`, `ade_plutus::tx_eval::*`, `ade_plutus::evaluator::*`, `ade_plutus::cost_model::*`. |
| **Key modules** | `evaluator.rs`, `cost_model.rs`, `script_context.rs`, `script_verdict.rs`, `tx_eval.rs`. |

---

### `ade_types`

> **Status carried from prior CODEMAP — unchanged at HEAD `a280954`.** PHASE4-N-G did not add any canonical type to `ade_types` — the new closed canonical types all live in their authoritative crates (`ServedChainSnapshot`/`ServedChainAdmitError` in `ade_ledger`; chain-sync/block-fetch server-role types in `ade_network`'s respective BLUE submodules). `ade_types::shelley::block::*` (carrying `OperationalCert` + `ShelleyHeader` + `ShelleyBlock`) is consumed unchanged by the N-G integration tests for the corpus-backed `self_accept` → `served_chain_admit` setup.

| Attribute | Value |
|-----------|-------|
| **Purpose** | Owns the canonical Cardano domain vocabulary — primitives, era enum, address forms, era-tagged transaction bodies / outputs / certificates, governance types — used by every other workspace crate as the lingua franca. The schema, separated from the codec. |
| **Creates** | `CardanoEra`, `SlotNo`, `BlockNo`, `EpochNo`, `Hash28`, `Hash32`, `Coin`, `Lovelace`, `NetworkId`, `Nonce`, `TxIn`, `RewardAccount`, `PoolId`, `Address`, `ByronAddress`, `Credential`, `StakeCredential`, `Certificate`, `PoolRegistrationCert`, `ConwayCert`, `CertDisposition` / `DepositEffect` / `CoinSource`, `MIRCert`, `MIRPot`, `DRep`, `GovAction`, `GovActionState`, `GovActionId`, `Anchor`, `ProposalProcedure`, `OperationalCert`, `NativeScript`, `PlutusV1Script`, `Datum`, `DatumOption`, `MultiAsset`, `AssetName`, `CostModel`, `ExUnits`, plus per-era tx-body / tx-out / witness wrappers. |
| **Interprets** | None — produce-only. Domain types are constructed by `ade_codec` decoders or `ade_ledger` rules. |
| **MUST NOT** | (1) Construct or decode `PreservedCbor`. (2) Use any BLUE-forbidden pattern. (3) Depend on any workspace crate. (4) Add open/extensible variants to closed enums without a versioned gate. |
| **Inbound deps** | `ade_codec`, `ade_crypto`, `ade_ledger`, `ade_plutus`, `ade_runtime`, `ade_testkit`, `ade_network`, `ade_core`, `ade_core_interop`. |
| **Outbound deps** | None. |
| **Entry points** | `ade_types::CardanoEra`, `ade_types::tx::{Coin, TxIn, RewardAccount}`, `ade_types::{Hash32, SlotNo, Hash28}`, `ade_types::conway::tx::ConwayTxBody`, `ade_types::conway::cert::*`, `ade_types::conway::governance::{ProposalProcedure, Anchor, GovAction, GovActionId}`, `ade_types::shelley::block::{OperationalCert, ProtocolVersion, ShelleyHeader, ShelleyBlock, VrfData}`. |
| **Key modules** | `primitives.rs`, `era.rs`, `tx.rs`, `address/`, `byron/`, `shelley/`, `allegra/`, `mary/`, `alonzo/`, `babbage/`, `conway/`. |

---

## GREEN Modules — Deterministic Glue

> Deterministic, non-authoritative. May depend on BLUE; must not affect authoritative outputs.

### `ade_testkit`

> **Status carried from prior CODEMAP — unchanged at HEAD `a280954`.** PHASE4-N-G did NOT add the `server_paths/` sub-module the cluster doc named at planning time. The N-G GREEN test surface ended up under `ade_runtime/tests/*.rs` instead (the orphan-rule + dev-dep direction made `ade_runtime::tests/` the natural host since the integration tests exercise the full RED dispatcher → GREEN adapter → BLUE reducer pipeline). The pre-existing `ade_testkit::validity::ConwayValidityCorpus` is the corpus the new tests in `ade_network/src/{chain_sync,block_fetch}/server.rs` and in `ade_runtime/tests/cross_impl_server_pipeline.rs` build their `ServedChainSnapshot` over. All prior N-C / PP-S2 / N-E S2 / B-track narrative carries forward.

| Attribute | Value |
|-----------|-------|
| **Purpose** | Test infrastructure: differential harness, oracle snapshot loader, regression corpus, genesis loader, era mapping, transcript replay, diff reporting; N-B consensus harness; B1 block-validity harness (`validity/`); B2 transaction-validity harness (`tx_validity/`); N-E S2 mempool-ingress replay harness (`mempool/`); PP-S2 canonical synthetic `proposal_procedures` corpus + replay harness (`governance/`); PHASE4-N-C producer test harness (`producer/`). |
| **Creates** | Carry-forward: `Era`, `OracleManifest`, `LoadedSnapshot`, `RegressionCorpus`, `ComparisonSurface`, `DifferentialReport`, `DiffReport`, `Divergence`, `LedgerDiffReport`, `ProtocolDiffReport`, `BlockFields`, `ExpectedVerdict`, `HarnessError`, `Transcript`, `StateHash`, `SnapshotHeader`. **Consensus (N-B):** `LedgerViewStub`, `EpochStakeFixture`, `PoolFixture`, `ReplayStep`, `ReplayResult`. **Validity (B1):** `ConwayValidityCorpus` + `CorpusLoadError` (now also the corpus the N-G integration tests build their `ServedChainSnapshot` from). **B2 (`tx_validity/`):** `ExtractedTx`, `TxReplay`, `ValidCase`, `WitnessMutation`. **N-E S2 (`mempool/`):** `BTrackCase`, `ExpectedOutcome`. **PP-S2 (`governance/`):** `CorpusEntry`, `ReplayOutcome`. **PHASE4-N-C (`producer/`):** `VrfReferenceVector`, `KesReferenceVector`, `ProducerReplayFixture`, `ExpectedErr`. |
| **Interprets** | Oracle reference snapshots, the regression-corpus manifest, the N-B consensus corpus, the B1 validity corpus, on-wire Conway transactions inside committed Conway-576 corpus blocks. The PHASE4-N-C `producer/` sub-tree interprets nothing at the wire layer — fixtures are baked-in signed-artifact constants. |
| **MUST NOT** | (1) Affect authoritative outputs. (2) Be linked from any BLUE crate. (3) Introduce nondeterminism that leaks into stored fixtures. (4–16) Carry-forward (see prior CODEMAP). |
| **Inbound deps** | None at compile time; consumption is via integration tests and dev-dep links from `ade_core`, `ade_runtime`, `ade_ledger`. `ade_core_interop` is a non-dev consumer. **NEW — dev-dep edge: `ade_network → ade_testkit`** introduced by PHASE4-N-G — the corpus-backed reducer tests inside `chain_sync/server.rs` and `block_fetch/server.rs` import `ade_testkit::validity::ConwayValidityCorpus`. |
| **Outbound deps** | `ade_types`, `ade_codec`, `ade_crypto`, `ade_core`, `ade_ledger`, `ade_plutus`, `ade_runtime`, `blake2`, `flate2`, `tar`, `serde`, `serde_json`, `toml`, `cardano-crypto` (dev-dep, N-C). |
| **Entry points** | `ade_testkit::harness::*`; N-B `consensus::*`; B1 `validity::*` (incl. `validity::ConwayValidityCorpus::load` — now N-G's primary corpus source); B2 `tx_validity::*`. N-E S2: `ade_testkit::mempool::*`. PP-S2: `ade_testkit::governance::*`. PHASE4-N-C: `ade_testkit::producer::*`. |
| **Key modules** | `harness/`, `consensus/`, `validity/`, `tx_validity/`, `mempool/`, `governance/`, `producer/` (N-C). **No new module added by N-G** — the `server_paths/` sub-tree named in the cluster planning doc was not materialized; the equivalent integration tests live at `crates/ade_runtime/tests/{cross_impl_server_pipeline,server_paths_transcript_replay,n2n_server_two_peer_determinism}.rs`. |

> **Classification note carried forward.** `ade_testkit` reads files from disk in test helpers and drives BLUE authorities from corpus inputs.

> **Gap surfaced (NEW — PHASE4-N-G).** The cluster plan named a `ade_testkit::server_paths::{fixtures, replay, cross_impl_adapter}` sub-tree; the shipped artifact lives one layer over, under `ade_runtime/tests/`. The CI gate `ci/ci_check_server_paths_corpus_present.sh` was updated to check the actual shipped locations. The deviation is documented here so future work does not re-introduce a duplicate harness under `ade_testkit`. Whether `ade_testkit::server_paths::*` should be added as a thin re-export shim for cross-crate consumption (e.g. a future `ade_node` integration test) is a follow-on judgment call — at HEAD there is no consumer that would benefit.

---

### `ade_runtime::producer::{broadcast_to_served, served_chain_lookups}` *(GREEN — PHASE4-N-G S5)*

> **Status: NEW at HEAD `a280954`.** This is a GREEN sub-tree physically inside the RED `ade_runtime` crate prefix. The shared crate prefix is RED per `.idd-config.json` `_core_paths_doc`; the GREEN sub-classification of these two specific files is **by file** — `// Core Contract:` banner present, file-header docstring naming the GREEN classification, and CI gate `ci/ci_check_broadcast_to_served_purity.sh` pinning the pure-fn shape mechanically. Documented as a sub-section of `ade_runtime` in the RED block below; surfaced here for the GREEN inventory.

| Attribute | Value |
|-----------|-------|
| **Purpose** | Bridges PHASE4-N-C's `BroadcastQueue` (RED) and PHASE4-N-G's `ServedChainSnapshot` (BLUE) to the `ServedHeaderLookup` / `ServedRangeLookup` trait seams the BLUE server reducers in `ade_network::{chain_sync, block_fetch}::server` consume. Pure, no I/O, no clock — observably deterministic over captured arrival sequences. |
| **Creates** | `ServedChainLookups<'a> { snap: &'a ServedChainSnapshot }` (the trait-impls carrier; implements both `ServedHeaderLookup` and `ServedRangeLookup`). Function `drain_and_admit(snap, queue) -> Result<(ServedChainSnapshot, BroadcastQueue, Vec<AcceptedBlock>), ServedChainAdmitError>`. |
| **MUST NOT** | (1) Affect authoritative outputs — these are deterministic projections over already-canonical BLUE state. (2) Introduce nondeterminism — no `tokio`, no `SystemTime`/`Instant`, no `rand`, no `HashMap` (`ci/ci_check_broadcast_to_served_purity.sh`). (3) Project header bytes via any path other than the canonical `accepted_block_header_bytes` — positive grep in the same script for the import line. (4) Construct `ServedChainSnapshot` outside the `served_chain_admit` API surface (the GREEN adapter only consumes the snapshot or transforms it via `served_chain_admit`). (5) Cache or memoize across calls — `ServedChainLookups<'_>` is a transient view that the dispatcher builds per reducer call. |
| **Inbound deps** | `ade_runtime::network::n2n_server` (constructs a `ServedChainLookups` per dispatch call); the integration tests under `ade_runtime/tests/`. |
| **Outbound deps** | `ade_ledger::{producer::{ServedChainSnapshot, ServedChainAdmitError, served_chain_admit, AcceptedBlock}, block_validity::{accepted_block_header_bytes, decode_block}}`, `ade_network::{chain_sync::server::{HeaderProjection, ServedHeaderLookup}, block_fetch::server::ServedRangeLookup, codec::chain_sync::Point}`, `ade_types::{Hash32, SlotNo}`. **Crate-level edges already established** by S6's `ade_runtime → ade_network` addition and the pre-existing `ade_runtime → ade_ledger`. |
| **Entry points** | `ade_runtime::producer::broadcast_to_served::drain_and_admit`, `ade_runtime::producer::served_chain_lookups::ServedChainLookups`. |

> **GREEN/RED partition risk note.** The GREEN sub-classification of `broadcast_to_served` and `served_chain_lookups` rests on (a) module-header convention, (b) the pure-fn shape gate `ci_check_broadcast_to_served_purity.sh`, and (c) the positive-grep enforcement of the canonical `accepted_block_header_bytes` import. The broader BLUE/GREEN/RED partition inside the `ade_runtime` crate prefix remains "by file" — same risk class as `ade_runtime::consensus::candidate_fragment` (GREEN sub-classification, no dedicated CI script for the broad partition). This is a **carried gap** of the same shape.

---

## RED Modules — Imperative Shell

> I/O, network, storage, clocks, retries. May depend on BLUE/GREEN. Must not modify core state directly or construct semantic types unsafely.

### `ade_core_interop` *(PHASE4-N-B S-B10; PHASE4-N-E S4/S5/S6; PHASE4-N-C S7; **PHASE4-N-G S7**)*

> **Status change at HEAD `a280954` (PHASE4-N-G S7 — new RED operator-action live binary `live_block_fetch_session`).** Between `694dd74` and HEAD, `ade_core_interop` gained the new binary `src/bin/live_block_fetch_session.rs` (141 LOC) — the CE-N-G-8 evidence-capture pass. The binary is RED by its own header comment and matches the established `live_consensus_session.rs` + `live_tx_submission_session.rs` + `live_block_production_session.rs` pattern. Default hermetic `main` prints readiness + exits; `--connect` is the explicit operator-action flag. **Honest scope at HEAD:** the actual tokio socket wiring from `ade_runtime::network::n2n_server` to the wire is stubbed pending operator-action — the binary in `--connect` mode prints the wiring stub plan and exits without opening sockets. The `--connect` pass is therefore not yet a runnable evidence capture; the `blocked_until_operator_peer_available` flag on `RO-LIVE-01` reflects this. All prior N-B follow / N-E S4/S5/S6 / N-C S7 narrative carries forward.

| Attribute | Value |
|-----------|-------|
| **Purpose** | Live cardano-node interop driver. Hosts (a) the N-B consensus orchestrator readiness probe `fresh_orchestrator(k, initial_nonce)` and its `live_consensus_session` binary; (b) the N-E S4 GREEN adapter + `ingest_n2n_events`; (c) the N-E S5 GREEN adapter + `ingest_n2c_events`; (d) the N-E S6 `live_tx_submission_session` RED binary; (e) the N-C S7 `live_block_production_session` RED binary; **(f) NEW — the N-G S7 `live_block_fetch_session` RED binary that performs the CE-N-G-8 evidence-capture pass against a private cardano-node peer, threading the producer-side server pump's `dispatch_block_fetch_frame` over a captured `ServedChainSnapshot`**. The crate is RED per `Cargo.toml` and the cluster TCB Color Maps; the S4/S5 sub-modules carry a GREEN sub-classification by their own module doc-comments. The four `live_*_session` binaries are RED by their own header comments. |
| **Creates** | `fresh_orchestrator` (N-B). N-E S4 sub-module: `PeerAccumulator` (GREEN). N-E S5 sub-module: `ClientAccumulator` (GREEN). Functions: `event_to_ingress` + `ingest_n2n_events` (S4); `local_event_to_ingress` + `ingest_n2c_events` (S5). N-E S6, N-C S7, **NEW — N-G S7**: no public Rust types — all four binaries are RED glue over the established BLUE/GREEN/RED surfaces. |
| **Interprets** | Live N2N byte streams from a real cardano-node peer (N-B + N-E + N-C + **NEW — N-G**). S4/S5 sub-modules interpret nothing at the bytes level. **NEW — N-G S7 `live_block_fetch_session`:** loads pre-captured `AcceptedBlock` fixtures (the bytes that earlier passed `self_accept` in the N-C corpus), drives the `n2n_server` dispatch reducers against a synthetic peer protocol, logs one JSON-Lines record per RequestRange answered. Writes the log to `docs/clusters/PHASE4-N-G/CE-N-G-LIVE_<date>.log`. At HEAD the tokio socket wiring is stubbed (honest-scope) pending operator-action; the binary prints the wiring stub plan in `--connect` mode and exits. |
| **MUST NOT** | (1) Contain authoritative state-transition logic. (2) Be depended on by any other workspace crate. (3) Run by default in CI — closure-gate tests are `#[ignore]`-gated (CE-N-B-6, CE-N-E-6, CE-N-C-8, **NEW — CE-N-G-8**). (4) Bypass `ade_codec`'s `PreservedCbor` chokepoints. (5) Read versions out of ambient context (DC-PROTO-06). (6) Vendor cardano-node mainnet genesis blobs. (7) N-E S4/S5 — must not branch on `IngressSource`, must not re-encode `tx_bytes`, must not bypass `mempool_ingress`. (8) N-E S6 binary — no direct `admit(`, no shadowing of `IngressSource`/`IngressEvent`/`PeerId`, no parallel codecs/transitions. (9) N-C S7 binary — must NOT bypass `forge_block` / `self_accept`; must NOT load private-key material into any BLUE call path; must NOT generate keys (OP-OPS-04); must NOT write a synthetic `CE-N-C-LIVE_<date>.log`. **(10) NEW — N-G S7 `live_block_fetch_session` binary (RED, RO-LIVE-01):** must NOT serve bytes that were not produced by `self_accept` (CN-CONS-07 strengthening — the binary's fixture loader populates `ServedChainSnapshot` via `served_chain_admit(snap, accepted)`, and `accepted` is by construction the output of `self_accept`; no shadow constructor for `AcceptedBlock` exists). Must NOT re-encode block bytes — the served `Block { bytes }` payload is sourced verbatim from `ServedChainSnapshot::range_bytes` (DC-CONS-17). Must NOT project header bytes via any path other than `accepted_block_header_bytes` — DC-CONS-18 single authority (the GREEN adapter `ServedChainLookups` handles this). Must NOT generate or hold any signing-key material — the binary serves pre-captured bytes only (key-boundary doctrine; the N2N server pump itself is mechanically forbidden from importing `producer::signing` by `ci/ci_check_n2n_server_no_signing_dep.sh`). Must NOT write a `CE-N-G-LIVE_<date>.log` with synthetic / fabricated entries — the log MUST reflect an actual run against a private cardano-node peer. The default `main` MUST stay hermetic (prints readiness and exits); `--connect` is the explicit operator-action flag. The `#[ignore]`-gated build-and-start test stays offline. At HEAD the binary's `--connect` path is honest-scope stubbed (tokio socket wiring deferred to operator-action work); MUST NOT claim live evidence has been captured until the operator runs the binary and commits the resulting log. |
| **Inbound deps** | None. |
| **Outbound deps** | `ade_core`, `ade_codec`, `ade_crypto`, `ade_ledger`, `ade_runtime` (**NEW heavy use sites in PHASE4-N-G** — the S7 binary imports `ade_runtime::network::n2n_server::*` + `ade_runtime::producer::{served_chain_lookups, broadcast_to_served}`), `ade_network`, `ade_testkit`, `ade_types`, `tokio`. Total Cargo deps: 8 workspace crates + `tokio`. No new crate-level edges in N-G — the S7 binary reuses the already-established `ade_core_interop → ade_runtime` and `ade_core_interop → ade_network` edges. |
| **Entry points** | `ade_core_interop::fresh_orchestrator(k, initial_nonce)`, binary `live_consensus_session` (CE-N-B-6 evidence). N-E S4: `ade_core_interop::tx_submission::*`. N-E S5: `ade_core_interop::local_tx_submission::*`. N-E S6 binary: `live_tx_submission_session` (CE-N-E-6). N-C S7 binary: `live_block_production_session` (CE-N-C-8). **NEW — N-G S7 binary: `live_block_fetch_session` (CE-N-G-8 evidence — conditional on operator-provided private cardano-node peer; honest-scope stubbed at HEAD).** No new public Rust API; the binary entry point is `fn main()`. |
| **Key modules** | `src/lib.rs`, `src/follow.rs` (N-B), `src/tx_submission.rs` (N-E S4), `src/local_tx_submission.rs` (N-E S5), `src/bin/live_consensus_session.rs`, `src/bin/live_tx_submission_session.rs` (N-E S6), `src/bin/live_block_production_session.rs` (N-C S7), **NEW — `src/bin/live_block_fetch_session.rs`** (PHASE4-N-G S7; 141 LOC; default hermetic `main` + explicit `--connect` stubbed live pass). |

> **Gap surfaced (carried + extended).** `ade_core_interop` at HEAD is a *readiness probe + bridges + live evidence capture binaries*. CE-N-B-6 closes via manual evidence log against a real cardano-node peer; live capture pending operator run. CE-N-E-6 is **CLOSED at HEAD**. CE-N-E-7 (live N2C tx-submission evidence) is deferred as `CE-NODE-N2C-LTX`. CE-N-C-8 is at `status = "enforced"` with `open_obligation` on `CN-CONS-06`: `blocked_until_operator_stake_available`. **NEW — CE-N-G-8 (live block-fetch evidence, RO-LIVE-01) is at `status = "partial"` with `open_obligation = "blocked_until_operator_peer_available"`. The mechanical half is closed (`cross_impl_server_pipeline.rs` over the N-C corpus, transcript-replay over the synthetic session corpus, multi-peer determinism over two synthetic peers); the live half awaits a private cardano-node peer being provisioned per `docs/clusters/PHASE4-N-G/CE-N-G-8_PROCEDURE.md`. Additionally, at HEAD the binary's `--connect` mode is honest-scope stubbed (the tokio socket wiring from `n2n_server` to the wire is one layer up — deferred to operator-action work). When the operator wires the socket and runs the pass, the log path will be appended to `RO-LIVE-01.evidence_notes`.** The S7 binary's RED classification is review-and-doc enforced (matching the S6 + N-C-S7 precedent — no narrow CI gate scopes the partition, only the presence gate `ci_check_server_paths_corpus_present.sh`).

---

### `ade_network` *(RED submodules + capture binaries)*

> **Status carried from prior CODEMAP — unchanged at HEAD `a280954`.** The RED half of `ade_network`: `mux::transport`, `session`, and the 7 `src/bin/capture_*` binaries. PHASE4-N-G did not touch this half of the crate.

| Attribute | Value |
|-----------|-------|
| **Purpose** | The only place inside `ade_network` where I/O happens — tokio-based TCP / Unix-socket scaffolding (`mux::transport`), the session-composition placeholder (`session`), and capture binaries that perform real handshakes against pinned cardano-node 11.0.1 peers. |
| **Creates** | `MuxTransport`. `session/mod.rs` is an empty placeholder. The capture binaries produce no public Rust types. |
| **Interprets** | Live TCP / Unix-socket byte streams; bytes flow into the BLUE mux frame decoder and protocol codecs without re-interpretation. |
| **MUST NOT** | (1) Contain protocol logic. (2) Define or modify any closed wire-message enum, agency type, or state. (3) Read a selected protocol version from ambient state. (4) Replace BLUE codec / state machine with a "fast path". (5) Bypass `ade_codec`'s `PreservedCbor` chokepoints. (6) Be depended on by any BLUE submodule of `ade_network`. |
| **Inbound deps** | `ade_core_interop`. At the library level no BLUE crate imports the RED submodules. |
| **Outbound deps** | `tokio`, `ade_types`, `ade_codec`, plus BLUE `ade_network::codec::*` and `ade_network::mux::frame`. |
| **Entry points** | `ade_network::mux::transport::{MuxTransport, open_tcp}`. The capture binaries. |
| **Key modules** | `mux/transport.rs`, `session/mod.rs` (placeholder), `bin/capture_*`. |

> **Gap surfaced (carried + extended).** `ade_network::session::mod.rs` is still a placeholder. **NEW — PHASE4-N-G implicitly re-opens this gap from a new angle:** the N-G S7 binary's honest-scope stub of the tokio socket wiring sits at the boundary between `ade_runtime::network::n2n_server` (pure dispatch driver) and the absent session-composition layer. A future cluster — likely the same node-binary cluster that owns `CE-NODE-N2C-LTX` — should own (a) the `ade_network::session` driver, (b) the tokio glue threading `dispatch_chain_sync_frame` / `dispatch_block_fetch_frame` over a real socket, and (c) the producer-side relay-mesh behaviour that the N-G S7 binary currently stubs.

---

### `ade_node`

> **Status carried from prior CODEMAP — unchanged at HEAD `a280954`.** Still a hello-world stub. PHASE4-N-G did not touch `ade_node`; the producer-side server pump is reachable today through the `live_block_fetch_session` operator-action binary and the three new integration tests, not through any `ade_node` orchestration.

| Attribute | Value |
|-----------|-------|
| **Purpose** | Binary entry point for the node process. Reserved as the assembly point for runtime + ledger + chain DB + network + consensus orchestrator + mempool + the deferred N2C server + the producer-side N2N server (now sketched but not assembled into a node binary). |
| **Creates** | None. |
| **Interprets** | None yet. |
| **MUST NOT** | (1) Bypass `ade_codec` to construct semantic types from bytes. (2) Modify `ade_ledger` or `ade_core::consensus` state in place — transitions go through `ade_ledger::rules::*`, `ade_ledger::block_validity::*`, `ade_ledger::tx_validity::*`, `ade_ledger::mempool::admit`, `ade_ledger::mempool::mempool_ingress`, `ade_ledger::producer::forge::forge_block` + `ade_ledger::producer::self_accept::self_accept`, or `ade_core::consensus::*` BLUE transitions. (3) Take an inbound dep from any other crate. (4) Read versions out of ambient context. (5) Construct an `OrchestratorState` outside `ade_runtime::consensus::chain_selector` or `ade_core_interop::fresh_orchestrator`. (6) Forward-looking — must reach the mempool only through `mempool_ingress`. (7) Forward-looking `CE-NODE-N2C-LTX` — must drive the GREEN bridge, not introduce a parallel path. (8) PP-S1 forward-looking — typed `Vec<ProposalProcedure>` only. (9) Forward-looking PHASE4-N-C / CN-CONS-07 — drive `forge_block` → `self_accept`; never fabricate `AcceptedBlock`; never introduce a parallel body-hash recipe; never bypass `opcert_validate`; hold private-key material inside `ade_runtime::producer::{signing, keys}` only. **(10) NEW — Forward-looking PHASE4-N-G / CN-CONS-07 strengthening + DC-CONS-17/18 + CN-PROTO-06:** any `ade_node`-level wiring of the producer-side N2N server pump must drive `ade_runtime::network::n2n_server::dispatch_*_frame` over a `ServedChainSnapshot` whose entries arrived via `served_chain_admit` (the broadcast gate); must NOT construct `ServedChainSnapshot` outside the admit API; must NOT bypass the GREEN `ServedChainLookups` adapter to wire BLUE reducers directly against ad-hoc lookups; must NOT serve `Block { bytes }` that did not trace through `AcceptedBlock.as_bytes()`; must NOT project a `RollForward.header` via any path other than `accepted_block_header_bytes`; must NOT introduce a parallel `ServerReply` constructor for client-agency wire variants (the closed wrapper makes this a compile error); must NOT link `ade_runtime::network::*` against `ade_runtime::producer::signing` (CI gate `ci_check_n2n_server_no_signing_dep.sh`). The default `ade_node` binary MUST stay hermetic — producer mode requires operator-supplied `.skey` set + explicit opt-in (carry-forward from N-C); the producer-side N2N server pump similarly requires explicit opt-in. |
| **Inbound deps** | None. |
| **Outbound deps** | None at present. |
| **Entry points** | `main()` in `crates/ade_node/src/main.rs`. |

> **Gap surfaced (carried).** `ade_node` is still a hello-world stub; its MUST NOT list is forward-looking with nothing to enforce today.

---

### `ade_runtime`

> **Status change at HEAD `a280954` (PHASE4-N-G S5 + S6 — one new RED top-level module + two new GREEN sub-modules under `producer/`).** Between `694dd74` and HEAD, `ade_runtime` gained: (a) the new RED `network/` top-level module + `network/n2n_server.rs` driver (S6) hosting `PerPeerN2nServerState`, the three dispatch functions (`dispatch_chain_sync_frame`, `dispatch_block_fetch_frame`, `poll_chain_sync_advance`), and the closed `DispatchError` sum — pure state-machine driver with no socket I/O (the socket layer is one level up at the binary boundary); (b) two new GREEN files under `producer/` — `broadcast_to_served.rs` (S5; `drain_and_admit` adapter) and `served_chain_lookups.rs` (S5; `ServedChainLookups<'a>` carrying the orphan-rule-blocked trait impls of `ServedHeaderLookup` + `ServedRangeLookup` over a borrowed `ServedChainSnapshot`). All prior N-D / N-B / N-A / N-C narrative carries forward.

| Attribute | Value |
|-----------|-------|
| **Purpose** | The imperative shell. Hosts (a) the `ChainDb` / `SnapshotStore` storage abstractions (N-D); (b) the GREEN chain-selector orchestrator + GREEN `CandidateFragment` materializer (N-B); (c) the RED genesis-text parser (N-B); (d) the RED producer-side scaffolding (`producer/`): private-key custody (`signing.rs`), cardano-cli `.skey` text-envelope loader (`keys.rs`), the pure RED scheduler state-transition (`scheduler.rs`), the GREEN deterministic tick-assembler (`tick_assembler.rs`), and the type-level-gated FIFO broadcast queue (`broadcast.rs`) — N-C; **(e) NEW — the RED producer-side N2N server pump (`network/n2n_server.rs`, S6): pure dispatch driver decoding inbound mini-protocol frames, running the BLUE reducers through the GREEN `ServedChainLookups` adapter, and encoding outgoing frames; (f) NEW — the GREEN broadcast-to-served adapter (`producer/broadcast_to_served.rs`, S5) draining `BroadcastQueue` and admitting each `AcceptedBlock` into `ServedChainSnapshot`; (g) NEW — the GREEN `ServedChainLookups` trait carrier (`producer/served_chain_lookups.rs`, S5) wrapping a borrowed snapshot and implementing both `ServedHeaderLookup` and `ServedRangeLookup` over the canonical `accepted_block_header_bytes` projection**. The crate remains RED at the prefix level; `chain_selector.rs` / `candidate_fragment.rs` / `tick_assembler.rs` / **NEW — `broadcast_to_served.rs` / `served_chain_lookups.rs`** are GREEN by file-level classification; `genesis_parser.rs` / `signing.rs` / `keys.rs` / `scheduler.rs` / `broadcast.rs` / **NEW — `network/n2n_server.rs`** are RED. |
| **Creates** | Carry-forward: `ChainDb`, `SnapshotStore`, `KillStrategy<D>`, `ChainTip`, `StoredBlock`, `ChainDbError`, `InMemoryChainDb`, `PersistentChainDb`, `PersistentChainDbOptions`, `SyncCadence`, `NoKill`, `BlockIter<'a>`; `Recoverable`, `StartingState`, `RecoveryReport<R>`, `RecoveryError<E>`; `NetworkMagic`, `GenesisBlob`, `GenesisBundle<'a>`, `GenesisParseError`; `StreamInput`, `OrchestratorError`, `OrchestratorState`, `RollbackSnapshot`, `DEFAULT_SNAPSHOT_LIMIT`; `build_candidate_fragment`; PHASE4-N-C `producer/` types (`VrfSigningKey`, `KesSecret`, `ColdSigningKey`, `SigningError`, `KeyLoadError`, `TickInputs`, `TickAssemblyError`, `SchedulerInput`, `SchedulerEffect`, `SchedulerHaltReason`, `SchedulerState`, `BroadcastError`, `BroadcastQueue`); functions `signing::{vrf_prove, kes_sign, kes_update}`, `keys::{load_*_skey, *_TYPE constants}`, `tick_assembler::assemble_tick`, `scheduler::scheduler_step`, `broadcast::BroadcastQueue::*`. **NEW — PHASE4-N-G S5 (GREEN):** function `drain_and_admit(snap, queue) -> Result<(ServedChainSnapshot, BroadcastQueue, Vec<AcceptedBlock>), ServedChainAdmitError>` (in `producer::broadcast_to_served`); struct `ServedChainLookups<'a> { snap: &'a ServedChainSnapshot }` with both `impl ServedHeaderLookup for ServedChainLookups<'_>` and `impl ServedRangeLookup for ServedChainLookups<'_>` (in `producer::served_chain_lookups`). **NEW — PHASE4-N-G S6 (RED):** `PerPeerN2nServerState { chain_sync, block_fetch, chain_sync_version, block_fetch_version }`; closed `DispatchError` sum (`ChainSyncDecode(CodecError)`, `BlockFetchDecode(CodecError)`, `ChainSync(ProducerServerError)`, `BlockFetch(ProducerBlockFetchServerError)`); functions `dispatch_chain_sync_frame(state, &[u8], &ServedChainSnapshot) -> Result<(PerPeerN2nServerState, Option<Vec<u8>>, bool), DispatchError>`, `dispatch_block_fetch_frame(...) -> Result<(PerPeerN2nServerState, Vec<Vec<u8>>, bool), DispatchError>`, `poll_chain_sync_advance(state, &ServedChainSnapshot) -> Result<(PerPeerN2nServerState, Option<Vec<u8>>), DispatchError>`. |
| **Interprets** | Block bytes for content-addressed storage. Genesis JSON. N-A `StreamInput` events. PHASE4-N-C: cardano-cli `.skey` text envelopes; RED-supplied signed artifacts; RED scheduler input stream; `AcceptedBlock` values handed across the BLUE→RED boundary into `BroadcastQueue`. **NEW — PHASE4-N-G S5/S6:** `AcceptedBlock` values drained from `BroadcastQueue` and admitted into `ServedChainSnapshot` via the BLUE `served_chain_admit` (no I/O, no interpretation at the wire layer); inbound mini-protocol frame bytes decoded once via the BLUE per-protocol `decode_*_message` and handed to the BLUE reducer through the GREEN `ServedChainLookups` adapter — `ade_runtime::network::n2n_server` itself never interprets block / header / tx bytes (those stay opaque pass-through per DC-PROTO-06). |
| **MUST NOT** | (1) ~~Depend on `ade_ledger`.~~ Carry-forward exception: now-permitted by the established RED → BLUE direction. (2) Leak `redb` through the `chaindb::*` public surface. (3) Have a second public chaindb path. (4) Couple snapshots to a specific state type. (5) Apply automatic snapshot pruning. (6) Allow partial-recovery success. (7) Add async recovery surface. (8) Treat "not found" as an error path or silently retry on real errors. (9) Consensus sub-tree GREEN `chain_selector` must not affect authoritative outputs. (10) Genesis parser — structured errors only. (11) `candidate_fragment` materializer — pure constructor. (12–16) PHASE4-N-C `producer/{signing, keys, tick_assembler, scheduler, broadcast}` carry-forward MUST NOTs. **(17) NEW — `producer::broadcast_to_served` (CE-N-G-5 + DC-PROTO-07, S5, GREEN):** must NOT import `std::time` / `tokio` / `rand` / `HashMap` / async constructs (enforced by `ci/ci_check_broadcast_to_served_purity.sh`); the `drain_and_admit` signature must remain `fn(ServedChainSnapshot, BroadcastQueue) -> Result<(ServedChainSnapshot, BroadcastQueue, Vec<AcceptedBlock>), ServedChainAdmitError>` (no `tokio::Mutex` wrap, no callback, no `Future` return). The adapter MUST be deterministic over captured arrival sequences — identical `Vec<AcceptedBlock>` arrival orders MUST produce byte-identical resulting snapshots (test-pinned by `drain_and_admit_is_deterministic_over_arrival_sequence`). **(18) NEW — `producer::served_chain_lookups` (DC-CONS-18 + DC-CONS-17, S5, GREEN):** must source the projected header bytes via `accepted_block_header_bytes` exclusively — no parallel header sub-slice walker (enforced by the positive grep in `ci/ci_check_broadcast_to_served_purity.sh`). Must source the range bytes via `ServedChainSnapshot::range_bytes` exclusively — no parallel range iteration; the `ServedRangeLookup::range_bytes` impl is a thin clone-into-owned-Vec wrapper because `ServerReply::block(bytes)` takes ownership. `ServedChainLookups<'_>` must NOT cache or memoize — it is a transient view that the dispatcher builds per reducer call (the snapshot's lifetime outlives every reducer invocation). The impls must NOT panic on a malformed admitted block — the `decode_block` call in `next_after`/`tip` is `.expect(...)` on the admit invariant (the admit gate already proved decode-validity); if that invariant is violated the panic is the right behaviour (defensive — should not be reachable under the admit gate). **(19) NEW — `network::n2n_server` (S6, RED, key-boundary doctrine + DC-PROTO-06 + DC-PROTO-07 + DC-PROTO-08):** must NOT import from `crate::producer::signing` — direct or transitive (enforced by `ci/ci_check_n2n_server_no_signing_dep.sh`). The handshake-negotiated versions MUST be threaded as **explicit per-call inputs** through `dispatch_*_frame` — never read from a session-global (DC-PROTO-06; tested by `dispatch_chain_sync_frame_threads_negotiated_version_to_reducer`). The dispatcher MUST NOT contain authoritative state-transition logic — it is a pure state-machine driver composing BLUE decode → BLUE reducer → BLUE encode. Per-peer state MUST be independent (`PerPeerN2nServerState`); cross-peer coordination MUST go through the shared `&ServedChainSnapshot` reference only (OQ-4 multi-peer determinism; tested by `n2n_server_two_peer_determinism.rs`). The dispatcher MUST NOT open sockets or perform any I/O — the socket layer is one level up (the `live_block_fetch_session` binary and, in the future, the deferred `ade_node` orchestration). On undecodable input the dispatcher MUST return a typed `DispatchError::*Decode(_)` — no panic, no silent drop. On session-Done from the peer the dispatcher MUST return `(state, _, true)` — the boolean MUST reflect "client said Done"; the binary layer reads it to drive shutdown. |
| **Inbound deps** | `ade_testkit` (non-dev dep, used by `stream_replay`), `ade_core_interop` (heavy consumer at HEAD — all four `live_*_session` binaries import from `ade_runtime`). The binary `chaindb_kill_target` lives at `src/bin/`. **NEW heavy consumption sites in PHASE4-N-G** — `ade_core_interop::bin::live_block_fetch_session` imports the full `network/` + `producer/{broadcast_to_served, served_chain_lookups}` sub-trees. The integration tests under `crates/ade_runtime/tests/` (`cross_impl_server_pipeline.rs`, `server_paths_transcript_replay.rs`, `n2n_server_two_peer_determinism.rs`) consume the same surfaces; they sit inside the crate's `tests/` directory so they are dev-dep edges. |
| **Outbound deps** | `ade_types`, `ade_core`, `ade_crypto`, `ade_codec`, `ade_ledger`, `redb`, `serde_json`, `cardano-crypto`, `ed25519-dalek`. **NEW outbound crate edge in PHASE4-N-G — `ade_runtime → ade_network`** (S6 — `network/n2n_server.rs` imports `ade_network::{chain_sync::server::*, block_fetch::server::*, codec::{chain_sync::*, block_fetch::*, version::*}, codec::CodecError}`; S5 — `producer/served_chain_lookups.rs` imports the same trait surfaces). Legal RED → BLUE direction; conformant with `ci/ci_check_dependency_boundary.sh` which forbids BLUE → RED. Dev-deps: `tempfile`, `ade_testkit`. |
| **Entry points** | **Chaindb / recovery:** `ade_runtime::chaindb::*`, `ade_runtime::recovery::*`. **Consensus:** `ade_runtime::consensus::chain_selector::*`, `ade_runtime::consensus::candidate_fragment::*`, `ade_runtime::consensus::genesis_parser::*`. **PHASE4-N-C `producer/`:** `ade_runtime::producer::{signing::*, keys::*, tick_assembler::*, scheduler::*, broadcast::*}`. **NEW — PHASE4-N-G `producer/` (GREEN):** `ade_runtime::producer::broadcast_to_served::drain_and_admit`; `ade_runtime::producer::served_chain_lookups::ServedChainLookups`. **NEW — PHASE4-N-G `network/` (RED):** `ade_runtime::network::n2n_server::{PerPeerN2nServerState, DispatchError, dispatch_chain_sync_frame, dispatch_block_fetch_frame, poll_chain_sync_advance}`. |
| **Key modules** | **`chaindb/`**, **`recovery.rs`**, **`consensus/`** (`genesis_parser.rs` RED + `candidate_fragment.rs` GREEN + `chain_selector.rs` GREEN), **`bin/chaindb_kill_target.rs`**, **`producer/`** (PHASE4-N-C `signing.rs` RED, `keys.rs` RED, `tick_assembler.rs` GREEN, `scheduler.rs` RED, `broadcast.rs` RED; **NEW — PHASE4-N-G `broadcast_to_served.rs` GREEN** (188 LOC + 3 inline tests), **NEW — PHASE4-N-G `served_chain_lookups.rs` GREEN** (120 LOC + 3 inline tests)), **NEW — `network/`** (PHASE4-N-G S6): `network/mod.rs` (20 LOC barrel), `network/n2n_server.rs` (216 LOC + 5 inline tests, **RED**). **Integration tests under `tests/`:** carry-forward `producer_pipeline_slot_deadline.rs` (N-C), `genesis_parser_corpus.rs` (N-B), `stress_kill_harness.rs` (N-D); **NEW — PHASE4-N-G S5/S6/S7:** `cross_impl_server_pipeline.rs` (S7, mechanical adapter — CE-N-G-7, 2 named tests), `server_paths_transcript_replay.rs` (S5, synthetic session-transcript replay — 3 named tests proving DC-PROTO-07 + DC-CONS-17 + DC-CONS-18), `n2n_server_two_peer_determinism.rs` (S6, OQ-4 multi-peer determinism). |
| **Mechanical enforcement** | Carry-forward chaindb / recovery / crash-safety + 8 PHASE4-N-C CI scripts. **NEW — three PHASE4-N-G CI scripts back the producer-side server-pump surface inside `ade_runtime`:** `ci/ci_check_broadcast_to_served_purity.sh` (CE-N-G-5, scope `crates/ade_runtime/src/producer/{broadcast_to_served,served_chain_lookups}.rs`), `ci/ci_check_n2n_server_no_signing_dep.sh` (S6 key-boundary, scope `crates/ade_runtime/src/network/`), `ci/ci_check_server_paths_corpus_present.sh` (S7 presence, scope `crates/ade_runtime/tests/cross_impl_server_pipeline.rs` + `crates/ade_runtime/tests/server_paths_transcript_replay.rs` + `crates/ade_core_interop/src/bin/live_block_fetch_session.rs` + `docs/clusters/PHASE4-N-G/CE-N-G-8_PROCEDURE.md`). |

> **Gap surfaced (carried + extended).** No dedicated CI script enforces the GREEN/RED partition *inside* `ade_runtime::consensus`. **NEW — same gap class extends to `ade_runtime::producer::{broadcast_to_served, served_chain_lookups}`:** the GREEN sub-classification of these two files inside the RED `ade_runtime` crate prefix is partially gated by `ci/ci_check_broadcast_to_served_purity.sh` (pure-fn shape) but the broader BLUE/GREEN/RED partition is "by file" and enforced by review + header comments. **NEW — same gap class also extends to `ade_runtime::network::n2n_server`:** the RED classification is review-and-doc enforced (file-header banner) plus the narrow key-boundary CI gate; no script asserts the broader RED partition. **NEW — at HEAD the `live_block_fetch_session` binary's `--connect` mode is honest-scope stubbed** (the tokio socket wiring from `n2n_server` to the wire is one level up — deferred to operator-action work); the binary prints the wiring stub plan and exits without opening sockets. The `n2n_server` dispatch driver itself is pure and complete; what is owed is the socket layer + the live-evidence pass that traverses it. This re-opens the `ade_network::session` placeholder gap from a new angle: the deferred node-binary cluster that owns `CE-NODE-N2C-LTX` will likely own the socket layer for the producer-side N2N server pump too.

---

## Cross-Module Rules (project-wide)

### Dependency direction

`ade_core_interop` → `{ade_core, ade_codec, ade_crypto, ade_ledger, ade_runtime, ade_network, ade_testkit, ade_types, tokio}` is legal (RED leaf binary). **No new crate-level outbound edge in PHASE4-N-G** — the S7 binary reuses these.
`ade_runtime` → `{ade_core, ade_crypto, ade_codec, ade_types, ade_ledger, redb, serde_json, cardano-crypto, ed25519-dalek, **NEW — ade_network**}` is legal (RED → BLUE; the new `ade_runtime → ade_network` edge introduced by PHASE4-N-G is RED-shell consuming BLUE mini-protocol server reducer authorities and is mechanically conformant with `ci_check_dependency_boundary.sh`).
`ade_testkit` → `{ade_core, ade_ledger, ade_plutus, ade_runtime, ade_crypto, ade_codec, ade_types, cardano-crypto}` is legal (GREEN).
`ade_network` (BLUE submodules) → `{ade_codec, ade_types}` is legal. **NEW dev-deps:** `{ade_ledger, ade_testkit, ade_crypto, ade_core}` — dev-only edges supporting the corpus-backed reducer tests inside `chain_sync/server.rs` and `block_fetch/server.rs`. Dev-dep direction is BLUE-test-only-on-BLUE — legal.
`ade_network` (RED submodules + capture bins) → `{tokio, ade_codec, ade_types, ade_network::codec::*, ade_network::mux::frame}` is legal.
`ade_ledger` → `{ade_core, ade_plutus, ade_crypto, ade_codec, ade_types, minicbor}` is legal (BLUE among BLUEs). The new `served_chain` module reuses these edges — no new outbound from `ade_ledger`.
`ade_core` → `{ade_types, ade_crypto, minicbor}` is legal (BLUE among BLUEs).
`ade_plutus` → `{ade_crypto, ade_codec, ade_types}` is legal.
`ade_crypto` → `{ade_types}` is legal.
`ade_codec` → `{ade_types}` is legal.
`ade_types` → `{}`.

**Forbidden directions.** Any BLUE crate (or BLUE `ade_network` submodule) depending on `ade_runtime`, `ade_node`, `ade_core_interop`, or the RED half of `ade_network` is a CI failure (`ci_check_dependency_boundary.sh` + `ci_check_no_async_in_blue.sh`). Any non-`ade_plutus` crate referring to `pallas_*` is a CI failure (`ci_check_pallas_quarantine.sh`). Any reference to `ChainDb` / `chain_db` inside `crates/ade_core/src/consensus/` is a CI failure (`ci_check_no_chaindb_in_consensus_blue.sh`). B1/B2/N-E — BLUE/GREEN sub-partition inside `ade_ledger::mempool` is review-and-test-enforced for the broad partition, with two narrow grep gates. B3 — deposit-param-authority gate. PP-S1 — `ProposalProcedure {…}` struct-literal scan. PHASE4-N-C dependency notes (the 8 producer CI gates pin specific properties; see prior CODEMAP for full detail). **NEW — PHASE4-N-G dependency notes (the 7 producer-side-server CI gates pin specific properties):** any new `pub fn .*header_bytes` outside `crates/ade_ledger/src/block_validity/header_input.rs` is a CI failure (`ci_check_no_parallel_header_splitter.sh`, DC-CONS-16 strengthening + DC-CONS-18). Any `HashMap`/`HashSet` in `ade_ledger::producer::served_chain` is a CI failure (`ci_check_served_chain_closure.sh`, CN-CONS-07 strengthening). Any `pub fn` returning a raw `ChainSyncMessage`/`BlockFetchMessage` inside `chain_sync/server.rs`/`block_fetch/server.rs` is a CI failure (`ci_check_chain_sync_server_closure.sh` + `ci_check_block_fetch_server_closure.sh`, CN-PROTO-06). Any `ServerReply { variant }` struct-literal outside the two `server.rs` files is a compile error (private inner enum). Any `ServerReply::block(bytes)` call where `bytes` is not sourced from `ServedRangeLookup::range_bytes` is a CI failure (`ci_check_block_fetch_server_closure.sh`, DC-CONS-17). Any `import` of `crate::producer::signing` inside `crates/ade_runtime/src/network/` is a CI failure (`ci_check_n2n_server_no_signing_dep.sh`, key-boundary). Any `std::time`/`tokio`/`rand`/`HashMap` inside `crates/ade_runtime/src/producer/{broadcast_to_served,served_chain_lookups}.rs` is a CI failure (`ci_check_broadcast_to_served_purity.sh`, CE-N-G-5). Absence of `accepted_block_header_bytes` import in `served_chain_lookups.rs` is a CI failure (same script — positive grep for canonical-authority use).

### Naming convention

All crates are prefixed `ade_`. TCB color is not encoded in the crate name. The authoritative classifier is `.idd-config.json` `core_paths` plus the cluster doc TCB Color Maps for sub-crate scopes; CI scripts hard-code their BLUE list. The seven full-BLUE-scoped scripts use the full BLUE set: 6 BLUE crates + 9 BLUE `ade_network` paths. The B3 `ci_check_deposit_param_authority.sh` scopes the 6 BLUE crates. Four narrow scripts target `crates/ade_core/src/consensus/`. The two N-E scripts scope `crates/ade_ledger/src/mempool/*` + `crates/ade_testkit/src/mempool/*`. The PP-S1 `ci_check_proposal_procedures_closed.sh` scopes `conway/{governance.rs, tx.rs}` paths. The 8 PHASE4-N-C scripts narrow as previously documented. **NEW — the 7 PHASE4-N-G scripts narrow as follows:** `ci_check_no_parallel_header_splitter.sh` scopes repo-wide (single header-projection authority); `ci_check_served_chain_closure.sh` scopes `crates/ade_ledger/src/producer/served_chain.rs`; `ci_check_chain_sync_server_closure.sh` scopes `crates/ade_network/src/chain_sync/server.rs`; `ci_check_block_fetch_server_closure.sh` scopes `crates/ade_network/src/block_fetch/server.rs`; `ci_check_broadcast_to_served_purity.sh` scopes `crates/ade_runtime/src/producer/{broadcast_to_served,served_chain_lookups}.rs`; `ci_check_n2n_server_no_signing_dep.sh` scopes `crates/ade_runtime/src/network/`; `ci_check_server_paths_corpus_present.sh` scopes the two integration tests + the binary src + Cargo.toml `[[bin]]` entry + the procedure doc. **No CI script narrowly scopes the new `live_block_fetch_session.rs` binary's RED classification** — that rests on its file-header comment, its placement under `src/bin/`, and the `#[ignore]`-gated closure-gate test pattern (matching the established `live_*_session` precedent).

### CI enforcement (47 scripts under `ci/`)

| Script | Enforces | Scope |
|---|---|---|
| **`ci_check_block_fetch_server_closure.sh`** *(NEW — PHASE4-N-G S4)* | **DC-CONS-17 foundation — closed `ServerReply` wrapper, no raw `BlockFetchMessage` returns, `Block { bytes }` sourced from `ServedRangeLookup`** | `crates/ade_network/src/block_fetch/server.rs` |
| **`ci_check_broadcast_to_served_purity.sh`** *(NEW — PHASE4-N-G S5)* | **CE-N-G-5 — pure GREEN adapter shape; positive `accepted_block_header_bytes` import; no `std::time`/`tokio`/`rand`/`HashMap`** | `crates/ade_runtime/src/producer/{broadcast_to_served,served_chain_lookups}.rs` |
| `ci_check_cbor_round_trip.sh` | T-ENC-03, DC-CBOR-01, DC-CBOR-02 | golden corpus |
| `ci_check_ce_n_a_5_proof.sh` *(PHASE4-N-A, S-A10)* | CE-N-A-5 5-condition evidence | `ade_network` (RED + real-capture corpus) |
| `ci_check_chaindb_contract.sh` | DC-STORE-02, DC-STORE-03, CN-STORE-04, CN-STORE-05 | `ade_runtime --lib chaindb::` (RED) |
| `ci_check_chaindb_crash_safety.sh` | T-REC-01 (crash variant), DC-STORE-01, CN-STORE-03 | `ade_runtime --test stress_kill_harness` (RED) |
| **`ci_check_chain_sync_server_closure.sh`** *(NEW — PHASE4-N-G S3)* | **DC-PROTO-08 — closed `ServerReply` wrapper, no raw `ChainSyncMessage` returns, total reducer, deterministic-resolution discipline** | `crates/ade_network/src/chain_sync/server.rs` |
| `ci_check_consensus_closed_enums.sh` *(PHASE4-N-B, S-B2; extended B1, B2)* | DC-CONS-04, DC-CONS-10, T-DET-01, DC-VAL-02/04/05/06, DC-TXV-01/02/04/05, DC-MEM-01/02 | `consensus/` + `block_validity/` + `tx_validity/` + `mempool/` |
| `ci_check_constitution_coverage.sh` | invariant-registry ↔ code/test coverage | repo-wide |
| `ci_check_conway_cert_classification_closed.sh` *(PHASE4-B3F)* | DC-TXV-06 | `ade_types::conway::cert` + `ade_codec::conway::cert` + `ade_ledger::cert_classify` |
| `ci_check_credential_discriminant_closed.sh` *(OQ5/COMMITTEE/DREP/ENACTMENT-COMMITTEE-FIDELITY + WRITEBACK)* | DC-LEDGER-10 | `ade_types::shelley::cert` + both era `ade_codec` decoders + `ade_ledger::{state, governance, fingerprint, rules}` + `ade_types::conway::governance` |
| `ci_check_crypto_vectors.sh` | crypto KAT regression | `ade_crypto` |
| `ci_check_dependency_boundary.sh` | T-BOUND-02 — BLUE ⇎ RED separation | full BLUE (6 crates + 9 `ade_network` paths) |
| `ci_check_deposit_param_authority.sh` *(PHASE4-B3)* | DC-TXV-07 | 6 BLUE crates |
| `ci_check_differential_divergence.sh` | DC-DIFF-* | replay outputs |
| `ci_check_forbidden_patterns.sh` | T-CORE-02 + `unsafe` allowlist | full BLUE |
| `ci_check_forge_purity.sh` *(PHASE4-N-C S3)* | DC-CONS-13/14/15 | `crates/ade_ledger/src/producer/forge.rs` |
| `ci_check_gov_cert_accumulation_closed.sh` *(PHASE4-B5)* | DC-LEDGER-09 | `ade_ledger::{gov_cert, rules, error, state}` |
| `ci_check_hash_uses_wire_bytes.sh` | DC-CBOR-02, T-ENC-01 | full BLUE |
| `ci_check_hfc_translation.sh` | DC-EPOCH-02 | `ade_ledger::hfc` |
| `ci_check_ingress_chokepoints.sh` | DC-INGRESS-01, T-INGRESS-01 | full BLUE |
| `ci_check_ledger_determinism.sh` | DC-LEDGER-01 | `ade_ledger` |
| `ci_check_mempool_ingress_closure.sh` *(PHASE4-N-E, S1)* | DC-MEM-03 (strengthens DC-MEM-01) | `ade_ledger::mempool/{ingress,admit,mod}.rs` + repo-wide `admit(` scan |
| `ci_check_mempool_ingress_replay.sh` *(PHASE4-N-E, S2 + S3)* | DC-MEM-04 | `ade_testkit::mempool/*` + `ade_testkit::lib.rs` + `ade_ledger::mempool::canonicalize` |
| `ci_check_module_headers.sh` | CE-04 contract banner | full BLUE |
| **`ci_check_n2n_server_no_signing_dep.sh`** *(NEW — PHASE4-N-G S6)* | **Key-boundary: `network::n2n_server` MUST NOT import `producer::signing`** | `crates/ade_runtime/src/network/` |
| `ci_check_no_async_in_blue.sh` *(PHASE4-N-A, S-A1)* | DC-CORE-01 | full BLUE |
| `ci_check_no_chaindb_in_consensus_blue.sh` *(PHASE4-N-B, S-B1)* | DC-CORE-01 + DC-CONS-07 | `crates/ade_core/src/consensus/` |
| `ci_check_no_density_in_fork_choice.sh` *(PHASE4-N-B, S-B8)* | DC-CONS-03 | `fork_choice.rs` + `candidate.rs` |
| `ci_check_no_float_in_consensus.sh` *(PHASE4-N-B, S-B1)* | T-CORE-02 + DC-CONS-07/08/09 | `crates/ade_core/src/consensus/` |
| **`ci_check_no_parallel_header_splitter.sh`** *(NEW — PHASE4-N-G S1)* | **DC-CONS-16 strengthening + DC-CONS-18 — single canonical header-projection authority; no parallel `pub fn .*header_bytes` / `split_header` / `split_block_envelope`** | repo-wide grep gate |
| `ci_check_no_private_keys_in_corpus.sh` *(PHASE4-N-C S3)* | DC-CRYPTO-03/04/05 corpus discipline | `crates/ade_testkit/src/producer/fixtures.rs` + `crates/ade_testkit/fixtures/producer/` |
| `ci_check_no_producer_body_encoder.sh` *(PHASE4-N-C S4)* | DC-CONS-16 — single body-hash authority | repo-wide grep gate |
| `ci_check_no_secrets.sh` | no credentials/IPs/keys | repo-wide |
| `ci_check_no_semantic_cfg.sh` | T-BUILD-01 | full BLUE |
| `ci_check_no_signing_in_blue.sh` | CE-05, T-KEY-01 | full BLUE |
| `ci_check_opcert_closed.sh` *(PHASE4-N-C S2)* | DC-CONS-11/12 | `crates/ade_codec/src/shelley/opcert.rs` + `crates/ade_core/src/consensus/opcert_validate.rs` |
| `ci_check_pallas_quarantine.sh` | O-29.2 | non-`ade_plutus` |
| `ci_check_private_key_custody.sh` *(PHASE4-N-C S1)* | DC-CRYPTO-03/04/05 + OP-OPS-04 | full BLUE + `crates/ade_runtime/src/producer/` |
| `ci_check_producer_corpus_present.sh` *(PHASE4-N-C S7)* | CN-CONS-06 mechanical half | `crates/ade_testkit/src/producer/fixtures.rs` |
| `ci_check_proposal_procedures_closed.sh` *(PROPOSAL-PROCEDURES-DECODE PP-S1)* | DC-LEDGER-11 | `conway/{governance,tx}.rs` paths + repo-wide `ProposalProcedure {` scan |
| `ci_check_recovery_contract.sh` | T-REC-01, T-REC-02, DC-STORE-05 | `ade_runtime --lib recovery::` (RED) |
| `ci_check_ref_provenance.sh` | DC-REF-01 | reference corpus |
| `ci_check_scheduler_closure.sh` *(PHASE4-N-C S6)* | DC-CONS-13 + DC-MEM-03 + OP-OPS-05 | `crates/ade_runtime/src/producer/{scheduler,broadcast,tick_assembler}.rs` |
| `ci_check_self_accept_gate.sh` *(PHASE4-N-C S5)* | CN-CONS-07 | `crates/ade_ledger/src/producer/self_accept.rs` + repo-wide `AcceptedBlock` literal scan |
| **`ci_check_served_chain_closure.sh`** *(NEW — PHASE4-N-G S2)* | **CN-CONS-07 strengthening + CE-N-G-2 — no `HashMap`/`HashSet`; admit single-argument; `blocks` field private** | `crates/ade_ledger/src/producer/served_chain.rs` |
| **`ci_check_server_paths_corpus_present.sh`** *(NEW — PHASE4-N-G S7)* | **CE-N-G-7 + CE-N-G-8 binary presence — mechanical adapter + transcript-replay tests exist with named functions; live binary src + Cargo.toml entry exist; CE-N-G-8 procedure doc exists** | `crates/ade_runtime/tests/cross_impl_server_pipeline.rs` + `crates/ade_runtime/tests/server_paths_transcript_replay.rs` + `crates/ade_core_interop/src/bin/live_block_fetch_session.rs` + `docs/clusters/PHASE4-N-G/CE-N-G-8_PROCEDURE.md` |

> **Post-`694dd74` CI delta. +7.** All seven new gates are the PHASE4-N-G producer-side-server gate set. **CI inventory 40 → 47.** The forward-looking `ci/ci_check_no_fail_open_in_validation.sh` (DC-VAL-06 / DC-TXV) is still **not** shipped — carried gap.

> **Carried residual gaps (unchanged at HEAD, plus the new N-G-introduced ones).** Carry-forward gaps (a)–(s) from the prior CODEMAP are all still open. **(t) NEW — `ade_runtime::producer::{broadcast_to_served, served_chain_lookups}` GREEN sub-classification inside the RED `ade_runtime` crate prefix is partially gated by `ci_check_broadcast_to_served_purity.sh` (pure-fn shape) but the broader BLUE/GREEN/RED partition is "by file" — no dedicated CI script for the broad partition.** **(u) NEW — `ade_runtime::network::n2n_server` RED classification is review-and-doc enforced (file-header banner + key-boundary CI gate); no script asserts the broader RED partition inside `network/`.** **(v) NEW — `ade_core_interop::bin::live_block_fetch_session` (S7) RED classification is review-and-doc enforced (matching the S6 + N-C-S7 precedent — no narrow CI gate scopes the partition, only the presence gate `ci_check_server_paths_corpus_present.sh`).** **(w) NEW — at HEAD the `live_block_fetch_session` binary's `--connect` mode is honest-scope stubbed: the actual tokio socket wiring from `n2n_server` to the wire is one layer up (deferred to operator-action work + the future `ade_node` orchestration cluster). The pure dispatch driver `n2n_server` is complete; what is owed is the socket layer and the live-evidence pass that traverses it.** **(x) NEW — CE-N-G-8 live evidence (RO-LIVE-01) carries `status = "partial"` + `open_obligation = "blocked_until_operator_peer_available"`. Reopen criteria: provision a private cardano-node peer, wire the socket layer (one-layer-up work), run the binary per `CE-N-G-8_PROCEDURE.md`, append the captured log path to RO-LIVE-01's `evidence_notes`. Mirrors the OP-OPS-04 / CE-N-C-8 precedent (enforced mechanical half + open obligation for the operator-action live half).** **(y) NEW — the cluster plan named `ade_testkit::server_paths::{fixtures, replay, cross_impl_adapter}` but the shipped artifact lives at `crates/ade_runtime/tests/*.rs` instead. The deviation is intentional (orphan-rule + dev-dep direction); whether `ade_testkit::server_paths::*` should be added as a thin re-export shim for cross-crate consumption is a follow-on judgment call — no current consumer would benefit.**
