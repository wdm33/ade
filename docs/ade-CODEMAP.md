# Module Authority Map — Ade

> **Status:** Living architectural document. Regenerated; not hand-edited.
> Per-project instance of `~/.claude/methodology/templates/codemap.md`.

> 12 crates, 471 canonical types, 2640 tests, 200 CI checks at HEAD (`1b851c07`, MEM-OPT-UTXO-DISK cluster-close refresh — the on-disk-UTxO preparation + owned-footprint memory win. **Unlike MEM-OPT-OPS, this cluster TOUCHED BLUE** (≈18 files, +1537/−72 in `ade_core`/`ade_types`/`ade_codec`/`ade_crypto`/`ade_ledger`/`ade_plutus` `src`): the +7 canonical-type delta (464→471) is ALL this cluster's BLUE work — `ade_crypto` +1 (`utxo_set_commitment::UtxoSetCommitment`, the Ristretto255 ECMH v2 set-commitment primitive) and `ade_ledger` +6 (`OverlayUtxo`, `WorkingSet`, `StaticUtxoFp`, `StaticUtxoFpError`, `IncrementalUtxoFp`, `UtxoFpCache`). The BLUE changes are proven behavior-invariant (identical verdict / fingerprint / failure-shape) EXCEPT the deliberate, versioned **v1→v2 UTxO-fingerprint cutover** (`fingerprint()` delegates to `fingerprint_v2`; `fingerprint_v1` FROZEN; the store carries `FINGERPRINT_VERSION=2`, fail-closed on opening an old store via `ChainDbError::FingerprintVersionMismatch`) — the v2 fingerprint is **Ade-internal, never peer-facing**. NEW crate **`ade_mem_diag`** (11→12) is the workspace's SOLE `unsafe`-FFI surface (a quarantined RED `mi_collect` diagnostic probe) so `ade_node` keeps `#![deny(unsafe_code)]` with ZERO local allows. +2 registry rules (`DC-MEM-09`/`DC-MEM-10`, both `enforced`), +10 CI gates, **`OP-MEM-02` `declared → enforced` (the SCOPED BA-08 flip).**)

---

## Conventions

- A **module** in Ade is a Cargo workspace crate (smallest independently-buildable unit). Source: `Cargo.toml` `[workspace] members` (**12** entries — `ade_mem_diag` is NEW this cluster). One exception to crate-granularity: `ade_network` is split by *submodule color* — `.idd-config.json` `core_paths` resolves BLUE at the submodule path level (9 paths), so its BLUE / GREEN / RED submodules are documented as separate entries.
- Several BLUE crates host sub-trees that carry a *different* TCB color by their own module banner; the RED `ade_runtime` and `ade_node` crates host numerous **GREEN-by-content** sub-trees. These are surfaced as sub-classification notes inside the owning crate's entry (or, where load-bearing, as their own GREEN/RED entries below). **MEM-OPT-UTXO-DISK added (a) the new RED crate `ade_mem_diag`** (the quarantined unsafe `mi_collect` probe), **(b) new BLUE source in `ade_crypto` + `ade_ledger`** (the v2 ECMH set-commitment primitive, the `OverlayUtxo` representation, the `StaticUtxoFp` / `IncrementalUtxoFp` / `UtxoFpCache` fingerprint machinery, the GREEN `pre_resolve` working-set, and the `UtxoStore` / `UtxoMembership` owned-lookup seam), **and (c) DORMANT RED on-disk-redb-UTxO B-infra** in `ade_runtime::chaindb` (`utxo_anchor`, `utxo_key`). The carried `ade_node::mem_measure` sub-tree (MEM-MEASURE / MEM-OPT-OPS) and the PHASE4-N-AO `ade_node` SELECT modules (`candidate_aggregator` / `lca_walk` / `selector_state` / `post_switch_continuity` / `fork_switch` / `fair_merge`) are carried verbatim from the prior refresh.
- Modules are listed by TCB color (BLUE → GREEN → RED), alphabetical within each color.

### TCB color sources (in order of authority)

1. `.idd-config.json` `core_paths` — substring match against absolute path. BLUE crate prefixes: `ade_codec`, `ade_types`, `ade_crypto`, `ade_core`, `ade_ledger`, `ade_plutus`. Plus the 9 BLUE `ade_network` submodule paths: `mux/frame.rs`, `codec/`, `handshake/`, `chain_sync/`, `block_fetch/`, `tx_submission/`, `keep_alive/`, `peer_sharing/`, `n2c/`.
2. `.idd-config.json` `_core_paths_doc` — `ade_runtime` is RED; `ade_testkit` is GREEN; `ade_node` is RED; `ade_network::mux::transport` + `ade_network::session` are nominally RED (but `session/` is GREEN-by-content per PHASE4-N-L). `ade_core_interop` is RED. **`ade_mem_diag` is RED** (it is not in `core_paths`; its own banner is the authority — see source rule 3).
3. **Module doc-comment banners** — every BLUE/GREEN source file opens with the `// Core Contract:` banner and/or a `//! BLUE …` / `//! GREEN …` / `//! RED …` line. This is the per-file authority that distinguishes GREEN-by-content from RED inside `ade_runtime` / `ade_node`. **The `e0c77492..1b851c07` span (MEM-OPT-UTXO-DISK) added SIX new source files** — BLUE `ade_crypto/src/utxo_set_commitment.rs`; BLUE-host `ade_ledger/src/utxo_overlay.rs` + `ade_ledger/src/pre_resolve.rs` (the GREEN-by-content working-set); RED `ade_runtime/src/chaindb/utxo_anchor.rs` + `ade_runtime/src/chaindb/utxo_key.rs` (DORMANT B-infra, `#![allow(dead_code)]`); and the new RED crate `ade_mem_diag/src/lib.rs` (the deliberately-NOT-`#![deny(unsafe_code)]` quarantine). The BLUE edits to existing files (`ade_ledger/src/{fingerprint,utxo,phase,snapshot/*,tx_validity/phase1,…}.rs`, the era-rule files) are behavior-preserving EXCEPT the versioned v1→v2 fingerprint cutover (DC-MEM-10). The `ade_mem_diag/src/lib.rs` banner is explicit: RED diagnostic crate, no authoritative semantics, NO BLUE crate may depend on it, its return value influences nothing.
4. Cluster TCB Color Maps under `docs/clusters/` and `docs/clusters/completed/` — §"TCB Color Map" of `docs/clusters/MEM-OPT-UTXO-DISK/cluster.md`: **RED** = `ade_mem_diag` (the `mi_collect` probe) + `ade_runtime::chaindb::{utxo_anchor, utxo_key}` (the dormant on-disk redb backend) + the `ade_node` `StaticUtxoFp` admission wiring; **GREEN** = `ade_ledger::pre_resolve` (the era-aware `collect_required_txins` working-set builder) + the `UtxoStore` / `UtxoMembership` seam interpretation; **BLUE** = `ade_crypto::utxo_set_commitment` (the v2 ECMH primitive) + `ade_ledger::{utxo_overlay::OverlayUtxo, fingerprint::{StaticUtxoFp, IncrementalUtxoFp, UtxoFpCache, fingerprint_v2}}` + the owned `utxo_lookup` signature change in `utxo.rs` / `phase.rs` / `tx_validity/phase1.rs`. The prior MEM-OPT-OPS map (allocator + streaming import + owned samplers) and the PHASE4-N-AO map (the live multi-candidate fork-choice SELECT) are carried.
5. The invariant registry `docs/ade-invariant-registry.toml` (**380** rules at HEAD) — each rule's `code_locus` + `statement` is the authoritative source for the **MUST NOT** rows below.

### Active cluster at HEAD

**MEM-OPT-UTXO-DISK (on-disk-UTxO preparation + the owned-footprint memory win — `DC-MEM-05…10` / `OP-MEM-02`) — at cluster close, this `/codemap` refresh.** HEAD `1b851c07` is the cluster's last commit (it reconciles the cluster to PATH A and finalizes the S2b statuses pre-close); the close commit will be created on top carrying this regenerated doc. The cluster span is `e0c77492..1b851c07` (first cluster commit `a2d22113`, parent `e0c77492` = the MEM-OPT-OPS close). **This cluster TOUCHED BLUE** (≈18 BLUE `src` files, +1537/−72) and is the home of the full +7 canonical-type delta. It shipped two distinct mechanisms — a LIVE one (PATH A) that delivered the memory win, and a BUILT-BUT-DORMANT one (the redb backend) that is preparation for the still-owed "B".

- **PATH A — the live memory win (`OP-MEM-02` SCOPED-enforced).** The live admission path runs `track_utxo=false` (it follows headers/tips; it does NOT mutate the UTxO per block), so the retained 1.9M-entry in-memory static UTxO was *accidental retention*, re-scanned per block to recompute a CONSTANT fingerprint. **A.2 (`c73a420f`/`e6b623d5`) drops it:** bootstrap computes the constant UTxO-component fingerprint ONCE — `ade_ledger::fingerprint::StaticUtxoFp` (fail-closed under `track_utxo=true` or a version mismatch) — BEFORE `drop(utxo)`; `post_fp` then uses the static component (`fingerprint_v2_with_utxo` ignores `state.utxo_state`, so the empty-UTxO live ledger yields a `post_fp` byte-identical to the full-UTxO one — DC-MEM-05 replay-equivalence preserved); the durable copy is the existing on-disk snapshot. Live re-measure (preprod docker peer, epoch 295, fresh 3.8 GB seed): active-admission owned `RssAnon` **1.94 GiB** (p50 == peak, n=33) vs the preprod Haskell baseline **2.57 GiB** → verdict **`ade_below`** (~25% below; 58% below the 4.59 GiB pre-A.2 baseline). Correctness: 36 blocks admitted, 1 agreed + 35 lagging, 0 diverged, 0 hash mismatches, `replay_verdict agreed`.
- **The on-disk redb UTxO backend (`DC-MEM-05`) — BUILT + UNIT-PROVEN but DORMANT.** `ade_runtime::chaindb::{utxo_anchor, utxo_key}` is the `TxIn → TxOut` on-disk anchor (`#![allow(dead_code)]` — "wired into admission in the next S2b step"), the fixed-width `txid[32] || BE-u32(index)` storage key (forcing redb's byte-sorted iteration to equal canonical `TxIn` order, DC-MEM-06), the `AnchorPosition`/`reconcile` recovery surface, and the GREEN `pre_resolve::{collect_required_txins, WorkingSet}` pre-resolution view (inputs ∪ collateral ∪ reference). It is reachable from NO live path (a CI guardrail keeps it out of BLUE — it deliberately does **not** implement `UtxoStore`). **It is preparation for B, NOT the mechanism that delivered the memory win, and is NOT live-proven for durability.**
- **S1 / S1.5 — the seam + the versioned fingerprint cutover (`DC-MEM-09` / `DC-MEM-10`, both `enforced`).** S1 (`DC-MEM-09`) changed the authoritative `utxo_lookup` from `-> Option<&TxOut>` to `-> Option<TxOut>` (OWNED) behind a minimal `UtxoStore` / `UtxoMembership` seam (so an on-disk backend can serve a resolved output by value without leaking storage lifetimes into the validity rules) — INTERFACE-PREP, explicitly NOT a memory victory. S1.5 (`DC-MEM-10`) introduced `ade_crypto::utxo_set_commitment` (a Ristretto255 ECMH set commitment — commutative, add/remove-inverse, value-binding, domain-separated, golden-vector-pinned) and the `fingerprint_v2` family; S1.5b then performed the PRODUCTION cutover: `fingerprint()` now delegates to `fingerprint_v2`, `fingerprint_v1` is FROZEN (the v1 goldens + the 12 boundary-snapshot pins repointed at it), and the chaindb META carries `FINGERPRINT_VERSION=2`, checked fail-closed on open (a v1/unversioned store is REJECTED, `ChainDbError::FingerprintVersionMismatch`, never silently upgraded). The per-block `IncrementalUtxoFp` (produce/spend over the shared `v2_entry_bytes`) is PROVEN == the full-recompute oracle after every block. **No peer-facing change** — the `AgreementVerdict` (block-hash) path is untouched.
- **S2a — the bounded overlay (`DC-MEM-07`, `partial`).** `ade_ledger::utxo_overlay::OverlayUtxo` is an `Arc`-shared immutable anchor + a bounded in-memory overlay of diffs (`Some` = insert / `None` = delete tombstone), capped by the fixed, closed, non-configurable `MAX_OVERLAY_ENTRIES`; exceeding it folds the overlay into a fresh anchor (`compact()`) so the in-memory diff never grows unboundedly. A clone is O(overlay) (the anchor `Arc` is shared); a mutation is an overlay append (no whole-`BTreeMap` clone). The CHANGELOG/overlay-bound clause is now mechanically enforced; the READ-CACHE-bound clause stays declared pending the on-disk anchor.
- **S0 — the owned-footprint diagnostic + the `ade_mem_diag` quarantine (`CE-UD-0`).** The S0 transcript measured active-admission owned footprint; its decisive control is a forced `mi_collect` (mimalloc returns freed-but-`MADV_FREE`-retained pages to the OS so a retained-freed footprint and a live working set are distinguishable). That ONE `unsafe` FFI call is quarantined in the new RED crate `ade_mem_diag` (so `ade_node` keeps `#![deny(unsafe_code)]` with zero local allows), gated behind the `ADE_MEM_PHASE_DIAGNOSTIC` env toggle (absent on every production run). **The `ade_mem_diag` probe is diagnostic-only and was NOT used in the BA-08 evidence run** (the owned-RSS measurement contains zero forced-collect points); it must never read as a dependency for the memory win.

**`OP-MEM-02` `enforced` is SCOPED (no over-claim).** Owned `RssAnon` 1.94 GiB < Haskell 2.57 GiB holds on the **CURRENT live `track_utxo=false` header/tip-following admission path ONLY** — NOT full live UTxO application. **Two obligations remain OWED:** (1) **`track_utxo=true` full live ledger application (LIVE-LEDGER-APPLY, "B")** — the redb backend is the preparation for it but is dormant; (2) the **10-day sustained BA-08 certification run** — the current evidence POSITIONS Ade to win the 10-day window but does NOT certify it. `enforced` here means "the SCOPED owned-footprint bound is mechanically backed on the live path," NOT "BA-08 is certified."

**RSS / owned-memory is observational ONLY** — it influences NO authoritative output. The RED `rss_sampler` reads the OS, returns numbers, and never enters a fingerprint, verdict, or validator pass/fail. `StaticUtxoFp` does not read RSS; it is a constant-fingerprint optimization gated fail-closed on `track_utxo`/version.

**Carried: MEM-OPT-OPS (operator-facing memory optimization — `DC-MEM-06` / `OP-MEM-02`) — CLOSED at `233644f7`.** The RED+GREEN substrate this cluster builds on: the process-wide `mimalloc` `#[global_allocator]` (S1, `VmRSS −29.8 %`), the STREAMING seed import (S2, `Deserializer::from_reader`, import peak halved, byte-identical fingerprint, `DuplicateTxIn` fail-closed), and the owned-footprint samplers `sample_rss_anon_kib` / `sample_private_dirty_kib` (S3) threaded through the closed observe-only memory-evidence vocabulary. MEM-OPT-OPS itself was **zero-BLUE**; its S3 honestly recorded `ade_heavier` on the owned metric (the finding that motivated MEM-OPT-UTXO-DISK's PATH A).

**Carried: MEM-MEASURE (bounded inbound admission + memory-evidence substrate — `CN-MEM-01` / `OP-MEM-01`) — CLOSED.** `crates/ade_node/src/mem_measure/` (A1) — the GREEN `bounded_admission` fold fronting the BLUE `mempool_ingress`; the GREEN `evidence` record whose verdict is computed ONLY from the replay-fingerprint pairing, never from RSS; the GREEN/RED `runner` measurement seam.

**Carried: PHASE4-N-AO (live multi-candidate fork-choice SELECT + adopt — `DC-NODE-37`) — CLOSED + ARCHIVED** at `862cd2cb`/`388d8073`. It flipped `CN-CONS-03` `declared → enforced` on a natural two-producer CE-AO-6 transcript. The six `ade_node` SELECT modules (`candidate_aggregator` / `lca_walk` / `selector_state` / `post_switch_continuity` GREEN; `fork_switch` GREEN-prove + RED-seam; `fair_merge` RED) + bin are carried verbatim below. **All prior closed clusters (N-AN … N-U, G-K…G-R)** remain archived under `docs/clusters/completed/`.

**What the binary `Node` arm actually runs (precise wiring honesty):** `--mode node` (`ade_node::node_lifecycle`) bootstraps via the single `bootstrap_initial_state` authority, spawns the live WirePump feed (per-peer bounded lanes drained by the deterministic `fair_merge`; keep-alive client sustaining each link), drains it through the sole `forward_sync::pump_block` durable-admit chokepoint, serves a read-only projection of the durable ChainDb to peers, and on the participant path drives the PHASE4-N-AO fork-switch SELECT. **MEM-OPT-UTXO-DISK changes the runtime memory profile of this path WITHOUT changing its authority surface:** on the live `track_utxo=false` path the bootstrap computes a `StaticUtxoFp` once then `drop`s the static UTxO; `post_fp` uses the cached constant component (byte-identical result, DC-MEM-05); the v2 fingerprint cutover is internal-only (no peer-facing change). The redb on-disk UTxO backend is built but not wired into this path. `RO-LIVE-01` stays operator-gated.

### Counts (mechanical, with sources)

| Count | Value | Source / command |
|---|---|---|
| Crates | **12** | `grep -cE '"crates/' Cargo.toml` (`[workspace] members`). **Δ vs prior on-disk CODEMAP (11): +1** — NEW crate `ade_mem_diag` (the quarantined RED `mi_collect` probe); `git diff --name-status e0c77492..1b851c07 -- Cargo.toml` shows the member added. |
| Canonical types | **471** | `git grep -hE "^(pub )?(struct\|enum) " 1b851c07` over the 6 BLUE crate `src/` trees + the 9 BLUE `ade_network` submodule paths, summed at HEAD `1b851c07`. **Δ vs prior on-disk CODEMAP (464): +7 — ALL from THIS cluster's BLUE work.** Breakdown: `ade_codec` 11, `ade_types` 82, `ade_crypto` **22** (21→22, **+1** = `utxo_set_commitment::UtxoSetCommitment`), `ade_core` 49, `ade_ledger` **187** (181→187, **+6** = `utxo_overlay::OverlayUtxo`, `pre_resolve::WorkingSet`, `fingerprint::{StaticUtxoFp, StaticUtxoFpError, IncrementalUtxoFp, UtxoFpCache}`), `ade_plutus` 9 (= 360) + `ade_network` BLUE submodules **111** (`mux/frame.rs` 5, `codec/` 40, `handshake/` 9, `chain_sync/` 11, `block_fetch/` 10, `tx_submission/` 5, `keep_alive/` 5, `peer_sharing/` 5, `n2c/` 21). The `ade_mem_diag` crate exports a single fn (no public type) and is NOT canonical-counted (RED). The dormant redb-anchor types (`AnchorPosition`, `RecoveryDecision`) are `pub(crate)` in the RED `ade_runtime` crate and are NOT canonical-counted. `canonical_type_registry: null`, so the structural grep is authoritative. |
| Tests | **2640** | `git grep -hE "#\[test\]\|#\[tokio::test\]" -- 'crates/**/*.rs' \| wc -l` = 2640 at `1b851c07`. Approximate per the template fallback rule (count of attributes, not a runner enumeration). **Δ vs the same grep at the cluster parent `e0c77492` (2595): +45** — the MEM-OPT-UTXO-DISK hermetic unit suites: the ECMH commitment suite (`order_independent`, `add_remove_is_exact_inverse`, `binds_value_not_just_key`, the 3 golden-digest vectors, `fingerprint_v2_is_deterministic`, `fingerprint_v2_utxo_is_insertion_order_independent`, `incremental_v2_equals_full_recompute_after_each_block`, `incremental_produce_spend_is_exact_inverse`, `v1_unversioned_store_is_rejected_fail_closed`), the overlay suite (`overlay_matches_btreemap_across_a_sequence`, `compact_preserves_effective_set_and_clears_overlay`, `clone_shares_anchor_and_is_independent`, `s2a_overlay_split_fingerprints_identically_to_direct_build`), the owned-lookup / static-fp / pre-resolve / disk-anchor / disk-key suites (`owned_lookup_returns_stored_value_and_does_not_mutate`, `static_utxo_fp_fails_closed_under_track_utxo_true_and_version_mismatch`, the `ade_mem_diag` `collect_is_a_safe_noop_to_call`, …). |
| CI checks | **200** | `ls ci/ci_check_*.sh \| wc -l` = 200 at `1b851c07` (cross-checked `git ls-tree -r 1b851c07 ci/`). CI lives entirely under `ci/` (`ci_dirs = ["ci"]`); there is no `.github/workflows/` invariant gate at HEAD. **Δ vs prior on-disk CODEMAP (190): +10** — verified `git diff --name-status e0c77492..1b851c07 -- 'ci/ci_check_*.sh'`: **10 `A`** (`ci_check_mem_diag_quarantine.sh`, `ci_check_mem_opt_utxo_disk_s0.sh`, `ci_check_overlay_utxo_s2a.sh`, `ci_check_utxo_admission_seam.sh`, `ci_check_utxo_disk_anchor.sh`, `ci_check_utxo_disk_key.sh`, `ci_check_utxo_fp_cache.sh`, `ci_check_utxo_fp_v2.sh`, `ci_check_utxo_lookup_owned.sh`, `ci_check_utxo_pre_resolve.sh`), **1 `M`** (`ci_check_mem_measure_evidence.sh`), **0 `D`**. |
| Registry rules | **380** | `grep -cE "^id = " docs/ade-invariant-registry.toml` = 380. Reference only — not a header count. **Status breakdown:** 253 `enforced` / 23 `partial` / 103 `declared` / 1 `enforced_scaffolding` / 0 `deprecated`. **Δ vs prior on-disk CODEMAP (378): +2** — the net-new rules are `DC-MEM-09` (`enforced`) + `DC-MEM-10` (`enforced`). **Flips this cluster:** `OP-MEM-02` `declared → enforced` (the SCOPED BA-08 flip); `DC-MEM-06` `declared → partial` (the allocator-neutrality clause mechanically enforced at MEM-OPT-OPS S1, the streaming clause at S2) and `DC-MEM-07` `declared → partial` (the overlay-bound clause at S2a). **No rule weakened.** |

---

## BLUE Modules — Pure Functional Core

> **Shared header (applies to every BLUE entry below).** Every `.rs` source file begins with the `// Core Contract:` banner, and the following deny attributes are present in each crate's `lib.rs` (or, for `ade_network`, at the crate root — BLUE submodules inherit them):
> `#![deny(unsafe_code)]`, `#![deny(clippy::unwrap_used)]`, `#![deny(clippy::expect_used)]`, `#![deny(clippy::panic)]`, `#![deny(clippy::float_arithmetic)]`.
>
> **The `#![deny(unsafe_code)]` invariant survived this cluster intact, including in the RED `ade_node` binary** — the one `unsafe` the workspace needs (mimalloc `mi_collect`) was quarantined in the dedicated RED crate `ade_mem_diag`, which is the SOLE crate that does NOT carry `#![deny(unsafe_code)]` (it IS the quarantine, gated by `ci_check_mem_diag_quarantine.sh`). No BLUE/GREEN crate may depend on it.
>
> Cross-cutting BLUE CI gates (all scope the full BLUE set — 6 BLUE crates + 9 BLUE `ade_network` submodule paths): `ci_check_module_headers.sh`, `ci_check_forbidden_patterns.sh`, `ci_check_dependency_boundary.sh`, `ci_check_no_signing_in_blue.sh`, `ci_check_no_semantic_cfg.sh`, `ci_check_hash_uses_wire_bytes.sh`, `ci_check_ingress_chokepoints.sh`, `ci_check_pallas_quarantine.sh`, `ci_check_no_async_in_blue.sh`.
>
> Narrow BLUE / consensus-authority gates: the standing `ci_check_no_density_in_fork_choice.sh` + `ci_check_consensus_closed_enums.sh` + `ci_check_chain_selection_arrival_order_independent.sh` + `ci_check_rollback_materialize_closure.sh` / `ci_check_rollback_materialize_eta0.sh` (`CN-STORE-07` / `T-REC-06`) + `ci_check_receive_reducer_closure.sh` (`DC-CONS-20`). **The `e0c77492..1b851c07` span added BLUE/storage-boundary gates IN this cluster** — `ci_check_utxo_fp_v2.sh` (DC-MEM-10: the v2 ECMH fingerprint is named/domain-separated/golden-pinned/version-tagged and the incremental == full recompute), `ci_check_utxo_lookup_owned.sh` (DC-MEM-09: the owned `utxo_lookup` signature + the `UtxoStore` seam + no redb in `ade_ledger`), `ci_check_overlay_utxo_s2a.sh` (DC-MEM-07: the overlay shape + the closed bound + compaction + fingerprint-identity), `ci_check_utxo_pre_resolve.sh` (the era-aware required-input enumeration completeness), `ci_check_utxo_disk_anchor.sh` / `ci_check_utxo_disk_key.sh` (DC-MEM-05/06: the dormant redb backend's pure-storage discipline + the fixed-width key order), `ci_check_utxo_fp_cache.sh` (OP-MEM-02: `StaticUtxoFp` + `drop(utxo)` + the fail-closed guard). The standing BLUE deny / closed-enum / canonical-codec gates apply to the changed BLUE files; the v1→v2 cutover is the only deliberate behavior change and is itself gated (the v1 goldens repointed at `fingerprint_v1`, a v1 store rejected fail-closed).

---

### `ade_codec`

| Attribute | Value |
|-----------|-------|
| **Purpose** | Owns Cardano-canonical CBOR ingress — the only place in the workspace that turns raw bytes into typed semantic values, with wire-byte preservation for every hash-bearing structure. Owns the standalone opcert byte authority and the canonical Conway-tx preserved-byte splitter, the canonical block-envelope encoder `cbor::envelope::encode_block_envelope`, and the single workspace **tag-24 CBOR-in-CBOR wire-envelope authority** `cbor::tag24::{wrap_tag24, unwrap_tag24}` + closed `TagEnvelopeError`. Also exposes the shared canonical CBOR read/write primitives the BLUE codecs in `ade_ledger` + `ade_network` build on — incl. the canonical UTxO write primitives (`write_array_header` / `write_map_header` / `write_bytes_canonical` / `write_uint_canonical` / `canonical_width`) reused both by the MEM-OPT-OPS S2 streaming seed-import sink and by the dormant MEM-OPT-UTXO-DISK redb anchor's `encode_tx_out_canonical` (byte-unchanged). Owns the POSITION-BLIND header `prev_hash` codec `shelley::block::decode_prev_hash` (CN-WIRE-09). **(Unchanged across the MEM-OPT-UTXO-DISK span — `ade_codec` is 11.)** |
| **Creates** | `PreservedCbor<T>`, `RawCbor`, `BlockEnvelope`, `ByronDecodedBlock`, `CodecContext`, `CodecError`, `ContainerEncoding`, `IntWidth`, era-tagged block/tx wrappers, `OpCertCodecError`, `TxComponents<'a>`, `TagEnvelopeError` (closed). 11 public types (byte-stable across the span). |
| **Interprets** | All canonical Cardano CBOR. `unwrap_tag24` is the single workspace authority that strips a tag-24 (`0xd8 0x18`) envelope (zero-copy borrow, fail-closed `TagEnvelopeError`). `shelley::block::decode_prev_hash` decodes the header `prev_hash` POSITION-BLIND (CN-WIRE-09). |
| **MUST NOT** | (1) Construct `PreservedCbor` outside `ade_codec`. (2) Re-encode wire bytes when computing hashes. (3) Use any forbidden BLUE pattern. (4) Depend on any workspace crate except `ade_types`. (5)–(11) per-construct prohibitions (Conway cert/withdrawals/governance, opcert, tx_components, single block-envelope encoder). (12) `cbor::tag24` (CN-WIRE-08) — `wrap_tag24`/`unwrap_tag24` defined exactly once; `unwrap_tag24` fails closed (never panic); no second/hand-rolled tag-24 parse anywhere. (13) `shelley::block::decode_prev_hash` (CN-WIRE-09) — the codec is the byte representation only and MUST stay POSITION-BLIND (no `block_number`); the position-aware coupling lives in `ade_ledger::block_validity::header_position`, never here. |
| **Inbound deps** | `ade_ledger` (heavy), `ade_plutus`, `ade_testkit`, `ade_network`, `ade_runtime` (incl. the MEM-OPT-OPS S2 streaming UTxO sink + the dormant MEM-OPT-UTXO-DISK redb anchor, both of which call only the canonical `cbor::write_*` primitives), `ade_core_interop`, `ade_node`. |
| **Outbound deps** | `ade_types`. std-only; dev-deps `serde_json`, `toml`. |
| **Entry points** | `ade_codec::cbor::envelope::{decode_block_envelope, encode_block_envelope}`, `ade_codec::{wrap_tag24, unwrap_tag24, TagEnvelopeError}`, the `cbor::{read_*, write_*_canonical, canonical_width, ContainerEncoding, IntWidth}` primitives, `ade_codec::traits::AdeEncode`, per-era `decode_*_block`, `ade_codec::shelley::{opcert::{encode_opcert, decode_opcert}, tx_components::split_conway_tx_components, block::decode_prev_hash}`. |
| **Key modules** | `cbor/` (incl. `envelope.rs`, `tag24.rs`), `byron/`, `shelley/` (incl. `block.rs`, `cert.rs`, `opcert.rs`, `tx_components.rs`), `allegra/`, `mary/`, `alonzo/`, `babbage/`, `conway/`, `address/`, `preserved.rs`, `traits.rs`, `primitives.rs`, `error.rs`. |

---

### `ade_core`

| Attribute | Value |
|-----------|-------|
| **Purpose** | The functional core (consensus half): Praos VRF leader-check + the expected-VRF-input recipe; **the chain-selection authority `consensus::fork_choice::select_best_chain` (the sole `DC-CONS-03` selector)**; the BLUE candidate/header-validation authorities the live SELECT path reuses — `consensus::header_validate::validate_and_apply_header`, `consensus::candidate::{CandidateFragment, TiebreakerView}`, `consensus::header_summary::{HeaderInput, HeaderVrf, ValidatedHeaderSummary}`, `consensus::events::Point`, `consensus::rollback`, `consensus::praos_leader_value`; the `PraosChainDepState` / `Nonce` evolution (incl. `overlay_recovered_eta0`); the HFC era-schedule `EraSchedule`; the closed `LeaderCheckVerdict`. **MEM-OPT-UTXO-DISK: UNTOUCHED** — `git diff e0c77492..1b851c07 -- crates/ade_core/src/` is empty; `ade_core` is **49 → 49**. The UTxO-fingerprint cutover lives in `ade_ledger` + `ade_crypto`; the consensus authority is byte-identical. |
| **Creates** | `PraosChainDepState`, `Nonce`, `EraSchedule`, `ExpectedVrfInput`, `LeaderCheckVerdict` (closed), `CandidateFragment`, `TiebreakerView`, `ValidatedHeaderSummary`, `HeaderInput`, `HeaderVrf`, `Point`, `BlockDistance`, candidate/VRF-input/leader-threshold types. 49 public types (byte-stable across the span). |
| **Interprets** | Praos header VRF/leader inputs; candidate chain summaries for `select_best_chain`. `validate_and_apply_header` validates one `HeaderInput` and emits a `ValidatedHeaderSummary` — the SOLE source the GREEN `ade_node::candidate_aggregator` is permitted to build a `CandidateFragment` from. `consensus::praos_state` evolves the nonce + overlays the recovered eta0. |
| **MUST NOT** | (1) Perform I/O / read a clock / use float / `HashMap`. **(2) Be the home of a SECOND chain selector — `select_best_chain` is the sole `DC-CONS-03` fork-choice authority (route-to, never duplicate); arrival-order-independent (`CN-CONS-01`). `validate_and_apply_header` is the sole candidate-summary source — a RED-minted `ValidatedHeaderSummary` (the `ade_core_interop::follow` shape) MUST NOT cross into this crate or reach `select_best_chain` (`DC-NODE-35`).** (3) Construct semantic types from raw bytes. (4) Depend on any RED/GREEN crate (only `ade_types`, `ade_crypto`, `minicbor`). (5) Add a SECOND `leader_vrf_input` / expected-VRF-input authority. (6) `PraosChainDepState::overlay_recovered_eta0` (T-REC-06) — the SINGLE eta0-overlay authority; never weaken/skip/bypass header VRF; stays pure. **(7, MEM cross-ref `DC-MEM-05`/`DC-MEM-06`) the consensus authority MUST stay independent of the process allocator + any UTxO storage-backend iteration order — a memory-representation / allocator / fingerprint-version change is NEVER a consensus or replay change (the v1→v2 UTxO-fingerprint cutover is Ade-internal and does NOT touch the block-hash / `AgreementVerdict` path this crate feeds).** |
| **Inbound deps** | `ade_ledger`, `ade_testkit`, `ade_core_interop`, `ade_runtime`, `ade_node` (incl. the PHASE4-N-AO SELECT consumers). |
| **Outbound deps** | `ade_types`, `ade_crypto`, `minicbor`. Dev-dep: `ade_testkit`. |
| **Entry points** | `ade_core::consensus::{fork_choice::select_best_chain, header_validate::validate_and_apply_header, leader_check::*, praos_state::{PraosChainDepState (incl. overlay_recovered_eta0), Nonce}, candidate::{CandidateFragment, TiebreakerView}, header_summary::{HeaderInput, HeaderVrf, ValidatedHeaderSummary}, events::{Point, BlockDistance}, praos_leader_value}`, `ade_core::era_schedule::EraSchedule`. |
| **Key modules** | `consensus/` (`fork_choice.rs`, `header_validate.rs`, `candidate.rs`, `header_summary.rs`, `events.rs`, `rollback.rs`, `leader_check.rs`, `praos_state.rs`, `nonce.rs`, `vrf_cert.rs`, `ledger_view.rs`, `era_schedule.rs`). |

---

### `ade_crypto`

| Attribute | Value |
|-----------|-------|
| **Purpose** | Pure cryptographic verification — Praos VRF (draft-03) verify, the Ade-owned BLUE Sum6KES algorithm (`kes_sum/`), Ed25519 DSIGN verify, blake2b hashing. **MEM-OPT-UTXO-DISK S1.5 (NEW this cluster): `utxo_set_commitment` — the v2 UTxO-fingerprint primitive.** `UtxoSetCommitment` is a Ristretto255 ECMH (elliptic-curve multiset hash, `curve25519-dalek 4`): `entry(bytes) = RistrettoPoint::from_uniform_bytes(blake2b_512("ade.utxo.fp.v2.entry" ‖ canonical_txin ‖ canonical_txout))`, `commitment = identity + group_sum(entries)`, `digest = blake2b_256("ade/fp/utxo/v2" ‖ compressed_point)` — commutative, add/remove-exact-inverse, value-binding (not key-only), deterministic, golden-vector-pinned (3 FROZEN vectors). It is Ade's INTERNAL replay-contract primitive, NOT peer-facing and NOT Cardano consensus. `blake2b_256` is the hash reused by the MEM-MEASURE evidence + the MEM-OPT-OPS S2 streaming-import fingerprint. **(`ade_crypto` is 21 → 22 — the +1 is `UtxoSetCommitment`.)** |
| **Creates** | `KesError`, `KesParseError`, VRF/KES/DSIGN verify types, `Sum6` key/signature representations, **`UtxoSetCommitment`** (NEW — the v2 ECMH set commitment). 22 public types. |
| **Interprets** | VRF proofs, KES signatures + the expanded `Sum6KES` envelope, Ed25519 signatures; `blake2b_256` / `blake2b_224`; **the v2 UTxO set commitment accumulates per-entry `(canonical TxIn, canonical TxOut)` points and produces a `blake2b_256` digest of the compressed group element.** |
| **MUST NOT** | (1) **Sign** anything — verification only; signing material custody lives in the RED `ade_runtime::producer` shell (`T-KEY-01` / `ci_check_no_signing_in_blue.sh`). (2) Perform I/O / read a clock / use float. (3) Re-implement Sum6KES divergently from Haskell `cardano-base` (the cardano-crypto Rust crate is a `#[cfg(test)]` oracle only). (4) Depend on any workspace crate except `ade_types`. **(5, MEM-OPT-UTXO-DISK `DC-MEM-10`) `utxo_set_commitment` — the v2 set commitment MUST stay a NAMED, domain-separated, version-tagged construction (NOT a naive XOR/sum); commutative + add/remove-exact-inverse + value-binding; the golden vectors are FROZEN; it is INTERNAL replay-contract only and MUST NOT become peer-facing or alter the block-hash / Cardano-consensus path.** |
| **Inbound deps** | `ade_core`, `ade_ledger` (incl. the v2 `fingerprint_utxo_v2` + `IncrementalUtxoFp` consumers), `ade_plutus`, `ade_runtime` (incl. the S2 streaming-import fingerprint), `ade_node`, `ade_core_interop`, `ade_testkit`. |
| **Outbound deps** | `ade_types`, `blake2`, `ed25519-dalek`, `cardano-crypto` (`["vrf-draft03", "kes-sum"(test-only), "dsign"]`), **`curve25519-dalek` (v4 — the Ristretto255 group for `utxo_set_commitment`)**. |
| **Entry points** | `ade_crypto::vrf::*`, `ade_crypto::kes_sum::{kes_update, kes_verify, *}`, `ade_crypto::dsign::*`, `ade_crypto::hash::{blake2b_256, blake2b_224}` (`ade_crypto::blake2b::blake2b_256`), **`ade_crypto::utxo_set_commitment::UtxoSetCommitment`**. |
| **Key modules** | `vrf/`, `kes_sum/`, `dsign/`, `hash.rs`, **`utxo_set_commitment.rs`** (the v2 ECMH primitive). |

---

### `ade_ledger`

| Attribute | Value |
|-----------|-------|
| **Purpose** | The functional core (ledger half): stateless ledger rules for every era; B1 block-validity verdict `block_validity::block_validity`; B2 tx-validity + mempool admission; **the single BLUE wire-ingress chokepoint `mempool::ingress::mempool_ingress`** (reused byte-unchanged behind the MEM-MEASURE GREEN `bounded_admission` fold, `CN-MEM-01`); the BLUE producer authority; the single body-hash authority; the receive/rollback authorities; **the canonical UTxO state `utxo::{UTxOState, TxOut}` + the `UtxoStore` / `UtxoMembership` owned-lookup seam (S1, `DC-MEM-09`) + the bounded-overlay `utxo_overlay::OverlayUtxo` (S2a, `DC-MEM-07`) + the GREEN `pre_resolve` working-set (B-infra) + the versioned UTxO-fingerprint authority `fingerprint::{fingerprint (= fingerprint_v2), fingerprint_v1 (FROZEN), fingerprint_v2_with_utxo, fingerprint_utxo_v2 (the full-recompute oracle), IncrementalUtxoFp, UtxoFpCache, StaticUtxoFp} (S1.5/A.2, `DC-MEM-10`/`OP-MEM-02`)**; the snapshot/bootstrap-anchor/genesis/seed-epoch authorities; the KES-signing pre-image recipe; the closed `RecoveredAnchorPoint`. **MEM-OPT-UTXO-DISK TOUCHED this crate** — `git diff e0c77492..1b851c07 -- crates/ade_ledger/src/` shows 2 NEW files (`utxo_overlay.rs`, `pre_resolve.rs`) + ≈13 modified (`fingerprint.rs`, `utxo.rs`, `phase.rs`, `snapshot/{mod,utxo_state}.rs`, `tx_validity/phase1.rs`, the era-rule files, `lib.rs`), and the canonical-type count is **181 → 187 (+6)**. The changes are behavior-invariant EXCEPT the deliberate v1→v2 fingerprint cutover. |
| **Creates** | **187** public types. Load-bearing carried: `BlockValidityError`, `BlockVerdict` / `BlockValidityVerdict`, `LedgerState`, `UTxOState`, `TxOut`, `WalEntry` (closed: `AdmitBlock` + `SeedEpochConsensusInputsImported` + `RollBack`), `RollbackPoint` / `RollbackReason` (incl. `ForkChoiceWin`), `LedgerFingerprint`, `TxValidity*`, `MempoolState`, `ProducerTick`, `ForgedBlock`, `ServedChainSnapshot`, `PoolDistrView`, `ProtocolParameters`, `Rational`, `SeedEpochConsensusInputs` (closed; `SEED_CINPUT_SCHEMA_VERSION = 2`), `BootstrapAnchor`, `RecoveredAnchorPoint`. **NEW this cluster (the +6):** `utxo_overlay::OverlayUtxo` (Arc anchor + bounded tombstoned overlay, `MAX_OVERLAY_ENTRIES`); `pre_resolve::WorkingSet` (the GREEN resolved-view; impls `UtxoStore`); `fingerprint::StaticUtxoFp` + `StaticUtxoFpError` (closed — the constant-component fingerprint + its fail-closed reason); `fingerprint::IncrementalUtxoFp` (per-block produce/spend over the shared v2 entry bytes); `fingerprint::UtxoFpCache` (the live `track_utxo=false` cached-component carrier). The `UtxoStore` / `UtxoMembership` traits are the owned-lookup seam (the v2 set-commitment `UtxoSetCommitment` it consumes is owned by `ade_crypto`; `PrevHash` is owned by `ade_types`). |
| **Interprets** | Canonical decoded blocks/txs/certs/snapshots; `block_validity` produces the per-block verdict + post-state; `mempool_ingress` is the sole BLUE chokepoint; **`fingerprint` (= `fingerprint_v2`) computes the canonical-CBOR-over-keys ledger fingerprint, swapping ONLY the UTxO component to the v2 ECMH digest (the other 6 components + the rollup unchanged); `fingerprint_v2_with_utxo` accepts a precomputed UTxO component so the RED admission loop can supply the cached constant (`StaticUtxoFp`) on the `track_utxo=false` path; `IncrementalUtxoFp` maintains the UTxO component per validated produce/spend, proven == `fingerprint_utxo_v2` after every block; `pre_resolve::collect_required_txins` enumerates the EXACT `TxIn` set (spend ∪ collateral ∪ reference) a tx's validation can read, as a deterministic `BTreeSet`.** |
| **MUST NOT** | All carry-forward ledger prohibitions (no I/O, no clock, closed cert/governance enums, single body-hash recipe). `producer::*` fences. `rollback::materialize` (T-REC-06 / CN-STORE-07) stays the SOLE rolled-back-state authority. `wal::{event, replay}` rollback marker (DC-NODE-27 / CN-WAL-01) closed. `block_validity` stays the single block verdict authority. **(MEM-OPT-UTXO-DISK `DC-MEM-05`/`06`/`08`/`09`/`10`, `OP-MEM-02`):** the UTxO/ledger fingerprint MUST be computed by the canonical CBOR encoder over canonically-encoded keys, NEVER from a storage backend's native iteration order, AND independent of the process allocator — a storage/memory-representation change is NEVER a consensus or replay change (`DC-MEM-05`); the v2 UTxO component MUST be the NAMED domain-separated `UtxoSetCommitment` (NOT XOR/sum), version-tagged, and `IncrementalUtxoFp` MUST equal the full recompute after each block (`DC-MEM-10`); `fingerprint()` delegates to `fingerprint_v2` while `fingerprint_v1` stays FROZEN (the v1 goldens repointed at it), and the v1→v2 cutover MUST NOT change the peer-facing block-hash / `AgreementVerdict` path; the authoritative `utxo_lookup` MUST return OWNED `Option<TxOut>` (never a storage borrow) through the `UtxoStore` seam, and the owned change MUST NOT alter any verdict/fingerprint/failure-shape nor introduce a whole-`BTreeMap` clone path (`DC-MEM-09`); the in-memory overlay/changelog MUST stay within the fixed closed `MAX_OVERLAY_ENTRIES` and compact rather than grow unboundedly (`DC-MEM-07`); a compact/lazily-decoded UTxO/`TxOut` representation MUST preserve canonical bytes + ledger semantics identically (`DC-MEM-08`); **`StaticUtxoFp` MUST fail closed under `track_utxo=true` or a fingerprint-version mismatch (`StaticUtxoFpError`) — it is valid ONLY while the live path runs `track_utxo=false`, never a stale fingerprint (`OP-MEM-02`).** `recovered_anchor_point` / `seed_consensus_inputs` / `bootstrap_anchor` / `genesis_source` provenance fences (carried). |
| **Inbound deps** | `ade_testkit`, `ade_core_interop`, `ade_runtime` (incl. the S2 streaming seed-import producing `UTxOState`; the dormant redb anchor's `encode_tx_out_canonical`; the chaindb `FINGERPRINT_VERSION` store marker), `ade_node` (incl. the `StaticUtxoFp` / `UtxoFpCache` admission wiring). |
| **Outbound deps** | `ade_types`, `ade_crypto` (incl. **`utxo_set_commitment::UtxoSetCommitment`** for the v2 fingerprint), `ade_codec`, `ade_plutus`, `ade_core`, `minicbor`, `num-bigint`, `num-integer`, `num-traits`. Dev-dep: `ade_testkit`. |
| **Entry points** | `ade_ledger::block_validity::{decode_block, validate_block, block_validity, BlockValidityVerdict, DecodedBlock, check_header_position, unsigned_header_pre_image::unsigned_header_pre_image}`, `ade_ledger::state::LedgerState`, `ade_ledger::utxo::{UTxOState, TxOut, UtxoStore, UtxoMembership}`, `ade_ledger::utxo_overlay::OverlayUtxo`, `ade_ledger::pre_resolve::{collect_required_txins, collect_required_txins_block, WorkingSet}`, `ade_ledger::fingerprint::{fingerprint, fingerprint_v1, fingerprint_v2, fingerprint_v2_with_utxo, fingerprint_utxo_v2, IncrementalUtxoFp, UtxoFpCache, StaticUtxoFp, StaticUtxoFpError, FINGERPRINT_VERSION_V1, FINGERPRINT_VERSION_V2}`, `ade_ledger::tx_validity::*`, `ade_ledger::mempool::{ingress::mempool_ingress, admit::*}`, `ade_ledger::producer::*`, `ade_ledger::block_body_hash::*`, `ade_ledger::{receive, rollback, snapshot}::*` (incl. `rollback::materialize::materialize_rolled_back_state`), `ade_ledger::wal::*`, `ade_ledger::pparams::ProtocolParameters`, `ade_ledger::rational::Rational`, `ade_ledger::seed_consensus_inputs::*`, `ade_ledger::consensus_view::{PoolDistrView, PoolEntry}`, `ade_ledger::bootstrap_anchor::*`, `ade_ledger::genesis_source::*`, `ade_ledger::recovered_anchor_point::*`. |
| **Key modules** | `block_validity/`, `tx_validity/` (incl. `phase1.rs` — routed through the owned `UtxoStore` lookup), `mempool/`, `cert_classify/`, `consensus_view.rs`, `producer/`, `block_body_hash.rs`, `receive/`, `rollback/`, `snapshot/` (incl. `utxo_state.rs` — v2 encode/decode), `pparams.rs`, `rational.rs`, `wal/`, `utxo.rs` (the owned `utxo_lookup` + the `UtxoStore`/`UtxoMembership` seam), **`utxo_overlay.rs`** (NEW — `OverlayUtxo` bounded overlay), **`pre_resolve.rs`** (NEW — GREEN `collect_required_txins`/`WorkingSet`), **`fingerprint.rs`** (the v1→v2 cutover + `StaticUtxoFp`/`IncrementalUtxoFp`/`UtxoFpCache`), `phase.rs` (the owned `apply_phase_2_failure` lookup), `seed_consensus_inputs.rs`, `bootstrap_anchor/`, `genesis_source.rs`, `recovered_anchor_point.rs`, `state.rs`, `plutus_eval.rs`. |

> **GREEN sub-classification:** `ade_ledger::mempool::{policy, canonicalize}` are GREEN-by-content; **`ade_ledger::pre_resolve`** (NEW this cluster) is GREEN-by-content — the era-aware required-input enumeration + the in-memory `WorkingSet` (a `BTreeSet`/`BTreeMap`, never a `HashSet`) the RED shell resolves the on-disk anchor into BEFORE BLUE runs (so BLUE never reaches disk). **RED sub-classification:** `ade_ledger::consensus_input_extract` is RED-by-content. Each carries its own module banner.

---

### `ade_network` *(BLUE submodules)*

| Attribute | Value |
|-----------|-------|
| **Purpose** | Owns the Cardano Ouroboros mini-protocol authority — the closed wire grammar (CBOR codecs) and pure state machines for all N2N + N2C mini-protocols, plus the BLUE mux frame primitive and the producer-side server-role reducers. Owns the per-protocol tag-24 composition authorities, the `block_fetch/` grammar, and the `keep_alive/` grammar. The `tx_submission/` grammar (mini-protocol 4) is on cardano-node's real wire form — `codec::tx_submission` decodes/encodes the era-tagged txid (`TxSubmissionTxId`) + definite/indefinite array forms against a real preprod capture corpus (`DC-PROTO-11` `enforced`). **(No MEM-OPT-UTXO-DISK change to `ade_network` — BLUE submodules 111, byte-stable.)** |
| **Creates** | **111** BLUE canonical types: `mux/frame.rs` 5, `codec/` **40** (incl. the closed `ArrayHead = Definite(u64) \| Indefinite`, CN-WIRE-11, + `codec::tx_submission::TxSubmissionTxId`), `handshake/` 9, `chain_sync/` 11, `block_fetch/` 10, `tx_submission/` 5, `keep_alive/` 5, `peer_sharing/` 5, `n2c/` 21. |
| **Interprets** | Mini-protocol wire frames; `mux::frame::decode_frame` is the single frame decode authority; per-protocol `*_transition` reducers consume decoded messages. `decompose_blockfetch_block` / `decompose_rollforward_header` strip the per-protocol tag-24 composition. `decode_find_intersect_points` decodes the `FindIntersect` points list (CN-WIRE-11). `keep_alive_transition` consumes a decoded `KeepAliveMessage`. The `tx_submission` codec decodes the real era-tagged txid + indefinite reply sequences. |
| **MUST NOT** | (1) `mux::frame` — single `encode_frame`/`decode_frame` pair. (2) handshake — single `n2n_transition` + single `n2c_transition`. (3) closed `AcceptedMiniProtocol` registry. (4) no socket I/O in BLUE submodules. (5) no async/tokio in BLUE submodules. (6) depend on no workspace crate beyond `ade_codec` + `ade_types`. (7) server reducers MUST NOT split headers in parallel or depend on signing. (8) tag-24 composition (CN-WIRE-08) — composers delegate byte-level wrap/strip to the single `ade_codec` authority. (8b) `n2n_supported_for_magic` (DC-NODE-07). (8c) `encode_n2n_version_params` (CN-WIRE-10). (8d) `decode_array_head_two_form` / `ArrayHead` (CN-WIRE-11). (8e) `producer_chain_sync_serve` FindIntersect cursor (DC-PROTO-10). (8f) `keep_alive` grammar (CN-PUMP-01 / DC-PUMP-03). (8g) `codec::tx_submission` (DC-PROTO-11) — the SINGLE tx-submission2 codec; rejects a bare txid / wrong txid-hash length / unterminated indefinite sequence (fail-closed). |
| **Inbound deps** | `ade_runtime`, `ade_node`, `ade_core_interop`, `ade_testkit`. |
| **Outbound deps** | `ade_types`, `ade_codec`. No external deps in BLUE submodules. |
| **Entry points** | `ade_network::mux::frame::{encode_frame, decode_frame}`, `ade_network::handshake::{n2n_transition, n2c_transition}`, `ade_network::handshake::version_table::{N2N_SUPPORTED, n2n_supported_for_magic, encode_n2n_version_params}`, `ade_network::chain_sync::{server::*, client::*}`, `ade_network::block_fetch::{server::*, client::*}`, `ade_network::keep_alive::{keep_alive_transition, KeepAliveState, KeepAliveEvent, KeepAliveOutput, KeepAliveError}`, `ade_network::tx_submission::{transition::*, event::*}`, `ade_network::codec::{block_fetch::{compose,decompose}_blockfetch_block, chain_sync::{{compose,decompose}_rollforward_header, Point}, tx_submission::{TxSubmissionTxId, *}, keep_alive::*, primitives::{decode_array_head_two_form, ArrayHead}}`, `ade_network::{peer_sharing, n2c}::*`. |
| **Key modules** | `mux/frame.rs`, `codec/` (incl. `block_fetch.rs`, `chain_sync.rs`, `tx_submission.rs`, `keep_alive.rs`, `primitives.rs`), `handshake/` (incl. `version_table.rs`), `chain_sync/` (incl. `server.rs`), `block_fetch/` (incl. `server.rs`), `tx_submission/` (incl. `transition.rs`, `event.rs`), `keep_alive/` (incl. `transition.rs`), `peer_sharing/`, `n2c/`. |

---

### `ade_plutus`

| Attribute | Value |
|-----------|-------|
| **Purpose** | Quarantine boundary between the Ade-canonical ledger and the ported UPLC evaluator from `aiken-lang/aiken` (pinned tag `v1.1.21`). A per-script declared-`ex_units` cap closes a Plutus false-accept (`tx_eval.rs` enforces the per-script declared budget against the cost-model output; the Plutus conformance + budget-cap + eval-purity + oracle-no-false-accept gates back `CN-PLUTUS-01`/`04`). **(No MEM-OPT-UTXO-DISK change — `ade_plutus` is 9, byte-stable.)** |
| **Creates** | `PlutusScript`, `PlutusLanguage`, `EvalOutput`, `PlutusError`, `CostModels`, `DecoderMode`, `PerScriptResult`, `TxEvalResult`, `RedeemerFields`. **9** top-level types. |
| **Interprets** | UPLC scripts (Plutus V1/V2/V3) and `CostModels` CBOR; phase-two tx evaluation with the per-script declared-`ex_units` cap. `PlutusScript::from_cbor` is a named ingress chokepoint. |
| **MUST NOT** | (1) Re-export any `pallas_*` or `aiken_uplc::` type. (2) Let another BLUE crate bypass the canonical entry. (3) Activate PV11 builtins. (4) Use any BLUE-forbidden pattern. (5) Construct `PreservedCbor` outside `ade_codec`. (6, `CN-PLUTUS-01`/`04`) phase-two evaluation MUST fail closed on a script that exceeds its declared per-script `ex_units` (no false-accept), under host-environment purity (no ambient I/O / clock in the evaluator). |
| **Inbound deps** | `ade_ledger`, `ade_testkit`. |
| **Outbound deps** | `ade_types`, `ade_crypto`, `ade_codec`, `aiken_uplc` (git, tag `v1.1.21`), `pallas-primitives` (internal-only). |
| **Entry points** | `ade_plutus::eval_tx_phase_two`, `ade_plutus::tx_eval::*`, `ade_plutus::evaluator::*`, `ade_plutus::cost_model::*`. |
| **Key modules** | `evaluator.rs`, `cost_model.rs`, `script_context.rs`, `script_verdict.rs`, `tx_eval.rs`. |

---

### `ade_types`

| Attribute | Value |
|-----------|-------|
| **Purpose** | Owns the canonical Cardano domain vocabulary — primitives, era enum, address forms, era-tagged tx bodies/outputs/certificates, governance types — the lingua franca for every other crate. `CardanoEra` carries `is_praos()`. Owns the closed sum `PrevHash = Genesis \| Block(Hash32)`. `TxIn` / `Coin` are the canonical UTxO key/value: the MEM-OPT-OPS S2 streaming import builds the `BTreeMap<TxIn, TxOut>` over them; the MEM-OPT-UTXO-DISK v2 set commitment binds the canonical `TxIn` encoding (so the fingerprint is over canonical `TxIn` keys, allocator/order-independent); the dormant redb key (`utxo_key`) packs `TxIn` into a fixed-width `txid[32] ‖ BE-u32(index)`. **(No `ade_types` change in the span — 82 byte-stable.)** |
| **Creates** | `CardanoEra` (with `is_praos()`), `SlotNo`, `BlockNo`, `EpochNo`, `Hash28`, `Hash32`, `Coin`, `Lovelace`, `NetworkId`, `Nonce`, `TxIn`, `RewardAccount`, `PoolId`, `Address`, `Credential`, `Certificate`, `ConwayCert`, `DRep`, `GovAction`, `OperationalCert`, per-era tx-body/tx-out/witness wrappers, `ShelleyBlock`/`ShelleyHeader`/`VrfData`/`ProtocolVersion`/`PrevHash`. 82 public types. |
| **Interprets** | None — produce-only. |
| **MUST NOT** | (1) Construct or decode `PreservedCbor`. (2) Use any BLUE-forbidden pattern. (3) Depend on any workspace crate. (4) Add open/extensible variants to closed enums without a versioned gate (`CardanoEra` discriminants closed: Byron=0 … Conway=7; `PrevHash` is a closed 2-variant `Genesis \| Block(Hash32)` sum, never a flat `Hash32`). |
| **Inbound deps** | Every other workspace crate. |
| **Outbound deps** | None. |
| **Entry points** | `ade_types::CardanoEra` (incl. `is_praos`), `ade_types::{Hash32, SlotNo, Hash28, BlockNo, EpochNo, Nonce}`, `ade_types::tx::{Coin, TxIn}`, `ade_types::conway::{tx::ConwayTxBody, cert::*, governance::*}`, `ade_types::shelley::block::{OperationalCert, ProtocolVersion, ShelleyHeader, ShelleyBlock, VrfData, PrevHash}`. |
| **Key modules** | `primitives.rs`, `era.rs`, `tx.rs`, `address/`, `byron/`, `shelley/`, `allegra/`, `mary/`, `alonzo/`, `babbage/`, `conway/`. |

---

## GREEN Modules — Deterministic Glue

> Deterministic, non-authoritative. May depend on BLUE; must not affect authoritative outputs. GREEN sub-trees inside the RED `ade_runtime` and `ade_node` crates carry a `//! GREEN …` banner (or the deterministic `// Core Contract:` banner) + the same deny attributes and are CI-gated for purity. **MEM-OPT-UTXO-DISK added the GREEN `ade_ledger::pre_resolve` working-set** (the era-aware required-input enumeration — documented as a sub-classification note in the BLUE `ade_ledger` entry above, since it is a sub-tree of a BLUE crate). The carried GREEN `mem_measure::{bounded_admission, evidence}` cores (MEM-MEASURE / MEM-OPT-OPS) and the PHASE4-N-AO GREEN `ade_node` modules (`candidate_aggregator`, `lca_walk`, `selector_state`, `post_switch_continuity`, `fork_switch::prevalidate_branch`) are carried verbatim.

### `ade_node::candidate_aggregator` *(GREEN by content — PHASE4-N-AO S2, `DC-NODE-35`)*

| Attribute | Value |
|-----------|-------|
| **Purpose** | The BLUE-safe candidate construction core: a PURE projection that, given a fork anchor + the chain-dep at that anchor + a peer's candidate header inputs, validates each header through the BLUE `validate_and_apply_header` authority and assembles a `CandidateFragment` for `select_best_chain`. The cluster's load-bearing entry-gate module — it keeps the SELECT from becoming "look over the peer's shoulder." (Unchanged across the MEM-OPT-UTXO-DISK span.) |
| **Creates** | `CandidateBuildError` (closed: `HeaderInvalid { index, error } \| EmptyHeaders`). Produces / orders the BLUE-owned `CandidateFragment`. Not canonical-counted (GREEN). |
| **MUST NOT** | Affect authoritative outputs; introduce nondeterminism (no wall-clock / rand / float / HashMap). **`DC-NODE-35`:** mint a `ValidatedHeaderSummary` — every summary in a fragment MUST be `validate_and_apply_header` output, NEVER a peer-trusted minted summary (the forbidden `ade_core_interop::follow` shape) and NEVER a raw followed-peer tip; a header that does not fully validate produces NO fragment (`HeaderInvalid`); zero headers fail closed (`EmptyHeaders`). Perform NO store read, NO materialize, NO selection, NO block-fetch, NO WAL, NO durable mutation. |
| **Inbound deps** | `ade_node::node_lifecycle` (`dispatch_competing_fork_choice`). |
| **Outbound deps** | BLUE `ade_core::consensus::{candidate, header_validate, header_summary, events, praos_state, ledger_view, era_schedule, errors}`; `ade_types::BlockNo`. No tokio (GREEN). |
| **Entry points** | `ade_node::candidate_aggregator::{build_candidate_fragment, assemble_candidate_set, CandidateBuildError}`. |

> **GREEN classification (OQ-AO-6 resolved):** promotable to BLUE on demand, never demotable to RED — BLUE-adjacent pure semantic projection that constructs nothing the BLUE authority did not already validate.

### `ade_node::lca_walk` *(GREEN by content — PHASE4-N-AO S7, `DC-NODE-38`; retention S13 `DC-NODE-40`)*

| Attribute | Value |
|-----------|-------|
| **Purpose** | The last-common-ancestor fork-anchor walk: `walk_to_durable_lca` walks a live competing branch's preserved parent links back to the durable LCA (a `ChainDb`-stored block) so a multi-block competing branch is eligible for SELECT. PURE-deterministic over the per-peer branch cache + the S13 rollback-retention + read-only `ChainDb` lookups. (Unchanged across the MEM-OPT-UTXO-DISK span.) |
| **Creates** | `CachedHeader { header, prev_hash, block_hash }`, `LcaError` (closed: `NoDurableAncestorWithinK \| BranchGap \| ExceededK \| CacheSelfBindingViolation`), `LcaResult { anchor_slot, anchor_hash, headers }`. Not canonical-counted (GREEN). |
| **MUST NOT** | Affect authoritative outputs; introduce nondeterminism (`BTreeMap`-keyed, arrival-order-independent); mutate durable state (read-only `ChainDb`). **`DC-NODE-38`:** treat the branch cache as authority — each entry self-binds (`block_hash == map key`) or fails closed; the LCA is authoritative ONLY when `ChainDb` confirms slot AND hash (`DC-NODE-29`); k-bounded by BLOCK DEPTH; over-k / branch-gap / no-durable-LCA / cache-self-binding-violation / lying-parent-link all fail closed. **`DC-NODE-40` (S13):** a retained rolled-back block is walk-visible EVIDENCE only — never the LCA anchor / durable authority / a rollback target; k-bounded + self-binding; never papers over a genuine gap. |
| **Inbound deps** | `ade_node::node_lifecycle`; `ade_node::fork_switch` (imports `LcaError`). |
| **Outbound deps** | `ade_core::consensus::header_summary::HeaderInput`; `ade_runtime::chaindb::ChainDb` (read-only `get_block_by_hash`); `ade_types::shelley::block::PrevHash`; `ade_types::{Hash32, SlotNo}`; `std::collections::BTreeMap`. No tokio. |
| **Entry points** | `ade_node::lca_walk::{walk_to_durable_lca, CachedHeader, LcaError, LcaResult}`. |

### `ade_node::selector_state` *(GREEN by content — PHASE4-N-AO S3, `DC-NODE-36`; winner_tip S6)*

| Attribute | Value |
|-----------|-------|
| **Purpose** | The selector-state projection for live fork-choice dispatch: `project_tiebreaker` derives a block's `TiebreakerView` from Ade's own already-admitted durable tip bytes so the current tip and the S2-validated candidates compare on the same `praos_leader_value` basis. Owns the provisional fork-choice carrier types S3 emits toward S4. PURE. (Unchanged across the MEM-OPT-UTXO-DISK span.) |
| **Creates** | `SelectorProjectError` (closed: `UnsupportedTpraosTip`), `ForkAnchor { slot, hash, block_no }`, `PendingForkSwitch { fork_anchor, winning_peer, winning_candidate, winner_tip }`. Not canonical-counted (GREEN). |
| **MUST NOT** | Affect authoritative outputs; introduce nondeterminism. **`DC-NODE-36`:** a `PendingForkSwitch` is provisional — S3 sets it + the `DC-NODE-28` forge fence but applies NOTHING; the durable chain is preserved until S4 fetches + validates the replacement; `winner_tip` is a fetch endpoint ONLY; `project_tiebreaker` projects from Ade's OWN durable tip bytes (a legacy TPraos tip fails closed); the `ForkAnchor` binds Ade's durable STORED slot+hash (`DC-NODE-29`), never peer-supplied. |
| **Inbound deps** | `ade_node::node_lifecycle`; `ade_node::fork_switch`. |
| **Outbound deps** | BLUE `ade_core::consensus::{candidate, events, header_summary, praos_leader_value}`; `ade_types::{BlockNo, Hash32, SlotNo}`. No tokio. |
| **Entry points** | `ade_node::selector_state::{project_tiebreaker, ForkAnchor, PendingForkSwitch, SelectorProjectError}`. |

### `ade_node::post_switch_continuity` *(GREEN by content — PHASE4-N-AO S10, `DC-EVIDENCE-05`)*

| Attribute | Value |
|-----------|-------|
| **Purpose** | The replayable post-switch branch-continuity verdict: a pure, total, deterministic reducer over the closed convergence-evidence transcript that, after a `ForkChoiceWin` adoption at tip X, classifies Ade's OWN validated admitted-block lineage into a closed `PostSwitchContinuity` verdict. The SINGLE implementation behind both the hermetic replay test and the live CE-AO-6 release gate. (Unchanged across the MEM-OPT-UTXO-DISK span.) |
| **Creates** | `EventView`, `PostSwitchContinuity` (closed: `ContinuesSelectedBranch { admitted_after_switch, tip_slot, tip_hash } \| Diverged \| BrokenLineage \| DanglingForkChoiceWin \| InsufficientEvidence`). Not canonical-counted (GREEN). |
| **MUST NOT** | Affect authoritative outputs; introduce nondeterminism (replay-equivalent). **`DC-EVIDENCE-05`:** be consumed by `select_best_chain` / `walk_to_durable_lca` / `apply_fork_switch` / `pump_block` / the forge fence (OBSERVE-ONLY); read anything but Ade's OWN admitted-block lineage (the peer's tip is NEVER an input); emit a free-form-string verdict (closed sum). |
| **Inbound deps** | `ade_node::bin::post_switch_continuity`; CI (`ci_check_post_switch_convergence_window.sh`); the hermetic replay tests. |
| **Outbound deps** | the closed `admission_log` event vocabulary; `std` only. No tokio. |
| **Entry points** | `ade_node::post_switch_continuity::{derive_post_switch_continuity, evaluate_release_window, PostSwitchContinuity, EventView}`; the bin `ade_node::bin::post_switch_continuity`. |

### `ade_node::fork_switch::prevalidate_branch` *(GREEN-pure prove core — PHASE4-N-AO S4, `DC-NODE-37`)*

> The `fork_switch` module is **split-color**: the `prevalidate_branch` prove core (+ the closed outcome/error types + `fork_switch_fence_resolved` + `map_lca_error` + `range_refetch_should_retry`) is GREEN-pure and BLUE-reused; its `BranchBodySource` RED seam + the `NullBranchBodySource` / `PrefetchedBranchBodies` impls are RED (documented in the RED `ade_node` entry). Summarized here once. (Unchanged across the MEM-OPT-UTXO-DISK span.)

| Attribute | Value |
|-----------|-------|
| **Purpose** | The fork-switch prove core. `prevalidate_branch` is PURE (no I/O, no store reads, no durable mutation): given the already-fetched bodies + the already-materialized read-only anchor state, it (1) BINDS each body's re-derived header to its S3-selected `ValidatedHeaderSummary`, (2) LINKS `prev_hash` from the durable fork anchor, and (3) folds BLUE `block_validity` over the bodies — three proofs, all STRICTLY BEFORE the RED driver's irreversible `commit_rollback`. Hosts the closed S11/S14 outcome types + the fence-resolution predicate. |
| **Creates** | `BranchProofError` (closed), `ProvenBlock`, `ProvenBranch`, `ForkSwitchOutcome` (closed), `MissingBridgeReason` (closed S11), `RangeRefetch`, `RangeRefetchOutcome` (closed S14), `PostSwitchFollow`, `MAX_RANGE_REFETCH_ATTEMPTS = 3`. Not canonical-counted (RED `ade_node` types). |
| **MUST NOT** | (GREEN-pure prove core) Affect authoritative outputs except via the BLUE `block_validity` fold it reuses; perform I/O / read the store / mutate durable state. **`DC-NODE-37`:** the prove MUST STRICTLY PRECEDE `commit_rollback`; any non-`Valid` verdict, body↔header mismatch, broken parent link, empty/short branch, or unreachable anchor MUST fail closed leaving ChainDb + LedgerState + PraosChainDepState byte-unchanged; trust NOTHING peer-asserted. `fork_switch_fence_resolved` clears the forge fence ONLY on a fully-resolved state. `MissingBridgeReason` / `RangeRefetchOutcome` are closed discriminants, no free-form strings. |
| **Inbound deps** | `ade_node::node_lifecycle`. |
| **Outbound deps** | BLUE `ade_core::consensus::*`; `ade_crypto::vrf::VrfOutput`; BLUE `ade_ledger::block_validity::*` + `state::LedgerState`; `ade_types::shelley::block::PrevHash`; `ade_types::{Hash32, SlotNo}`; the GREEN `ade_node::lca_walk::LcaError` + `selector_state::{ForkAnchor, PendingForkSwitch}`. No tokio in the prove core. |
| **Entry points** | `ade_node::fork_switch::{prevalidate_branch, ProvenBranch, ProvenBlock, BranchProofError, ForkSwitchOutcome, MissingBridgeReason, map_lca_error, RangeRefetch, RangeRefetchOutcome, PostSwitchFollow, MAX_RANGE_REFETCH_ATTEMPTS, range_refetch_should_retry, fork_switch_fence_resolved, BranchBodySource, NullBranchBodySource, PrefetchedBranchBodies, FetchError}`. |

### `ade_node::mem_measure` *(GREEN cores `bounded_admission` + `evidence`; RED `rss_sampler`; GREEN/RED `runner` — MEM-MEASURE A1, owned samplers MEM-OPT-OPS S3)*

> The `mem_measure` module is **split-color by file**, all inside the RED `ade_node` crate (`mod.rs` banner is the authority). The GREEN cores are summarized here; the RED `rss_sampler` is repeated in the RED `ade_node` entry's sub-classification. (Unchanged across the MEM-OPT-UTXO-DISK span.)

| Attribute | Value |
|-----------|-------|
| **Purpose** | The memory-measurement substrate. **GREEN `bounded_admission`** is the deterministic bounded inbound-admission fold that fronts the BLUE `mempool_ingress` (`CN-MEM-01`): `replay_bounded_ingress_trace` admits within the closed non-configurable caps `MAX_INBOUND_ADMISSION_BYTES` / `MAX_INBOUND_ADMISSION_COUNT` and sheds the rest with a closed `ShedReason` (`DC-MEM-07`). **GREEN `evidence`** is the load-bearing measurement discipline: `MemEvidenceRecord` pairs a memory measurement (RED RSS observations) with a REPLAY FINGERPRINT + `ReplayVerdict`; `validate_evidence` returns `EvidenceDefect::VerdictNotAgreed` if the replay verdict is not `Agreed`. MEM-OPT-OPS S3 extended the record/summary with the OWNED-footprint fields, all observational. The GREEN/RED `runner` drives the GREEN workload under RED sampling and emits a paired record. |
| **Creates** | GREEN: `BoundedOutcome` / `ShedReason` (closed) + `MAX_INBOUND_ADMISSION_BYTES` / `MAX_INBOUND_ADMISSION_COUNT` (`bounded_admission`); `MemEvidenceRecord`, `ReplayVerdict` (closed), `EvidenceDefect` (closed) + `fingerprint_hex` / `pair_replay` / `validate_evidence` (`evidence`). RED: `RssSampleKib`, `RssWindow` (`rss_sampler`). None canonical-counted (all in the RED `ade_node` crate). |
| **MUST NOT** | Affect authoritative outputs; introduce nondeterminism (no wall-clock / rand / float / HashMap; the percentile math is integer nearest-rank). **`DC-MEM-06`/`OP-MEM-01`/`OP-MEM-02`:** let an RSS / owned-footprint magnitude enter a fingerprint, verdict, validator pass/fail, or any authoritative comparison (RSS is release-tier evidence ONLY — the run's REPLAY VERDICT, not RSS, decides validity); accept a measurement whose replay verdict is not `Agreed` as valid evidence; grow the bounded inbound working set past the closed caps (`DC-MEM-07`); overstate semantic truth (a low-memory or `lagging` run is not "success"). The `bounded_admission` fold MUST gate inbound bytes BEFORE the BLUE `mempool_ingress`, never inside / bypassing it. |
| **Inbound deps** | `ade_node::{main, node_lifecycle, admission::{runner, bootstrap}, convergence_evidence}` (the runner seam + the owned-sampler taps); the hermetic mem-measure tests + the `ci_check_mem_*` gates. |
| **Outbound deps** | GREEN cores: BLUE `ade_ledger::mempool::ingress::mempool_ingress` (reused unchanged, behind the bounded fold) + `ade_crypto::blake2b::blake2b_256` + `serde`; RED `rss_sampler`: `std::fs` (`/proc/self/status`, `/proc/self/smaps_rollup`) only. No tokio in the pure cores. |
| **Entry points** | `ade_node::mem_measure::{replay_bounded_ingress_trace, BoundedOutcome, ShedReason, MAX_INBOUND_ADMISSION_BYTES, MAX_INBOUND_ADMISSION_COUNT, validate_evidence, pair_replay, fingerprint_hex, MemEvidenceRecord, ReplayVerdict, EvidenceDefect, sample_vm_rss_kib, sample_vm_hwm_kib, sample_rss_anon_kib, sample_private_dirty_kib, RssSampleKib, RssWindow, run_hermetic_bounded_ingress_measurement}`. |
| **Key modules** | `mem_measure/mod.rs`, `bounded_admission.rs` (GREEN), `evidence.rs` (GREEN), `rss_sampler.rs` (RED — `sample_rss_anon_kib` / `sample_private_dirty_kib`), `runner.rs` (GREEN/RED seam). |

> **GREEN classification (load-bearing):** `mem_measure::{bounded_admission, evidence}` are **promotable to BLUE on demand** (pure deterministic folds over canonical inputs), never demotable to RED. The RSS magnitudes they CARRY are RED evidence; the verdict logic that decides validity is the GREEN fingerprint pairing, never the magnitudes.

### `ade_node::convergence_evidence` *(GREEN by content — PHASE4-N-AJ; fork-choice + range-refetch evidence PHASE4-N-AO S9/S14; owned-memory evidence MEM-OPT-OPS S3)*

| Attribute | Value |
|-----------|-------|
| **Purpose** | The GREEN closed-vocabulary convergence-evidence sink for the live Participant rollback-follow + SELECT path. `ConvergenceEvidenceSink` writes the closed `AgreementVerdict` vocabulary (`block_received` / `block_admitted` / `agreement_verdict`) + the closed fork-choice + range-refetch event sequence to a dedicated `--convergence-evidence-path` JSONL sink as an emit-only side-output. The MEM-OPT-OPS S3 owned-footprint fields on the `memory_measure` / `memory_summary` points are observational, never authority. (Unchanged across the MEM-OPT-UTXO-DISK span.) |
| **Creates** | `ConvergenceEvidenceSink` (over a `Box<dyn Write>`), `ConvergenceEvidence` (the fingerprint-bound emitter), `EvidenceEmitResult { Written \| Disabled \| FailedAndPoisoned }` (closed). The closed event vocabulary + `ForkChoiceResult` + `ForkChoiceEvidenceFailure` live in `admission_log::event`. Not canonical-counted (GREEN). |
| **MUST NOT** | Affect authoritative outputs; halt authority on a write failure (`FailedAndPoisoned` never halts the authority path); **`DC-EVIDENCE-04`/`DC-NODE-41`:** add an open/wildcard event variant to the closed vocabulary; emit a free-form error string; be CONSUMED by `select_best_chain` / `walk_to_durable_lca` / `apply_fork_switch` / the forge fence (observe-only); overstate semantic truth (`lagging` is evidence-only). **(MEM-OPT-OPS S3, `OP-MEM-02`)** let any `*_kib` memory field gate selection / admission / a verdict — the owned + gross memory numbers are OBSERVE-ONLY, never read by any authority path, and RSS magnitude never gates the run's replay verdict. |
| **Inbound deps** | `ade_node::node_lifecycle` (the dispatch decide-half + the relay-loop apply-half emit taps); `ade_node::admission::runner`. |
| **Outbound deps** | the closed `ade_node::admission_log::{AdmissionLogEvent, AdmissionLogWriter, ForkChoiceResult, ForkChoiceEvidenceFailure}` vocabulary; the reused `AgreementVerdict` verdict vocabulary; the RED `mem_measure::rss_sampler::{sample_rss_anon_kib, sample_private_dirty_kib, sample_vm_rss_kib, sample_vm_hwm_kib}`; `ade_types::Hash32`; `std::io::Write`. |
| **Entry points** | `ade_node::convergence_evidence::{ConvergenceEvidenceSink, ConvergenceEvidence, EvidenceEmitResult}`. |

### `ade_network::session` *(GREEN by content — PHASE4-N-L; outbound segmentation N-AB)*

| Attribute | Value |
|-----------|-------|
| **Purpose** | The deterministic GREEN session reducer for the Cardano Ouroboros mux — `step` is the SOLE pure fn reducing `(SessionState, ByteChunkIn) -> Result<(SessionState', Vec<SessionEffect>), SessionError>` (CN-SESS-03), composing the BLUE `mux::frame` + `handshake::n2n_transition` + the closed `AcceptedMiniProtocol` registry + per-mini-protocol frame demux/reassembly (INBOUND one `DeliverPeerFrame` per COMPLETE CBOR item; OUTBOUND `handle_outbound` SEGMENTS a payload > `MAX_PAYLOAD`, fail-closed above `MAX_OUTBOUND_PAYLOAD_BYTES = 16 MiB`). GREEN-by-content. (Unchanged across the span.) |
| **Creates** | `SessionState`, `ConnectedState`, `HandshakeProgress`, `SessionEffect` (closed), `SessionError` (closed — incl. `ReassemblyBufferOverflow` + `OutboundPayloadTooLarge`), `AcceptedMiniProtocol` (closed registry — GREEN), `ByteChunkIn`, `HandshakeRole`, `FrameBuffer`. None canonical-counted. |
| **MUST NOT** | Affect authoritative outputs; introduce nondeterminism. **CN-SESS-03:** `step` is the SOLE reduction entry. **CN-SESS-01/02:** frame encode/decode through the single BLUE `mux::frame`; handshake through the single `n2n_transition`. **DC-SESS-01:** a `Handshaking` session MUST NOT deliver any non-handshake frame. **CN-SESS-04:** one accumulating buffer per `AcceptedMiniProtocol`. **CN-SESS-05:** an outbound payload > `MAX_PAYLOAD` MUST be SEGMENTED (`concat(segments) == payload`; fail closed above 16 MiB). |
| **Inbound deps** | `ade_runtime::network::{mux_pump, n2n_dialer, n2n_listener}`, `ade_runtime::admission::wire_pump`, `ade_node::wire_only`, `ade_network::mux::transport`. |
| **Outbound deps** | BLUE `ade_network::{mux::frame, handshake}`; `ade_codec`; `ade_types`. No tokio (GREEN). |
| **Entry points** | `ade_network::session::{step, FrameBuffer, SessionState, ConnectedState, HandshakeProgress, SessionEffect, SessionError, AcceptedMiniProtocol, ByteChunkIn, HandshakeRole}`, `ade_network::session::handshake_driver::{run_n2n_handshake_initiator, run_n2n_handshake_responder}`. |
| **Key modules** | `session/core.rs`, `session/state.rs`, `session/demux.rs`, `session/event.rs`, `session/handshake_driver.rs`. |

> **GREEN classification (load-bearing):** promotable to BLUE on demand, never demotable to RED.

### `ade_runtime::seed_consensus_merge` *(GREEN by content — PHASE4-N-F-A; eta0 threaded N-F-G-N)*

| Attribute | Value |
|-----------|-------|
| **Purpose** | The deterministic, no-I/O merge transform lifting a verified-bootstrap `LiveConsensusInputsCanonical` plus the minted anchor fingerprint + seed epoch into the BLUE single-map `SeedEpochConsensusInputs`, carrying the recovered Praos `epoch_nonce` (eta0) into the BLUE record (T-REC-04). (Unchanged across the span.) |
| **Creates** | `SeedConsensusMergeError` (closed: `PoolMissingVrfKeyhash` / `PoolMissingStake`). Not canonical-counted (GREEN). |
| **MUST NOT** | Affect authoritative outputs. Introduce nondeterminism (`BTreeMap` only). Default-fill on a provenance gap. Carry the recovered eta0 as anything but verbatim (DC-CINPUT-03). |
| **Inbound deps** | `ade_runtime::{genesis_bootstrap, mithril_bootstrap}`. |
| **Outbound deps** | `ade_ledger::{consensus_view::PoolEntry, seed_consensus_inputs::SeedEpochConsensusInputs}`, `ade_runtime::consensus_inputs::LiveConsensusInputsCanonical`, `ade_core::consensus::praos_state::Nonce`, `ade_types`. |
| **Entry points** | `ade_runtime::seed_consensus_merge::{merge_seed_epoch_consensus_inputs, SeedConsensusMergeError}`. |

---

### `ade_runtime` GREEN-by-content sub-trees (carried + MEM-OPT-OPS S2)

> All carry a `//! GREEN …` banner inside the RED `ade_runtime` crate, the BLUE deny attributes, and a purity CI gate. Each is "promotable to BLUE on demand, never demotable to RED" unless noted.

| Sub-tree | Purpose | MUST NOT | CI gate |
|---|---|---|---|
| `producer::self_accepted_handoff` *(N-F-G-B)* | GREEN constructor-fenced carrier for a BLUE self-accepted forged block. | Carry a non-BLUE-self-accepted artifact; re-derive; affect authoritative outputs. | `ci_check_served_chain_handoff_fence.sh` |
| `consensus_inputs::protocol_params` *(N-F-G-A)* | GREEN cardano-cli pparams JSON → BLUE `ProtocolParameters` by integer arithmetic. | Use any float path; affect authoritative outputs. | `ci_check_recovered_ledger_pparams_sourced.sh` |
| `forward_sync::reducer` *(N-Y; recovered_anchor N-AK; recovered_eta0 N-AN)* | GREEN forward-sync lifecycle reducer; `AdmitPlan::durable` is the sole `AdvanceTip` emitter. | Emit `AdvanceTip` before durability (DC-SYNC-01); affect authoritative outputs; carry `recovered_eta0` as anything but verbatim. | `ci_check_forward_sync_chokepoint_only.sh`, `ci_check_rollback_materialize_eta0.sh` |
| `producer::coordinator` *(N-Q)* | Pure coordinator for live producer-mode. | Own/store private signing material (CN-PROD-02); affect authoritative outputs. | `ci_check_producer_coordinator_no_secrets.sh` |
| `producer::producer_log` *(N-Q, ext. N-W)* | Closed-vocabulary producer evidence log. | Add an open variant; emit `BlockServed` for a non-served block (CN-PROD-04). | `ci_check_operator_evidence_manifest_schema.sh` |
| `producer::chain_evolution` *(N-T; N-F-G-J)* | Linear `ChainEvolution` typestate; `prev_hash() -> PrevHash`. | Mint `AcceptedBlock`; advance on disagreement; propose an all-zero genesis `Hash32` (CN-WIRE-09). | `ci_check_prevhash_single_wire_authority.sh` |
| `clock` *(N-K; N-F-G-A)* | GREEN clock trait + deterministic impl + pure slot projections. | `DeterministicClock` reads no wall-clock; `checked_millis_to_slot` uses no float. | `ci_check_clock_seam.sh` |
| `orchestrator::{mod, event, state, core}` *(N-K)* | GREEN core reducer + closed-vocabulary event + state. | tokio imports in core; open event vocabulary. | `ci_check_orchestrator_core_purity.sh` |
| `rollback::{cadence, …}` *(N-I/N-J/N-K)* | GREEN rollback adapter glue + snapshot cadence + persistent cache/writer. | Parallel snapshot cadence; affect authoritative rollback state. | `ci_check_persistent_writer_no_parallel_cadence.sh`, `ci_check_snapshot_cadence_purity.sh` |
| `seed_import` *(N-M-A; STREAMING MEM-OPT-OPS S2)* | Single cardano-cli JSON UTxO dump → canonical seed entries. **S2: `import_cardano_cli_json_utxo` is STREAMING (a `CanonicalUtxoSink` serde `Visitor` over `Deserializer::from_reader`), byte-identical to the retained whole-buffer oracle `_from_bytes`; `DuplicateTxIn` fail-closed; removes the ~6.8 GB import peak.** | Construct semantic types bypassing canonical decoders; **(S2, `DC-MEM-06`) produce a fingerprint that depends on parse/iteration order or the allocator — the streaming + whole-buffer paths MUST agree byte-for-byte on identical-by-canonical-key inputs, and ANY canonical-`TxIn` duplicate fails closed (never an order-dependent survivor).** | `ci_check_seed_import_closure.sh`, `ci_check_seed_import_full_preprod_support.sh`, `ci_check_mem_opt_s2_import_peak.sh` |
| `consensus_inputs` *(N-M-C; ext. N-F-G-A; `set`-snapshot fix)* | Operator-extracted `LiveConsensusInputs` importer + pparams parser. *(Leader-election stake sourced from the `go`/`set` snapshot, not stake-distribution.)* | Treat the peer as runtime authority; overstate semantic truth. | `ci_check_live_consensus_inputs_closure.sh`, `ci_check_live_consensus_inputs_fingerprint.sh`, `check_ade1_leader_stake_active.sh` |
| `admission::*` (GREEN reducer half + reused dial/pump) *(N-M-C; reused N-F-G-C; tag-24 N-F-G-O; rollback preserve N-AI; keep-alive N-AM; raw-frame tolerance)* | GREEN admission verdict/agreement reducer; the RED `admission::{dial_for_admission, run_admission_wire_pump}` reused by the live `--mode node` feed. | Emit RED verdicts; skip reference scripts; treat `lagging` as success. | `ci_check_admission_runner_closure.sh`, `ci_check_admission_no_red_verdicts.sh`, `ci_check_lagging_is_evidence_only.sh`, `ci_check_admit_replay_equivalence.sh`, `ci_check_keep_alive_wire_only.sh` |

> **Note on `bootstrap` (the `bootstrap_initial_state` authority, N-K; eta0 N-F-G-N; live-follow start N-AK; restore-before-materialize reorder N-AN; **`StaticUtxoFp` + `ade_mem_diag` toggle MEM-OPT-UTXO-DISK A.2/S0**):** carries a `//! GREEN single bootstrap authority` banner but also hosts the **RED** A3b warm-start sidecar restore. **The `ade_node` bootstrap (NOT `ade_runtime`) is where the live `track_utxo=false` path computes the `StaticUtxoFp` once then `drop`s the static UTxO, and where the `ADE_MEM_PHASE_DIAGNOSTIC`-gated `ade_mem_diag` forced collect lives — documented in the RED `ade_node` entry below.** The `ade_runtime::bootstrap` shape is unchanged by MEM-OPT-UTXO-DISK. |

---

### `ade_node` GREEN-by-content sub-trees (carried)

| Sub-tree | Purpose | MUST NOT | CI gate |
|---|---|---|---|
| `forge_intent` *(N-F-F)* | GREEN pure tri-state forge-intent classifier. | Observe key contents; carry a third variant / secret byte. | `ci_check_forge_intent_closed.sh` |
| `run_loop_planner` *(N-F-D; venue policy N-AG)* | GREEN pure lifecycle planner (closed `LoopStep`/`ForgeSlotStatus`/`VenuePolicy`). | Reference `SlotNo`; return a non-closed `LoopStep`; add an open `VenuePolicy` arm. | `ci_check_loop_planner_closed.sh`, `ci_check_single_producer_loop_continuation.sh` |
| `node_sync::forge_epoch_admission` *(N-F-G-A)* | GREEN pure epoch-boundary guard over the closed `ForgeEpochAdmission`. | Fabricate the candidate epoch; run after leadership. | `ci_check_node_forge_single_epoch_fail_closed.sh` |
| `live_log::{sched_event, sched_writer}` *(N-F-G-J; forge-base N-AH)* | GREEN closed emit-only feed/forge scheduling-event vocabulary `NodeSchedEvent`. | Alter control flow; add an open variant; be READ by any authority path. | `ci_check_node_sched_events_emit_only.sh`, `ci_check_live_transcript_forge_base.sh` |
| `ba02_evidence` *(N-F-C)* | GREEN BA-02 evidence correlator. | Treat a weaker signal as acceptance; recompute the forged hash. | `ci_check_ba02_evidence_closed.sh`, `ci_check_ba02_evidence_manifest_schema.sh` |
| `convergence_evidence` *(N-AJ; fork-choice N-AO S9/S14; owned-memory MEM-OPT-OPS S3)* | GREEN closed-vocabulary convergence-evidence sink. *(Full entry above.)* | Affect authoritative outputs; add an open variant; be consumed by selection/walk/apply/fence; let a `*_kib` field gate any authority. | `ci_check_convergence_evidence_vocabulary_closed.sh`, `ci_check_convergence_evidence_emit_only.sh`, `ci_check_fork_choice_evidence_closed.sh` |
| `mem_measure::{bounded_admission, evidence}` *(MEM-MEASURE A1; owned MEM-OPT-OPS S3)* | GREEN bounded inbound-admission fold + memory-evidence record/validator/replay-pairing. *(Full entry above; RED `rss_sampler` below.)* | Let RSS / owned-footprint enter a fingerprint/verdict/validator; accept a non-`Agreed` replay as valid evidence; grow the inbound set past the closed caps (DC-MEM-06/07; OP-MEM-01/02). | `ci_check_bounded_inbound_admission.sh`, `ci_check_mem_measure_evidence.sh`, `ci_check_mem_compare_evidence.sh`, `ci_check_mem_opt_s2_import_peak.sh`, `ci_check_mem_opt_s3_owned.sh` |
| `candidate_aggregator` *(N-AO S2)* | GREEN BLUE-safe candidate construction. *(Full entry above.)* | Mint a `ValidatedHeaderSummary`; store read / materialize / selection / mutation (DC-NODE-35). | `ci_check_candidate_construction_validated.sh` |
| `lca_walk` *(N-AO S7; retention S13)* | GREEN LCA fork-anchor walk. *(Full entry above.)* | Treat the cache/retention as authority; anchor on a non-durable block; exceed the block-depth k (DC-NODE-38/40). | `ci_check_lca_anchor_walk.sh`, `ci_check_rollback_retention_evidence.sh` |
| `selector_state` *(N-AO S3)* | GREEN selector-state projection. *(Full entry above.)* | Apply a `PendingForkSwitch` in S3; bind the `ForkAnchor` to peer data (DC-NODE-36). | `ci_check_live_selector_dispatch.sh` |
| `fork_switch::prevalidate_branch` *(N-AO S4; S11/S14 types)* | GREEN-pure fork-switch prove core + closed outcome/error types. *(Full entry above; RED seam below.)* | Commit before prove; abandon the durable chain on a failed/incomplete branch (DC-NODE-37/39/41). | `ci_check_fork_switch_never_abandons.sh`, `ci_check_missing_bridge_fail_closed.sh`, `ci_check_missing_bridge_refetch.sh` |
| `post_switch_continuity` *(N-AO S10)* | GREEN replayable post-switch branch-continuity reducer. *(Full entry above.)* | Be consumed by any authority path; read the peer tip (DC-EVIDENCE-05). | `ci_check_post_switch_convergence_window.sh` |
| `admission_log` *(N-M-B; fork-choice N-AO S9/S14; memory-measure MEM-MEASURE/MEM-OPT-OPS)* | GREEN admission-mode JSONL event vocabulary + writer (closed enum). The closed `AdmissionLogEvent::{MemoryMeasure, MemorySummary}` variants carry the gross + owned `*_kib` evidence fields. | Add an open/wildcard event variant; emit a free-form error string. | `ci_check_admission_log_vocabulary_closed.sh`, `ci_check_fork_choice_evidence_closed.sh` |
| `live_log` *(N-L-LIVE)* | GREEN closed JSONL vocabulary for the wire-only live pass. | Add an open variant; overstate semantic truth. | `ci_check_wire_only_event_vocabulary_closed.sh` |

> **Note on `node_sync` / `node_lifecycle` color (carried + N-AO):** `node_sync` + `node_lifecycle` are **RED** (they drive the apply path / own the persistent-store reads + the live-feed forge wrap + the serve sibling + the PHASE4-N-AO fork-switch DRIVER + the `StaticUtxoFp`-cached `post_fp` admission loop). Their GREEN-by-function decisions carry GREEN-pure semantics within the RED host modules. `ade_testkit::consensus::genesis_pinning` is GREEN `#[cfg(test)]` test infrastructure.

---
### `ade_testkit` *(GREEN — deterministic test/corpus infrastructure; dev-dep / `#[cfg(test)]`-scoped)*

| Attribute | Value |
|-----------|-------|
| **Purpose** | GREEN deterministic TEST / CORPUS infrastructure for the workspace — the oracle/differential harness, the validity & tx-validity corpora, the consensus & governance replay corpora, the producer reference vectors, the snapshot/genesis/ledger diff loaders, the `consensus::ledger_view_stub::LedgerViewStub`, and the Plutus-conformance harness. The home of `replay_cmd = cargo test -p ade_testkit`. **MEM-OPT-UTXO-DISK: the replay/differential corpora are reused byte-unchanged to PROVE the v2 fingerprint cutover + the owned-lookup change behavior-invariant (the DC-WAL-03 `wal_replay_from_anchor`, the admission-replay-equivalence, the adversarial + cross-epoch guards all stay green under v2).** (No structural change in the span.) |
| **Creates** | GREEN test/corpus value types — NONE canonical-counted: `ConwayValidityCorpus` + loaders, `BlockReplay` / `TxReplay`, the adversarial mutation enums, `OracleManifest` / `RegressionCorpus` + validators, `DifferentialReport`, `Transcript`, `LedgerViewStub` / `EpochStakeFixture` / `PoolFixture`, `VrfReferenceVector` / `KesReferenceVector`. All GREEN. |
| **Interprets** | Committed reference corpora + oracle dumps + the Plutus-conformance manifest, for the test harness only. Evidence inputs, never runtime authority. |
| **MUST NOT** | (1) **Be a production runtime dependency.** GREEN dev-dep / `#[cfg(test)]`-scoped (its sole plain edge is the RED-leaf `ade_core_interop`); MUST NOT be promoted onto a hashed/persisted/consensus path. (2) Affect authoritative outputs or introduce nondeterminism. (3) Construct semantic types bypassing canonical decoders. (4) Compare an Ade-internal-state fingerprint to a Haskell-serialized-state hash (DC-COMPAT-01). (5) Commit secret key material. (6) Overstate semantic truth. |
| **Inbound deps** | `ade_core` (dev), `ade_ledger` (dev), `ade_network` (dev), `ade_runtime` (dev), `ade_node` (dev), `ade_core_interop` (plain — the RED-leaf interop/test driver). |
| **Outbound deps** | `ade_core`, `ade_ledger`, `ade_plutus`, `ade_crypto`, `ade_codec`, `ade_types`, `ade_runtime`, `cardano-crypto`, `blake2`, `ed25519-dalek`, `serde`, `serde_json`, `toml`, `flate2`, `tar`. |
| **Entry points** | `ade_testkit::validity::*`, `ade_testkit::tx_validity::*`, `ade_testkit::consensus::{corpus::*, stream_replay::*, ledger_view_stub::{LedgerViewStub, EpochStakeFixture, PoolFixture}}`, `ade_testkit::governance::*`, `ade_testkit::harness::*`, `ade_testkit::mempool::ingress_replay::*`. |
| **Key modules** | `consensus/`, `governance/`, `harness/`, `mempool/ingress_replay.rs`, `producer/`, `tx_validity/`, `validity/`. |

> **GREEN classification (load-bearing):** `ade_testkit` is GREEN test/corpus infrastructure, **never promotable onto a production runtime path**.

---

## RED Modules — Imperative Shell

> I/O, network, storage, clocks, retries. May depend on BLUE/GREEN. Must not modify core state directly or construct semantic types unsafely.

### `ade_mem_diag` *(RED — NEW this cluster; the workspace's SOLE `unsafe`-FFI surface)*

| Attribute | Value |
|-----------|-------|
| **Purpose** | A quarantined RED DIAGNOSTIC crate whose SOLE reason to exist is to wrap the one `unsafe` FFI call the workspace needs for a process-memory diagnostic — mimalloc `mi_collect(force=true)` (a forced-reclaim probe that returns freed-but-`MADV_FREE`-retained pages to the OS, so an owned-RSS sample reflects the reclaimed state). It is the QUARANTINE: it deliberately does NOT carry `#![deny(unsafe_code)]` (the single `unsafe` block is its entire unsafe surface), so the `ade_node` binary keeps `#![deny(unsafe_code)]` with ZERO local allows. Used by `ade_node` ONLY behind the `ADE_MEM_PHASE_DIAGNOSTIC` env toggle (absent on every production run), off the authoritative admission loop. **DIAGNOSTIC-ONLY: it changes NO authoritative output and was NOT used in the BA-08 evidence run** (the owned-RSS measurement contains zero forced-collect points) — it must never read as a dependency for the memory win. |
| **Creates (RED-only)** | No public type. One public fn: `force_allocator_collect_for_diagnostic_only()`. NOT canonical-counted. |
| **Interprets** | Nothing semantic — it issues a single `libmimalloc_sys::mi_collect(true)` FFI call (a no-op on a non-mimalloc build, e.g. a unit test on the default allocator). |
| **MUST NOT** | (1) Carry any authoritative semantics or affect authoritative state. (2) Have its return value (it returns nothing) influence the ledger, consensus, storage, chain selection, replay fingerprints, or protocol behavior. (3) Be depended on by any BLUE crate (and no BLUE crate may depend on it). (4) Become a hidden BA-08 / memory-win dependency — the forced reclaim is admissible only as a MEASUREMENT INTERVENTION, never a production memory-management requirement and never a replay-agreement dependency. (5) Be invoked outside the `ADE_MEM_PHASE_DIAGNOSTIC` toggle on a production path. Enforced by `ci_check_mem_diag_quarantine.sh`. |
| **Inbound deps** | `ade_node` ONLY (behind the `ADE_MEM_PHASE_DIAGNOSTIC` toggle). |
| **Outbound deps** | `libmimalloc-sys` (`["extended"]` — the ONLY dependency; the feature that exposes `mi_collect`). No workspace crate. |
| **Entry points** | `ade_mem_diag::force_allocator_collect_for_diagnostic_only`. |
| **Key modules** | `lib.rs` (the entire crate; the contract header + the single `unsafe` block). |

### `ade_node`

| Attribute | Value |
|-----------|-------|
| **Purpose** | The node binary + library entry. Owns argv parsing, the node lifecycle, and the mode drivers dispatched by `main()`: `--mode wire_only`, `--mode admission`, `--mode key_gen_kes`, `--mode produce`, and `--mode node` (`node_lifecycle`, the single `PHASE4-N-F-C-LIFECYCLE-OWNER`). Carries the full N-F-C…N-AO `--mode node` live surface incl. the PHASE4-N-AO fork-switch SELECT drivers and the carried MEM-OPT-OPS/MEM-MEASURE memory substrate (the `#[global_allocator] mimalloc`, the `mem_measure/` sub-tree, the owned-footprint evidence taps). **MEM-OPT-UTXO-DISK (NEW this cluster):** the admission bootstrap/runner compute a `StaticUtxoFp` ONCE on the live `track_utxo=false` path (compute-once-then-`drop` the static UTxO) and feed the cached constant UTxO component into `fingerprint_v2_with_utxo` per block (`UtxoFpCache`); a `StaticUtxoFp` used under `track_utxo=true` or a version mismatch is a fail-closed exit — `AdmissionExitCode::StaticUtxoFpInvalid` → `EXIT_LIVE_STATIC_UTXO_FP_INVALID = 35`; the new RED crate `ade_mem_diag` is a dependency, invoked ONLY behind the `ADE_MEM_PHASE_DIAGNOSTIC` toggle. **`#![deny(unsafe_code)]` is preserved with ZERO local allows** (the one `unsafe` lives in `ade_mem_diag`). **No new BLUE type, no new CLI flag** from this cluster — the new BLUE types live in `ade_crypto`/`ade_ledger`; the `ade_node` surface is RED wiring. |
| **Creates (RED-only)** | `Cli` (incl. `peer_addrs`, `network_magic`), `CliError`, `ProduceCli`, `KeyGenKesCli`, `Mode` (closed: `WireOnly`/`Admission`/`KeyGenKes`/`Produce`/`Node`), `NodeStartupInputs`, `NodeShutdownEvidence`, `NodeRunError`, exit-code constants (**+ `EXIT_LIVE_STATIC_UTXO_FP_INVALID = 35`**). `admission::runner` (RED): `AdmissionExitCode` (closed — **+ `StaticUtxoFpInvalid`**), the `static_utxo_fp: Option<StaticUtxoFp>` / `utxo_fp_cache` admission state. `node_lifecycle` (RED): `NodeStart`, `NodeLifecycleError`, `ForgeActivation<'a>` (incl. the PHASE4-N-AO fork-switch lifecycle state), `run_relay_loop` / `run_relay_loop_with_sched`, `spawn_live_wire_pump_source` (S8 per-peer lanes + `fair_merge`), `apply_chain_event` / `run_participant_sync`, the PHASE4-N-AO RED drivers `dispatch_competing_fork_choice` / `prove_fork_switch` / `apply_fork_switch` / `recover_missing_range` / `prefetch_branch_bodies` + `map_branch_proof_failure`. `node_sync` (RED): closed `NodeBlockSource`, closed `NodeSyncItem { Block { peer, bytes } \| RollBack(Point) }` (`DC-NODE-34`), `NodeSyncError`. **MEM-MEASURE/MEM-OPT-OPS (RED `ade_node`, NOT canonical-counted):** `mem_measure::rss_sampler::{RssSampleKib, RssWindow, sample_vm_rss_kib, sample_vm_hwm_kib, sample_rss_anon_kib, sample_private_dirty_kib}` + the GREEN `bounded_admission` / `evidence` cores + the closed `AdmissionLogEvent::{MemoryMeasure, MemorySummary}` variants. Plus the carried PHASE4-N-AO GREEN `candidate_aggregator` / `lca_walk` / `selector_state` / `post_switch_continuity` (+ bin) + the split-color `fork_switch` + the RED `fair_merge`. |
| **Interprets** | argv (closed mode set); operator key/genesis/opcert file paths; evidence-manifest TOML schema; a peer's tag-24-wrapped BlockFetch `MsgBlock` (tolerating raw `[era,block]` framing); the live receive path interprets each peer item as a peer-tagged `NodeSyncItem` and routes a Participant `NeedsForkChoice` into `dispatch_competing_fork_choice`. **MEM-OPT-UTXO-DISK:** the admission bootstrap interprets the seeded UTxO ONCE into a `StaticUtxoFp` then drops it; the runner supplies the cached constant component to `fingerprint_v2_with_utxo` per block. **MEM-OPT-OPS/MEM-MEASURE:** the RED `rss_sampler` interprets `/proc/self/status` (`VmRSS`/`VmHWM`/`RssAnon`) + `/proc/self/smaps_rollup` (`Private_Dirty`) into `RssSampleKib` — fail-soft, never a panic into a measurement; the GREEN `evidence` validator interprets a `MemEvidenceRecord` into `EvidenceDefect`s (the verdict from the replay-fingerprint pairing, never the RSS). |
| **MUST NOT** | All carry-forward `--mode node` prohibitions (closed mode set, single lifecycle owner via `bootstrap_initial_state`, durable-tip ONLY via `pump_block`, content-blind planner, the serve-as-projection / forge-on-followed-tip / receive-idempotency fences). The PHASE4-N-AO SELECT driver fences (`DC-NODE-34…41` / `DC-PUMP-04` / `DC-EVIDENCE-04/05`): peer identity preserved end-to-end; route the candidate SET to the SINGLE `select_best_chain`; NEVER commit a rollback before the replacement branch is fetched + linked + validated; the fork anchor binds the durable LCA on slot AND hash; the convergence/continuity evidence is observe-only. **MEM-OPT-UTXO-DISK (NEW):** **`OP-MEM-02`** — a `StaticUtxoFp` MUST be used ONLY on the live `track_utxo=false` path; under `track_utxo=true` or a fingerprint-version mismatch it MUST fail closed (`StaticUtxoFpInvalid` → exit 35), NEVER a stale fingerprint; the cached constant UTxO component MUST yield a `post_fp` byte-identical to the full-UTxO recompute (DC-MEM-05 replay-equivalence); owned-RSS magnitude MUST NOT gate any verdict; the SCOPED win MUST NOT be reported as full BA-08 certification (the 10-day run + `track_utxo=true`/"B" are owed). **`ade_mem_diag`** — the forced-collect probe MUST stay behind the `ADE_MEM_PHASE_DIAGNOSTIC` toggle, off the authoritative loop, and MUST NOT become a hidden BA-08 / memory-win dependency. **Carried `DC-MEM-06`/`07`/`OP-MEM-01`** — the allocator change stays determinism-neutral; the bounded inbound set stays within the closed caps; RSS / owned-footprint magnitude MUST NOT enter a fingerprint/verdict/validator; the `AdmissionLogEvent::{MemoryMeasure, MemorySummary}` vocabulary stays CLOSED. **`#![deny(unsafe_code)]` MUST stay with ZERO local allows** (the one `unsafe` is quarantined in `ade_mem_diag`). |
| **Inbound deps** | None (binary + integration tests). |
| **Outbound deps** | `ade_types`, `ade_core` (incl. `consensus::{fork_choice::select_best_chain, header_validate, candidate, header_summary, events, praos_leader_value, praos_state, era_schedule, ledger_view}`), `ade_crypto` (incl. `vrf` + `blake2b`), `ade_ledger` (incl. `block_validity::*`, `state::LedgerState`, `utxo::{UTxOState, TxOut, UtxoStore}`, **`fingerprint::{fingerprint_v2_with_utxo, StaticUtxoFp, UtxoFpCache}`**, `rollback::materialize::materialize_rolled_back_state`, `wal::event::*`, `mempool::ingress::mempool_ingress` behind the bounded fold, `pump_block` via `forward_sync`), `ade_runtime` (incl. `chaindb::ChainDb::get_block_by_hash`; `admission::{dial_for_admission, run_admission_wire_pump, AdmissionPeerEvent}`; `seed_import` for the streaming UTxO import; the BlockFetch `RequestRange` shells), `ade_network` (incl. `codec::chain_sync::Point`, `block_fetch::client`), `ade_codec`, `mimalloc` (binary-crate-only, the `#[global_allocator]`), **`ade_mem_diag`** (binary-crate-only, the quarantined `mi_collect` probe behind the diagnostic toggle), `serde`, `serde_json`, `tokio`. Dev-deps: `ade_testkit`, `tempfile`. |
| **Entry points** | `main()`; `ade_node::run_node_lifecycle` (`--mode node`); `ade_node::node_lifecycle::{run_relay_loop, ForgeActivation, spawn_live_wire_pump_source, apply_chain_event, run_participant_sync, dispatch_competing_fork_choice, prove_fork_switch, apply_fork_switch, recover_missing_range, prefetch_branch_bodies, …}`; `ade_node::node_sync::{NodeBlockSource, NodeSyncItem, run_node_sync, classify_receive, resolve_disposition, …}`; `ade_node::admission::runner::{AdmissionExitCode, EXIT_LIVE_STATIC_UTXO_FP_INVALID, …}`; `ade_node::mem_measure::{replay_bounded_ingress_trace, validate_evidence, pair_replay, MemEvidenceRecord, ReplayVerdict, sample_rss_anon_kib, sample_private_dirty_kib, sample_vm_rss_kib, sample_vm_hwm_kib, RssWindow, run_hermetic_bounded_ingress_measurement, MAX_INBOUND_ADMISSION_BYTES, MAX_INBOUND_ADMISSION_COUNT}`; the PHASE4-N-AO GREEN cores `ade_node::{candidate_aggregator, lca_walk, selector_state, post_switch_continuity}::*` + the split-color `ade_node::fork_switch::*` + the RED `ade_node::fair_merge::*`; `ade_node::convergence_evidence::*` (GREEN); `ade_node::admission_log::*`; `ade_node::run_loop_planner::*` / `live_log::*` / `forge_intent::*` (GREEN); `ade_node::operator_forge::*` / `ba02_pass::*` / `rehearsal_pass::*` / `produce_mode::*` (RED); `ade_node::cli::{Cli, ProduceCli, Mode}`; `ade_node::{admission, wire_only, key_gen}::*`. |
| **Key modules** | `lib.rs`, `cli.rs`, `node.rs`, `main.rs` (RED entry + the `#[global_allocator] mimalloc`), `node_lifecycle.rs` (the `--mode node` owner + the PHASE4-N-AO fork-switch drivers), `node_sync.rs` (`NodeSyncItem::Block.peer`), `admission/` (**incl. `bootstrap.rs` — the `StaticUtxoFp::from_bootstrap_utxo` + `drop(utxo)` + the `ADE_MEM_PHASE_DIAGNOSTIC` `ade_mem_diag` collect; `runner.rs` — the `StaticUtxoFp`/`UtxoFpCache`-cached `post_fp` loop + `AdmissionExitCode::StaticUtxoFpInvalid` (exit 35)**), `mem_measure/` (`mod.rs`, `bounded_admission.rs` GREEN, `evidence.rs` GREEN, `rss_sampler.rs` RED, `runner.rs` GREEN/RED), `candidate_aggregator.rs` / `lca_walk.rs` / `selector_state.rs` / `post_switch_continuity.rs` + `bin/post_switch_continuity.rs` (GREEN), `fork_switch.rs` (GREEN prove + RED seam), `fair_merge.rs` (RED), `convergence_evidence.rs` (GREEN), `admission_log/` (GREEN), `forge_intent.rs` / `run_loop_planner.rs` / `ba02_evidence.rs` / `live_log/` (GREEN), `operator_forge.rs` / `ba02_pass.rs` / `rehearsal_pass.rs` / `produce_mode.rs` / `wire_only.rs` / `key_gen.rs` (RED). |

> **GREEN/RED sub-classification (carried + MEM-OPT-UTXO-DISK):** GREEN-by-content: `forge_intent`, `run_loop_planner`, `ba02_evidence`, `live_log::{sched_event,sched_writer}`, `convergence_evidence`, `candidate_aggregator`, `lca_walk`, `selector_state`, `post_switch_continuity`, `fork_switch::prevalidate_branch`, `mem_measure::{bounded_admission, evidence}`. RED-by-content: `node_lifecycle` (+ the fork-switch DRIVERS) + `node_sync` + `operator_forge` + `ba02_pass` + `rehearsal_pass` + the `fork_switch::{BranchBodySource, …}` seam + `fair_merge` + `main.rs`'s `#[global_allocator]` + `mem_measure::rss_sampler` (the `/proc` reader) + **the `admission::{bootstrap,runner}` `StaticUtxoFp` compute-once-then-drop wiring + the `ade_mem_diag` diagnostic collect (MEM-OPT-UTXO-DISK A.2/S0)**. The `mem_measure::runner` is a GREEN-workload-under-RED-sampling seam.

### `ade_runtime`

| Attribute | Value |
|-----------|-------|
| **Purpose** | The imperative shell — moves bytes, owns sockets/files/clocks/keys, drives tokio tasks. Hosts: producer-mode key custody + shell composition; the N2N network drivers (incl. the BlockFetch `RequestRange` shells); the node orchestrator runners; the receive/rollback/admission shells; the seed-import importer (MEM-OPT-OPS S2 streaming `CanonicalUtxoSink` + the retained `_from_bytes` oracle); the WAL/consensus-inputs importers; ChainDB (incl. `get_block_by_hash`) + recovery shells; the durable-chain serve projection (bounded at `MAX_SERVE_RANGE_BLOCKS = 256`); the `pump_block` hash-exact idempotency gate (the sole durable admit); the recovered-anchor load/store surface. **MEM-OPT-UTXO-DISK (NEW this cluster, RED):** the DORMANT on-disk redb UTxO backend `chaindb::{utxo_anchor, utxo_key}` (the `TxIn → TxOut` anchor + the fixed-width key + `AnchorPosition`/`reconcile`; `#![allow(dead_code)]`, not reachable from any live path — preparation for "B"), AND the chaindb `FINGERPRINT_VERSION=2` store-META marker checked fail-closed on open (`ChainDbError::FingerprintVersionMismatch` — a v1/unversioned store is REJECTED, never silently upgraded). The BLUE core, the serve projection, and `pump_block` are reused byte-unchanged; `mimalloc`/`ade_mem_diag` live on `ade_node`, not here. |
| **Creates (RED-only)** | `KesSecret`, `VrfSigningKey`, `ColdSigningKey` custody wrappers; `KeyLoadError`; `MuxTransportHandle` consumers; `OutboundCommand` (closed); `PerPeerOutbound`; `DispatchError` (closed); `ServerPeerStates`; `ServedChainHandle`; `ProducerShell`; `AdmissionPeerEvent` (closed); `AdmissionWirePumpError` (closed); `KEEP_ALIVE_CADENCE`. Clock: `SystemClock`, `DeterministicClock`, `SlotAlignmentError`. consensus_inputs / bootstrap / serve plumbing: `LiveConsensusInputsCanonical`, `BootstrapState`, `BootstrapInputs`, `BootstrapError`, `ChainDbServedSource<'a>`, `ServedChainSource<'a>`, `CappedSlotRange`, `ServeRangeOutcome`, `MAX_SERVE_RANGE_BLOCKS = 256`. seed_import: `UtxoFingerprint`, `JsonSeedError` (closed — incl. `DuplicateTxIn`), the `CanonicalUtxoSink` streaming visitor. chaindb (MEM-OPT-UTXO-DISK, dormant): `ChainDbError::FingerprintVersionMismatch { expected, found }` (closed variant) + the `pub(crate)` `AnchorPosition` / `RecoveryDecision` (the redb anchor's position/reconcile types — NOT canonical-counted, RED + `dead_code`). |
| **Interprets** | Canonicalizes peer bytes/files for the BLUE core: the cold/VRF/KES loaders; `genesis_parser::parse_shelley_genesis`; `seed_import::import_cardano_cli_json_utxo` (STREAMING, S2 — parses cardano-cli JSON entry-by-entry into the canonical `BTreeMap<TxIn, TxOut>`, fail-closed on any canonical-`TxIn` duplicate, byte-identical to the whole-buffer oracle); `admission::{dial_for_admission, run_admission_wire_pump}`; `bootstrap::restore_seed_epoch_consensus_inputs`; `recovered_anchor::load_recovered_anchor_point`. **MEM-OPT-UTXO-DISK:** `chaindb::utxo_anchor` interprets a fixed-width `txid[32] ‖ BE-u32(index)` key + canonical `TxOut` bytes into/out of the on-disk anchor (DORMANT — the RED shell would pre-resolve required inputs from it into a `WorkingSet` BEFORE BLUE runs, but no live path reaches it yet); the chaindb open path interprets the store `FINGERPRINT_VERSION` and REJECTS a v1/unversioned store (fail-closed). |
| **MUST NOT** | (1) Modify BLUE state directly or construct semantic types from raw bytes. (2) Bypass canonical validation. (3) `producer::signing` (DC-CRYPTO-03/05) — RED-confined custody. (3b) `kes_sign_header_advancing` (DC-CRYPTO-10) — evolve before sign. (4)–(7) `producer::keys` / `coordinator` / `outbound_command` / per-peer outbound fences. (7b–7d) `network::serve_dispatch` / `n2n_listener` / `served_chain_projection` (DC-NODE-07 / 13/14 / DC-SERVEMEM-01) — READ-ONLY, bounded per-request. (8) `admission::wire_pump` keep-alive client (DC-PUMP-03). (9) `forward_sync::pump_block` (DC-SYNC-01 / DC-NODE-16) — refuse to advance the tip before `StoreBlockBytes` + `AppendWal`; HASH-keyed already-have skip; the SOLE durable admit. (9b, MEM-OPT-OPS S2 `DC-MEM-06`) `seed_import::import_cardano_cli_json_utxo` — the STREAMING import MUST produce a fingerprint over canonical `TxIn` keys identical to the whole-buffer oracle; ANY canonical-`TxIn` duplicate fails closed (`DuplicateTxIn`); the whole-buffer `_from_bytes` stays the retained equivalence ORACLE, not a second production authority. **(9c, MEM-OPT-UTXO-DISK `DC-MEM-05`/`06`) `chaindb::{utxo_anchor, utxo_key}` — the on-disk redb UTxO anchor is a PURE STORAGE backend (a byte-identical replay under both backends: same WAL + checkpoint ⇒ same tail fingerprint); it MUST deliberately NOT implement `UtxoStore` (BLUE never holds a storage backend — the RED shell resolves into an in-memory `WorkingSet` first); the storage key MUST be the fixed-width `txid[32] ‖ BE-u32(index)` so redb's byte-sorted iteration equals canonical `TxIn` order, NEVER used for hashing/tx-IDs/block-IDs/signatures/replay-fingerprints; the anchor commit MUST be one write-txn per block (old anchor valid OR new anchor valid, never half-applied); recovery MUST `reconcile` the `AnchorPosition` to the WAL and HALT on a shared-position conflict, never silently reconcile.** **(9d, MEM-OPT-UTXO-DISK `DC-MEM-10`) `chaindb` open MUST check the store `FINGERPRINT_VERSION` and fail closed (`FingerprintVersionMismatch`) on a v1/unversioned store — never silently upgrade or replay it.** (10)–(22) `mithril_import` / `genesis_bootstrap` / `recovery::restart` / `bootstrap` / `seed_consensus_provenance` / `chaindb` recovered-anchor surface / `resolve_live_follow_start` / `clock::checked_millis_to_slot` / `consensus_inputs` / rollback-signal-preservation fences. |
| **Inbound deps** | `ade_node`, `ade_core_interop`, `ade_testkit` (dev/integration). |
| **Outbound deps** | `ade_types`, `ade_core`, `ade_crypto`, `ade_codec` (incl. `unwrap_tag24` + the canonical `cbor::write_*` primitives the S2 sink + the dormant redb anchor use), `ade_ledger` (incl. `utxo::{UTxOState, TxOut}`, `rollback::materialize::materialize_rolled_back_state`, `recovered_anchor_point::*`; the dormant redb anchor uses `snapshot::encode_tx_out_canonical` + the `fingerprint` version constants), `ade_network` (incl. `keep_alive` + `block_fetch::client`), `redb`, `serde`, `serde_json` (incl. `Deserializer::from_reader` for the S2 streaming import), `bech32`, `base58`, `cardano-crypto`, `ed25519-dalek`, `tokio` (dev-deps: `tokio` `["test-util"]`, `tempfile`, `ade_testkit`). |
| **Entry points** | `ade_runtime::producer::*`, `ade_runtime::admission::{dial_for_admission, run_admission_wire_pump, AdmissionPeerEvent, AdmissionWirePumpError}`, `ade_runtime::network::{n2n_listener::*, n2n_dialer::*, mux_pump::*, n2n_server::*, outbound_command::*, serve_dispatch::*, served_chain_projection::*, block_fetch shells}`, `ade_runtime::orchestrator::*`, `ade_runtime::bootstrap::{bootstrap_initial_state, BootstrapInputs, BootstrapState, BootstrapError}`, `ade_runtime::recovered_anchor::load_recovered_anchor_point`, `ade_runtime::seed_consensus_merge::*`, `ade_runtime::consensus_inputs::*`, `ade_runtime::seed_import::{import_cardano_cli_json_utxo, import_cardano_cli_json_utxo_from_bytes, UtxoFingerprint, JsonSeedError}`, `ade_runtime::{forward_sync (incl. pump::pump_block), mithril_import, genesis_bootstrap, mithril_bootstrap, recovery::restart, chaindb (incl. ChainDb::get_block_by_hash; the dormant utxo_anchor/utxo_key; the FINGERPRINT_VERSION open-check), clock, wal, bootstrap_anchor, seed_epoch_lineage}::*`. |
| **Key modules** | `producer/`, `network/` (incl. `served_chain_projection.rs`, `serve_dispatch.rs`, `n2n_listener.rs`, the block-fetch shells), `orchestrator/`, `admission/` (incl. `wire_pump.rs`), `receive/`, `rollback/`, `seed_import/` (incl. `importer.rs` — the S2 streaming `CanonicalUtxoSink` + the retained `_from_bytes` oracle; `json.rs`), `consensus_inputs/` (incl. `json.rs` — the `set`/`go`-snapshot leader-stake fix), `wal/`, `chaindb/` (redb schema; the bounded serve primitives + `get_block_by_hash`; **`error.rs` — `FingerprintVersionMismatch`; `persistent.rs` — the `FINGERPRINT_VERSION=2` open-check; the DORMANT `utxo_anchor.rs` + `utxo_key.rs`**), `forward_sync/` (incl. `pump.rs` — the sole durable admit), `mithril_import/`, `seed_consensus_merge.rs`, `seed_epoch_lineage.rs`, `mithril_bootstrap.rs`, `genesis_bootstrap.rs`, `recovery/`, `bootstrap.rs`, `recovered_anchor.rs`, `clock.rs`. |

> **RED sub-entries carried** (unchanged in shape across the span): `ade_runtime::network::serve_dispatch` *(RED — N-F-G-H)* and `ade_runtime::network::served_chain_projection` *(RED — N-U S3 / DC-NODE-13)*. **NEW dormant RED sub-entry:** `ade_runtime::chaindb::{utxo_anchor, utxo_key}` *(RED — MEM-OPT-UTXO-DISK S2b; `#![allow(dead_code)]`; the on-disk redb UTxO backend + fixed-width key — BUILT + UNIT-PROVEN but NOT wired into any live path; preparation for "B", NOT the mechanism that delivered the memory win, NOT live-proven for durability).**

---

### `ade_core_interop`

| Attribute | Value |
|-----------|-------|
| **Purpose** | Live cardano-node interop driver. Hosts the `live_*_session` RED binaries plus the N-E S4/S5 GREEN tx-submission bridges and the chain-follow driver (`follow`). `follow` strips the peer's ChainSync `RollForward` envelope via `ade_codec::unwrap_tag24`. `follow.rs` is the FORBIDDEN minted-`ValidatedHeaderSummary` shape the PHASE4-N-AO `DC-NODE-35` gate forbids from reaching `select_best_chain`. (Unchanged across the MEM-OPT-UTXO-DISK span.) |
| **Creates (RED-only)** | Live-session drivers and transcript types; never semantic types. |
| **MUST NOT** | (1) Construct semantic types from raw bytes. (2) Be depended on by any BLUE/GREEN crate (RED leaf). (3) Overstate semantic truth in evidence. (4) `follow` (CN-WIRE-08) — strip via `ade_codec::unwrap_tag24`. (5, cross-ref DC-NODE-35) the `follow.rs` minted summary shape MUST NOT cross into BLUE / reach `select_best_chain`. |
| **Inbound deps** | None (RED leaf — binaries). |
| **Outbound deps** | `ade_core`, `ade_codec`, `ade_crypto`, `ade_ledger`, `ade_runtime`, `ade_network`, `ade_testkit`, `ade_types`, `tokio`. |
| **Entry points** | `ade_core_interop::bin::{live_tx_submission_session, live_block_production_session, live_block_fetch_session, live_block_follow_session}`; `ade_core_interop::follow::*`; `ade_core_interop::{tx_submission, local_tx_submission}`. |
| **Key modules** | `bin/`, `follow.rs`, `tx_submission.rs`, `local_tx_submission.rs`. |

---

### `ade_network::mux::transport` *(RED)*

| Attribute | Value |
|-----------|-------|
| **Purpose** | The only place inside `ade_network` where socket I/O happens. RED tokio shell over the BLUE mux frame primitive and the GREEN session reducer. Provides `MuxTransportHandle` + closed `TransportError` + `DuplexCapacity::DEFAULT` + `spawn_duplex`. (Unchanged across the span.) |
| **Creates (RED-only)** | `MuxTransportHandle`, `TransportError`, `DuplexCapacity`, `MuxTransport`. |
| **MUST NOT** | (1) Construct semantic types. (2) Bypass `mux::frame` for framing. (3) Live in BLUE scope. |
| **Inbound deps** | `ade_runtime::network::{mux_pump, n2n_dialer}`. |
| **Outbound deps** | `ade_network::mux::frame` (BLUE), `ade_network::session` (GREEN), `tokio`. |
| **Entry points** | `ade_network::mux::transport::{MuxTransportHandle, spawn_duplex, open_tcp}`. |
| **Key modules** | `mux/transport.rs`. |

---

### `ade_network` *(RED capture binaries — non-session)*

| Attribute | Value |
|-----------|-------|
| **Purpose** | Operator-action capture binaries (live evidence harness). `ade_chain_sync_capture` (`--intersect-slot`/`--intersect-hash`) captures a real Conway `RollForward` golden fixture; `ade_keep_alive_capture` captures real keep-alive traffic; `capture_tx_submission2_server.rs` captures the real tx-submission2 server-role exchange (the corpus that backs `DC-PROTO-11`). (Unchanged across the MEM-OPT-UTXO-DISK span.) |
| **MUST NOT** | (1) Construct semantic types from raw bytes. (2) Overstate semantic truth in captured evidence. |
| **Outbound deps** | `ade_network` BLUE/GREEN submodules, `ade_codec`, `ade_types`, `tokio`. |

---
## Cross-Module Rules (project-wide)

### Dependency direction (RED → GREEN → BLUE; never outward)

```
ade_types     → {}
ade_codec     → {ade_types}
ade_crypto    → {ade_types, blake2, ed25519-dalek, cardano-crypto[vrf-draft03,kes-sum(test-only),dsign], curve25519-dalek(v4)}   [BLUE; +1 type UtxoSetCommitment (MEM-OPT-UTXO-DISK S1.5 v2 ECMH set commitment, DC-MEM-10); curve25519-dalek is the NEW dep for the Ristretto255 group]
ade_core      → {ade_types, ade_crypto, minicbor}   [BLUE; the SOLE chain-selector select_best_chain + the candidate/header-validate authorities reused byte-unchanged; 49→49 — MEM-OPT-UTXO-DISK UNTOUCHED]
ade_ledger    → {ade_core, ade_plutus, ade_crypto (incl. utxo_set_commitment), ade_codec, ade_types, minicbor, num-*}   [BLUE; TOUCHED — 181→187 (+6: OverlayUtxo, WorkingSet, StaticUtxoFp, StaticUtxoFpError, IncrementalUtxoFp, UtxoFpCache); behavior-invariant EXCEPT the versioned v1→v2 UTxO-fingerprint cutover (fingerprint()→fingerprint_v2; fingerprint_v1 FROZEN); + the owned utxo_lookup / UtxoStore seam (DC-MEM-09) + the GREEN pre_resolve working-set]
ade_plutus    → {ade_crypto, ade_codec, ade_types, aiken_uplc, pallas-primitives(internal)}   [BLUE; 9, byte-stable — no MEM-OPT-UTXO-DISK change]
ade_network   → BLUE submodules → {ade_codec, ade_types}; session(GREEN) → +BLUE submodules; transport(RED) → +tokio   [BLUE; 111, byte-stable — no MEM-OPT-UTXO-DISK change]
ade_testkit   → {ade_core, ade_ledger, ade_plutus, ade_runtime, ade_crypto, ade_codec, ade_types, cardano-crypto}   [GREEN]
ade_mem_diag  → {libmimalloc-sys["extended"]}   [RED diagnostic leaf; the workspace's SOLE unsafe-FFI surface (mi_collect); NO workspace dep; NO BLUE crate may depend on it; consumed by ade_node ONLY behind ADE_MEM_PHASE_DIAGNOSTIC]
ade_runtime   → {ade_core, ade_crypto, ade_codec, ade_types, ade_ledger, ade_network, redb, serde*, bech32, base58, cardano-crypto[vrf-draft03,dsign], ed25519-dalek, tokio}   [RED; MEM-OPT-UTXO-DISK adds the DORMANT redb UTxO anchor + key (chaindb::{utxo_anchor,utxo_key}, #![allow(dead_code)]) + the FINGERPRINT_VERSION=2 fail-closed open-check — no new dep; mimalloc/ade_mem_diag live on ade_node]
ade_node      → {ade_types, ade_core, ade_crypto, ade_ledger, ade_runtime, ade_network, ade_codec, mimalloc, ade_mem_diag, serde, serde_json, tokio}   [RED; MEM-OPT-UTXO-DISK adds the binary-only ade_mem_diag dep + the StaticUtxoFp compute-once-then-drop admission wiring (exit 35 fail-closed) — all RED reusing the BLUE core; keeps #![deny(unsafe_code)] zero local allows; the PHASE4-N-AO SELECT + the MEM-OPT-OPS mem substrate are carried]
ade_core_interop → {ade_core, ade_codec, ade_crypto, ade_ledger, ade_runtime, ade_network, ade_testkit, ade_types, tokio}   [RED leaf; follow.rs is the FORBIDDEN minted-summary shape — DC-NODE-35]
```

**Forbidden directions (carried + the span).** Any BLUE crate (or BLUE `ade_network` submodule) depending on `ade_runtime`, `ade_node`, `ade_core_interop`, `ade_mem_diag`, or the RED half of `ade_network` is a CI failure (`ci_check_dependency_boundary.sh`). Any non-`ade_plutus` crate referring to `pallas_*` is a CI failure. The carried fences (a second block-envelope encoder, a second `leader_vrf_input`, a second `wrap_tag24`/`unwrap_tag24`, a forward-sync `AdvanceTip` before durability, a bootstrap path bypassing `bootstrap_initial_state`, an internal-fingerprint-vs-Haskell-hash equality assertion, a second rollback implementation, the PHASE4-N-AO SELECT fences `DC-NODE-34…41` / `DC-PUMP-04` / `DC-EVIDENCE-04/05`, …) all hold. **NEW this cluster (MEM-OPT-UTXO-DISK):**
- **The `unsafe`-FFI quarantine (`ade_mem_diag`):** the single workspace `unsafe` (mimalloc `mi_collect`) MUST live ONLY in `ade_mem_diag`; `ade_node` (and every other crate) keeps `#![deny(unsafe_code)]` with zero local allows; no BLUE/GREEN crate may depend on `ade_mem_diag`; its forced collect MUST stay behind `ADE_MEM_PHASE_DIAGNOSTIC` and MUST NOT become a memory-win / BA-08 dependency (`ci_check_mem_diag_quarantine.sh`).
- **The versioned v1→v2 UTxO-fingerprint cutover (`DC-MEM-10`):** the v2 UTxO component MUST be the NAMED domain-separated `UtxoSetCommitment` (NOT XOR/sum), golden-vector-pinned, version-tagged; `IncrementalUtxoFp` MUST equal the full recompute after each block; `fingerprint()` delegates to `fingerprint_v2` while `fingerprint_v1` stays FROZEN; a v1/unversioned store is REJECTED fail-closed on open (`FingerprintVersionMismatch`); the cutover is Ade-INTERNAL and MUST NOT touch the peer-facing block-hash / `AgreementVerdict` path (`ci_check_utxo_fp_v2.sh`).
- **The owned-lookup seam (`DC-MEM-09`):** the authoritative `utxo_lookup` MUST return OWNED `Option<TxOut>` through the `UtxoStore` seam, with no whole-`BTreeMap` clone path, and MUST NOT alter any verdict/fingerprint/failure-shape; no redb in `ade_ledger` (`ci_check_utxo_lookup_owned.sh`).
- **The bounded overlay (`DC-MEM-07`):** the in-memory overlay/changelog MUST stay within the fixed closed `MAX_OVERLAY_ENTRIES` and `compact()` rather than grow unboundedly; the overlay-split state fingerprints identically to a direct build (`ci_check_overlay_utxo_s2a.sh`).
- **The on-disk redb backend discipline (`DC-MEM-05`/`06`, DORMANT):** the redb UTxO anchor MUST be a pure storage substitution (byte-identical replay under both backends), MUST NOT implement `UtxoStore`, MUST use the fixed-width `txid[32] ‖ BE-u32(index)` key (NEVER for hashing/IDs/signatures/fingerprints), and MUST commit one write-txn per block + reconcile-or-halt on recovery (`ci_check_utxo_disk_anchor.sh`, `ci_check_utxo_disk_key.sh`, `ci_check_utxo_admission_seam.sh`, `ci_check_utxo_pre_resolve.sh`).
- **The SCOPED owned-memory win (`OP-MEM-02`):** `StaticUtxoFp` MUST fail closed under `track_utxo=true` / version mismatch (`StaticUtxoFpInvalid` → exit 35) and yield a byte-identical `post_fp` (DC-MEM-05); owned-RSS magnitude MUST NOT gate any verdict; the win MUST NOT be claimed beyond the live `track_utxo=false` scope (the 10-day BA-08 cert + `track_utxo=true`/"B" are OWED) (`ci_check_utxo_fp_cache.sh`, `ci_check_mem_opt_s3_owned.sh`).

### Naming convention

All crates are prefixed `ade_`. TCB color is not encoded in the crate name. The authoritative classifier is `.idd-config.json` `core_paths` + the per-file `// Core Contract:` / `//! BLUE|GREEN|RED` banner + the cluster TCB Color Maps; CI scripts hard-code their BLUE list. **`ade_mem_diag` is the one crate that does NOT carry `#![deny(unsafe_code)]`** — it is the unsafe-FFI quarantine, RED by its banner, depended on by `ade_node` only.

### Closed enums / registries (for SEAMS cross-reference)

Closed semantic surfaces detected at HEAD (carried + the span): `AcceptedMiniProtocol` (`ade_network::session`), `TagEnvelopeError` (`ade_codec::cbor::tag24`), `ExpectedVrfInput` / `LeaderCheckVerdict` (`ade_core::consensus`), `CandidateFragment` / `TiebreakerView` / `ValidatedHeaderSummary` / `HeaderInput` / `HeaderVrf` / `Point` (`ade_core::consensus`), `KeepAliveMessage` / `KeepAliveState` (`ade_network::keep_alive`), `TxSubmissionTxId` + the tx-submission2 message set (`ade_network::codec::tx_submission` + `tx_submission::event`, `DC-PROTO-11`, real-capture-locked), `OutboundCommand` + `DispatchError` + `AdmissionPeerEvent` + `AdmissionWirePumpError` (`ade_runtime`), `JsonSeedError` (closed — incl. `DuplicateTxIn`; `ade_runtime::seed_import`), `KesError` / `KesParseError`, the admission/wire-only/live-log JSONL vocabularies, the operator-evidence manifest TOML schema, the closed `CardanoEra` / Conway cert + governance enums, `PrevHash` (`ade_types::shelley::block`), the closed BLUE `ArrayHead` (`ade_network::codec::primitives`), `SeedEpochConsensusInputs` + `WalEntry::{SeedEpochConsensusInputsImported, RollBack}` + `RollbackReason::ForkChoiceWin` + `RecoveredAnchorPoint` (`ade_ledger`), and the carried PHASE4-N-AO closed `ade_node` surfaces (`CandidateBuildError`, `LcaError`, `SelectorProjectError`, `BranchProofError` / `ForkSwitchOutcome` / `MissingBridgeReason` / `RangeRefetchOutcome`, `PostSwitchContinuity`, the `AdmissionLogEvent` fork-choice + range-refetch vocabulary + `ForkChoiceResult` + `ForkChoiceEvidenceFailure`), plus the carried MEM-MEASURE/MEM-OPT-OPS `BoundedOutcome` / `ShedReason` / `ReplayVerdict` / `EvidenceDefect` + `AdmissionLogEvent::{MemoryMeasure, MemorySummary}`. **MEM-OPT-UTXO-DISK adds these closed surfaces:** BLUE `StaticUtxoFpError` (`ade_ledger::fingerprint` — the fail-closed reason a static fingerprint may not be used) + the explicit `FINGERPRINT_VERSION_V1` / `V2` version tags; RED `ChainDbError::FingerprintVersionMismatch` (`ade_runtime::chaindb::error`) + the dormant `RecoveryDecision` (`utxo_anchor`); RED `AdmissionExitCode::StaticUtxoFpInvalid` (`ade_node::admission::runner`). The BLUE `UtxoSetCommitment` (`ade_crypto`) is a closed-by-construction primitive (a named domain-separated set commitment, NOT an open registry). **`DC-MEM-09` + `DC-MEM-10` are `enforced`; `DC-MEM-06` + `DC-MEM-07` are `partial`; `DC-MEM-05` + `DC-MEM-08` are `declared`; `OP-MEM-02` is `enforced` (SCOPED to the live `track_utxo=false` path — the 10-day BA-08 cert + `track_utxo=true` "B" are OWED).** **Governance note:** the v1→v2 UTxO-fingerprint cutover IS a versioned change to Ade's INTERNAL replay contract (DC-MEM-10, gated + golden-pinned, a v1 store rejected fail-closed) but is NOT a peer-facing / Cardano-consensus change (the block-hash `AgreementVerdict` path is untouched). The +7 canonical-type delta (1 in `ade_crypto`, 6 in `ade_ledger`) is all this cluster's BLUE work and is closed-by-construction.

### CI enforcement (200 scripts under `ci/`)

The full list is mechanically obtainable via `ls ci/ci_check_*.sh` (200 at HEAD `1b851c07`). CI lives entirely under `ci/` (`ci_dirs = ["ci"]`); there is no `.github/workflows/` invariant gate at HEAD. **The `e0c77492..1b851c07` span (MEM-OPT-UTXO-DISK) added +10 CI scripts and modified 1 in place** — verified `git diff --name-status e0c77492..1b851c07 -- 'ci/ci_check_*.sh'`: **10 `A`** + **1 `M`** + **0 `D`**. The ten new gates:

| Script | Enforces | Slice |
|---|---|---|
| `ci_check_mem_diag_quarantine.sh` | The `ade_mem_diag` quarantine — the SOLE workspace `unsafe`-FFI surface; no BLUE dep; `ade_node` keeps `#![deny(unsafe_code)]` zero local allows; the probe stays behind `ADE_MEM_PHASE_DIAGNOSTIC`. | S0 |
| `ci_check_mem_opt_utxo_disk_s0.sh` | **CE-UD-0** — the active-admission owned-footprint diagnostic transcript (the forced-collect labeled `…_diagnostic_only`). | S0 |
| `ci_check_utxo_lookup_owned.sh` | **DC-MEM-09** — the authoritative `utxo_lookup` returns OWNED `Option<TxOut>` behind the `UtxoStore` seam; routed sites; no redb in `ade_ledger`. | S1 |
| `ci_check_utxo_fp_v2.sh` | **DC-MEM-10** — the v2 UTxO fingerprint is a NAMED domain-separated golden-pinned version-tagged ECMH set commitment; incremental == full recompute; v1 store rejected fail-closed. | S1.5 |
| `ci_check_overlay_utxo_s2a.sh` | **DC-MEM-07** — `OverlayUtxo` is an Arc anchor + a bounded overlay capped by the closed `MAX_OVERLAY_ENTRIES` + compaction + the `UtxoStore`/`UtxoMembership` seam; in-memory only (no redb); fingerprint-identical. | S2a |
| `ci_check_utxo_disk_anchor.sh` | **DC-MEM-05** — the on-disk redb UTxO anchor is a pure storage backend; deliberately not a `UtxoStore`; one write-txn per block; reconcile-or-halt. | S2b |
| `ci_check_utxo_disk_key.sh` | **DC-MEM-06** — the on-disk anchor's storage key is the fixed-width `txid[32] ‖ BE-u32(index)`, never used for hashing/IDs/fingerprints, forcing redb byte order == canonical `TxIn` order. | S2b |
| `ci_check_utxo_admission_seam.sh` | The admission-seam SPEC (the RED shell pre-resolves required inputs into an in-memory `WorkingSet` before BLUE runs). | S2b |
| `ci_check_utxo_pre_resolve.sh` | The ONE authoritative era-aware required-input enumeration (`collect_required_txins` = spend ∪ collateral ∪ reference; completeness fails closed if any class is dropped). | S2b |
| `ci_check_utxo_fp_cache.sh` | **OP-MEM-02** — `StaticUtxoFp` + `drop(utxo)` + the fail-closed `track_utxo=true`/version-mismatch guard (the live constant-component fingerprint). | A.2 |

> **The 1 modified gate this span is extended (no rule weakened):** `ci_check_mem_measure_evidence.sh` — extended for the S0 owned-footprint diagnostic transcript schema.
>
> **MEM-OPT-UTXO-DISK is test-backed beyond its gates:** the ECMH commitment suite (`order_independent`, `add_remove_is_exact_inverse`, `binds_value_not_just_key`, the 3 golden-digest vectors, `fingerprint_v2_is_deterministic`, `fingerprint_v2_utxo_is_insertion_order_independent`, `incremental_v2_equals_full_recompute_after_each_block`, `incremental_produce_spend_is_exact_inverse`, `v1_unversioned_store_is_rejected_fail_closed`), the overlay suite (`overlay_matches_btreemap_across_a_sequence`, `compact_preserves_effective_set_and_clears_overlay`, `clone_shares_anchor_and_is_independent`, `s2a_overlay_split_fingerprints_identically_to_direct_build`), `owned_lookup_returns_stored_value_and_does_not_mutate`, `static_utxo_fp_fails_closed_under_track_utxo_true_and_version_mismatch`, and the `ade_mem_diag` `collect_is_a_safe_noop_to_call`.

---

## Generation notes

- Regenerated (**MEM-OPT-UTXO-DISK cluster-close refresh** — the on-disk-UTxO preparation + the owned-footprint memory win; `DC-MEM-05…10` / `OP-MEM-02`) at HEAD `1b851c07` (`git rev-parse --short HEAD` — the cluster's last commit; the close commit will be created on top carrying this doc). The prior on-disk CODEMAP was committed at the MEM-OPT-OPS close `233644f7`; the cluster span is `e0c77492..1b851c07` (first cluster commit `a2d22113`, parent `e0c77492` = the MEM-OPT-OPS close).
- **This cluster TOUCHED BLUE — the prior "zero BLUE" / "BLUE untouched" header framing is FALSE for this cluster and has been REMOVED.** Verified: `git diff --stat e0c77492..1b851c07 -- crates/ade_core/src crates/ade_types/src crates/ade_codec/src crates/ade_crypto/src crates/ade_ledger/src crates/ade_plutus/src` shows ≈18 BLUE source files changed, +1537/−72; `ade_crypto` 21→22 (`UtxoSetCommitment`) and `ade_ledger` 181→187 (`OverlayUtxo`, `WorkingSet`, `StaticUtxoFp`, `StaticUtxoFpError`, `IncrementalUtxoFp`, `UtxoFpCache`). `ade_core` 49→49, `ade_types` 82→82, `ade_codec` 11→11, `ade_plutus` 9→9, `ade_network` BLUE 111→111 are byte-stable. The BLUE changes are behavior-invariant EXCEPT the deliberate, versioned v1→v2 UTxO-fingerprint cutover (DC-MEM-10), which is Ade-internal (never peer-facing).
- Mechanical counts recomputed fresh from the tree at `1b851c07` (not copied): **12 crates** (`grep -cE '"crates/' Cargo.toml`; Δ +1 — NEW `ade_mem_diag`), **471 canonical types** (`git grep -hE "^(pub )?(struct|enum) " 1b851c07` over the BLUE `core_paths` trees; Δ +7 vs the prior on-disk 464 — `ade_crypto` +1 + `ade_ledger` +6, **all from THIS cluster's BLUE work**), **2640 tests** (`git grep -hE "#\[test\]|#\[tokio::test\]" -- 'crates/**/*.rs' | wc -l`; Δ +45 vs the cluster parent `e0c77492`'s 2595 — the MEM-OPT-UTXO-DISK hermetic suites), **200 CI checks** (`ls ci/ci_check_*.sh | wc -l`; cross-checked `git ls-tree -r 1b851c07 ci/`; Δ +10 — `git diff --name-status e0c77492..1b851c07 -- 'ci/ci_check_*.sh'` = 10 `A` + 1 `M` + 0 `D`), **380 registry rules** (`grep -cE '^id = ' docs/ade-invariant-registry.toml`; Δ +2; status 253 enforced / 23 partial / 103 declared / 1 enforced_scaffolding / 0 deprecated).
- **Registry rule reconciliation (cluster):** the two net-new rules vs the prior on-disk 378 are `DC-MEM-09` (`enforced`) + `DC-MEM-10` (`enforced`). **Flips this cluster:** `OP-MEM-02` `declared → enforced` (SCOPED to the live `track_utxo=false` path; the 10-day BA-08 cert + `track_utxo=true` "B" are OWED — `enforced` does NOT read as full BA-08 certification); `DC-MEM-06` `declared → partial` (allocator-neutrality enforced at MEM-OPT-OPS S1, the streaming clause at S2; the store-iteration-order clause still declared pending the live on-disk UTxO); `DC-MEM-07` `declared → partial` (the overlay/changelog bound enforced at S2a; the read-cache bound still declared pending the on-disk anchor). **No rule weakened.**
- **HONESTY FRAMING reflected (a prior IDD review flagged this — recorded verbatim-correct):** (1) `OP-MEM-02` `enforced` is SCOPED — owned `RssAnon` 1.94 GiB < Haskell 2.57 GiB on the LIVE `track_utxo=false` admission path ONLY; the 10-day sustained BA-08 cert + `track_utxo=true` full live ledger application ("B", LIVE-LEDGER-APPLY) remain OWED; `enforced` never reads as full BA-08 certification. (2) The on-disk redb UTxO backend (`utxo_anchor`/`utxo_key`/`pre_resolve`/the key) is BUILT + UNIT-PROVEN but DORMANT (`#![allow(dead_code)]`, a CI guardrail keeps it out of BLUE) — preparation for "B", NOT the mechanism that delivered the memory win; the actual live mechanism is PATH A (`StaticUtxoFp` computed once at bootstrap + dropping the in-memory 1.9M-entry static UTxO, fail-closed under `track_utxo=true`/version-mismatch); the redb backend is NOT live or live-proven for durability. (3) The BLUE changes are proven behavior-invariant EXCEPT the deliberate versioned v1→v2 fingerprint cutover (`fingerprint()` → `fingerprint_v2`; `fingerprint_v1` FROZEN; store `FINGERPRINT_VERSION=2` fail-closed on opening an old store); the v2 fingerprint is Ade-internal, never peer-facing. (4) The `ade_mem_diag` unsafe `mi_collect` probe is diagnostic-only and was NOT used in the BA-08 evidence run (the owned-RSS measurement contains zero forced-collect points); it must never read as a dependency for the memory win.
- **TCB color for the SIX new source files + the new crate verified against the on-disk banners at `1b851c07`:** BLUE `ade_crypto/src/utxo_set_commitment.rs`; BLUE-host `ade_ledger/src/{utxo_overlay.rs (BLUE), pre_resolve.rs (GREEN-by-content)}`; RED `ade_runtime/src/chaindb/{utxo_anchor.rs, utxo_key.rs}` (DORMANT, `#![allow(dead_code)]`); RED `ade_mem_diag/src/lib.rs` (the deliberately-NOT-`#![deny(unsafe_code)]` quarantine — its banner is the authority). NONE recolors an existing module. The BLUE edits to existing `ade_ledger` files are behavior-preserving except the DC-MEM-10 cutover.
- **`.idd-config.json` annotations are STALE at this regen (informational; this run does NOT modify the config).** At this HEAD the config's `head_deltas_baseline` is still `862cd2cb` (the PHASE4-N-AO close) and its `_invariant_registry_doc` count predates the MEM-OPT-OPS + MEM-OPT-UTXO-DISK rules; the next `/head-deltas` run owns the bump to `1b851c07`. This `/codemap` run does NOT touch `.idd-config.json` or any source file.
- **Cluster-doc location at this regen:** the MEM-OPT-UTXO-DISK cluster + slice docs (S0/S1/S1.5/S2a/S2b/A.2) live under `docs/clusters/MEM-OPT-UTXO-DISK/`; the planning docs under `docs/planning/mem-opt-{cluster-plan,grounding}.md`; the committed memory evidence under `docs/evidence/mem-opt-utxo-disk-*.{jsonl,md}` (in-repo — process-memory numbers, not credentials, so no competition-secrecy bar applies). The MEM-OPT-OPS / MEM-MEASURE / PHASE4-N-AO cluster docs are archived under `docs/clusters/completed/`.
- **Test-count method caveat (load-bearing — carried).** The documented method (`git grep -hE "#\[test\]|#\[tokio::test\]"`) matches `#[test]` and bare `#[tokio::test]` but NOT `#[tokio::test(flavor = …)]`. The MEM-OPT-UTXO-DISK suites are `#[test]` (the pure ECMH/overlay/owned-lookup/static-fp tests) — all captured by the grep; the +45 net is the mechanically-verified delta of the documented grep across the changed `.rs` files (`2595 → 2640`).
- The doc is regenerated, not edited. If a value drifts, fix the source, not the doc.
