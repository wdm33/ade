# Module Authority Map — Ade

> **Status:** Living architectural document. Regenerated; not hand-edited.
> Per-project instance of `~/.claude/methodology/templates/codemap.md`.

> 11 crates, 376 canonical types, 1325 tests, 29 CI checks at HEAD (`168ac02`).

---

## Conventions

- A **module** in Ade is a Cargo workspace crate (smallest independently-buildable unit). One exception: `ade_network` is split by *submodule color* — its BLUE submodules and its RED submodules are documented as two separate entries below, because `.idd-config.json` `core_paths` resolves BLUE at the submodule path level rather than crate-wide. The `ade_core::consensus` submodule sits *inside* the BLUE `ade_core` crate and is covered by that entry. The `ade_ledger::block_validity` / `ade_ledger::consensus_view` / `ade_ledger::tx_validity` / `ade_ledger::mempool::admit` / `ade_ledger::cert_classify` modules sit inside the BLUE `ade_ledger` crate and are covered by that entry; the RED `ade_ledger::consensus_input_extract` and the GREEN `ade_ledger::mempool::policy` sit inside the BLUE `ade_ledger` crate but carry a different color by their own module doc-comment and the cluster TCB Color Maps — they are surfaced as sub-classification notes inside the `ade_ledger` entry.
- Modules are listed by TCB color (BLUE → GREEN → RED), alphabetical within each color.
- TCB color sources, in order of authority:
  1. `.idd-config.json` `core_paths` — substring match against absolute path. BLUE matches: `ade_codec`, `ade_types`, `ade_crypto`, `ade_core`, `ade_ledger`, `ade_plutus`, and the 9 `ade_network` submodule paths (`mux/frame.rs`, `codec/`, `handshake/`, `chain_sync/`, `block_fetch/`, `tx_submission/`, `keep_alive/`, `peer_sharing/`, `n2c/`).
  2. `.idd-config.json` `_core_paths_doc` — `ade_runtime` is RED; `ade_testkit` is GREEN; `ade_node` is RED; `ade_network::mux::transport` and `ade_network::session` are RED; `ade_network::lib` and `ade_network::mux::mod` are GREEN barrels. `ade_core_interop` is **RED** per its `Cargo.toml` header comment and the PHASE4-N-B TCB Color Map.
  3. `docs/clusters/PHASE4-N-B/cluster.md` § "TCB Color Map" — `era_schedule`, `praos_state`, `vrf_cert`, `nonce`, `op_cert`, `leader_schedule`, `header_validate`, `fork_choice`, `rollback` are BLUE; `chain_selector`, `candidate_fragment` are GREEN; `genesis_parser` is RED; `ade_testkit::consensus` is GREEN; `ade_core_interop` is RED.
  4. `docs/clusters/PHASE4-B1/cluster.md` § "TCB Color Map" — `ade_ledger::consensus_view`, `ade_ledger::block_validity` are BLUE; `ade_core::consensus::{header_validate, kes_check}` extensions are BLUE; `ade_testkit::validity` is GREEN; `ade_ledger::consensus_input_extract` is RED (parses an external dump format rather than a canonical type).
  5. PHASE4-B2 (tx-validity agreement) — `ade_ledger::tx_validity::{required_signers, witness, verdict, encoding, phase1, transition}` and `ade_ledger::mempool::admit` are **BLUE** (per their module doc-comments / `// Core Contract:` banners); `ade_ledger::mempool::policy` is **GREEN** (Tier-5, declared "GREEN (Tier-5)" in its own header); `ade_testkit::tx_validity::{extract, adversarial, valid_synthetic, mod}` is **GREEN**.
  6. PHASE4-B3 (full Conway tx value-conservation accounting) — all new and changed modules are **BLUE**, under the already-BLUE `ade_codec` / `ade_ledger` / `ade_types` crate prefixes (`.idd-config.json` `core_paths`): `ade_codec::conway::{cert, withdrawals}`, `ade_ledger::cert_classify`, the `ConwayOnlyDepositParams` / `ConwayDepositParams` additions to `ade_ledger::pparams`, the `conway_deposit_params` field + `conway_deposit_view()` on `ade_ledger::state`, the new `ValidationEnvironmentError` / `LedgerError` variants in `ade_ledger::error`, the full-equation `check_conway_coin_conservation` in `ade_ledger::conway`, the deposit-param fold in `ade_ledger::fingerprint`, and the `ConwayCert` / `CertDisposition` / `DepositEffect` / `CoinSource` types in `ade_types::conway::cert` plus `RewardAccount` in `ade_types::tx`.
  7. PHASE4-B5 (Conway governance-certificate accumulation) — all new and changed modules are **BLUE**, under the already-BLUE `ade_ledger` crate prefix (`.idd-config.json` `core_paths`): the new `ade_ledger::gov_cert` module (`apply_conway_gov_cert`), the `GovCertEnv` type + `LedgerState::gov_cert_env()` accessor in `ade_ledger::state`, the `ConwayOnlyDepositParams.drep_activity` field in `ade_ledger::pparams`, the `ValidationEnvironmentError::{MissingDRepActivityParam, DRepActivityOverflow}` variants in `ade_ledger::error`, the `Option<ConwayGovState>`-threading `accumulate_tx_certs` / `process_block_certificates` and gov-state-carrying `apply_block` in `ade_ledger::rules`, and the `drep_activity` extension to the Conway-deposit fingerprint tag in `ade_ledger::fingerprint`.

- **Post-3180e27 drift (GREEN only, no structural change).** Six commits land between the prior CODEMAP HEAD (`3180e27`) and this HEAD (`168ac02`): `3d94c22` (prior grounding refresh), `69e2d4b` (testkit hardening — already folded into the prior CODEMAP narrative under ENACTMENT-COMMITTEE-WRITEBACK), `b9cfaf9` (new GREEN real-chain committee oracle test for mainnet 575→576, strengthens DC-EPOCH-01 + DC-LEDGER-10), `396664a` (testkit/plutus alignment to a regenerated corpus, no source-shape change), `c78ec76` (new GREEN `#[ignore]`-gated `emit_reward_provenance` corpus generator), `168ac02` (snapshot-loader follow-up: tip-slot extraction via `parse_with_origin_slot`; Conway UMElem dual-layout decode in `parse_registered_credentials` — pre-Conway `StrictMaybe RDPair` vs Conway compact `[reward, deposit, …]`; fail-closed `terminated` guards on `parse_cold_credential_set` / `parse_cold_credential_epoch_map`; new `under-length set must reject` decode-rejects-malformed regression). All sit inside `ade_testkit` (GREEN). No BLUE/RED module added or removed; no new crate edge; no new CI script; no net new canonical type; no MUST NOT change. Mechanical counts: tests 1322 → 1325 (+3, all `ade_testkit`); CI checks 29 → 29; canonical types 376 → 376.

- Counts:
  - Crates: 11, from `Cargo.toml` `[workspace] members`.
  - Canonical types: 376, from `grep -rE "^(pub )?(struct|enum) "` across the full BLUE scope. Breakdown: `ade_codec` 8, `ade_types` 80, `ade_crypto` 12, `ade_core` 43, `ade_ledger` 127, `ade_plutus` 8, plus the 9 BLUE `ade_network` submodule paths 98. Registry `canonical_type_registry: null`, so a structural count is used. **Unchanged since 3180e27.** The post-3180e27 testkit drift adds no canonical type — `parse_with_origin_slot` and the new `terminated` guards on `parse_cold_credential_set` / `parse_cold_credential_epoch_map` are functions and field-local flags, not new public types; `SnapshotHeader` (`ade_testkit`, GREEN, not in the BLUE count) gained a `slot: u64` field in place; the new `emit_reward_provenance.rs` is an integration-test binary with no public types. All prior structural-count narrative carries forward unchanged: OQ5-CREDENTIAL-FIDELITY was a type-fidelity refactor on `StakeCredential` (`ade_types` stays at 80), the COMMITTEE/DREP/ENACTMENT-COMMITTEE re-keyings of `ConwayGovState` / `GovActionState` / `EnactmentEffects` are field-type changes on existing types, ENACTMENT-COMMITTEE-WRITEBACK re-shaped `GovAction::UpdateCommittee` in place and added a field to `EnactmentEffects` in place, PHASE4-B5 added `GovCertEnv` (`ade_ledger` 126 → 127), PHASE4-B4 added 6 owner-tagged apply types (`ade_ledger` 120 → 126), PHASE4-B3 added 12 (`ade_types` 76 → 80 + `ade_core` +1 unrelated + `ade_ledger` +7). `ade_codec` stays at 8 — all post-B3 additions to that crate are functions and `CodecError` variants, not new `struct`/`enum` types.
  - Tests: 1325 — count of `#[test]` / `#[tokio::test]` attributes across `crates/`. Reported as approximate per the template's fallback rule (test runner not executed). Per-crate at HEAD: `ade_codec` 134, `ade_types` 23, `ade_crypto` 49, `ade_core` 119, `ade_ledger` 461, `ade_plutus` 28, `ade_testkit` 293, `ade_runtime` 33, `ade_network` 176, `ade_node` 0, `ade_core_interop` 9. **+3 since 3180e27**, all `ade_testkit`: `committee_oracle_mainnet_575_576_noop_agreement` (in `crates/ade_testkit/tests/epoch_oracle_comparison.rs`, b9cfaf9 — replays the 575→576 epoch boundary against the real mainnet snapshot and asserts noop agreement; strengthens DC-EPOCH-01 + DC-LEDGER-10 by extending real-chain oracle coverage), `emit_alonzo310_registered_creds` and `emit_conway576_registered_creds` (in `crates/ade_testkit/tests/emit_reward_provenance.rs`, c78ec76 — `#[ignore]`-gated reward-provenance corpus generators; only run under `cargo test -- --ignored`). The prior CODEMAP header stated 1319 tests at 3180e27, which was already 3 under the mechanical count at that ref (1322); this regeneration uses the mechanical count at HEAD. All prior +N deltas (OQ5 +8, COMMITTEE +3, DREP +2, ENACTMENT-COMMITTEE-FIDELITY +1, ENACTMENT-COMMITTEE-WRITEBACK +9, PHASE4-B5 +17, PHASE4-B4 +17, PHASE4-B3F +2, PHASE4-B3 +39) carry forward unchanged from the prior CODEMAP.
  - CI checks: 29 — file count under `ci/ci_check_*.sh`. **Unchanged since 3180e27.** Inventory at HEAD: `cbor_round_trip`, `ce_n_a_5_proof`, `chaindb_contract`, `chaindb_crash_safety`, `consensus_closed_enums`, `constitution_coverage`, `conway_cert_classification_closed`, `credential_discriminant_closed`, `crypto_vectors`, `dependency_boundary`, `deposit_param_authority`, `differential_divergence`, `forbidden_patterns`, `gov_cert_accumulation_closed`, `hash_uses_wire_bytes`, `hfc_translation`, `ingress_chokepoints`, `ledger_determinism`, `module_headers`, `no_async_in_blue`, `no_chaindb_in_consensus_blue`, `no_density_in_fork_choice`, `no_float_in_consensus`, `no_secrets`, `no_semantic_cfg`, `no_signing_in_blue`, `pallas_quarantine`, `recovery_contract`, `ref_provenance`. ENACTMENT-COMMITTEE-WRITEBACK extended `ci/ci_check_credential_discriminant_closed.sh` in place (covers the live enactment surface — `EnactmentEffects.committee_changes` must stay `StakeCredential`-typed, `GovAction::UpdateCommittee.{removed,added}` must stay discriminated `StakeCredential`, `governance.rs` must define `apply_committee_enactment`, and `rules.rs` epoch boundary must call it). Same gate (DC-LEDGER-10) also covers OQ5, COMMITTEE-CRED-FIDELITY, DREP-VOTE-FIDELITY, and ENACTMENT-COMMITTEE-FIDELITY. The forward-looking `ci/ci_check_no_fail_open_in_validation.sh` (the `if len == K { check } else { skip }` grep gate, DC-VAL-06 / DC-TXV) is still **not** shipped. No `.github/workflows/` yet.

---

## BLUE Modules — Pure Functional Core

> **Shared header (applies to every BLUE entry below).** Every `.rs` source file begins with the contract banner
> `// Core Contract:` and the following deny attributes are present in each crate's `lib.rs` (or, for `ade_network`,
> at the crate root — the BLUE submodules inherit them):
> `#![deny(unsafe_code)]`, `#![deny(clippy::unwrap_used)]`, `#![deny(clippy::expect_used)]`,
> `#![deny(clippy::panic)]`, `#![deny(clippy::float_arithmetic)]`.
> CI scripts that enforce the shared rules (all 7 scope the full BLUE set — the 6 BLUE crates plus the 9 BLUE `ade_network` submodule paths declared in `.idd-config.json` `core_paths`):
> - `ci/ci_check_module_headers.sh` — banner first-line check.
> - `ci/ci_check_forbidden_patterns.sh` — `HashMap`, `HashSet`, `IndexMap`/`IndexSet`/`indexmap::`, `SystemTime`, `Instant`, `std::fs`, `std::net`, `tokio`, `async fn`, `f32`/`f64`, `anyhow`, `rand::thread_rng`, `thread::spawn`, plus `unsafe` outside a documented allowlist.
> - `ci/ci_check_dependency_boundary.sh` — no BLUE crate depends on a RED crate.
> - `ci/ci_check_no_signing_in_blue.sh` — `SigningKey`/`SecretKey`/`PrivateKey`/`private_key`/`sign_message`/`sign_block` forbidden in BLUE.
> - `ci/ci_check_no_semantic_cfg.sh` — `#[cfg(feature = ...)]` and `cfg!(feature = ...)` forbidden in BLUE.
> - `ci/ci_check_hash_uses_wire_bytes.sh` — no hashing of `canonical_bytes` / re-encoded bytes in BLUE.
> - `ci/ci_check_ingress_chokepoints.sh` — only named `decode_*` chokepoints construct `PreservedCbor`. The named-chokepoint list covers `decode_block_envelope`, the per-era block decoders (`decode_byron_ebb_block`, `decode_byron_regular_block`, `decode_shelley_block`, `decode_allegra_block`, `decode_mary_block`, `decode_alonzo_block`, `decode_babbage_block`, `decode_conway_block`), `decode_address`, and `PlutusScript::from_cbor` in `ade_plutus`. Check 3 explicitly allowlists `ade_plutus/src/evaluator.rs` because Plutus script CBOR is a distinct ingress surface from block CBOR (its decoder goes through aiken/pallas rather than the ade_codec primitives).
> - `ci/ci_check_pallas_quarantine.sh` — `pallas-*` references confined to `ade_plutus`.
> - `ci/ci_check_no_async_in_blue.sh` *(PHASE4-N-A, S-A1)* — `async fn`, `.await`, `tokio`, `async_std`, `futures::`, `tokio::spawn` / `async_std::task::spawn` / `smol::spawn`, and `tokio::time::*` / `async_std::task::sleep` / `futures_timer::*` forbidden anywhere in the BLUE scope. Enforces DC-CORE-01.
>
> Three additional CI scripts narrow the shared header to the `ade_core::consensus` tree specifically (PHASE4-N-B):
> - `ci/ci_check_no_chaindb_in_consensus_blue.sh` — `ChainDb` / `chain_db` token forbidden anywhere under `crates/ade_core/src/consensus/`. Strengthens DC-CORE-01 + DC-CONS-07.
> - `ci/ci_check_no_float_in_consensus.sh` — `f32`/`f64` token forbidden anywhere under `crates/ade_core/src/consensus/`. Narrows T-CORE-02 + DC-CONS-07/08/09 to the consensus surface.
> - `ci/ci_check_consensus_closed_enums.sh` — `#[non_exhaustive]`, open-tail `Other`/`Unknown` enum variants, owned `String` fields, and `Box<dyn ...>` are all forbidden. `TARGETS` scope: `crates/ade_core/src/consensus/`, `crates/ade_ledger/src/block_validity/`, `crates/ade_ledger/src/tx_validity/`, `crates/ade_ledger/src/mempool/` (extended for B1 then B2). Strengthens DC-CONS-04 + DC-CONS-10 + T-DET-01, and gates DC-VAL-02/04/05/06 + DC-TXV-01/02/04/05 + DC-MEM-01/02.
>
> A fourth narrow check enforces a single fork-choice rule:
> - `ci/ci_check_no_density_in_fork_choice.sh` — the term `density` (case-insensitive) is forbidden in `crates/ade_core/src/consensus/fork_choice.rs` and `crates/ade_core/src/consensus/candidate.rs`, with a documented `// no-density:` annotation as the only allowed exception. Strengthens DC-CONS-03.
>
> A fifth narrow check (PHASE4-B3) enforces deposit-parameter authority over the BLUE crates:
> - `ci/ci_check_deposit_param_authority.sh` — across `BLUE_CRATES=(ade_codec ade_types ade_crypto ade_core ade_ledger ade_plutus)`, every deposit/refund amount (`key_deposit`, `pool_deposit`, `drep_deposit`, `gov_action_deposit`) must be sourced from canonical ledger state — `ProtocolParameters.{key_deposit,pool_deposit}` plus `LedgerState.conway_deposit_params` — and NEVER from a literal constant written next to a deposit field, nor from a testkit `ConwayGovParams`. The only legitimate write path is the RED snapshot loader in `ade_testkit` populating `conway_deposit_params` from parsed snapshot bytes. Enforces DC-TXV-07.
>
> A sixth narrow check (PHASE4-B5) enforces gov-cert accumulation closure:
> - `ci/ci_check_gov_cert_accumulation_closed.sh` — defends four properties of the gov-accumulation surface: (1) `apply_conway_gov_cert` exists and its `ConwayCert` match has no `_ =>` catch-all; (2) the B4 observe-and-drop is gone — the "routed out of B4 mutation scope" comment must not reappear and `accumulate_tx_certs` must call `apply_conway_gov_cert`; (3) DRep expiry uses `checked_add`; (4) `MissingDRepActivityParam` env fail-fast + `gov_cert_env()` constructor are present. Enforces DC-LEDGER-09.
>
> A seventh narrow check (OQ5/COMMITTEE/DREP/ENACTMENT-COMMITTEE-FIDELITY + ENACTMENT-COMMITTEE-WRITEBACK) enforces credential-discriminant closure:
> - `ci/ci_check_credential_discriminant_closed.sh` — defends, in one extended gate: `StakeCredential` is the closed `enum` in `ade_types::shelley::cert` (the tuple-struct `StakeCredential(Hash28)` must not reappear); both era decoders (`ade_codec::shelley::cert` / `ade_codec::conway::cert`) preserve the tag and reject an unknown discriminant; no bare-hash `StakeCredential(<hash>)` tuple-construction coercion remains on the BLUE path; `ConwayGovState.{vote_delegations,committee_hot_keys,drep_expiry,committee}` stay `StakeCredential`-keyed; `GovActionState.{committee_votes,drep_votes}` stay `Vec<(StakeCredential, Vote)>`; `EnactmentEffects.committee_changes` stays `Option<(Vec<StakeCredential>, Vec<(StakeCredential, u64)>)>`; `GovAction::UpdateCommittee.{removed,added}` stay discriminated `StakeCredential`; `governance.rs` defines `apply_committee_enactment`; `rules.rs` epoch boundary calls it. Enforces DC-LEDGER-10.
>
> A BLUE crate or BLUE `ade_network` submodule that adds a feature flag, an async function, a `HashMap`, or a RED dep fails CI on push.
> The 3 RED-scope CI scripts (`ci_check_chaindb_contract.sh`, `ci_check_recovery_contract.sh`, `ci_check_chaindb_crash_safety.sh`) and the 1 evidence script `ci_check_ce_n_a_5_proof.sh` are not part of this shared header — they are documented in the cross-module CI matrix at the bottom.

---

### `ade_codec`

> **Status carried from prior CODEMAP — unchanged at HEAD `168ac02`.** No change since `3180e27`.
>
> Inherits all prior-cluster status notes: PHASE4-B3 added the two new closed-grammar Conway decoders under `conway/` (`conway::cert::decode_conway_certs` — closed grammar over CDDL tags `0..18` with `CodecError::UnknownCertTag` for tags ≥19, tags 5/6 decoding to `ConwayCert::RemovedInConway`, **no catch-all accept arm** — and `conway::withdrawals::{decode_withdrawals, withdrawals_sum}` — `BTreeMap<RewardAccount, Coin>` with `CodecError::DuplicateMapKey` reject, never last-wins, plus exact-`i128` sum) and two new `CodecError` variants (`UnknownCertTag { tag, offset }`, `DuplicateMapKey { offset }`). PHASE4-B3F hardened `decode_conway_certs` to reject trailing bytes (`CodecError::TrailingBytes`, parity with `decode_withdrawals`) and bound preallocation by remaining input. PHASE4-B4 made `decode_conway_certs` **owner-complete** — every `ConwayCert` variant retains all owner payloads (stake / DRep / committee credentials, pool id, full `PoolRegistrationCert` incl. `pool_owners`, DRep delegation targets); a new closed `decode_drep` reads the `drep` grammar with no catch-all; the single shared pool-params decoder moved to `crates/ade_codec/src/shelley/cert.rs` as `pub(crate) read_pool_registration_cert`, called by both era cert decoders. OQ5-CREDENTIAL-FIDELITY made both era credential decoders (`shelley::cert::decode_stake_credential` and `conway::cert::decode_stake_credential`) **preserve the key/script discriminant** — `0 → StakeCredential::KeyHash`, `1 → StakeCredential::ScriptHash`, an unknown tag rejects with `CodecError::InvalidCborStructure { detail: "unknown stake credential type" }`; the prior tag-discarding `let (_cred_type, _) = …` / `let (_tag, _) = …` form is gone.

| Attribute | Value |
|-----------|-------|
| **Purpose** | Owns Cardano-canonical CBOR ingress: the only place in the workspace that turns raw bytes into typed semantic values, with wire-byte preservation for every hash-bearing structure. |
| **Creates** | `PreservedCbor<T>`, `RawCbor`, `BlockEnvelope`, `ByronDecodedBlock`, `CodecContext`, `CodecError` (incl. `UnknownCertTag`, `DuplicateMapKey`, `TrailingBytes`), `ContainerEncoding`, `IntWidth`, plus era-tagged block/tx wrappers under `byron/`, `shelley/`, `allegra/`, `mary/`, `alonzo/`, `babbage/`, `conway/`. Functions (not types): `conway::cert::{decode_conway_certs, decode_drep}`, `conway::withdrawals::{decode_withdrawals, withdrawals_sum}`, `shelley::cert::read_pool_registration_cert`, both era `decode_stake_credential`. |
| **Interprets** | All canonical Cardano CBOR — block envelopes, era-specific blocks, tx bodies, tx outs, certificates, addresses. Conway certificate array (closed over CDDL tags 0..18, owner-complete) and Conway withdrawals map (deduplicated). Both era credential decoders preserve the key/script tag. Sole authority for `PreservedCbor::new` (constructor is `pub(crate)`). |
| **MUST NOT** | (1) Construct `PreservedCbor` outside `ade_codec` (enforced by `pub(crate)` + `ci_check_ingress_chokepoints.sh`). (2) Re-encode wire bytes when computing hashes (`ci_check_hash_uses_wire_bytes.sh`). (3) Use any forbidden BLUE pattern. (4) Depend on any other workspace crate except `ade_types`. (5) **`conway::cert` (DC-LEDGER-08):** silently accept an unknown or removed certificate tag — tags ≥19 reject with `CodecError::UnknownCertTag`, tags 5/6 decode to `ConwayCert::RemovedInConway` (never an accept); no catch-all arm; trailing bytes after the cert array reject (`CodecError::TrailingBytes`); preallocation bounded by remaining input; owner payloads retained; `read_pool_registration_cert` retains `pool_owners`; `decode_drep` is closed (no catch-all). (6) **`conway::withdrawals`:** never last-wins on a duplicate `RewardAccount` key — `CodecError::DuplicateMapKey`. (7) **`shelley::cert` / `conway::cert` `decode_stake_credential` (DC-LEDGER-10):** must not erase the credential tag — discriminant is read and mapped to `StakeCredential::{KeyHash, ScriptHash}`; an unknown tag rejects (`CodecError::InvalidCborStructure { detail: "unknown stake credential type" }`), never tag-discard, never bare-hash coercion (enforced by `ci_check_credential_discriminant_closed.sh`). |
| **Inbound deps** | `ade_ledger`, `ade_plutus`, `ade_testkit`, `ade_network`, `ade_runtime` (consensus genesis parser uses canonical CBOR primitives for the bootstrap anchor). |
| **Outbound deps** | `ade_types`. No external dependencies; std-only. Dev-deps: `serde_json`, `toml`. |
| **Entry points** | `ade_codec::cbor::envelope::decode_block_envelope` (31 import sites), `ade_codec::cbor`, `ade_codec::traits::AdeEncode`, `ade_codec::CodecContext`, per-era `decode_*_block` functions, `ade_codec::address::decode_address`. B2: `ade_codec::conway::tx::decode_conway_tx_body` consumed by `ade_ledger::tx_validity::phase1::decode_tx`. B3: `ade_codec::conway::cert::decode_conway_certs` and `ade_codec::conway::withdrawals::{decode_withdrawals, withdrawals_sum}` consumed by `ade_ledger::conway::check_conway_coin_conservation`. B4: `ade_codec::conway::cert::decode_conway_certs` (owner-complete) consumed by `ade_ledger::rules::accumulate_tx_certs` Conway branch; `ade_codec::shelley::cert::decode_certificates` by the Shelley..Babbage branch. Also `ade_codec::cbor::{canonical_width, write_array_header, write_bytes_canonical, ContainerEncoding}` consumed by `ade_runtime::consensus::genesis_parser::compute_anchor_hash`. |
| **Key modules** | `cbor/`, `byron/`, `shelley/` (incl. `cert.rs`'s shared `pub(crate) read_pool_registration_cert`), `allegra/`, `mary/`, `alonzo/`, `babbage/`, `conway/` (incl. `cert.rs` owner-complete + `decode_drep` + discriminant-preserving `decode_stake_credential`; `withdrawals.rs`), `address/`, `preserved.rs` (`PreservedCbor`), `traits.rs`, `primitives.rs`, `error.rs`. |

---

### `ade_core`

> **Status carried from prior CODEMAP — unchanged at HEAD `168ac02`.** PHASE4-N-B established the consensus authority surface under `consensus/`; PHASE4-B1 added the Praos VRF verify path, the `kes_check` module, the `HeaderVrf`/`HeaderKes` carriers in `header_summary`, and `FieldKind`/`FieldError` + new `HeaderValidationError` variants. PHASE4-B2/B3/B4/B5 and the post-3180e27 drift did not touch `ade_core`.

| Attribute | Value |
|-----------|-------|
| **Purpose** | BLUE authoritative Praos consensus core. Owns canonical types and pure state-transitions that decide which header / chain Ade accepts: HFC era schedule + slot↔era↔time translation, Praos chain-dep state (nonce / op-cert counters), VRF cert verification (TPraos two-proof **and** Praos single-proof) + leader-eligibility predicate, KES signature + op-cert period verification wired into header admission, header validation pipeline, fork-choice (`(BlockNo, TiebreakerView)`), rollback authority, leader-schedule query, and canonical encodings of all chain-dep state and chain events. |
| **Creates** | **Schedule:** `BootstrapAnchorHash`, `EraSchedule`, `EraSummary`, `EraLocation`. **State:** `PraosChainDepState`, `OpCertCounterMap`, `Nonce`. **Events/points:** `Point`, `ChainHash`, `BlockDistance`, `SecurityParam`, `ChainEvent`, `ChainSelectionReject`. **Errors:** `HFCError`, `SlotTimeError`, `OutsideForecastRange`, `HeaderValidationError`, `FieldError`, `FieldKind`, `VrfCertError`, `OpCertCounterError`, `NonceEvolutionError`, `LeaderScheduleError`. **Header surface:** `HeaderInput`, `HeaderVrf` (closed TPraos/Praos VRF discriminant), `HeaderKes`, `ValidatedHeaderSummary`, `HeaderApplied`. **Fork-choice:** `TiebreakerView`, `CandidateFragment`, `ChainSelectorState`, `ForkChoiceError`. **Op-cert/nonce:** `OpCertObservation`, `NonceInput`. **VRF:** `VrfRole`, `VerifiedVrf`, `StakeFraction`, `ActiveSlotsCoeff`. **Leader schedule:** `LeaderScheduleQuery`, `LeaderScheduleAnswer`. **Ledger view boundary:** `LedgerView` trait. **Rollback:** `RollBackRequest`, `RollBackApplied`. **Encoding:** `DecodeError`. 43 public types across the consensus tree. |
| **Interprets** | Canonical inputs from the `ade_runtime` shell — `HeaderInput` projections (already-parsed header fields, carrying `HeaderVrf` + `HeaderKes`), `EraSchedule` materialized once from genesis JSON, `LedgerView` snapshots from `ade_ledger::consensus_view::PoolDistrView`, and an ordered stream of `StreamInput` events. KES check verifies the hot KES key signature over the header **body** CBOR bytes (`currentKESPeriod = slot / SLOTS_PER_KES_PERIOD`, mainnet `129_600`). All inputs are typed values; BLUE never reads raw bytes, files, sockets, or a chain store. Also decodes own canonically-encoded `PraosChainDepState` and `ChainEvent` for replay. |
| **MUST NOT** | (1) Take or accept a `&ChainDb` / `&chain_db` / any storage reference — `ci_check_no_chaindb_in_consensus_blue.sh` (DC-CORE-01 + DC-CONS-07). (2) Use any `f32`/`f64` — `ci_check_no_float_in_consensus.sh`; the leader-eligibility predicate uses Q.123 fixed-point in `u128`. (3) Add `#[non_exhaustive]` to any consensus enum, define an open-tail `Other`/`Unknown`, hold owned `String` fields in `errors.rs`/`encoding.rs`/`events.rs`, or use `Box<dyn ...>` — `ci_check_consensus_closed_enums.sh` (DC-CONS-04 + DC-CONS-10 + T-DET-01). (4) Reference `density` in `fork_choice.rs`/`candidate.rs` outside a `// no-density:` annotation — `ci_check_no_density_in_fork_choice.sh` (DC-CONS-03). (5) Read wall-clock time — `slot_to_time_ms` is pure integer arithmetic; DC-CONS-08. (6) Use `HashMap`/`HashSet`; `OpCertCounterMap` uses `BTreeMap`. (7) `async fn`, `.await`, `tokio` — `ci_check_no_async_in_blue.sh`. (8) Construct a `PreservedCbor`. (9) Re-derive stake snapshots — consume `LedgerView` only (DC-CONSENSUS-02). (10) Bypass the canonical `validate_and_apply_header` pipeline. (11) **B1 fail-closed rule (DC-VAL-06):** never apply a fixed-size crypto-field check via `if len == K { check } else { skip }`; every size guard goes through `kes_check::expect_size`. (12) **B1 KES rule (DC-CRYPTO-01):** never accept a header whose KES signature or op-cert period fails; `verify_header_kes` is fail-closed. (13) All shared-header BLUE rules. |
| **Inbound deps** | `ade_ledger` (B1 — `block_validity` calls `validate_and_apply_header`), `ade_runtime` (consensus orchestrator + genesis parser), `ade_testkit`, `ade_core_interop`. |
| **Outbound deps** | `ade_types`, `ade_crypto` (`blake2b`, `verify_vrf`, `kes::{verify_kes, verify_opcert, build_opcert_signable}`), `minicbor`. Dev-deps: `ade_testkit`, `serde_json`, `cardano-crypto` (vrf-draft03; tests only). |
| **Entry points** | `use ade_core::consensus::{...}` aggregator, `ade_core::consensus::ledger_view::LedgerView`, `ade_core::consensus::vrf_cert::{vrf_input, verify_praos_vrf, praos_vrf_input, praos_leader_value, praos_nonce_value}`, `ade_core::consensus::kes_check::{verify_header_kes, expect_size}`, `ade_core::consensus::praos_state::{Nonce, PraosChainDepState}`, `ade_core::consensus::header_summary::{HeaderInput, HeaderVrf, HeaderKes, ValidatedHeaderSummary}`. Top-level transitions: `validate_and_apply_header`, `select_best_chain`, `apply_rollback`, `apply_nonce_input`, `apply_op_cert`, `query_leader_schedule` + `is_leader_for_vrf_output`, `verify_vrf_cert` + `verify_praos_vrf` + `check_leader_claim` + `is_leader`, `tiebreaker_prefer`, `encode/decode_chain_dep_state`, `encode/decode_chain_event`. |
| **Key modules** | `consensus/era_schedule.rs`, `consensus/praos_state.rs`, `consensus/events.rs`, `consensus/errors.rs`, `consensus/vrf_cert.rs`, `consensus/kes_check.rs`, `consensus/nonce.rs`, `consensus/op_cert.rs`, `consensus/leader_schedule.rs`, `consensus/header_summary.rs` + `consensus/header_validate.rs`, `consensus/candidate.rs`, `consensus/fork_choice.rs`, `consensus/rollback.rs`, `consensus/ledger_view.rs`, `consensus/encoding.rs`. |

---

### `ade_crypto`

> **Status carried from prior CODEMAP — unchanged at HEAD `168ac02`.**

| Attribute | Value |
|-----------|-------|
| **Purpose** | Owns pure cryptographic verification primitives — Blake2b hashing, Ed25519 / Byron-bootstrap signature verification, KES verification with operational certificates, VRF verification. Verification only — signing lives in `ade_runtime`. |
| **Creates** | `Blake2b224`, `Blake2b256`, `HashAlgorithm` trait, `Ed25519VerificationKey`, `Ed25519Signature`, `ByronExtendedVerificationKey`, `KesVerificationKey`, `KesPeriod`, `OperationalCertData`, `VrfVerificationKey`, `VrfProof`, `VrfOutput`, `CryptoError`. |
| **Interprets** | Verification key / signature / proof byte structures. Not a CBOR parser — accepts already-decoded byte slices. |
| **MUST NOT** | (1) Implement signing (`ci_check_no_signing_in_blue.sh`). (2) Allocate global state — verification is `fn(&[u8], &[u8]) -> bool`. (3) Use any BLUE forbidden pattern. (4) Use `unsafe` outside the allowlisted FFI in `src/vrf.rs`. (5) **B1:** `build_opcert_signable` must produce the spec-correct **raw concatenation** of the op-cert fields, not a CBOR-wrapped form. |
| **Inbound deps** | `ade_core` (consensus VRF wrapping, KES via `kes_check`, blake2b), `ade_ledger`, `ade_plutus`, `ade_testkit`. |
| **Outbound deps** | `ade_types`, `blake2`, `ed25519-dalek`, `cardano-crypto` (vrf-draft03 + kes-sum + dsign features, `default-features = false`). |
| **Entry points** | `ade_crypto::blake2b::blake2b_256` (top external), `blake2b_224`, `blake2b_256`, `block_header_hash`, `transaction_id`, `script_hash`, `credential_hash`, `verify_ed25519`, `verify_byron_bootstrap`, `verify_kes`, `verify_opcert`, `verify_vrf`. B2: `ade_crypto::{blake2b_224, verify_ed25519, Ed25519Signature, Ed25519VerificationKey}` consumed by `ade_ledger::tx_validity::witness::verify_required_witnesses`. `ade_crypto::vrf::*` consumed by `ade_core::consensus::vrf_cert`; `ade_crypto::kes::*` consumed by `ade_core::consensus::kes_check`. |

---

### `ade_ledger`

> **Status at HEAD `168ac02` — unchanged source vs `3180e27`.** All prior cluster status notes carry forward; no new module, no new crate edge, no net new canonical type, no MUST NOT change. The post-3180e27 drift is GREEN-scope only and lives in `ade_testkit`.
>
> **Status from ENACTMENT-COMMITTEE-WRITEBACK (strengthens DC-EPOCH-01 and DC-LEDGER-10).** The committee-enactment write-back is wired — the dormant `EnactmentEffects.committee_changes` field is live. `governance::enact_proposals`'s `GovAction::UpdateCommittee` arm is no longer a `let _ = raw` no-op: it populates `committee_changes` (discriminated removed-set + added-with-expiry map) and `EnactmentEffects.committee_threshold: Option<(u64,u64)>`. The new pure transition `governance::apply_committee_enactment` applies `committee_dissolved` → `committee_changes` → `committee_threshold` deterministically. The epoch-boundary apply site in `rules.rs` calls it and writes the result into the next `ConwayGovState` (replacing the prior clone-unchanged that dropped both effects — so a ratified `NoConfidence` no longer leaves the committee intact: DC-EPOCH-01 was violable, now enforced). `fingerprint::write_gov_action`'s `UpdateCommittee` arm emits the wire-faithful 5-field Conway shape, replacing the opaque `[4, prev, raw_bytes]` (T-DET-01 migration). Members removed/added stay discriminated `StakeCredential` end-to-end (DC-LEDGER-10). `EnactmentEffects` gains a `committee_threshold` field in place; `apply_committee_enactment` is a function (no new type); `ci/ci_check_credential_discriminant_closed.sh` extended in place (no new CI script).
>
> **Status from ENACTMENT-COMMITTEE-FIDELITY (DC-LEDGER-10 strengthened, `strengthened_in += ENACTMENT-COMMITTEE-FIDELITY`).** A preventive type migration: `governance::EnactmentEffects.committee_changes` re-typed `Option<(Vec<Hash28>, Vec<(Hash28, u64)>)>` → `Option<(Vec<StakeCredential>, Vec<(StakeCredential, u64)>)>`. At HEAD this field is **live** (post-ENACTMENT-COMMITTEE-WRITEBACK); the discriminated type prevents re-collapse on write-back.
>
> **Status from DREP-VOTE-FIDELITY (DC-LEDGER-10 strengthened, `strengthened_in += DREP-VOTE-FIDELITY`).** DRep tally now resolves by **exact credential variant** — `lookup_stake` maps `StakeCredential::KeyHash → DRep::KeyHash` and `StakeCredential::ScriptHash → DRep::ScriptHash` with no key/script OR-fallback. `fingerprint::write_committee_vote_list` was renamed `write_credential_vote_list` (now serves committee + DRep); `drep_votes` is routed through it; `spo_votes` stays on `write_vote_list` by design.
>
> **Status from OQ5-CREDENTIAL-FIDELITY.** `state::ConwayGovState` re-keyed `vote_delegations` / `committee_hot_keys` / `drep_expiry` from bare `Hash28` to `StakeCredential` (matching cardano-node's `Credential`-keyed UMap/VState). `fingerprint::write_stake_credential` emits discriminant (`0`/`1`) then 28-byte hash; the gov-map fingerprint writers call it (T-DET-01 migration). `gov_cert::apply_conway_gov_cert` consumes the discriminated credential directly. `governance::evaluate_ratification` / `check_ratification` and `rules::apply_epoch_boundary_with_registrations` use `cred.hash()` ONLY as a read-only boundary adapter against the `Hash28`-keyed stake-distribution snapshot (declared non-goal surface).
>
> **Status from COMMITTEE-CRED-FIDELITY (DC-LEDGER-10 strengthened, `strengthened_in += COMMITTEE-CRED-FIDELITY`).** `state::ConwayGovState.committee` re-keyed `Hash28` → `StakeCredential`. `governance::evaluate_ratification` / `check_ratification` resolve a vote by full-credential equality — no `.hash()` on the committee path. `fingerprint` gained `write_committee_vote_list` (renamed `write_credential_vote_list` in DREP-VOTE-FIDELITY).
>
> **Status from PHASE4-B5.** Shipped the **closed, total, era-versioned Conway governance-certificate accumulation** (DC-LEDGER-09, `status=enforced`), retiring B4's observe-and-drop. New BLUE `gov_cert` — `apply_conway_gov_cert(&ConwayGovState, &ConwayCert, Option<&GovCertEnv>) -> Result<ConwayGovState, LedgerError>`: native total dispatch over owner-complete `ConwayCert` that applies governance-affecting certs into `ConwayGovState` — vote-delegation (9/10/12/13) → `vote_delegations[cred] = drep`; committee auth (14) → `committee_hot_keys[hot] = cold`; committee cold-resign (15) → clears that member's hot authorizations; DRep register/update (16/18) → `drep_expiry[cred] = env.current_epoch + env.drep_activity` (env-driven, `checked_add`); DRep unregister (17) → removes `drep_expiry[cred]`. Mutates **only** governance-owned fields of `ConwayGovState`; neither takes nor returns B4-owned `CertState`. `state` gained `GovCertEnv { current_epoch, drep_activity }` + fail-fast `LedgerState::gov_cert_env()`; `pparams` gained `ConwayOnlyDepositParams.drep_activity`; `error` gained `ValidationEnvironmentError::{MissingDRepActivityParam, DRepActivityOverflow}`. `rules::accumulate_tx_certs` / `process_block_certificates` thread `Option<ConwayGovState>`; `apply_block` carries `gov_state` forward. `fingerprint`'s Conway-deposit tag extended 2→3 fields for `drep_activity`.
>
> **Status from PHASE4-B4.** Shipped the **closed, total, era-versioned Conway cert-state accumulation** (DC-LEDGER-08, `status=enforced`). `delegation` gained the native owner-tagged Conway apply model: `apply_conway_cert(state, cert, env) -> Result<ConwayCertOutcome, LedgerError>`, closed `conway_cert_action(&ConwayCert) -> ConwayCertAction` (compiler-exhaustive over all 18 tags + 5/6), and types `ConwayCertAction`, `GovernanceOwner`, `GovernanceCertEffect`, `OwnerTaggedEffect`, `ConwayCertOutcome`, `ConwayCertEnv`. Delegation/pool certs mutate B4-owned `CertState`; governance-affecting certs are owner-tagged to `ConwayGovState` (B5 applies them). Composite certs (10/12/13) do both. Removed tags 5/6 reject with `LedgerError::EraInvalidCertificate`. `apply_pool_registration` populates `PoolParams.owners`. `rules::process_block_certificates` era-dispatches via `accumulate_tx_certs`: decode/apply errors halt block transition as structured `LedgerError` (both prior fail-open swallows removed).
>
> **Status from PHASE4-B3.** Closed the Conway tx value-conservation accounting. New BLUE submodule `cert_classify` (total closed map over `ConwayCert`; returns structured reject rather than guessing state-dependent deposit/refund). `pparams` gained `ConwayOnlyDepositParams` + `ConwayDepositParams` view; `state` gained `LedgerState.conway_deposit_params: Option<ConwayOnlyDepositParams>` + fail-fast `conway_deposit_view()`; `error` gained `ValidationEnvironmentError::MissingConwayDepositParams` and `LedgerError::{EraInvalidCertificate, UnsupportedStateDependentDeposit, ValidationEnvironment}`. **`conway::check_conway_coin_conservation` enforces the FULL preservation-of-value equation** — `consumed = inputs + withdrawals + refunded_deposits == produced = outputs + fee + donation + new_deposits`, with §9.1 reject precedence (decode → structural → state-dependent → conservation last); the B2 cert/withdrawal early-out is **REMOVED**. `fingerprint::fingerprint_pparams` folds the Conway deposit params under `CONWAY_DEPOSIT_PARAMS_TAG` (byte-identical for non-Conway states).
>
> **Prior status (PHASE4-B2 / PHASE4-B1).** B2: transaction-validity authority (`tx_validity::*`, `mempool::admit` BLUE, `mempool::policy` GREEN) and pure-spend `check_conway_coin_conservation`. B1: block-validity authority; depends on `ade_core` (acyclic). BLUE submodules `consensus_view` (`PoolDistrView`) and `block_validity::*`; RED submodule `consensus_input_extract`.

| Attribute | Value |
|-----------|-------|
| **Purpose** | The functional core (ledger half): stateless ledger rules for every Cardano era — UTxO transitions, certificate effects, fee/value conservation, epoch boundary accounting, hard-fork translations, governance ratification/enactment, canonical fingerprint of `LedgerState`; the B1 top-level **block-validity** verdict; the B2 top-level **transaction-validity** verdict (`tx_validity`, DC-TXV-01..05) plus the **mempool admission** gate (`mempool::admit`, DC-MEM-01); as of B3, the **full Conway value-conservation accounting** — closed certificate-deposit classification (`cert_classify`, DC-TXV-06) sourcing every deposit/refund from canonical deposit-parameter authority (`pparams`/`state`, DC-TXV-07); as of B4, the **closed, total, era-versioned Conway cert-state accumulation** (`delegation::apply_conway_cert`, DC-LEDGER-08); as of B5, the **closed, total, era-versioned Conway governance-certificate accumulation** (`gov_cert::apply_conway_gov_cert`, DC-LEDGER-09); and, as of ENACTMENT-COMMITTEE-WRITEBACK, the live committee-enactment write-back that rewrites the next-epoch `ConwayGovState.committee` + `committee_quorum` from ratified `NoConfidence` / `UpdateCommittee` governance actions (DC-EPOCH-01). |
| **Creates** | Pre-B1: `LedgerState`, `EpochState`, `UTxOState`, `TxOut`, `Value`, `MultiAsset`, `AssetName`, `DelegationState`, `CertState`, `PoolParams`, `ConwayGovState` (post-OQ5/COMMITTEE: discriminated-`StakeCredential`-keyed gov maps + committee), `GovActionState` (post-COMMITTEE/DREP: `committee_votes` + `drep_votes` carry `StakeCredential`), `RatificationResult`, `EnactmentEffects` (post-ENACTMENT-COMMITTEE-FIDELITY: discriminated `committee_changes`; post-WRITEBACK: live + new `committee_threshold` field), `ProtocolParameters`, `BlockApplyResult`, `BlockVerdict`, `TxVerdict`, `ScriptPosture`, `EpochBoundaryResult`, `LedgerFingerprint`, `Rational`, `WitnessInfo`, the layered `LedgerError` family. **B1 (`consensus_view`):** `PoolDistrView`, `PoolEntry`. **B1 (`block_validity`):** `BlockValidityOutcome`, `DecodedBlock`, `BlockValidityVerdict`, `BlockRejectClass`, `BlockValidityError`, `FieldError`, `FieldKind`, `MissingInput`, `VerdictSurface`, `SurfaceDecodeError`. **B2 (`tx_validity`):** `TxValidityVerdict`, `TxRejectClass`, `TxValidityError`, `TxValidityOutcome`, `TxVerdictSurface`, `TxSurfaceDecodeError`, `DecodedTx`, `VKeyWitnessRef`, `WitnessClosureError`, `WitnessField`, `SignerSource`, `RequiredSigners`, `RequiredSignerError`, `RequiredSignerField`, `ResolvedInputs`, `ResolvedOutput`. **B2 (`mempool`):** `MempoolState`, `AdmitOutcome` (BLUE); `OrderPolicy` (GREEN). **B3 (`pparams`):** `ConwayOnlyDepositParams` (post-B5: + `drep_activity`), `ConwayDepositParams`. **B3 (`error`):** `ValidationEnvironmentError` (incl. `MissingConwayDepositParams`; post-B5: + `MissingDRepActivityParam`, `DRepActivityOverflow`), `UnsupportedStateDependentDepositAccounting`, `EraInvalidCertificateError`. **B4 (`delegation`):** `ConwayCertAction`, `GovernanceOwner`, `GovernanceCertEffect`, `OwnerTaggedEffect`, `ConwayCertOutcome`, `ConwayCertEnv`. **B5 (`state`):** `GovCertEnv`. Functions (no new types): `cert_classify::classify`, `delegation::{apply_conway_cert, conway_cert_action}`, `gov_cert::apply_conway_gov_cert`, `governance::apply_committee_enactment` (ENACTMENT-COMMITTEE-WRITEBACK). |
| **Interprets** | Decoded canonical types from `ade_codec`. Hosts per-era validation composers and the late-era witness decoder. **B1:** `block_validity::header_input::decode_block`; `block_validity::encoding::*`. **B2:** `tx_validity::phase1::decode_tx` lifts the preserved Conway tx-body slice; `tx_validity::encoding::*` is the canonical per-tx replay/comparison surface. **B3:** `conway::check_conway_coin_conservation` decodes the cert array via `ade_codec::conway::cert::decode_conway_certs`, classifies via `cert_classify::classify`, decodes withdrawals via `ade_codec::conway::withdrawals::decode_withdrawals`, sums exactly (`i128`), balances the full equation. **B4:** `rules::accumulate_tx_certs` era-dispatches (Conway → owner-complete `decode_conway_certs` + `apply_conway_cert`; Shelley..Babbage → `decode_certificates` + `apply_cert`). **B5:** `accumulate_tx_certs` also feeds each owner-complete `ConwayCert` into `gov_cert::apply_conway_gov_cert` (when `gov_state.is_some()`); DRep register/update consult fail-fast `gov_cert_env()` for `current_epoch + drep_activity` with `checked_add`. **ENACTMENT-COMMITTEE-WRITEBACK:** `rules::apply_epoch_boundary*` consumes `EnactmentEffects` (live `committee_changes` + `committee_threshold`) and applies them via `governance::apply_committee_enactment`; `fingerprint::write_gov_action` interprets structured `GovAction::UpdateCommittee` into wire-faithful 5-field Conway encoding. |
| **MUST NOT** | (1) Perform I/O. (2) Use `HashMap`, floats, or any other BLUE-forbidden pattern (`ci_check_forbidden_patterns.sh`). (3) Hash anything other than wire bytes (`ci_check_hash_uses_wire_bytes.sh`). (4) Construct `PreservedCbor` directly outside named decoders (`ci_check_ingress_chokepoints.sh`). (5) Depend on `ade_runtime` (`ci_check_dependency_boundary.sh`). (6) Use signing primitives or `#[cfg(feature)]` gates. (7) Omit `// Core Contract:` banner. (8) **B1 `consensus_view` (DC-CONSENSUS-02):** must not rederive a stake snapshot; `BTreeMap` only. (9) **B1 `block_validity` (DC-VAL-02/03):** must not produce `Valid` while skipping the header or body authority. (10) **B1 (DC-VAL-05):** invalid path returns unchanged input states plus structured error; no partial mutation. (11) **B1/B2 (DC-VAL-06):** every crypto-input/field-size check rejects on wrong size/shape; `if X.len() == K { check } else { skip }` is forbidden. (12) **B2 `tx_validity` (DC-TXV-01):** must be pure function of `(LedgerState, tx_cbor)`. (13) **B2 (DC-TXV-02):** never `Valid` while skipping phase-1 or (when Plutus present) phase-2. (14) **B2 (DC-TXV-04):** `Valid` yields applied `LedgerState'`; `Invalid` returns unchanged state + structured reason. (15) **B2 (DC-TXV-05):** `required_signers` is CLOSED, era-versioned. (16) **B2 `witness` (DC-VAL-06 / CN-LEDGER-09):** witness coverage is fail-closed. (17) **B2 `mempool::admit` (DC-MEM-01):** admit iff `tx_validity` is `Valid` — NO FALSE ACCEPT. (18) **B2 `mempool::policy` (GREEN, DC-MEM-02):** must not call `tx_validity`, touch accumulating state, or change any admit verdict. (19) **B3 `cert_classify` (DC-TXV-06):** must not guess a state-dependent deposit/refund; closed total over `ConwayCert`; unresolvable refund/deposit returns `UnsupportedStateDependentDepositAccounting`; removed tag → `CertDisposition::NotValidInConway`, never an accept. (20) **B3 `pparams` / `state` (DC-TXV-07):** every deposit/refund amount comes from `ProtocolParameters` + `LedgerState.conway_deposit_params` via `conway_deposit_view()`; fail-fast `MissingConwayDepositParams` (`ci_check_deposit_param_authority.sh`). (21) **B3 `conway::check_conway_coin_conservation` (T-CONSERV-01 / CN-LEDGER-07):** must not accept a cert/withdrawal-bearing tx without the full value check — B2 early-out removed; full equation runs for every Conway tx with §9.1 reject precedence. (22) **B3 `fingerprint` (DC-LEDGER-01):** Conway deposit-param fold under `CONWAY_DEPOSIT_PARAMS_TAG` byte-identical when `conway_deposit_params == None`. (23) **B4 `delegation::apply_conway_cert` (DC-LEDGER-08):** must not reduce `ConwayCert` to 7-variant Shelley `Certificate`, flatten to neutral, drop owner payloads, or swallow apply errors; governance-affecting certs owner-tagged out of B4 mutation scope; removed tags 5/6 → `EraInvalidCertificate`. (24) **B4 `rules::accumulate_tx_certs` / `process_block_certificates` (DC-LEDGER-08):** must not swallow decode/apply errors; Conway bytes dispatch to Conway decoder, never Shelley 6-variant. (25) **B5 `gov_cert::apply_conway_gov_cert` (DC-LEDGER-09):** must not mutate B4-owned `CertState`, must not double-apply delegation/pool half of composite certs (10/12/13), must not drop a governance payload, must not leave non-exhaustive `ConwayCert` match (no `_ =>` — `ci_check_gov_cert_accumulation_closed.sh`), must not default DRep expiry — `MissingDRepActivityParam` env fail-fast, `DRepActivityOverflow` for `checked_add` overflow. (26) **B5 `rules::accumulate_tx_certs` / `process_block_certificates` (DC-LEDGER-09):** must not observe-and-drop governance effects; gov half applied via `apply_conway_gov_cert`; `gov_state` carried through `apply_block` (never nulled); gov-apply error halts block transition. (27) **OQ5+COMMITTEE+DREP `state::ConwayGovState` + `fingerprint::write_stake_credential` (DC-LEDGER-10):** gov maps (`vote_delegations`, `committee_hot_keys`, `drep_expiry`, `committee`) key on discriminated `StakeCredential`, never tag-erased `Hash28`; `GovActionState.{committee_votes, drep_votes}` carry discriminated `StakeCredential` (`spo_votes` stays `Hash28` by design); fingerprint serializes discriminant before hash; committee + DRep ratification resolve by full-credential equality / exact variant (no `.hash()` cross-resolve, no key/script OR-fallback); `cred.hash()` permitted ONLY as read-only boundary adapter against the `Hash28`-keyed stake-distribution snapshot. (28) **ENACTMENT-COMMITTEE-FIDELITY `EnactmentEffects.committee_changes` (DC-LEDGER-10):** must carry discriminated `StakeCredential`, no reversion to `Hash28`. (29) **ENACTMENT-COMMITTEE-WRITEBACK `governance::{enact_proposals, apply_committee_enactment}` + `rules` apply site (DC-EPOCH-01 / DC-LEDGER-10):** must not observe-and-drop a ratified committee change — `enact_proposals` populates `committee_changes` + `committee_threshold`; `apply_committee_enactment` applies dissolve → changes → threshold; `rules.rs` epoch-boundary calls it (a `committee: gov.committee.clone()` ignoring effects is forbidden); members removed/added stay discriminated `StakeCredential`; `fingerprint::write_gov_action`'s `UpdateCommittee` emits wire-faithful 5-field Conway shape; malformed payload rejects deterministically at decode. |
| **Inbound deps** | `ade_testkit`. |
| **Outbound deps** | `ade_types`, `ade_crypto`, `ade_codec`, `ade_plutus`, `ade_core` (B1), `minicbor`, `num-bigint`, `num-integer`, `num-traits`. Dev-dep: `ade_testkit`. **No new crate edge in B3/B4/B5/OQ5/COMMITTEE/DREP/ENACTMENT-COMMITTEE-FIDELITY/WRITEBACK or in the post-3180e27 drift.** |
| **Entry points** | `ade_ledger::rules::apply_block` (8 import sites), `apply_block_classified`, `apply_block_with_accounting`, `apply_block_with_verdicts`, `apply_conway_tx_to_utxo` (B2), `apply_epoch_boundary_full`, `ade_ledger::state::{LedgerState, EpochState}` (B3: `conway_deposit_view`; B5: `gov_cert_env`), `ade_ledger::pparams::{ProtocolParameters, ConwayOnlyDepositParams, ConwayDepositParams}`, `ade_ledger::utxo::UTxOState`, `ade_ledger::hfc::translate_era`, `ade_ledger::fingerprint::fingerprint`, `ade_ledger::governance::*`, `ade_ledger::delegation::*`, `ade_ledger::epoch::*`, `ade_ledger::witness::*`. **B1:** `ade_ledger::block_validity::*`, `ade_ledger::consensus_view::{PoolDistrView, PoolEntry}`. **B2:** `ade_ledger::tx_validity::{tx_validity, TxValidityOutcome, TxValidityVerdict, TxRejectClass, TxValidityError, encode_tx_verdict_surface, decode_tx_verdict_surface, decode_tx, tx_phase_one, required_signers, tx_derived_required_signers, RequiredSigners, SignerSource, verify_required_witnesses, VKeyWitnessRef}`, `ade_ledger::mempool::{admit, MempoolState, AdmitOutcome, order, OrderPolicy}`. **B3:** `ade_ledger::cert_classify::classify`, `ade_ledger::conway::check_conway_coin_conservation`. **B4:** `ade_ledger::delegation::{apply_conway_cert, conway_cert_action, ConwayCertEnv, ConwayCertOutcome, ConwayCertAction, GovernanceOwner, GovernanceCertEffect, OwnerTaggedEffect}`; `ade_ledger::rules::accumulate_tx_certs`. **B5:** `ade_ledger::gov_cert::apply_conway_gov_cert`; `ade_ledger::state::{GovCertEnv, LedgerState::gov_cert_env}`. **ENACTMENT-COMMITTEE-WRITEBACK:** `ade_ledger::governance::apply_committee_enactment` (called from `rules::apply_epoch_boundary*`); `enact_proposals` (live `committee_changes` + `committee_threshold`); `fingerprint::write_gov_action` (5-field structured `UpdateCommittee`). |
| **Key modules** | `byron.rs`, `shelley.rs`, `mary.rs`, `alonzo.rs`, `babbage.rs`, `conway.rs`, `rules.rs`, `state.rs`, `utxo.rs`, `delegation.rs`, `epoch.rs`, `governance.rs` (post-WRITEBACK: live `committee_changes` + `apply_committee_enactment`), `hfc.rs`, `late_era_validation.rs`, `phase.rs`, `plutus_eval.rs`, `pparams.rs`, `rational.rs`, `scripts.rs`, `value.rs`, `witness.rs`, `fingerprint.rs` (post-WRITEBACK: structured 5-field `UpdateCommittee`), `error.rs`, `gov_cert.rs` (B5), `consensus_view.rs` (B1, BLUE), `block_validity/{mod.rs, verdict.rs, encoding.rs, transition.rs, header_input.rs}` (B1, BLUE), `consensus_input_extract.rs` (B1, **RED**), `tx_validity/{mod.rs, required_signers.rs, witness.rs, verdict.rs, encoding.rs, phase1.rs, transition.rs}` (B2, BLUE), `mempool/{mod.rs, admit.rs (BLUE), policy.rs (GREEN)}`, `cert_classify.rs` (B3, BLUE). |

> **GREEN sub-classification (`ade_ledger::mempool::policy`).** Physically inside the BLUE `ade_ledger` crate but **GREEN (Tier-5)** per its own `// Core Contract:` header. Pure projection over already-admitted tx ids; never calls `tx_validity`, never touches accumulating state, cannot change an admit verdict (DC-MEM-02). Tier-5-below-Tier-1 asserted by `policy_does_not_change_validity`. **Enforcement note:** the BLUE/GREEN partition inside `ade_ledger::mempool` is *by module*, not mechanical — no CI script forbids `policy.rs` from importing `tx_validity`. Surfaced as a gap.

> **RED sub-classification (`ade_ledger::consensus_input_extract`).** Physically inside the BLUE `ade_ledger` crate but **RED** per its own module doc-comment and the PHASE4-B1 TCB Color Map: tail-scans an external UTxO-HD dump format for the five `PraosState` nonces (`Nonce`, `PraosNonces`, `NonceScanError`). Reached only by RED snapshot loader and GREEN corpus harness, never by `block_validity` or `tx_validity` — review-and-doc invariant, not mechanical. Surfaced as a gap.

> **All prior closure narratives unchanged at HEAD.** B3 closed-deposit-classification + deposit-param authority (DC-TXV-06 grep-gated by `ci_check_conway_cert_classification_closed.sh`; DC-TXV-07 by `ci_check_deposit_param_authority.sh`); B2 closed-enum properties grep-gated by `ci_check_consensus_closed_enums.sh` (the `if len == K` fail-open grep gate still owed); Conway value-conservation follow-up CLOSED in B3; Conway cert-state accumulation CLOSED in B4 (DC-LEDGER-08); Conway governance-cert accumulation CLOSED in B5 (DC-LEDGER-09); credential discriminant fidelity CLOSED in OQ5/COMMITTEE/DREP/ENACTMENT-COMMITTEE-FIDELITY/WRITEBACK (DC-LEDGER-10, `status=enforced`; one extended grep gate `ci_check_credential_discriminant_closed.sh`). The Conway block-body vkey-witness closure is still not wired into the `rules.rs` block-body loop (carried adjacent gap).

---

### `ade_network` *(BLUE submodules)*

> **Status carried from prior CODEMAP — unchanged at HEAD `168ac02`.** Per-submodule BLUE scoping per `.idd-config.json` `_core_paths_doc`. The crate is split into its BLUE half (this entry) and its RED half (further down). Two submodules — `lib.rs` (the barrel) and `mux/mod.rs` (a re-export shim) — are GREEN and carry no authority. Slice provenance: cluster PHASE4-N-A, slices S-A1 through S-A10.

| Attribute | Value |
|-----------|-------|
| **Purpose** | Owns the Cardano Ouroboros mini-protocol authority — the closed wire grammar (CBOR codecs) and pure state machines for all 11 N2N + N2C mini-protocols, plus the BLUE Ouroboros mux frame primitive. Encoders/decoders and transitions only; bytes-on-the-wire and effect emission belong to the RED half of the crate. |
| **Creates** | **Wire grammar (`codec/`):** `HandshakeMessage`, `N2cHandshakeMessage`, `ChainSyncMessage`, `BlockFetchMessage`, `TxSubmission2Message`, `KeepAliveMessage`, `PeerSharingMessage`, `LocalChainSyncMessage`, `LocalTxSubmissionMessage`, `LocalStateQueryMessage`, `LocalTxMonitorMessage`; closed payload types `VersionTable`/`VersionParams`/`N2cVersionTable`/`N2cVersionParams`, `RefuseReason`/`N2cRefuseReason`, `Point`, `Tip`, `Range`, `TxIdAndSize`, `TxAcceptance`, `TxRejection`, `KeepAliveCookie`, `PeerAddress`, `AcquireFailure`, `QueryPayload`, `ResultPayload`, `MempoolMeasures`/`MempoolSizeAndCapacity`/`MeasureSizeAndCapacity`/`MeasureName`; structured error taxonomy `CodecError` + `ProtocolKind`; typed per-protocol version newtypes. **State machines:** one closed `*State` / `*Agency` / `*Output` / `*Error` quad per protocol plus event/value carriers. **Mux primitives (`mux/frame.rs`):** `MuxFrame`, `MuxHeader`, `MuxMode`, `MiniProtocolId`, `MuxError`. |
| **Interprets** | The on-wire CBOR of every Ouroboros mini-protocol message (closed grammar) and the 8-byte Ouroboros mux frame header. Block, header, and tx-body bytes inside RollForward / Block / Tx are *not* interpreted at this layer (opaque pass-through per DC-PROTO-06); LSQ Query/Result payloads also opaque. |
| **MUST NOT** | (1) `async fn`, `.await`, `tokio`, `async_std`, `futures::`, async-runtime spawn, or async timers in any file under the 9 BLUE paths (`ci_check_no_async_in_blue.sh`; DC-CORE-01). (2) Decode block / header / tx-body bytes — opaque `Vec<u8>` (DC-PROTO-06). (3) Construct a `PreservedCbor` outside `ade_codec`'s named chokepoints. (4) Add `#[non_exhaustive]` to any wire-message enum, introduce a generic `Codec<P>` trait, or use `dyn MessageHandler` (DC-PROTO-03/04). (5) Define a generic `Agency<P>` wrapper. (6) Read a selected version from ambient session state — explicit input to every transition (DC-PROTO-06). (7) Redefine `TxId` / `SlotNo` / `Hash32` / `CardanoEra` — use `ade_types`. (8) Depend on `pallas-network`. (9) Hold `String` in any error variant. (10) Use any other BLUE forbidden pattern. |
| **Inbound deps** | `ade_core_interop` (live consensus session binary, RED → RED). At the library level no BLUE crate imports `ade_network`. The 7 capture binaries import the BLUE codecs internally; the 19 integration tests exercise the BLUE surface end-to-end. |
| **Outbound deps** | `ade_types`, `ade_codec` (for `CodecError::MalformedCbor`). No external deps in the BLUE submodules; the `tokio` line in `Cargo.toml` is confined to `mux/transport.rs` and the capture binaries (RED). |
| **Entry points** | **Codec** (11 protocols, each `encode_<protocol>_message` / `decode_<protocol>_message`); `ade_network::codec::primitives::*` (8 import sites); `ade_network::codec::version::*`; `ade_network::codec::{CodecError, ProtocolKind}`. **State machines:** `ade_network::handshake::{n2n_transition, n2c_transition, select_n2n_version, select_n2c_version, N2N_SUPPORTED, N2C_SUPPORTED}`; per-protocol `*_transition` functions. **Mux:** `ade_network::mux::frame::{encode_frame, decode_frame, MuxFrame, MuxHeader, MuxMode, MiniProtocolId, MuxError, HEADER_LEN, MAX_PAYLOAD}` (8 import sites). |
| **Key modules** | `codec/` (11 message-codec modules + `primitives.rs` + `version.rs` + `error.rs`); `handshake/`, `chain_sync/`, `block_fetch/`, `tx_submission/`, `keep_alive/`, `peer_sharing/`; `n2c/` (4 local-* submodules); `mux/frame.rs`. |

---

### `ade_plutus`

> **Status carried from prior CODEMAP — unchanged at HEAD `168ac02`.** The post-3180e27 commit `396664a` adjusted `ade_plutus` test compile against the regenerated corpus (no source-shape change in this crate's library surface).

| Attribute | Value |
|-----------|-------|
| **Purpose** | Quarantine boundary between the Ade-canonical ledger and the ported UPLC evaluator from `aiken-lang/aiken` (pinned to tag `v1.1.21`, commit `42babe5d`). Exposes Ade-canonical types only; aiken- and pallas-originated types are strictly internal. |
| **Creates** | `PlutusScript`, `PlutusLanguage`, `EvalOutput`, `PlutusError`, `CostModels`, `DecoderMode`, `PerScriptResult`, `TxEvalResult`. |
| **Interprets** | UPLC scripts (Plutus V1/V2/V3) and `CostModels` CBOR. Phase-two transaction evaluation. `PlutusScript::from_cbor` is a named ingress chokepoint, allowlisted in `ci_check_ingress_chokepoints.sh` Check 3. |
| **MUST NOT** | (1) Re-export any `pallas_*` or `aiken_uplc::` type (`ci_check_pallas_quarantine.sh`). (2) Allow another BLUE crate to bypass the canonical entry. (3) Activate PV11 builtins. (4) Use any BLUE-forbidden pattern. (5) Construct `PreservedCbor` outside `ade_codec`. |
| **Inbound deps** | `ade_ledger`, `ade_testkit`. |
| **Outbound deps** | `ade_types`, `ade_crypto`, `ade_codec`, `aiken_uplc` (git, tag `v1.1.21`), `pallas-primitives` (internal-only). |
| **Entry points** | `ade_plutus::eval_tx_phase_two`, `ade_plutus::tx_eval::{SlotConfig, MAINNET_SLOT_CONFIG}`, `ade_plutus::evaluator::{programs_alpha_equivalent, EvalOutput, PlutusLanguage, PlutusScript}`, `ade_plutus::cost_model::{CostModels, DecoderMode, decode_cost_models}`. B2: `tx_validity` phase-2 dispatch reaches `ade_plutus` through `ade_ledger::plutus_eval::try_evaluate_tx`, not directly. |
| **Key modules** | `evaluator.rs`, `cost_model.rs`, `script_context.rs`, `script_verdict.rs`, `tx_eval.rs`. |

---

### `ade_types`

> **Status carried from prior CODEMAP — unchanged at HEAD `168ac02`.** All prior cluster status notes apply:
>
> PHASE4-B3 added closed `ConwayCert` (in `conway/cert.rs`), `CertDisposition`, `DepositEffect`, `CoinSource`, and `RewardAccount([u8; 29])` in `tx.rs`. PHASE4-B4 enriched `ConwayCert` in place to be owner-complete and added `owners: Vec<Hash28>` to `PoolRegistrationCert` (no new type). OQ5-CREDENTIAL-FIDELITY re-shaped `ade_types::shelley::cert::StakeCredential` from the tuple struct `StakeCredential(pub Hash28)` to the closed 2-variant `enum { KeyHash(Hash28), ScriptHash(Hash28) }` with `hash(&self) -> &Hash28` accessor (same already-counted type, new discriminated shape; `Ord` derives lexicographically over `(variant, hash)`). COMMITTEE-CRED-FIDELITY re-typed `GovActionState.committee_votes` `Vec<(Hash28, Vote)>` → `Vec<(StakeCredential, Vote)>`. DREP-VOTE-FIDELITY re-typed `GovActionState.drep_votes` `Vec<(Hash28, Vote)>` → `Vec<(StakeCredential, Vote)>` (the `spo_votes` list stays `Hash28`-keyed by design). ENACTMENT-COMMITTEE-WRITEBACK re-shaped `GovAction::UpdateCommittee` from opaque `{ prev_action, raw: Vec<u8> }` to structured `{ prev_action, removed: BTreeSet<StakeCredential>, added: BTreeMap<StakeCredential,u64>, threshold:(u64,u64) }` (re-shaping of one variant of the existing closed `GovAction`, not a new type). Crate stays at 80 canonical types; remains produce-only and closed.

| Attribute | Value |
|-----------|-------|
| **Purpose** | Owns the canonical Cardano domain vocabulary — primitives, era enum, address forms, era-tagged transaction bodies / outputs / certificates, governance types — used by every other workspace crate as the lingua franca. The schema, separated from the codec. |
| **Creates** | `CardanoEra`, `SlotNo`, `BlockNo`, `EpochNo`, `Hash28`, `Hash32`, `Coin`, `Lovelace`, `NetworkId`, `Nonce`, `TxIn`, `RewardAccount` (B3), `PoolId`, `Address`, `ByronAddress`, `Credential`, `StakeCredential` (OQ5: closed `enum { KeyHash, ScriptHash }` + `hash()` accessor), `Certificate`, `PoolRegistrationCert` (B4: `owners: Vec<Hash28>`), `ConwayCert` (B3 closed + B4 owner-complete), `CertDisposition` / `DepositEffect` / `CoinSource` (B3), `MIRCert`, `MIRPot`, `DRep`, `GovAction` (ENACTMENT-COMMITTEE-WRITEBACK: `UpdateCommittee` structured over discriminated `StakeCredential`), `GovActionState` (COMMITTEE: `committee_votes` discriminated; DREP: `drep_votes` discriminated), `GovActionId`, `Anchor`, `OperationalCert`, `NativeScript`, `PlutusV1Script`, `Datum`, `DatumOption`, `MultiAsset`, `AssetName`, `CostModel`, `ExUnits`, plus per-era tx-body / tx-out / witness wrappers (incl. `conway::tx::ConwayTxBody`) and Byron era block headers. |
| **Interprets** | None — produce-only. Domain types are constructed by `ade_codec` decoders or `ade_ledger` rules. |
| **MUST NOT** | (1) Construct or decode `PreservedCbor`. (2) Use any BLUE-forbidden pattern. (3) Depend on any workspace crate — root of the dependency DAG, no `[dependencies]` block. (4) Add open/extensible variants to closed era / certificate / governance enums without a versioned gate; B3 extends this to closed `ConwayCert` / `CertDisposition` / `DepositEffect` / `CoinSource`: no `#[non_exhaustive]`, no open-tail `Other`/`Unknown`; `ConwayCert::RemovedInConway { tag }` is an explicit closed marker, not an open tail. **B4 (DC-LEDGER-08):** owner-completion enrichment of `ConwayCert` and the `owners` field on `PoolRegistrationCert` must stay additive within the closed surface — no open tail, `#[non_exhaustive]`, or catch-all (closed `ConwayCert` shape grep-gated by `ci_check_conway_cert_classification_closed.sh`). **OQ5 (DC-LEDGER-10):** must not revert to tag-erasing tuple struct `StakeCredential(Hash28)` — credential identity is closed sum over `{KeyHash, ScriptHash}`, never bare `Hash28`; `hash()` accessor is read-only boundary projection (enforced by `ci_check_credential_discriminant_closed.sh`). |
| **Inbound deps** | `ade_codec`, `ade_crypto`, `ade_ledger`, `ade_plutus`, `ade_runtime`, `ade_testkit`, `ade_network`, `ade_core`, `ade_core_interop`. |
| **Outbound deps** | None. |
| **Entry points** | `ade_types::CardanoEra`, `ade_types::tx::{Coin, TxIn, RewardAccount}`, `ade_types::{Hash32, SlotNo}`, `ade_types::Hash28`, `ade_types::conway::tx::ConwayTxBody`, `ade_types::conway::cert::{ConwayCert, CertDisposition, DepositEffect, CoinSource}`. |
| **Key modules** | `primitives.rs`, `era.rs`, `tx.rs` (B3: `RewardAccount`), `address/`, `byron/`, `shelley/` (B4: `cert.rs` — `PoolRegistrationCert.owners`; OQ5: `cert.rs` — `StakeCredential` closed enum + `hash()`), `allegra/`, `mary/`, `alonzo/`, `babbage/`, `conway/` (B3: `cert.rs` — closed `ConwayCert` + deposit-effect sums; B4: owner-complete; ENACTMENT-COMMITTEE-WRITEBACK: `governance.rs` — `GovAction::UpdateCommittee` structured discriminated fields). |

---

## GREEN Modules — Deterministic Glue

> Deterministic, non-authoritative. May depend on BLUE; must not affect authoritative outputs.

### `ade_testkit`

> **Status change in 168ac02 (snapshot-loader follow-ups).** `harness/snapshot_loader.rs` (GREEN) received three orthogonal hardenings, all fixture-decode helpers — still GREEN, still a materialization helper, not an authoritative path. (1) The `LoadedSnapshot` now carries the real tip slot from the `HeaderState`'s `WithOrigin (AnnTip ...)` prefix instead of hard-coding `slot=0`: new helper `parse_with_origin_slot` reads `array(1) [array(N≥3) [slot, blockNo, hash, …]]` and returns `(offset_past_with_origin, tip_slot)`, falling back to `0` for the `Origin` case (no block yet applied). `SnapshotHeader` gained a `slot: u64` field in place (a doc-commented field on the existing GREEN struct — not a new public type, and not in the BLUE canonical-type count). (2) `parse_registered_credentials` now decodes both UMElem layouts — pre-Conway `array(4) [StrictMaybe RDPair, Set Ptr, StrictMaybe Pool, StrictMaybe DRep]` (registered iff RDPair is SJust) and the Conway compact `array(4) [uint reward, uint deposit, nullable Pool, nullable DRep]` (registered iff deposit > 0). Discrimination is by major type of UMElem[0]: array(major=4) = pre-Conway, uint(major=0) = Conway. (3) `parse_cold_credential_set` and `parse_cold_credential_epoch_map` are now **fail-closed on truncation**: each tracks a `terminated` flag and rejects with a `HarnessError::ParseError` if the declared-but-under-length set/map exits the loop without consuming all declared members — replacing the prior silent advance that depended on a downstream field to hit EOF. A new regression test (`under-length set must reject` in the existing `parse_gov_action` test) covers the structured reject. **No new canonical type, no new BLUE/RED module, no new crate edge, no new CI script.** The hardenings preserve fixture-decode determinism and align the GREEN snapshot loader's structured-decode discipline with the BLUE enactment surface introduced in ENACTMENT-COMMITTEE-WRITEBACK.
>
> **Status change in c78ec76 (corpus generator).** `crates/ade_testkit/tests/emit_reward_provenance.rs` added a new GREEN, `#[ignore]`-gated re-runnable corpus generator — two tests (`emit_alonzo310_registered_creds`, `emit_conway576_registered_creds`) that materialize the reward-provenance corpus from snapshot bytes. Only runs under `cargo test -- --ignored`. No source-shape change anywhere in BLUE.
>
> **Status change in b9cfaf9 (committee oracle).** `crates/ade_testkit/tests/epoch_oracle_comparison.rs` added one new GREEN test `committee_oracle_mainnet_575_576_noop_agreement` that replays the mainnet 575→576 epoch boundary against the real cardano-node snapshot and asserts a noop agreement on the committee surface. Strengthens DC-EPOCH-01 + DC-LEDGER-10 by extending real-chain oracle coverage to the live committee write-back path.
>
> **Status change in 396664a (corpus alignment).** Six `ade_testkit` integration tests (`boundary_fingerprint_agreement.rs`, `boundary_stateful_replay.rs`, `contiguous_plutus_verdict_harness.rs`, `emit_divergent_fixture.rs`, `late_era_composer_integration.rs`, `transition_proof_surface.rs`) and the `ade_plutus` test compile were realigned against the regenerated corpus. No new tests, no source-shape change in the library surfaces.
>
> **Status change in PHASE4-B2.** Added GREEN harness sub-tree `tx_validity/` (corpus tx extractor, deterministic adversarial mutator set, synthetic valid-case / dependent-pair builder, GREEN driver). **PHASE4-B3 did not add a harness sub-tree;** `ade_testkit` remains the legitimate (RED-snapshot-loader) write path that populates `LedgerState.conway_deposit_params` — `ci_check_deposit_param_authority.sh` explicitly allowlists the testkit snapshot-loader as the only place that may construct `ConwayOnlyDepositParams` from parsed input rather than canonical state. **OQ5/COMMITTEE/DREP/ENACTMENT-COMMITTEE-FIDELITY/WRITEBACK:** the snapshot loader's gov-map, DRep-registration, committee-state, committee-vote, DRep-vote, and `UpdateCommittee` payload parses build the discriminated `StakeCredential` (preserving the key/script tag) and decode the structured Conway `update_committee` shape via fail-closed helpers; the `parse_committee_vote_map` was renamed `parse_credential_vote_map` (committee + DRep tag-preserving). **Still GREEN.**
>
> **Prior status (PHASE4-N-B / PHASE4-B1).** Depends on `ade_runtime` (used by `consensus::stream_replay`); B1 added `validity/` block-validity harness.

| Attribute | Value |
|-----------|-------|
| **Purpose** | Test infrastructure: differential harness, oracle snapshot loader, regression corpus, genesis loader, era mapping, transcript replay, diff reporting; N-B consensus harness; B1 block-validity harness (`validity/`); B2 transaction-validity harness (`tx_validity/`). Not authoritative — produces comparisons of authoritative outputs against oracle/corpus data and serves the `replay_cmd` test suites. |
| **Creates** | **Harness (pre-N-B, selected):** `Era`, `OracleManifest`, `LoadedSnapshot`, `RegressionCorpus`, `ComparisonSurface`, `DifferentialReport`, `DiffReport`, `Divergence`, `LedgerDiffReport`, `ProtocolDiffReport`, `BlockFields`, `ExpectedVerdict`, `HarnessError`, `Transcript`, `StateHash`, stub applicators, violation reports. `SnapshotHeader` (post-168ac02: + `slot: u64` field). **Consensus (N-B):** `LedgerViewStub`, `EpochStakeFixture`, `PoolFixture`, `ReplayStep`, `ReplayResult`. **Validity (B1, `validity/`):** `ConwayValidityCorpus` + `CorpusLoadError`, production-`PoolDistrView` builder, GREEN replay harness, M1–M6 mutators. **B2 (`tx_validity/`):** `ExtractedTx` + `ExtractError`, `TxReplay`, `ValidCase` + `DependentPair`, `WitnessMutation` + `WitnessMutationOutcome` + `SyntheticMutation` + `SyntheticCase`. Functions (no new types) added post-3180e27: `parse_with_origin_slot` (168ac02). |
| **Interprets** | Oracle reference snapshots, the regression-corpus manifest, the N-B consensus corpus, the B1 validity corpus, on-wire Conway transactions inside committed Conway-576 corpus blocks (B2). **B3:** the snapshot loader parses Conway deposit parameters (`drep_deposit`, `gov_action_deposit`) from snapshot bytes — the one allowlisted non-canonical-state deposit-param source under DC-TXV-07. **168ac02:** the snapshot loader extracts the tip slot from the HeaderState's `WithOrigin (AnnTip …)` prefix and decodes both pre-Conway and Conway-compact UMElem layouts in `parse_registered_credentials`. **ENACTMENT-COMMITTEE-WRITEBACK (carried):** `parse_gov_action` tag-4 (`UpdateCommittee`) decodes the structured Conway shape (removed set + added-with-expiry map + threshold) via fail-closed `parse_cold_credential` / `parse_cold_credential_set` / `parse_cold_credential_epoch_map` / `parse_unit_interval`. |
| **MUST NOT** | (1) Affect authoritative outputs — testkit code is excluded from BLUE forbidden-pattern scans and may use `HashMap`/`serde_json`/`flate2`/`tar`, but its results must never feed back into BLUE/RED authoritative state. (2) Be linked from any BLUE crate. (3) Introduce nondeterminism that leaks into stored fixtures. (4) `consensus::ledger_view_stub` must use `BTreeMap` only. (5) Stream-replay driver must report identical event sequences across runs. (6) **B1 `validity::replay`** must report identical verdict streams across runs and must not alter the BLUE verdict. (7) **B2 `tx_validity`** must not alter the BLUE `tx_validity` verdict — adversarial verdict-surface stream replays byte-identically; `no_mutation_is_ever_valid` is the CE-B2-4 false-accept guard. (8) **B3** the snapshot-loader deposit-param construction must never be reached by the BLUE deposit-conservation path at runtime; fixture-materialization helper only. (9) **168ac02** `parse_cold_credential_set` / `parse_cold_credential_epoch_map` must reject a truncated declared-length set/map deterministically (the new `terminated` guards); silent under-length advance is forbidden. (10) **168ac02** `parse_registered_credentials` must reject an unknown UMElem layout — only `array(4) [array(…), …]` (pre-Conway) and `array(4) [uint, uint, …, …]` (Conway compact) are accepted; anything else flips `is_registered = false` without claiming the credential. |
| **Inbound deps** | None at compile time. All consumption is through its own integration tests and dev-dep links from `ade_core`, `ade_runtime`, `ade_ledger`, `ade_core_interop`. |
| **Outbound deps** | `ade_types`, `ade_codec`, `ade_crypto`, `ade_core`, `ade_ledger`, `ade_plutus`, `ade_runtime`, `blake2`, `flate2`, `tar`, `serde`, `serde_json`, `toml`. |
| **Entry points** | `ade_testkit::harness::snapshot_loader::LoadedSnapshot` (post-168ac02: tip slot live), `extract_state_from_tarball` / `parse_*` family (post-168ac02: `parse_with_origin_slot`, dual-layout `parse_registered_credentials`, fail-closed `parse_cold_credential_set` / `parse_cold_credential_epoch_map`), `ade_testkit::harness::genesis_loader::load_genesis_utxo`; N-B: `ade_testkit::consensus::{ledger_view_stub::*, stream_replay::*, corpus::*}`; B1: `ade_testkit::validity::{corpus::ConwayValidityCorpus, ledger_view::*, replay::*, adversarial::*}`; B2: `ade_testkit::tx_validity::{extract_corpus_txs, extract_block_txs, ExtractedTx, replay_tx_validity, validate_tx_against_corpus, TxReplay, build_valid, build_dependent_pair, mutate_witness, build_synthetic, judge}`. |
| **Key modules** | **`harness/`** (pre-N-B; B3: snapshot loader's deposit-param parse; ENACTMENT-COMMITTEE-WRITEBACK + 168ac02: `snapshot_loader.rs` — fail-closed `parse_cold_credential` / `parse_cold_credential_set` / `parse_cold_credential_epoch_map` / `parse_unit_interval` + `parse_with_origin_slot` + dual-layout `parse_registered_credentials`). **`consensus/`** (N-B). **`validity/`** (B1). **`tx_validity/`** (B2): `mod.rs`, `extract.rs`, `adversarial.rs`, `valid_synthetic.rs`. **Integration tests of note (post-3180e27):** `tests/emit_reward_provenance.rs` (c78ec76, `#[ignore]`-gated corpus generators), `tests/epoch_oracle_comparison.rs` (b9cfaf9, new 575→576 committee noop-agreement oracle). |

> **Classification note.** `ade_testkit` reads files from disk in test helpers and drives BLUE authorities from corpus inputs. I/O exists only to materialize oracle/corpus fixtures consumed by tests and to compose deterministic GREEN harnesses for replay equivalence. By IDD doctrine that is GREEN. The crate is "demotable to RED" if/when it grows orchestration code that mutates authoritative state.

---

## RED Modules — Imperative Shell

> I/O, network, storage, clocks, retries. May depend on BLUE/GREEN. Must not modify core state directly or construct semantic types unsafely.

### `ade_core_interop` *(PHASE4-N-B, S-B10)*

> **Status carried from prior CODEMAP — unchanged at HEAD `168ac02`.**

| Attribute | Value |
|-----------|-------|
| **Purpose** | Live cardano-node interop driver for the consensus orchestrator. Builds the orchestrator at genesis (`fresh_orchestrator(k, initial_nonce)`), connects to a pinned-Docker cardano-node 10.6.2 peer over N2N via `ade_network`, feeds arriving headers and rollbacks into `ade_runtime::consensus::chain_selector::process_stream_input`, and asserts best-chain agreement for a sustained window. The release-evidence test is `#[ignore]`-gated. |
| **Creates** | One library helper: `ade_core_interop::fresh_orchestrator(k, initial_nonce) -> OrchestratorState`. No new public types. |
| **Interprets** | Live N2N byte streams from a real cardano-node 10.6.2 peer; bytes flow in via `ade_network::mux::transport::MuxTransport` into BLUE codecs / state machines / consensus transitions without re-interpretation. |
| **MUST NOT** | (1) Contain authoritative state-transition logic. (2) Be depended on by any other workspace crate. (3) Run by default in CI — the closure-gate test is `#[ignore]`-gated. (4) Bypass `ade_codec`'s `PreservedCbor` chokepoints. (5) Read versions out of ambient context (DC-PROTO-06). (6) Vendor cardano-node mainnet genesis blobs into the crate. |
| **Inbound deps** | None. |
| **Outbound deps** | `ade_core`, `ade_runtime`, `ade_network`, `ade_testkit`, `ade_types`, `tokio`. |
| **Entry points** | `ade_core_interop::fresh_orchestrator(k, initial_nonce)`, binary `live_consensus_session`. Closure-gate test (`#[ignore]`). |
| **Key modules** | `src/lib.rs`, `src/bin/live_consensus_session.rs`. |

> **Gap surfaced.** `ade_core_interop` at HEAD is a *readiness probe*; the full live tip-agreement loop is the operator's manual task. CE-N-B-6 closes via manual evidence log, not via CI.

---

### `ade_network` *(RED submodules + capture binaries)*

> **Status carried from prior CODEMAP — unchanged at HEAD `168ac02`.** The RED half of the `ade_network` crate: `mux::transport`, `session`, and the 7 `src/bin/capture_*` binaries. Provenance: cluster PHASE4-N-A.

| Attribute | Value |
|-----------|-------|
| **Purpose** | The only place inside `ade_network` where I/O happens — tokio-based TCP / Unix-socket scaffolding for the Ouroboros mux layer (`mux::transport`), the session-composition placeholder (`session`), and 7 capture binaries that perform real handshakes against pinned cardano-node 11.0.1 peers and write the recorded byte stream as canonical replay corpus. |
| **Creates** | `MuxTransport` (tokio TcpStream wrapper). `session/mod.rs` is an empty placeholder. The capture binaries produce no public Rust types. |
| **Interprets** | Live TCP / Unix-socket byte streams from cardano-node 11.0.1 peers; bytes flow in via `MuxTransport::read_raw` into the BLUE mux frame decoder and protocol codecs without re-interpretation. |
| **MUST NOT** | (1) Contain protocol logic. (2) Define or modify any closed wire-message enum, agency type, or state. (3) Read a selected protocol version from ambient state and pass it into a BLUE transition implicitly (DC-PROTO-06). (4) Replace the BLUE codec / state machine with a "fast path" (CN-WIRE-07). (5) Bypass `ade_codec`'s `PreservedCbor` chokepoints. (6) Be depended on by any BLUE submodule of `ade_network`. |
| **Inbound deps** | `ade_core_interop`. At the library level, no BLUE crate imports the RED submodules. |
| **Outbound deps** | `tokio`, `ade_types`, `ade_codec`, plus the BLUE `ade_network::codec::*` and `ade_network::mux::frame`. |
| **Entry points** | `ade_network::mux::transport::{MuxTransport, open_tcp}`. The capture binaries. `session::*` exposes no entry points at HEAD. |
| **Key modules** | `mux/transport.rs`, `session/mod.rs` (empty placeholder), `bin/capture_*`. |

> **Gap surfaced.** `ade_network::session::mod.rs` is still a placeholder. The session driver remains owed by a future orchestration cluster.

---

### `ade_node`

> **Status carried from prior CODEMAP — unchanged at HEAD `168ac02`.**

| Attribute | Value |
|-----------|-------|
| **Purpose** | Binary entry point for the node process. Currently a hello-world stub. Reserved as the assembly point for runtime + ledger + chain DB + network + consensus orchestrator + mempool. |
| **Creates** | None. |
| **Interprets** | None yet. |
| **MUST NOT** | (1) Bypass `ade_codec` to construct semantic types from bytes. (2) Modify `ade_ledger` or `ade_core::consensus` state in place — transitions go through `ade_ledger::rules::*`, `ade_ledger::block_validity::*`, `ade_ledger::tx_validity::*`, `ade_ledger::mempool::admit`, or `ade_core::consensus::*` BLUE transitions. (3) Take an inbound dep from any other crate. (4) Read versions out of ambient context and pass them into `ade_network` BLUE transitions implicitly (DC-PROTO-06). (5) Construct an `OrchestratorState` outside `ade_runtime::consensus::chain_selector` or `ade_core_interop::fresh_orchestrator`. |
| **Inbound deps** | None. |
| **Outbound deps** | None at present. |
| **Entry points** | `main()` in `crates/ade_node/src/main.rs`. |

> **Gap surfaced.** `ade_node` is still a hello-world stub; its MUST NOT list is forward-looking with nothing to enforce today.

---

### `ade_runtime`

> **Status carried from prior CODEMAP — unchanged at HEAD `168ac02`.** Hosts storage + recovery (cluster N-D) and the consensus orchestrator (`consensus/`, cluster N-B). The crate remains RED; `chain_selector.rs` / `candidate_fragment.rs` are GREEN by file-level classification, `genesis_parser.rs` is RED. PHASE4-B1/B2/B3/B4/B5 and the post-3180e27 drift did not change `ade_runtime`.

| Attribute | Value |
|-----------|-------|
| **Purpose** | The imperative shell. Hosts (a) the `ChainDb` / `SnapshotStore` storage abstractions and their in-memory / redb-backed impls, the crash-safety harness, and the snapshot + forward-replay recovery primitive (N-D); (b) the GREEN chain-selector orchestrator + GREEN `CandidateFragment` materializer (N-B); (c) the RED genesis-text parser that materializes the BLUE `EraSchedule` and computes the `BootstrapAnchorHash` (N-B). |
| **Creates** | **ChainDb (`chaindb/`):** `ChainDb` trait, `SnapshotStore` trait, `KillStrategy<D>` trait, `ChainTip`, `StoredBlock`, `ChainDbError`, `InMemoryChainDb`, `PersistentChainDb`, `PersistentChainDbOptions`, `SyncCadence`, `NoKill`, `BlockIter<'a>`. **Recovery (`recovery.rs`):** `Recoverable` trait, `StartingState`, `RecoveryReport<R>`, `RecoveryError<E>`. **Genesis (`consensus/genesis_parser.rs` — RED):** `NetworkMagic`, `GenesisBlob`, `GenesisBundle<'a>`, `GenesisParseError`. **Orchestrator (`consensus/chain_selector.rs` — GREEN):** `StreamInput`, `OrchestratorError`, `OrchestratorState`, `RollbackSnapshot`, `DEFAULT_SNAPSHOT_LIMIT`. **Materializer (`consensus/candidate_fragment.rs` — GREEN):** `build_candidate_fragment`. |
| **Interprets** | Block bytes for content-addressed storage. Snapshot bytes are byte-opaque to chaindb. Genesis JSON for the four blobs → typed `EraSummary` + `system_start_unix_ms` + immutable `EraSchedule`. N-A `StreamInput` events → canonical BLUE input shape threaded through BLUE transitions. |
| **MUST NOT** | (1) Depend on `ade_ledger`. (2) Leak `redb` through the `chaindb::*` public surface. (3) Have a second public chaindb path. (4) Couple snapshots to a specific state type. (5) Apply automatic snapshot pruning. (6) Allow partial-recovery success. (7) Add async recovery surface. (8) Treat "not found" as an error path or silently retry on real errors. (9) **Consensus sub-tree:** the GREEN `chain_selector` must not affect authoritative outputs; no `HashMap`/`HashSet`, no async, no wall-clock, no global mutable state. (10) **Genesis parser:** must not be reached by BLUE; structured errors only. (11) **`candidate_fragment` materializer:** must not own state — pure constructor. |
| **Inbound deps** | `ade_testkit` (non-dev dep, used by `stream_replay`), `ade_core_interop`. The binary `chaindb_kill_target` lives at `src/bin/`. |
| **Outbound deps** | `ade_types`, `ade_core`, `ade_crypto` (`blake2b_256`), `ade_codec`, `redb`, `serde_json`. Dev-deps: `tempfile`, `ade_testkit`, `cardano-crypto` (tests only). |
| **Entry points** | **Chaindb / recovery:** `ade_runtime::chaindb::{ChainDb, SnapshotStore, PersistentChainDb, PersistentChainDbOptions, SyncCadence, InMemoryChainDb, run_contract_tests, run_snapshot_contract_tests, run_crash_safety_tests}`, `ade_runtime::recovery::{recover, Recoverable}`. **Consensus:** `ade_runtime::consensus::chain_selector::{process_stream_input, OrchestratorError, OrchestratorState, RollbackSnapshot, StreamInput, DEFAULT_SNAPSHOT_LIMIT}`, `ade_runtime::consensus::candidate_fragment::build_candidate_fragment`, `ade_runtime::consensus::genesis_parser::{compute_anchor_hash, parse_genesis, GenesisBlob, GenesisBundle, GenesisParseError, NetworkMagic}`. |
| **Key modules** | **`chaindb/`**: `mod.rs`, `contract.rs`, `snapshot_contract.rs`, `crash_safety.rs`, `in_memory.rs`, `persistent.rs`, `types.rs`, `error.rs`. **`recovery.rs`**. **`consensus/`**: `mod.rs`, `genesis_parser.rs` (RED), `candidate_fragment.rs` (GREEN), `chain_selector.rs` (GREEN). **`bin/chaindb_kill_target.rs`**. |
| **Mechanical enforcement** | Three dedicated CI scripts back the chaindb / recovery / crash-safety surface: `ci/ci_check_chaindb_contract.sh`, `ci/ci_check_recovery_contract.sh`, `ci/ci_check_chaindb_crash_safety.sh`. The consensus sub-tree has no dedicated CI script. |

> **Gap surfaced.** No dedicated CI script enforces the GREEN/RED partition *inside* `ade_runtime::consensus`. At HEAD the partition is "by file" and enforced only by review + header comments. Candidate name (forward): `ci/ci_check_consensus_runtime_partition.sh`.

---

## Cross-Module Rules (project-wide)

### Dependency direction

`ade_core_interop` → `{ade_core, ade_runtime, ade_network, ade_testkit, ade_types, tokio}` is legal (RED leaf binary).
`ade_runtime` → `{ade_core, ade_crypto, ade_codec, ade_types, redb, serde_json}` is legal (RED + GREEN glue).
`ade_testkit` → `{ade_core, ade_ledger, ade_plutus, ade_runtime, ade_crypto, ade_codec, ade_types}` is legal (GREEN).
`ade_network` (BLUE submodules) → `{ade_codec, ade_types}` is legal (BLUE among BLUEs).
`ade_network` (RED submodules + capture bins) → `{tokio, ade_codec, ade_types, ade_network::codec::*, ade_network::mux::frame}` is legal.
**`ade_ledger` → `{ade_core, ade_plutus, ade_crypto, ade_codec, ade_types, minicbor}` is legal (BLUE among BLUEs).** The `ade_ledger → ade_core` edge (PHASE4-B1) is **acyclic**. **PHASE4-B2, B3, B4, B5, OQ5/COMMITTEE/DREP/ENACTMENT-COMMITTEE-FIDELITY/WRITEBACK, and the post-3180e27 drift added no new crate edge.**
`ade_core` → `{ade_types, ade_crypto, minicbor}` is legal (BLUE among BLUEs).
`ade_plutus` → `{ade_crypto, ade_codec, ade_types}` is legal.
`ade_crypto` → `{ade_types}` is legal.
`ade_codec` → `{ade_types}` is legal.
`ade_types` → `{}`.

**Forbidden directions.** Any BLUE crate (or BLUE `ade_network` submodule) depending on `ade_runtime`, `ade_node`, `ade_core_interop`, or the RED half of `ade_network` is a CI failure (`ci_check_dependency_boundary.sh` + `ci_check_no_async_in_blue.sh`). Any non-`ade_plutus` crate referring to `pallas_*` is a CI failure (`ci_check_pallas_quarantine.sh`). Any reference to `ChainDb` / `chain_db` inside `crates/ade_core/src/consensus/` is a CI failure (`ci_check_no_chaindb_in_consensus_blue.sh`). **B1/B2 note:** the BLUE `tx_validity`/`mempool::admit` and GREEN `mempool::policy` all live under the already-BLUE `ade_ledger` crate prefix; the GREEN/RED/BLUE *sub-crate* partition inside `ade_ledger::mempool` and `ade_ledger::consensus_input_extract` is review-and-test-enforced, not mechanical (see gap notes). **B3 note:** any BLUE-crate deposit/refund literal next to a deposit field, or any BLUE read of a testkit `ConwayGovParams`, is a CI failure (`ci_check_deposit_param_authority.sh`, DC-TXV-07) — the sole allowlisted non-canonical deposit-param source is the RED snapshot loader in `ade_testkit`.

### Naming convention

All crates are prefixed `ade_`. TCB color is not encoded in the crate name. The authoritative classifier is `.idd-config.json` `core_paths` plus the cluster doc TCB Color Maps for sub-crate scopes; CI scripts hard-code their BLUE list. The seven full-BLUE-scoped scripts (`ci_check_module_headers.sh`, `ci_check_no_semantic_cfg.sh`, `ci_check_no_signing_in_blue.sh`, `ci_check_hash_uses_wire_bytes.sh`, `ci_check_ingress_chokepoints.sh`, `ci_check_dependency_boundary.sh`, `ci_check_no_async_in_blue.sh`) use the full BLUE set: the 6 BLUE crates plus the 9 BLUE `ade_network` submodule paths. The B3 `ci_check_deposit_param_authority.sh` scopes the 6 BLUE crates (`BLUE_CRATES=(ade_codec ade_types ade_crypto ade_core ade_ledger ade_plutus)`). Four narrow scripts target `crates/ade_core/src/consensus/`. **No script narrowly scopes `crates/ade_ledger/src/tx_validity/`, `crates/ade_ledger/src/mempool/`, `crates/ade_types/src/conway/`, or `crates/ade_ledger/src/delegation.rs`** — the B2/B3/B4 closed-enum / closed-grammar / owner-tagged-action properties on those paths rely on the full-BLUE scans plus the compiler-exhaustive `match` and named tests. (`ci_check_conway_cert_classification_closed.sh` does scope `ade_types::conway::cert` + `ade_codec::conway::cert` + `ade_ledger::cert_classify`; the B5 `ci_check_gov_cert_accumulation_closed.sh` does scope `crates/ade_ledger/src/{gov_cert.rs,rules.rs,error.rs,state.rs}`; the DC-LEDGER-10 `ci_check_credential_discriminant_closed.sh` scopes `ade_types::shelley::cert` + both era `ade_codec` decoders + `ade_ledger::{state, governance, fingerprint, rules}` + `ade_types::conway::governance`.)

### CI enforcement (29 scripts under `ci/`)

| Script | Enforces | Scope |
|---|---|---|
| `ci_check_cbor_round_trip.sh` | T-ENC-03, DC-CBOR-01, DC-CBOR-02 | golden corpus |
| `ci_check_ce_n_a_5_proof.sh` *(PHASE4-N-A, S-A10)* | CE-N-A-5 5-condition evidence | `ade_network` (RED + real-capture corpus) |
| `ci_check_chaindb_contract.sh` | DC-STORE-02, DC-STORE-03, CN-STORE-04, CN-STORE-05 | `ade_runtime --lib chaindb::` (RED) |
| `ci_check_chaindb_crash_safety.sh` | T-REC-01 (crash variant), DC-STORE-01, CN-STORE-03; CE-N-D-1 gate | `ade_runtime --test stress_kill_harness` (RED) |
| `ci_check_consensus_closed_enums.sh` *(PHASE4-N-B, S-B2; extended B1, B2)* | DC-CONS-04, DC-CONS-10, T-DET-01, DC-VAL-02/04/05/06, DC-TXV-01/02/04/05, DC-MEM-01/02 | `consensus/` + `block_validity/` + `tx_validity/` + `mempool/` |
| `ci_check_constitution_coverage.sh` | invariant-registry ↔ code/test coverage | repo-wide |
| `ci_check_conway_cert_classification_closed.sh` *(PHASE4-B3F)* | DC-TXV-06 — Conway cert-classification closure (no `#[non_exhaustive]`/open-tail on the cert types, no catch-all accept arm in the decoder, no `_ =>` in `classify`) | `ade_types::conway::cert` + `ade_codec::conway::cert` + `ade_ledger::cert_classify` |
| `ci_check_credential_discriminant_closed.sh` *(OQ5 + COMMITTEE + DREP + ENACTMENT-COMMITTEE-FIDELITY + WRITEBACK)* | DC-LEDGER-10 — credential discriminant fidelity (closed `StakeCredential` enum + discriminant-preserving decoders + discriminated gov-state keys + discriminated committee/DRep vote lists + discriminated enactment-effects + structured live `UpdateCommittee` + `apply_committee_enactment` present and called) | `ade_types::shelley::cert` + both era `ade_codec` decoders + `ade_ledger::{state, governance, fingerprint, rules}` + `ade_types::conway::governance` |
| `ci_check_crypto_vectors.sh` | crypto KAT regression | `ade_crypto` |
| `ci_check_dependency_boundary.sh` | T-BOUND-02 — BLUE ⇎ RED separation | full BLUE (6 crates + 9 `ade_network` paths) |
| `ci_check_deposit_param_authority.sh` *(PHASE4-B3)* | DC-TXV-07 — canonical deposit-param authority | 6 BLUE crates (deposit literals + testkit `ConwayGovParams` reads) |
| `ci_check_differential_divergence.sh` | DC-DIFF-* | replay outputs |
| `ci_check_forbidden_patterns.sh` | T-CORE-02 — no `HashMap`/floats/clocks/etc. + `unsafe` allowlist | full BLUE |
| `ci_check_gov_cert_accumulation_closed.sh` *(PHASE4-B5)* | DC-LEDGER-09 — Conway gov-cert accumulation closure | `ade_ledger::{gov_cert,rules,error,state}` |
| `ci_check_hash_uses_wire_bytes.sh` | DC-CBOR-02, T-ENC-01 | full BLUE |
| `ci_check_hfc_translation.sh` | DC-EPOCH-02 (CE-73-semantic) | `ade_ledger::hfc` |
| `ci_check_ingress_chokepoints.sh` | DC-INGRESS-01, T-INGRESS-01 | full BLUE |
| `ci_check_ledger_determinism.sh` | DC-LEDGER-01 (CE-74) | `ade_ledger` |
| `ci_check_module_headers.sh` | CE-04 contract banner | full BLUE |
| `ci_check_no_async_in_blue.sh` *(PHASE4-N-A, S-A1)* | DC-CORE-01 | full BLUE |
| `ci_check_no_chaindb_in_consensus_blue.sh` *(PHASE4-N-B, S-B1)* | DC-CORE-01 + DC-CONS-07 | `crates/ade_core/src/consensus/` |
| `ci_check_no_density_in_fork_choice.sh` *(PHASE4-N-B, S-B8)* | DC-CONS-03 | `fork_choice.rs` + `candidate.rs` |
| `ci_check_no_float_in_consensus.sh` *(PHASE4-N-B, S-B1)* | T-CORE-02 + DC-CONS-07/08/09 | `crates/ade_core/src/consensus/` |
| `ci_check_no_secrets.sh` | no credentials/IPs/keys in tree | repo-wide |
| `ci_check_no_semantic_cfg.sh` | T-BUILD-01 — no `#[cfg(feature)]` | full BLUE |
| `ci_check_no_signing_in_blue.sh` | CE-05, T-KEY-01 — signing in RED only | full BLUE |
| `ci_check_pallas_quarantine.sh` | O-29.2 — `pallas-*` confined to `ade_plutus` | non-`ade_plutus` |
| `ci_check_recovery_contract.sh` | T-REC-01, T-REC-02, DC-STORE-05 | `ade_runtime --lib recovery::` (RED) |
| `ci_check_ref_provenance.sh` | DC-REF-01 — manifest checksum integrity | reference corpus |

> **Post-3180e27 CI delta.** **None.** All six post-3180e27 commits are GREEN-scope and add no CI script. The `ci/ci_check_credential_discriminant_closed.sh` gate at HEAD reflects the full DC-LEDGER-10 surface introduced incrementally through OQ5 / COMMITTEE / DREP / ENACTMENT-COMMITTEE-FIDELITY / WRITEBACK; no further extension was needed for the snapshot-loader hardenings (which are GREEN). The B1-owed `ci/ci_check_no_fail_open_in_validation.sh` (the `if len == K { check } else { skip }` grep gate, DC-VAL-06 / DC-TXV) is **still not shipped** — carried forward from prior CODEMAP as a known residual gap.

> **Carried residual gaps (unchanged at HEAD).** (a) `ci/ci_check_no_fail_open_in_validation.sh` not shipped. (b) The `ade_ledger::mempool` BLUE/GREEN partition is review-and-test-enforced, not mechanical. (c) The `ade_ledger::consensus_input_extract` RED sub-classification is review-and-doc-enforced, not mechanical. (d) The `ade_runtime::consensus` GREEN/RED partition is "by file" — no dedicated CI script. (e) CE-N-B-6 manual evidence (live tip-agreement) closes via operator log, not CI. (f) The no-owned-`String` / no-`Box<dyn>` shape of `ade_types::conway::cert` types is outside the `TARGETS` array of `ci_check_consensus_closed_enums.sh` (extending it to `crates/ade_types/src/conway/` would fold the residue). (g) The `ade_ledger::delegation` owner-tagged action types are outside any narrow closed-enum grep gate (compiler-match + test + review enforced). (h) DC-LEDGER-08 / DC-LEDGER-09 / DC-LEDGER-10's real epoch-576 oracle is environment-blocked (absent UMap snapshot). (i) The Conway block-body vkey-witness closure is still not wired into the `rules.rs` block-body loop.
