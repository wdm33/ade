# Module Authority Map — Ade

> **Status:** Living architectural document. Regenerated; not hand-edited.
> Per-project instance of `~/.claude/methodology/templates/codemap.md`.

> 11 crates, 462 canonical types, 2469 tests, 159 CI checks at HEAD (`b4c0983d`, PHASE4-N-AK close — recovered-anchor live-follow start + recovered-anchor rollback no-op; the sole new BLUE authority is the version-gated `RecoveredAnchorPoint` closed record + its sole canonical CBOR codec, a SEPARATE additive record from `SeedEpochConsensusInputs`).

---

## Conventions

- A **module** in Ade is a Cargo workspace crate (smallest independently-buildable unit). Source: `Cargo.toml` `[workspace] members` (11 entries). One exception to crate-granularity: `ade_network` is split by *submodule color* — `.idd-config.json` `core_paths` resolves BLUE at the submodule path level (9 paths), so its BLUE / GREEN / RED submodules are documented as separate entries.
- Several BLUE crates host sub-trees that carry a *different* TCB color by their own module banner; the RED `ade_runtime` and `ade_node` crates host numerous **GREEN-by-content** sub-trees. These are surfaced as sub-classification notes inside the owning crate's entry (or, where load-bearing, as their own GREEN/RED entries below).
- Modules are listed by TCB color (BLUE → GREEN → RED), alphabetical within each color.

### TCB color sources (in order of authority)

1. `.idd-config.json` `core_paths` — substring match against absolute path. BLUE crate prefixes: `ade_codec`, `ade_types`, `ade_crypto`, `ade_core`, `ade_ledger`, `ade_plutus`. Plus the 9 BLUE `ade_network` submodule paths: `mux/frame.rs`, `codec/`, `handshake/`, `chain_sync/`, `block_fetch/`, `tx_submission/`, `keep_alive/`, `peer_sharing/`, `n2c/`.
2. `.idd-config.json` `_core_paths_doc` — `ade_runtime` is RED; `ade_testkit` is GREEN; `ade_node` is RED; `ade_network::mux::transport` + `ade_network::session` are nominally RED (but `session/` is GREEN-by-content per PHASE4-N-L). `ade_core_interop` is RED.
3. **Module doc-comment banners** — every BLUE/GREEN source file opens with the `// Core Contract:` banner and/or a `//! BLUE …` / `//! GREEN …` / `//! RED …` line. This is the per-file authority that distinguishes GREEN-by-content from RED inside `ade_runtime` / `ade_node`. Verified directly against the tree at HEAD `b4c0983d` for every new/changed module in the PHASE4-N-AK span: the two NEW BLUE files `ade_ledger::recovered_anchor_point.rs` (`// Core Contract:` + `//! BLUE RecoveredAnchorPoint type + canonical CBOR codec`) and `ade_runtime::recovered_anchor.rs` (`// Core Contract:` + `//! BLUE recovered anchor-point load + fail-closed verify`); the RED/GREEN host changes in `ade_runtime::{bootstrap (//! GREEN single bootstrap authority), chaindb/* (RED), forward_sync/reducer (GREEN forward-sync lifecycle reducer), seed_epoch_lineage (RED)}` + `ade_node::{node_sync, node_lifecycle (//! RED `--mode node`)}`.
4. Cluster TCB Color Maps under `docs/clusters/` and `docs/clusters/completed/` — §5 "TCB color map (FC/IS partition)" of each `cluster.md`.
5. The invariant registry `docs/ade-invariant-registry.toml` (358 rules at HEAD) — each rule's `code_locus` + `statement` is the authoritative source for the **MUST NOT** rows below.

### Active cluster at HEAD

**PHASE4-N-AK (recovered-anchor live-follow start + recovered-anchor rollback no-op — `DC-NODE-31` / `DC-NODE-32`) — CLOSED.** HEAD `b4c0983d` is the close. N-AK is **BLUE+RED+GREEN**: it adds ONE new BLUE module + authority (`ade_ledger::recovered_anchor_point` — the closed `RecoveredAnchorPoint` record + its SOLE canonical CBOR codec, version-gated at `RECOVERED_ANCHOR_POINT_SCHEMA_VERSION = 1`; +2 BLUE canonical types `RecoveredAnchorPoint` + `RecoveredAnchorPointError`, 460 → 462), ONE new BLUE load+verify fn (`ade_runtime::recovered_anchor::load_recovered_anchor_point` — kept OUT of `bootstrap.rs` to preserve the CN-NODE-01 single-`pub fn` bootstrap authority), a RED anchor-fp-keyed store surface (`SnapshotStore::{put,get}_recovered_anchor_point` — disjoint namespace, new redb table `recovered_anchor_point_by_anchor_fp`), and the RED/GREEN node-lifecycle + single-producer follow wiring. It flips TWO rules → enforced (`DC-NODE-31`, `DC-NODE-32`), both **test-enforced** (`ci_script = ""` — no dedicated CI gate; consistent with the project's prior test-enforced rules `DC-PROTO-10` / `T-REC-05`). The N-AK cluster + slice docs (S1 recovered-anchor-live-follow-start, S2 recovered-anchor-follow-rollback) live under the active `docs/clusters/PHASE4-N-AK/`; its invariants/plan under `docs/planning/phase4-n-ak-{recovered-anchor-tip-invariants, s2-recovered-anchor-boundary-invariants}.md`. The CE-AK-3 live transcript (re-recover → FindIntersect at the persisted anchor → RollBackward(anchor) idempotent no-op → caught up to `forge_base_block_no=13`, the frozen c2-relay tip) is the live evidence; it does NOT claim full ChainSel convergence. **Carried: PHASE4-N-AJ (participant-path convergence evidence emission — the CE-AI-6 bridge — `DC-NODE-30`) — CLOSED** at `b1bed361`. N-AJ was **evidence-only, ZERO BLUE change** (460 → 460 types; first BLUE-empty span since the G-N window): it added the GREEN `ade_node::convergence_evidence` module (a closed `ConvergenceEvidenceSink` over `Box<dyn Write>`, RED file I/O, no raw-writer accessor) emitting the reused closed `AgreementVerdict` vocabulary to a dedicated `--convergence-evidence-path` sink (emit-only GREEN side-output; absent path ⇒ byte-unchanged), flipped `DC-NODE-30` enforced + `DC-EVIDENCE-03` enforced_scaffolding, and added +2 CI gates (`ci_check_convergence_evidence_{vocabulary_closed,emit_only}.sh`). **Carried: PHASE4-N-AI (live fork-choice rollback-follow wiring — single-best-peer FOLLOW, NOT full ChainSel; `DC-NODE-23`…`DC-NODE-29`) — CLOSED** at `5ec841c8`. N-AI was **BLUE+RED+GREEN** (the FIRST BLUE delta since the G-N span): it added the BLUE `WalEntry::RollBack` durable MARKER + its `RollbackPoint` / `RollbackReason` payload types (458 → 460), flipped SEVEN rules → enforced, strengthened 13, and added +9 CI gates. The BLUE `select_best_chain` production path is UNCHANGED (the `fork_choice.rs` touch is a `#[cfg(test)]`-only arrival-order proof for CN-CONS-01). A per-cluster SECURITY REVIEW caught HIGH H-1 (rollback target built from peer slot + locally-verified hash) → AI-S6 `DC-NODE-29` pre-commit canonical binding; re-review CLOSED. **Carried: PHASE4-N-AH (local selected durable chain forge-base authority + cert evidence-only + single-producer warm-start re-entry — `DC-NODE-20` / `DC-NODE-21` / `DC-NODE-22`) — CLOSED** at `5858288e` (RED/GREEN-only; the `VenueAdoptionCertificate` type + cert parser fully REMOVED). **Carried: PHASE4-N-AG (single-producer loop-continuation-after-feed-EOF — `DC-NODE-19`, declared) — CLOSED.** **Carried: PHASE4-N-AF (single-producer extend-own-durable-spine — `DC-NODE-18`) — CLOSED** (its cert-promotion mechanism SUPERSEDED by N-AH). **Carried: PHASE4-N-AE / N-AE.F (recover→serve continuity + forge-on-followed-tip admissibility + receive idempotency — `DC-NODE-14` / `DC-NODE-15` / `DC-CONS-24` / `DC-PROTO-10` / `DC-NODE-16`) — CLOSED.** **Carried: PHASE4-N-AD (tip-successor durability proof) — CLOSED** (test-only). **Carried: PHASE4-N-AC (KES signing evolves key to current period — `DC-CRYPTO-10`) — CLOSED.** **Carried: PHASE4-N-AB (outbound mux segmentation — `CN-SESS-05`) — CLOSED.** **Carried: PHASE4-N-AA (bounded peer-driven serve range — `DC-SERVEMEM-01`) — CLOSED.** **Carried: PHASE4-N-U (forged-block durability — `DC-NODE-12` / `DC-CONS-23` / `DC-WAL-04` / `T-REC-05` / `DC-NODE-13`) — CLOSED.** **Carried (prior refresh):** the PHASE4-N-F-G-K…G-R catch-up + the C1 genesis-successor rehearsal evidence — the eight G-K…G-R clusters each shipped a single headline rule (G-N two), all `enforced`. Every prior closed cluster doc — the entire PHASE4-N-F-A…N-F-G-J set, the N-Q/N-R-*/N-S-* set, the N-M-* sub-trees, N-O…N-Z — is archived under `docs/clusters/completed/`.

**What the binary `Node` arm actually runs (precise wiring honesty):** `--mode node` (`ade_node::node_lifecycle`) bootstraps via the single `bootstrap_initial_state` authority (WarmStart overlays the recovered eta0 + — **PHASE4-N-AK** — resolves the live-follow start tip via `resolve_live_follow_start(chaindb.tip(), recovered_anchor)`), spawns the live WirePump feed (FindIntersect at the resolved start point via `wire_pump_start_point`, never blind Origin for a bare-anchor recovery), drains it through the sole `forward_sync::pump_block` durable-admit chokepoint, serves a read-only projection of the durable ChainDb to peers (the serve listener's lifetime independent of feed-end), and — for a declared single-producer venue — forges on its local durable spine head. **PHASE4-N-AK:** the single-producer follow path (`run_node_sync`) now accepts a peer `RollBackward` binding EXACTLY (slot AND hash) to the persisted recovered anchor as an idempotent no-op boundary rewind; every other rollback (incl. Origin, slot-only, hash-only, no-recovered-anchor) still fails closed (`UnexpectedRollback`). The Participant rollback-follow path (`run_participant_sync`) is separate and UNTOUCHED. `RO-LIVE-01` stays operator-gated.

### Counts (mechanical, with sources)

| Count | Value | Source / command |
|---|---|---|
| Crates | **11** | `grep -cE '"crates/' Cargo.toml` (`[workspace] members`). **Δ vs prior on-disk CODEMAP (11): 0** — the `b1bed361..HEAD` PHASE4-N-AK span added no crate (`git diff b1bed361..HEAD -- Cargo.toml` is empty). |
| Canonical types | **462** | `git grep -hE "^(pub )?(struct\|enum) "` over the 6 BLUE crate `src/` trees + the 9 BLUE `ade_network` submodule paths, summed at HEAD `b4c0983d`. **Δ vs prior on-disk CODEMAP (460): +2** — the two NEW BLUE types are `ade_ledger::recovered_anchor_point::{RecoveredAnchorPoint, RecoveredAnchorPointError}` (the closed anchor-point record + its closed encode/decode error sum). `RECOVERED_ANCHOR_POINT_SCHEMA_VERSION = 1` is a `const` (NOT counted); `encode_recovered_anchor_point` / `decode_recovered_anchor_point` are fns (NOT counted). Breakdown (vs N-AJ): `ade_codec` 11, `ade_types` 82, `ade_crypto` 21, `ade_core` 49, **`ade_ledger` 181 (179 → 181, +2)**, `ade_plutus` 8 (= 352) + `ade_network` BLUE submodules **110** (`mux/frame.rs` 5, `codec/` 39, `handshake/` 9, `chain_sync/` 11, `block_fetch/` 10, `tx_submission/` 5, `keep_alive/` 5, `peer_sharing/` 5, `n2c/` 21). `canonical_type_registry: null`, so the structural grep is authoritative. |
| Tests | **2469** | `git grep -hE "#\[test\]\|#\[tokio::test" -- 'crates/**/*.rs' \| wc -l` = 2469 at `b4c0983d`. Approximate per the template fallback rule (count of attributes, not a runner enumeration). **Δ vs prior on-disk CODEMAP (2413 at `5ec841c8`): +56** across the N-AJ + N-AK spans — the AK-S1 `RecoveredAnchorPoint` round-trip + version/short-hash/trailing-byte rejection tests (`ade_ledger::recovered_anchor_point`), the `resolve_live_follow_start` / warm-start anchor-point load + fail-closed binding tests (`ade_runtime::bootstrap` + `recovered_anchor` + `seed_epoch_lineage`), the AK-S2 recovered-anchor rollback idempotent-no-op + Origin/non-anchor/slot-only/hash-only/no-anchor fail-closed tests (`ade_node::node_sync` + `tests/live_fork_choice_ai_s4bii.rs`), and the N-AJ convergence-evidence emit-only tests (`ade_node::convergence_evidence`). **Method caveat (carried, see Generation notes):** the documented grep undercounts `#[tokio::test(flavor = "multi_thread", …)]`, so the *true* new-test count for the span is higher. |
| CI checks | **159** | `ls ci/ci_check_*.sh \| wc -l` = 159 at `b4c0983d` (cross-checked `git ls-tree -r HEAD ci/`). CI lives entirely under `ci/` (`ci_dirs = ["ci"]`); there is no `.github/workflows/` invariant gate at HEAD. **Δ vs prior on-disk CODEMAP (157): +2** — both NEW gates are from PHASE4-N-AJ (`ci_check_convergence_evidence_vocabulary_closed.sh`, `ci_check_convergence_evidence_emit_only.sh`). **PHASE4-N-AK added ZERO CI gates** — verified mechanically: `git diff --name-status b1bed361..HEAD -- 'ci/ci_check_*.sh'` lists 0 `A`, 0 `M`, 0 `D` (empty). Both N-AK rules `DC-NODE-31` / `DC-NODE-32` are **test-enforced** (`ci_script = ""`). |
| Registry rules | **358** | `grep -cE "^id = " docs/ade-invariant-registry.toml` = 358. Reference only — not a header count. **Status breakdown:** 224 `enforced` / 19 `partial` / 114 `declared` / 1 `enforced_scaffolding`. **Δ vs prior on-disk CODEMAP (354): +4** — the N-AJ `DC-NODE-30` (enforced) + `DC-EVIDENCE-03` (enforced_scaffolding), and the N-AK `DC-NODE-31` + `DC-NODE-32` (both enforced, both `ci_script = ""` test-enforced). **No rule weakened.** `CN-CONS-03` stays `partial` (single-best-peer venue only; full multi-peer ChainSel out of scope). |

---

## BLUE Modules — Pure Functional Core

> **Shared header (applies to every BLUE entry below).** Every `.rs` source file begins with the `// Core Contract:` banner, and the following deny attributes are present in each crate's `lib.rs` (or, for `ade_network`, at the crate root — BLUE submodules inherit them):
> `#![deny(unsafe_code)]`, `#![deny(clippy::unwrap_used)]`, `#![deny(clippy::expect_used)]`, `#![deny(clippy::panic)]`, `#![deny(clippy::float_arithmetic)]`.
>
> Cross-cutting BLUE CI gates (all scope the full BLUE set — 6 BLUE crates + 9 BLUE `ade_network` submodule paths): `ci_check_module_headers.sh`, `ci_check_forbidden_patterns.sh`, `ci_check_dependency_boundary.sh`, `ci_check_no_signing_in_blue.sh`, `ci_check_no_semantic_cfg.sh`, `ci_check_hash_uses_wire_bytes.sh`, `ci_check_ingress_chokepoints.sh`, `ci_check_pallas_quarantine.sh`, `ci_check_no_async_in_blue.sh`.
>
> Narrow BLUE / wire / forge / recovered-surface gates carried through prior spans: the N-W `ci_check_producer_praos_vrf.sh` (CN-FORGE-04), the N-X `ci_check_tag24_wire_authority.sh` (CN-WIRE-08), the N-Y `ci_check_forward_sync_chokepoint_only.sh` (DC-SYNC-01) / `ci_check_mithril_uses_bootstrap_initial_state.sh` (CN-MITHRIL-01) / `ci_check_no_haskell_fingerprint_equality.sh` (DC-COMPAT-01), the workspace-wide `ci_check_registry_code_locus_exists.sh`, the N-F-A `ci_check_consensus_input_provenance.sh` (CN-CINPUT-02), the N-F-G-J `ci_check_prevhash_single_wire_authority.sh` (CN-WIRE-09), the G-L/G-M/G-N/G-O wire-authority family (`ci_check_n2n_handshake_versiondata_authority.sh`, `ci_check_chainsync_findintersect_compat.sh`, `ci_check_warmstart_eta0_overlay.sh`, `ci_check_feed_tag24_unwrap.sh`), and the N-AI `ci_check_wal_rollback_replay_equiv.sh` (DC-NODE-27 — the `WalEntry::RollBack` marker + the replay re-anchor) / `ci_check_chain_selection_arrival_order_independent.sh` (CN-CONS-01 — the `#[cfg(test)]` arrival-order proof over the UNCHANGED `select_best_chain`). **PHASE4-N-AK added NO BLUE-set CI gate** — the new BLUE module `ade_ledger::recovered_anchor_point` (`RecoveredAnchorPoint` + sole codec) and the new BLUE fn `ade_runtime::recovered_anchor::load_recovered_anchor_point` are TEST-ENFORCED (`DC-NODE-31.ci_script = ""`); the standing BLUE deny / closed-enum / canonical-codec gates apply to the new module unchanged.

---

### `ade_codec`

| Attribute | Value |
|-----------|-------|
| **Purpose** | Owns Cardano-canonical CBOR ingress — the only place in the workspace that turns raw bytes into typed semantic values, with wire-byte preservation for every hash-bearing structure. Owns the standalone opcert byte authority and the canonical Conway-tx preserved-byte splitter, the canonical block-envelope encoder `cbor::envelope::encode_block_envelope`, and (N-X) the single workspace **tag-24 CBOR-in-CBOR wire-envelope authority** `cbor::tag24::{wrap_tag24, unwrap_tag24}` + closed `TagEnvelopeError`. Also exposes the shared canonical CBOR read/write primitives the BLUE codecs in `ade_ledger` + `ade_network` build on — incl. the `canonical_width` / `read_array_header` / `read_bytes` / `read_uint` / `write_array_header` / `write_bytes_canonical` / `write_uint_canonical` primitives the **PHASE4-N-AK** `RecoveredAnchorPoint` codec in `ade_ledger` consumes. **N-F-G-J** owns the POSITION-BLIND header `prev_hash` codec `shelley::block::decode_prev_hash` (`null -> Genesis` / `hash32 -> Block`; CN-WIRE-09). |
| **Creates** | `PreservedCbor<T>`, `RawCbor`, `BlockEnvelope`, `ByronDecodedBlock`, `CodecContext`, `CodecError`, `ContainerEncoding`, `IntWidth`, era-tagged block/tx wrappers, `OpCertCodecError`, `TxComponents<'a>`, `TagEnvelopeError` (closed). 11 public types (byte-stable across the G-K…N-AK span). |
| **Interprets** | All canonical Cardano CBOR. `unwrap_tag24` is the single workspace authority that strips a tag-24 (`0xd8 0x18`) envelope (zero-copy borrow, fail-closed `TagEnvelopeError`). The feed-side RED `ade_runtime::admission::wire_pump` calls `unwrap_tag24` before the BLUE decode (N-F-G-O) — `ade_codec` stays the single tag-24 authority. `shelley::block::decode_prev_hash` decodes the header `prev_hash` POSITION-BLIND (CN-WIRE-09). |
| **MUST NOT** | (1) Construct `PreservedCbor` outside `ade_codec`. (2) Re-encode wire bytes when computing hashes. (3) Use any forbidden BLUE pattern. (4) Depend on any workspace crate except `ade_types`. (5)–(11) per-construct prohibitions (Conway cert/withdrawals/governance, opcert, tx_components, single block-envelope encoder). (12) `cbor::tag24` (CN-WIRE-08) — `wrap_tag24`/`unwrap_tag24` defined exactly once; `unwrap_tag24` fails closed (never panic); no second/hand-rolled tag-24 parse anywhere (incl. the feed-side WirePump, which MUST reuse this authority — CN-WIRE-12). (13) `shelley::block::decode_prev_hash` (CN-WIRE-09) — the codec is the byte representation only and MUST stay POSITION-BLIND (no `block_number`); the position-aware coupling lives in `ade_ledger::block_validity::header_position`, never here. |
| **Inbound deps** | `ade_ledger` (heavy — incl. the N-AK `recovered_anchor_point` codec via the canonical primitives), `ade_plutus`, `ade_testkit`, `ade_network`, `ade_runtime`, `ade_core_interop`, `ade_node`. |
| **Outbound deps** | `ade_types`. std-only; dev-deps `serde_json`, `toml`. |
| **Entry points** | `ade_codec::cbor::envelope::{decode_block_envelope, encode_block_envelope}`, `ade_codec::{wrap_tag24, unwrap_tag24, TagEnvelopeError}`, the `cbor::{read_*, write_*_canonical, canonical_width, ContainerEncoding, IntWidth}` primitives, `ade_codec::traits::AdeEncode`, per-era `decode_*_block`, `ade_codec::shelley::{opcert::{encode_opcert, decode_opcert}, tx_components::split_conway_tx_components, block::decode_prev_hash}`. |
| **Key modules** | `cbor/` (incl. `envelope.rs`, `tag24.rs`), `byron/`, `shelley/` (incl. `block.rs`, `cert.rs`, `opcert.rs`, `tx_components.rs`), `allegra/`, `mary/`, `alonzo/`, `babbage/`, `conway/`, `address/`, `preserved.rs`, `traits.rs`, `primitives.rs`, `error.rs`. |

---

### `ade_core`

| Attribute | Value |
|-----------|-------|
| **Purpose** | The functional core (consensus half): Praos VRF leader-check + the expected-VRF-input recipe; the chain-selection authority `consensus::fork_choice::select_best_chain` (the sole DC-CONS-03 selector); the `PraosChainDepState` / `Nonce` evolution; the HFC era-schedule `EraSchedule`; the closed `LeaderCheckVerdict`. (No `ade_core` production change in the N-AK span; the N-AI `fork_choice.rs` touch was a `#[cfg(test)]`-only arrival-order proof for CN-CONS-01 — production `select_best_chain` byte-unchanged.) |
| **Creates** | `PraosChainDepState`, `Nonce`, `EraSchedule`, `ExpectedVrfInput`, `LeaderCheckVerdict` (closed), `ChainSelection` / candidate types, VRF-input + leader-threshold types. 49 public types. |
| **Interprets** | Praos header VRF/leader inputs; candidate chain summaries for `select_best_chain`. `consensus::praos_state` evolves the nonce. |
| **MUST NOT** | (1) Perform I/O / read a clock / use float / `HashMap`. (2) Be the home of a SECOND chain selector — `select_best_chain` is the sole DC-CONS-03 fork-choice authority (no parallel selector; arrival-order-independent — CN-CONS-01). (3) Construct semantic types from raw bytes. (4) Depend on any RED/GREEN crate (only `ade_types`, `ade_crypto`, `minicbor`). (5) Add a SECOND `leader_vrf_input` / expected-VRF-input authority. |
| **Inbound deps** | `ade_ledger`, `ade_testkit`, `ade_core_interop`, `ade_runtime`, `ade_node`. |
| **Outbound deps** | `ade_types`, `ade_crypto`, `minicbor`. Dev-dep: `ade_testkit`. |
| **Entry points** | `ade_core::consensus::{fork_choice::select_best_chain, leader_check::*, praos_state::{PraosChainDepState, Nonce}}`, `ade_core::era_schedule::EraSchedule`. |
| **Key modules** | `consensus/` (`fork_choice.rs`, `leader_check.rs`, `praos_state.rs`), `era_schedule.rs`. |

---

### `ade_crypto`

| Attribute | Value |
|-----------|-------|
| **Purpose** | Pure cryptographic verification — Praos VRF (draft-03) verify, the Ade-owned BLUE Sum6KES algorithm (`kes_sum/` — matches Haskell `cardano-base` byte-for-byte; the `kes_update` deterministic evolution + the KES verifier), Ed25519 DSIGN verify, blake2b hashing. (Unchanged across the N-AK span.) |
| **Creates** | `KesError`, `KesParseError`, VRF/KES/DSIGN verify types, `Sum6` key/signature representations. 21 public types. |
| **Interprets** | VRF proofs, KES signatures + the expanded `Sum6KES` envelope, Ed25519 signatures; `blake2b_256` / `blake2b_224`. |
| **MUST NOT** | (1) **Sign** anything — verification only; signing material custody lives in the RED `ade_runtime::producer` shell (`T-KEY-01` / `ci_check_no_signing_in_blue.sh`). (2) Perform I/O / read a clock / use float. (3) Re-implement Sum6KES divergently from Haskell `cardano-base` (the cardano-crypto Rust crate is a `#[cfg(test)]` oracle only). (4) Depend on any workspace crate except `ade_types`. |
| **Inbound deps** | `ade_core`, `ade_ledger`, `ade_plutus`, `ade_runtime`, `ade_node`, `ade_core_interop`, `ade_testkit`. |
| **Outbound deps** | `ade_types`, `blake2`, `ed25519-dalek`, `cardano-crypto` (`["vrf-draft03", "kes-sum"(test-only), "dsign"]`). |
| **Entry points** | `ade_crypto::vrf::*`, `ade_crypto::kes_sum::{kes_update, kes_verify, *}`, `ade_crypto::dsign::*`, `ade_crypto::hash::{blake2b_256, blake2b_224}`. |
| **Key modules** | `vrf/`, `kes_sum/`, `dsign/`, `hash.rs`. |

---

### `ade_ledger`

| Attribute | Value |
|-----------|-------|
| **Purpose** | The functional core (ledger half): stateless ledger rules for every era; B1 block-validity verdict; B2 tx-validity verdict + mempool admission; B3 Conway value-conservation; B4/B5 closed Conway cert-state + governance-cert accumulation; live committee enactment; the single BLUE wire-ingress chokepoint `mempool::ingress::mempool_ingress`; the BLUE producer authority (`producer::{forge, self_accept, served_chain, state}`); the single body-hash authority (`block_body_hash`); the receive/rollback/snapshot/WAL/bootstrap-anchor/genesis-source authorities; the KES-signing pre-image recipe `block_validity::unsigned_header_pre_image`; the exact-`Rational` arithmetic; the `ProtocolParameters` model; (N-F-A) the closed `SeedEpochConsensusInputs` recovered-state record + its SOLE codec (`epoch_nonce`/eta0 field, `SEED_CINPUT_SCHEMA_VERSION = 2`), the `PoolDistrView::from_seed_epoch_consensus_inputs` projection, and the WAL provenance variant; (N-F-G-J) the single POSITION-AWARE header authority `block_validity::header_position::check_header_position`; (N-AI) the BLUE `wal::event::WalEntry::RollBack` durable MARKER + its `RollbackPoint` / `RollbackReason` payload types + the `wal::replay` rollback re-anchor arm. **PHASE4-N-AK (NEW — the lone new BLUE module + the +2 new BLUE canonical types this cluster):** the NEW module `recovered_anchor_point` owns the closed, version-gated, byte-canonical record `RecoveredAnchorPoint {anchor_fp: Hash32, slot: SlotNo, block_hash: Hash32}` — the durable `(slot, block_hash)` the recovered store was seeded at, bound to the recovered anchor fingerprint (`anchor_fp`) — and its SOLE canonical CBOR codec pair `encode_recovered_anchor_point` / `decode_recovered_anchor_point` (CN-CINPUT-01 analog: the ONLY pub fn pair encoding/decoding the record). `RECOVERED_ANCHOR_POINT_SCHEMA_VERSION = 1` is written into the encoded `array(4) [version, anchor_fp, slot, block_hash]`; decode rejects unknown versions / short hash bytes / trailing bytes / any non-byte-canonical encoding (re-encode != input) fail-fast. It is a **SEPARATE additive record from `SeedEpochConsensusInputs`** — disjoint shape, disjoint schema version, disjoint `SnapshotStore` surface; both share the same `anchor_fp` key (`BootstrapAnchor.initial_ledger_fingerprint`). No `Default`, no `#[non_exhaustive]` — the type system requires all fields at construction. A zero/null `block_hash` denotes a genesis seed point (truly Origin); the Origin distinction lives in the resolver (`ade_runtime::bootstrap::resolve_live_follow_start`), not the codec. |
| **Creates** | ~179 + 2 = **181** public types (the N-AK +2 are `recovered_anchor_point::{RecoveredAnchorPoint, RecoveredAnchorPointError}`). Load-bearing: `BlockValidityError` (incl. `HeaderPositionInvalid`), `BlockVerdict`, `TxValidity*`, `MempoolState`, `ProducerTick`, `ForgedBlock`, `ForgeError`, `AcceptedBlock` (field private to `producer::self_accept`), `ServedChainSnapshot`, `PoolDistrView`, `PoolEntry`, `LedgerState`, `ProtocolParameters`, `Rational`, `WalEntry` (closed sum: `AdmitBlock` + `SeedEpochConsensusInputsImported` at tag 3 + `RollBack` at tag 1, N-AI), `RollbackPoint` / `RollbackReason` (N-AI payload types), `RecoveredBootstrapProvenance`, `ReplayOutcome`, `SeedEpochConsensusInputs` (closed; carries `epoch_nonce: Nonce`; `SEED_CINPUT_SCHEMA_VERSION = 2`), `SeedConsensusInputsError` (closed), `BootstrapAnchor`, `SeedProvenance` (closed), `SeedPoint`, `ANCHOR_SCHEMA_VERSION = 2`, `MithrilManifestReport`, `BootstrapAnchorError`, `ConwayGenesisConfig`, `GenesisSourceError`, `UnsignedHeaderPreImage`, and **`RecoveredAnchorPoint`** (closed; bound to `anchor_fp`; `RECOVERED_ANCHOR_POINT_SCHEMA_VERSION = 1`) + **`RecoveredAnchorPointError`** (closed sum: `MalformedCbor` / `UnknownVersion` / `Structural` / `TrailingBytes`). `PrevHash` is owned by `ade_types` (consumed here as the additive `DecodedBlock.prev_hash` field). |
| **Interprets** | Canonical decoded blocks/txs/certs/snapshots; `mempool_ingress` is the sole BLUE chokepoint; `self_accept` re-validates a forged artifact; `decode_bootstrap_anchor` reads the version-gated framing; `verify_mithril_binding` cross-checks the Mithril manifest; `genesis_initial_state` produces the cold-start pair; `unsigned_header_pre_image` produces the canonical CBOR of `ShelleyHeaderBody`; `decode_block` calls `check_header_position` before the header authority; `decode_seed_epoch_consensus_inputs` (the SOLE sidecar decoder) reads the 7-field v2 record; `decode_wal_entry` decodes the tag-1 `WalEntry::RollBack` payload (N-AI). **PHASE4-N-AK:** `decode_recovered_anchor_point` (the SOLE recovered-anchor-point decoder) reads the `array(4)` record, fail-closed on unknown version / wrong array shape / short hash / trailing bytes / non-byte-canonical encoding — a structurally valid but non-canonically-encoded buffer (e.g. a uint written wider than minimal) decodes to the same value but re-encodes to different bytes and is rejected `MalformedCbor`. |
| **MUST NOT** | All carry-forward ledger prohibitions (no I/O, no clock, closed cert/governance enums, single body-hash recipe). `producer::{forge, self_accept, served_chain}` fences (CN-FORGE-01/03, CN-PROD-04). `unsigned_header_pre_image` (CN-KES-HEADER-01). `block_validity::header_position` (CN-WIRE-09 position clause). `bootstrap_anchor` / `genesis_source` / `wal` provenance fences. **`seed_consensus_inputs` (CN-CINPUT-01, strengthened by T-REC-04 / DC-CINPUT-03):** a single closed canonical type with a SOLE encoder/decoder pair; deterministic `BTreeMap`-ordered CBOR, version-gated; no `Default`, no `#[non_exhaustive]`; no I/O / clock / float. **`block_validity::header_input` header projection (DC-CONS-18):** `block_header_bytes(&[u8])` + `accepted_block_header_bytes(&AcceptedBlock)` are the SINGLE header-projection authority sharing one `header_cbor_slice` recipe. **`wal::{event, replay}` rollback marker (DC-NODE-27 / CN-WAL-01 / CN-STORE-07 / DC-CONS-20, N-AI):** `WalEntry::RollBack` is a DURABLE MARKER — it MUST NOT implement a second rollback (replay re-invokes the EXISTING `materialize_rolled_back_state` + lockstep `commit_rollback`); the replay re-anchor MUST use `to_point`'s EXISTING in-chain `post_fp`; the WAL stays append-only (tag 1 reserved, tag 2 stays reserved). **`recovered_anchor_point` (CN-CINPUT-01 analog, DC-NODE-31, PHASE4-N-AK):** `RecoveredAnchorPoint` MUST stay a single closed canonical type with a SOLE codec pair `encode_recovered_anchor_point` / `decode_recovered_anchor_point` (no parallel encoder/decoder anywhere — the RED `recovered_anchor::load_recovered_anchor_point` + `seed_epoch_lineage` MUST reuse this codec, never hand-roll); deterministic byte-canonical CBOR, version-GATED (`RECOVERED_ANCHOR_POINT_SCHEMA_VERSION = 1` written into the wire form; decode MUST reject any other version fail-fast, reject short hash bytes, reject trailing bytes, and reject any non-byte-canonical encoding via the re-encode == input check); no `Default`, no `#[non_exhaustive]` (all fields required at construction); no I/O / clock / float / `HashMap`. It MUST stay a SEPARATE additive record from `SeedEpochConsensusInputs` — it MUST NOT touch the seed-epoch sidecar's shape, schema version, or `sidecar_hash`/provenance binding, and MUST NOT share its codec or its store namespace. The Origin distinction (a zero/null `block_hash` is a genesis seed point, never a usable live-follow start) is the RESOLVER's job (`ade_runtime::bootstrap::resolve_live_follow_start`), and MUST NOT be smuggled into the codec (a zero-hash record is a representable, round-trippable value). Test-enforced (`DC-NODE-31.ci_script = ""`). |
| **Inbound deps** | `ade_testkit`, `ade_core_interop`, `ade_runtime` (incl. the N-AK `recovered_anchor_point` codec consumers `recovered_anchor` + `bootstrap` + `seed_epoch_lineage`), `ade_node`. |
| **Outbound deps** | `ade_types`, `ade_crypto`, `ade_codec` (incl. the canonical CBOR primitives the N-AK `recovered_anchor_point` codec uses), `ade_plutus`, `ade_core` (incl. `consensus::praos_state::Nonce`), `minicbor`, `num-bigint`, `num-integer`, `num-traits`. Dev-dep: `ade_testkit`. |
| **Entry points** | `ade_ledger::block_validity::{decode_block, validate_block, check_header_position, block_header_bytes, accepted_block_header_bytes, unsigned_header_pre_image::unsigned_header_pre_image}`, `ade_ledger::tx_validity::*`, `ade_ledger::mempool::{ingress::mempool_ingress, admit::*}`, `ade_ledger::producer::{forge::forge_block, self_accept::{self_accept, AcceptedBlock}, served_chain::*}`, `ade_ledger::block_body_hash::*`, `ade_ledger::{receive, rollback, snapshot}::*`, `ade_ledger::pparams::ProtocolParameters`, `ade_ledger::rational::Rational`, `ade_ledger::wal::*` (incl. `wal::event::{WalEntry::RollBack, RollbackPoint, RollbackReason}`, `wal::replay::compute_superseded`), `ade_ledger::seed_consensus_inputs::{SeedEpochConsensusInputs, encode_seed_epoch_consensus_inputs, decode_seed_epoch_consensus_inputs, SeedConsensusInputsError, SEED_CINPUT_SCHEMA_VERSION}`, `ade_ledger::consensus_view::{PoolDistrView, PoolEntry}`, `ade_ledger::bootstrap_anchor::*`, `ade_ledger::genesis_source::*`, **`ade_ledger::recovered_anchor_point::{RecoveredAnchorPoint, RecoveredAnchorPointError, encode_recovered_anchor_point, decode_recovered_anchor_point, RECOVERED_ANCHOR_POINT_SCHEMA_VERSION}`** (NEW, PHASE4-N-AK). |
| **Key modules** | `block_validity/` (incl. `unsigned_header_pre_image.rs`, `header_input.rs` — `block_header_bytes(&[u8])` + the `DecodedBlock.prev_hash` field, `header_position.rs`, `verdict.rs`), `tx_validity/`, `mempool/`, `cert_classify/`, `consensus_view.rs`, `producer/`, `block_body_hash.rs`, `receive/`, `rollback/`, `snapshot/`, `pparams.rs`, `rational.rs`, `wal/` (incl. `event.rs` — the `WalEntry::RollBack` variant + `RollbackPoint`/`RollbackReason` at tag 1; `replay.rs` — the rollback re-anchor + `compute_superseded`; `error.rs` — `WalError::RollbackTargetNotInChain`), `seed_consensus_inputs.rs` (`epoch_nonce` field + `SEED_CINPUT_SCHEMA_VERSION = 2`), `bootstrap_anchor/`, `genesis_source.rs`, **`recovered_anchor_point.rs`** (NEW, PHASE4-N-AK: the closed `RecoveredAnchorPoint` record + `RecoveredAnchorPointError` + the sole canonical codec pair + `RECOVERED_ANCHOR_POINT_SCHEMA_VERSION = 1`). |

> **GREEN sub-classification:** `ade_ledger::mempool::{policy, canonicalize}` are GREEN-by-content. **RED sub-classification:** `ade_ledger::consensus_input_extract` is RED-by-content. Both carry their own module banner.

---

### `ade_network` *(BLUE submodules)*

| Attribute | Value |
|-----------|-------|
| **Purpose** | Owns the Cardano Ouroboros mini-protocol authority — the closed wire grammar (CBOR codecs) and pure state machines for all N2N + N2C mini-protocols, plus the BLUE mux frame primitive and the producer-side server-role reducers. Owns the per-protocol tag-24 composition authorities. **`handshake/` (G-L):** owns the single per-version N2N `versionData` wire encoding `handshake::version_table::encode_n2n_version_params` (the cardano-node-compatible NodeToNodeV_15 encoding, CN-WIRE-10). **`chain_sync/` + `codec/` (G-M):** owns the scoped two-form ChainSync `FindIntersect` decode `codec::primitives::decode_array_head_two_form` over the closed `ArrayHead = Definite(u64) \| Indefinite`, and the `chain_sync::server` `Origin → IntersectFound[Origin]` reply (CN-WIRE-11). **`chain_sync/server.rs` (N-AE):** `producer_chain_sync_serve`'s `FindIntersect` handler sets the server read cursor `last_announced` to the resolved intersect `Point` so the next `RequestNext` serves `next_after(point)` (`DC-PROTO-10`). (No `ade_network` change in the N-AK span — the recovered-anchor FindIntersect start point is supplied by `ade_node::wire_pump_start_point` over the existing `codec::chain_sync::Point`; the BLUE submodules are byte-untouched.) |
| **Creates** | **110** BLUE canonical types: `mux/frame.rs` 5, `codec/` 39 (incl. the closed `ArrayHead = Definite(u64) \| Indefinite`, CN-WIRE-11), `handshake/` 9, `chain_sync/` 11, `block_fetch/` 10, `tx_submission/` 5, `keep_alive/` 5, `peer_sharing/` 5, `n2c/` 21. |
| **Interprets** | Mini-protocol wire frames; `mux::frame::decode_frame` is the single frame decode authority; per-protocol `*_transition` reducers consume decoded messages. `decompose_blockfetch_block` / `decompose_rollforward_header` strip the per-protocol tag-24 composition (delegating the inner unwrap to `ade_codec::unwrap_tag24`). `decode_find_intersect_points` decodes the `FindIntersect` points list via `decode_array_head_two_form` (SCOPED to FindIntersect — CN-WIRE-11). |
| **MUST NOT** | (1) `mux::frame` — single `encode_frame`/`decode_frame` pair. (2) handshake — single `n2n_transition` + single `n2c_transition`. (3) closed `AcceptedMiniProtocol` registry. (4) no socket I/O in BLUE submodules. (5) no async/tokio in BLUE submodules. (6) depend on no workspace crate beyond `ade_codec` + `ade_types`. (7) server reducers MUST NOT split headers in parallel or depend on signing. (8) tag-24 composition (CN-WIRE-08) — composers delegate byte-level wrap/strip to the single `ade_codec` authority; pinned byte-identically against captured cardano-node 11.0.1 fixtures. **(8b) `n2n_supported_for_magic` (DC-NODE-07) — parameterizes ONLY `network_magic`; no version-set widening.** **(8c) `encode_n2n_version_params` (CN-WIRE-10) — the SINGLE per-version N2N `versionData` encoder; cardano-node-compatible per-version field shape; MUST NOT widen the closed `N2N_SUPPORTED` version SET (`ci_check_n2n_handshake_versiondata_authority.sh`).** **(8d) `decode_array_head_two_form` / `ArrayHead` + the `Origin` reply (CN-WIRE-11) — the indefinite-length acceptance is SCOPED to ChainSync `FindIntersect`; the `Origin → IntersectFound[Origin]` reply MUST NOT widen the served chain beyond the Origin intersect (`ci_check_chainsync_findintersect_compat.sh`).** **(8e) `producer_chain_sync_serve` FindIntersect cursor (DC-PROTO-10):** after `IntersectFound(point)`, `last_announced` MUST equal that point so the next `RequestNext` serves `next_after(point)`, NEVER `next_after(None)` (the chain start); an `Origin` intersect MUST keep the cursor `None`. Test-enforced (`ci_script = ""`). |
| **Inbound deps** | `ade_runtime`, `ade_node`, `ade_core_interop`, `ade_testkit` (and the `ade_network` GREEN/RED submodules). |
| **Outbound deps** | `ade_types`, `ade_codec`. No external deps in BLUE submodules. |
| **Entry points** | `ade_network::mux::frame::{encode_frame, decode_frame}`, `ade_network::handshake::{n2n_transition, n2c_transition}`, `ade_network::handshake::version_table::{N2N_SUPPORTED, n2n_supported_for_magic, encode_n2n_version_params}`, `ade_network::chain_sync::{server::*, client::*}`, `ade_network::block_fetch::{server::*, client::*}`, `ade_network::codec::{block_fetch::{compose,decompose}_blockfetch_block, chain_sync::{{compose,decompose}_rollforward_header, Point}, primitives::{decode_array_head_two_form, ArrayHead}}`, `ade_network::{tx_submission, keep_alive, peer_sharing, n2c}::*`. |
| **Key modules** | `mux/frame.rs`, `codec/` (incl. `block_fetch.rs`, `chain_sync.rs`, `primitives.rs`), `handshake/` (incl. `version_table.rs`), `chain_sync/` (incl. `server.rs`), `block_fetch/` (incl. `server.rs`), `tx_submission/`, `keep_alive/`, `peer_sharing/`, `n2c/`. |

---

### `ade_plutus`

| Attribute | Value |
|-----------|-------|
| **Purpose** | Quarantine boundary between the Ade-canonical ledger and the ported UPLC evaluator from `aiken-lang/aiken` (pinned tag `v1.1.21`). (Unchanged across the N-AK span.) |
| **Creates** | `PlutusScript`, `PlutusLanguage`, `EvalOutput`, `PlutusError`, `CostModels`, `DecoderMode`, `PerScriptResult`, `TxEvalResult`. 8 public types. |
| **Interprets** | UPLC scripts (Plutus V1/V2/V3) and `CostModels` CBOR; phase-two tx evaluation. `PlutusScript::from_cbor` is a named ingress chokepoint. |
| **MUST NOT** | (1) Re-export any `pallas_*` or `aiken_uplc::` type. (2) Let another BLUE crate bypass the canonical entry. (3) Activate PV11 builtins. (4) Use any BLUE-forbidden pattern. (5) Construct `PreservedCbor` outside `ade_codec`. |
| **Inbound deps** | `ade_ledger`, `ade_testkit`. |
| **Outbound deps** | `ade_types`, `ade_crypto`, `ade_codec`, `aiken_uplc` (git, tag `v1.1.21`), `pallas-primitives` (internal-only). |
| **Entry points** | `ade_plutus::eval_tx_phase_two`, `ade_plutus::tx_eval::*`, `ade_plutus::evaluator::*`, `ade_plutus::cost_model::*`. |
| **Key modules** | `evaluator.rs`, `cost_model.rs`, `script_context.rs`, `script_verdict.rs`, `tx_eval.rs`. |

---

### `ade_types`

| Attribute | Value |
|-----------|-------|
| **Purpose** | Owns the canonical Cardano domain vocabulary — primitives, era enum, address forms, era-tagged tx bodies/outputs/certificates, governance types — the lingua franca for every other crate. `CardanoEra` carries `is_praos()`. **N-F-G-J (carried)** owns the closed sum `PrevHash = Genesis | Block(Hash32)`. `Hash32` / `SlotNo` are the field types of the **PHASE4-N-AK** `RecoveredAnchorPoint` record (consumed by `ade_ledger`, not created there). (No `ade_types` change in the N-AK span — `82` byte-stable.) |
| **Creates** | `CardanoEra` (with `is_praos()`), `SlotNo`, `BlockNo`, `EpochNo`, `Hash28`, `Hash32`, `Coin`, `Lovelace`, `NetworkId`, `Nonce`, `TxIn`, `RewardAccount`, `PoolId`, `Address`, `ByronAddress`, `Credential`, `StakeCredential`, `Certificate`, `ConwayCert`, `DRep`, `GovAction`, `GovActionId`, `Anchor`, `ProposalProcedure`, `OperationalCert`, `NativeScript`, `Datum`, `MultiAsset`, `AssetName`, `CostModel`, `ExUnits`, per-era tx-body/tx-out/witness wrappers, `ShelleyBlock`/`ShelleyHeader`/`VrfData`/`ProtocolVersion`/`PrevHash`. 82 public types. |
| **Interprets** | None — produce-only. |
| **MUST NOT** | (1) Construct or decode `PreservedCbor`. (2) Use any BLUE-forbidden pattern. (3) Depend on any workspace crate. (4) Add open/extensible variants to closed enums without a versioned gate (`CardanoEra` discriminants closed: Byron=0 … Conway=7; `PrevHash` is a closed 2-variant `Genesis | Block(Hash32)` sum, never a flat `Hash32`). |
| **Inbound deps** | Every other workspace crate. |
| **Outbound deps** | None. |
| **Entry points** | `ade_types::CardanoEra` (incl. `is_praos`), `ade_types::tx::{Coin, TxIn, RewardAccount}`, `ade_types::{Hash32, SlotNo, Hash28, BlockNo, EpochNo, Nonce}`, `ade_types::conway::{tx::ConwayTxBody, cert::*, governance::*}`, `ade_types::shelley::block::{OperationalCert, ProtocolVersion, ShelleyHeader, ShelleyBlock, VrfData, PrevHash}`. |
| **Key modules** | `primitives.rs`, `era.rs`, `tx.rs`, `address/`, `byron/`, `shelley/`, `allegra/`, `mary/`, `alonzo/`, `babbage/`, `conway/`. |

---

## GREEN Modules — Deterministic Glue

> Deterministic, non-authoritative. May depend on BLUE; must not affect authoritative outputs. GREEN sub-trees inside the RED `ade_runtime` and `ade_node` crates carry a `//! GREEN …` banner + the same deny attributes and are CI-gated for purity. **PHASE4-N-AK touched ONE nominally-GREEN host (`ade_runtime::forward_sync::reducer`)** — adding the `ForwardSyncState.recovered_anchor: Option<ChainTip>` field (replay-derivable; default `None`), the threaded recovered-anchor authority the single-producer RollBack handler reads (DC-NODE-32). The full per-module GREEN entries below are carried forward.

### `ade_node::convergence_evidence` *(GREEN by content — PHASE4-N-AJ)*

| Attribute | Value |
|-----------|-------|
| **Purpose** | The GREEN closed-vocabulary convergence-evidence sink for the live Participant rollback-follow path. `ConvergenceEvidenceSink` writes the reused closed `AgreementVerdict` vocabulary (`block_received` / `block_admitted` / `agreement_verdict` via `verdict::derive`) to a dedicated `--convergence-evidence-path` JSONL sink as an emit-only side-output — the CE-AI-6 bridge that closed the gap where the rollback-follow (node) path emitted no agreement evidence while `--mode admission` ignored rollbacks. |
| **Creates** | `ConvergenceEvidenceSink` (over a `Box<dyn Write>` — RED file I/O underneath, no raw-writer accessor), `EvidenceEmitResult { Written \| Disabled \| FailedAndPoisoned }` (closed). Not canonical-counted (GREEN). |
| **MUST NOT** | Affect authoritative outputs; halt authority on a write failure (a write-failure surfaces + poisons + marks incomplete via `EvidenceEmitResult::FailedAndPoisoned`, NEVER swallowed silently, NEVER halts the authority path); add an open/wildcard event variant to the closed `AgreementVerdict` vocabulary (DC-ADMIT-04 — closed-vocab isolation extends to this file); emit on a non-Diverged path that overstates semantic truth (emit-only on `Diverged`); change the byte output when `--convergence-evidence-path` is absent (absent path ⇒ byte-unchanged); expose a raw-writer accessor. |
| **Inbound deps** | `ade_node::node_lifecycle` (the live Participant rollback-follow path). |
| **Outbound deps** | the reused `AgreementVerdict` verdict vocabulary + `verdict::derive`; `std::io::Write`. |
| **Entry points** | `ade_node::convergence_evidence::{ConvergenceEvidenceSink, EvidenceEmitResult}`. |

### `ade_network::session` *(GREEN by content — PHASE4-N-L; outbound segmentation N-AB)*

| Attribute | Value |
|-----------|-------|
| **Purpose** | The deterministic GREEN session reducer for the Cardano Ouroboros mux — `step` is the SOLE pure fn reducing `(SessionState, ByteChunkIn) -> Result<(SessionState', Vec<SessionEffect>), SessionError>` (CN-SESS-03), composing the BLUE `mux::frame::{encode,decode}_frame` + `handshake::n2n_transition` + the closed `AcceptedMiniProtocol` registry + the per-mini-protocol frame demux/reassembly. It owns the type-state (`Handshaking` → `Connected`; DC-SESS-01) and the symmetric per-mini-protocol payload assembly across mux-frame boundaries: INBOUND it accumulates a per-protocol buffer and emits one `DeliverPeerFrame` per COMPLETE CBOR item (CN-SESS-04); OUTBOUND `handle_outbound` SEGMENTS an outbound payload larger than `MAX_PAYLOAD` into ordered `<= MAX_PAYLOAD` mux frames (via `encode_inner_frame`), fail-closed above the fixed `MAX_OUTBOUND_PAYLOAD_BYTES = 16 MiB` (CN-SESS-05). GREEN-by-content — every file is pure / deterministic / no tokio / SystemTime / Instant / rand; the async tokio layer lives in `ade_runtime::network::{mux_pump, n2n_dialer}`. |
| **Creates** | `SessionState` (type-state), `ConnectedState` (closed-sum-indexed `proto_buffers` — no HashMap), `HandshakeProgress`, `SessionEffect` (closed), `SessionError` (closed — incl. `ReassemblyBufferOverflow` + `OutboundPayloadTooLarge`), `AcceptedMiniProtocol` (the CLOSED registry — GREEN), `ByteChunkIn`, `HandshakeRole`, `FrameBuffer`. The const `MAX_OUTBOUND_PAYLOAD_BYTES = 16 MiB` — a GREEN constant, NOT a canonical type. None canonical-counted. |
| **MUST NOT** | Affect authoritative outputs; introduce nondeterminism (no wall-clock / rand / float / HashMap). **CN-SESS-03:** `step` is the SOLE reduction entry. **CN-SESS-01/02:** frame encode/decode through the single BLUE `mux::frame`; handshake through the single `n2n_transition`. **DC-SESS-01:** a `Handshaking` session MUST NOT deliver any non-handshake frame. **CN-SESS-04:** one accumulating buffer per `AcceptedMiniProtocol`; one `DeliverPeerFrame` per COMPLETE CBOR item. **CN-SESS-05:** an outbound payload > `MAX_PAYLOAD` MUST be SEGMENTED via the single `encode_inner_frame` authority (no truncate/drop/reorder; `concat(segments) == payload`; same id/mode/timestamp across segments; fail closed above the FIXED `MAX_OUTBOUND_PAYLOAD_BYTES = 16 MiB`). |
| **Inbound deps** | `ade_runtime::network::{mux_pump, n2n_dialer, n2n_listener}`, `ade_runtime::admission::wire_pump`, `ade_node::wire_only`, `ade_network::mux::transport`. |
| **Outbound deps** | BLUE `ade_network::{mux::frame, handshake}`; `ade_codec` (`cbor::skip_item`, `error::CodecError`); `ade_types`. No tokio (GREEN). |
| **Entry points** | `ade_network::session::{step, FrameBuffer, SessionState, ConnectedState, HandshakeProgress, SessionEffect, SessionError, AcceptedMiniProtocol, ByteChunkIn, HandshakeRole}`, `ade_network::session::handshake_driver::{run_n2n_handshake_initiator, run_n2n_handshake_responder}`. |
| **Key modules** | `session/core.rs` (`step` + inbound reassembly + outbound segmentation), `session/state.rs`, `session/demux.rs`, `session/event.rs`, `session/handshake_driver.rs`. |

> **GREEN classification (load-bearing):** `ade_network::session` is **promotable to BLUE on demand, never demotable to RED**.

### `ade_runtime::seed_consensus_merge` *(GREEN by content — PHASE4-N-F-A A2; eta0 threaded N-F-G-N)*

| Attribute | Value |
|-----------|-------|
| **Purpose** | The deterministic, no-I/O merge transform lifting a verified-bootstrap `LiveConsensusInputsCanonical` plus the minted anchor fingerprint + seed epoch into the BLUE single-map `SeedEpochConsensusInputs`, carrying the recovered Praos `epoch_nonce` (eta0) into the BLUE record's `epoch_nonce` field (T-REC-04). (Unchanged in shape across the N-AK span.) |
| **Creates** | `SeedConsensusMergeError` (closed: `PoolMissingVrfKeyhash` / `PoolMissingStake`). Not canonical-counted (GREEN). |
| **MUST NOT** | Affect authoritative outputs. Introduce nondeterminism (`BTreeMap` only). Default-fill on a provenance gap. Carry the recovered eta0 as anything but verbatim (DC-CINPUT-03 — never a zero-eta0 fallback). |
| **Inbound deps** | `ade_runtime::{genesis_bootstrap, mithril_bootstrap}`. |
| **Outbound deps** | `ade_ledger::{consensus_view::PoolEntry, seed_consensus_inputs::SeedEpochConsensusInputs}`, `ade_runtime::consensus_inputs::LiveConsensusInputsCanonical`, `ade_core::consensus::praos_state::Nonce`, `ade_types`. |
| **Entry points** | `ade_runtime::seed_consensus_merge::{merge_seed_epoch_consensus_inputs, SeedConsensusMergeError}`. |

---

### `ade_runtime` GREEN-by-content sub-trees (carried)

> All carry a `//! GREEN …` banner inside the RED `ade_runtime` crate, the BLUE deny attributes, and a purity CI gate. Each is "promotable to BLUE on demand, never demotable to RED" unless noted.

| Sub-tree | Purpose | MUST NOT | CI gate |
|---|---|---|---|
| `producer::self_accepted_handoff` *(N-F-G-B)* | GREEN constructor-fenced carrier for a BLUE self-accepted forged block (sole ctor `from_self_accepted(AcceptedBlock)`). | Carry an artifact that was not BLUE self-accepted; re-derive / re-validate; affect authoritative outputs. | `ci_check_served_chain_handoff_fence.sh` |
| `consensus_inputs::protocol_params` *(N-F-G-A)* | GREEN cardano-cli protocol-parameters JSON → BLUE `ProtocolParameters`; exact decimal/scientific → `Rational` by integer arithmetic. | Use any float path; affect authoritative outputs; introduce nondeterminism. | `ci_check_recovered_ledger_pparams_sourced.sh` |
| `consensus_inputs::canonical` (`require_forge_current_pparams`) *(N-F-G-A)* | GREEN hash-bound current-pparams accessor (`protocol_params_json` OUTSIDE the 15-field fingerprint). | Alter the bundle fingerprint; return pparams without the `blake2b_256` hash bind. | `ci_check_recovered_ledger_pparams_sourced.sh` |
| `seed_consensus_merge` *(N-F-A; eta0 N-F-G-N)* | GREEN bootstrap-time merge → BLUE `SeedEpochConsensusInputs` (carrying recovered eta0). *(Full entry above.)* | Default-fill a provenance gap / zero-eta0; affect authoritative outputs. | `ci_check_consensus_input_provenance.sh`, `ci_check_warmstart_eta0_overlay.sh` |
| `forward_sync::reducer` *(N-Y; recovered_anchor field N-AK)* | GREEN forward-sync lifecycle reducer; the private `AdmitPlan::durable` is the sole `AdvanceTip` emitter (durable-before-tip). **N-AK (CHANGED):** `ForwardSyncState` gains the additive field `recovered_anchor: Option<ChainTip>` (replay-derivable; default `None`) — the threaded recovered-anchor authority the single-producer RollBack handler reads (`run_node_sync` recognises a `RollBackward(anchor)` as an idempotent boundary rewind; DC-NODE-32). It is NOT a servable block, is never persisted as one, and the new `step` arms do not advance the tip from it. | Emit `AdvanceTip` before `StoreBlockBytes` + `AppendWal` (DC-SYNC-01); affect authoritative outputs; **(N-AK) materialize `recovered_anchor` into a servable block, advance the durable tip from it, or treat it as anything but a recovery snapshot boundary point.** | `ci_check_forward_sync_chokepoint_only.sh` |
| `producer::coordinator` *(N-Q)* | Pure coordinator for live producer-mode; hosts `CoordinatorState::kes_period_for_slot`. | Own/store private signing material (CN-PROD-02); affect authoritative outputs. | `ci_check_producer_coordinator_no_secrets.sh` |
| `producer::producer_log` *(N-Q, extended N-W)* | Closed-vocabulary producer evidence log. | Add an open/wildcard event variant; emit `BlockServed` for a block not in the served snapshot (CN-PROD-04). | `ci_check_operator_evidence_manifest_schema.sh` |
| `producer::chain_evolution` *(N-T; N-F-G-J)* | Linear `ChainEvolution` typestate; `prev_hash() -> PrevHash` (cold-start proposes `PrevHash::Genesis`). | Never mints `AcceptedBlock`; advance on authority disagreement; propose an all-zero `Hash32` for the genesis predecessor (CN-WIRE-09). | `ci_check_prevhash_single_wire_authority.sh` |
| `producer::{broadcast_to_served, served_chain_lookups}` *(N-G)* | GREEN served-chain broadcast targets + read-side lookups. | Affect authoritative outputs; serve a block that failed self-accept. | `ci_check_broadcast_to_served_purity.sh` |
| `clock` (`DeterministicClock` + `Clock` + `millis_to_slot` + `checked_millis_to_slot`) *(N-K; N-F-G-A)* | GREEN clock trait + deterministic impl + the pure slot projections. | `DeterministicClock` reads no wall-clock; `checked_millis_to_slot` uses no float/wall-clock and MUST NOT saturate a before-anchor tick. | `ci_check_clock_seam.sh` |
| `orchestrator::{mod, event, state, core}` *(N-K)* | GREEN core reducer + closed-vocabulary event + state. | tokio imports in core; open event vocabulary. | `ci_check_orchestrator_core_purity.sh` |
| `rollback::{cadence, in_memory_cache, chaindb_block_source, persistent_cache, persistent_writer}` *(N-I/N-J/N-K)* | GREEN rollback adapter glue + snapshot cadence + persistent cache/writer. | Parallel snapshot cadence; affect authoritative rollback state. | `ci_check_persistent_writer_no_parallel_cadence.sh`, `ci_check_snapshot_cadence_purity.sh` |
| `receive::{events_to_state, in_memory_chain_write}` *(N-H)* | GREEN receive-side glue. | Affect authoritative receive verdict. | `ci_check_receive_replay_purity.sh` |
| `seed_import` *(N-M-A)* | Single authority converting a cardano-cli JSON UTxO dump into canonical seed entries. | Construct semantic types bypassing canonical decoders. | `ci_check_seed_import_closure.sh`, `ci_check_seed_import_full_preprod_support.sh` |
| `consensus_inputs` *(N-M-C; extended N-F-G-A)* | Operator-extracted `LiveConsensusInputs` importer + the `protocol_params` parser + `require_forge_current_pparams`. | Treat the peer as runtime authority; overstate semantic truth. | `ci_check_live_consensus_inputs_closure.sh`, `ci_check_live_consensus_inputs_fingerprint.sh` |
| `admission::*` (GREEN reducer half + the reused dial/pump) *(N-M-C; reused N-F-G-C; tag-24 unwrap N-F-G-O; rollback preserve N-AI)* | GREEN admission verdict/agreement reducer; the RED `admission::{dial_for_admission, run_admission_wire_pump}` are reused verbatim by the live `--mode node` feed. The feed-side tag-24 unwrap + the `AdmissionPeerEvent::RollBackward` rollback-signal preservation are RED (full notes in the RED `ade_runtime` entry). | Emit RED verdicts; skip reference scripts; treat `lagging` as success (DC-EVIDENCE-01). | `ci_check_admission_runner_closure.sh`, `ci_check_admission_no_red_verdicts.sh`, `ci_check_lagging_is_evidence_only.sh`, `ci_check_admit_replay_equivalence.sh` |

> **Note on `bootstrap` (the `bootstrap_initial_state` authority, N-K; eta0 N-F-G-N; live-follow start N-AK):** it carries a `//! GREEN single bootstrap authority` banner but also hosts the **RED** A3b warm-start sidecar restore (`restore_seed_epoch_consensus_inputs`, a `SnapshotStore` read). It is documented in the RED `ade_runtime` entry below. **PHASE4-N-AK:** `bootstrap_initial_state` now also resolves the live-follow start tip via the NEW private pure fn `resolve_live_follow_start(servable_chaindb_tip, recovered_anchor) -> Option<ChainTip>` (servable ChainDb tip wins; else a non-Origin recovered anchor; else `None`) — a GREEN composition over already-authoritative inputs that sets `BootstrapState.tip`. The `BootstrapInputs.recovered_anchor: Option<ChainTip>` field is the canonical input; the load that READS the record off the store (`recovered_anchor::load_recovered_anchor_point`) is the BLUE half (a single RED `SnapshotStore` read of a BLUE-authoritative record + a BLUE decode + binding check). `resolve_live_follow_start` is module-private (sole caller `bootstrap_initial_state`) to preserve the CN-NODE-01 single-`pub fn` bootstrap authority.

---

### `ade_node` GREEN-by-content sub-trees (carried)

| Sub-tree | Purpose | MUST NOT | CI gate |
|---|---|---|---|
| `forge_intent` *(N-F-F S1)* | GREEN pure tri-state forge-intent classifier. | Observe key contents; carry a third "partial" variant; carry a path/secret byte. | `ci_check_forge_intent_closed.sh` |
| `run_loop_planner` *(N-F-D S1; forge step N-F-E S1; venue policy N-AG)* | GREEN pure lifecycle planner (closed `LoopStep`/`ForgeSlotStatus`/`VenuePolicy`). | `plan_loop_step` reference `SlotNo`; return a non-closed `LoopStep`; decide any authority; add an open/wildcard `VenuePolicy` arm. | `ci_check_loop_planner_closed.sh`, `ci_check_single_producer_loop_continuation.sh` |
| `node_sync::forge_epoch_admission` *(N-F-G-A S4)* | GREEN pure epoch-boundary guard over the closed `ForgeEpochAdmission`, via the BLUE `EraSchedule::locate`. | Fabricate the candidate epoch; run after leadership; drive a nonce promotion (DC-EPOCH-03). | `ci_check_node_forge_single_epoch_fail_closed.sh` |
| `live_log::{sched_event, sched_writer}` *(N-F-G-J S1; forge-base evidence N-AH S4a)* | GREEN closed **emit-only** feed/forge scheduling-event vocabulary `NodeSchedEvent` (incl. the N-AH `ForgeBaseSelected` + `ForgeBaseSource::LocalChaindbTip`) + a hand-rolled byte-deterministic JSONL writer; operational/diagnostic tier ONLY (CN-NODE-04). | Alter the relay loop's control flow; add an open/wildcard variant; be READ by the planner / any authority path; carry a `ForgeBaseSource` other than `LocalChaindbTip` (DC-NODE-20/21). | `ci_check_node_sched_events_emit_only.sh`, `ci_check_live_transcript_forge_base.sh` |
| `ba02_evidence` *(N-F-C, library; consumed by `ba02_pass`)* | GREEN BA-02 evidence correlator (`correlate` is the sole `Ba02Manifest` ctor). | Treat a weaker signal as acceptance; recompute the forged hash; forge/admit/persist. | `ci_check_ba02_evidence_closed.sh`, `ci_check_ba02_evidence_manifest_schema.sh` |
| `convergence_evidence` *(N-AJ)* | GREEN closed `AgreementVerdict`-vocabulary convergence-evidence sink (`ConvergenceEvidenceSink`). *(Full entry above.)* | Affect authoritative outputs; halt authority on a write failure; add an open variant; emit on non-Diverged; change bytes when the path is absent. | `ci_check_convergence_evidence_vocabulary_closed.sh`, `ci_check_convergence_evidence_emit_only.sh` |
| `rehearsal_evidence` *(N-F-G-D S2)* | GREEN non-promotable private-testnet rehearsal-evidence envelope. | Construct from anything but a correlate-produced payload; live under / be referenced by the bounty home; flip any RO-LIVE rule. | `ci_check_rehearsal_manifest_schema.sh` |
| `admission_log` *(N-M-B)* | GREEN admission-mode JSONL event vocabulary + writer (closed enum). | Add open/wildcard event variant. | `ci_check_admission_log_vocabulary_closed.sh` |
| `live_log` *(N-L-LIVE)* | GREEN closed JSONL vocabulary for the wire-only live pass. | Add open event variant; overstate semantic truth. | `ci_check_wire_only_event_vocabulary_closed.sh` |

> **Note on `node_sync` / `node_lifecycle` color (carried + N-AF…N-AK):** `node_sync` + `node_lifecycle` are **RED** (they drive the apply path / own the persistent-store reads + the live-feed forge wrap + the serve sibling). Their GREEN-by-function decisions (`forge_epoch_admission`, `forge_header_position`, `may_cold_start_forge`, `forge_followed_tip_admission`, `single_producer_forge_decision`, `venue_policy`, `warm_start_forge_mode`, `forge_mode_after_admit`, `classify_receive`, `resolve_disposition`, `pending_reselection_forge_refusal`, `durable_tip_matches`) carry GREEN-pure semantics within the RED host modules. **PHASE4-N-AK:** the single-producer RollBack handler in `run_node_sync` (the recovered-anchor idempotent-no-op vs fail-closed decision) is a BLUE-authority decision expressed as RED loop wiring — it reads the threaded `ForwardSyncState.recovered_anchor` (the SINGLE anchor authority, never re-read from the store inside the loop) and the `node_lifecycle` ON-arm sets `fwd.recovered_anchor = state.tip` + the `wire_pump_start_point` helper resolves the FindIntersect start from the same `recovered_tip`. `ade_testkit::consensus::genesis_pinning` is GREEN `#[cfg(test)]` test infrastructure.

---
### `ade_testkit` *(GREEN — deterministic test/corpus infrastructure; dev-dep / `#[cfg(test)]`-scoped)*

| Attribute | Value |
|-----------|-------|
| **Purpose** | GREEN deterministic TEST / CORPUS infrastructure for the workspace — the oracle/differential harness, the validity & tx-validity corpora (incl. the Conway `ConwayValidityCorpus` loader), the consensus & governance replay corpora, the producer reference vectors, and the snapshot/genesis/ledger diff loaders. The crate `lib.rs` opens the `// Core Contract:` banner (deterministic; no wall-clock / rand / HashMap / float) and re-exports seven sub-trees: `consensus` (the `corpus`/`stream_replay` HFC + nonce/leader/fork-choice/rollback corpus-path drivers + the `#[cfg(test)]` `genesis_pinning` harness + the `ledger_view_stub`), `governance`, `harness` (the oracle/diff toolkit — per-era block-field decoders, manifest/corpus/provenance parsers + validators, differential reports, snapshot/shelley/genesis loaders, transcript, address-extractor, era-mapping), `mempool` (ingress-replay over the adversarial corpus), `producer` (VRF/KES reference vectors + the cross-impl adapter + fixtures + replay), `tx_validity`, and `validity`. It exercises the REAL BLUE/RED authorities and asserts against committed reference corpora — the home of `replay_cmd = cargo test -p ade_testkit`. |
| **Creates** | GREEN test/corpus value types — NONE canonical-counted: `ConwayValidityCorpus` + `CorpusRatio` / `CorpusPool` / `CorpusLoadError`; `BlockReplay` / `TxReplay` + `BlockRejectClass` / `TxRejectClass`; the adversarial mutation enums `Mutation` / `WitnessMutation` / `SyntheticMutation` + `ExtractedTx` / `ExtractError`; `OracleManifest` / `RegressionCorpus` + validators + `HarnessError`; `DifferentialReport`, `Transcript` / `TranscriptMessage` / `Direction`, `ShelleyOracleUtxo` / `SnapshotHeader`, `EpochStakeFixture` / `PoolFixture`, `CorpusEntry`; `VrfReferenceVector` / `KesReferenceVector`. All GREEN. |
| **Interprets** | Committed reference corpora + oracle dumps, for the test harness only: per-era raw block CBOR; cardano-node LedgerDB snapshot tarballs; Shelley genesis UTxO dumps; oracle/regression-corpus TOML; corpus tx/block bytes. Evidence inputs, never runtime authority. |
| **MUST NOT** | (1) **Be a production runtime dependency.** It is GREEN dev-dep / `#[cfg(test)]`-scoped — an INBOUND `[dev-dependencies]` of the BLUE crates (`ade_core`, `ade_ledger`) and the RED crates (`ade_network`, `ade_runtime`, `ade_node`), NEVER in any production crate's plain `[dependencies]` (its sole plain-`[dependencies]` edge is the RED-leaf `ade_core_interop`, a non-shipping live-interop / test driver depended on by nothing). It MUST NOT be promoted onto a hashed/persisted/consensus path. (2) Affect authoritative outputs or introduce nondeterminism. (3) Construct semantic types bypassing the canonical decoders. (4) Compare an Ade-internal-state fingerprint to a Haskell-serialized-state hash (DC-COMPAT-01; enforced for `consensus::genesis_pinning`). (5) Commit secret key material in any corpus/fixture dir. (6) Overstate semantic truth. |
| **Inbound deps** | `ade_core` (dev), `ade_ledger` (dev), `ade_network` (dev), `ade_runtime` (dev), `ade_node` (dev), `ade_core_interop` (plain — the RED-leaf interop/test driver). |
| **Outbound deps** | `ade_core`, `ade_ledger`, `ade_plutus`, `ade_crypto`, `ade_codec`, `ade_types`, `ade_runtime` (it drives the real BLUE/RED authorities — incl. `ade_runtime::{bootstrap, chaindb}` for the `genesis_pinning` warm-start), `cardano-crypto` (`["vrf-draft03", "kes-sum", "dsign"]`), `blake2`, `ed25519-dalek`, `serde`, `serde_json`, `toml`, `flate2`, `tar`. |
| **Entry points** | `ade_testkit::validity::{corpus::ConwayValidityCorpus, replay::*, adversarial::Mutation}`, `ade_testkit::tx_validity::{replay_tx_validity, extract::*, adversarial::*, valid_synthetic::*}`, `ade_testkit::consensus::{corpus::*, stream_replay::*}`, `ade_testkit::governance::*`, `ade_testkit::producer::{reference_vectors::*, cross_impl_adapter::*}`, `ade_testkit::harness::{oracle_manifest::*, regression_corpus::*, snapshot_loader::*, shelley_loader::*, genesis_loader::*, transcript::*, adapters::*}`, `ade_testkit::mempool::ingress_replay::*`. |
| **Key modules** | `consensus/` (`corpus.rs`, `stream_replay.rs`, `genesis_pinning.rs` *(`#[cfg(test)]`)*, `ledger_view_stub.rs`), `governance/`, `harness/` (`adapters/`, `oracle_manifest.rs`, `regression_corpus.rs`, `provenance.rs`, `*_diff.rs`, `snapshot_loader.rs`, `shelley_loader.rs`, `genesis_loader.rs`, `transcript.rs`, `address_extractor.rs`, `era_mapping.rs`), `mempool/ingress_replay.rs`, `producer/`, `tx_validity/`, `validity/`. |

> **GREEN classification (load-bearing):** `ade_testkit` is GREEN test/corpus infrastructure, **never promotable onto a production runtime path**. Its `consensus::genesis_pinning` sub-module is the `#[cfg(test)]` genesis-consistency pinning harness that drives the REAL `ade_runtime::bootstrap::bootstrap_initial_state` warm-start and pins Ade's recovered eta0 / ASC / stake / VRF-keyhash / `praos_vrf_input` against the genesis-derived reference.

---

## RED Modules — Imperative Shell

> I/O, network, storage, clocks, retries. May depend on BLUE/GREEN. Must not modify core state directly or construct semantic types unsafely.

### `ade_runtime`

| Attribute | Value |
|-----------|-------|
| **Purpose** | The imperative shell — moves bytes, owns sockets/files/clocks/keys, drives tokio tasks. Hosts: producer-mode key custody + shell composition; the N2N network drivers; the node orchestrator runners; the receive/rollback/admission shells (incl. the `admission::{dial_for_admission, run_admission_wire_pump}` reused by the live `--mode node` feed); the seed-import/WAL/consensus-inputs importers; ChainDB + recovery shells; the N-Y bootstrap/forward-sync surfaces; the N-Z `mithril_bootstrap` composition; the clock seam; the N-F-A seed-epoch consensus-input recovered-state shell; the durable-chain serve projection (`network::served_chain_projection` — `ChainDbServedSource`, read-only over the durable ChainDb, bounded per-request at `MAX_SERVE_RANGE_BLOCKS = 256` via the hash-free `range_bytes_capped` / `last_block_bytes` primitives; the FindIntersect-only forge-parent `intersect` clause); the N-AE.F `pump_block` hash-exact already-have idempotency gate; the N-AI `admission::wire_pump` `AdmissionPeerEvent::RollBackward` rollback-signal preservation; and (N-AC) the RED producer `kes_sign_header_advancing` evolve-then-sign method. **PHASE4-N-AK (NEW + CHANGED, all RED except the BLUE-authoritative record decode):** NEW BLUE-by-content fn module `recovered_anchor` — `load_recovered_anchor_point(snapshot_store, expected_anchor_fp) -> Result<ChainTip, BootstrapError>` loads + fail-closed verifies the persisted recovered anchor-point record (a single `SnapshotStore::get_recovered_anchor_point` read = RED I/O of a BLUE-authoritative record; the decode via the sole `ade_ledger::recovered_anchor_point` codec + the `anchor_fp` binding check are BLUE). Kept OUT of `bootstrap.rs` so that module stays the single-`pub fn` bootstrap authority (CN-NODE-01); mirrors `restore_seed_epoch_consensus_inputs`; only called on the recover path (a missing / malformed / fingerprint-mismatched record is a deterministic fail-closed halt, never a silent Origin fallback). CHANGED `bootstrap.rs` — the NEW private pure tip resolver `resolve_live_follow_start` + the `BootstrapInputs.recovered_anchor: Option<ChainTip>` canonical input field + the 3 NEW `BootstrapError::{RecoveredAnchorPointMissing { anchor_fp }, RecoveredAnchorPointDecode(RecoveredAnchorPointError), RecoveredAnchorPointBindingMismatch { expected_anchor_fp, actual_anchor_fp }}` variants; `bootstrap_initial_state` sets `BootstrapState.tip = resolve_live_follow_start(tip, inputs.recovered_anchor)` (the materialization `target` + `ChainDb::tip()` are UNTOUCHED; no synthetic block is ever made). CHANGED `chaindb` — the RED `SnapshotStore` trait gains `put_recovered_anchor_point(&self, anchor_fp, bytes)` + `get_recovered_anchor_point(&self, anchor_fp) -> Option<Vec<u8>>` (a DISJOINT anchor-fp-keyed namespace — impls in `in_memory.rs` (a `BTreeMap<Hash32, Vec<u8>>`) + `persistent.rs` (the NEW redb table `recovered_anchor_point_by_anchor_fp`; idempotent same-bytes put, `InvalidOperation` on conflicting bytes — mirrors `put_seed_epoch_consensus_inputs`; a read on a pre-AK store returns `Ok(None)` via `TableDoesNotExist`, redb `SCHEMA_VERSION` stays at 3 — additive table, no bump). CHANGED `seed_epoch_lineage.rs` — `persist_seed_epoch_consensus_inputs` now ALSO writes the anchor-point record (`put_recovered_anchor_point(anchor_fp, encode_recovered_anchor_point(RecoveredAnchorPoint { anchor_fp, slot = anchor.seed_point.slot, block_hash = anchor.seed_point.block_hash }))`) at seed/recover, BEFORE the WAL provenance commit point (a crash between the puts + the WAL append leaves the store recovered as "not imported" — the WAL provenance is the discovery+commit gate; a store carrying the sidecar+WAL but missing this record fails closed at the warm-start anchor-point load). |
| **Creates (RED-only)** | `KesSecret`, `VrfSigningKey`, `ColdSigningKey` custody wrappers; `KeyLoadError`; `MuxTransportHandle` consumers; `OutboundCommand` (closed); `PerPeerOutbound`; `DispatchError` (closed 4-variant); `ServerPeerStates`; the GREEN-by-content `ServedBlockEvidence`; `ServedChainHandle`; `GenesisAnchor`; `ProducerShell` (+ `ShellInitError`); `AdmissionPeerEvent` (closed — incl. the N-AI `RollBackward {peer, point, tip}` variant). Clock: `SystemClock`, `DeterministicClock`, closed `SlotAlignmentError`. consensus_inputs: `LiveConsensusInputsCanonical`, closed `ProtocolParamsParseError`, `ForgeCurrentPParamsError`. N-Y/N-Z/N-F-A: `forward_sync::pump::*`, `mithril_import::*`, `recovery::restart::*`, `mithril_bootstrap::*`, `genesis_bootstrap::*`. `bootstrap::{SeedEpochConsensusSource (closed), BootstrapState, BootstrapInputs, BootstrapError}` — **PHASE4-N-AK: `BootstrapInputs` gains `recovered_anchor: Option<ChainTip>`; `BootstrapError` gains 3 closed variants `RecoveredAnchorPointMissing` / `RecoveredAnchorPointDecode` / `RecoveredAnchorPointBindingMismatch`** (RED orchestration error variants over the BLUE `RecoveredAnchorPointError`, NOT canonical-counted). The serve plumbing types `ChainDbServedSource<'a>`, the closed `ServedChainSource<'a> { Snapshot \| DurableChainDb }`, the `CappedSlotRange` value type, the closed `ServeRangeOutcome` + `MAX_SERVE_RANGE_BLOCKS = 256` (all RED — serve plumbing, never semantic/canonical types). **PHASE4-N-AK (RED store/orchestration plumbing — NOT canonical-counted):** the two new `SnapshotStore` trait methods `put_recovered_anchor_point` / `get_recovered_anchor_point` (methods, not types) + the `in_memory` `recovered_anchor_points: BTreeMap<Hash32, Vec<u8>>` field + the `persistent` redb table `recovered_anchor_point_by_anchor_fp`. No new `WalEntry` variant, no new `NodeBlockSource` variant. |
| **Interprets** | Canonicalizes peer bytes/files for the BLUE core: the cold/VRF/KES loaders; `genesis_parser::parse_shelley_genesis`; `opcert_envelope::parse_opcert_envelope`; `seed_import`; `admission::{dial_for_admission, run_admission_wire_pump}` (the N2N dial + chain-sync/block-fetch pump producing `AdmissionPeerEvent`, now incl. the preserved `RollBackward` point). `admission::wire_pump` strips the peer's BlockFetch payload tag-24 envelope via `ade_codec::unwrap_tag24` before the BLUE decode (CN-WIRE-12). `bootstrap::restore_seed_epoch_consensus_inputs` reads the anchor-fp-keyed sidecar (the v2 record incl. `epoch_nonce`) and verifies it fail-closed. **PHASE4-N-AK:** `recovered_anchor::load_recovered_anchor_point` reads the anchor-fp-keyed recovered-anchor-point record (`get_recovered_anchor_point`) and verifies it fail-closed via the SOLE BLUE `decode_recovered_anchor_point` codec + the `anchor_fp` binding check, projecting it into a `ChainTip { slot, hash = block_hash }`; the resolver `resolve_live_follow_start` interprets that recovered anchor + the servable `ChainDb::tip()` into the live-follow start tip. |
| **MUST NOT** | (1) Modify BLUE state directly or construct semantic types from raw bytes. (2) Bypass canonical validation. (3) `producer::signing` (DC-CRYPTO-03/05) — RED-confined custody. **(3b) `producer::producer_shell::kes_sign_header_advancing` (DC-CRYPTO-10):** the forge's single real KES sign MUST evolve the operator KES key to the requested period BEFORE signing via the EXISTING deterministic Sum6KES update (period verbatim), fail-closed `EvolutionBackwards` / `EvolutionExhausted`; signing stays RED (`ci_check_kes_evolution_before_sign.sh`). (4)–(7) `producer::keys` / `coordinator` / `outbound_command` (CN-OUTBOUND-RELAY-01) / per-peer outbound map fences. (7b) `network::serve_dispatch` (DC-NODE-07) — single `dispatch_server_frame_event_to_outbound`. (7c) `network::n2n_listener` magic-aware serve table (DC-NODE-07). **(7d) `network::served_chain_projection::ChainDbServedSource` (DC-NODE-13 / DC-NODE-14 / DC-SERVEMEM-01):** READ-ONLY — advances no tip, admits nothing, mutates no ChainDb; serves `StoredBlock.bytes` verbatim; reuses the single `block_header_bytes` + `decode_block`; bounded per-request via the hash-free `range_bytes_capped` / `last_block_bytes` (`MAX_SERVE_RANGE_BLOCKS = 256` FIXED); the FindIntersect-only forge-parent `intersect` clause serves NO bytes for the projected point (`ci_check_served_chain_projection.sh` + `ci_check_serve_range_bounded.sh` + `ci_check_recovered_anchor_intersectable.sh`). **(9) `forward_sync::pump` (DC-SYNC-01 / DC-NODE-16):** refuse to advance the tip before `StoreBlockBytes` + `AppendWal`; the already-have skip is HASH-keyed before the BLUE reducer (`ci_check_receive_idempotency.sh`). (10)–(13) `mithril_import` / `genesis_bootstrap` / `recovery::restart` / `mithril_bootstrap` fences. **(14) `bootstrap::restore_seed_epoch_consensus_inputs` (DC-CINPUT-01; T-REC-04 / DC-CINPUT-03):** fail closed on a missing sidecar / hash mismatch / non-canonical decode / binding mismatch; the recovered eta0 MUST be read from the v2 sidecar (`ci_check_warmstart_eta0_overlay.sh`). (15)–(16) `seed_consensus_provenance` / `chaindb` seed-sidecar surface (CN-CINPUT-02) — anchor-fp-keyed namespace disjoint from the slot-keyed snapshot space. **(16b) `chaindb` recovered-anchor-point surface (DC-NODE-31, PHASE4-N-AK):** `put_recovered_anchor_point` / `get_recovered_anchor_point` MUST be a DISJOINT anchor-fp-keyed namespace — it MUST NOT collide with the slot-keyed snapshot space (`put_snapshot`) or the seed-epoch consensus-inputs sidecar space (`put_seed_epoch_consensus_inputs`); the stored bytes MUST be the canonical `ade_ledger::recovered_anchor_point` encoding (the impls MUST NOT re-encode or hand-roll the record); the put MUST be idempotent on identical bytes and MUST fail (`InvalidOperation`) on conflicting bytes for the same `anchor_fp` (mirrors `put_snapshot`); a pre-AK store / a torn write before the WAL commit MUST surface as `Ok(None)` (the warm-start load — not the store — fails closed on absence). The redb table is ADDITIVE (`recovered_anchor_point_by_anchor_fp`) and MUST NOT require a `SCHEMA_VERSION` bump (a pre-AK store reads it as `TableDoesNotExist → Ok(None)`). **(16c) `recovered_anchor::load_recovered_anchor_point` (DC-NODE-31, PHASE4-N-AK):** the load is a single `SnapshotStore::get_recovered_anchor_point` read (RED I/O) of a BLUE-authoritative record — it MUST decode ONLY via the sole `ade_ledger::recovered_anchor_point::decode_recovered_anchor_point` codec (no second/hand-rolled decode), MUST verify the decoded `anchor_fp` binds to the recovered `anchor_fp` it was loaded for (`RecoveredAnchorPointBindingMismatch` else), and MUST fail closed on a missing record (`RecoveredAnchorPointMissing`) / a decode failure (`RecoveredAnchorPointDecode`) / a binding mismatch — NEVER a silent Origin fallback on the recover path (where the store is definitively non-Origin). It MUST stay OUT of `bootstrap.rs` (so `bootstrap_initial_state` remains the single `pub fn` bootstrap authority — CN-NODE-01, `ci_check_bootstrap_closure.sh`); it advances no tip, writes nothing, and mutates no ChainDb/ledger. **(16d) `bootstrap::resolve_live_follow_start` (DC-NODE-31, PHASE4-N-AK):** the live-follow start-tip resolution MUST be a pure, total, module-private fn whose sole caller is `bootstrap_initial_state` — resolution order is servable `ChainDb::tip()` wins → else the persisted recovered anchor IFF non-Origin (non-zero `block_hash`) → else `None`; it MUST treat a zero/null-hash recovered anchor as Origin (rule 3 — a genesis seed point carries no peer-intersectable block, so it is NOT a usable start); it MUST NOT change `ChainDb::tip()` semantics, MUST NOT synthesize a servable block, and MUST set ONLY `BootstrapState.tip` (the FindIntersect start surface) from already-authoritative inputs (the materialization `target` + `ChainDb::tip()` are untouched). The AI-S4a `RollBackward(Origin)` fail-close is UNCHANGED; the wire-pump consumer (`spawn_live_wire_pump_source` / `wire_pump_start_point`) is an UNCHANGED consumer of the resolved tip. Test-enforced (`DC-NODE-31.ci_script = ""`). (17)–(22) `producer::keys` / `admission::{dial,pump}` / feed tag-24 unwrap (CN-WIRE-12) / `clock::checked_millis_to_slot` / `consensus_inputs` / `admission::wire_pump` rollback signal preservation (DC-PUMP-01) fences. |
| **Inbound deps** | `ade_node`, `ade_core_interop`, `ade_testkit` (dev/integration). |
| **Outbound deps** | `ade_types`, `ade_core`, `ade_crypto`, `ade_codec` (incl. `unwrap_tag24` + the canonical primitives the N-AK `recovered_anchor_point` codec uses transitively via `ade_ledger`), `ade_ledger` (incl. `recovered_anchor_point::{RecoveredAnchorPoint, decode_recovered_anchor_point, encode_recovered_anchor_point}`, N-AK), `ade_network`, `redb`, `serde`, `serde_json`, `bech32`, `base58`, `cardano-crypto` (`["vrf-draft03", "dsign"]`), `ed25519-dalek`, `tokio`. |
| **Entry points** | `ade_runtime::producer::{producer_shell::* (incl. `kes_sign_header_advancing`), keys::*, coordinator::*, served_chain_handle::*, genesis_parser::*, opcert_envelope::*, self_accepted_handoff::*}`, `ade_runtime::admission::{dial_for_admission, run_admission_wire_pump, AdmissionPeerEvent}`, `ade_runtime::network::{n2n_listener::*, n2n_dialer::*, mux_pump::*, n2n_server::*, outbound_command::*, serve_dispatch::{ServedChainSource, *}, served_chain_projection::{ChainDbServedSource, ServeRangeOutcome, MAX_SERVE_RANGE_BLOCKS}}`, `ade_runtime::orchestrator::*`, `ade_runtime::bootstrap::{bootstrap_initial_state, BootstrapInputs, BootstrapState, SeedEpochConsensusSource, BootstrapError}` (**PHASE4-N-AK: `BootstrapInputs.recovered_anchor` + the 3 `RecoveredAnchorPoint*` error variants; `resolve_live_follow_start` is module-private**), **`ade_runtime::recovered_anchor::load_recovered_anchor_point`** (NEW, PHASE4-N-AK), `ade_runtime::seed_consensus_merge::*`, `ade_runtime::consensus_inputs::*`, `ade_runtime::{forward_sync, mithril_import, genesis_bootstrap, mithril_bootstrap, recovery::restart, chaindb, clock, seed_import, wal, bootstrap_anchor, seed_epoch_lineage}::*` (incl. the PHASE4-N-AA bounded serve primitives `chaindb::ChainDb::{range_bytes_capped, last_block_bytes}` + the **PHASE4-N-AK `chaindb::SnapshotStore::{put_recovered_anchor_point, get_recovered_anchor_point}`** + the `seed_epoch_lineage::persist_seed_epoch_consensus_inputs` anchor-point write). |
| **Key modules** | `producer/` (incl. `producer_shell.rs`), `network/` (incl. `served_chain_projection.rs`, `serve_dispatch.rs`, `n2n_listener.rs`), `orchestrator/`, `admission/` (incl. `wire_pump.rs` — the feed tag-24 unwrap + the N-AI `AdmissionPeerEvent::RollBackward`; `dial_for_admission` + `run_admission_wire_pump`), `receive/`, `rollback/`, `seed_import/`, `consensus_inputs/`, `wal/`, **`chaindb/`** (redb schema v3; the bounded serve primitives; **PHASE4-N-AK: the `put/get_recovered_anchor_point` trait methods in `mod.rs`, the `recovered_anchor_points` map in `in_memory.rs`, the `recovered_anchor_point_by_anchor_fp` redb table in `persistent.rs`**), `forward_sync/` (incl. `pump.rs` — the DC-NODE-16 idempotency gate; `reducer.rs` — the GREEN `ForwardSyncState.recovered_anchor` field, N-AK), `mithril_import/`, `seed_consensus_merge.rs`, `seed_consensus_provenance.rs`, **`seed_epoch_lineage.rs`** (PHASE4-N-AK: the anchor-point write at seed/recover), `mithril_bootstrap.rs`, `genesis_bootstrap.rs`, `recovery/`, **`bootstrap.rs`** (GREEN composition + RED A3b restore; **PHASE4-N-AK: `resolve_live_follow_start` + `BootstrapInputs.recovered_anchor` + the 3 `RecoveredAnchorPoint*` `BootstrapError` variants**), **`recovered_anchor.rs`** (NEW, PHASE4-N-AK: `load_recovered_anchor_point` — the BLUE load + fail-closed verify, kept OUT of `bootstrap.rs`), `bootstrap_anchor.rs`, `clock.rs`. |

> **RED sub-entries carried** (unchanged in shape across the N-AK span): `ade_runtime::network::serve_dispatch` *(RED — N-F-G-H; the single shared `dispatch_server_frame_event_to_outbound`)* and `ade_runtime::network::served_chain_projection` *(RED — N-U S3 / DC-NODE-13; the read-only durable-ChainDb serve projection bounded by N-AA `range_bytes_capped` / `MAX_SERVE_RANGE_BLOCKS = 256` + the N-AE FindIntersect-only forge-parent `intersect` clause that serves NO bytes)*. The PHASE4-N-AK recovered-anchor store surface is in `ade_runtime::chaindb` (the `SnapshotStore::{put,get}_recovered_anchor_point` anchor-fp-keyed namespace) + the load+verify is in `ade_runtime::recovered_anchor` — neither is a serve-path change.

---

### `ade_node`

| Attribute | Value |
|-----------|-------|
| **Purpose** | The node binary + library entry. Owns argv parsing, the node lifecycle, and the mode drivers dispatched by `main()`: `--mode wire_only` (default), `--mode admission`, `--mode key_gen_kes`, `--mode produce`, and (N-F-C) `--mode node` (`node_lifecycle`). `node_lifecycle` is the single `PHASE4-N-F-C-LIFECYCLE-OWNER`. Carries the full N-F-C…N-AJ `--mode node` live surface (bootstrap/recovery, forge-intent flip, real forge constants + fail-closed boundaries, the self-accept→serve handoff, the live WirePump feed, the bounded-memory + bounded-serve caps, the rehearsal harness, the serve-to-peer sibling, the genesis-successor cold-start forge, the forged-block durable admit via `pump_block`, the forge-on-followed-tip gate, the receive idempotency, the local-durable single-producer forge base + warm-start re-entry, the live fork-choice rollback-follow wiring (Participant), and the N-AJ convergence-evidence emission). **PHASE4-N-AK (recovered-anchor live-follow start + recovered-anchor rollback no-op, RED/GREEN; the +2 new BLUE types live in `ade_ledger`, not here):** **AK-S1 (DC-NODE-31)** `node_lifecycle::warm_start_recovery` now LOADS the recovered anchor-point record once it has discovered the recovered `anchor_fp` (`load_recovered_anchor_point(chaindb, &anchor_fp)`, fail-closed on missing / malformed / fingerprint-mismatch — never a silent Origin fallback), threads the returned `ChainTip` as `BootstrapInputs.recovered_anchor` into `bootstrap_initial_state` (which resolves `BootstrapState.tip` via `resolve_live_follow_start`), and the new private helper `wire_pump_start_point(recovered_tip) -> ade_network::codec::chain_sync::Point` resolves the live WirePump FindIntersect start from the SAME recovered tip (`Block { slot, hash }` for a real recovered tip, `Origin` only when truly Origin) — so a bare-anchor recovery FindIntersects at the anchor, not Origin (which the relay answers with `RollBackward(Origin)`, tripping the AI-S4a Origin fail-close). **AK-S2 (DC-NODE-32)** the forge-ON arm of `run_relay_loop_with_sched` sets `fwd.recovered_anchor = state.tip` (threading the already-loaded `BootstrapState.tip` into the `ForwardSyncState` — the SINGLE anchor authority, the SAME value the wire pump FindIntersects at, never re-read from the store inside the loop), and `node_sync::run_node_sync`'s RollBack handler now accepts a peer `RollBackward` binding EXACTLY (slot AND hash) to the threaded recovered anchor as an IDEMPOTENT NO-OP boundary rewind (`continue` — no `commit_rollback`, no `WalEntry::RollBack`, no ChainDb / ledger / chain_dep / cursor mutation; the anchor is a recovery snapshot, NOT a stored servable block); EVERY other rollback — `RollBackward(Origin)`, a non-anchor point, a slot-only or hash-only match, or a `RollBack` with no recovered anchor — still FAILS CLOSED (`NodeSyncError::UnexpectedRollback`). The first forward block after the anchor admits through the EXISTING sole `pump_block` path (its `prev_hash` binds the recovered `chain_dep`) — AK-S2 adds NO forward-link code. The Participant rollback-follow path (`run_participant_sync` @ `node_lifecycle`) is SEPARATE and UNTOUCHED. **DC-CONS-03 untouched.** **NO RO-LIVE flip** (`RO-LIVE-01` operator-gated — the CE-AK-3 live transcript re-recovered → FindIntersected at the persisted anchor → idempotent-no-op'd the relay's `RollBackward(anchor)` → caught up to `forge_base_block_no=13` (== the frozen relay tip); it restores the recover→follow path to resume N-AJ / CE-AI-6, it does NOT claim full ChainSel convergence). |
| **Creates (RED-only)** | `Cli` (incl. `peer_addrs`, `network_magic`), `CliError`, `ProduceCli`, `KeyGenKesCli`, `Mode` (closed: `WireOnly`/`Admission`/`KeyGenKes`/`Produce`/`Node`), `NodeStartupInputs`, `NodeShutdownEvidence`, `NodeRunError`, exit-code constants. `node_lifecycle` (RED): `NodeStart` (closed), `NodeLifecycleError` (closed), `ForgeActivation<'a>` (incl. `last_forge_refused` + `pending_reselection`), `run_relay_loop` / `run_relay_loop_with_sched`, `spawn_live_wire_pump_source`, **`wire_pump_start_point`** (NEW, PHASE4-N-AK — the FindIntersect start-point helper), `run_node_serve_task`, `bind_serve_listener`, closed `ServeStartError`, the GREEN-by-function `may_cold_start_forge`, the apply-driver `apply_chain_event` / `run_participant_sync` / `declare_participant_venue` + `AppliedTip` / `ApplyError` (N-AI). `node_sync` (RED): closed 2-variant `NodeBlockSource { WirePump, InMemory }`, the closed `NodeSyncItem { Block \| RollBack(Point) }` (N-AI), `NodeSyncError { Pump \| Capture \| UnexpectedRollback \| RollbackPointSlotMismatch }` (N-AI), `NodeForgeError`, the GREEN-by-function classifiers (`forge_epoch_admission`, `forge_header_position`, `forge_followed_tip_admission`, `single_producer_forge_decision`, `venue_policy`, `warm_start_forge_mode`, `forge_mode_after_admit`, `classify_receive`, `resolve_disposition`, `pending_reselection_forge_refusal`, `durable_tip_matches`), the closed `ForgeMode` (3-state) / `VenueRole` / `ForgeFollowedTipAdmission` / `NotCaughtUpReason` / `ReceiveClass` / `ReceiveDisposition` / `CandidateSummary` / `ForgeRefused` / `NodeForgeOutcome`, `FollowedPeerTipSignal`, the `MAX_WIRE_PUMP_LOOKAHEAD = 256` cap. GREEN sub-trees `forge_intent` / `run_loop_planner` / `live_log::{sched_event,sched_writer}` / `ba02_evidence` / `convergence_evidence` / `rehearsal_evidence` (entries above). RED `operator_forge` / `ba02_pass` / `rehearsal_pass`. **PHASE4-N-AK (RED/GREEN node plumbing — NOT canonical-counted):** in `node_lifecycle` — the `wire_pump_start_point` helper + the `warm_start_recovery` anchor-point load + the ON-arm `fwd.recovered_anchor = state.tip` thread; in `node_sync` — the `run_node_sync` RollBack handler's recovered-anchor idempotent-no-op-vs-fail-closed predicate (it reuses the existing `NodeSyncItem::RollBack` + `Point` + `NodeSyncError::UnexpectedRollback` — adds NO new type). The threaded `ForwardSyncState.recovered_anchor` field lives in `ade_runtime::forward_sync::reducer` (GREEN); the +2 new BLUE canonical types (`RecoveredAnchorPoint` / `RecoveredAnchorPointError`) live in `ade_ledger`. NO new `WalEntry` / `NodeBlockSource` variant. |
| **Interprets** | argv (closed mode set); operator key/genesis/opcert file paths; evidence-manifest TOML schema; the admission runner interprets a peer's tag-24-wrapped BlockFetch `MsgBlock` via `ade_codec::unwrap_tag24`; the feed's header-validation interprets the recovered consensus surface's leader-threshold view (Steps 5 + 7); the successor forge interprets the evolved admitted spine state; `warm_start_recovery` interprets the recovered seed-epoch sidecar to reconstruct the recovery `era_schedule` + `ledger_view`; the `--mode node` served view reads the durable ChainDb via `ChainDbServedSource`; the live Participant receive path interprets each peer item as a `NodeSyncItem` and routes a Participant `NeedsForkChoice` into the existing `chain_selector`. **PHASE4-N-AK:** `warm_start_recovery` interprets the persisted recovered anchor-point record (`load_recovered_anchor_point` → the BLUE `decode_recovered_anchor_point` + binding check) into the `recovered_anchor: ChainTip` it threads as the canonical live-follow start input; `run_node_sync` interprets each `NodeSyncItem::RollBack(point)` against the threaded `ForwardSyncState.recovered_anchor` — a `Point::Block { slot, hash }` matching the anchor on BOTH slot AND hash is an idempotent no-op, every other point (incl. `Origin`) fails closed. |
| **MUST NOT** | All carry-forward `--mode node` prohibitions (closed mode set, single lifecycle owner via `bootstrap_initial_state`, durable-tip ONLY via `run_node_sync → pump_block`, `run_relay_loop` containment byte-unchanged, content-blind planner, the self-accept→serve handoff fence, the live-feed reuse + lookahead bound, the path-fidelity + rehearsal-evidence fences, the serve-to-peer DC-NODE-07 fence, the genesis-successor cold-start + PrevHash position fence, the serve-as-projection DC-NODE-13 fence, the forge-on-followed-tip DC-NODE-15 / DC-CONS-24 fence, the receive-idempotency DC-NODE-16 fence, the local-durable forge-base DC-NODE-20 / cert-evidence-only DC-NODE-21 / warm-start-re-entry DC-NODE-22 fences, the live fork-choice DC-NODE-23…29 fences). **`run_node_serve_task` lifetime (DC-NODE-09 — PRESERVED):** the serve sibling is spawned OUTSIDE `run_relay_loop` and stays up past feed-end (`ci_check_node_serve_lifetime.sh`). **(NEW) recovered-anchor live-follow start (DC-NODE-31, PHASE4-N-AK):** `warm_start_recovery` MUST load the persisted recovered anchor-point record via the single `load_recovered_anchor_point` authority (fail-closed on missing / malformed / fingerprint-mismatch — a non-Origin recovered store with no anchor-point record HALTS, never a silent Origin fallback), MUST thread the loaded `ChainTip` as `BootstrapInputs.recovered_anchor` (so `bootstrap_initial_state` resolves the live-follow start via `resolve_live_follow_start`), and the WirePump FindIntersect start MUST be derived from the SAME recovered tip via `wire_pump_start_point` (a bare-anchor recovery FindIntersects at the anchor, NEVER blind Origin — which the relay answers with `RollBackward(Origin)`, tripping the AI-S4a fail-close); the persisted anchor point is the durable restart authority — it MUST NOT be re-supplied from the CLI (the CLI seed-point is first-run input only); it MUST NOT change `ChainDb::tip()` semantics or synthesize a servable block. Test-enforced (`DC-NODE-31.ci_script = ""`). **(NEW) recovered-anchor rollback boundary (DC-NODE-32, PHASE4-N-AK):** on the single-producer follow path (`run_node_sync`), a peer `RollBackward` whose target binds EXACTLY (slot AND hash) to the persisted recovered anchor (`BootstrapState.tip`, threaded as `ForwardSyncState.recovered_anchor`) MUST be an IDEMPOTENT NO-OP boundary rewind — no WAL, no ChainDb mutation, no ledger mutation, no cursor (the anchor is a recovery snapshot boundary, NOT a stored servable block, and is NEVER synthesized into one — `ChainDb::tip()` / `last_block_bytes` / serve never return it); `RollBackward(Origin)` MUST still fail closed (AI-S4a unchanged); EVERY non-anchor / non-Origin rollback, and a match on slot-alone or hash-alone, and a `RollBack` with no recovered anchor, MUST fail closed (`NodeSyncError::UnexpectedRollback`); the accepted point MUST bind to the PERSISTED anchor on slot AND hash, NEVER peer-supplied alone; the anchor consumed by `run_node_sync` MUST be the single authority `BootstrapState.tip` threaded in — NEVER re-read from the store inside the loop; the first forward block after the anchor MUST admit through the EXISTING sole `pump_block` path (AK-S2 adds NO forward-link code); the path MUST stay replay-equivalent (same store + same ordered peer feed ⇒ byte-identical post-state + admit sequence — extends T-REC-05 / DC-NODE-31 to the follow). SCOPE: it MUST NOT add general stored-block rollback-follow on the single-producer path, and MUST NOT touch the Participant path (`run_participant_sync` — a separate follow-on). Test-enforced (`DC-NODE-32.ci_script = ""`). |
| **Inbound deps** | None (binary + integration tests). |
| **Outbound deps** | `ade_types`, `ade_core`, `ade_crypto`, `ade_ledger` (incl. `recovered_anchor_point::{RecoveredAnchorPoint, encode_recovered_anchor_point}` in tests, N-AK), `ade_runtime` (incl. `recovered_anchor::load_recovered_anchor_point` + `bootstrap::BootstrapInputs.recovered_anchor`, N-AK; `admission::{dial_for_admission, run_admission_wire_pump, AdmissionPeerEvent}`), `ade_network` (incl. `codec::chain_sync::Point`), `ade_codec`, `serde`, `serde_json`, `tokio`. Dev-deps: `ade_testkit`, `tempfile`. |
| **Entry points** | `main()`; `ade_node::run_node_lifecycle` (`--mode node`); `ade_node::node_lifecycle::{run_relay_loop, run_relay_loop_with_sched, ForgeActivation, spawn_live_wire_pump_source, run_node_serve_task, bind_serve_listener, ServeStartError, may_cold_start_forge, warm_start_recovery, apply_chain_event, run_participant_sync, declare_participant_venue, AppliedTip, ApplyError}` (**PHASE4-N-AK: `warm_start_recovery` loads the anchor-point record + threads `recovered_anchor`; `wire_pump_start_point` resolves the FindIntersect start — module-private helper**); `ade_node::node_sync::{NodeBlockSource, run_node_sync, forge_one_from_recovered, admit_forged_block_durably, forge_epoch_admission, forge_header_position, forge_followed_tip_admission, NodeSyncItem, classify_receive, resolve_disposition, ReceiveClass, ReceiveDisposition, NodeSyncError, ForgeRefused, NodeForgeOutcome, FollowedPeerTipSignal, NodeForgeError}` (**PHASE4-N-AK: `run_node_sync`'s RollBack handler accepts the recovered-anchor rollback as an idempotent no-op**); `ade_node::run_loop_planner::*` (GREEN); `ade_node::live_log::{NodeSchedEvent, NodeSchedLogWriter, NodeSchedSink}` (GREEN); `ade_node::convergence_evidence::{ConvergenceEvidenceSink, EvidenceEmitResult}` (GREEN, N-AJ); `ade_node::forge_intent::*` (GREEN); `ade_node::operator_forge::*` (RED); `ade_node::ba02_pass::*` + `ade_node::ba02_evidence::*`; `ade_node::rehearsal_pass::*` + `ade_node::rehearsal_evidence::*`; `ade_node::produce_mode::*`; `ade_node::cli::{Cli, ProduceCli, Mode}`; `ade_node::{admission, wire_only, key_gen}::*`. |
| **Key modules** | `lib.rs`, `cli.rs`, `node.rs`, `main.rs`, **`node_lifecycle.rs`** (the `--mode node` owner + `run_relay_loop` + the carried N-F-C…N-AJ wiring + **PHASE4-N-AK: `warm_start_recovery` loads the recovered anchor-point record via `load_recovered_anchor_point` + threads `recovered_anchor` into `bootstrap_initial_state`; the forge-ON arm sets `fwd.recovered_anchor = state.tip`; the new `wire_pump_start_point` FindIntersect-start helper**), **`node_sync.rs`** (the sync driver + the carried caps/conventions + the N-AI detector/resolver/apply plumbing + **PHASE4-N-AK: the `run_node_sync` RollBack handler — the recovered-anchor idempotent-no-op-vs-fail-closed single-producer rollback-resolution predicate, DC-NODE-32**), `forge_intent.rs` (GREEN), `operator_forge.rs` (RED), `run_loop_planner.rs` (GREEN), `ba02_evidence.rs` (GREEN), `convergence_evidence.rs` (GREEN, N-AJ), `live_log/` (GREEN sched vocabulary), `ba02_pass.rs` (RED), `rehearsal_evidence.rs` (GREEN), `rehearsal_pass.rs` (RED), `produce_mode.rs`, `wire_only.rs`, `key_gen.rs`, `admission/`, `admission_log/`. |

> **GREEN sub-classification (carried):** `forge_intent`, `run_loop_planner`, `ba02_evidence`, `live_log::{sched_event,sched_writer}`, `convergence_evidence`, `rehearsal_evidence` are GREEN-by-content; `node_sync::{forge_epoch_admission, forge_header_position, forge_followed_tip_admission, single_producer_forge_decision, venue_policy, warm_start_forge_mode, forge_mode_after_admit, classify_receive, resolve_disposition, pending_reselection_forge_refusal, durable_tip_matches}` + `node_lifecycle::may_cold_start_forge` are GREEN-by-function. `node_lifecycle` + `node_sync` + `operator_forge` + `ba02_pass` + `rehearsal_pass` are RED. **PHASE4-N-AK:** the `wire_pump_start_point` helper + the `warm_start_recovery` anchor-point load/thread are RED `node_lifecycle` wiring; the `run_node_sync` RollBack handler's recovered-anchor predicate is a BLUE-decision-expressed-as-RED-loop-wiring (it reads the threaded `ForwardSyncState.recovered_anchor` — the single anchor authority). The threaded `ForwardSyncState.recovered_anchor` field lives in the GREEN `ade_runtime::forward_sync::reducer`; the +2 new BLUE canonical types (`RecoveredAnchorPoint` / `RecoveredAnchorPointError`) + the BLUE load+verify fn (`load_recovered_anchor_point`) live in `ade_ledger` / `ade_runtime::recovered_anchor`, NOT in `ade_node`; the BLUE `decode_recovered_anchor_point` codec it depends on is byte-stable.

---

### `ade_core_interop`

| Attribute | Value |
|-----------|-------|
| **Purpose** | Live cardano-node interop driver. Hosts the `live_*_session` RED binaries plus the N-E S4/S5 GREEN tx-submission bridges and the chain-follow driver (`follow`). `follow` strips the peer's ChainSync `RollForward` `[era_tag, tag24(header_cbor)]` envelope via `ade_codec::unwrap_tag24`. (Unchanged across the N-AK span.) |
| **Creates (RED-only)** | Live-session drivers and transcript types; never semantic types. |
| **MUST NOT** | (1) Construct semantic types from raw bytes. (2) Be depended on by any BLUE/GREEN crate (RED leaf). (3) Overstate semantic truth in evidence. (4) `follow` (CN-WIRE-08) — strip via `ade_codec::unwrap_tag24`. |
| **Inbound deps** | None (RED leaf — binaries). |
| **Outbound deps** | `ade_core`, `ade_codec`, `ade_crypto`, `ade_ledger`, `ade_runtime`, `ade_network`, `ade_testkit`, `ade_types`, `tokio`. |
| **Entry points** | `ade_core_interop::bin::{live_tx_submission_session, live_block_production_session, live_block_fetch_session, live_block_follow_session}`; `ade_core_interop::follow::*`; `ade_core_interop::{tx_submission, local_tx_submission}`. |
| **Key modules** | `bin/`, `follow.rs`, `tx_submission.rs`, `local_tx_submission.rs`. |

---

### `ade_network::mux::transport` *(RED)*

| Attribute | Value |
|-----------|-------|
| **Purpose** | The only place inside `ade_network` where socket I/O happens. RED tokio shell over the BLUE mux frame primitive and the GREEN session reducer. Provides `MuxTransportHandle` + closed `TransportError` + `DuplexCapacity::DEFAULT` + `spawn_duplex`. (Unchanged across the span.) |
| **Creates (RED-only)** | `MuxTransportHandle`, `TransportError`, `DuplexCapacity`, `MuxTransport`. |
| **MUST NOT** | (1) Construct semantic types. (2) Bypass `mux::frame` for framing. (3) Live in BLUE scope. |
| **Inbound deps** | `ade_runtime::network::{mux_pump, n2n_dialer}`. |
| **Outbound deps** | `ade_network::mux::frame` (BLUE), `ade_network::session` (GREEN), `tokio`. |
| **Entry points** | `ade_network::mux::transport::{MuxTransportHandle, spawn_duplex, open_tcp}`. |
| **Key modules** | `mux/transport.rs`. |

---

### `ade_network` *(RED capture binaries — non-session)*

| Attribute | Value |
|-----------|-------|
| **Purpose** | Operator-action capture binaries (live evidence harness) inside `ade_network`. The `ade_chain_sync_capture` binary supports `--intersect-slot` / `--intersect-hash` flags to capture a real Conway `RollForward` golden fixture; the G-L/G-M real-cardano-node handshake + FindIntersect fixtures were captured via this harness family. |
| **MUST NOT** | (1) Construct semantic types from raw bytes. (2) Overstate semantic truth in captured evidence. |
| **Outbound deps** | `ade_network` BLUE/GREEN submodules, `ade_codec`, `ade_types`, `tokio`. |

---
## Cross-Module Rules (project-wide)

### Dependency direction (RED → GREEN → BLUE; never outward)

```
ade_types     → {}
ade_codec     → {ade_types}
ade_crypto    → {ade_types, blake2, ed25519-dalek, cardano-crypto[vrf-draft03,kes-sum(test-only),dsign]}
ade_core      → {ade_types, ade_crypto, minicbor}   [BLUE; production select_best_chain byte-unchanged across N-AI/N-AJ/N-AK — the fork_choice.rs touch was a #[cfg(test)]-only arrival-order proof, CN-CONS-01]
ade_ledger    → {ade_core, ade_plutus, ade_crypto, ade_codec, ade_types, minicbor, num-*}   [BLUE; seed_consensus_inputs carries epoch_nonce: Nonce + SEED_CINPUT_SCHEMA_VERSION 2 (N-F-G-N); block_validity::header_input::DecodedBlock carries the additive prev_hash: PrevHash field (N-AE); wal::event carries the closed-sum variant WalEntry::RollBack {to_point, reason, prior_tip, selected_tip} at tag 1 + RollbackPoint/RollbackReason + WalError::RollbackTargetNotInChain (N-AI); PHASE4-N-AK adds the NEW module recovered_anchor_point — the closed RecoveredAnchorPoint {anchor_fp, slot, block_hash} record + RecoveredAnchorPointError + the SOLE canonical CBOR codec encode/decode_recovered_anchor_point, version-gated RECOVERED_ANCHOR_POINT_SCHEMA_VERSION = 1; +2 BLUE canonical types, 179→181; a SEPARATE additive record from SeedEpochConsensusInputs (disjoint shape/schema/sidecar binding), built on ade_codec canonical primitives]
ade_plutus    → {ade_crypto, ade_codec, ade_types, aiken_uplc, pallas-primitives(internal)}
ade_network   → BLUE submodules → {ade_codec, ade_types}; session(GREEN) → +BLUE submodules; transport(RED) → +tokio   [no ade_network change in the N-AK span — the recovered-anchor FindIntersect start point is supplied by ade_node::wire_pump_start_point over the existing codec::chain_sync::Point]
ade_testkit   → {ade_core, ade_ledger, ade_plutus, ade_runtime, ade_crypto, ade_codec, ade_types, cardano-crypto}   [GREEN]
ade_runtime   → {ade_core, ade_crypto, ade_codec, ade_types, ade_ledger, ade_network, redb, serde*, bech32, base58, cardano-crypto[vrf-draft03,dsign], ed25519-dalek, tokio}   [RED; admission::wire_pump calls ade_codec::unwrap_tag24 on the feed side (N-F-G-O) + preserves the RollBackward point as AdmissionPeerEvent::RollBackward (N-AI); bootstrap/seed_consensus_merge thread the recovered epoch_nonce/eta0 (N-F-G-N); network::serve_dispatch is the single shared serve-dispatch core reading a closed ServedChainSource{Snapshot|DurableChainDb}; network::served_chain_projection (ChainDbServedSource) is the read-only durable-ChainDb serve projection bounded by range_bytes_capped/last_block_bytes (MAX_SERVE_RANGE_BLOCKS=256, DC-SERVEMEM-01) with the N-AE FindIntersect-only forge-parent intersect clause (DC-NODE-14); producer::producer_shell::kes_sign_header_advancing evolves the KES key before signing (DC-CRYPTO-10); forward_sync::pump::pump_block has the hash-exact already-have idempotency gate (DC-NODE-16). PHASE4-N-AK (RED + a BLUE-record load): NEW module recovered_anchor — load_recovered_anchor_point (single SnapshotStore read = RED I/O of a BLUE record; decode via the sole ade_ledger codec + anchor_fp binding check = BLUE), kept OUT of bootstrap.rs (CN-NODE-01); bootstrap.rs adds the private pure resolve_live_follow_start + BootstrapInputs.recovered_anchor + 3 BootstrapError::RecoveredAnchorPoint{Missing,Decode,BindingMismatch} variants; chaindb adds the disjoint anchor-fp-keyed SnapshotStore::{put,get}_recovered_anchor_point (in_memory BTreeMap + persistent redb table recovered_anchor_point_by_anchor_fp, additive, no SCHEMA_VERSION bump); seed_epoch_lineage::persist_seed_epoch_consensus_inputs also writes the anchor-point record at seed/recover; forward_sync::reducer (GREEN) adds the ForwardSyncState.recovered_anchor: Option<ChainTip> threaded field. NO new BLUE/external dep — the +2 BLUE types are in ade_ledger]
ade_node      → {ade_types, ade_core, ade_crypto, ade_ledger, ade_runtime, ade_network, ade_codec, serde, serde_json, tokio}   [RED; node_lifecycle carries the N-F-G-K…N-AJ live serve/feed/forge/rollback-follow/convergence-evidence wiring; PHASE4-N-AK (RED/GREEN; the +2 new BLUE types live in ade_ledger): node_lifecycle::warm_start_recovery loads the recovered anchor-point record via ade_runtime::recovered_anchor::load_recovered_anchor_point + threads recovered_anchor into bootstrap_initial_state (DC-NODE-31), the forge-ON arm sets fwd.recovered_anchor = BootstrapState.tip, the new wire_pump_start_point helper resolves the WirePump FindIntersect start from the same recovered tip; node_sync::run_node_sync's RollBack handler accepts a peer RollBackward binding EXACTLY (slot AND hash) to the threaded recovered anchor as an idempotent no-op, every other rollback (Origin, slot-only, hash-only, no-anchor) fails closed UnexpectedRollback (DC-NODE-32); run_participant_sync UNTOUCHED; DC-CONS-03 untouched; pump_block stays the sole durable admit authority. forge_intent + run_loop_planner + ba02_evidence + convergence_evidence + live_log + rehearsal_evidence + node_sync GREEN-by-function classifiers are GREEN-by-content/function]
ade_core_interop → {ade_core, ade_codec, ade_crypto, ade_ledger, ade_runtime, ade_network, ade_testkit, ade_types, tokio}   [RED leaf]
```

**Forbidden directions (carried + the N-AK span).** Any BLUE crate (or BLUE `ade_network` submodule) depending on `ade_runtime`, `ade_node`, `ade_core_interop`, or the RED half of `ade_network` is a CI failure (`ci_check_dependency_boundary.sh`). Any non-`ade_plutus` crate referring to `pallas_*` is a CI failure. A second block-envelope encoder, a second `leader_vrf_input` authority, a second `wrap_tag24`/`unwrap_tag24` definition or a hand-rolled tag-24 parse in RED, a forward-sync `AdvanceTip` before durability, a Mithril/genesis bootstrap path bypassing the single `bootstrap_initial_state`, an internal-fingerprint-vs-Haskell-hash equality assertion, a registry rule citing a non-existent `code_locus`, a Mithril-bootstrap path sourcing the anchor `seed_point` from the manifest, **a `WalEntry::RollBack` that implements a second rollback (N-AI), and (PHASE4-N-AK) a second/hand-rolled `RecoveredAnchorPoint` encoder/decoder, a recovered-anchor-point store surface that collides with the slot-keyed snapshot or seed-epoch sidecar namespaces, a `resolve_live_follow_start` that synthesizes a servable block or treats a zero-hash anchor as a usable start, a `warm_start_recovery` that silently falls back to Origin on a missing/malformed/fingerprint-mismatched anchor-point record, or a `run_node_sync` recovered-anchor rollback no-op that binds on slot-alone / hash-alone / a peer-supplied point rather than the persisted `BootstrapState.tip` on both slot AND hash** are all CI failures (the carried N-A…N-AI fences + the N-AK test-enforced rules).

**NEW (PHASE4-N-AK — recovered-anchor live-follow start + recovered-anchor rollback no-op):**
- **N-AK AK-S1 (DC-NODE-31):** after recovery from a non-Origin bootstrap anchor, the recovered store MUST persist the bootstrap anchor point `(slot, block_hash)` as replayable recovery provenance bound to the recovered `anchor_fp` (via the disjoint anchor-fp-keyed `put_recovered_anchor_point`, written at seed/recover by `seed_epoch_lineage`), and on warm-start `BootstrapState` MUST resolve the live-follow start tip from it whenever ChainDb has no servable post-anchor block (resolution order = servable `ChainDb::tip()` → persisted recovered anchor point, non-Origin + provenance-bound → Origin/None only if truly Origin/cold-start). A non-Origin recovered store whose anchor-point record is missing / malformed / fingerprint-mismatched MUST FAIL CLOSED before live follow starts (never a silent Origin fallback). The encode/decode MUST go through the SOLE `ade_ledger::recovered_anchor_point` codec; the load+verify MUST stay OUT of `bootstrap.rs` (CN-NODE-01 single-`pub fn`); `resolve_live_follow_start` MUST be a pure module-private resolver that synthesizes no block and changes no `ChainDb::tip()` semantics; the WirePump FindIntersect start MUST be the SAME recovered tip (`wire_pump_start_point`). Same recovered store + same WAL ⇒ same anchor point ⇒ same `BootstrapState.tip` ⇒ same FindIntersect start (replay-equivalent; extends T-REC-05). The persisted anchor point is the durable restart authority — NOT CLI re-supply. AI-S4a `RollBackward(Origin)` fail-close unchanged; the wire-pump consumer is UNCHANGED. A violation is a correctness failure — **test-enforced (`DC-NODE-31.ci_script = ""`; no dedicated CI gate this cluster)**.
- **N-AK AK-S2 (DC-NODE-32):** on the single-producer follow path (`run_node_sync`), a peer `RollBackward` whose target binds EXACTLY (slot AND hash) to the persisted recovered anchor (`DC-NODE-31` / `BootstrapState.tip`, threaded as `ForwardSyncState.recovered_anchor`) MUST be an IDEMPOTENT NO-OP boundary rewind: no WAL, no ChainDb mutation, no ledger mutation, no cursor. The anchor is a recovery-snapshot boundary, NOT a stored servable block, and MUST NEVER be synthesized into one (`ChainDb::tip()` / `last_block_bytes` / serve never return it). `RollBackward(Origin)` MUST still fail closed (AI-S4a unchanged); every non-anchor / non-Origin rollback, and a match on slot-alone or hash-alone, and a `RollBack` with no recovered anchor, MUST fail closed (`UnexpectedRollback`). The accepted point MUST bind to the PERSISTED anchor on slot AND hash, NEVER peer-supplied alone; the anchor consumed MUST be the single `BootstrapState.tip` threaded in, NEVER re-read from the store inside the loop. The first forward block after the anchor MUST admit through the EXISTING sole `pump_block` path (AK-S2 adds NO forward-link code). Recover→follow on the single-producer path MUST be replay-equivalent (same store + same ordered peer feed ⇒ byte-identical post-state + admit sequence). SCOPE: it MUST NOT add general stored-block rollback-follow on the single-producer path, and MUST NOT touch the Participant path (`run_participant_sync` — a separate follow-on). DC-CONS-03 untouched. A violation is a correctness failure — **test-enforced (`DC-NODE-32.ci_script = ""`; no dedicated CI gate this cluster)**. `RO-LIVE-01` stays operator-gated.

**NEW (PHASE4-N-AJ — participant-path convergence evidence emission):**
- **N-AJ (DC-NODE-30 / DC-EVIDENCE-03 / DC-ADMIT-04):** the participant rollback-follow path MUST emit the reused closed `AgreementVerdict` vocabulary (`block_received` / `block_admitted` / `agreement_verdict` via `verdict::derive`) to a dedicated `--convergence-evidence-path` sink as an EMIT-ONLY GREEN side-output — a write-failure surfaces + poisons + marks incomplete (`EvidenceEmitResult::FailedAndPoisoned`), is NEVER swallowed, and NEVER halts the authority; emit-only on `Diverged`; an absent path leaves the byte output unchanged; the vocabulary stays closed (no open variant — DC-ADMIT-04 closed-vocab isolation extends to this file); no raw-writer accessor. `CN-CONS-03` is NOT flipped (the CE-AI-6 convergence transcript is now PRODUCIBLE + mechanically checkable; the flip is the follow-on operator pass ONLY). `ci_check_convergence_evidence_vocabulary_closed.sh` + `ci_check_convergence_evidence_emit_only.sh`.

**NEW (PHASE4-N-AI — live fork-choice rollback-follow wiring; single-best-peer FOLLOW, NOT full ChainSel):** — `DC-NODE-23`…`DC-NODE-29` + `CN-CONS-01`. Gates: `ci_check_wal_rollback_replay_equiv.sh` (AI-S1, the `WalEntry::RollBack` MARKER + replay re-anchor — re-invokes the EXISTING `materialize_rolled_back_state` + `commit_rollback`, NOT a second rollback impl; a rollback target not in the active chain fails closed), `ci_check_receive_detector_venue_split.sh` (AI-S2, `classify_receive` venue-blind + `resolve_disposition` venue-split), `ci_check_live_fork_choice_apply.sh` + `ci_check_live_fork_choice_wiring.sh` (AI-S3/S4b, durable apply via the existing authorities + no-forge-across-unresolved-reselection), `ci_check_wire_rollback_signal_preserved.sh` (AI-S4a, the preserved `AdmissionPeerEvent::RollBackward` point), `ci_check_participant_venue_inert.sh` (AI-S4b-i), `ci_check_rollback_target_canonical_binding.sh` (AI-S6, the stored chain point is the SOLE rollback-target authority — the H-1 Byzantine-truncation remediation), `ci_check_chain_selection_arrival_order_independent.sh` + `ci_check_convergence_evidence_schema.sh` (AI-S5). **DC-CONS-03 stays the sole selector; `CN-CONS-03` stays `partial`; NO RO-LIVE flip.**

**Carried (compressed) earlier-cluster fences — full detail in the registry's `ci_script` fields:** PHASE4-N-AH (`DC-NODE-20`/`21`/`22` — local-durable forge base + cert evidence-only + warm-start re-entry; `ci_check_{local_durable_forge_base,cert_evidence_only,warm_start_re_entry,live_transcript_forge_base}.sh`), PHASE4-N-AG (`DC-NODE-19` declared — `ci_check_single_producer_loop_continuation.sh`), PHASE4-N-AF (`DC-NODE-18`, cert mechanism SUPERSEDED by N-AH — `ci_check_single_producer_extend_own_spine.sh`), PHASE4-N-AE / N-AE.F (`DC-NODE-14`/`15`/`DC-CONS-24`/`DC-PROTO-10`/`DC-NODE-16` — forge-on-followed-tip + recovered-parent intersectability + recover→follow WAL lineage + FindIntersect cursor + receive idempotency; `ci_check_{forge_followed_tip_admission,recovered_anchor_intersectable,recover_follow_wal_lineage,receive_idempotency}.sh`), PHASE4-N-AC (`DC-CRYPTO-10` — KES evolve-then-sign; `ci_check_kes_evolution_before_sign.sh`), PHASE4-N-AB (`CN-SESS-05` — outbound mux segmentation; `ci_check_outbound_segmentation.sh`), PHASE4-N-AA (`DC-SERVEMEM-01` — bounded peer-driven serve; `ci_check_serve_range_bounded.sh`), PHASE4-N-U (`DC-NODE-12`/`DC-CONS-23`/`DC-WAL-04`/`T-REC-05`/`DC-NODE-13` — forged-block durability via `pump_block` + forged-tip recovery + serve-as-projection; `ci_check_{forged_durable_admit_via_pump,served_chain_projection}.sh` + `ci_check_node_run_loop_containment.sh`; the G-R `ci_check_served_chain_stability.sh` RETIRED), and the N-F-G-K…G-R wire/serve/feed/forge fences (`ci_check_{node_serve_lifetime,n2n_handshake_versiondata_authority,chainsync_findintersect_compat,warmstart_eta0_overlay,feed_tag24_unwrap,feed_leader_threshold_view,forge_successor_evolved_spine}.sh`).

### Naming convention

All crates are prefixed `ade_`. TCB color is not encoded in the crate name. The authoritative classifier is `.idd-config.json` `core_paths` + the per-file `// Core Contract:` / `//! BLUE|GREEN|RED` banner + the cluster TCB Color Maps; CI scripts hard-code their BLUE list.

### Closed enums / registries (for SEAMS cross-reference)

Closed semantic surfaces detected at HEAD (carried + the N-AK span): `AcceptedMiniProtocol` (`ade_network::session`), `TagEnvelopeError` (`ade_codec::cbor::tag24`), `ExpectedVrfInput` / `LeaderCheckVerdict` (`ade_core::consensus`), `OutboundCommand` + `DispatchError` (`ade_runtime::network`), `KesError` / `KesParseError`, the admission/wire-only/live-log JSONL vocabularies (incl. the emit-only `NodeSchedEvent` + the N-AJ `AgreementVerdict` convergence-evidence vocabulary), the operator-evidence manifest TOML schema, the closed `CardanoEra` / Conway cert + governance enums, `PrevHash` (`ade_types::shelley::block`, the closed `Genesis | Block(Hash32)` wire grammar), the closed BLUE `ArrayHead { Definite(u64), Indefinite }` (`ade_network::codec::primitives`, SCOPED to FindIntersect — CN-WIRE-11). **N-F-A:** `SeedEpochConsensusInputs` (version-gated; the SOLE codec; carries `epoch_nonce` under `SEED_CINPUT_SCHEMA_VERSION = 2`) + `SeedConsensusInputsError` / `SeedConsensusMergeError` / `SeedEpochConsensusSource` + `WalEntry::SeedEpochConsensusInputsImported`. **N-F-C/D/E/F + N-F-G-A/B/D/H:** `Mode` (5-variant), `NodeStart`, `NodeBlockSource` (2-variant), the BA-02 vocabulary, `LoopStep`/`ForgeSlotStatus`, `ForgeIntent`, `SlotAlignmentError`/`ProtocolParamsParseError`/`ForgeCurrentPParamsError`/`ForgeEpochAdmission`, `SelfAcceptedHandoff`, `RehearsalVenue`/`PrivateRehearsalManifest`, `DispatchError` (4-variant)/`ServeStartError` (2-variant). **N-U/N-AA:** `ServedChainSource { Snapshot \| DurableChainDb }`, `ServeRangeOutcome { Served \| Empty \| CapExceeded \| ReadError }` + `CappedSlotRange` + `MAX_SERVE_RANGE_BLOCKS = 256`. **N-AE:** `ForgeFollowedTipAdmission { CaughtUp \| NotCaughtUp(NotCaughtUpReason) }` + `NotCaughtUpReason` + `ForgeRefused` + `NodeForgeOutcome` + `FollowedPeerTipSignal`. **N-AI:** the BLUE `WalEntry::RollBack {to_point, reason, prior_tip, selected_tip}` (tag 1, append-only) + `RollbackReason {ForkChoiceWin \| PeerRollBackward}` + `RollbackPoint {slot, hash, block_no}` + `WalError::RollbackTargetNotInChain`; the RED `NodeSyncItem {Block \| RollBack(Point)}` / `ReceiveClass` / `ReceiveDisposition` + the extended `NodeSyncError` + `ForgeRefused::ReselectionPending` + `AdmissionPeerEvent::RollBackward` + `AdmissionWirePumpError::UnsupportedRollbackPoint`. **N-AJ:** the GREEN closed `EvidenceEmitResult {Written \| Disabled \| FailedAndPoisoned}` + the reused closed `AgreementVerdict` vocabulary on the convergence-evidence file. **PHASE4-N-AK (NEW closed surfaces):** the BLUE closed record `RecoveredAnchorPoint {anchor_fp: Hash32, slot: SlotNo, block_hash: Hash32}` (`ade_ledger::recovered_anchor_point`; version-gated `RECOVERED_ANCHOR_POINT_SCHEMA_VERSION = 1` written into the `array(4)` wire form; the SOLE codec `encode/decode_recovered_anchor_point`; no `Default`, no `#[non_exhaustive]`) + the closed error sum `RecoveredAnchorPointError {MalformedCbor \| UnknownVersion {expected, found} \| Structural {reason} \| TrailingBytes {extra}}` (the +2 new BLUE canonical types) + the RED closed `BootstrapError` additions `RecoveredAnchorPointMissing {anchor_fp}` / `RecoveredAnchorPointDecode(RecoveredAnchorPointError)` / `RecoveredAnchorPointBindingMismatch {expected_anchor_fp, actual_anchor_fp}` (RED orchestration variants — NOT canonical-counted). **Governance:** `RecoveredAnchorPoint` is a NEW version-gated closed canonical type with a SOLE codec — a SEPARATE additive record from `SeedEpochConsensusInputs` (disjoint shape, disjoint `RECOVERED_ANCHOR_POINT_SCHEMA_VERSION = 1` version line, disjoint anchor-fp-keyed `SnapshotStore` surface — it does NOT touch the seed-epoch sidecar's shape / schema / `sidecar_hash` / provenance binding); the redb table `recovered_anchor_point_by_anchor_fp` is ADDITIVE (no `SCHEMA_VERSION` bump — a pre-AK store reads `TableDoesNotExist → Ok(None)`); the two N-AK rules `DC-NODE-31` / `DC-NODE-32` are both `tier = derived` + both `enforced` (test-enforced, `ci_script = ""`); none flips an RO-LIVE rule (`RO-LIVE-01` stays operator-gated); the Origin distinction (a zero-hash record is a representable round-trippable value, never a usable start) lives in the resolver, not the codec, so the closed record stays a pure data carrier.

### CI enforcement (159 scripts under `ci/`)

The full list is mechanically obtainable via `ls ci/ci_check_*.sh` (159 at HEAD `b4c0983d`). CI lives entirely under `ci/` (`ci_dirs = ["ci"]`); there is no `.github/workflows/` invariant gate at HEAD. **PHASE4-N-AK added ZERO CI scripts** — `git diff --name-status b1bed361..HEAD -- 'ci/ci_check_*.sh'` is empty (0 `A` / 0 `M` / 0 `D`); both N-AK rules `DC-NODE-31` + `DC-NODE-32` are **test-enforced** (`ci_script = ""`), consistent with the project's prior test-enforced rules (`DC-PROTO-10`, `T-REC-05`). The +2 vs the prior on-disk CODEMAP (157) are the PHASE4-N-AJ gates `ci_check_convergence_evidence_vocabulary_closed.sh` + `ci_check_convergence_evidence_emit_only.sh`. The earlier-span gates (N-AI and back) are unchanged. The PHASE4-N-AJ + carried N-AI new gates:

| Script | Enforces | Cluster / commit |
|---|---|---|
| `ci_check_convergence_evidence_vocabulary_closed.sh` | **NEW (PHASE4-N-AJ, DC-NODE-30 / DC-ADMIT-04)** — the `--convergence-evidence-path` sink emits the reused CLOSED `AgreementVerdict` vocabulary only (`block_received` / `block_admitted` / `agreement_verdict` via `verdict::derive`); an open/wildcard variant or a stringly authoritative field is a compile error + an allow-list-test failure. | PHASE4-N-AJ |
| `ci_check_convergence_evidence_emit_only.sh` | **NEW (PHASE4-N-AJ, DC-NODE-30 / DC-EVIDENCE-03)** — the convergence-evidence sink is EMIT-ONLY: a write-failure surfaces + poisons + marks incomplete (`EvidenceEmitResult::FailedAndPoisoned`), never swallowed, never halts the authority; emit-only on `Diverged`; an absent path leaves the byte output unchanged; no raw-writer accessor. | PHASE4-N-AJ |
| `ci_check_wal_rollback_replay_equiv.sh` | **NEW (PHASE4-N-AI AI-S1, DC-NODE-27)** — the ordered live receive-event sequence replayed against the same anchor + durable log produces a BYTE-IDENTICAL durable tip + ledger fp + PraosChainDepState INCLUDING rollback+reselection; a live rollback is the append-only canonical `WalEntry::RollBack` MARKER (tag 1) whose replay re-anchors to `to_point`'s EXISTING in-chain `post_fp` + re-invokes the EXISTING `materialize_rolled_back_state` + lockstep `commit_rollback` — NOT a second rollback impl; a rollback target not in the active chain fails closed (`RollbackTargetNotInChain`). | PHASE4-N-AI AI-S1 |
| `ci_check_receive_detector_venue_split.sh` | **NEW (PHASE4-N-AI AI-S2, DC-NODE-23/24)** — a peer-origin item is classified ONCE venue-blind by the pure total `classify_receive`; `resolve_disposition` is TOTAL over the closed venue set (SingleProducer/Unknown ⇒ `RefuseSingleProducer` byte-unchanged, Participant ⇒ `NeedsForkChoice` routed to the existing `chain_selector` → BLUE `select_best_chain`); a raw `followed_peer_tip` never reaches `select_best_chain`. | PHASE4-N-AI AI-S2 |
| `ci_check_live_fork_choice_apply.sh` | **NEW (PHASE4-N-AI AI-S3, DC-NODE-25/26/27)** — a `ChainSelected`/`RolledBack` outcome is applied to the durable stores ONLY via the existing enforced authorities; no second apply / tip-advance / rollback-materialize path; a fork-choice win is adopted ONLY when its BODIES validate+apply; after any applied decision `selector.current_tip` EQUALS the durable `ChainDb::tip`; a rejected decision makes no durable change. | PHASE4-N-AI AI-S3 |
| `ci_check_live_fork_choice_wiring.sh` | **NEW (PHASE4-N-AI AI-S3/S4b, DC-NODE-25/28)** — the live receive path wires detector → resolver → existing `chain_selector` → durable apply, and the forge is DISABLED while a re-selection is pending (typed `ForgeRefused::ReselectionPending`). | PHASE4-N-AI AI-S3/S4b |
| `ci_check_wire_rollback_signal_preserved.sh` | **NEW (PHASE4-N-AI AI-S4a, DC-PUMP-01)** — the feed-side WirePump surfaces a `RollBackward` as the closed `AdmissionPeerEvent::RollBackward {peer, point, tip}` (the rollback POINT preserved, never a `TipUpdate`-only); a `RollBackward(Origin)` fails closed (`UnsupportedRollbackPoint`). | PHASE4-N-AI AI-S4a |
| `ci_check_participant_venue_inert.sh` | **NEW (PHASE4-N-AI AI-S4b-i, DC-NODE-28)** — a declared Participant venue is INERT before the live flip; a producer tick during a pending re-selection fails closed (`ForgeRefused::ReselectionPending`). | PHASE4-N-AI AI-S4b-i |
| `ci_check_rollback_target_canonical_binding.sh` | **NEW (PHASE4-N-AI AI-S6, DC-NODE-29; H-1 remediation)** — a live peer `RollBackward(point)` rollback target uses the STORED chain point (stored slot + hash) as the SOLE authority; the peer-supplied slot MUST equal the stored slot for that hash; on any mismatch / unknown hash / `Origin` the path fails closed with a typed error BEFORE `commit_rollback`, BEFORE `WalEntry::RollBack`, BEFORE any durable mutation. | PHASE4-N-AI AI-S6 |
| `ci_check_chain_selection_arrival_order_independent.sh` | **NEW (PHASE4-N-AI AI-S5, CN-CONS-01)** — `select_best_chain` picks the same fork-choice-maximal tip regardless of candidate presentation order (the arrival-order-permutation proof over the UNCHANGED production `select_best_chain`). | PHASE4-N-AI AI-S5 |
| `ci_check_convergence_evidence_schema.sh` | **NEW (PHASE4-N-AI AI-S5, CE-AI-5)** — the convergence-evidence manifest matches the closed schema; RED evidence ONLY (`RO-LIVE-01` operator-gated, CE-AI-6 vacuous-until-committed). | PHASE4-N-AI AI-S5 |

> **PHASE4-N-AK added no CI gate.** Both `DC-NODE-31` (recovered-anchor live-follow start) and `DC-NODE-32` (recovered-anchor rollback no-op) are TEST-ENFORCED (`ci_script = ""`), via the suites named in their registry `tests` arrays: `ade_ledger::recovered_anchor_point::recovered_anchor_point_round_trips_byte_identical` (+ the version / short-hash / trailing-byte / short-buffer rejection tests), `ade_runtime::bootstrap::{resolve_live_follow_start_treats_zero_hash_anchor_as_origin, bootstrap_bare_anchor_recovery_surfaces_anchor_as_live_follow_tip, bootstrap_true_origin_recovery_surfaces_none_tip, bootstrap_servable_chaindb_tip_wins_over_anchor, warm_start_loads_persisted_anchor_point, warm_start_non_origin_anchor_missing_anchor_point_fails_closed, warm_start_anchor_point_fingerprint_mismatch_fails_closed, same_store_same_anchor_point_same_findintersect_start}`, `ade_runtime::seed_epoch_lineage::bootstrap_recover_persists_anchor_point_sidecar`, `ade_node::node_lifecycle::recovered_bare_anchor_findintersect_starts_at_anchor_not_origin`, and the AK-S2 suite `ade_node::tests::live_fork_choice_ai_s4bii::{ak_s2_rollback_to_recovered_anchor_is_idempotent_noop, ak_s2_rollback_to_origin_fails_closed_even_with_anchor, ak_s2_non_anchor_rollback_fails_closed_slot_and_hash_bound, ak_s2_no_recovered_anchor_still_fails_closed, ak_s2_after_anchor_noop_forward_block_reaches_pump_block_validation_holds, singleproducer_rollback_refused_by_run_node_sync}` + `ade_node::node_sync::ak_s2_valid_forward_block_admits_after_recovered_anchor_noop`. This is a real enforcement surface — but **no dedicated `ci_check_*.sh` gate scans for the rules' structural shape** (e.g. "the load is the SOLE recovered-anchor-point decode site", "the rollback no-op binds on slot AND hash", "the anchor is never synthesized into a servable block"). Surfaced as a gap below, not guessed.

---

## Generation notes

- Regenerated (**PHASE4-N-AK cluster-close refresh** — recovered-anchor live-follow start + recovered-anchor rollback no-op) at HEAD `b4c0983d` (`git rev-parse --short HEAD`). The prior on-disk CODEMAP was pinned at the PHASE4-N-AI close (`5ec841c8`, 460 canonical types, 2413 tests by the documented grep, 157 CI checks, 354 rules); the N-AJ close (`b1bed361`) refreshed the OTHER three grounding docs (SEAMS / HEAD_DELTAS / TRACEABILITY) + the registry (356 rules) but left the CODEMAP at the N-AI close — this regeneration folds BOTH the `5ec841c8..b1bed361` N-AJ delta (evidence-only, ZERO BLUE change — the GREEN `convergence_evidence` module + the +2 convergence-evidence gates) AND the `b1bed361..b4c0983d` N-AK delta into the CODEMAP.
- **PHASE4-N-AK adds 2 new BLUE canonical types and ONE new BLUE module + authority (`ade_ledger::recovered_anchor_point` + the `ade_runtime::recovered_anchor::load_recovered_anchor_point` load+verify fn).** Verified mechanically: `git grep -hE "^(pub )?(struct|enum) "` over the BLUE `core_paths` trees is 460 → **462** (the +2 = `ade_ledger::recovered_anchor_point::{RecoveredAnchorPoint, RecoveredAnchorPointError}`; `ade_ledger` 179 → 181, confirmed against `git grep` at `b1bed361` = 179). `RECOVERED_ANCHOR_POINT_SCHEMA_VERSION = 1` is a `const` (NOT counted); `encode/decode_recovered_anchor_point` are fns (NOT counted). `git diff b1bed361..HEAD -- Cargo.toml` is empty — 11 crates. The new BLUE record is a SEPARATE additive record from `SeedEpochConsensusInputs` (disjoint shape / schema-version / sidecar binding / store namespace). The new BLUE authority `load_recovered_anchor_point` is kept OUT of `bootstrap.rs` (so `bootstrap_initial_state` stays the single `pub fn` bootstrap authority — CN-NODE-01); the single `SnapshotStore` read inside it is RED I/O of a BLUE-authoritative record, the decode + binding check are BLUE. The BLUE `select_best_chain` chain-selection authority (DC-CONS-03) is byte-UNCHANGED across both spans.
- Mechanical counts recomputed fresh from the tree at `b4c0983d` (not copied): **11 crates** (`grep -cE '"crates/' Cargo.toml`; `git diff b1bed361..HEAD -- Cargo.toml` empty — Δ 0), **462 canonical types** (Δ +2 vs the on-disk 460 — the `recovered_anchor_point` record + error sum in `ade_ledger`; `git grep -hE "^(pub )?(struct|enum) "` over the BLUE `core_paths` trees), **2469 tests** by the documented `#[test]|#[tokio::test]` grep (`git grep -hE "#\[test\]|#\[tokio::test\]" -- 'crates/**/*.rs' | wc -l`; Δ +56 vs the 2413 at `5ec841c8`, across the N-AJ + N-AK spans), **159 CI checks** (`ls ci/ci_check_*.sh | wc -l`; cross-checked `git ls-tree -r HEAD ci/`; Δ +2 vs the on-disk 157 — both from N-AJ; `git diff --name-status b1bed361..HEAD -- 'ci/ci_check_*.sh'` is EMPTY, N-AK added 0), **358 registry rules** (`grep -cE '^id = ' docs/ade-invariant-registry.toml`; Δ +4 vs 354; status breakdown 224 enforced / 19 partial / 114 declared / 1 enforced_scaffolding; no rule weakened).
- **Registry rule reconciliation (N-AJ + N-AK spans):** the FOUR net-new rules vs the prior on-disk 354 are `DC-NODE-30` (N-AJ, participant-path convergence evidence emission; `enforced`; `ci_check_convergence_evidence_vocabulary_closed.sh, ci_check_convergence_evidence_emit_only.sh`), `DC-EVIDENCE-03` (N-AJ, convergence-through-reorg transcript shape; `enforced_scaffolding`; `ci_check_convergence_evidence_schema.sh` vacuous-until-committed), `DC-NODE-31` (N-AK, recovered-anchor live-follow start authority; `enforced`; `ci_script = ""` — TEST-ENFORCED; `cross_ref = [DC-NODE-20, DC-NODE-22, DC-NODE-23, DC-NODE-29, T-REC-05, CN-CONS-03]`), `DC-NODE-32` (N-AK, recovered-anchor rollback boundary on the single-producer follow path; `enforced`; `ci_script = ""` — TEST-ENFORCED; `cross_ref = [DC-NODE-31, DC-NODE-29, DC-NODE-23, T-REC-05, CN-CONS-03]`). Both N-AK rules `introduced_in = "PHASE4-N-AK"`, `strengthened_in = []`, `tier = derived`. `DC-ADMIT-04` was strengthened by N-AJ (closed-vocab isolation extended to the convergence-evidence file). **No rule weakened; `CN-CONS-03` NOT flipped to enforced** (it stays `partial` — N-AK's CE-AK-3 live transcript restores the recover→follow path to resume CE-AI-6, it does NOT claim full ChainSel convergence).
- **TCB color for every new/changed module verified against the on-disk `// Core Contract:` / `//!` banner at `b4c0983d`:** `crates/ade_ledger/src/recovered_anchor_point.rs` opens `// Core Contract:` + `//! BLUE RecoveredAnchorPoint type + canonical CBOR codec (PHASE4-N-AK AK-S1)` → **BLUE** (the closed record + the sole codec); `crates/ade_runtime/src/recovered_anchor.rs` opens `// Core Contract:` + `//! BLUE recovered anchor-point load + fail-closed verify (PHASE4-N-AK AK-S1)` → **BLUE-by-content** (the single `SnapshotStore` read is RED I/O of a BLUE-authoritative record; the decode + binding check are BLUE; kept OUT of `bootstrap.rs` to preserve CN-NODE-01); `crates/ade_runtime/src/bootstrap.rs` opens `//! GREEN single bootstrap authority` → **GREEN composition + RED A3b restore** (the additive `resolve_live_follow_start` private resolver + `BootstrapInputs.recovered_anchor` + the 3 `RecoveredAnchorPoint*` `BootstrapError` variants are GREEN-composed over the BLUE authorities inside the RED `ade_runtime` crate); `crates/ade_runtime/src/chaindb/{mod,in_memory,persistent}.rs` open the `// Core Contract:` banner inside the RED `ade_runtime` crate → **RED** (the `put/get_recovered_anchor_point` anchor-fp-keyed store surface is RED storage plumbing); `crates/ade_runtime/src/seed_epoch_lineage.rs` is RED (the anchor-point write at seed/recover); `crates/ade_runtime/src/forward_sync/reducer.rs` opens `//! GREEN` → **GREEN** (the additive `ForwardSyncState.recovered_anchor: Option<ChainTip>` field — replay-derivable, default `None`); `crates/ade_node/src/{node_sync,node_lifecycle}.rs` carry `//! RED `--mode node`` → **RED** (the `wire_pump_start_point` helper + the `warm_start_recovery` anchor-point load/thread + the `run_node_sync` recovered-anchor RollBack handler are RED loop/lifecycle wiring; the RollBack handler's recovered-anchor predicate is a BLUE decision expressed in RED). The changed test files `crates/ade_node/tests/{live_fork_choice_ai_s4bii, shutdown_resume_identity}.rs` are `tests/` integration suites (no module banner — normal).
- **Cluster-doc location at this regen:** the PHASE4-N-AK cluster doc + slice docs (`cluster.md`, `S1-recovered-anchor-live-follow-start.md`, `S2-recovered-anchor-follow-rollback.md`) live under the ACTIVE `docs/clusters/PHASE4-N-AK/`; its invariants under `docs/planning/phase4-n-ak-{recovered-anchor-tip-invariants, s2-recovered-anchor-boundary-invariants}.md`. The PHASE4-N-AJ cluster doc + slice docs live under the ACTIVE `docs/clusters/PHASE4-N-AJ/` (its invariants/plan under `docs/planning/phase4-n-aj-*`); the convergence/operator evidence under `docs/evidence/`. Every prior closed cluster doc remains archived under `docs/clusters/completed/`.
- **`.idd-config.json` annotation drift (informational, NOT modified by this regen).** At this HEAD the config's `head_deltas_baseline` is `b1bed361` (the PHASE4-N-AJ close baseline) and `_invariant_registry_doc` reads "354 entries … through PHASE4-N-AI" — both lag the N-AJ + N-AK closes (the registry annotation should add `DC-NODE-30` / `DC-EVIDENCE-03` / `DC-NODE-31` / `DC-NODE-32` → 358 entries; the baseline should bump to `b4c0983d` at the next `/head-deltas`). They are config annotations the `/head-deltas` step bumps, not enforced counts; this `/codemap` run does NOT touch `.idd-config.json` or any source file. The next `/head-deltas` measures from `b1bed361` through (and past) `b4c0983d`.
- **Carried delta-stacks compressed, not deleted.** The full per-cluster N-AI / N-AH / N-AG / N-AF / N-AE.F / N-AE / N-AD / N-AC / N-AB / N-AA / N-U / G-K…G-R delta narratives that the prior on-disk CODEMAP carried at length are compressed into the "Active cluster at HEAD" + the "Carried (compressed)" CI/forbidden-direction sections + the per-module entries; the load-bearing MUST NOT content for each surface is preserved (in the per-module tables + the forbidden-directions list). The registry's per-rule `ci_script` / `statement` / `code_locus` fields are the authoritative source for any compressed detail.
- **Test-count method caveat (load-bearing — carried).** The documented method (`git grep -hE "#\[test\]|#\[tokio::test\]"`) matches `#[test]` and bare `#[tokio::test]` but NOT `#[tokio::test(flavor = "multi_thread", …)]`. The N-AK AK-S2 live-shape rollback tests in `ade_node/tests/live_fork_choice_ai_s4bii.rs` use the multi-thread flavor and are therefore NOT fully captured, so the *true* new-test count for the span is higher than the documented +56; the header tracks the documented method for consistency with the carried baseline. The N-AK invariants are mechanically enforced by the test suites named in the `DC-NODE-31` / `DC-NODE-32` registry `tests` arrays (no dedicated CI gate this cluster — see the CI-enforcement note + the gap below).
- The doc is regenerated, not edited. If a value drifts, fix the source, not the doc.
