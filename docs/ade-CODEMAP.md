# Module Authority Map — Ade

> **Status:** Living architectural document. Regenerated; not hand-edited.
> Per-project instance of `~/.claude/methodology/templates/codemap.md`.

> 11 crates, 421 canonical types, 1584 tests, 54 CI checks at HEAD (`75f75da`).

---

## Conventions

- A **module** in Ade is a Cargo workspace crate (smallest independently-buildable unit). One exception: `ade_network` is split by *submodule color* — its BLUE submodules and its RED submodules are documented as two separate entries below, because `.idd-config.json` `core_paths` resolves BLUE at the submodule path level rather than crate-wide. The `ade_core::consensus` submodule sits *inside* the BLUE `ade_core` crate and is covered by that entry. The `ade_ledger::block_validity` / `ade_ledger::consensus_view` / `ade_ledger::tx_validity` / `ade_ledger::mempool::admit` / `ade_ledger::mempool::ingress` / `ade_ledger::cert_classify` modules sit inside the BLUE `ade_ledger` crate and are covered by that entry; the `ade_ledger::producer` sub-tree (PHASE4-N-C — `forge`, `self_accept`, `state`; PHASE4-N-G — `served_chain`), the `ade_ledger::block_body_hash` top-level module (PHASE4-N-C S4 single body-hash authority), the `ade_ledger::receive` sub-tree (PHASE4-N-H), and **NEW — the `ade_ledger::rollback` sub-tree (PHASE4-N-I — `traits`, `error`, `materialize`, `commit`)** are likewise BLUE under the already-BLUE `ade_ledger` crate prefix. The RED `ade_ledger::consensus_input_extract`, the GREEN `ade_ledger::mempool::policy`, and the GREEN `ade_ledger::mempool::canonicalize` sit inside the BLUE `ade_ledger` crate but carry a different color by their own module doc-comment and the cluster TCB Color Maps — they are surfaced as sub-classification notes inside the `ade_ledger` entry. The `ade_testkit::mempool` sub-tree (PHASE4-N-E S2), the `ade_testkit::governance` sub-tree (PROPOSAL-PROCEDURES-DECODE PP-S2), and the `ade_testkit::producer` sub-tree (PHASE4-N-C) sit inside the GREEN `ade_testkit` crate, classified GREEN by their own module doc-comments and the cluster TCB Color Maps. **PHASE4-N-G — `ade_runtime::network` sub-tree** sits inside the RED `ade_runtime` crate and hosts the per-peer N2N **server** session driver (RED). **PHASE4-N-H — `ade_runtime::receive` sub-tree** sits inside the RED `ade_runtime` crate and hosts the GREEN+RED receive-side glue: `events_to_state` (GREEN), `in_memory_chain_write` (GREEN), `orchestrator` (RED). **NEW — PHASE4-N-I — `ade_runtime::rollback` sub-tree** sits inside the RED `ade_runtime` crate and hosts the GREEN+RED rollback adapter glue: `cadence` (GREEN; `should_snapshot_after_block` + `SnapshotCadence`), `in_memory_cache` (GREEN; `InMemorySnapshotCache` impl `SnapshotReader`), `chaindb_block_source` (GREEN; `ChainDbBlockSource<'a, D: ChainDb>` impl `BlockSource`), `snapshot_writer` (RED; `maybe_capture_snapshot` post-admission hook).
- Modules are listed by TCB color (BLUE → GREEN → RED), alphabetical within each color.
- TCB color sources, in order of authority:
  1. `.idd-config.json` `core_paths` — substring match against absolute path. BLUE matches: `ade_codec`, `ade_types`, `ade_crypto`, `ade_core`, `ade_ledger` (covers the new `ade_ledger::rollback` sub-tree), `ade_plutus`, and the 9 `ade_network` submodule paths (`mux/frame.rs`, `codec/`, `handshake/`, `chain_sync/`, `block_fetch/`, `tx_submission/`, `keep_alive/`, `peer_sharing/`, `n2c/`).
  2. `.idd-config.json` `_core_paths_doc` — `ade_runtime` is RED; `ade_testkit` is GREEN; `ade_node` is RED; `ade_network::mux::transport` and `ade_network::session` are RED; `ade_network::lib` and `ade_network::mux::mod` are GREEN barrels. `ade_core_interop` is **RED** per its `Cargo.toml` header comment and the PHASE4-N-B TCB Color Map; the `ade_core_interop::tx_submission` (S4) and `ade_core_interop::local_tx_submission` (S5) modules carry a GREEN sub-classification by their own module doc-comments. The RED binaries `ade_core_interop::bin::{live_tx_submission_session, live_block_production_session, live_block_fetch_session, live_block_follow_session}` are the operator-action live evidence pattern (RED by their own file-header comments and the established `live_consensus_session` precedent).
  3. `docs/clusters/completed/PHASE4-N-B/cluster.md` § "TCB Color Map" — `era_schedule`, `praos_state`, `vrf_cert`, `nonce`, `op_cert`, `leader_schedule`, `header_validate`, `fork_choice`, `rollback` (the consensus-side header rollback applier, not the new ledger-side `ade_ledger::rollback`) are BLUE; `chain_selector`, `candidate_fragment` are GREEN; `genesis_parser` is RED; `ade_testkit::consensus` is GREEN; `ade_core_interop` is RED.
  4. `docs/clusters/completed/PHASE4-B1/cluster.md` § "TCB Color Map" — `ade_ledger::consensus_view`, `ade_ledger::block_validity` are BLUE; `ade_core::consensus::{header_validate, kes_check}` extensions are BLUE; `ade_testkit::validity` is GREEN; `ade_ledger::consensus_input_extract` is RED.
  5. PHASE4-B2 (tx-validity agreement) — `ade_ledger::tx_validity::*` and `ade_ledger::mempool::admit` are BLUE; `ade_ledger::mempool::policy` is GREEN (Tier-5); `ade_testkit::tx_validity` is GREEN.
  6. PHASE4-B3/B5 (tx value-conservation + Conway governance-cert accumulation) — all new/changed modules are BLUE under the already-BLUE crate prefixes.
  7. PHASE4-N-E (mempool wire ingress) — `ade_ledger::mempool::ingress` is BLUE (S1); `ade_ledger::mempool::canonicalize` is GREEN (S3); `ade_testkit::mempool::ingress_replay` is GREEN (S2); `ade_core_interop::tx_submission` is GREEN (S4); `ade_core_interop::local_tx_submission` is GREEN (S5); `ade_core_interop::bin::live_tx_submission_session` is RED (S6).
  8. PROPOSAL-PROCEDURES-DECODE — `ade_codec::conway::governance` is BLUE; the new `ProposalProcedure` canonical type in `ade_types::conway::governance` is BLUE; `ade_testkit::governance::proposal_procedures_replay` is GREEN.
  9. PHASE4-N-C — `ade_ledger::producer::{forge, self_accept, state}` BLUE; `ade_ledger::block_body_hash` BLUE; `ade_codec::shelley::{opcert, tx_components}` BLUE; `ade_core::consensus::opcert_validate` BLUE; `ade_crypto::kes::KesSignature` BLUE; `ade_runtime::producer::{signing, keys, scheduler, broadcast}` RED; `ade_runtime::producer::tick_assembler` GREEN; `ade_testkit::producer::*` GREEN; `ade_core_interop::bin::live_block_production_session` RED.
  10. PHASE4-N-G — `ade_network::chain_sync::server` and `ade_network::block_fetch::server` BLUE; `ade_ledger::producer::served_chain` BLUE; the `accepted_block_header_bytes` accessor on `ade_ledger::block_validity::header_input` BLUE; `ade_runtime::producer::{broadcast_to_served, served_chain_lookups}` GREEN; `ade_runtime::network::n2n_server` RED; `ade_core_interop::bin::live_block_fetch_session` RED.
  11. `docs/clusters/completed/PHASE4-N-H/cluster.md` § "TCB Color Map" — `ade_ledger::receive::{admitted, chain_write, events, pending_header_cache, reducer}` are BLUE; `ade_runtime::receive::{events_to_state, in_memory_chain_write}` are GREEN; `ade_runtime::receive::orchestrator` is RED; `ade_core_interop::bin::live_block_follow_session` is RED.
  12. **NEW — `docs/clusters/completed/PHASE4-N-I/cluster.md` § "TCB Color Map"** — `ade_ledger::rollback::{traits, error, materialize, commit}` (S1, S2, S3) are **BLUE** (under the already-BLUE `ade_ledger` crate prefix; closure shape gated mechanically by `ci/ci_check_rollback_materialize_closure.sh`); the `ChainDbWrite::rollback_to_slot(slot)` trait method extension on `ade_ledger::receive::chain_write` (S3) is **BLUE** (preserves the admission-gate-shape extension into the rollback path); the `RollbackContext<'a>` struct + `receive_apply`'s new `rollback_ctx: Option<&RollbackContext>` parameter + the new `roll_backward` helper in `ade_ledger::receive::reducer` (S6) are **BLUE** (under the already-BLUE `ade_ledger::receive` sub-tree; closure shape gated by the existing `ci/ci_check_receive_reducer_closure.sh`); `ade_runtime::rollback::{cadence, in_memory_cache, chaindb_block_source}` (S4) are **GREEN** (pure deterministic glue + thin adapters; cadence-purity + cache-purity gated mechanically by `ci/ci_check_snapshot_cadence_purity.sh`); `ade_runtime::rollback::snapshot_writer` (S5) is **RED** (post-admission write hook composing RED dispatch outcomes with the GREEN cadence + cache — pure decision logic but RED-shaped by location and file-header banner). The 5 new integration tests in `crates/ade_runtime/tests/receive_rollback_integration.rs` (S6) are GREEN test scaffolding; no new operator-action live binary was added by N-I (the rollback-side has no separate cross-impl live evidence beyond the existing N-H receive-side pass).

- **Active cluster at HEAD (none).** PHASE4-N-I (in-memory snapshot + replay-forward rollback) is **closed at HEAD `75f75da`** and archived under `docs/clusters/completed/PHASE4-N-I/`. The cluster closes the PHASE4-N-H deferred `open_obligation` on `DC-CONS-20` (ChainDb-ledger-chain_dep lockstep — rollback-side half). The cluster's new registry rules at `status = "enforced"`: `CN-STORE-07` (single materialize authority), `DC-CONS-22` (replay-forward correctness), `DC-STORE-07` (snapshot cadence determinism). `DC-CONS-20` flips to `enforced` (the `open_obligation` is removed; both admit-side and rollback-side halves are now mechanically enforced). **DC-CONS-21 (snapshot encode/decode round-trip equivalence) stays `declared` with a new `open_obligation = "persistent_ledger_snapshot_encoding_follow_on_cluster"`** — the cluster's explicit scope decision (2026-05-26) was to ship the in-memory variant only and carve out the persistent on-disk encoder to a follow-on cluster (≈1500-2000 LoC of canonical LedgerState field-walk code; too large to ship in one slice without partial-implementation risk). Existing rules strengthened in PHASE4-N-I: `DC-CONS-20` (admit→admit+rollback closure, `strengthened_in` gains `PHASE4-N-I`). Existing operator-action `open_obligation`s carried unchanged from N-H close: `CN-CONS-06` (live producer stake; via N-C), `RO-LIVE-01` (live block-fetch evidence; via N-G), `RO-LIVE-02` (live cross-impl follow evidence; via N-H). The pre-existing `ade_network::session` placeholder gap is untouched. `docs/clusters/` currently contains only the archive of the PHASE4-N-B follow-on (CE-N-B-6 manual evidence still pending operator log) and the closed `completed/` set (13 directories: COMMITTEE-CRED-FIDELITY, DREP-VOTE-FIDELITY, ENACTMENT-COMMITTEE-FIDELITY, ENACTMENT-COMMITTEE-WRITEBACK, OQ5-CREDENTIAL-FIDELITY, PHASE4-B1..B5, PHASE4-B3F, PHASE4-N-A, N-B, N-C, N-D, N-E, N-G, N-H, **N-I (NEW)**, PROPOSAL-PROCEDURES-DECODE).

- **Delta since prior CODEMAP HEAD `efe1fb9` (this regeneration — PHASE4-N-I cluster, 6 slices S1–S6 over commits `0e7e9ee..75f75da`).**

  **Structural deltas summarized:**
  - **NEW BLUE sub-tree:** `ade_ledger::rollback/` (S1, S2, S3). Five files: `mod.rs` (32 LOC barrel — `pub use` re-exports `commit_rollback`, `CommitRollbackError`, `MaterializeError`, `materialize_rolled_back_state`, `TargetPoint`, `BlockSource`, `SnapshotReader`); `traits.rs` (88 LOC + 2 inline tests — `SnapshotReader` trait with single method `nearest_le(target_slot) -> Option<(SlotNo, LedgerState, PraosChainDepState)>` and `BlockSource` trait with single method `blocks_in_range(from_exclusive, to_inclusive) -> Vec<(SlotNo, Vec<u8>)>` — narrow read-only seams; production impls live in `ade_runtime::rollback` as GREEN adapters); `error.rs` (98 LOC + 2 inline tests — closed `MaterializeError` sum with exactly three variants `RollbackTooDeep { target_slot, oldest_snapshot }` / `ReplayFailedAt { slot, error: BlockValidityError }` / `EraNotSupported { era, slot }`, no `String`, no `#[non_exhaustive]`; closed `CommitRollbackError` sum with one variant `ChainDb(ChainWriteError)`); `materialize.rs` (402 LOC + 5 inline tests — `TargetPoint { slot, hash }`, `materialize_rolled_back_state(target, &dyn SnapshotReader, &dyn BlockSource, &EraSchedule, &dyn LedgerView) -> Result<(LedgerState, PraosChainDepState), MaterializeError>` — **the sole `pub fn` in the project returning `(LedgerState, PraosChainDepState)`** per CN-STORE-07, enforced by `ci_check_rollback_materialize_closure.sh`'s repo-wide grep; composes one `SnapshotReader::nearest_le` lookup + replay-forward fold over `block_validity::transition::block_validity` (the same admission authority N-H's receive bridge uses, per CN-CONS-08 single-admission-authority); pure, total, deterministic; supported eras are Babbage + Conway, pre-Conway returns `EraNotSupported`); `commit.rs` (200 LOC + 3 inline tests — `commit_rollback<W: ChainDbWrite>(state, target, new_ledger, new_chain_dep, chain_write) -> Result<(), CommitRollbackError>` — atomic state replacement helper with the irreversible step first: 1. `chain_write.rollback_to_slot(target.slot)` (irreversible; failure leaves state unchanged), 2. `state.ledger = new_ledger`, 3. `state.chain_dep = new_chain_dep`, 4. `state.pending_headers = PendingHeaderCache::new()` — staged-then-committed shape preserves failure-atomicity).
  - **`ade_ledger::receive::chain_write` extension (S3, BLUE — non-breaking trait addition).** The `ChainDbWrite` trait gains a second method `fn rollback_to_slot(&mut self, slot: SlotNo) -> Result<(), ChainWriteError>` alongside the existing `fn write_admitted(&mut self, AdmittedBlock)`. The trait still takes the admission-gate-shape AdmittedBlock by value on `write_admitted`; the new method takes a `SlotNo` for the rollback target. Closed `ChainWriteError` sum unchanged. The trait impl on `ade_runtime::receive::in_memory_chain_write::ChainDbWriter` (GREEN, S3 extension) was extended to implement the new method via `ChainDb::rollback_to_slot` over the underlying chain store. One new inline test in `chain_write.rs` exercises the trait surface.
  - **`ade_ledger::receive::reducer` extension (S6, BLUE — additive `RollbackContext`-gated branch).** Three additions: (a) new `RollbackContext<'a> { snapshot_reader: &'a dyn rollback::SnapshotReader, block_source: &'a dyn rollback::BlockSource }` BLUE struct (does not own state — purely a borrow bundle the orchestrator hands the reducer for the RollBackward arm). (b) `receive_apply` signature extended with a new sixth parameter `rollback_ctx: Option<&RollbackContext>` (additive — `None` preserves the pre-N-I behavior of returning `Err(ReceiveError::RollbackOutOfScope)`, so all existing N-H test sites still compile/pass; `Some(ctx)` activates the new branch). (c) new private `roll_backward<W: ChainDbWrite>(state, target_point, chain_write, era_schedule, ledger_view, ctx) -> Result<ReceiveEffect, ReceiveError>` helper that composes `materialize_rolled_back_state` (BLUE driver) + `commit_rollback` (BLUE atomic helper); on success returns `Ok(ReceiveEffect::RolledBack { to_slot: target_point.slot })`; on `MaterializeError::RollbackTooDeep` returns `Err(ReceiveError::RollbackOutOfScope)` (preserves the receive-side error shape so callers don't need to handle a new variant), other materialize errors propagate via existing `Validity`/`ChainDb` variants. The closed `ReceiveEffect` sum already had the `RolledBack { to_slot: SlotNo }` variant defined in N-H — no enum widening. **DC-CONS-20 closes here**: the receive bridge's RollBackward arm now atomically rolls ChainDb + LedgerState + PraosChainDepState + pending_headers to the same slot. Eight new inline tests in `reducer.rs` exercise the new arm + the legacy `None`-context path.
  - **NEW GREEN+RED sub-tree (inside RED `ade_runtime`):** `ade_runtime::rollback/` (S4, S5). Five files: `mod.rs` (26 LOC barrel; `pub use` re-exports `should_snapshot_after_block`, `SnapshotCadence`, `ChainDbBlockSource`, `InMemorySnapshotCache`, `maybe_capture_snapshot`); `cadence.rs` (143 LOC + 5 inline tests, **GREEN**; `SnapshotCadence { every_n_blocks: u32 }` — single BLUE-structural field, `Default = 100`; `should_snapshot_after_block(slot, block_no, cadence, last_snapshot) -> bool` — pure decision function: returns true iff `block_no % cadence.every_n_blocks == 0 && last_snapshot.is_none_or(|last| last < slot)`; **DC-STORE-07: operator-tunable cadence is explicitly out of scope** per the cluster's scope decisions — pinned at the type level by the single-field discipline enforced by `ci_check_snapshot_cadence_purity.sh`); `in_memory_cache.rs` (165 LOC + 4 inline tests, **GREEN**; `InMemorySnapshotCache { entries: BTreeMap<SlotNo, (LedgerState, PraosChainDepState)> }` — BTreeMap-backed canonical iteration; methods `new`, `admit(slot, ledger, chain_dep)`, `capture_from(slot, &ReceiveState)`, `len`, `is_empty`, `oldest`, `most_recent`, `slots`, `iter_for_test`; `impl SnapshotReader for InMemorySnapshotCache` clones owned state on lookup); `chaindb_block_source.rs` (113 LOC + 3 inline tests, **GREEN**; `ChainDbBlockSource<'a, D: ChainDb> { pub db: &'a D }` — `impl<'a, D: ChainDb> BlockSource for ChainDbBlockSource<'a, D>`: `blocks_in_range(from_exclusive, to_inclusive)` calls `ChainDb::iter_from_slot(from_exclusive + 1)` and collects until iterator yields slot > to_inclusive; inclusive on upper, exclusive on lower per the BLUE trait contract); `snapshot_writer.rs` (146 LOC + 4 inline tests, **RED**; `maybe_capture_snapshot(&mut InMemorySnapshotCache, SnapshotCadence, &ReceiveEffect, &ReceiveState) -> bool` — post-admission hook: returns false on non-`Admitted` effects or when `state.chain_dep.last_block_no` is None or when cadence does not elect to snapshot; on a positive decision calls `cache.capture_from(slot, state)` and returns true; pure decision + in-memory write, no I/O, no clock).
  - **NEW integration tests under `ade_runtime/tests/`:** `receive_rollback_integration.rs` (S6, 445 LOC, **5 tests** — `rollback_branch_returns_rolled_back_on_in_memory_snapshot` (DC-CONS-20 happy path with a snapshot below the target slot, replay-forward across one Conway corpus block), `rollback_branch_returns_rollback_too_deep_when_no_snapshot` (closed error path), `rollback_branch_state_unchanged_on_materialize_failure` (failure-atomicity property), `rollback_branch_without_ctx_returns_legacy_rollback_out_of_scope` (preserves the pre-N-I N-H N-default behavior — `rollback_ctx: None`), `rollback_then_continue_admit_equals_straight_line_admit` (end-to-end: rollback + continue equals direct apply; the integration counterpart of DC-CONS-22)).
  - **NEW closed canonical types (+4 BLUE).** Per source file: `MaterializeError`, `CommitRollbackError` (in `ade_ledger::rollback::error`); `TargetPoint` (in `ade_ledger::rollback::materialize`); `RollbackContext<'a>` (in `ade_ledger::receive::reducer`). The three GREEN types `SnapshotCadence`, `InMemorySnapshotCache`, `ChainDbBlockSource<'a, D: ChainDb>` live inside the RED `ade_runtime` crate prefix and are not counted in the BLUE canonical-type total. The two BLUE traits `SnapshotReader` and `BlockSource` are pub traits (not structs/enums) and are not counted by the `^(pub )?(struct|enum) ` grep — both contribute to the authority surface but not to the canonical-type fingerprint. **Canonical type count: 417 → 421 (+4).** Per-BLUE-crate breakdown at HEAD: `ade_codec` 10, `ade_types` 81, `ade_crypto` 13, `ade_core` 44, `ade_ledger` **156 (+4)**, `ade_plutus` 8, BLUE `ade_network` 109 (5+38+9+11+10+5+5+5+21) = 421.
  - **NEW CI gates (+2):** `ci/ci_check_rollback_materialize_closure.sh` (S2; CN-STORE-07 + DC-CONS-22; scopes `crates/ade_ledger/src/rollback/materialize.rs` for production-code purity — no `HashMap`/`HashSet`/wall-clock/`tokio`/`rand`; **CN-STORE-07 closure scan over the whole rollback module tree** — only one `pub fn` in `crates/ade_ledger/src/rollback/*.rs` returns the signature `(LedgerState, PraosChainDepState)` (the function name is `materialize_rolled_back_state`); positive grep for `block_validity` call site — the single admission authority CN-CONS-08); `ci/ci_check_snapshot_cadence_purity.sh` (S4; DC-STORE-07; scopes `crates/ade_runtime/src/rollback/*.rs` — no `HashMap`/`HashSet`/wall-clock/`tokio`/`rand`; **SnapshotCadence struct-body field-count = 1** with field name `every_n_blocks` — operator-tunable cadence pinned out of scope at the type level; positive grep for `pub fn should_snapshot_after_block` + `impl SnapshotReader for InMemorySnapshotCache` + `impl<...> BlockSource for ChainDbBlockSource`). **CI script count: 52 → 54 (+2).**
  - **NEW registry rules / strengthenings:** 4 new entries — `CN-STORE-07` (enforced, single materialize authority for rolled-back state), `DC-CONS-22` (enforced, replay-forward correctness — snapshot+replay-forward fingerprint-equals-direct-apply), `DC-STORE-07` (enforced, snapshot cadence determinism — pure function of `(slot, block_no, cadence_params, last_snapshot)`, BLUE-structural cadence not operator-tunable), `DC-CONS-21` (declared, snapshot encode/decode round-trip equivalence — `open_obligation = "persistent_ledger_snapshot_encoding_follow_on_cluster"` — explicit scope decision: the in-memory rollback variant ships in N-I; the on-disk canonical encoder is carved out to a follow-on cluster). `DC-CONS-20` (already declared in N-H) **flips from `declared` (with `open_obligation = "rollback_side_blocked_until_ledger_snapshot_cluster"`) to `enforced`** — the open_obligation is removed; `strengthened_in` gains `PHASE4-N-I`. **Total registry rules: 202 → 206 (+4).**
  - **NEW test inventory (+33).** Per-crate at HEAD: `ade_codec` 162 (unchanged), `ade_types` 23 (unchanged), `ade_crypto` 51 (unchanged), `ade_core` 126 (unchanged), `ade_ledger` 522 → **534 (+12)** (rollback inline: traits 2 + error 2 + materialize 5 + commit 3 = 12; chain_write +1 → covered by single absorbed test plus existing carry; receive::reducer +8 absorbed by N-I S6 — note the N-I reducer additions and the N-I chain_write addition together total 12 new tests landing under the `ade_ledger` crate header, mapping cleanly to 12 inline tests across the new rollback files + reducer/chain_write extensions — see below), `ade_plutus` 28 (unchanged), `ade_testkit` 312 (unchanged), `ade_runtime` 100 → **121 (+21)** (rollback inline: cadence 5 + in_memory_cache 4 + chaindb_block_source 3 + snapshot_writer 4 = 16; integration `tests/receive_rollback_integration.rs` 5 = 21), `ade_network` 200 (unchanged), `ade_node` 0, `ade_core_interop` 27 (unchanged). **Test inventory 1551 → 1584 (+33)**, reported approximate per the template's fallback rule (test runner not executed; counts from `grep -cE "#\[test\]|#\[tokio::test\]"` workspace-wide).
  - **No new crate-level outbound edges.** PHASE4-N-I reuses the established `ade_runtime → {ade_ledger, ade_core, ade_codec, ade_types, ade_crypto}` and `ade_ledger → {ade_core, ade_codec, ade_types, ade_crypto, ade_plutus, minicbor}` sets. The new `ade_runtime::rollback::*` files import `ade_ledger::rollback::*` + `ade_ledger::receive::ReceiveState`/`ReceiveEffect` + `ade_ledger::state::LedgerState` + `ade_core::consensus::praos_state::PraosChainDepState` + `crate::chaindb::{ChainDb, ChainDbError, StoredBlock}` over existing edges. The new BLUE `ade_ledger::rollback::*` files import `ade_core::consensus::{era_schedule, ledger_view, praos_state}` + `ade_codec::cbor::envelope::decode_block_envelope` + `ade_types::{CardanoEra, Hash32, SlotNo}` over existing edges.

- Counts:
  - Crates: 11, from `Cargo.toml` `[workspace] members`. Unchanged.
  - Canonical types: 421, from `grep -rE "^(pub )?(struct|enum) "` across the full BLUE scope. Breakdown: `ade_codec` 10, `ade_types` 81, `ade_crypto` 13, `ade_core` 44, `ade_ledger` **156 (+4)**, `ade_plutus` 8, plus the 9 BLUE `ade_network` submodule paths 109. Registry `canonical_type_registry: null`, so a structural count is used. **+4 since `efe1fb9`** — N-I rollback closed-error sums + TargetPoint + RollbackContext.
  - Tests: 1584 — count of `#[test]` / `#[tokio::test]` attributes across `crates/`. Reported as approximate per the template's fallback rule. **+33 since `efe1fb9`**, breakdown above.
  - CI checks: 54 — file count under `ci/ci_check_*.sh`. **+2 since `efe1fb9`** — `ci_check_rollback_materialize_closure.sh`, `ci_check_snapshot_cadence_purity.sh` (both PHASE4-N-I). Full inventory at HEAD: `admitted_block_closure`, `block_fetch_server_closure`, `broadcast_to_served_purity`, `cbor_round_trip`, `ce_n_a_5_proof`, `chaindb_contract`, `chaindb_crash_safety`, `chain_sync_server_closure`, `consensus_closed_enums`, `constitution_coverage`, `conway_cert_classification_closed`, `credential_discriminant_closed`, `crypto_vectors`, `dependency_boundary`, `deposit_param_authority`, `differential_divergence`, `forbidden_patterns`, `forge_purity`, `gov_cert_accumulation_closed`, `hash_uses_wire_bytes`, `hfc_translation`, `ingress_chokepoints`, `ledger_determinism`, `mempool_ingress_closure`, `mempool_ingress_replay`, `module_headers`, `n2n_server_no_signing_dep`, `no_async_in_blue`, `no_chaindb_in_consensus_blue`, `no_density_in_fork_choice`, `no_float_in_consensus`, `no_parallel_header_splitter`, `no_private_keys_in_corpus`, `no_producer_body_encoder`, `no_secrets`, `no_semantic_cfg`, `no_signing_in_blue`, `opcert_closed`, `pallas_quarantine`, `private_key_custody`, `producer_corpus_present`, `proposal_procedures_closed`, `receive_orchestrator_no_producer_dep`, `receive_paths_corpus_present`, `receive_reducer_closure`, `receive_replay_purity`, `recovery_contract`, `ref_provenance`, `rollback_materialize_closure` *(NEW)*, `scheduler_closure`, `self_accept_gate`, `served_chain_closure`, `server_paths_corpus_present`, `snapshot_cadence_purity` *(NEW)*. The forward-looking `ci/ci_check_no_fail_open_in_validation.sh` (the `if len == K { check } else { skip }` grep gate, DC-VAL-06 / DC-TXV) is still **not** shipped. No `.github/workflows/` yet.

---

## BLUE Modules — Pure Functional Core

> **Shared header (applies to every BLUE entry below).** Every `.rs` source file begins with the contract banner
> `// Core Contract:` and the following deny attributes are present in each crate's `lib.rs` (or, for `ade_network`,
> at the crate root — the BLUE submodules inherit them):
> `#![deny(unsafe_code)]`, `#![deny(clippy::unwrap_used)]`, `#![deny(clippy::expect_used)]`,
> `#![deny(clippy::panic)]`, `#![deny(clippy::float_arithmetic)]`.
> CI scripts that enforce the shared rules (all 7 scope the full BLUE set — the 6 BLUE crates plus the 9 BLUE `ade_network` submodule paths declared in `.idd-config.json` `core_paths`):
> - `ci/ci_check_module_headers.sh` — banner first-line check.
> - `ci/ci_check_forbidden_patterns.sh` — `HashMap`, `HashSet`, `IndexMap`/`IndexSet`/`indexmap::`, `SystemTime`, `Instant`, `std::fs`, `std::net`, `tokio`, `async fn`, `f32`/`f64`, `anyhow`, `rand::thread_rng`, `thread::spawn`, plus `unsafe` outside a documented allowlist.
> - `ci/ci_check_dependency_boundary.sh` — no BLUE crate depends on a RED crate.
> - `ci/ci_check_no_signing_in_blue.sh` — `SigningKey`/`SecretKey`/`PrivateKey`/`private_key`/`sign_message`/`sign_block` forbidden in BLUE.
> - `ci/ci_check_no_semantic_cfg.sh` — `#[cfg(feature = ...)]` and `cfg!(feature = ...)` forbidden in BLUE.
> - `ci/ci_check_hash_uses_wire_bytes.sh` — no hashing of `canonical_bytes` / re-encoded bytes in BLUE.
> - `ci/ci_check_ingress_chokepoints.sh` — only named `decode_*` chokepoints construct `PreservedCbor`.
> - `ci/ci_check_pallas_quarantine.sh` — `pallas-*` references confined to `ade_plutus`.
> - `ci/ci_check_no_async_in_blue.sh` *(PHASE4-N-A, S-A1)* — async constructs forbidden anywhere in the BLUE scope. Enforces DC-CORE-01.
>
> Three additional CI scripts narrow the shared header to the `ade_core::consensus` tree (PHASE4-N-B): `ci/ci_check_no_chaindb_in_consensus_blue.sh`, `ci/ci_check_no_float_in_consensus.sh`, `ci/ci_check_consensus_closed_enums.sh` (TARGETS scope: `crates/ade_core/src/consensus/`, `crates/ade_ledger/src/block_validity/`, `crates/ade_ledger/src/tx_validity/`, `crates/ade_ledger/src/mempool/`).
> A fourth narrow check enforces a single fork-choice rule: `ci/ci_check_no_density_in_fork_choice.sh` (DC-CONS-03).
> A fifth narrow check (PHASE4-B3) enforces deposit-parameter authority: `ci/ci_check_deposit_param_authority.sh` (DC-TXV-07).
> A sixth narrow check (PHASE4-B5) enforces gov-cert accumulation closure: `ci/ci_check_gov_cert_accumulation_closed.sh` (DC-LEDGER-09).
> A seventh narrow check (OQ5/COMMITTEE/DREP/ENACTMENT-COMMITTEE-FIDELITY + ENACTMENT-COMMITTEE-WRITEBACK) enforces credential-discriminant closure: `ci/ci_check_credential_discriminant_closed.sh` (DC-LEDGER-10).
> An eighth narrow check (PROPOSAL-PROCEDURES-DECODE, PP-S1) enforces proposal_procedures closure: `ci/ci_check_proposal_procedures_closed.sh` (DC-LEDGER-11).
>
> **Eight narrow checks added by PHASE4-N-C (the producer-authority gate set):**
> - `ci/ci_check_forge_purity.sh` (DC-CONS-13/14/15) — forge body purity.
> - `ci/ci_check_no_producer_body_encoder.sh` (DC-CONS-16) — single body-hash authority, repo-wide.
> - `ci/ci_check_no_private_keys_in_corpus.sh` (DC-CRYPTO-03/04/05 corpus discipline) — producer + (N-G S5 extension) server-paths corpora private-key-free.
> - `ci/ci_check_opcert_closed.sh` (DC-CONS-11/12) — closed opcert encoders + acceptance authority.
> - `ci/ci_check_private_key_custody.sh` (DC-CRYPTO-03/04/05 + OP-OPS-04) — signing primitives RED-confined.
> - `ci/ci_check_producer_corpus_present.sh` (CN-CONS-06 mechanical half) — fixture-presence guard.
> - `ci/ci_check_scheduler_closure.sh` (DC-CONS-13 + DC-MEM-03 + OP-OPS-05) — RED scheduler purity + BroadcastQueue value-take.
> - `ci/ci_check_self_accept_gate.sh` (CN-CONS-07) — type-level broadcast-gate guards.
>
> **Seven narrow checks added by PHASE4-N-G (the producer-side server-role gate set):**
> - `ci/ci_check_no_parallel_header_splitter.sh` (DC-CONS-16 strengthening + DC-CONS-18, S1) — repo-wide grep gate: the only `pub fn .*header_bytes` site is `accepted_block_header_bytes` in `crates/ade_ledger/src/block_validity/header_input.rs`.
> - `ci/ci_check_served_chain_closure.sh` (CN-CONS-07 strengthening + CE-N-G-2, S2) — scopes `crates/ade_ledger/src/producer/served_chain.rs`.
> - `ci/ci_check_chain_sync_server_closure.sh` (DC-PROTO-08, S3) — scopes `crates/ade_network/src/chain_sync/server.rs`.
> - `ci/ci_check_block_fetch_server_closure.sh` (DC-CONS-17 enforcement foundation, S4) — scopes `crates/ade_network/src/block_fetch/server.rs`.
> - `ci/ci_check_broadcast_to_served_purity.sh` (CE-N-G-5, S5) — scopes the GREEN adapter pair under `ade_runtime::producer/`.
> - `ci/ci_check_n2n_server_no_signing_dep.sh` (S6 key-boundary doctrine) — scopes `crates/ade_runtime/src/network/`.
> - `ci/ci_check_server_paths_corpus_present.sh` (S7 presence) — checks the two integration tests + binary src + Cargo.toml entry + procedure doc.
>
> **Four narrow checks added by PHASE4-N-H (the receive-side authority gate set):**
> - `ci/ci_check_admitted_block_closure.sh` (S1; CE-N-H-1 + CN-PROTO-07) — repo-wide grep on `AdmittedBlock` construction + events.rs closure.
> - `ci/ci_check_receive_reducer_closure.sh` (S2; CE-N-H-2 + CN-CONS-08 + DC-CONS-19) — scopes `crates/ade_ledger/src/receive/reducer.rs`.
> - `ci/ci_check_receive_replay_purity.sh` (S3; CE-N-H-3 + DC-PROTO-09) — scopes `crates/ade_runtime/src/receive/{events_to_state,in_memory_chain_write}.rs`.
> - `ci/ci_check_receive_orchestrator_no_producer_dep.sh` (S4; key-boundary) — scopes `crates/ade_runtime/src/receive/`.
> - `ci/ci_check_receive_paths_corpus_present.sh` (S6; presence) — checks the three N-H integration tests + binary src + Cargo.toml entry + procedure doc.
>
> **NEW — Two narrow checks added by PHASE4-N-I (the rollback authority gate set):**
> - `ci/ci_check_rollback_materialize_closure.sh` (S2; CN-STORE-07 + DC-CONS-22) — scopes `crates/ade_ledger/src/rollback/materialize.rs` for production purity (no `HashMap`/`HashSet`/wall-clock/`tokio`/`rand`); **CN-STORE-07 single-authority scan over `crates/ade_ledger/src/rollback/*.rs`** — exactly one `pub fn` whose signature contains `(LedgerState[^{]*PraosChainDepState` (the function name is `materialize_rolled_back_state`); positive grep for `block_validity` call site (CN-CONS-08 single admission authority for the underlying step) and for `pub fn materialize_rolled_back_state` presence.
> - `ci/ci_check_snapshot_cadence_purity.sh` (S4; DC-STORE-07) — scopes `crates/ade_runtime/src/rollback/*.rs` for production purity (no `HashMap`/`HashSet`/wall-clock/`tokio`/`rand`); **SnapshotCadence struct-body single-field discipline** — exactly one `pub <type>:` field in the struct body, field name must contain `every_n_blocks` (operator-tunable cadence pinned out of scope at the type level); positive presence of `pub fn should_snapshot_after_block`, `impl SnapshotReader for InMemorySnapshotCache`, `impl<...> BlockSource for ChainDbBlockSource`.
>
> Two checks narrow the shared header to the wire-ingress path of `ade_ledger::mempool` (PHASE4-N-E):
> - `ci/ci_check_mempool_ingress_closure.sh` (DC-MEM-03, S1).
> - `ci/ci_check_mempool_ingress_replay.sh` (DC-MEM-04, S2 + S3).
>
> A BLUE crate or BLUE `ade_network` submodule that adds a feature flag, an async function, a `HashMap`, or a RED dep fails CI on push.
> The 3 RED-scope CI scripts (`ci_check_chaindb_contract.sh`, `ci_check_recovery_contract.sh`, `ci_check_chaindb_crash_safety.sh`) and the 1 evidence script `ci_check_ce_n_a_5_proof.sh` are not part of this shared header — they are documented in the cross-module CI matrix at the bottom.

---

### `ade_codec`

> **Status carried from prior CODEMAP — unchanged at HEAD `75f75da`.** PHASE4-N-I did not touch `ade_codec`. The receive-side reducer's S6 RollBackward arm uses `decode_block_envelope` for replay-forward era detection (already a heavy site since N-H). The N-G / N-C / N-H narrative carries forward.

| Attribute | Value |
|-----------|-------|
| **Purpose** | Owns Cardano-canonical CBOR ingress: the only place in the workspace that turns raw bytes into typed semantic values, with wire-byte preservation for every hash-bearing structure. Also owns the standalone opcert byte authority (`shelley::opcert::{encode_opcert, decode_opcert}`) and the canonical Conway-tx preserved-byte splitter (`shelley::tx_components::split_conway_tx_components` — reused by `ade_ledger::producer::forge` and the body-hash recomputation path). |
| **Creates** | `PreservedCbor<T>`, `RawCbor`, `BlockEnvelope`, `ByronDecodedBlock`, `CodecContext`, `CodecError` (incl. `UnknownCertTag`, `DuplicateMapKey`, `TrailingBytes`, `InvalidCborStructure`), `ContainerEncoding`, `IntWidth`, plus era-tagged block/tx wrappers under `byron/`, `shelley/`, `allegra/`, `mary/`, `alonzo/`, `babbage/`, `conway/`. Functions: `conway::cert::{decode_conway_certs, decode_drep}`, `conway::withdrawals::{decode_withdrawals, withdrawals_sum}`, `conway::governance::{decode_proposal_procedures, encode_proposal_procedures}`, `shelley::cert::read_pool_registration_cert`, both era `decode_stake_credential`, `shelley::opcert::{encode_opcert, decode_opcert, write_opcert_fields_into}`, `shelley::tx_components::split_conway_tx_components`. Types: `OpCertCodecError`, `TxComponents<'a>`. |
| **Interprets** | All canonical Cardano CBOR — block envelopes, era-specific blocks, tx bodies, tx outs, certificates, addresses. Conway certificate array (closed over CDDL tags 0..18, owner-complete) and Conway withdrawals map (deduplicated). Both era credential decoders preserve the key/script tag. Sole authority for `PreservedCbor::new` (constructor is `pub(crate)`). CIP-1694 `proposal_procedure` array. Standalone cardano-cli `OperationalCertificate` 4-tuple. Conway transaction 4-tuple (preserved-byte slicing only). |
| **MUST NOT** | (1) Construct `PreservedCbor` outside `ade_codec` (`pub(crate)` + `ci_check_ingress_chokepoints.sh`). (2) Re-encode wire bytes when computing hashes (`ci_check_hash_uses_wire_bytes.sh`). (3) Use any forbidden BLUE pattern. (4) Depend on any other workspace crate except `ade_types`. (5) `conway::cert` (DC-LEDGER-08) — no unknown-tag swallow; owner-complete; no catch-all. (6) `conway::withdrawals` — no last-wins on duplicate `RewardAccount`. (7) `decode_stake_credential` (DC-LEDGER-10) — must not erase the credential tag. (8) `conway::governance` (DC-LEDGER-11) — no silent skip on unknown `GovAction`; no opaque pass-through at body codec key 20. (9) `shelley::opcert` (DC-CONS-11/12) — cardano-byte-identical 4-tuple; dedicated `OpCertCodecError` variant per shape failure; not a second header-embedded opcert encoder (enforced by `ci_check_opcert_closed.sh`). (10) `shelley::tx_components` (DC-CONS-13/16) — preserved-byte slices that alias the input buffer (no `to_vec` / clones); reject non-4-tuple shapes; not re-encode the boolean validity flag or the auxiliary-data null. |
| **Inbound deps** | `ade_ledger` (heavy — the new `ade_ledger::rollback::materialize` uses `cbor::envelope::decode_block_envelope` for replay-forward era detection; `ade_ledger::receive::reducer` reuses `block_validity::decode_block`), `ade_plutus`, `ade_testkit`, `ade_network`, `ade_runtime`, `ade_core_interop`. No new inbound crate edge in PHASE4-N-I. |
| **Outbound deps** | `ade_types`. No external dependencies; std-only. Dev-deps: `serde_json`, `toml`. |
| **Entry points** | `ade_codec::cbor::envelope::decode_block_envelope` (also a new heavy use site in `ade_ledger::rollback::materialize`), `ade_codec::cbor`, `ade_codec::traits::AdeEncode`, `ade_codec::CodecContext`, per-era `decode_*_block`, `ade_codec::address::decode_address`. B2: `ade_codec::conway::tx::decode_conway_tx_body`. B3: `ade_codec::conway::cert::decode_conway_certs` and `ade_codec::conway::withdrawals::{decode_withdrawals, withdrawals_sum}`. PP-S1: `ade_codec::conway::governance::{decode_proposal_procedures, encode_proposal_procedures}`. N-C: `ade_codec::shelley::opcert::{encode_opcert, decode_opcert, OpCertCodecError}`, `ade_codec::shelley::tx_components::{split_conway_tx_components, TxComponents}`. |
| **Key modules** | `cbor/`, `byron/`, `shelley/` (incl. `cert.rs`, `opcert.rs`, `tx_components.rs`), `allegra/`, `mary/`, `alonzo/`, `babbage/`, `conway/`, `address/`, `preserved.rs`, `traits.rs`, `primitives.rs`, `error.rs`. |

---

### `ade_core`

> **Status carried from prior CODEMAP — unchanged at HEAD `75f75da`.** PHASE4-N-I did not touch `ade_core`. The N-C-introduced `consensus::opcert_validate.rs` module carries forward unchanged; the `ade_core::consensus::{era_schedule, ledger_view, praos_state}` surface is consumed unchanged by the new `ade_ledger::rollback` driver (which carries `&EraSchedule` + `&dyn LedgerView` through to the underlying `block_validity` step, and reads/writes `PraosChainDepState` as owned values across snapshot lookup + replay-forward fold).

| Attribute | Value |
|-----------|-------|
| **Purpose** | BLUE authoritative Praos consensus core. Owns canonical types and pure state-transitions that decide which header / chain Ade accepts: HFC era schedule + slot↔era↔time translation, Praos chain-dep state (nonce / op-cert counters), VRF cert verification + leader-eligibility predicate, KES signature + op-cert period verification wired into header admission, header validation pipeline, fork-choice, **header-level rollback authority (`consensus::rollback::apply_rollback`)** — distinct from the new N-I ledger-side `ade_ledger::rollback` — leader-schedule query, canonical encodings of all chain-dep state and chain events. Owns the producer-side opcert acceptance authority (`opcert_validate`). |
| **Creates** | **Schedule:** `BootstrapAnchorHash`, `EraSchedule`, `EraSummary`, `EraLocation`. **State:** `PraosChainDepState`, `OpCertCounterMap`, `Nonce`. **Events/points:** `Point`, `ChainHash`, `BlockDistance`, `SecurityParam`, `ChainEvent`, `ChainSelectionReject`. **Errors:** `HFCError`, `SlotTimeError`, `OutsideForecastRange`, `HeaderValidationError`, `FieldError`, `FieldKind`, `VrfCertError`, `OpCertCounterError`, `NonceEvolutionError`, `LeaderScheduleError`, `OpCertError`. **Header surface:** `HeaderInput`, `HeaderVrf`, `HeaderKes`, `ValidatedHeaderSummary`, `HeaderApplied`. **Fork-choice:** `TiebreakerView`, `CandidateFragment`, `ChainSelectorState`, `ForkChoiceError`. **Op-cert/nonce:** `OpCertObservation`, `NonceInput`. **VRF:** `VrfRole`, `VerifiedVrf`, `StakeFraction`, `ActiveSlotsCoeff`. **Leader schedule:** `LeaderScheduleQuery`, `LeaderScheduleAnswer`. **Ledger view boundary:** `LedgerView` trait. **Rollback (header-level):** `RollBackRequest`, `RollBackApplied`. **Encoding:** `DecodeError`. 44 public types. |
| **Interprets** | Canonical inputs from the `ade_runtime` shell — `HeaderInput` projections, `EraSchedule` materialized once from genesis JSON, `LedgerView` snapshots from `ade_ledger::consensus_view::PoolDistrView`, ordered `StreamInput` events. KES check verifies hot KES key signature over the header body CBOR bytes. `opcert_validate` consumes an `OperationalCert`, cold key, expected period, and prev counter. **New consumption site in PHASE4-N-I:** `ade_ledger::rollback::materialize_rolled_back_state` carries `&EraSchedule` + `&dyn LedgerView` + owned `PraosChainDepState` through one `SnapshotReader::nearest_le` lookup + a `block_validity`-driven replay-forward fold — the `PraosChainDepState` is consumed as input and produced as output of the replay-forward step. |
| **MUST NOT** | (1) Take or accept a `&ChainDb` reference (`ci_check_no_chaindb_in_consensus_blue.sh`). (2) Use any `f32`/`f64` (`ci_check_no_float_in_consensus.sh`). (3) Add `#[non_exhaustive]` to any consensus enum, open-tail `Other`/`Unknown`, owned `String` fields, or `Box<dyn ...>` (`ci_check_consensus_closed_enums.sh`). (4) Reference `density` in `fork_choice.rs`/`candidate.rs` outside a `// no-density:` annotation. (5) Read wall-clock. (6) Use `HashMap`/`HashSet`; `OpCertCounterMap` uses `BTreeMap`. (7) `async fn`, `.await`, `tokio`. (8) Construct a `PreservedCbor`. (9) Re-derive stake snapshots (DC-CONSENSUS-02). (10) Bypass the canonical `validate_and_apply_header` pipeline. (11) B1 fail-closed (DC-VAL-06). (12) B1 KES rule (DC-CRYPTO-01). (13) All shared-header BLUE rules. (14) `consensus::opcert_validate` (DC-CONS-11/12) — `OpCertError` closed; sub-check order pinned; `prev_counter == None` only first-opcert acceptance path; no external counter store. |
| **Inbound deps** | `ade_ledger` (B1 + N-C + N-H + **NEW heavy site in N-I: `ade_ledger::rollback::materialize_rolled_back_state` takes `&EraSchedule` + `&dyn LedgerView` + owned `PraosChainDepState` — composes `block_validity` which calls `validate_and_apply_header`**), `ade_runtime`, `ade_testkit`, `ade_core_interop`. No new inbound crate edge in PHASE4-N-I — the rollback-side consumption rides on `ade_ledger → ade_core`. |
| **Outbound deps** | `ade_types`, `ade_crypto`. Dev-deps: `ade_testkit`, `serde_json`, `cardano-crypto`. |
| **Entry points** | `use ade_core::consensus::{...}` aggregator, `ade_core::consensus::ledger_view::LedgerView`, `ade_core::consensus::vrf_cert::{vrf_input, verify_praos_vrf, ...}`, `ade_core::consensus::kes_check::{verify_header_kes, expect_size}`, `ade_core::consensus::praos_state::*` (now also consumed by `ade_ledger::rollback` as owned `PraosChainDepState`), `ade_core::consensus::header_summary::*`. Top-level transitions: `validate_and_apply_header`, `select_best_chain`, `apply_rollback` (header-level), `apply_nonce_input`, `apply_op_cert`, `query_leader_schedule` + `is_leader_for_vrf_output`, `verify_vrf_cert` + `verify_praos_vrf` + `check_leader_claim` + `is_leader`, `tiebreaker_prefer`, `encode/decode_chain_dep_state`, `encode/decode_chain_event`. `ade_core::consensus::opcert_validate::{opcert_validate, OpCertError}`. |
| **Key modules** | `consensus/era_schedule.rs`, `consensus/praos_state.rs`, `consensus/events.rs`, `consensus/errors.rs`, `consensus/vrf_cert.rs`, `consensus/kes_check.rs`, `consensus/nonce.rs`, `consensus/op_cert.rs`, `consensus/leader_schedule.rs`, `consensus/header_summary.rs` + `consensus/header_validate.rs`, `consensus/candidate.rs`, `consensus/fork_choice.rs`, `consensus/rollback.rs` (header-level rollback), `consensus/ledger_view.rs`, `consensus/encoding.rs`, `consensus/opcert_validate.rs`. |

---

### `ade_crypto`

> **Status carried from prior CODEMAP — unchanged at HEAD `75f75da`.** PHASE4-N-I did not touch `ade_crypto`.

| Attribute | Value |
|-----------|-------|
| **Purpose** | Owns pure cryptographic verification primitives — Blake2b hashing, Ed25519 / Byron-bootstrap signature verification, KES verification with operational certificates, VRF verification, plus the closed signature-artifact types (`KesSignature`, `VrfProof`, `VrfOutput`, `Ed25519Signature`) that BLUE consumes across the RED→BLUE boundary. Verification only — signing lives in `ade_runtime::producer::signing`. |
| **Creates** | `Blake2b224`, `Blake2b256`, `HashAlgorithm` trait, `Ed25519VerificationKey`, `Ed25519Signature`, `ByronExtendedVerificationKey`, `KesVerificationKey`, `KesPeriod`, `OperationalCertData`, `VrfVerificationKey`, `VrfProof`, `VrfOutput`, `CryptoError`, `KesSignature(pub [u8; SUM6_KES_SIG_LEN])`, `SUM6_KES_SIG_LEN: usize = 448`. |
| **Interprets** | Verification key / signature / proof byte structures. Not a CBOR parser — accepts already-decoded byte slices. |
| **MUST NOT** | (1) Implement signing (`ci_check_no_signing_in_blue.sh`). (2) Allocate global state. (3) Use any BLUE forbidden pattern. (4) Use `unsafe` outside the allowlisted FFI in `src/vrf.rs`. (5) `build_opcert_signable` must produce the spec-correct raw concatenation. (6) `KesSignature` (DC-CRYPTO-04) — closed length-pinned wrapper; only `from_bytes` construction; custom redacting `Debug`; no `PartialOrd`/`Ord`/`Hash` derives. (7) No `Drop` impl that zeroizes — `KesSignature` is BLUE and not secret. |
| **Inbound deps** | `ade_core`, `ade_ledger`, `ade_plutus`, `ade_testkit`, `ade_core_interop`, `ade_runtime`. No new inbound crate edge in PHASE4-N-I. |
| **Outbound deps** | `ade_types`, `blake2`, `ed25519-dalek`, `cardano-crypto` (vrf-draft03 + kes-sum + dsign features, `default-features = false`). |
| **Entry points** | `ade_crypto::blake2b::blake2b_256`, `blake2b_224`, `blake2b_256`, `block_header_hash`, `transaction_id`, `script_hash`, `credential_hash`, `verify_ed25519`, `verify_byron_bootstrap`, `verify_kes`, `verify_opcert`, `verify_vrf`, `ade_crypto::kes::{KesSignature, SUM6_KES_SIG_LEN}`. |

---

### `ade_ledger`

> **Status change at HEAD `75f75da` (PHASE4-N-I S1–S6 — one new BLUE sub-tree under `rollback/` + a non-breaking trait extension on `receive::chain_write::ChainDbWrite` + an additive `RollbackContext`-gated branch in `receive::reducer::receive_apply`).** Between `efe1fb9` and HEAD: (a) the new `ade_ledger::rollback` sub-module (5 files, 819 LOC + 12 inline tests) hosts the BLUE rollback authority: `traits.rs` defines the narrow read-only seams `SnapshotReader::nearest_le(SlotNo) -> Option<(SlotNo, LedgerState, PraosChainDepState)>` + `BlockSource::blocks_in_range(from_exclusive, to_inclusive) -> Vec<(SlotNo, Vec<u8>)>`; `error.rs` defines closed `MaterializeError` (`RollbackTooDeep` / `ReplayFailedAt` / `EraNotSupported`) + closed `CommitRollbackError` (`ChainDb(ChainWriteError)`); `materialize.rs` defines `TargetPoint { slot, hash }` + `materialize_rolled_back_state` — **CN-STORE-07 sole `pub fn` returning `(LedgerState, PraosChainDepState)` in the rollback module tree**, composes one snapshot lookup + replay-forward fold over `block_validity::transition::block_validity` (the same CN-CONS-08 admission authority N-H's receive bridge uses), supported eras are Babbage + Conway with pre-Conway surfacing `EraNotSupported`; `commit.rs` defines `commit_rollback` — atomic state replacement with the irreversible step first (`chain_write.rollback_to_slot(target.slot)` → `state.ledger = new_ledger` → `state.chain_dep = new_chain_dep` → `state.pending_headers = PendingHeaderCache::new()`). (b) the `ChainDbWrite` trait in `receive::chain_write` gains the new method `fn rollback_to_slot(&mut self, slot: SlotNo) -> Result<(), ChainWriteError>` — non-breaking trait extension; the existing `write_admitted(AdmittedBlock)` method is unchanged. (c) the `receive::reducer::receive_apply` function gains a new sixth parameter `rollback_ctx: Option<&RollbackContext>` — additive; `None` preserves the pre-N-I `Err(RollbackOutOfScope)` legacy behavior for callers that haven't wired the rollback context yet, `Some(ctx)` activates the new BLUE `roll_backward` helper that composes `materialize_rolled_back_state` + `commit_rollback`. The new `RollbackContext<'a> { snapshot_reader, block_source }` BLUE struct is a borrow-only bundle — it does not own state. **CN-CONS-08 strengthened in N-I**: the admission authority's twin on the rollback side is CN-STORE-07 — the rollback-side has a single materialize authority just as the admit-side has a single admission authority. **DC-CONS-20 closes here**: ChainDb + LedgerState + PraosChainDepState + pending_headers all roll atomically to the target slot. All prior B-track / N-E / PROPOSAL-PROCEDURES-DECODE / N-C / N-G / N-H narrative carries forward unchanged.

| Attribute | Value |
|-----------|-------|
| **Purpose** | The functional core (ledger half): stateless ledger rules for every Cardano era; the B1 top-level block-validity verdict; the B2 top-level transaction-validity verdict + mempool admission; the B3 full Conway value-conservation accounting; the B4 closed Conway cert-state accumulation; the B5 closed Conway governance-cert accumulation; the live committee-enactment write-back; the single BLUE chokepoint `mempool::ingress::mempool_ingress` from wire ingress into `admit`; the BLUE producer authority (`forge_block`, `self_accept`, `AcceptedBlock` broadcast token, `block_body_hash` recipe); the BLUE producer-side served-chain index and single canonical header-projection authority; the BLUE receive-side header→body bridge authority (`receive/`). **NEW — PHASE4-N-I:** the BLUE rollback authority (`rollback/`): single materialize authority (`materialize_rolled_back_state` — CN-STORE-07 sole `pub fn` returning `(LedgerState, PraosChainDepState)` in the rollback module tree), atomic commit helper (`commit_rollback` — staged-then-committed with the ChainDb `rollback_to_slot` irreversible step first), narrow read-only trait seams (`SnapshotReader` + `BlockSource`), closed error sums (`MaterializeError` + `CommitRollbackError`). Plus the `receive::reducer` extension (RollBackward arm wired through `roll_backward` helper composing `materialize_rolled_back_state` + `commit_rollback`) that closes DC-CONS-20 lockstep across ChainDb + LedgerState + PraosChainDepState + pending_headers. Scope: in-memory variant; persistent on-disk encoder explicitly carved out to a follow-on cluster (`DC-CONS-21` stays `declared` with `open_obligation = "persistent_ledger_snapshot_encoding_follow_on_cluster"`). |
| **Creates** | Carry-forward (all N-H + earlier types). **NEW — PHASE4-N-I (BLUE):** `MaterializeError` (closed enum with three variants `RollbackTooDeep { target_slot, oldest_snapshot }` / `ReplayFailedAt { slot, error: BlockValidityError }` / `EraNotSupported { era: CardanoEra, slot }`, in `rollback::error`); `CommitRollbackError` (closed enum with one variant `ChainDb(ChainWriteError)`, in `rollback::error`); `TargetPoint { slot: SlotNo, hash: Hash32 }` (in `rollback::materialize`); `RollbackContext<'a> { snapshot_reader: &'a dyn rollback::SnapshotReader, block_source: &'a dyn rollback::BlockSource }` (in `receive::reducer`). Traits: `SnapshotReader { fn nearest_le(target_slot: SlotNo) -> Option<(SlotNo, LedgerState, PraosChainDepState)> }`, `BlockSource { fn blocks_in_range(from_exclusive: SlotNo, to_inclusive: SlotNo) -> Vec<(SlotNo, Vec<u8>)> }` (both in `rollback::traits`). Trait extension: `ChainDbWrite::rollback_to_slot(&mut self, slot: SlotNo) -> Result<(), ChainWriteError>` (in `receive::chain_write`). Functions: `materialize_rolled_back_state`, `commit_rollback`, and the private `roll_backward` helper in `receive::reducer`. |
| **Interprets** | Decoded canonical types from `ade_codec`. Hosts per-era validation composers and the late-era witness decoder. B1: `block_validity::header_input::decode_block`. B2: `tx_validity::phase1::decode_tx`. B3: `conway::check_conway_coin_conservation`. B4: `rules::accumulate_tx_certs`. B5: `gov_cert::apply_conway_gov_cert`. PHASE4-N-E S1: `mempool_ingress` is a pure pass-through. PHASE4-N-C: `producer::forge::forge_block` / `producer::self_accept::self_accept`. PHASE4-N-G: `producer::served_chain::served_chain_admit`. PHASE4-N-H: `receive::admitted::admit_via_block_validity` (single admission authority). **NEW — PHASE4-N-I:** `rollback::materialize::materialize_rolled_back_state` interprets each block from `BlockSource::blocks_in_range` exactly once: first via `ade_codec::cbor::envelope::decode_block_envelope` for era detection (rejects pre-Conway with `EraNotSupported`), then hands the same bytes to `block_validity::transition::block_validity` for the actual replay-forward step (CN-STORE-07 + DC-CONS-22). `receive::reducer::roll_backward` interprets nothing at the bytes level — it composes `materialize_rolled_back_state` + `commit_rollback` as pure type-driven composition. `commit_rollback` interprets nothing at the bytes level — it is staged state replacement only. |
| **MUST NOT** | (1) Perform I/O. (2) Use `HashMap`, floats, or any other BLUE-forbidden pattern. (3) Hash anything other than wire bytes. (4) Construct `PreservedCbor` outside named decoders. (5) Depend on `ade_runtime`. (6) Use signing primitives or `#[cfg(feature)]` gates. (7) Omit `// Core Contract:` banner. (8–39) B1/B2/B3/B4/B5/N-E/PP-S1/N-C/N-G/N-H carry-forward (see prior CODEMAP for detail). **(40) NEW — `rollback::materialize` (CN-STORE-07 + DC-CONS-22, S2):** `materialize_rolled_back_state` MUST remain the only `pub fn` in `crates/ade_ledger/src/rollback/*.rs` whose signature contains `(LedgerState[^{]*PraosChainDepState` — enforced repo-wide by `ci/ci_check_rollback_materialize_closure.sh`'s grep gate. Production code MUST NOT use `HashMap`/`HashSet`/wall-clock/`tokio`/`rand`. MUST positively call `block_validity` — the SAME CN-CONS-08 admission authority the N-H receive bridge calls, so admit and replay-forward share one composition. MUST NOT bypass the per-block validity step (no "fast path" that admits replayed blocks at a different verdict shape than direct apply). MUST surface era-out-of-scope explicitly as `EraNotSupported { era, slot }` rather than silently dropping pre-Conway eras. **(41) NEW — `rollback::commit` (DC-CONS-20, S3):** `commit_rollback` MUST perform `chain_write.rollback_to_slot(target.slot)` BEFORE assigning `state.ledger`/`state.chain_dep`/`state.pending_headers` — the staged-then-committed shape preserves failure-atomicity (any underlying chain-write failure leaves receive state unchanged). MUST clear `state.pending_headers = PendingHeaderCache::new()` on every successful rollback — post-rollback cached headers are stale and must not be replayed against the new ledger. MUST NOT introduce a second public entry into the rollback path (no parallel `pub fn` admits the new `(ledger, chain_dep)` outside `commit_rollback` and `roll_backward`). **(42) NEW — `rollback::traits` (S1):** the two trait methods MUST remain single-method per trait — narrow read-only seams; widening either trait to multi-method or adding write methods would break the GREEN/RED partition assumption that production impls live one layer up. The trait return types MUST remain owned `(LedgerState, PraosChainDepState)` + owned `Vec<(SlotNo, Vec<u8>)>` — the materialize driver needs ownership to fold mutably; borrowed-return shapes would block the driver's loop. **(43) NEW — `rollback::error` (S1):** both enums MUST remain closed (no `#[non_exhaustive]`), `String`-free (the `ReplayFailedAt` variant carries a typed `BlockValidityError`, the `ChainDb` variant carries a typed `ChainWriteError` — no opaque strings), and MUST NOT add new variants without a versioned gate per the closed-semantic-surface rule. **(44) NEW — `receive::chain_write::ChainDbWrite::rollback_to_slot` trait method (S3):** the method MUST take a `SlotNo` (not a `(SlotNo, Hash32)` tuple, not an opaque cursor) — the hash check belongs at the materialize layer where the snapshot is consulted, not at the irreversible chain-write step. Rolling back beyond the empty tip MUST return `Ok(())` per the ChainDb contract — idempotent rollback. **(45) NEW — `receive::reducer::roll_backward` helper + `RollbackContext` parameter (S6, DC-CONS-20):** the new sixth parameter `rollback_ctx: Option<&RollbackContext>` MUST remain `Option`-typed — `None` preserves the legacy `RollbackOutOfScope` behavior so the change is non-breaking to existing call sites that haven't wired a rollback context. The `roll_backward` helper MUST compose `materialize_rolled_back_state` + `commit_rollback` (no parallel rollback composition); on `MaterializeError::RollbackTooDeep` it MUST map to `ReceiveError::RollbackOutOfScope` (preserves the receive-side error shape so callers don't need to handle a new variant). The `RollbackContext<'a>` MUST remain a borrow bundle — no owned state; the snapshot reader + block source live one layer up (`ade_runtime::rollback`). |
| **Inbound deps** | `ade_testkit`, `ade_core_interop`, `ade_runtime`. PHASE4-N-G + N-H heavy sites (carried). **NEW heavy consumption sites in PHASE4-N-I** — `ade_runtime::rollback::*` consume `ade_ledger::rollback::{SnapshotReader, BlockSource, MaterializeError, materialize_rolled_back_state, TargetPoint}` + `ade_ledger::receive::ReceiveState`/`ReceiveEffect` + `ade_ledger::state::LedgerState`; the integration tests under `ade_runtime/tests/receive_rollback_integration.rs` consume `ade_ledger::rollback::*` + `ade_ledger::receive::reducer::{RollbackContext, receive_apply, ReceiveState}` + the new `ChainDbWrite::rollback_to_slot` method. |
| **Outbound deps** | `ade_types`, `ade_crypto`, `ade_codec`, `ade_plutus`, `ade_core`, `minicbor`, `num-bigint`, `num-integer`, `num-traits`. Dev-dep: `ade_testkit`. **No new outbound crate edge in PHASE4-N-I** — the new `rollback` module reuses the established `ade_ledger → {ade_core, ade_codec, ade_types}` edges (`rollback::materialize` uses `ade_core::consensus::{era_schedule::EraSchedule, ledger_view::LedgerView, praos_state::PraosChainDepState}` + `ade_codec::cbor::envelope::decode_block_envelope` + `block_validity::transition::block_validity` + `ade_types::{CardanoEra, Hash32, SlotNo}`). |
| **Entry points** | Carry-forward (all N-H + earlier surfaces). **NEW — PHASE4-N-I:** `ade_ledger::rollback::{materialize_rolled_back_state, TargetPoint, commit_rollback, MaterializeError, CommitRollbackError, SnapshotReader, BlockSource}`. Re-exported at the `ade_ledger::rollback` module root via `pub use`. Plus `ade_ledger::receive::reducer::RollbackContext`. Plus the extended `ade_ledger::receive::ChainDbWrite::rollback_to_slot` method. Plus the extended `ade_ledger::receive::receive_apply` signature (new sixth parameter `rollback_ctx: Option<&RollbackContext>`). Consumed by `ade_runtime::rollback::*` (the GREEN+RED counterpart sub-tree) and the integration tests under `ade_runtime/tests/receive_rollback_integration.rs`. |
| **Key modules** | All prior keys (`byron.rs`, `shelley.rs`, …, `rules.rs`, `state.rs`, `utxo.rs`, …, `block_validity/`, `tx_validity/`, `mempool/`, `cert_classify.rs`, `block_body_hash.rs`, `producer/`, `receive/`). **NEW — `rollback/`** (PHASE4-N-I S1 + S2 + S3): `rollback/mod.rs` (32 LOC barrel; re-exports the full rollback surface), `rollback/traits.rs` (88 LOC + 2 inline tests — `SnapshotReader` + `BlockSource`), `rollback/error.rs` (98 LOC + 2 inline tests — `MaterializeError` + `CommitRollbackError`), `rollback/materialize.rs` (402 LOC + 5 inline tests — `TargetPoint`, `materialize_rolled_back_state`, private `is_supported_era` helper), `rollback/commit.rs` (200 LOC + 3 inline tests — `commit_rollback`). **EXTENDED — `receive/chain_write.rs`** (S3): trait method `rollback_to_slot(slot: SlotNo) -> Result<(), ChainWriteError>` added; +1 inline test. **EXTENDED — `receive/reducer.rs`** (S6): `RollbackContext<'a>` struct, `roll_backward` private helper, new `rollback_ctx: Option<&RollbackContext>` parameter on `receive_apply`; +8 inline tests. |

> **GREEN sub-classification (`ade_ledger::mempool::policy`, `ade_ledger::mempool::canonicalize`).** Carry-forward.

> **RED sub-classification (`ade_ledger::consensus_input_extract`).** Carry-forward.

> **`ade_ledger::producer::served_chain` BLUE classification (N-G).** Carry-forward.

> **`ade_ledger::receive` BLUE classification (N-H).** Carry-forward.

> **NEW — `ade_ledger::rollback` BLUE classification rests on four layers:** (a) the crate prefix is BLUE under `.idd-config.json` `core_paths`; (b) every file begins with the `// Core Contract:` banner (enforced by `ci_check_module_headers.sh`); (c) the narrow CI gate `ci_check_rollback_materialize_closure.sh` pins the CN-STORE-07 single-authority shape repo-wide across the rollback module tree, the production-purity shape of `materialize.rs`, and the positive `block_validity` call site (admission-authority composition); (d) the existing `ci_check_receive_reducer_closure.sh` (CE-N-H-2 + DC-CONS-19) gates the receive::reducer extension — production code still cannot use `HashMap`/wall-clock/`tokio`/`rand` and the `roll_forward` helper still cannot mutate authoritative state (Invariant I-6 carries through despite the new `roll_backward` sibling). The CN-CONS-08 admission-authority composition remains positively-grepped in the same script.

> **All prior closure narratives unchanged at HEAD.** B-track + N-E + PROPOSAL-PROCEDURES-DECODE + N-C + N-G + N-H all carry forward.

---

### `ade_network` *(BLUE submodules)*

> **Status carried from prior CODEMAP — unchanged at HEAD `75f75da`.** PHASE4-N-I did not touch any BLUE submodule of `ade_network`. The pre-existing PHASE4-N-A `chain_sync::signal::ForkChoiceSignal` is consumed unchanged by the existing GREEN adapter `ade_runtime::receive::events_to_state`, which lifts its `RollBackward { target_point, tip }` variant into `ade_ledger::receive::ReceiveEvent::RollBackward { target_point, tip }`. The new N-I `roll_backward` arm consumes `target_point.slot` to call into `materialize_rolled_back_state`; no change to the BLUE wire-grammar surface is required.

| Attribute | Value |
|-----------|-------|
| **Purpose** | Owns the Cardano Ouroboros mini-protocol authority — the closed wire grammar (CBOR codecs) and pure state machines for all 11 N2N + N2C mini-protocols, plus the BLUE Ouroboros mux frame primitive. Also owns the producer-side server-role reducer surface (PHASE4-N-G `chain_sync::server` + `block_fetch::server`). |
| **Creates** | **Wire grammar (`codec/`):** 11 closed mini-protocol message enums + closed payload types + structured `CodecError` + `ProtocolKind`. **State machines:** one closed `*State` / `*Agency` / `*Output` / `*Error` quad per protocol. **Mux primitives (`mux/frame.rs`):** `MuxFrame`, `MuxHeader`, `MuxMode`, `MiniProtocolId`, `MuxError`. **PHASE4-N-G (`chain_sync/server.rs` + `block_fetch/server.rs`):** `ServerReply(ServerVariant)` closed wrappers, `HeaderProjection`, `ProducerChainSyncServerState`, `ProducerServerError`, `ServerStep`, trait `ServedHeaderLookup`; `ProducerBlockFetchServerState`, `ProducerBlockFetchServerError`, `BlockFetchServerStep`, trait `ServedRangeLookup`. Functions: `producer_chain_sync_serve`, `producer_chain_sync_advance_tip`, `producer_block_fetch_serve`. |
| **Interprets** | The on-wire CBOR of every Ouroboros mini-protocol message (closed grammar) and the 8-byte Ouroboros mux frame header. Block/header/tx-body bytes inside RollForward / Block / Tx are opaque pass-through (DC-PROTO-06); LSQ Query/Result payloads also opaque. PHASE4-N-G server reducers interpret already-decoded `ChainSyncMessage` / `BlockFetchMessage` values. |
| **MUST NOT** | (1) `async fn`, `.await`, `tokio`, `async_std`, `futures::`, async-runtime spawn, or async timers in any file under the 9 BLUE paths. (2) Decode block / header / tx-body bytes. (3) Construct a `PreservedCbor` outside `ade_codec`'s named chokepoints. (4) Add `#[non_exhaustive]` to any wire-message enum or introduce a generic `Codec<P>` trait. (5) Define a generic `Agency<P>` wrapper. (6) Read a selected version from ambient session state. (7) Redefine `TxId` / `SlotNo` / `Hash32` / `CardanoEra`. (8) Depend on `pallas-network`. (9) Hold `String` in any error variant. (10) Use any other BLUE forbidden pattern. (11–13) PHASE4-N-G server-role MUST NOTs (carry-forward). |
| **Inbound deps** | `ade_core_interop` (live consensus session binary + N-E S6 + N-C S7 + N-G S7 + N-H S6 RED binaries; all RED → BLUE). The non-dev edge `ade_runtime → ade_network` (introduced by PHASE4-N-G S5/S6, extended by PHASE4-N-H `receive::*`) carries forward. **No new inbound crate edge in PHASE4-N-I** — the rollback path consumes `chain_sync::signal::ForkChoiceSignal::RollBackward` over the established N-H lift chain (lifter → `ReceiveEvent::RollBackward` → `receive_apply` → `roll_backward` helper). Dev-deps `ade_network → {ade_ledger, ade_testkit, ade_crypto, ade_core}` carry forward. |
| **Outbound deps** | `ade_types`, `ade_codec`. No external deps in the BLUE submodules. |
| **Entry points** | Codec entry points per protocol; `ade_network::codec::primitives::*`; `ade_network::codec::version::*`; `ade_network::codec::{CodecError, ProtocolKind}`. State-machine `*_transition` per protocol. Mux: `ade_network::mux::frame::{encode_frame, decode_frame, ...}`. PHASE4-N-G: `ade_network::chain_sync::server::*` + `ade_network::block_fetch::server::*`. |
| **Key modules** | `codec/` (11 protocols + `primitives.rs` + `version.rs` + `error.rs`); `handshake/`, `chain_sync/` (incl. PHASE4-N-G `server.rs`), `block_fetch/` (incl. PHASE4-N-G `server.rs`), `tx_submission/`, `keep_alive/`, `peer_sharing/`; `n2c/`; `mux/frame.rs`. |

---

### `ade_plutus`

> **Status carried from prior CODEMAP — unchanged at HEAD `75f75da`.** PHASE4-N-I did not touch `ade_plutus`.

| Attribute | Value |
|-----------|-------|
| **Purpose** | Quarantine boundary between the Ade-canonical ledger and the ported UPLC evaluator from `aiken-lang/aiken` (pinned to tag `v1.1.21`). Exposes Ade-canonical types only; aiken- and pallas-originated types are strictly internal. |
| **Creates** | `PlutusScript`, `PlutusLanguage`, `EvalOutput`, `PlutusError`, `CostModels`, `DecoderMode`, `PerScriptResult`, `TxEvalResult`. |
| **Interprets** | UPLC scripts (Plutus V1/V2/V3) and `CostModels` CBOR. Phase-two transaction evaluation. `PlutusScript::from_cbor` is a named ingress chokepoint. |
| **MUST NOT** | (1) Re-export any `pallas_*` or `aiken_uplc::` type. (2) Allow another BLUE crate to bypass the canonical entry. (3) Activate PV11 builtins. (4) Use any BLUE-forbidden pattern. (5) Construct `PreservedCbor` outside `ade_codec`. |
| **Inbound deps** | `ade_ledger`, `ade_testkit`. |
| **Outbound deps** | `ade_types`, `ade_crypto`, `ade_codec`, `aiken_uplc` (git, tag `v1.1.21`), `pallas-primitives` (internal-only). |
| **Entry points** | `ade_plutus::eval_tx_phase_two`, `ade_plutus::tx_eval::*`, `ade_plutus::evaluator::*`, `ade_plutus::cost_model::*`. |
| **Key modules** | `evaluator.rs`, `cost_model.rs`, `script_context.rs`, `script_verdict.rs`, `tx_eval.rs`. |

---

### `ade_types`

> **Status carried from prior CODEMAP — unchanged at HEAD `75f75da`.** PHASE4-N-I did not add any canonical type to `ade_types` — the new closed canonical types all live in `ade_ledger::rollback`'s authoritative sub-tree and `ade_ledger::receive::reducer`. `ade_types::{Hash32, SlotNo, CardanoEra}` continue as the lingua franca; the rollback types `TargetPoint` and `MaterializeError`/`EraNotSupported` embed them in their fields. |

| Attribute | Value |
|-----------|-------|
| **Purpose** | Owns the canonical Cardano domain vocabulary — primitives, era enum, address forms, era-tagged transaction bodies / outputs / certificates, governance types — used by every other workspace crate as the lingua franca. The schema, separated from the codec. |
| **Creates** | `CardanoEra`, `SlotNo`, `BlockNo`, `EpochNo`, `Hash28`, `Hash32`, `Coin`, `Lovelace`, `NetworkId`, `Nonce`, `TxIn`, `RewardAccount`, `PoolId`, `Address`, `ByronAddress`, `Credential`, `StakeCredential`, `Certificate`, `PoolRegistrationCert`, `ConwayCert`, `CertDisposition` / `DepositEffect` / `CoinSource`, `MIRCert`, `MIRPot`, `DRep`, `GovAction`, `GovActionState`, `GovActionId`, `Anchor`, `ProposalProcedure`, `OperationalCert`, `NativeScript`, `PlutusV1Script`, `Datum`, `DatumOption`, `MultiAsset`, `AssetName`, `CostModel`, `ExUnits`, plus per-era tx-body / tx-out / witness wrappers. |
| **Interprets** | None — produce-only. Domain types are constructed by `ade_codec` decoders or `ade_ledger` rules. |
| **MUST NOT** | (1) Construct or decode `PreservedCbor`. (2) Use any BLUE-forbidden pattern. (3) Depend on any workspace crate. (4) Add open/extensible variants to closed enums without a versioned gate. |
| **Inbound deps** | `ade_codec`, `ade_crypto`, `ade_ledger`, `ade_plutus`, `ade_runtime`, `ade_testkit`, `ade_network`, `ade_core`, `ade_core_interop`. |
| **Outbound deps** | None. |
| **Entry points** | `ade_types::CardanoEra`, `ade_types::tx::{Coin, TxIn, RewardAccount}`, `ade_types::{Hash32, SlotNo, Hash28, BlockNo, EpochNo}`, `ade_types::conway::tx::ConwayTxBody`, `ade_types::conway::cert::*`, `ade_types::conway::governance::{ProposalProcedure, Anchor, GovAction, GovActionId}`, `ade_types::shelley::block::{OperationalCert, ProtocolVersion, ShelleyHeader, ShelleyBlock, VrfData}`. |
| **Key modules** | `primitives.rs`, `era.rs`, `tx.rs`, `address/`, `byron/`, `shelley/`, `allegra/`, `mary/`, `alonzo/`, `babbage/`, `conway/`. |

---

## GREEN Modules — Deterministic Glue

> Deterministic, non-authoritative. May depend on BLUE; must not affect authoritative outputs.

### `ade_testkit`

> **Status carried from prior CODEMAP — unchanged at HEAD `75f75da`.** PHASE4-N-I did NOT add a `rollback/` sub-module to `ade_testkit` — the N-I GREEN test surface ended up under `ade_runtime/tests/receive_rollback_integration.rs` (mirroring the N-H + N-G precedent — orphan-rule + dev-dep direction made `ade_runtime::tests/` the natural host since the integration tests exercise the full RED snapshot-writer hook + GREEN rollback adapter + BLUE materialize/commit composition). The pre-existing `ade_testkit::validity::ConwayValidityCorpus` is the corpus the new integration test in `ade_runtime/tests/receive_rollback_integration.rs` builds its `(LedgerState, PraosChainDepState)` over for the replay-forward proof.

| Attribute | Value |
|-----------|-------|
| **Purpose** | Test infrastructure: differential harness, oracle snapshot loader, regression corpus, genesis loader, era mapping, transcript replay, diff reporting; N-B consensus harness; B1 block-validity harness (`validity/`); B2 transaction-validity harness (`tx_validity/`); N-E S2 mempool-ingress replay harness (`mempool/`); PP-S2 canonical synthetic `proposal_procedures` corpus + replay harness (`governance/`); PHASE4-N-C producer test harness (`producer/`). |
| **Creates** | Carry-forward (all entries from prior CODEMAP). No new types in PHASE4-N-I. |
| **Interprets** | Carry-forward. |
| **MUST NOT** | Carry-forward (1)–(16). |
| **Inbound deps** | None at compile time; consumption is via integration tests and dev-dep links from `ade_core`, `ade_runtime`, `ade_ledger`. `ade_core_interop` is a non-dev consumer. **NEW dev-dep consumption sites in PHASE4-N-I:** the `ade_ledger::rollback::materialize` inline tests use `ade_testkit::validity::ConwayValidityCorpus` directly (dev-dep `ade_ledger → ade_testkit` was already in place); the integration test under `ade_runtime/tests/receive_rollback_integration.rs` uses the same corpus via the existing dev-dep `ade_runtime → ade_testkit`. |
| **Outbound deps** | `ade_types`, `ade_codec`, `ade_crypto`, `ade_core`, `ade_ledger`, `ade_plutus`, `ade_runtime`, `blake2`, `flate2`, `tar`, `serde`, `serde_json`, `toml`, `cardano-crypto` (dev-dep, N-C). |
| **Entry points** | `ade_testkit::harness::*`; N-B `consensus::*`; B1 `validity::*` (incl. `validity::ConwayValidityCorpus::load` — now also N-I's primary corpus source for the replay-forward integration test); B2 `tx_validity::*`. N-E S2: `ade_testkit::mempool::*`. PP-S2: `ade_testkit::governance::*`. PHASE4-N-C: `ade_testkit::producer::*`. |
| **Key modules** | `harness/`, `consensus/`, `validity/`, `tx_validity/`, `mempool/`, `governance/`, `producer/` (N-C). **No new module added by N-I** — the planned `rollback/` sub-tree was not materialized; the equivalent integration test lives at `crates/ade_runtime/tests/receive_rollback_integration.rs`. |

> **Classification note carried forward.** `ade_testkit` reads files from disk in test helpers and drives BLUE authorities from corpus inputs.

> **Gap surfaced (NEW — PHASE4-N-I, same shape as N-G's and N-H's).** The cluster plan implicitly anticipated a `ade_testkit::rollback::*` sub-tree (mirroring `producer/` + `validity/` + the deferred `receive/`); the shipped artifacts live one layer over, under `ade_runtime/tests/`. **No new CI presence-gate** was added for the rollback integration test (unlike the N-G/N-H `ci_check_{server,receive}_paths_corpus_present.sh` scripts) — N-I's mechanical CE evidence is the inline test set in `rollback/materialize.rs` itself plus the single integration file; no operator-action live binary was added that would need a presence gate. The deviation is documented here so future work does not re-introduce a duplicate harness under `ade_testkit`. Whether `ade_testkit::rollback::*` should be added as a thin re-export shim for cross-crate consumption (e.g., a future `ade_node` integration test) is a follow-on judgment call — at HEAD there is no consumer that would benefit.

---

### `ade_runtime::producer::{broadcast_to_served, served_chain_lookups}` *(GREEN — PHASE4-N-G S5)*

> **Status carried from prior CODEMAP — unchanged at HEAD `75f75da`.** PHASE4-N-I did not touch this GREEN sub-tree. Carry-forward.

| Attribute | Value |
|-----------|-------|
| **Purpose** | Bridges PHASE4-N-C's `BroadcastQueue` (RED) and PHASE4-N-G's `ServedChainSnapshot` (BLUE) to the `ServedHeaderLookup` / `ServedRangeLookup` trait seams the BLUE server reducers consume. |
| **Creates** | `ServedChainLookups<'a>`; function `drain_and_admit`. |
| **MUST NOT** | Carry-forward. |
| **Inbound deps** | `ade_runtime::network::n2n_server`; the integration tests under `ade_runtime/tests/`. |
| **Outbound deps** | `ade_ledger::{producer, block_validity}`, `ade_network::{chain_sync::server, block_fetch::server, codec::chain_sync}`, `ade_types`. |
| **Entry points** | `ade_runtime::producer::broadcast_to_served::drain_and_admit`, `ade_runtime::producer::served_chain_lookups::ServedChainLookups`. |

---

### `ade_runtime::receive::{events_to_state, in_memory_chain_write}` *(GREEN — PHASE4-N-H S3)*

> **Status change at HEAD `75f75da` — minor extension to `in_memory_chain_write.rs` only.** PHASE4-N-I S3 extended the `ChainDbWrite` trait with the new method `rollback_to_slot(slot: SlotNo)`; the GREEN `ChainDbWriter<'a, D: ChainDb>` impl was extended to implement the new method via `ChainDb::rollback_to_slot` over the underlying chain store (+10 LOC delta in `in_memory_chain_write.rs`; orchestrator gained 1 LOC of carrier wiring). `events_to_state.rs` is fully unchanged.

| Attribute | Value |
|-----------|-------|
| **Purpose** | Bridges PHASE4-N-A's `ade_network::chain_sync::signal::ForkChoiceSignal` and `ade_network::block_fetch::event::BatchDeliveryEvent` to the BLUE `ade_ledger::receive::ReceiveEvent` stream, and wires the BLUE `ChainDbWrite` trait to any `ade_runtime::chaindb::ChainDb` impl. **NEW — PHASE4-N-I:** the `ChainDbWriter` impl now also implements `ChainDbWrite::rollback_to_slot` — the BLUE rollback commit helper's irreversible step composes through this GREEN adapter onto the underlying `ChainDb`. |
| **Creates** | Functions `lift_chain_sync_signal` + `lift_block_fetch_event` (in `events_to_state`). Struct `ChainDbWriter<'a, D: ChainDb>` with `impl<'a, D: ChainDb> ChainDbWrite for ChainDbWriter<'a, D>` — write_admitted derives `(slot, hash)` via `decode_block` and calls `ChainDb::put_block`; **NEW — `rollback_to_slot` calls `ChainDb::rollback_to_slot` over the underlying chain store and maps the closed `ChainDbError` to `ChainWriteError`**. |
| **MUST NOT** | (1) Affect authoritative outputs. (2) Introduce nondeterminism (no `tokio`, no `SystemTime`/`Instant`, no `rand`, no `HashMap` — `ci/ci_check_receive_replay_purity.sh`). (3) `events_to_state` MUST NOT decode header_bytes or block_bytes. (4) `in_memory_chain_write` MAY decode the AdmittedBlock bytes once to derive the `(slot, hash)` key. (5–7) Carry-forward. **(8) NEW — `in_memory_chain_write::ChainDbWriter::rollback_to_slot` (PHASE4-N-I S3 extension):** MUST forward to `ChainDb::rollback_to_slot` directly — no parallel rollback path; MUST map the closed `ChainDbError` to `ChainWriteError` via the existing private helper (no panic, no `unwrap`). |
| **Inbound deps** | `ade_runtime::receive::orchestrator`; the integration tests under `ade_runtime/tests/receive_*.rs`. **NEW — `ade_runtime::tests::receive_rollback_integration` consumes `ChainDbWriter` as the `ChainDbWrite` impl driven by the new `roll_backward` path through `commit_rollback::chain_write.rollback_to_slot`.** |
| **Outbound deps** | `ade_ledger::receive::{ReceiveEvent, TargetPoint, TipPoint, AdmittedBlock, ChainDbWrite, ChainWriteError, ChainWriteErrorKind}`, `ade_ledger::block_validity::decode_block`, `ade_network::{chain_sync::signal::*, block_fetch::event::*}`, `ade_types::{Hash32, SlotNo}`, `crate::chaindb::{ChainDb, ChainDbError, StoredBlock}`. No new edges in N-I. |
| **Entry points** | `ade_runtime::receive::events_to_state::{lift_chain_sync_signal, lift_block_fetch_event}`, `ade_runtime::receive::in_memory_chain_write::ChainDbWriter`. |

> **GREEN/RED partition risk note (carried).** The GREEN sub-classification rests on (a) module-header convention, (b) `ci_check_receive_replay_purity.sh`, and (c) the negative-grep enforcement of the no-decode rule on `events_to_state`. The `rollback_to_slot` extension is gated by review + the existing replay-purity check.

---

### `ade_runtime::rollback::{cadence, in_memory_cache, chaindb_block_source}` *(GREEN — PHASE4-N-I S4, NEW)*

> **Status: NEW at HEAD `75f75da`.** A GREEN sub-trio physically inside the RED `ade_runtime` crate prefix. The shared crate prefix is RED per `.idd-config.json` `_core_paths_doc`; the GREEN sub-classification of these three specific files is **by file** — `// Core Contract:` banner present, file-header docstring naming the GREEN classification, and CI gate `ci/ci_check_snapshot_cadence_purity.sh` pinning the pure-fn + cadence-purity + single-field-discipline shape mechanically. Documented as a sub-section of `ade_runtime` in the RED block below; surfaced here for the GREEN inventory.

| Attribute | Value |
|-----------|-------|
| **Purpose** | Production implementations of the BLUE `ade_ledger::rollback` trait seams + the cadence policy that decides when to snapshot. `cadence` is the pure decision function `should_snapshot_after_block(slot, block_no, cadence, last_snapshot) -> bool` plus the BLUE-structural single-field `SnapshotCadence { every_n_blocks: u32 }` type (operator-tunable cadence pinned out of scope by the single-field discipline). `in_memory_cache` is the `InMemorySnapshotCache` impl of `SnapshotReader` — BTreeMap-backed canonical iteration. `chaindb_block_source` adapts any `ChainDb` impl into a `BlockSource` for the replay-forward driver. |
| **Creates** | **`cadence.rs`:** `SnapshotCadence { every_n_blocks: u32 }` with `DEFAULT = 100` and `impl Default`; function `should_snapshot_after_block(SlotNo, BlockNo, SnapshotCadence, Option<SlotNo>) -> bool`. **`in_memory_cache.rs`:** `InMemorySnapshotCache { entries: BTreeMap<SlotNo, (LedgerState, PraosChainDepState)> }` with methods `new`, `admit(slot, ledger, chain_dep)`, `capture_from(slot, &ReceiveState)`, `len`, `is_empty`, `oldest`, `most_recent`, `slots`, `iter_for_test`; `impl SnapshotReader for InMemorySnapshotCache` returning the largest key ≤ target via `BTreeMap::range(..=target_slot).last()`. **`chaindb_block_source.rs`:** `ChainDbBlockSource<'a, D: ChainDb> { pub db: &'a D }` with `new(db)`; `impl<'a, D: ChainDb> BlockSource for ChainDbBlockSource<'a, D>` returning `Vec<(SlotNo, Vec<u8>)>` over `ChainDb::iter_from_slot(from_exclusive + 1)` until iterator yields slot > to_inclusive (closed-on-upper, open-on-lower per the BLUE trait contract). |
| **MUST NOT** | (1) Affect authoritative outputs — these are deterministic projections + a thin write adapter. (2) Introduce nondeterminism — no `tokio`, no `SystemTime`/`Instant`, no `rand`, no `HashMap` (`ci/ci_check_snapshot_cadence_purity.sh`). (3) `SnapshotCadence` MUST have exactly one field (`every_n_blocks: u32`) — operator-tunable cadence is explicitly out of scope per DC-STORE-07 (operator-tunable cadence would weaken the replay-equivalence guarantee — see DC-STORE-07's wording: "Same canonical input chain history produces the same set of snapshot slot keys. Cadence is BLUE-structural; not operator-tunable in this cluster"). Single-field discipline gated mechanically by the same script. (4) `should_snapshot_after_block` MUST remain a pure function — no `&self`, no captured state; all inputs are explicit parameters. (5) `InMemorySnapshotCache` MUST use `BTreeMap` — `HashMap` would violate the determinism rule and produce non-canonical snapshot-slot enumeration. (6) `InMemorySnapshotCache::nearest_le` MUST return owned `(LedgerState, PraosChainDepState)` (clones) — the materialize driver folds mutably and needs ownership. (7) `ChainDbBlockSource` MUST honor the BLUE trait's closed-on-upper / open-on-lower contract — `iter_from_slot(from_exclusive + 1)` is correct because `ChainDb::iter_from_slot` is inclusive on the lower bound. (8) MUST NOT introduce a second SnapshotReader or BlockSource impl in the production tree without explicit cluster-doc opt-in (single production-impl discipline; the BLUE driver is composition-agnostic by design but the production stack is single-implementation by convention). |
| **Inbound deps** | `ade_runtime::rollback::snapshot_writer` (RED hook calls into `InMemorySnapshotCache::capture_from` + `should_snapshot_after_block` + `most_recent`); the integration test under `ade_runtime/tests/receive_rollback_integration.rs`. |
| **Outbound deps** | `ade_ledger::rollback::{SnapshotReader, BlockSource}`, `ade_ledger::receive::ReceiveState`, `ade_ledger::state::LedgerState`, `ade_core::consensus::praos_state::PraosChainDepState`, `ade_types::{SlotNo, BlockNo}`, `crate::chaindb::ChainDb`. **Crate-level edges already established** by PHASE4-N-G's `ade_runtime → ade_network` and the pre-existing `ade_runtime → ade_ledger` / `ade_runtime → ade_core`. |
| **Entry points** | `ade_runtime::rollback::cadence::{SnapshotCadence, should_snapshot_after_block}`, `ade_runtime::rollback::in_memory_cache::InMemorySnapshotCache`, `ade_runtime::rollback::chaindb_block_source::ChainDbBlockSource`. All three re-exported at `ade_runtime::rollback::*` via `pub use`. |

> **GREEN/RED partition risk note.** The GREEN sub-classification of `cadence` + `in_memory_cache` + `chaindb_block_source` rests on (a) module-header convention, (b) the pure-fn + single-field shape gate `ci_check_snapshot_cadence_purity.sh`, and (c) the `BTreeMap`-only / no-wall-clock / no-tokio enforcement in the same script. The broader BLUE/GREEN/RED partition inside the `ade_runtime` crate prefix remains "by file" — same risk class as `ade_runtime::consensus::candidate_fragment` (GREEN, no dedicated CI script for the broad partition), the N-G `producer::broadcast_to_served` / `producer::served_chain_lookups` pair, and the N-H `receive::events_to_state` / `receive::in_memory_chain_write` pair. This is a **carried gap** of the same shape.

---

## RED Modules — Imperative Shell

> I/O, network, storage, clocks, retries. May depend on BLUE/GREEN. Must not modify core state directly or construct semantic types unsafely.

### `ade_core_interop` *(PHASE4-N-B S-B10; PHASE4-N-E S4/S5/S6; PHASE4-N-C S7; PHASE4-N-G S7; PHASE4-N-H S6)*

> **Status carried from prior CODEMAP — unchanged at HEAD `75f75da`.** PHASE4-N-I did not add a new live binary or new operator-action surface — the rollback path is exercised end-to-end through the existing `live_block_follow_session` operator-action binary once the operator wires the socket layer (`RollBackward` lifts through the existing N-H pipeline and now reaches the new `roll_backward` arm when `rollback_ctx` is supplied; today the binary's `--connect` mode is still honest-scope stubbed pending operator-action). The CE-N-H-6 procedure doc carries forward as the live-evidence procedure for the rollback half too.

| Attribute | Value |
|-----------|-------|
| **Purpose** | Live cardano-node interop driver. Hosts the five `live_*_session` RED binaries plus the N-E S4/S5 GREEN bridges (`tx_submission` + `local_tx_submission`). |
| **Creates** | `fresh_orchestrator` (N-B). N-E S4 sub-module: `PeerAccumulator` (GREEN). N-E S5 sub-module: `ClientAccumulator` (GREEN). Functions: `event_to_ingress` + `ingest_n2n_events` (S4); `local_event_to_ingress` + `ingest_n2c_events` (S5). The five `live_*_session` binaries are RED glue with no public Rust types. |
| **Interprets** | Live N2N byte streams from a real cardano-node peer (carry-forward across N-B + N-E + N-C + N-G + N-H). |
| **MUST NOT** | (1)–(11) carry-forward (see prior CODEMAP). No N-I additions. |
| **Inbound deps** | None. |
| **Outbound deps** | `ade_core`, `ade_codec`, `ade_crypto`, `ade_ledger`, `ade_runtime`, `ade_network`, `ade_testkit`, `ade_types`, `tokio`. No new edges in N-I. |
| **Entry points** | `ade_core_interop::fresh_orchestrator(k, initial_nonce)`, binaries `live_consensus_session` + `live_tx_submission_session` + `live_block_production_session` + `live_block_fetch_session` + `live_block_follow_session`. |
| **Key modules** | `src/lib.rs`, `src/follow.rs` (N-B), `src/tx_submission.rs` (N-E S4), `src/local_tx_submission.rs` (N-E S5), `src/bin/live_consensus_session.rs`, `src/bin/live_tx_submission_session.rs` (N-E S6), `src/bin/live_block_production_session.rs` (N-C S7), `src/bin/live_block_fetch_session.rs` (N-G S7), `src/bin/live_block_follow_session.rs` (N-H S6). |

> **Gap surfaced (carried).** CE-N-B-6 closes via manual evidence log against a real cardano-node peer; live capture pending operator run. CE-N-E-6 is CLOSED at HEAD. CE-N-E-7 is deferred as `CE-NODE-N2C-LTX`. CE-N-C-8 is at `status = "enforced"` with `open_obligation` on `CN-CONS-06`: `blocked_until_operator_stake_available`. CE-N-G-8 (RO-LIVE-01) is at `status = "partial"` with `open_obligation = "blocked_until_operator_peer_available"`. CE-N-H-6 (RO-LIVE-02) is at `status = "partial"` with the same operator-action obligation. **No new RO-LIVE rule in PHASE4-N-I** — the rollback path's live half rides on the same `live_block_follow_session` binary as the admit path (the binary's RollBackward dispatch would now reach the new `roll_backward` arm if/when the operator wires the socket layer and supplies a rollback context).

---

### `ade_network` *(RED submodules + capture binaries)*

> **Status carried from prior CODEMAP — unchanged at HEAD `75f75da`.** The RED half of `ade_network`: `mux::transport`, `session`, and the 7 `src/bin/capture_*` binaries. PHASE4-N-I did not touch this half of the crate.

| Attribute | Value |
|-----------|-------|
| **Purpose** | The only place inside `ade_network` where I/O happens — tokio-based TCP / Unix-socket scaffolding (`mux::transport`), the session-composition placeholder (`session`), and capture binaries that perform real handshakes against pinned cardano-node 11.0.1 peers. |
| **Creates** | `MuxTransport`. `session/mod.rs` is an empty placeholder. The capture binaries produce no public Rust types. |
| **Interprets** | Live TCP / Unix-socket byte streams; bytes flow into the BLUE mux frame decoder and protocol codecs without re-interpretation. |
| **MUST NOT** | (1) Contain protocol logic. (2) Define or modify any closed wire-message enum, agency type, or state. (3) Read a selected protocol version from ambient state. (4) Replace BLUE codec / state machine with a "fast path". (5) Bypass `ade_codec`'s `PreservedCbor` chokepoints. (6) Be depended on by any BLUE submodule of `ade_network`. |
| **Inbound deps** | `ade_core_interop`. At the library level no BLUE crate imports the RED submodules. |
| **Outbound deps** | `tokio`, `ade_types`, `ade_codec`, plus BLUE `ade_network::codec::*` and `ade_network::mux::frame`. |
| **Entry points** | `ade_network::mux::transport::{MuxTransport, open_tcp}`. The capture binaries. |
| **Key modules** | `mux/transport.rs`, `session/mod.rs` (placeholder), `bin/capture_*`. |

> **Gap surfaced (carried, unchanged at HEAD).** `ade_network::session::mod.rs` is still a placeholder. The N-G/N-H honest-scope stubs of the tokio socket wiring still sit at the boundary between the pure dispatch drivers in `ade_runtime::{network::n2n_server, receive::orchestrator}` and the absent session-composition layer. PHASE4-N-I did not surface or close any N-network gap.

---

### `ade_node`

> **Status carried from prior CODEMAP — unchanged at HEAD `75f75da`.** Still a hello-world stub. PHASE4-N-I did not touch `ade_node`; the rollback bridge is reachable today through the new integration test under `ade_runtime/tests/receive_rollback_integration.rs`, not through any `ade_node` orchestration.

| Attribute | Value |
|-----------|-------|
| **Purpose** | Binary entry point for the node process. Reserved as the assembly point for runtime + ledger + chain DB + network + consensus orchestrator + mempool + the deferred N2C server + the producer-side N2N server + the receive-side N2N client pump + **NEW — the snapshot-cache plumbing + rollback-context wiring (now sketched but not assembled into a node binary)**. |
| **Creates** | None. |
| **Interprets** | None yet. |
| **MUST NOT** | (1) Bypass `ade_codec` to construct semantic types from bytes. (2) Modify `ade_ledger` or `ade_core::consensus` state in place — transitions go through `ade_ledger::rules::*`, `ade_ledger::block_validity::*`, `ade_ledger::tx_validity::*`, `ade_ledger::mempool::admit`, `ade_ledger::mempool::mempool_ingress`, `ade_ledger::producer::forge::forge_block` + `ade_ledger::producer::self_accept::self_accept`, `ade_ledger::producer::served_chain::served_chain_admit`, `ade_ledger::receive::admit_via_block_validity` + `ade_ledger::receive::receive_apply`, **`ade_ledger::rollback::materialize_rolled_back_state` + `ade_ledger::rollback::commit_rollback`** (NEW — N-I), or `ade_core::consensus::*` BLUE transitions. (3) Take an inbound dep from any other crate. (4) Read versions out of ambient context. (5) Construct an `OrchestratorState` outside `ade_runtime::consensus::chain_selector` or `ade_core_interop::fresh_orchestrator`. (6) Forward-looking — must reach the mempool only through `mempool_ingress`. (7) Forward-looking `CE-NODE-N2C-LTX` — must drive the GREEN bridge, not introduce a parallel path. (8) PP-S1 forward-looking — typed `Vec<ProposalProcedure>` only. (9) Forward-looking PHASE4-N-C / CN-CONS-07 — drive `forge_block` → `self_accept`. (10) Forward-looking PHASE4-N-G / CN-CONS-07 strengthening + DC-CONS-17/18 + CN-PROTO-06 — drive the producer-side N2N server pump only via `ade_runtime::network::n2n_server`. (11) Forward-looking PHASE4-N-H / CN-CONS-07 + CN-CONS-08 + DC-CONS-19 + DC-PROTO-09 + CN-PROTO-07 — drive the receive-side N2N client pump only via `ade_runtime::receive::orchestrator`. **(12) NEW — Forward-looking PHASE4-N-I / CN-STORE-07 + DC-CONS-20 + DC-CONS-22 + DC-STORE-07:** any `ade_node`-level rollback wiring MUST drive `ade_runtime::receive::orchestrator::dispatch_*_inbound` with a `RollbackContext { snapshot_reader: &cache, block_source: &chaindb_source }` constructed from `InMemorySnapshotCache` + `ChainDbBlockSource` (the production trait impls) — no parallel `SnapshotReader` or `BlockSource` impl outside that pair; MUST plumb the cadence-driven snapshot capture via `maybe_capture_snapshot(&mut cache, cadence, &effect, &state)` after every `dispatch_*_inbound` call returns an `Admitted` effect (per S5's contract — DC-STORE-07 cadence determinism + DC-CONS-22 replay-forward correctness); MUST NOT introduce a parallel `materialize_rolled_back_state` (CN-STORE-07 single-authority — repo-wide grep gate already enforces); MUST NOT call `commit_rollback` directly — the only call site is `receive::reducer::roll_backward` (additive `RollbackContext`-gated branch of `receive_apply`); MUST NOT bypass the staged-then-committed shape of `commit_rollback` (irreversible step first; failure leaves state unchanged); MUST surface `MaterializeError::EraNotSupported` to the operator surface as a peer-halt signal at this cluster's scope (pre-Conway rollback is out of scope; a follow-on cluster widens the supported era set if needed); the default `ade_node` binary MUST stay hermetic — rollback-context wiring requires explicit operator opt-in matching the existing producer-mode + receiving-mode precedents. **DC-CONS-21 forward-looking note:** when the persistent snapshot encoder lands (the carved-out follow-on cluster), the operator surface gains an on-disk snapshot store as the second `SnapshotReader` production impl; the `InMemorySnapshotCache` remains the in-process cache for warm reads. The follow-on cluster owns the encode/decode round-trip + fingerprint-binding rule. |
| **Inbound deps** | None. |
| **Outbound deps** | None at present. |
| **Entry points** | `main()` in `crates/ade_node/src/main.rs`. |

> **Gap surfaced (carried).** `ade_node` is still a hello-world stub; its MUST NOT list is forward-looking with nothing to enforce today.

---

### `ade_runtime`

> **Status change at HEAD `75f75da` (PHASE4-N-I S3 + S4 + S5 — one new top-level module `rollback/` containing three GREEN files + one RED file, plus a small `receive/in_memory_chain_write.rs` extension wiring the new BLUE `ChainDbWrite::rollback_to_slot` trait method).** Between `efe1fb9` and HEAD, `ade_runtime` gained the new `rollback/` top-level module: (a) three GREEN files documented above in the GREEN block — `rollback/cadence.rs` (S4; pure decision function + single-field cadence type), `rollback/in_memory_cache.rs` (S4; `InMemorySnapshotCache` impl `SnapshotReader`), `rollback/chaindb_block_source.rs` (S4; `ChainDbBlockSource<'a, D: ChainDb>` impl `BlockSource`); (b) one RED file `rollback/snapshot_writer.rs` (S5; `maybe_capture_snapshot` post-admission hook). The `receive/in_memory_chain_write.rs::ChainDbWriter` impl was extended to implement the new BLUE `ChainDbWrite::rollback_to_slot` method by forwarding to `ChainDb::rollback_to_slot` over the underlying chain store (+10 LOC). `receive/orchestrator.rs` gained 1 LOC of trait-extension carrier wiring. All prior N-H / N-G / N-D / N-B / N-A / N-C narrative carries forward unchanged.

| Attribute | Value |
|-----------|-------|
| **Purpose** | The imperative shell. Hosts (a) the `ChainDb` / `SnapshotStore` storage abstractions (N-D); (b) the GREEN chain-selector orchestrator + GREEN `CandidateFragment` materializer (N-B); (c) the RED genesis-text parser (N-B); (d) the RED producer-side scaffolding (`producer/`); (e) the RED producer-side N2N server pump (`network/n2n_server.rs`, N-G S6); (f) the GREEN broadcast-to-served adapter (`producer/{broadcast_to_served, served_chain_lookups}.rs`, N-G S5); (g) the GREEN receive-side adapter pair + the RED receive-side N2N client orchestrator (`receive/`, N-H S3 + S4); **(h) NEW — the GREEN rollback adapter trio (`rollback/{cadence, in_memory_cache, chaindb_block_source}.rs`, N-I S4) implementing the BLUE `SnapshotReader` + `BlockSource` trait seams plus the cadence decision function with its single-field BLUE-structural `SnapshotCadence`; (i) NEW — the RED snapshot-write hook (`rollback/snapshot_writer.rs`, N-I S5) — `maybe_capture_snapshot` composes RED dispatch outcomes (`ReceiveEffect`) with the GREEN cadence + cache to capture per-peer snapshots after each cadence-elected admission**. The crate remains RED at the prefix level; `chain_selector.rs` / `candidate_fragment.rs` / `tick_assembler.rs` / `broadcast_to_served.rs` / `served_chain_lookups.rs` / `receive/events_to_state.rs` / `receive/in_memory_chain_write.rs` / **NEW — `rollback/cadence.rs` / `rollback/in_memory_cache.rs` / `rollback/chaindb_block_source.rs`** are GREEN by file-level classification; `genesis_parser.rs` / `signing.rs` / `keys.rs` / `scheduler.rs` / `broadcast.rs` / `network/n2n_server.rs` / `receive/orchestrator.rs` / **NEW — `rollback/snapshot_writer.rs`** are RED. |
| **Creates** | Carry-forward (all N-H + earlier types). **NEW — PHASE4-N-I S4 (GREEN):** `SnapshotCadence { every_n_blocks: u32 }` + `should_snapshot_after_block(SlotNo, BlockNo, SnapshotCadence, Option<SlotNo>) -> bool` (in `rollback::cadence`); `InMemorySnapshotCache { entries: BTreeMap<SlotNo, (LedgerState, PraosChainDepState)> }` + `impl SnapshotReader for InMemorySnapshotCache` (in `rollback::in_memory_cache`); `ChainDbBlockSource<'a, D: ChainDb> { pub db: &'a D }` + `impl<'a, D: ChainDb> BlockSource for ChainDbBlockSource<'a, D>` (in `rollback::chaindb_block_source`). **NEW — PHASE4-N-I S5 (RED):** function `maybe_capture_snapshot(&mut InMemorySnapshotCache, SnapshotCadence, &ReceiveEffect, &ReceiveState) -> bool` (in `rollback::snapshot_writer`). |
| **Interprets** | Block bytes for content-addressed storage. Genesis JSON. N-A `StreamInput` events. PHASE4-N-C/N-G/N-H carry-forward. **NEW — PHASE4-N-I:** `rollback::cadence::should_snapshot_after_block` interprets only the four explicit parameter values (pure decision); `rollback::in_memory_cache::InMemorySnapshotCache::nearest_le` interprets only the `target_slot` parameter (pure BTreeMap query); `rollback::chaindb_block_source::ChainDbBlockSource::blocks_in_range` interprets only the two slot parameters (pure ChainDb iteration); `rollback::snapshot_writer::maybe_capture_snapshot` interprets only the four parameters (pure decision + in-memory write). No file in `ade_runtime::rollback` decodes block bytes — replay-forward decode happens once inside the BLUE `rollback::materialize_rolled_back_state` driver (via `decode_block_envelope` for era detection + `block_validity::transition::block_validity` for replay). |
| **MUST NOT** | (1) ~~Depend on `ade_ledger`.~~ Carry-forward exception (RED → BLUE legal direction). (2) Leak `redb` through the `chaindb::*` public surface. (3) Have a second public chaindb path. (4) Couple snapshots to a specific state type. (5) Apply automatic snapshot pruning. (6) Allow partial-recovery success. (7) Add async recovery surface. (8) Treat "not found" as an error path or silently retry on real errors. (9) Consensus sub-tree GREEN `chain_selector` must not affect authoritative outputs. (10) Genesis parser — structured errors only. (11) `candidate_fragment` materializer — pure constructor. (12–22) PHASE4-N-C + N-G + N-H carry-forward MUST NOTs. **(23) NEW — `rollback::cadence` (DC-STORE-07, S4, GREEN):** MUST NOT import wall-clock / tokio / rand / HashMap (enforced by `ci/ci_check_snapshot_cadence_purity.sh`). `SnapshotCadence` MUST have exactly one field — operator-tunable cadence is explicitly out of scope; the single-field discipline pins the BLUE-structural property at the type level (operator-tunable cadence would weaken DC-STORE-07's replay-equivalence guarantee). The decision function MUST remain pure — no `&self`, no captured state. **(24) NEW — `rollback::in_memory_cache` (DC-STORE-07, S4, GREEN):** MUST use `BTreeMap` — `HashMap` would violate determinism + produce non-canonical slot enumeration. The `entries` field MUST remain private. `nearest_le` MUST return owned `(LedgerState, PraosChainDepState)` — the materialize driver needs ownership for the mutable fold. **(25) NEW — `rollback::chaindb_block_source` (S4, GREEN):** MUST honor the BLUE trait's closed-on-upper / open-on-lower contract — `iter_from_slot(from_exclusive + 1)` (saturating) is the correct adapter for `ChainDb::iter_from_slot`'s inclusive-on-lower contract. MUST NOT cache or memoize across calls — `ChainDbBlockSource<'a, D>` is a transient view. **(26) NEW — `rollback::snapshot_writer` (DC-STORE-07 + DC-CONS-22 grounding, S5, RED):** MUST NOT introduce a parallel snapshot-capture path — `maybe_capture_snapshot` is the sole snapshot-capture site at this cluster's scope; new capture paths would weaken cadence determinism. MUST gate on `ReceiveEffect::Admitted` only — `Cached`/`RolledBack`/`NoOp` effects MUST return false (rolling back invalidates cached state; a snapshot taken post-rollback would be captured at the next `Admitted` effect via the normal cadence path). MUST consult `state.chain_dep.last_block_no` — block-no-based cadence rather than slot-based avoids the empty-slot edge case. MUST call `cache.most_recent()` as the `last_snapshot` input — same-slot reinsertion is blocked at the cadence-decision layer. MUST NOT capture on `last_block_no == None` (chain head not yet advanced — premature). |
| **Inbound deps** | `ade_testkit` (non-dev dep, used by `stream_replay`), `ade_core_interop` (heavy consumer at HEAD — all five `live_*_session` binaries import from `ade_runtime`). The binary `chaindb_kill_target` lives at `src/bin/`. **NEW heavy consumption sites in PHASE4-N-I** — the integration test `crates/ade_runtime/tests/receive_rollback_integration.rs` consumes `ade_runtime::rollback::{maybe_capture_snapshot, ChainDbBlockSource, InMemorySnapshotCache, SnapshotCadence}` + `ade_runtime::receive::ChainDbWriter` (extended to impl the new `rollback_to_slot` method) + `ade_runtime::chaindb::{InMemoryChainDb, StoredBlock}`. No new outside consumer of `ade_runtime::rollback::*` yet — when the operator-action wiring lands in a future node-binary cluster (forward-looking note 12 on `ade_node`), it will compose `InMemorySnapshotCache` + `ChainDbBlockSource` into a `RollbackContext` per-peer. |
| **Outbound deps** | `ade_types`, `ade_core`, `ade_crypto`, `ade_codec`, `ade_ledger`, `ade_network`, `redb`, `serde_json`, `cardano-crypto`, `ed25519-dalek`. **No new outbound crate edge in PHASE4-N-I** — the new `rollback/` sub-tree reuses the established `ade_runtime → {ade_ledger, ade_core}` edges (`rollback::cadence` imports `ade_types::{SlotNo, BlockNo}`; `rollback::in_memory_cache` imports `ade_ledger::{rollback::SnapshotReader, receive::ReceiveState, state::LedgerState}` + `ade_core::consensus::praos_state::PraosChainDepState` + `ade_types::SlotNo`; `rollback::chaindb_block_source` imports `ade_ledger::rollback::BlockSource` + `ade_types::SlotNo` + `crate::chaindb::ChainDb`; `rollback::snapshot_writer` imports `ade_ledger::receive::{ReceiveEffect, ReceiveState}` + `crate::rollback::cadence::*` + `crate::rollback::in_memory_cache::InMemorySnapshotCache`). Dev-deps: `tempfile`, `ade_testkit`. |
| **Entry points** | All N-H + earlier surfaces (`chaindb::*`, `recovery::*`, `consensus::*`, `producer::*`, `network::*`, `receive::*`). **NEW — PHASE4-N-I `rollback/` (GREEN):** `ade_runtime::rollback::{cadence::{SnapshotCadence, should_snapshot_after_block}, in_memory_cache::InMemorySnapshotCache, chaindb_block_source::ChainDbBlockSource}`. **NEW — PHASE4-N-I `rollback/` (RED):** `ade_runtime::rollback::snapshot_writer::maybe_capture_snapshot`. All re-exported at `ade_runtime::rollback::*` via `pub use`. |
| **Key modules** | All prior keys (`chaindb/`, `recovery.rs`, `consensus/`, `bin/chaindb_kill_target.rs`, `producer/`, `network/`, `receive/`). **NEW — `rollback/`** (PHASE4-N-I S4 + S5): `rollback/mod.rs` (26 LOC barrel; re-exports `should_snapshot_after_block` + `SnapshotCadence` + `ChainDbBlockSource` + `InMemorySnapshotCache` + `maybe_capture_snapshot`), `rollback/cadence.rs` (143 LOC + 5 inline tests, **GREEN**), `rollback/in_memory_cache.rs` (165 LOC + 4 inline tests, **GREEN**), `rollback/chaindb_block_source.rs` (113 LOC + 3 inline tests, **GREEN**), `rollback/snapshot_writer.rs` (146 LOC + 4 inline tests, **RED**). **Integration tests under `tests/`:** carry-forward; **NEW — PHASE4-N-I S6:** `receive_rollback_integration.rs` (445 LOC + 5 tests — `rollback_branch_returns_rolled_back_on_in_memory_snapshot` (DC-CONS-20 happy path: snapshot below target + replay-forward across one Conway corpus block), `rollback_branch_returns_rollback_too_deep_when_no_snapshot` (closed-error path), `rollback_branch_state_unchanged_on_materialize_failure` (failure-atomicity), `rollback_branch_without_ctx_returns_legacy_rollback_out_of_scope` (preserves pre-N-I `None`-context behavior), `rollback_then_continue_admit_equals_straight_line_admit` (integration counterpart of DC-CONS-22: rollback + continue = direct apply)). |
| **Mechanical enforcement** | Carry-forward (chaindb / recovery / crash-safety + 8 N-C + 7 N-G + 5 N-H CI scripts). **NEW — two PHASE4-N-I CI scripts back the rollback surface inside `ade_runtime`:** `ci/ci_check_snapshot_cadence_purity.sh` (DC-STORE-07, S4, scope `crates/ade_runtime/src/rollback/*.rs`). The other new script `ci/ci_check_rollback_materialize_closure.sh` is BLUE-scoped on `crates/ade_ledger/src/rollback/`. **No new `ade_runtime`-presence CI script** for the integration test under `tests/receive_rollback_integration.rs` — none of the prior cluster-presence scripts (`receive_paths_corpus_present`, `server_paths_corpus_present`) were extended to cover the rollback integration test either; the rollback integration test's presence is review-and-doc enforced. |

> **Gap surfaced (carried + extended).** No dedicated CI script enforces the GREEN/RED partition *inside* `ade_runtime::consensus` or `ade_runtime::producer` or `ade_runtime::network` or `ade_runtime::receive`. **NEW — same gap class extends to `ade_runtime::rollback/`:** the GREEN/RED sub-partition is "by file" — `cadence.rs` + `in_memory_cache.rs` + `chaindb_block_source.rs` are GREEN (pure decision + thin adapters, gated by `ci_check_snapshot_cadence_purity.sh` for purity + single-field + impl-presence), `snapshot_writer.rs` is RED (pure decision composing RED dispatch outcomes with the GREEN cadence + cache, file-header banner only); the broader sub-partition rests on file-header banners + review. **NEW — no presence-gate CI script** for the integration test `crates/ade_runtime/tests/receive_rollback_integration.rs` — unlike N-G/N-H which added `ci_check_{server,receive}_paths_corpus_present.sh`. **NEW — no live operator-action binary** for the rollback path's cross-impl evidence — the rollback half rides on the existing N-H `live_block_follow_session` binary (whose `--connect` mode remains honest-scope stubbed pending operator-action); a future operator run will exercise the new `roll_backward` arm naturally if/when the operator supplies a rollback context via the binary's flag set (which itself is forward-looking work). **DC-CONS-21 (persistent encoder) explicit scope carve-out**: the cluster's explicit scope decision (2026-05-26) was to ship the in-memory variant only and defer the persistent on-disk encoder to a follow-on cluster — `DC-CONS-21` stays `declared` with `open_obligation = "persistent_ledger_snapshot_encoding_follow_on_cluster"`. The persistent encoder is ≈1500-2000 LoC of canonical LedgerState field-walk code (mirrors `ade_ledger::fingerprint`'s structure) and was deemed too large to ship in one slice without partial-implementation risk. The in-memory scope still closes `CN-STORE-07`, `DC-CONS-22`, `DC-STORE-07`, and `DC-CONS-20` fully; persistence across restarts is the follow-on cluster's deliverable.

---

## Cross-Module Rules (project-wide)

### Dependency direction

`ade_core_interop` → `{ade_core, ade_codec, ade_crypto, ade_ledger, ade_runtime, ade_network, ade_testkit, ade_types, tokio}` is legal (RED leaf binary). **No new crate-level outbound edge in PHASE4-N-I.**
`ade_runtime` → `{ade_core, ade_crypto, ade_codec, ade_types, ade_ledger, ade_network, redb, serde_json, cardano-crypto, ed25519-dalek}` is legal (RED → BLUE). **No new crate-level outbound edge in PHASE4-N-I** — the new `rollback/` sub-tree reuses the established edges.
`ade_testkit` → `{ade_core, ade_ledger, ade_plutus, ade_runtime, ade_crypto, ade_codec, ade_types, cardano-crypto}` is legal (GREEN).
`ade_network` (BLUE submodules) → `{ade_codec, ade_types}` is legal.
`ade_network` (RED submodules + capture bins) → `{tokio, ade_codec, ade_types, ade_network::codec::*, ade_network::mux::frame}` is legal.
`ade_ledger` → `{ade_core, ade_plutus, ade_crypto, ade_codec, ade_types, minicbor}` is legal (BLUE among BLUEs). **The new `rollback` module reuses these edges — no new outbound from `ade_ledger`** (the rollback driver imports `ade_core::consensus::{era_schedule, ledger_view, praos_state}` + `ade_codec::cbor::envelope::decode_block_envelope` + `ade_types::{CardanoEra, Hash32, SlotNo}` over existing edges).
`ade_core` → `{ade_types, ade_crypto, minicbor}` is legal (BLUE among BLUEs).
`ade_plutus` → `{ade_crypto, ade_codec, ade_types}` is legal.
`ade_crypto` → `{ade_types}` is legal.
`ade_codec` → `{ade_types}` is legal.
`ade_types` → `{}`.

**Forbidden directions.** Any BLUE crate (or BLUE `ade_network` submodule) depending on `ade_runtime`, `ade_node`, `ade_core_interop`, or the RED half of `ade_network` is a CI failure (`ci_check_dependency_boundary.sh` + `ci_check_no_async_in_blue.sh`). Any non-`ade_plutus` crate referring to `pallas_*` is a CI failure (`ci_check_pallas_quarantine.sh`). Any reference to `ChainDb` / `chain_db` inside `crates/ade_core/src/consensus/` is a CI failure (`ci_check_no_chaindb_in_consensus_blue.sh`). B1/B2/N-E — BLUE/GREEN sub-partition inside `ade_ledger::mempool` is review-and-test-enforced for the broad partition, with two narrow grep gates. B3 — deposit-param-authority gate. PP-S1 — `ProposalProcedure {…}` struct-literal scan. PHASE4-N-C dependency notes (the 8 producer CI gates pin specific properties). PHASE4-N-G dependency notes (the 7 producer-side-server CI gates pin specific properties). PHASE4-N-H dependency notes (the 5 receive-side CI gates pin specific properties). **NEW — PHASE4-N-I dependency notes (the 2 rollback-authority CI gates pin specific properties):** any `pub fn` whose signature contains `(LedgerState[^{]*PraosChainDepState` outside `crates/ade_ledger/src/rollback/materialize.rs` (in the rollback module tree) is a CI failure (`ci_check_rollback_materialize_closure.sh`, CN-STORE-07). Any `HashMap`/`HashSet`/wall-clock/`tokio`/`rand` in `crates/ade_ledger/src/rollback/materialize.rs` production code is a CI failure (same script). Absence of a `block_validity` call site or `pub fn materialize_rolled_back_state` in `materialize.rs` is a CI failure (same script — positive presence). Any `HashMap`/`HashSet`/wall-clock/`tokio`/`rand` in `crates/ade_runtime/src/rollback/*.rs` production code is a CI failure (`ci_check_snapshot_cadence_purity.sh`, DC-STORE-07). `SnapshotCadence` with anything other than exactly one `pub <type>:` field is a CI failure (same script). `SnapshotCadence` lacking an `every_n_blocks` field is a CI failure (same script — operator-tunable cadence pinned out of scope). Absence of `pub fn should_snapshot_after_block` in `cadence.rs` or `impl SnapshotReader for InMemorySnapshotCache` in `in_memory_cache.rs` or `impl<...> BlockSource for ChainDbBlockSource` in `chaindb_block_source.rs` is a CI failure (same script — positive presence).

### Naming convention

All crates are prefixed `ade_`. TCB color is not encoded in the crate name. The authoritative classifier is `.idd-config.json` `core_paths` plus the cluster doc TCB Color Maps for sub-crate scopes; CI scripts hard-code their BLUE list. The seven full-BLUE-scoped scripts use the full BLUE set: 6 BLUE crates + 9 BLUE `ade_network` paths. The B3 `ci_check_deposit_param_authority.sh` scopes the 6 BLUE crates. Four narrow scripts target `crates/ade_core/src/consensus/`. The two N-E scripts scope `crates/ade_ledger/src/mempool/*` + `crates/ade_testkit/src/mempool/*`. The PP-S1 `ci_check_proposal_procedures_closed.sh` scopes `conway/{governance.rs, tx.rs}` paths. The 8 N-C scripts narrow as previously documented. The 7 N-G scripts narrow as previously documented. The 5 N-H scripts narrow as previously documented. **NEW — the 2 PHASE4-N-I scripts narrow as follows:** `ci_check_rollback_materialize_closure.sh` scopes `crates/ade_ledger/src/rollback/materialize.rs` (production-purity scan) + iterates `crates/ade_ledger/src/rollback/*.rs` (CN-STORE-07 single-authority scan) + uses a comment-stripping AWK helper to avoid false-positive grep hits inside doc-comments / `cfg(test)` blocks; `ci_check_snapshot_cadence_purity.sh` scopes `crates/ade_runtime/src/rollback/*.rs` (all four files: cadence + in_memory_cache + chaindb_block_source + snapshot_writer) + uses the same AWK comment-stripping helper + extracts the `SnapshotCadence` struct body with a small AWK braces-tracker to count fields. **No CI script narrowly scopes the new rollback integration test** under `crates/ade_runtime/tests/receive_rollback_integration.rs` — the closure evidence rests on the inline tests in `materialize.rs` + `commit.rs` plus the integration file itself (no operator-action live binary was added by N-I that would warrant a presence gate).

### CI enforcement (54 scripts under `ci/`)

| Script | Enforces | Scope |
|---|---|---|
| `ci_check_admitted_block_closure.sh` *(PHASE4-N-H S1)* | CE-N-H-1 + CN-CONS-08 + CN-PROTO-07 — single `AdmittedBlock` construction site + events.rs closure | repo-wide grep gate + `crates/ade_ledger/src/receive/events.rs` |
| `ci_check_block_fetch_server_closure.sh` *(PHASE4-N-G S4)* | DC-CONS-17 foundation | `crates/ade_network/src/block_fetch/server.rs` |
| `ci_check_broadcast_to_served_purity.sh` *(PHASE4-N-G S5)* | CE-N-G-5 | `crates/ade_runtime/src/producer/{broadcast_to_served,served_chain_lookups}.rs` |
| `ci_check_cbor_round_trip.sh` | T-ENC-03, DC-CBOR-01, DC-CBOR-02 | golden corpus |
| `ci_check_ce_n_a_5_proof.sh` *(PHASE4-N-A, S-A10)* | CE-N-A-5 5-condition evidence | `ade_network` (RED + real-capture corpus) |
| `ci_check_chaindb_contract.sh` | DC-STORE-02, DC-STORE-03, CN-STORE-04, CN-STORE-05 | `ade_runtime --lib chaindb::` (RED) |
| `ci_check_chaindb_crash_safety.sh` | T-REC-01 (crash variant), DC-STORE-01, CN-STORE-03 | `ade_runtime --test stress_kill_harness` (RED) |
| `ci_check_chain_sync_server_closure.sh` *(PHASE4-N-G S3)* | DC-PROTO-08 | `crates/ade_network/src/chain_sync/server.rs` |
| `ci_check_consensus_closed_enums.sh` | DC-CONS-04, DC-CONS-10, T-DET-01, DC-VAL-02/04/05/06, DC-TXV-01/02/04/05, DC-MEM-01/02 | `consensus/` + `block_validity/` + `tx_validity/` + `mempool/` |
| `ci_check_constitution_coverage.sh` | invariant-registry ↔ code/test coverage | repo-wide |
| `ci_check_conway_cert_classification_closed.sh` *(PHASE4-B3F)* | DC-TXV-06 | `ade_types::conway::cert` + `ade_codec::conway::cert` + `ade_ledger::cert_classify` |
| `ci_check_credential_discriminant_closed.sh` *(OQ5)* | DC-LEDGER-10 | `ade_types::shelley::cert` + both era `ade_codec` decoders + `ade_ledger::{state, governance, fingerprint, rules}` + `ade_types::conway::governance` |
| `ci_check_crypto_vectors.sh` | crypto KAT regression | `ade_crypto` |
| `ci_check_dependency_boundary.sh` | T-BOUND-02 — BLUE ⇎ RED separation | full BLUE |
| `ci_check_deposit_param_authority.sh` *(PHASE4-B3)* | DC-TXV-07 | 6 BLUE crates |
| `ci_check_differential_divergence.sh` | DC-DIFF-* | replay outputs |
| `ci_check_forbidden_patterns.sh` | T-CORE-02 + `unsafe` allowlist | full BLUE |
| `ci_check_forge_purity.sh` *(PHASE4-N-C S3)* | DC-CONS-13/14/15 | `crates/ade_ledger/src/producer/forge.rs` |
| `ci_check_gov_cert_accumulation_closed.sh` *(PHASE4-B5)* | DC-LEDGER-09 | `ade_ledger::{gov_cert, rules, error, state}` |
| `ci_check_hash_uses_wire_bytes.sh` | DC-CBOR-02, T-ENC-01 | full BLUE |
| `ci_check_hfc_translation.sh` | DC-EPOCH-02 | `ade_ledger::hfc` |
| `ci_check_ingress_chokepoints.sh` | DC-INGRESS-01, T-INGRESS-01 | full BLUE |
| `ci_check_ledger_determinism.sh` | DC-LEDGER-01 | `ade_ledger` |
| `ci_check_mempool_ingress_closure.sh` *(PHASE4-N-E, S1)* | DC-MEM-03 | `ade_ledger::mempool/{ingress,admit,mod}.rs` + repo-wide `admit(` scan |
| `ci_check_mempool_ingress_replay.sh` *(PHASE4-N-E, S2 + S3)* | DC-MEM-04 | `ade_testkit::mempool/*` + `ade_testkit::lib.rs` + `ade_ledger::mempool::canonicalize` |
| `ci_check_module_headers.sh` | CE-04 contract banner | full BLUE |
| `ci_check_n2n_server_no_signing_dep.sh` *(PHASE4-N-G S6)* | Key-boundary | `crates/ade_runtime/src/network/` |
| `ci_check_no_async_in_blue.sh` *(PHASE4-N-A, S-A1)* | DC-CORE-01 | full BLUE |
| `ci_check_no_chaindb_in_consensus_blue.sh` *(PHASE4-N-B, S-B1)* | DC-CORE-01 + DC-CONS-07 | `crates/ade_core/src/consensus/` |
| `ci_check_no_density_in_fork_choice.sh` *(PHASE4-N-B, S-B8)* | DC-CONS-03 | `fork_choice.rs` + `candidate.rs` |
| `ci_check_no_float_in_consensus.sh` *(PHASE4-N-B, S-B1)* | T-CORE-02 + DC-CONS-07/08/09 | `crates/ade_core/src/consensus/` |
| `ci_check_no_parallel_header_splitter.sh` *(PHASE4-N-G S1)* | DC-CONS-16 strengthening + DC-CONS-18 | repo-wide grep gate |
| `ci_check_no_private_keys_in_corpus.sh` *(PHASE4-N-C S3)* | DC-CRYPTO-03/04/05 corpus discipline | `crates/ade_testkit/src/producer/fixtures.rs` + `crates/ade_testkit/fixtures/producer/` |
| `ci_check_no_producer_body_encoder.sh` *(PHASE4-N-C S4)* | DC-CONS-16 | repo-wide grep gate |
| `ci_check_no_secrets.sh` | no credentials/IPs/keys | repo-wide |
| `ci_check_no_semantic_cfg.sh` | T-BUILD-01 | full BLUE |
| `ci_check_no_signing_in_blue.sh` | CE-05, T-KEY-01 | full BLUE |
| `ci_check_opcert_closed.sh` *(PHASE4-N-C S2)* | DC-CONS-11/12 | `crates/ade_codec/src/shelley/opcert.rs` + `crates/ade_core/src/consensus/opcert_validate.rs` |
| `ci_check_pallas_quarantine.sh` | O-29.2 | non-`ade_plutus` |
| `ci_check_private_key_custody.sh` *(PHASE4-N-C S1)* | DC-CRYPTO-03/04/05 + OP-OPS-04 | full BLUE + `crates/ade_runtime/src/producer/` |
| `ci_check_producer_corpus_present.sh` *(PHASE4-N-C S7)* | CN-CONS-06 mechanical half | `crates/ade_testkit/src/producer/fixtures.rs` |
| `ci_check_proposal_procedures_closed.sh` *(PROPOSAL-PROCEDURES-DECODE PP-S1)* | DC-LEDGER-11 | `conway/{governance,tx}.rs` paths + repo-wide `ProposalProcedure {` scan |
| `ci_check_receive_orchestrator_no_producer_dep.sh` *(PHASE4-N-H S4)* | Key-boundary | `crates/ade_runtime/src/receive/` |
| `ci_check_receive_paths_corpus_present.sh` *(PHASE4-N-H S6)* | CE-N-H-5 + CE-N-H-6 binary presence | N-H integration tests + binary src + procedure doc |
| `ci_check_receive_reducer_closure.sh` *(PHASE4-N-H S2)* | CE-N-H-2 + CN-CONS-08 + DC-CONS-19 | `crates/ade_ledger/src/receive/reducer.rs` |
| `ci_check_receive_replay_purity.sh` *(PHASE4-N-H S3)* | CE-N-H-3 + DC-PROTO-09 | `crates/ade_runtime/src/receive/{events_to_state,in_memory_chain_write}.rs` |
| `ci_check_recovery_contract.sh` | T-REC-01, T-REC-02, DC-STORE-05 | `ade_runtime --lib recovery::` (RED) |
| `ci_check_ref_provenance.sh` | DC-REF-01 | reference corpus |
| **`ci_check_rollback_materialize_closure.sh`** *(NEW — PHASE4-N-I S2)* | **CN-STORE-07 + DC-CONS-22 — single `pub fn` in rollback module tree returning `(LedgerState, PraosChainDepState)` (the function is `materialize_rolled_back_state`); no HashMap/wall-clock/tokio/rand in `materialize.rs`; positive `block_validity` call site (CN-CONS-08 admission-authority composition) + `pub fn materialize_rolled_back_state` presence** | `crates/ade_ledger/src/rollback/materialize.rs` (production-purity) + `crates/ade_ledger/src/rollback/*.rs` (CN-STORE-07 single-authority scan) |
| `ci_check_scheduler_closure.sh` *(PHASE4-N-C S6)* | DC-CONS-13 + DC-MEM-03 + OP-OPS-05 | `crates/ade_runtime/src/producer/{scheduler,broadcast,tick_assembler}.rs` |
| `ci_check_self_accept_gate.sh` *(PHASE4-N-C S5)* | CN-CONS-07 | `crates/ade_ledger/src/producer/self_accept.rs` + repo-wide `AcceptedBlock` literal scan |
| `ci_check_served_chain_closure.sh` *(PHASE4-N-G S2)* | CN-CONS-07 strengthening + CE-N-G-2 | `crates/ade_ledger/src/producer/served_chain.rs` |
| `ci_check_server_paths_corpus_present.sh` *(PHASE4-N-G S7)* | CE-N-G-7 + CE-N-G-8 binary presence | N-G integration tests + binary src + procedure doc |
| **`ci_check_snapshot_cadence_purity.sh`** *(NEW — PHASE4-N-I S4)* | **DC-STORE-07 — no HashMap/wall-clock/tokio/rand in `rollback/*.rs`; SnapshotCadence struct body has exactly one `pub <type>:` field with name containing `every_n_blocks` (operator-tunable cadence out of scope); positive presence of `should_snapshot_after_block` + `impl SnapshotReader for InMemorySnapshotCache` + `impl<...> BlockSource for ChainDbBlockSource`** | `crates/ade_runtime/src/rollback/*.rs` (cadence + in_memory_cache + chaindb_block_source + snapshot_writer) |

> **Post-`efe1fb9` CI delta. +2.** Both new gates are the PHASE4-N-I rollback-authority gate set. **CI inventory 52 → 54.** The forward-looking `ci/ci_check_no_fail_open_in_validation.sh` (DC-VAL-06 / DC-TXV) is still **not** shipped — carried gap.

> **Carried residual gaps (unchanged at HEAD, plus the new N-I-introduced ones).** Carry-forward gaps (a)–(ff) from the prior CODEMAP are all still open EXCEPT that **(ee) NEW closes — DC-CONS-20 (ChainDb-ledger-chain_dep lockstep) now carries `status = "enforced"`** (the rollback-side half is closed at HEAD; both admit-side via CN-CONS-08 + DC-CONS-19 + DC-PROTO-09 AND rollback-side via CN-STORE-07 + DC-CONS-22 + DC-STORE-07 + the atomic `commit_rollback` shape are now mechanically enforced; `strengthened_in` gains `PHASE4-N-I`). **(gg) NEW — `ade_runtime::rollback::{cadence, in_memory_cache, chaindb_block_source}` GREEN sub-classification inside the RED `ade_runtime` crate prefix is partially gated by `ci_check_snapshot_cadence_purity.sh` (pure-fn + single-field shape + impl presence) but the broader BLUE/GREEN/RED partition is "by file" — no dedicated CI script for the broad partition.** **(hh) NEW — `ade_runtime::rollback::snapshot_writer` RED classification is review-and-doc enforced (file-header banner); no narrow script asserts the broader RED partition inside `rollback/`.** **(ii) NEW — no presence-gate CI script for the integration test `crates/ade_runtime/tests/receive_rollback_integration.rs`** — unlike the N-G/N-H precedent that added `ci_check_{server,receive}_paths_corpus_present.sh`. The closure evidence rests on the inline tests in `rollback/materialize.rs` + `rollback/commit.rs` plus the integration file itself; the integration file's presence + the 5 named tests are review-enforced. **(jj) NEW — no live operator-action binary for the rollback path** — the rollback half rides on the existing N-H `live_block_follow_session` binary; the binary's `--connect` mode remains honest-scope stubbed pending operator-action; a future operator run will naturally exercise the new `roll_backward` arm if/when the operator wires the socket layer AND supplies a `RollbackContext` via the binary's flag set (forward-looking). The N-H `RO-LIVE-02` rule continues to carry the live-evidence obligation; no new RO-LIVE rule was added for the rollback half. **(kk) NEW — DC-CONS-21 (persistent snapshot encode/decode round-trip equivalence) carries `status = "declared"` + `open_obligation = "persistent_ledger_snapshot_encoding_follow_on_cluster"`.** Explicit scope decision (2026-05-26): N-I ships the in-memory variant; the persistent on-disk canonical LedgerState encoder is ≈1500-2000 LoC of field-walk code (mirrors `ade_ledger::fingerprint`'s structure) and was deemed too large to ship in one slice without partial-implementation risk. The in-memory scope still closes `CN-STORE-07`, `DC-CONS-22`, `DC-STORE-07`, and `DC-CONS-20` fully; persistence across restarts is the follow-on cluster's deliverable. Reopen criteria: when the persistent encoder cluster lands, DC-CONS-21 flips to `enforced` and the cluster adds a `SnapshotReader` impl over the on-disk store as the second production impl alongside `InMemorySnapshotCache`. **(ll) NEW — the cluster plan implicitly anticipated `ade_testkit::rollback::*` but the shipped artifacts live at `crates/ade_runtime/tests/receive_rollback_integration.rs` instead.** Same shape as the N-G + N-H deviations; intentional (orphan-rule + dev-dep direction); whether `ade_testkit::rollback::*` should be added as a thin re-export shim for cross-crate consumption is a follow-on judgment call — no current consumer would benefit.
