# Module Authority Map — Ade

> **Status:** Living architectural document. Regenerated; not hand-edited.
> Per-project instance of `~/.claude/methodology/templates/codemap.md`.

> 11 crates, 456 canonical types, 2169 tests, 110 CI checks at HEAD (`cd2484f`, PHASE4-N-F-E cluster close).
>
> **PHASE4-N-F-E delta (forge-tick wiring, hermetic / single-epoch / self-accept-only):** **NO BLUE crate changed** (456 canonical types unchanged). All changes land in the RED binary/driver crate `ade_node`. The **GREEN** `ade_node::run_loop_planner` gained a 4th closed `LoopStep` variant `ForgeTick`, a content-blind `ForgeSlotStatus { Due, NotDue }` input, an extended `plan_loop_step(loop_state, sync_status, forge_slot_status, shutdown_status)`, and a pure monotonic guard `forge_slot_status(last_forged_slot, current_slot)` (the only fn there that observes a `SlotNo`). The **RED** `ade_node::node_lifecycle` gained `pub struct ForgeActivation<'a>` + a `ForgeTick` branch in `run_relay_loop` (now `forge: Option<&mut ForgeActivation>`): the branch derives the slot via the clock seam, reuses `CoordinatorState::kes_period_for_slot`, calls **exactly one fenced** `node_sync::forge_one_from_recovered`, advances NO durable tip, and serves/admits/gossips nothing. Forge is opt-in: `None` = exact N-F-D relay behavior; the binary path passes `None`. `ade_node::node_sync` had **no production change** (`#[cfg(test)]` additions only). Two CI gates evolved (count unchanged): `ci_check_loop_planner_closed.sh` (scoped the `SlotNo` ban to `plan_loop_step`; added `ForgeTick`/`ForgeSlotStatus` to the closed set) and `ci_check_node_run_loop_containment.sh` (permits exactly one fenced `forge_one_from_recovered`; retains all tip/serve/admit/`run_real_forge`/evidence prohibitions). Registry → 310 rules; `DC-NODE-05` flipped `declared → enforced`. **Boundary honesty: N-F-E is hermetic, single-epoch, self-accept-only. NO operator-key ingestion in `--mode node`; NO serve/broadcast/gossip of a forged block; NO durable apply / tip mutation by the forge; NO live peer / BA-02 / RO-LIVE claim. The forge handoff is reached only when a `ForgeActivation` is supplied (test-only this cluster).**

---

## Conventions

- A **module** in Ade is a Cargo workspace crate (smallest independently-buildable unit). Source: `Cargo.toml` `[workspace] members` (11 entries). One exception to crate-granularity: `ade_network` is split by *submodule color* — `.idd-config.json` `core_paths` resolves BLUE at the submodule path level (9 paths), so its BLUE / GREEN / RED submodules are documented as separate entries.
- Several BLUE crates host sub-trees that carry a *different* TCB color by their own module banner and cluster TCB Color Map; and the RED `ade_runtime` and `ade_node` crates host numerous **GREEN-by-content** sub-trees. These are surfaced as sub-classification notes inside the owning crate's entry (or, where load-bearing, as their own GREEN/RED entries below).
- Modules are listed by TCB color (BLUE → GREEN → RED), alphabetical within each color.

### TCB color sources (in order of authority)

1. `.idd-config.json` `core_paths` — substring match against absolute path. BLUE crate prefixes: `ade_codec`, `ade_types`, `ade_crypto`, `ade_core`, `ade_ledger`, `ade_plutus`. Plus the 9 BLUE `ade_network` submodule paths: `mux/frame.rs`, `codec/`, `handshake/`, `chain_sync/`, `block_fetch/`, `tx_submission/`, `keep_alive/`, `peer_sharing/`, `n2c/`.
2. `.idd-config.json` `_core_paths_doc` — `ade_runtime` is RED; `ade_testkit` is GREEN; `ade_node` is RED; `ade_network::mux::transport` + `ade_network::session` are nominally RED (but `session/` is GREEN-by-content per PHASE4-N-L). `ade_core_interop` is RED.
3. **Module doc-comment banners** — every BLUE/GREEN source file opens with the `// Core Contract:` banner followed by `//! BLUE …` / `//! GREEN …` / `//! RED …`. This is the per-file authority that distinguishes GREEN-by-content from RED inside `ade_runtime` / `ade_node`. Verified directly against the tree at HEAD for every new/changed module in this regeneration (`run_loop_planner.rs` → `//! GREEN`; `node_lifecycle.rs` → `//! RED`).
4. Cluster TCB Color Maps under `docs/clusters/` and `docs/clusters/completed/` — §5 "TCB color map (FC/IS partition)" of each `cluster.md`.
5. The invariant registry `docs/ade-invariant-registry.toml` (310 rules at HEAD) — each rule's `code_locus` + `statement` is the authoritative source for the **MUST NOT** rows below.

### Active cluster at HEAD

**PHASE4-N-F-E — CLOSING (forge-tick on the relay run-loop spine).** HEAD `cd2484f` is the S3b close-tail commit. The cluster wires the recovered-state forge handoff (`node_sync::forge_one_from_recovered`) into the `--mode node` relay loop's slot path on top of the N-F-D run-loop spine. It brings a wall-clock→`SlotNo` adapter (via the existing clock seam) and `DC-NODE-05` forge-slot discipline. Four slices: S1 (`214b0d3` GREEN planner forge step), S2 (`98b488a` RED forge-tick wiring, self-accept-only), S3a (`2484dd1` forge-tick replay-equivalence), S3b (`cd2484f` single-epoch / KES fail-closed containment). An in-range commit `ffa76fc` repaired the N-F-D `node_sync` test suite (missing imports, a moved `anchor_fp`, a size-ordered feed corrected to slot-ordered) and corrected the `T-REC-03` `code_locus` in the registry — test-only / doc-only.

**Boundary language (load-bearing — do not soften):** N-F-E is a **hermetic, single-epoch, self-accept-only** forge-tick wiring cluster. There is NO operator-key ingestion in `--mode node`; NO serve/broadcast/gossip of a forged block; NO durable apply / tip mutation by the forge; NO live peer / BA-02 / RO-LIVE claim. The forge handoff is reached by the `--mode node` loop only when a `ForgeActivation` is supplied (hermetic/test-only this cluster); the binary path passes `None` and reproduces the exact N-F-D relay behavior.

**What the binary `Node` arm actually runs (precise wiring honesty):**
- `main()` `Mode::Node → run_node_lifecycle → run_node_lifecycle_inner`. The arm is **fully wired and durable for bootstrap + recovery**: on a true first run it runs `first_run_mithril_bootstrap` (fail-closes on `verify_mithril_binding` before any state is admitted, persists the seed-epoch sidecar + WAL provenance); on a non-empty store it runs `warm_start_recovery` (WAL replay from the independently-discovered anchor lineage, restoring the verified recovered `BootstrapState` through `bootstrap_initial_state(RequiredFromRecoveredProvenance)`). Both arms then converge into `run_relay_loop`, the single live-run owner (PHASE4-N-F-D S2). The relay loop drives each iteration over the closed `LoopStep` vocabulary; the tip advances ONLY through `run_node_sync → pump_block` (durable-before-advance).
- **N-F-E forge path:** `run_relay_loop` now takes `forge: Option<&mut ForgeActivation>`. With `None` (the binary path) the planner is fed `ForgeSlotStatus::NotDue`, never returns `LoopStep::ForgeTick`, and the loop is byte-identical to N-F-D relay. With `Some(activation)` (hermetic/test-only at this HEAD) the `ForgeTick` arm derives the slot via the clock seam (`Clock::next_tick → millis_to_slot → SlotNo`), reuses `CoordinatorState::kes_period_for_slot`, calls **exactly one** fenced `node_sync::forge_one_from_recovered`, advances NO durable tip, and records the outcome into the in-memory `hermetic_forge_outcomes: Vec<CoordinatorEvent>` test-observation field — it serves/admits/gossips nothing.
- The L6 BA-02 evidence correlator (`ba02_evidence`) remains a **library surface reached by no binary arm** (tested only). **BA-02 is satisfied nowhere** — no Haskell peer accepts an Ade-forged block at this HEAD.

**No BLUE crate was modified by N-F-D or N-F-E** (verified: every BLUE crate's `(pub )?struct|enum` count is unchanged — `ade_ledger` 177, `ade_core` 49, etc.; canonical-type total holds at 456). All N-F-D/N-F-E code lands in the RED binary/driver crate `ade_node` (`run_loop_planner` GREEN-by-content, `node_lifecycle` RED, `node_sync` RED), and uses `BTreeMap` (never `HashMap`), sorted keys, and no clock/rand/float in production paths (the only `SlotNo` observation is the pure `forge_slot_status` monotonic guard, and the only wall-clock read is the existing RED clock seam).

The cluster introduces / extends:
- **GREEN (planner)** `ade_node::run_loop_planner` (EXTENDED, S1) — the pure lifecycle decision function gained a 4th closed `LoopStep` variant `ForgeTick`; a content-blind `ForgeSlotStatus { Due, NotDue }` input; an extended `plan_loop_step(loop_state, sync_status, forge_slot_status, shutdown_status)` whose decision table is total; and a pure `forge_slot_status(last_forged_slot, current_slot)` monotonic guard (at most once per `SlotNo`, never a past slot — the ONLY fn here that observes a `SlotNo`). With `ForgeSlotStatus::NotDue` the table collapses to the exact N-F-D relay mapping (no `ForgeTick`). DC-NODE-05.
- **RED (binary/driver)** `ade_node::node_lifecycle` (EXTENDED, S2/S3) — `pub struct ForgeActivation<'a>` (holds the recovered-state forge inputs + clock seam + the derived per-iteration slot + `hermetic_forge_outcomes: Vec<CoordinatorEvent>` test-observation field) and a `ForgeTick` branch in `run_relay_loop` (now `forge: Option<&mut ForgeActivation>`). The branch derives the slot via the clock seam, reuses `CoordinatorState::kes_period_for_slot`, calls exactly one fenced `node_sync::forge_one_from_recovered`, advances NO durable tip, serves/admits/gossips nothing. Opt-in: `None` reproduces N-F-D relay. CN-NODE-02 / DC-SYNC-02 / DC-NODE-05.
- **RED (binary/driver)** `ade_node::node_sync` (UNCHANGED in production by N-F-E) — only `#[cfg(test)]` additions (the S2/S3a/S3b forge/replay/fail-closed tests). `forge_one_from_recovered` (recovered `BootstrapState` → `PoolDistrView::from_seed_epoch_consensus_inputs` → `ForgeRequestContext` → `run_real_forge`) and `run_node_sync` (the sole `pump_block` durable-apply driver) are byte-for-byte the N-F-D production surface. DC-CINPUT-02b / CN-CINPUT-03 (forge consume-side fence) carry forward.
- **EXTENDED CI gate** `ci_check_loop_planner_closed.sh` (S1, DC-NODE-05) — `SlotNo` is no longer in the whole-module ban; it is forbidden ONLY inside `plan_loop_step` (scoped check) so step selection stays content-blind over the closed `ForgeSlotStatus`; the pure `forge_slot_status` guard legitimately consumes a `SlotNo`. `ForgeTick`/`ForgeSlotStatus` are pinned in the closed set.
- **EXTENDED CI gate** `ci_check_node_run_loop_containment.sh` (S2, CN-NODE-02 / DC-SYNC-02) — the relay-loop body must call `run_node_sync(` (the sole tip-advance path), may reach NO tip-mutation token directly, references NO `run_real_forge` / `correlate(` / `Ba02Manifest`, may have **exactly one** `forge_one_from_recovered(` call (the fenced self-accept-only forge attempt, CE-E-4), serves/broadcasts/gossips nothing (`served_chain_admit` / `push_atomic` / `OutboundCommand` / `broadcast` / `block_fetch` all forbidden in the loop body), and uses no verdict/follower token.
- **Registry:** 310 rules at HEAD. `DC-NODE-05` flipped `declared → enforced` (its `tests` + `ci_script` arrays populated). `strengthened_in += "PHASE4-N-F-E"` on six rules: `CN-NODE-02`, `DC-SYNC-02`, `T-REC-03`, `DC-NODE-03`, `CN-PROD-02`, `DC-CINPUT-02b`. **NOTE (gap):** `docs/ade-invariant-registry.toml` is `M` in `git status` — the committed-vs-worktree `id =` count is identical (310 = 310), so the count is stable, but the working-tree diff (the six `strengthened_in` bumps + the `DC-NODE-05` flip) is staged-but-uncommitted and is owed alongside this CODEMAP at the close commit.

**Governance note.** N-F-E upholds the load-bearing structural lines. The forge stays **subordinate**: the GREEN planner learns only whether a slot is *due* (content-blind `ForgeSlotStatus`), never who is a leader — leadership eligibility is decided BLUE inside `forge_one_from_recovered`. The single durable tip-advance path remains `run_node_sync → pump_block`; the forge advances no tip. A forged block is **self-accept-only** — the loop serves/admits/broadcasts/gossips nothing (CE-E-4). The recovered surface is consumed for leadership ONLY via the closed `PoolDistrView::from_seed_epoch_consensus_inputs` projection (CN-CINPUT-03 forge consume-side fence, carried forward). Forge is **opt-in via `ForgeActivation`** — the binary path passes `None` and is byte-identical to N-F-D relay.

**Carried-forward (PHASE4-N-F-D, the run-loop spine).** The live RELAY run-loop: GREEN `ade_node::run_loop_planner` (pure `plan_loop_step` over the closed `LoopStep`), RED `ade_node::node_lifecycle::run_relay_loop` (both `--mode node` arms converge into it; `NodeLifecycleError::RelaySync`), and RED `ade_node::node_sync`'s content-blind `NodeBlockSource` readiness (`has_work_ready` / `is_ended` / `wait_ready`) with a non-blocking `next_block`. `run_node_sync` is UNMODIFIED. Relay-only + hermetic; live unbounded peer remains the RO-LIVE-01 follow-on.

**Carried-forward (PHASE4-N-F-C, the node lifecycle).** RED `ade_node::node_lifecycle` (the single `PHASE4-N-F-C-LIFECYCLE-OWNER`: closed `NodeStart` { `FirstRun` / `WarmStart` } + pure `classify_start`; closed `NodeLifecycleError`; `first_run_mithril_bootstrap` durable; `warm_start_recovery`), RED `ade_node::node_sync` (closed `NodeBlockSource`; `run_node_sync` the first production `pump_block` caller; `forge_one_from_recovered`), GREEN `ade_node::ba02_evidence` (the BA-02 evidence correlator, library/unwired), the `Mode::Node` variant, and the RED `ade_runtime::chaindb` `list_seed_epoch_consensus_anchor_fps()` discovery surface — all unchanged in shape. CN-NODE-01 / DC-SYNC-01 / RO-LIVE-06.

**Carried-forward (PHASE4-N-F-A, still load-bearing below).** The recovered seed-epoch consensus-input CAPABILITY surface — **BLUE** `ade_ledger::seed_consensus_inputs` (closed `SeedEpochConsensusInputs` + SOLE codec, `SEED_CINPUT_SCHEMA_VERSION = 1`), the `PoolDistrView::from_seed_epoch_consensus_inputs` projection (`consensus_view`, CONSUMED by the forge handoff), the additive `WalEntry::SeedEpochConsensusInputsImported` (tag 3) + `ReplayOutcome`/`RecoveredBootstrapProvenance` (`wal`), **GREEN** `ade_runtime::seed_consensus_merge`, **RED** `ade_runtime::{seed_consensus_provenance, bootstrap (A3b warm-start restore), genesis_bootstrap, mithril_bootstrap}`, and the anchor-fp-keyed `chaindb` sidecar surface (redb `SCHEMA_VERSION = 3`) — is inventoried unchanged. **Carried-forward (PHASE4-N-Z / N-Y).** The Mithril production-bootstrap composition (`mithril_bootstrap`), forward-sync (`forward_sync::{reducer, pump}` — `pump_block` consumed by `node_sync`), and the bootstrap-anchor / genesis-source BLUE types are unchanged in shape.

**Cluster-doc location.** The PHASE4-N-F-E cluster doc + slice docs (S1, S2, S3a, S3b) live under `docs/clusters/PHASE4-N-F-E/`; on close they archive under `docs/clusters/completed/`. Every prior closed cluster doc — the **PHASE4-N-F-A / N-F-C / N-F-D** sets, the entire **PHASE4-N-Q / N-R-\* / N-S-\*** set, the **N-M-\*** sub-trees, **N-O**, **N-P**, **N-T**, **N-V**, **N-W**, **N-X**, **N-Y**, **N-Z** — is archived under `docs/clusters/completed/`.

This regeneration is an **incremental, scoped refresh** of the prior full inventory (on-disk doc at the N-F-C close, HEAD `71a6c80`/`a02e1f5` era) to catch up through **PHASE4-N-F-D** (the run-loop spine, already closed at origin/main `7de1462`) AND **PHASE4-N-F-E** (the forge-tick wiring, closing now at `cd2484f`), plus the mechanical counts that drifted. Unrelated module entries (every BLUE crate, and the N-Q/N-R/N-S/N-W/N-X/N-Y/N-Z/N-F-A/N-F-C surfaces) are carried forward unchanged.

### Counts (mechanical, with sources)

| Count | Value | Source / command |
|---|---|---|
| Crates | **11** | `grep -cE '"crates/' Cargo.toml` (`[workspace] members`). **Δ vs prior (11): 0.** N-F-D/N-F-E added no crate.
| Canonical types | **456** | `grep -rhE "^(pub )?(struct\|enum) " --include='*.rs'` over the 6 BLUE crate `src/` trees + the 9 BLUE `ade_network` submodule paths, summed at `cd2484f`. Breakdown: `ade_codec` 11, `ade_types` 81, `ade_crypto` 21, `ade_core` 49, `ade_ledger` **177**, `ade_plutus` 8 (= 347) + `ade_network` BLUE submodules 109 (`mux/frame.rs` 5, `codec/` 38, `handshake/` 9, `chain_sync/` 11, `block_fetch/` 10, `tx_submission/` 5, `keep_alive/` 5, `peer_sharing/` 5, `n2c/` 21). `canonical_type_registry: null`, so the structural grep is authoritative. **Δ vs prior CODEMAP (456): 0** — N-F-D and N-F-E touched **no BLUE crate** (every BLUE crate count is byte-for-byte unchanged). All N-F-D/N-F-E types (`LoopStep` + `ForgeTick`, `ForgeSlotStatus`, `ForgeActivation`, `NodeBlockSource`) live in the RED binary/driver `ade_node` and are not canonical-counted. |
| Tests | **2169** | `grep -rEc "#\[test\]\|#\[tokio::test\]" --include='*.rs' crates/`, summed at `cd2484f`. Approximate per the template fallback rule (count of attributes, not a runner enumeration). **Δ vs prior on-disk doc (2144): +25** — the N-F-D/N-F-E surface: `run_loop_planner.rs` (11 — forge precedence table, the `forge_slot_status` monotonic guard {`none_is_due`, `at_most_once_per_slot`, `rejects_past_slot`}, `NotDue` reduces to relay, determinism), `node_lifecycle.rs` (forge-tick + replay-equivalence + single-epoch/KES fail-closed tests — `relay_loop_forge_slot_derived_via_clock_seam`, `relay_loop_forge_tick_attempts_forge_advances_no_tip`, `relay_loop_without_producer_material_matches_nfd_relay`, `relay_loop_forge_two_runs_byte_identical`, `forge_tick_rotated_kes_period_skips_no_retroactive_sign`, `forge_tick_off_epoch_slot_fails_closed_local`), `node_sync.rs` (the `ffa76fc` N-F-D test-suite repair + S2/S3 `#[cfg(test)]` forge/replay tests — production unchanged). |
| CI checks | **110** | `ls ci/ci_check_*.sh \| wc -l` at `cd2484f`. CI lives entirely under `ci/` (`ci_dirs = ["ci"]`); there is no `.github/workflows/` invariant gate at HEAD. **Δ vs prior (108): +2** — these are the two **N-F-D** gates (`ci_check_loop_planner_closed.sh`, `ci_check_node_run_loop_containment.sh`) that landed in the N-F-D span the prior CODEMAP predated. **N-F-E added NO new gate** — it *extended* both N-F-D gates in place (S1 scoped the `SlotNo` ban + added the forge tokens to `ci_check_loop_planner_closed.sh`; S2 added the one-fenced-`forge_one_from_recovered` allowance + no-serve tokens to `ci_check_node_run_loop_containment.sh`). |
| Registry rules | **310** | `grep -cE "^id = " docs/ade-invariant-registry.toml` = 310 in the working tree; `git show HEAD:…` = **310 committed** (count stable). Reference only — not a header count. The working tree has `docs/ade-invariant-registry.toml` `M` in `git status`: the *diff* is the six `strengthened_in += "PHASE4-N-F-E"` bumps (CN-NODE-02, DC-SYNC-02, T-REC-03, DC-NODE-03, CN-PROD-02, DC-CINPUT-02b) + the `DC-NODE-05` `declared → enforced` flip (its `tests` + `ci_script` populated). No `id =` was added/removed, so 310 holds; the staged delta is owed alongside this CODEMAP at the close commit. |

---

## BLUE Modules — Pure Functional Core

> **Shared header (applies to every BLUE entry below).** Every `.rs` source file begins with the `// Core Contract:` banner and the following deny attributes are present in each crate's `lib.rs` (or, for `ade_network`, at the crate root — BLUE submodules inherit them):
> `#![deny(unsafe_code)]`, `#![deny(clippy::unwrap_used)]`, `#![deny(clippy::expect_used)]`, `#![deny(clippy::panic)]`, `#![deny(clippy::float_arithmetic)]`.
>
> Cross-cutting BLUE CI gates (all scope the full BLUE set — 6 BLUE crates + 9 BLUE `ade_network` submodule paths):
> - `ci_check_module_headers.sh` — banner first-line check.
> - `ci_check_forbidden_patterns.sh` — `HashMap`/`HashSet`/`IndexMap`/`indexmap::`, `SystemTime`, `Instant`, `std::fs`, `std::net`, `tokio`, `async fn`, `f32`/`f64`, `anyhow`, `rand::thread_rng`, `thread::spawn`, `unsafe` outside an allowlist.
> - `ci_check_dependency_boundary.sh` — no BLUE crate depends on a RED crate.
> - `ci_check_no_signing_in_blue.sh` — signing operations forbidden in BLUE.
> - `ci_check_no_semantic_cfg.sh` — `#[cfg(feature = …)]` / `cfg!(feature = …)` forbidden in BLUE.
> - `ci_check_hash_uses_wire_bytes.sh` — no hashing of re-encoded bytes in BLUE.
> - `ci_check_ingress_chokepoints.sh` — only named `decode_*` chokepoints construct `PreservedCbor`.
> - `ci_check_pallas_quarantine.sh` — `pallas-*` confined to `ade_plutus`.
> - `ci_check_no_async_in_blue.sh` — async constructs forbidden in BLUE (DC-CORE-01).
>
> Narrow BLUE gates: `ci_check_no_chaindb_in_consensus_blue.sh`, `ci_check_no_float_in_consensus.sh`, `ci_check_consensus_closed_enums.sh`, `ci_check_no_density_in_fork_choice.sh` (DC-CONS-03), `ci_check_deposit_param_authority.sh` (DC-TXV-07), `ci_check_gov_cert_accumulation_closed.sh` (DC-LEDGER-09), `ci_check_credential_discriminant_closed.sh` (DC-LEDGER-10), `ci_check_proposal_procedures_closed.sh` (DC-LEDGER-11), `ci_check_conway_cert_classification_closed.sh`, the N-C producer-authority gate set, the N-G server-role gate set, the N-H receive gate set, the N-I/N-J rollback+snapshot gate set, the mempool-ingress gate set, the N-P KES gate, the N-R/N-S/N-V BLUE gates, the N-W gate `ci_check_producer_praos_vrf.sh` (CN-FORGE-04), the N-X gate `ci_check_tag24_wire_authority.sh` (CN-WIRE-08), the N-Y gates `ci_check_forward_sync_chokepoint_only.sh` (DC-SYNC-01), `ci_check_mithril_uses_bootstrap_initial_state.sh` (CN-MITHRIL-01), `ci_check_no_haskell_fingerprint_equality.sh` (DC-COMPAT-01), the workspace-wide `ci_check_registry_code_locus_exists.sh`, the N-Z `ci_check_mithril_seed_point_independence.sh` (DC-MITHRIL-02), the N-F-A `ci_check_consensus_input_provenance.sh` (CN-CINPUT-02), and the N-F-C node-lifecycle gates `ci_check_lifecycle_owner_uses_bootstrap_initial_state.sh` (CN-NODE-01), `ci_check_node_sync_via_pump.sh` (DC-SYNC-01), `ci_check_ba02_evidence_closed.sh` (RO-LIVE-06). **N-F-D added** the run-loop gates `ci_check_loop_planner_closed.sh` (the GREEN planner's closed `LoopStep` vocabulary + content-blind step selection) and `ci_check_node_run_loop_containment.sh` (the RED relay-loop body fence). **N-F-E extended both in place** (no new gate): `ci_check_loop_planner_closed.sh` scoped the `SlotNo` ban to `plan_loop_step` and pinned `ForgeTick`/`ForgeSlotStatus` in the closed set (DC-NODE-05); `ci_check_node_run_loop_containment.sh` permits exactly one fenced `forge_one_from_recovered` and adds the no-serve tokens (CN-NODE-02 / CE-E-4).

---

### `ade_codec`

| Attribute | Value |
|-----------|-------|
| **Purpose** | Owns Cardano-canonical CBOR ingress — the only place in the workspace that turns raw bytes into typed semantic values, with wire-byte preservation for every hash-bearing structure. Owns the standalone opcert byte authority and the canonical Conway-tx preserved-byte splitter, the canonical block-envelope encoder `cbor::envelope::encode_block_envelope`, and (N-X) the single workspace **tag-24 CBOR-in-CBOR wire-envelope authority** `cbor::tag24::{wrap_tag24, unwrap_tag24}` + closed `TagEnvelopeError`. Also exposes the shared canonical CBOR read/write primitives (`cbor::{read_array_header, read_map_header, read_uint, read_bytes, write_*_canonical, canonical_width, ContainerEncoding, IntWidth}`) the BLUE codecs in `ade_ledger` (incl. the `SeedEpochConsensusInputs` + WAL codecs) build on. |
| **Creates** | `PreservedCbor<T>`, `RawCbor`, `BlockEnvelope`, `ByronDecodedBlock`, `CodecContext`, `CodecError` (incl. `UnknownCertTag`, `DuplicateMapKey`, `TrailingBytes`, `InvalidCborStructure`), `ContainerEncoding`, `IntWidth`, era-tagged block/tx wrappers, `OpCertCodecError`, `TxComponents<'a>`, `TagEnvelopeError` (closed: `NotTag24`, `NotByteString`, `Truncated`, `TrailingBytes`). Functions: `conway::cert::{decode_conway_certs, decode_drep}`, `conway::withdrawals::{decode_withdrawals, withdrawals_sum}`, `conway::governance::{decode,encode}_proposal_procedures`, `shelley::opcert::{encode_opcert, decode_opcert}`, `shelley::tx_components::split_conway_tx_components`, `cbor::envelope::{decode_block_envelope, encode_block_envelope}`, `cbor::tag24::{wrap_tag24, unwrap_tag24}`. |
| **Interprets** | All canonical Cardano CBOR — block envelopes (`[era, block]`), era-specific blocks, tx bodies/outs, certificates, addresses. `unwrap_tag24` is the single workspace authority that strips a tag-24 (`0xd8 0x18`) envelope, returning a zero-copy borrow of the verbatim inner bytes (no re-encode) and failing closed with a typed `TagEnvelopeError`. `cbor::mod::{read_bytes, read_text, skip_item}` fail-close (no panic) on an overflowing declared length via `checked_add`. |
| **MUST NOT** | (1) Construct `PreservedCbor` outside `ade_codec` (`pub(crate)` + `ci_check_ingress_chokepoints.sh`). (2) Re-encode wire bytes when computing hashes. (3) Use any forbidden BLUE pattern. (4) Depend on any workspace crate except `ade_types`. (5) `conway::cert` (DC-LEDGER-08) — no unknown-tag swallow. (6) `conway::withdrawals` — no last-wins on duplicate `RewardAccount`. (7) `decode_stake_credential` (DC-LEDGER-10) — must not erase the credential tag. (8) `conway::governance` (DC-LEDGER-11) — no silent skip on unknown `GovAction`. (9) `shelley::opcert` (DC-CONS-11/12). (10) `shelley::tx_components` (DC-CONS-13/16). (11) `encode_block_envelope` (CN-FORGE-03) — the workspace's single block-envelope encoder; no parallel serializer. (12) `cbor::tag24` (CN-WIRE-08) — `wrap_tag24`/`unwrap_tag24` defined exactly once; `unwrap_tag24` fails closed (typed `TagEnvelopeError`, never panic); inner bytes copied verbatim / zero-copy borrowed; `read_bytes`/`read_text`/`skip_item` reject an overflowing declared length without panic (`ci_check_tag24_wire_authority.sh`). |
| **Inbound deps** | `ade_ledger` (heavy), `ade_plutus`, `ade_testkit`, `ade_network`, `ade_runtime`, `ade_core_interop`, `ade_node`. |
| **Outbound deps** | `ade_types`. std-only; dev-deps `serde_json`, `toml`. |
| **Entry points** | `ade_codec::cbor::envelope::{decode_block_envelope, encode_block_envelope}`, `ade_codec::{wrap_tag24, unwrap_tag24, TagEnvelopeError}` (crate-root re-exports), `ade_codec::cbor::{read_array_header, read_map_header, read_uint, read_bytes, write_uint_canonical, write_bytes_canonical, write_array_header, write_map_header, canonical_width, ContainerEncoding, IntWidth}`, `ade_codec::traits::AdeEncode`, `ade_codec::CodecContext`, per-era `decode_*_block`, `ade_codec::address::decode_address`, `ade_codec::conway::tx::decode_conway_tx_body`, `ade_codec::conway::cert::decode_conway_certs`, `ade_codec::conway::governance::{decode,encode}_proposal_procedures`, `ade_codec::shelley::opcert::{encode_opcert, decode_opcert}`, `ade_codec::shelley::tx_components::split_conway_tx_components`. |
| **Key modules** | `cbor/` (incl. `envelope.rs`, `tag24.rs`, `mod.rs`), `byron/`, `shelley/` (incl. `cert.rs`, `opcert.rs`, `tx_components.rs`), `allegra/`, `mary/`, `alonzo/`, `babbage/`, `conway/`, `address/`, `preserved.rs`, `traits.rs`, `primitives.rs`, `error.rs`. |

---

### `ade_core`

| Attribute | Value |
|-----------|-------|
| **Purpose** | BLUE authoritative Praos consensus core. Owns canonical types and pure state-transitions deciding which header/chain Ade accepts: HFC era schedule + slot↔era↔time, Praos chain-dep state, VRF cert verification + leader-eligibility, KES + op-cert period verification, header validation, fork-choice, header-level rollback, leader-schedule query, canonical encodings, producer-side opcert acceptance, the closed BLUE leader-check evaluator (`consensus::leader_check`, N-R-A), and the single era→leader-VRF-input authority `consensus::vrf_cert::{ExpectedVrfInput, leader_vrf_input, leader_value_for}` (N-W). The `consensus::vrf_cert::ActiveSlotsCoeff` is the canonical ASC type the `SeedEpochConsensusInputs` record carries verbatim. |
| **Creates** | **Schedule:** `BootstrapAnchorHash`, `EraSchedule`, `EraSummary`, `EraLocation`. **State:** `PraosChainDepState`, `OpCertCounterMap`, `Nonce`. **Events/points:** `Point`, `ChainHash`, `BlockDistance`, `SecurityParam`, `ChainEvent`, `ChainSelectionReject`. **Errors:** `HFCError`, `SlotTimeError`, `HeaderValidationError`, `VrfCertError`, `OpCertCounterError`, `NonceEvolutionError`, `LeaderScheduleError`, `OpCertError`. **Header surface:** `HeaderInput`, `HeaderVrf`, `HeaderKes`, `ValidatedHeaderSummary`, `HeaderApplied`. **Fork-choice:** `TiebreakerView`, `CandidateFragment`, `ChainSelectorState`, `ForkChoiceError`. **VRF/leader:** `VrfRole`, `VerifiedVrf`, `StakeFraction`, `ActiveSlotsCoeff`, `LeaderScheduleQuery`, `LeaderScheduleAnswer`, `ExpectedVrfInput` (closed 2-variant era-discriminated leader-VRF-input enum). **Boundary:** `LedgerView` trait. **Rollback:** `RollBackRequest`, `RollBackApplied`. **`leader_check`:** `LeaderCheckVerdict`, `LeaderCheckError`, `VrfOutputFingerprint`, `LeaderProofFingerprint`. 49 public types. |
| **Interprets** | Canonical inputs from the `ade_runtime` shell. KES check verifies the hot-KES signature via `ade_crypto::kes::verify_kes`. `leader_vrf_input(era, slot, eta0)` constructs the era-correct alpha; `leader_value_for` applies the era-correct range-extension. `leader_check::verify_and_evaluate_leader` consumes only public-key material + canonical inputs and fail-closes on full-enum `ExpectedVrfInput` mismatch. `opcert_validate` consumes an `OperationalCert`, cold key, expected period, prev counter. |
| **MUST NOT** | (1)–(14) consensus carry-forward (no I/O, no clock, no `ChainDb` in BLUE consensus, no density in fork-choice, closed consensus enums, etc.). (15) `consensus::leader_check` (CN-FORGE-02) — MUST NOT depend on `LedgerView`/`EraSchedule`/`ChainDepState`/wall-clock/storage/any RED crate; MUST NOT observe `KesSecret`/`VrfSigningKey`/`ColdSigningKey`; `NotEligible` carries only a bounded fingerprint. (16) `consensus::vrf_cert` leader-VRF authority (CN-FORGE-04) — `leader_vrf_input` is the single era→construction authority; no path accepts both TPraos and Praos inputs for one era; Praos producer alpha = validator alpha (`ci_check_producer_praos_vrf.sh`). |
| **Inbound deps** | `ade_ledger`, `ade_runtime` (heavy), `ade_testkit`, `ade_core_interop`, `ade_node`. |
| **Outbound deps** | `ade_types`, `ade_crypto`, `minicbor`. Dev-deps: `ade_testkit`, `serde_json`, `cardano-crypto`. |
| **Entry points** | `use ade_core::consensus::{…}` aggregator; `validate_and_apply_header`, `select_best_chain`, `apply_rollback`, `apply_nonce_input`, `apply_op_cert`, `query_leader_schedule`, `verify_vrf_cert`, `tiebreaker_prefer`; `ade_core::consensus::opcert_validate::opcert_validate`; `ade_core::consensus::leader_check::{verify_and_evaluate_leader, LeaderCheckVerdict, is_leader_for_vrf_output}`; `ade_core::consensus::vrf_cert::{ExpectedVrfInput, ActiveSlotsCoeff, leader_vrf_input, leader_value_for, praos_vrf_input, praos_leader_value}`. |
| **Key modules** | `consensus/{era_schedule, praos_state, events, errors, vrf_cert, kes_check, nonce, op_cert, leader_schedule, header_summary, header_validate, candidate, fork_choice, rollback, ledger_view, encoding, opcert_validate, leader_check}.rs`. |

---

### `ade_crypto`

| Attribute | Value |
|-----------|-------|
| **Purpose** | Pure cryptographic verification primitives — Blake2b hashing, Ed25519 / Byron-bootstrap signature verification, KES verification with operational certificates, VRF verification, plus closed signature-artifact types. Verification only — signing lives in RED `ade_runtime::producer::signing`. Also owns the Ade-native `Sum6KES Ed25519DSIGN` algorithm (`kes_sum/`), byte-identical to Haskell `cardano-base`. `blake2b_256` is the function the sidecar-provenance helper hashes the canonical sidecar bytes with. |
| **Creates** | `Blake2b224`, `Blake2b256`, `HashAlgorithm`, `Ed25519VerificationKey`, `Ed25519Signature`, `ByronExtendedVerificationKey`, `KesVerificationKey`, `KesPeriod`, `OperationalCertData`, `VrfVerificationKey`, `VrfProof`, `VrfOutput`, `CryptoError`, `KesSignature`, `SUM6_KES_SIG_LEN = 448`. **`kes_sum::`** (BLUE): `KesAlgorithm` trait, `Sum0Kes`, `Sum0SigningKey`, `Sum0Signature`, `SumKes<D>`, `SumSigningKey<D>`, `SumSignature<D>`, aliases `Sum1Kes..Sum6Kes`, closed `KesError`, closed `KesParseError`. |
| **Interprets** | Already-decoded byte slices. `Sum6Kes::raw_deserialize_signing_key_kes` interprets the canonical 608-byte expanded cardano-cli skey; `raw_deserialize_signature_kes` the 448-byte signature; `period_from_zeroed_sum6_tree_shape` infers the current period from tree shape. |
| **MUST NOT** | (1) Implement signing as a top-level `pub fn sign_*` (`ci_check_no_signing_in_blue.sh`). (2) Allocate global state. (3) Use any BLUE-forbidden pattern. (4) `unsafe` outside the allowlisted FFI in `vrf.rs`. (5) `build_opcert_signable` must produce the spec-correct concat. (6) `KesSignature` (DC-CRYPTO-04) — closed length-pinned wrapper, `from_bytes` only, redacting `Debug`. (7) `kes_sum` (DC-CRYPTO-08) — MUST NOT import `cardano_crypto::kes::*` in production paths (`#[cfg(test)]` oracle only; `ci_check_kes_sum_compatibility.sh` Guard 3). (8) `kes_sum::hash::expand_seed` — Haskell prefix bytes `0x01`/`0x02`. (9) every `*SigningKey` hand-rolls zeroizing `Drop` + redacting `Debug` + no public byte accessors. (10) `kes_sum` corpus — throwaway-fixture comment; no `.skey` files (Guards 1, 2). (11) error variants carry only non-secret primitives. |
| **Inbound deps** | `ade_core`, `ade_ledger`, `ade_plutus`, `ade_testkit`, `ade_core_interop`, `ade_runtime`. |
| **Outbound deps** | `ade_types`, `blake2`, `ed25519-dalek`, `cardano-crypto` (`["vrf-draft03", "kes-sum", "dsign"]`, `default-features = false`; `kes-sum` retained for `#[cfg(test)]` oracle only). |
| **Entry points** | `ade_crypto::blake2b::*`, `ade_crypto::{blake2b_256, blake2b_224}`, `verify_ed25519`, `verify_byron_bootstrap`, `verify_kes`, `verify_opcert`, `verify_vrf`, `ade_crypto::kes::{KesSignature, SUM6_KES_SIG_LEN, KesVerificationKey}`, `ade_crypto::kes_sum::{KesAlgorithm, Sum6Kes, KesError, KesParseError, period_from_zeroed_sum6_tree_shape}`. |
| **Key modules** | `blake2b.rs`, `ed25519.rs`, `error.rs`, `kes.rs`, `kes_sum/` (`mod`, `single`, `sum`, `hash`, `errors`, `period`, `cardano_cli_corpus` `#[cfg(test)]`, `tests` `#[cfg(test)]`), `traits.rs`, `vrf.rs`. |

---

### `ade_ledger`

| Attribute | Value |
|-----------|-------|
| **Purpose** | The functional core (ledger half): stateless ledger rules for every era; B1 block-validity verdict; B2 transaction-validity verdict + mempool admission; B3 Conway value-conservation; B4/B5 closed Conway cert-state + governance-cert accumulation; live committee enactment; the single BLUE wire-ingress chokepoint `mempool::ingress::mempool_ingress`; the BLUE producer authority (`producer::{forge, self_accept, served_chain, state}`); the single canonical body-hash authority (`block_body_hash`); the BLUE receive-side bridge (`receive::*`); the BLUE rollback authority (`rollback::*`); the BLUE canonical snapshot encoder/decoder (`snapshot::*`); the BLUE Ade-native WAL replay+event authority (`wal::*`); the BLUE bootstrap-anchor + Mithril-binding types (`bootstrap_anchor::*`); the BLUE Conway-genesis→initial-state transform (`genesis_source`, N-Y); the canonical KES-signing pre-image recipe `block_validity::unsigned_header_pre_image`; and (N-F-A) the BLUE closed `SeedEpochConsensusInputs` recovered-state record + its SOLE codec (`seed_consensus_inputs`), the recovered-surface `PoolDistrView::from_seed_epoch_consensus_inputs` projection (`consensus_view`), and the additive WAL provenance variant + `ReplayOutcome`/`RecoveredBootstrapProvenance` (`wal`). |
| **Creates** | ~177 public types. Load-bearing: `BlockValidityError`, `BlockVerdict`, `TxValidity*`, `MempoolState`, `ProducerTick`, `ForgedBlock`, `ForgeError`, `ForgeEffects`, `ServedChainSnapshot`, `ServedChainAdmitError`, `PoolDistrView`, `PoolEntry`, `LedgerState`, `WalEntry` (closed sum: `AdmitBlock` + `SeedEpochConsensusInputsImported` at tag 3), `BlockVerdictTag`, `RecoveredBootstrapProvenance`, `ReplayOutcome`, `WalError` (incl. `DuplicateProvenance`, `ProvenanceAnchorMismatch`), `SeedEpochConsensusInputs` (closed; `anchor_fp`/`epoch_no`/`active_slots_coeff`/`total_active_stake`/`pool_distribution: BTreeMap<Hash28, PoolEntry>`; `SEED_CINPUT_SCHEMA_VERSION = 1`), `SeedConsensusInputsError` (closed: `MalformedCbor`/`UnknownVersion`/`Structural`/`NonCanonicalMapOrder`/`DuplicatePoolKey`/`TrailingBytes`), `BootstrapAnchor`, `SeedProvenance` (closed: `CardanoCliJson` / `Mithril { … }`), `SeedPoint`, `ANCHOR_SCHEMA_VERSION = 2`, `MithrilManifestReport`, `MithrilImportError`, `BootstrapAnchorError`, `ConwayGenesisConfig`, `GenesisSourceError` (closed: `NonConwayEra`), `UnsignedHeaderPreImage`, `UnsignedHeaderPreImageError`. |
| **Interprets** | Canonical decoded blocks/txs/certs/snapshots; `mempool_ingress` is the sole BLUE chokepoint from wire ingress to `admit`; `self_accept` re-validates a forged artifact; `served_chain_admit` derives the served index; `decode_bootstrap_anchor` reads the version-gated `ANCHOR_SCHEMA_VERSION = 2` CBOR framing; `verify_mithril_binding(report, anchor)` cross-checks the Mithril manifest against the anchor; `genesis_initial_state(&ConwayGenesisConfig)` produces the cold-start pair; `unsigned_header_pre_image` produces the canonical CBOR of `ShelleyHeaderBody`. `decode_seed_epoch_consensus_inputs` is the SOLE decoder of the closed sidecar record (version-gated, byte-canonical, rejects non-canonical/duplicate pool-map keys + trailing bytes); `replay_from_anchor` folds the WAL into a `ReplayOutcome`, reconstructing the typed `RecoveredBootstrapProvenance` (at most one; fail-closed on duplicate / anchor mismatch) without disturbing the `AdmitBlock` fingerprint chain. |
| **MUST NOT** | All carry-forward ledger prohibitions (no I/O, no clock, closed cert/governance enums, single body-hash recipe, etc.). **`producer::forge` (CN-FORGE-01/03):** emit exactly one of `ForgeSucceeded`/`ForgeNotLeader`/`ForgeFailed`; no `ForgeSucceeded` unless `self_accept` accepts; wrap output via `ade_codec::encode_block_envelope`; no parallel forge codepath. **`producer::served_chain` (CN-PROD-04):** only self-accepted blocks admitted; sole entry `served_chain_admit`. **`block_validity::unsigned_header_pre_image` (CN-KES-HEADER-01 / DC-KES-HEADER-01):** branded `UnsignedHeaderPreImage`'s only constructor is the canonical recipe; byte-identical to the validator-side extractor; pure. **`bootstrap_anchor` (CN-ANCHOR-01 / DC-ANCHOR-01):** `ANCHOR_SCHEMA_VERSION` version-gated; the `SeedProvenance` enum is closed. **`bootstrap_anchor::binding` (CN-MITHRIL-01 / DC-MITHRIL-01):** `verify_mithril_binding` is a pure fail-closed predicate, never re-verifies the STM multisig. **`genesis_source` (DC-GENESIS-SRC-01):** `genesis_initial_state` is Conway-only (fail-closed `NonConwayEra`). **`seed_consensus_inputs` (CN-CINPUT-01):** `SeedEpochConsensusInputs` is a single closed canonical type with a SOLE encoder/decoder pair; deterministic CBOR, `BTreeMap`-ordered, version-gated, byte-canonical; no `Default`, no `#[non_exhaustive]`; no I/O / clock / float. **`wal` provenance (DC-CINPUT-01):** the `SeedEpochConsensusInputsImported` variant stays distinct from the `AdmitBlock` chain; `replay_from_anchor` allows at most one provenance entry and fails closed on duplicate / anchor mismatch; remains pure. **`consensus_view::from_seed_epoch_consensus_inputs` (DC-CINPUT-02a):** a pure BLUE field-map preserving single-epoch semantics (off-epoch queries return `None`); no I/O / clock / `HashMap` — it is the *projection* the forge handoff (`node_sync::forge_one_from_recovered`) consumes. |
| **Inbound deps** | `ade_testkit`, `ade_core_interop`, `ade_runtime`, `ade_node`. |
| **Outbound deps** | `ade_types`, `ade_crypto`, `ade_codec`, `ade_plutus`, `ade_core`, `minicbor`, `num-bigint`, `num-integer`, `num-traits`. Dev-dep: `ade_testkit`. (`seed_consensus_inputs` imports the `ade_codec::cbor` canonical primitives + `ade_core::consensus::vrf_cert::ActiveSlotsCoeff`.) |
| **Entry points** | `ade_ledger::block_validity::{decode_block, validate_block, unsigned_header_pre_image::unsigned_header_pre_image}`, `ade_ledger::tx_validity::*`, `ade_ledger::mempool::{ingress::mempool_ingress, admit::*}`, `ade_ledger::producer::{forge::forge_block, self_accept::self_accept, served_chain::{ServedChainSnapshot, served_chain_admit}}`, `ade_ledger::block_body_hash::*`, `ade_ledger::receive::reducer::*`, `ade_ledger::rollback::*`, `ade_ledger::snapshot::*`, `ade_ledger::wal::{replay_from_anchor, ReplayOutcome, RecoveredBootstrapProvenance, WalEntry, WalError, encode_wal_entry, decode_wal_entry, WalStore}`, `ade_ledger::seed_consensus_inputs::{SeedEpochConsensusInputs, encode_seed_epoch_consensus_inputs, decode_seed_epoch_consensus_inputs, SeedConsensusInputsError, SEED_CINPUT_SCHEMA_VERSION}`, `ade_ledger::consensus_view::{PoolDistrView, PoolEntry}` (incl. `PoolDistrView::{new, from_seed_epoch_consensus_inputs}`), `ade_ledger::bootstrap_anchor::{encode_bootstrap_anchor, decode_bootstrap_anchor, BootstrapAnchor, SeedProvenance, verify_mithril_binding, MithrilManifestReport, MithrilImportError}`, `ade_ledger::genesis_source::{genesis_initial_state, ConwayGenesisConfig, GenesisSourceError}`. |
| **Key modules** | `block_validity/` (incl. `unsigned_header_pre_image.rs`, `header_input.rs`, `transition.rs`, `verdict.rs`), `tx_validity/`, `mempool/`, `cert_classify/`, `consensus_view.rs` (incl. `PoolDistrView::from_seed_epoch_consensus_inputs` — consumed by the N-F-C/N-F-E lifecycle forge handoff `node_sync::forge_one_from_recovered`), `producer/` (`forge`, `self_accept`, `served_chain`, `state`), `block_body_hash.rs`, `receive/`, `rollback/`, `snapshot/`, `wal/` (`mod`, `error`, `event`, `replay`, `store_trait`), `seed_consensus_inputs.rs`, `bootstrap_anchor/` (`mod`, `anchor`, `binding`, `error`), `genesis_source.rs`. |

> **GREEN sub-classification:** `ade_ledger::mempool::{policy, canonicalize}` are GREEN-by-content. **RED sub-classification:** `ade_ledger::consensus_input_extract` is RED-by-content. Both carry their own module banner.

---

### `ade_network` *(BLUE submodules)*

| Attribute | Value |
|-----------|-------|
| **Purpose** | Owns the Cardano Ouroboros mini-protocol authority — the closed wire grammar (CBOR codecs) and pure state machines for all N2N + N2C mini-protocols, plus the BLUE mux frame primitive and the producer-side server-role reducers. Owns the per-protocol tag-24 **composition** authorities layered over `ade_codec::{wrap_tag24, unwrap_tag24}` — `codec::block_fetch::{compose,decompose}_blockfetch_block` (era **inside** the wrap; Conway = storage index 7) and `codec::chain_sync::{compose,decompose}_rollforward_header` + `chain_sync_wire_era_index` (era_tag **outside** the wrap; CONSENSUS era index Conway = 6). |
| **Creates** | 109 BLUE canonical types: `mux/frame.rs` 5, `codec/` 38, `handshake/` 9, `chain_sync/` 11, `block_fetch/` 10, `tx_submission/` 5, `keep_alive/` 5, `peer_sharing/` 5, `n2c/` 21. `chain_sync::server::HeaderProjection` carries an `era: CardanoEra` field. |
| **Interprets** | Mini-protocol wire frames; `mux::frame::decode_frame` is the single frame decode authority; per-protocol `*_transition` reducers consume decoded messages. `decompose_blockfetch_block` / `decompose_rollforward_header` strip the per-protocol tag-24 composition (delegating the inner unwrap to `ade_codec::unwrap_tag24`). |
| **MUST NOT** | (1) `mux::frame` — single `encode_frame`/`decode_frame` pair. (2) handshake — single `n2n_transition` + single `n2c_transition`. (3) closed `AcceptedMiniProtocol` registry. (4) no socket I/O in BLUE submodules. (5) no async/tokio in BLUE submodules. (6) depend on no workspace crate beyond `ade_codec` + `ade_types`. (7) server reducers MUST NOT split headers in parallel or depend on signing. (8) tag-24 composition (CN-WIRE-08) — composers delegate byte-level wrap/strip to the single `ade_codec` authority; a served BlockFetch `MsgBlock` payload is `tag24(bytes([era, block]))` and a served ChainSync `RollForward` header is `[era_tag, tag24(bytes(header_cbor))]`; both pinned byte-identically against captured cardano-node 11.0.1 fixtures (`ci_check_tag24_wire_authority.sh`). |
| **Inbound deps** | `ade_runtime`, `ade_node`, `ade_core_interop`, `ade_testkit` (and the `ade_network` GREEN/RED submodules). |
| **Outbound deps** | `ade_types`, `ade_codec`. No external deps in BLUE submodules. |
| **Entry points** | `ade_network::mux::frame::{encode_frame, decode_frame}`, `ade_network::handshake::{n2n_transition, n2c_transition}`, `ade_network::chain_sync::{server::*, client::*}`, `ade_network::block_fetch::{server::*, client::*}`, `ade_network::codec::block_fetch::{compose,decompose}_blockfetch_block`, `ade_network::codec::chain_sync::{compose,decompose}_rollforward_header, chain_sync_wire_era_index}`, `ade_network::{tx_submission, keep_alive, peer_sharing, n2c}::*`. |
| **Key modules** | `mux/frame.rs`, `codec/` (incl. `block_fetch.rs`, `chain_sync.rs`), `handshake/`, `chain_sync/` (incl. `server.rs`), `block_fetch/` (incl. `server.rs`), `tx_submission/`, `keep_alive/`, `peer_sharing/`, `n2c/`. |

---

### `ade_plutus`

| Attribute | Value |
|-----------|-------|
| **Purpose** | Quarantine boundary between the Ade-canonical ledger and the ported UPLC evaluator from `aiken-lang/aiken` (pinned tag `v1.1.21`). |
| **Creates** | `PlutusScript`, `PlutusLanguage`, `EvalOutput`, `PlutusError`, `CostModels`, `DecoderMode`, `PerScriptResult`, `TxEvalResult`. |
| **Interprets** | UPLC scripts (Plutus V1/V2/V3) and `CostModels` CBOR; phase-two tx evaluation. `PlutusScript::from_cbor` is a named ingress chokepoint. |
| **MUST NOT** | (1) Re-export any `pallas_*` or `aiken_uplc::` type. (2) Let another BLUE crate bypass the canonical entry. (3) Activate PV11 builtins. (4) Use any BLUE-forbidden pattern. (5) Construct `PreservedCbor` outside `ade_codec`. |
| **Inbound deps** | `ade_ledger`, `ade_testkit`. |
| **Outbound deps** | `ade_types`, `ade_crypto`, `ade_codec`, `aiken_uplc` (git, tag `v1.1.21`), `pallas-primitives` (internal-only). |
| **Entry points** | `ade_plutus::eval_tx_phase_two`, `ade_plutus::tx_eval::*`, `ade_plutus::evaluator::*`, `ade_plutus::cost_model::*`. |
| **Key modules** | `evaluator.rs`, `cost_model.rs`, `script_context.rs`, `script_verdict.rs`, `tx_eval.rs`. |

---

### `ade_types`

| Attribute | Value |
|-----------|-------|
| **Purpose** | Owns the canonical Cardano domain vocabulary — primitives, era enum, address forms, era-tagged tx bodies/outputs/certificates, governance types — the lingua franca for every other crate. `CardanoEra` carries the `is_praos()` classifier (Babbage + Conway). `Hash28`/`Hash32`/`EpochNo`/`SlotNo` are the primitives the recovered-state record and the forge-slot guard are keyed/typed on. |
| **Creates** | `CardanoEra` (with `is_praos()`), `SlotNo`, `BlockNo`, `EpochNo`, `Hash28`, `Hash32`, `Coin`, `Lovelace`, `NetworkId`, `Nonce`, `TxIn`, `RewardAccount`, `PoolId`, `Address`, `ByronAddress`, `Credential`, `StakeCredential`, `Certificate`, `PoolRegistrationCert`, `ConwayCert`, `MIRCert`, `DRep`, `GovAction`, `GovActionState`, `GovActionId`, `Anchor`, `ProposalProcedure`, `OperationalCert`, `NativeScript`, `Datum`, `MultiAsset`, `AssetName`, `CostModel`, `ExUnits`, plus per-era tx-body/tx-out/witness wrappers and `ShelleyBlock`/`ShelleyHeader`/`VrfData`/`ProtocolVersion`. |
| **Interprets** | None — produce-only. |
| **MUST NOT** | (1) Construct or decode `PreservedCbor`. (2) Use any BLUE-forbidden pattern. (3) Depend on any workspace crate. (4) Add open/extensible variants to closed enums without a versioned gate (`CardanoEra` discriminants are closed: Byron=0 … Conway=7; `is_praos()` classifies exactly {Babbage, Conway}). |
| **Inbound deps** | Every other workspace crate. |
| **Outbound deps** | None. |
| **Entry points** | `ade_types::CardanoEra` (incl. `is_praos`, `is_byron`, `as_u8`), `ade_types::tx::{Coin, TxIn, RewardAccount}`, `ade_types::{Hash32, SlotNo, Hash28, BlockNo, EpochNo}`, `ade_types::conway::{tx::ConwayTxBody, cert::*, governance::{ProposalProcedure, GovAction, GovActionId}}`, `ade_types::shelley::block::{OperationalCert, ProtocolVersion, ShelleyHeader, ShelleyBlock, VrfData}`. |
| **Key modules** | `primitives.rs`, `era.rs`, `tx.rs`, `address/`, `byron/`, `shelley/`, `allegra/`, `mary/`, `alonzo/`, `babbage/`, `conway/`. |

---

## GREEN Modules — Deterministic Glue

> Deterministic, non-authoritative. May depend on BLUE; must not affect authoritative outputs. GREEN sub-trees inside the RED `ade_runtime` and `ade_node` crates carry a `//! GREEN …` banner + the same deny attributes and are CI-gated for purity.

### `ade_node::run_loop_planner` *(GREEN by content — PHASE4-N-F-D S1; forge step PHASE4-N-F-E S1)*

| Attribute | Value |
|-----------|-------|
| **Purpose** | The pure lifecycle decision function for the `--mode node` relay run loop (`//! GREEN`). `plan_loop_step` selects each iteration's `LoopStep` from four closed, orthogonal, lifecycle-level inputs — operator intent (`ShutdownStatus`), momentary source readiness (`SyncStatus`), forge-slot scheduling (`ForgeSlotStatus`), and structural feed liveness (`LoopState`). It owns **no authority**: it cannot decide ledger validity, chain selection, leadership, forge eligibility, or evidence — its closed output vocabulary makes an authority decision unrepresentable as a planner result. **N-F-E (forge stays subordinate):** the planner learns only whether a forge slot is *due* via the content-blind `ForgeSlotStatus` — never who is a leader (leadership is decided BLUE inside `forge_one_from_recovered`). The forge-slot *monotonicity* (at most once per `SlotNo`, never a past slot) is the pure `forge_slot_status` guard — the ONLY function here that observes a `SlotNo`. |
| **Creates** | `LoopState`, `SyncStatus`, `ShutdownStatus`, closed `ForgeSlotStatus { Due, NotDue }` (N-F-E), closed `LoopStep { SyncOnce, Idle, HaltCleanly, ForgeTick }` (the 4th variant `ForgeTick` added N-F-E). Pure functions: `plan_loop_step(loop_state, sync_status, forge_slot_status, shutdown_status) -> LoopStep` (total decision table), `forge_slot_status(last_forged_slot: Option<SlotNo>, current_slot: SlotNo) -> ForgeSlotStatus` (monotonic guard). Not canonical-counted (GREEN; RED `ade_node`). |
| **MUST NOT** | (1) `plan_loop_step` MUST stay content-blind — it may NOT reference `SlotNo` (only the `forge_slot_status` guard may); step selection ranges only over the closed `ForgeSlotStatus` (DC-NODE-05; `ci_check_loop_planner_closed.sh` scoped check). (2) Return a `LoopStep` that is not one of the four closed variants; the closed `LoopStep` / `ForgeSlotStatus` set is pinned in CI. (3) Perform I/O / hold a clock / allocate / `await` — pure (the only wall-clock read lives in the RED caller's clock seam). (4) Decide any authority (validity / chain selection / leadership / forge eligibility / evidence). (5) Return `ForgeTick` under `ForgeSlotStatus::NotDue` — with `NotDue` the table collapses to the exact N-F-D relay mapping. |
| **Inbound deps** | `ade_node::node_lifecycle` (`run_relay_loop` is the sole caller). |
| **Outbound deps** | `ade_types` (`SlotNo`, in the `forge_slot_status` guard only). |
| **Entry points** | `ade_node::run_loop_planner::{plan_loop_step, forge_slot_status, LoopStep, ForgeSlotStatus, LoopState, SyncStatus, ShutdownStatus}`. |

> **Promotable / demotable:** firmly **promotable to BLUE on demand** (pure, deterministic, content-blind, closed-vocabulary) — it sits in `ade_node` only to live beside its RED caller; it is never demotable to RED.

---

### `ade_testkit`

| Attribute | Value |
|-----------|-------|
| **Purpose** | Test infrastructure: differential harness, oracle snapshot loader, regression corpus, genesis loader, era mapping, transcript replay, diff reporting; consensus / block-validity / tx-validity / mempool-ingress / proposal-procedures / producer test harnesses; the N-Y observable-surface snapshot→tip differential harness (`harness::sync_diff`). Supplies the `ConwayValidityCorpus` the bootstrap/warm-start/forge tests build a warm-startable chaindb + `PoolDistrView` from. |
| **Creates** | Harness + corpus types (`ConwayValidityCorpus`, differential replay drivers, snapshot loaders). **N-Y `harness::sync_diff`:** `BlockVerdict` (closed observable-surface verdict string), `ObservableBlockSurface`, `UtxoEntry`, `SyncOracleFixture`, `AdeObservedSurfaces`, `SyncRegressionFixture`, `RegressionFixtureViolation`. Not canonical-counted (GREEN). |
| **MUST NOT** | (1) Affect authoritative outputs. (2) Introduce nondeterminism into the BLUE-under-test path. (3) Construct semantic types bypassing the canonical decoders. **`harness::sync_diff` (DC-COMPAT-01):** MUST NOT compare Ade's internal ledger `fingerprint` to a Haskell / serialized-state hash — compatibility is proven only on observable surfaces (`ci_check_no_haskell_fingerprint_equality.sh`). |
| **Inbound deps** | None at compile time; consumed via integration tests + dev-dep links. |
| **Outbound deps** | `ade_types`, `ade_codec`, `ade_crypto`, `ade_core`, `ade_ledger`, `ade_plutus`, `ade_runtime`, `blake2`, `flate2`, `tar`, `serde`, `serde_json`, `toml`, `cardano-crypto` (dev). |
| **Entry points** | `ade_testkit::validity::corpus::ConwayValidityCorpus`, the differential `*_replay` drivers, the snapshot loaders, the genesis loader, `ade_testkit::harness::sync_diff::{sync_differential_snapshot_to_tip, diff_observable_surfaces, parse_sync_oracle_fixture, load_committed_regression_fixtures, validate_regression_fixture}`. |
| **Key modules** | `consensus/`, `validity/`, `tx_validity/`, `mempool/`, `governance/`, `producer/`, `harness/` (incl. `sync_diff.rs`, `diff_report.rs`), differential + corpus infrastructure. |

---

### `ade_network::session` *(GREEN by content — PHASE4-N-L)*

| Attribute | Value |
|-----------|-------|
| **Purpose** | The pure session driver. Composes the BLUE authorities `mux::frame::{encode,decode}_frame`, `handshake::n2n_transition`, and per-mini-protocol state machines through `session::core::step`; owns the partial-frame buffer + `SessionState` type-state + closed `AcceptedMiniProtocol` registry + per-mini-protocol payload reassembly. |
| **Creates** | ~12 closed types across `session::{event, state, demux, core, handshake_driver}`. |
| **MUST NOT** | (1) `session::core::step` is the only pub reducer in `session/`. (2) no `tokio::*` imports in session core. (3) `AcceptedMiniProtocol` closed. (4) no unbounded queue (`ci_check_session_no_unbounded.sh`). (5) affect authoritative outputs. |
| **Inbound deps** | `ade_runtime::network::{mux_pump, n2n_dialer}`, `ade_runtime::orchestrator::keep_alive_session`. |
| **Outbound deps** | `ade_network` BLUE submodules, `ade_codec`, `ade_types`. |
| **Entry points** | `ade_network::session::core::step`, `ade_network::session::{state::SessionState, event::*, demux::*, handshake_driver::*}`. |
| **Key modules** | `session/{mod, core, event, state, demux, handshake_driver}.rs`. |

---

### `ade_runtime::seed_consensus_merge` *(GREEN by content — PHASE4-N-F-A A2)*

| Attribute | Value |
|-----------|-------|
| **Purpose** | The deterministic, no-I/O merge transform that lifts a verified-bootstrap `LiveConsensusInputsCanonical` (bootstrap-time two-map shape: `pool_distribution` carries only `active_stake`, with VRF keyhashes in the separate `pool_vrf_keyhashes` map) plus the minted anchor fingerprint + seed epoch into the BLUE single-map `SeedEpochConsensusInputs`. `total_active_stake` is the saturating sum of every pool's `active_stake`, derived exactly as the forge-time projection does — so the recovered-surface projection matches the prior forge-time output for the seed epoch. |
| **Creates** | `SeedConsensusMergeError` (closed: `PoolMissingVrfKeyhash { pool }` / `PoolMissingStake { pool }`). Not canonical-counted (GREEN; it produces the BLUE `SeedEpochConsensusInputs` but is not its author). |
| **MUST NOT** | Affect authoritative outputs (it is glue producing a record the bootstrap composer persists). Introduce nondeterminism (`BTreeMap` only). **Default-fill on a provenance gap:** a pool present in `pool_distribution` but absent from `pool_vrf_keyhashes` (or the reverse) MUST fail closed with the structured `SeedConsensusMergeError` — never a zero-hash fill. |
| **Inbound deps** | `ade_runtime::{genesis_bootstrap, mithril_bootstrap}` (the two verified-bootstrap composers). |
| **Outbound deps** | `ade_ledger::{consensus_view::PoolEntry, seed_consensus_inputs::SeedEpochConsensusInputs}`, `ade_runtime::consensus_inputs::LiveConsensusInputsCanonical`, `ade_types`. |
| **Entry points** | `ade_runtime::seed_consensus_merge::{merge_seed_epoch_consensus_inputs, SeedConsensusMergeError}`. |

---

### `ade_runtime` GREEN-by-content sub-trees

> All carry a `//! GREEN …` banner inside the RED `ade_runtime` crate, the BLUE deny attributes, and a purity CI gate. Each is "promotable to BLUE on demand, never demotable to RED" unless noted.

| Sub-tree | Purpose | MUST NOT | CI gate |
|---|---|---|---|
| `seed_consensus_merge` *(N-F-A A2)* | GREEN bootstrap-time merge: two-map `LiveConsensusInputsCanonical` → single-map BLUE `SeedEpochConsensusInputs`; saturating stake sum matches the forge-time projection. *(Full entry above.)* | Default-fill a provenance gap (zero-hash); affect authoritative outputs; introduce nondeterminism. | `ci_check_consensus_input_provenance.sh` (populate-side containment) |
| `forward_sync::reducer` *(N-Y)* | GREEN forward-sync lifecycle reducer. Composes the BLUE admit chokepoint and emits a closed `SyncEffect` plan; the private `AdmitPlan::durable` is the sole `AdvanceTip` emitter (durable-before-tip). The RED `forward_sync::pump_block` driver gained its first production caller in N-F-C (`ade_node::node_sync::run_node_sync`). | Emit `AdvanceTip` for a block before its `StoreBlockBytes` + `AppendWal` (DC-SYNC-01) — structurally unrepresentable; affect authoritative outputs; introduce nondeterminism. | `ci_check_forward_sync_chokepoint_only.sh` |
| `producer::coordinator` *(N-Q)* | Pure-state-machine coordinator for live producer-mode. Emits closed `CoordinatorEffect`s; hosts `CoordinatorState::kes_period_for_slot` (reused by the N-F-E forge tick) + `ForgedBlockArtifact` / `CoordinatorEvent`. | Own/store private signing material (CN-PROD-02); affect authoritative outputs. | `ci_check_producer_coordinator_no_secrets.sh` |
| `producer::producer_log` *(N-Q, extended N-W)* | Closed-vocabulary producer evidence log — closed `ProducerLogEvent` + closed `ForgeFailureReason` (incl. `UnsupportedProducerEra`). | Add an open/wildcard event variant; emit `BlockServed` for a block not in the served snapshot (CN-PROD-04). | `ci_check_operator_evidence_manifest_schema.sh` |
| `producer::chain_evolution` *(N-T)* | Linear `ChainEvolution` typestate threading the producer's chain state forward. | Never mints `AcceptedBlock` (CE-T-7); advance on authority disagreement (CE-T-6b); introduce nondeterminism. | gated by CE-T-7; purity inherited |
| `producer::{broadcast_to_served, served_chain_lookups}` *(N-G)* | GREEN glue computing served-chain broadcast targets + read-side lookups. | Affect authoritative outputs; serve a block that failed self-accept. | `ci_check_broadcast_to_served_purity.sh` |
| `clock` (`DeterministicClock` + `Clock` trait) *(N-K)* | GREEN clock trait + deterministic impl (DC-NODE-03). The `Clock::next_tick → millis_to_slot → SlotNo` seam is what the N-F-E forge tick derives its slot from (the sole wall-clock read on the forge path). | DeterministicClock must read no wall-clock. | `ci_check_clock_seam.sh` |
| `orchestrator::{mod, event, state, core}` *(N-K)* | GREEN core reducer + closed-vocabulary event + state for the node orchestrator. | tokio imports in core; open event vocabulary. | `ci_check_orchestrator_core_purity.sh` |
| `rollback::{cadence, in_memory_cache, chaindb_block_source, persistent_cache, persistent_writer}` *(N-I/N-J/N-K)* | GREEN rollback adapter glue + snapshot cadence + persistent cache/writer (DC-NODE-02, DC-CONS-21). | Parallel snapshot cadence; affect authoritative rollback state. | `ci_check_persistent_writer_no_parallel_cadence.sh`, `ci_check_snapshot_cadence_purity.sh` |
| `receive::{events_to_state, in_memory_chain_write}` *(N-H)* | GREEN receive-side glue mapping BLUE receive events to in-memory chain writes. | Affect authoritative receive verdict. | `ci_check_receive_replay_purity.sh` |
| `seed_import` *(N-M-A)* | Single authority converting a cardano-cli JSON UTxO dump into canonical seed entries. | Construct semantic types bypassing canonical decoders. | `ci_check_seed_import_closure.sh`, `ci_check_seed_import_full_preprod_support.sh` |
| `bootstrap_anchor` *(N-M-A)* | RED-shell composer minting `BootstrapAnchor` from import inputs (the *types* + binding predicate are BLUE in `ade_ledger`). | Mint an anchor outside this composer; bind a Mithril anchor without the BLUE `verify_mithril_binding` cross-check. | `ci_check_bootstrap_anchor_closure.sh` |
| `wal` *(N-M-A)* | File-backed Ade-native WAL (append-only). | Mutate/rewrite committed WAL entries. | `ci_check_wal_append_only.sh` |
| `consensus_inputs` *(N-M-C)* | Operator-extracted `LiveConsensusInputs` importer. | Treat the peer as runtime authority; overstate semantic truth. | `ci_check_live_consensus_inputs_closure.sh`, `ci_check_live_consensus_inputs_fingerprint.sh` |
| `admission::*` (the GREEN reducer half) *(N-M-C)* | GREEN admission verdict/agreement reducer comparing already-authoritative outputs. | Emit RED verdicts; skip reference scripts; treat `lagging` as success (DC-EVIDENCE-01). | `ci_check_admission_runner_closure.sh`, `ci_check_admission_no_red_verdicts.sh`, `ci_check_lagging_is_evidence_only.sh`, `ci_check_admit_replay_equivalence.sh` |

> **Note on `bootstrap` (the `bootstrap_initial_state` authority, N-K):** it carries a `//! GREEN` banner but at HEAD also hosts the **RED** A3b warm-start sidecar restore (a `SnapshotStore` read). It is documented in the RED `ade_runtime` entry below to keep the I/O-touching surface (`restore_seed_epoch_consensus_inputs`) visible; the cold-start / warm-start materialization composition remains GREEN over the BLUE authorities.

---

### `ade_node::ba02_evidence` *(GREEN by content — PHASE4-N-F-C L6; library, unwired)*

| Attribute | Value |
|-----------|-------|
| **Purpose** | The BA-02 peer-acceptance evidence surface: it COMPARES two already-authoritative outputs — the BLUE-minted forged-block hash (read verbatim from `ForgedBlockArtifact`, never recomputed) and an operator-captured peer-accept signal — and emits a closed verdict. GREEN evidence (`feedback-evidence-reducers-are-green-not-authority`): it forges nothing, admits nothing, persists no node state; a `Ba02Manifest` is a CLAIM ABOUT authority, not authority. Reached by no binary arm at this HEAD (exercised only by its `#[cfg(test)]` suite). |
| **Creates** | `AdeForgeRecord`; closed `PeerAcceptEvent` (2-variant allow-list: `PeerServedBlock` strongest / `PeerChainTip` corroborating); `PeerAcceptSource`; `NoEvidenceReason` (closed: `NoPeerAccept`/`HashMismatch`/`ChainPointMismatch`/`ConflictingPeerSignals`); `BA02Outcome` (closed: `Ba02Manifest` / `NoEvidence`); versioned `Ba02Manifest` (`BA02_MANIFEST_SCHEMA_VERSION = 1`). GREEN — not canonical-counted. |
| **MUST NOT** | (1) Affect authoritative outputs / forge / admit / persist node state. (2) Recompute the forged-block hash (read it verbatim from `ForgedBlockArtifact`). (3) Parse block bytes or construct semantic types. (4) **(RO-LIVE-06 / BA-02 honesty)** treat any weaker signal as acceptance — Ade self-accept, `ForgeSucceeded`, `block_received`, a lagging/diverged agreement verdict, or conflicting peer signals are all `NoEvidence`; the allow-list parser refuses to construct them as `PeerAcceptEvent`. (5) Construct a `Ba02Manifest` anywhere but `correlate`'s exact-match arm (single constructor, hash-primary, fail-closed). (6) Commit a synthetic manifest under `docs/evidence/*ba02*` (`ci_check_ba02_evidence_closed.sh`). |
| **Inbound deps** | None at HEAD — `main()` does not call it; reached only by tests. (Intended caller: the operator-pass live leg under RO-LIVE-01, deferred.) |
| **Outbound deps** | `ade_runtime::producer::coordinator::ForgedBlockArtifact`, `ade_types::Hash32`, `serde`, `serde_json`. |
| **Entry points** | `ade_node::ba02_evidence::{parse_peer_accept_events, correlate, AdeForgeRecord, Ba02Manifest, BA02Outcome, PeerAcceptEvent}`. |

---

### `ade_node` GREEN-by-content sub-trees

| Sub-tree | Purpose | MUST NOT | CI gate |
|---|---|---|---|
| `run_loop_planner` *(N-F-D S1; forge step N-F-E S1)* | GREEN pure lifecycle planner — `plan_loop_step` over the closed `LoopStep { SyncOnce, Idle, HaltCleanly, ForgeTick }`; content-blind `ForgeSlotStatus`; pure `forge_slot_status` monotonic guard. *(Full entry above.)* | `plan_loop_step` reference `SlotNo` (only the guard may); return a non-closed `LoopStep`; decide any authority; return `ForgeTick` under `NotDue`. | `ci_check_loop_planner_closed.sh` |
| `admission_log` *(N-M-B)* | GREEN admission-mode JSONL event vocabulary + writer (closed enum). | Add open/wildcard event variant. | `ci_check_admission_log_vocabulary_closed.sh` |
| `live_log` *(N-L-LIVE)* | GREEN closed JSONL vocabulary for the wire-only live pass. | Add open event variant; overstate semantic truth. | `ci_check_wire_only_event_vocabulary_closed.sh` |
| `admission` (the GREEN half of the orchestrator) *(N-M-B)* | GREEN admission orchestrator reducer + verdict mapping. | Affect authoritative verdict; skip reference-script validation. | `ci_check_admission_no_refscript_skip.sh` |
| `ba02_evidence` *(N-F-C, library/unwired)* | GREEN BA-02 evidence correlator — compares the BLUE forged-block hash vs an operator-captured peer-accept signal; single `Ba02Manifest` constructor inside `correlate`. *(Full entry above.)* | Treat a weaker signal as acceptance; recompute the forged hash; forge/admit/persist; commit a synthetic manifest. | `ci_check_ba02_evidence_closed.sh` |

---

## RED Modules — Imperative Shell

> I/O, network, storage, clocks, retries. May depend on BLUE/GREEN. Must not modify core state directly or construct semantic types unsafely.

### `ade_runtime`

| Attribute | Value |
|-----------|-------|
| **Purpose** | The imperative shell — moves bytes, owns sockets/files/clocks/keys, drives tokio tasks. Hosts: producer-mode key custody + shell composition (`producer/`), the N2N network drivers (`network/`), the node orchestrator runners (`orchestrator/`), the receive/rollback/admission shells, the seed-import/WAL/consensus-inputs importers, ChainDB + recovery shells, the N-Y bootstrap/forward-sync surfaces (`forward_sync::pump`, `mithril_import`, `genesis_bootstrap`, `recovery/restart`), the N-Z `mithril_bootstrap` Mithril production-bootstrap composition, and (N-F-A) the seed-epoch consensus-input recovered-state shell: the `bootstrap_initial_state` authority's A3b warm-start sidecar restore (`bootstrap`), the shared WAL provenance append helper (`seed_consensus_provenance`), the anchor-fp-keyed sidecar `SnapshotStore` surface (`chaindb`, incl. the N-F-C `list_seed_epoch_consensus_anchor_fps()` discovery method), and the genesis/Mithril composers' sidecar-persist tail. |
| **Creates (RED-only)** | `KesSecret`, `VrfSigningKey`, `ColdSigningKey` custody wrappers; `KeyLoadError`; `MuxTransportHandle` consumers; `OutboundCommand` (closed enum, N-S-B); `PerPeerOutbound` map; `DispatchError`; `ServedChainHandle`; `GenesisAnchor`. **N-Y:** `forward_sync::pump::{PumpError, PumpTip, SnapshotSink, NoCheckpointSink}`; `mithril_import::{RawMithrilManifest, …, MithrilManifestError, MithrilProvenanceImport}`; `recovery::restart::{NodeRecoveryError, RecoveredNode}`. **N-Z:** `mithril_bootstrap::{MithrilSeedPointInputs, MithrilBootstrapOutput, MithrilBootstrapError}`. **N-F-A:** `bootstrap::{SeedEpochConsensusSource (closed: NotRequired / RequiredFromRecoveredProvenance), BootstrapState, BootstrapInputs, BootstrapError (incl. SeedConsensusProvenanceMissing / SeedConsensusSidecarMissing / SeedConsensusHashMismatch / SeedConsensusBindingMismatch / SeedConsensusSidecarDecode)}`; `seed_consensus_merge::SeedConsensusMergeError` (GREEN, entry above); `genesis_bootstrap::{GenesisBootstrapError, GenesisBootstrapOutput}`; `mithril_bootstrap::MithrilBootstrapError`. Never semantic/canonical types. |
| **Interprets** | Canonicalizes peer bytes/files for the BLUE core: `producer::keys::load_kes_signing_key_skey`; `producer::genesis_parser`; `producer::opcert_envelope`; `seed_import`. **N-Y:** `mithril_import::json::parse_mithril_manifest_json` is the SOLE manifest-JSON parser. **N-F-A:** `bootstrap::restore_seed_epoch_consensus_inputs` reads the anchor-fp-keyed sidecar bytes from the `SnapshotStore` and verifies them fail-closed (hash bind → `decode_seed_epoch_consensus_inputs` → anchor/epoch binding → byte-identity re-encode). `seed_consensus_provenance::append_seed_epoch_provenance` `blake2b_256`-hashes the exact A1 sidecar bytes the composer just `put` and appends the closed `WalEntry::SeedEpochConsensusInputsImported`. |
| **MUST NOT** | (1) Modify BLUE state directly or construct semantic types from raw bytes. (2) Bypass canonical validation. (3) `producer::signing` (DC-CRYPTO-03/05) — RED-confined key custody. (4) `producer::keys` — no `KesSecret::from_*` inside `load_kes_signing_key_skey`. (5) `producer::coordinator` is GREEN and holds no secrets. (6) `network::outbound_command` (CN-OUTBOUND-RELAY-01) — `OutboundCommand` is the sole channel to MuxPump's encoder; no `Vec<u8>` byte tunnel; no direct transport write from `produce_mode`. (7) per-peer outbound map (CN-PEER-OUTBOUND-MAP-01 / DC-OUTBOUND-FIFO-01). (8) `network::n2n_server` — no signing dep. **(9) `forward_sync::pump` (DC-SYNC-01):** apply the GREEN reducer's `SyncEffect` plan in order; refuse to advance the tip before `StoreBlockBytes` + `AppendWal` return Ok (`PumpError::TipBeforeDurable`). **(10) `mithril_import` (CN-MITHRIL-01):** no semantic decision; never re-verify the STM multisig; route initial state through the single `bootstrap_initial_state` authority. **(11) `genesis_bootstrap` (CN-NODE-01 / DC-GENESIS-SRC-01 / CN-CINPUT-02):** route through the single `bootstrap_initial_state` authority; records `SeedProvenance::CardanoCliJson`; sidecar tail = GREEN-merge → A1-encode → sidecar put (durable) THEN WAL provenance append (commit point); the composer WRITES the sidecar, never consumes one (`SeedEpochConsensusSource::NotRequired`). **(12) `recovery::restart` (recovery-contract / DC-WAL-* / DC-STORE-05 / T-REC-01/02):** compose the existing authorities; fail fast on `NodeRecoveryError::WalTailFingerprintMismatch`. (`recover_node_state` is the test-only secondary recovery entry.) **(13) `mithril_bootstrap` (DC-MITHRIL-02 / CN-MITHRIL-01 / CN-NODE-01 / CN-CINPUT-02):** the anchor `seed_point` MUST be minted from `MithrilSeedPointInputs`, never sourced from the manifest; run `verify_mithril_binding` fail-closed BEFORE any `bootstrap_initial_state`; sidecar tail runs only on success, put-then-WAL-append, writing — never consuming — the sidecar. **(14) `bootstrap::restore_seed_epoch_consensus_inputs` (DC-CINPUT-01):** fail closed on a missing sidecar, a `blake2b_256` mismatch, a non-canonical/unknown-version decode, an `anchor_fp`/`epoch_no` binding mismatch, or a non-byte-identical re-encode — and MUST NOT fall back to the forge-time `--consensus-inputs-path` bundle on any failure. **(15) `seed_consensus_provenance::append_seed_epoch_provenance` (CN-CINPUT-02 / DC-CINPUT-01):** MUST hash the EXACT A1 bytes the composer `put`; called only AFTER the durable sidecar put; referenced only at the two verified-bootstrap composition sites (`ci_check_consensus_input_provenance.sh`). **(16) `chaindb` seed-sidecar surface (CN-CINPUT-02):** `put_/get_seed_epoch_consensus_inputs` is an anchor-fp-keyed namespace **disjoint** from the slot-keyed snapshot space; idempotent on identical bytes, `InvalidOperation` on conflicting bytes; the redb backend gates `SCHEMA_VERSION = 3`. `list_seed_epoch_consensus_anchor_fps()` is a read-only ascending discovery surface (its sole caller is `node_lifecycle::warm_start_recovery`) — discovery is not proof; the warm-start verify chain remains the authority. |
| **Inbound deps** | `ade_node`, `ade_core_interop`, `ade_testkit` (dev/integration). |
| **Outbound deps** | `ade_types`, `ade_core`, `ade_crypto`, `ade_codec`, `ade_ledger`, `ade_network`, `redb`, `serde`, `serde_json`, `bech32`, `base58`, `cardano-crypto` (`["vrf-draft03", "dsign"]`), `ed25519-dalek`, `tokio`. |
| **Entry points** | `ade_runtime::producer::{producer_shell::*, coordinator::{*, ForgedBlockArtifact, CoordinatorEvent, CoordinatorState (incl. kes_period_for_slot)}, served_chain_handle::{ServedChainHandle, push_atomic}, genesis_parser::*, opcert_envelope::*}`, `ade_runtime::network::{n2n_listener::*, n2n_dialer::*, mux_pump::*, n2n_server::*, outbound_command::OutboundCommand}`, `ade_runtime::orchestrator::*`, `ade_runtime::bootstrap::{bootstrap_initial_state, BootstrapInputs, BootstrapState, SeedEpochConsensusSource, BootstrapError}`, `ade_runtime::seed_consensus_merge::{merge_seed_epoch_consensus_inputs, SeedConsensusMergeError}`, `ade_runtime::seed_consensus_provenance::append_seed_epoch_provenance`, `ade_runtime::forward_sync::{forward_sync_step, pump_block, SyncEffect, PumpError, SnapshotSink}`, `ade_runtime::mithril_import::{import_mithril_manifest, parse_mithril_manifest_json, MithrilProvenanceImport, MithrilManifestError}`, `ade_runtime::genesis_bootstrap::{bootstrap_from_conway_genesis, GenesisBootstrapError, GenesisBootstrapOutput}`, `ade_runtime::mithril_bootstrap::{bootstrap_from_mithril_snapshot, MithrilSeedPointInputs, MithrilBootstrapOutput, MithrilBootstrapError}`, `ade_runtime::recovery::restart::{recover_node_state, NodeRecoveryError}` (test-only secondary), `ade_runtime::chaindb::{ChainDb, SnapshotStore (incl. put_/get_seed_epoch_consensus_inputs, list_seed_epoch_consensus_anchor_fps), InMemoryChainDb}`, `ade_runtime::clock::{Clock, DeterministicClock, SystemClock}`, `ade_runtime::{seed_import, consensus_inputs, wal, bootstrap_anchor}::*`. |
| **Key modules** | `producer/` (RED + GREEN-by-content), `network/`, `orchestrator/` (RED runners + GREEN core), `receive/`, `rollback/`, `admission/`, `seed_import/`, `consensus_inputs/`, `wal/`, `chaindb/` (`mod`, `persistent`, `in_memory`, `snapshot_contract` — anchor-fp-keyed sidecar surface; redb `seed_cinputs_by_anchor_fp` table, schema v3; the read-only `list_seed_epoch_consensus_anchor_fps()` discovery method), `forward_sync/` (`mod`, `reducer` GREEN, `pump` RED), `mithril_import/`, `seed_consensus_merge.rs` (GREEN, N-F-A), `seed_consensus_provenance.rs` (RED, N-F-A), `mithril_bootstrap.rs`, `genesis_bootstrap.rs`, `recovery/` (`mod`, `restart`), `bootstrap.rs` (GREEN composition + RED A3b restore), `bootstrap_anchor.rs`, `clock.rs`. |

---

### `ade_node`

| Attribute | Value |
|-----------|-------|
| **Purpose** | The node binary + library entry. Owns argv parsing, the node lifecycle, and the mode drivers dispatched by `main()`: `--mode wire_only` (default, `wire_only`), `--mode admission` (`admission/`), `--mode key_gen_kes` (`key_gen`), `--mode produce` (`produce_mode`), and (N-F-C) `--mode node` (`node_lifecycle`). `produce_mode::run_real_forge` carries an era guard (non-Praos → `ForgeFailureReason::UnsupportedProducerEra`). `admission::runner` strips the served block's tag-24 envelope via `ade_codec::unwrap_tag24`. **N-F-C/N-F-D/N-F-E scope (the `--mode node` live surface):** `node_lifecycle` is the single `PHASE4-N-F-C-LIFECYCLE-OWNER` — it opens a `PersistentChainDb` + `FileWalStore`, classifies first-run-vs-warm-start as a pure function of on-disk state, then (FirstRun) runs the Mithril-only `bootstrap_from_mithril_snapshot` durably or (WarmStart) replays the WAL and recovers the verified `BootstrapState` via `bootstrap_initial_state(RequiredFromRecoveredProvenance)`. Both arms converge into `run_relay_loop` (N-F-D), the single live-run owner: each iteration is selected by the GREEN `plan_loop_step` over the closed `LoopStep`, and the tip advances ONLY via `run_node_sync → pump_block`. **N-F-E** added the `ForgeTick` arm: with `forge: Some(&mut ForgeActivation)` (hermetic/test-only at HEAD) the loop derives the slot via the clock seam, reuses `CoordinatorState::kes_period_for_slot`, calls exactly one fenced `node_sync::forge_one_from_recovered`, advances NO durable tip, and serves/admits/gossips nothing; with `None` (the binary path) it is byte-identical to N-F-D relay. The L6 BA-02 evidence correlator (`ba02_evidence`) is a **library surface reached by no binary arm** (tested only). `produce_mode` still cold-starts from `--consensus-inputs-path` with `SeedEpochConsensusSource::NotRequired` and does not consume the recovered surface. |
| **Creates (RED-only)** | `Cli`, `CliError`, `ProduceCli`, `KeyGenKesCli`, `Mode` (closed: `WireOnly`/`Admission`/`KeyGenKes`/`Produce`/`Node`), `NodeStartupInputs`, `NodeShutdownEvidence`, `NodeRunError`, exit-code constants; the `produce_mode` slot loop + ticker + evidence-I/O types; the admission runner types. **N-F-C (`node_lifecycle`, RED):** `NodeStart` (closed: `FirstRun`/`WarmStart`), `NodeLifecycleError` (closed fail-closed surface, incl. `RelaySync` from N-F-D), exit-code constants. **N-F-D (`node_lifecycle`, RED):** `run_relay_loop`. **N-F-E (`node_lifecycle`, RED):** `ForgeActivation<'a>` (recovered-state forge inputs + clock seam + the per-iteration derived slot + the in-memory `hermetic_forge_outcomes: Vec<CoordinatorEvent>` test-observation field). **N-F-C/D (`node_sync`, RED):** `NodeBlockSource` (closed: `WirePump`/`InMemory`, with the content-blind `has_work_ready`/`is_ended`/`wait_ready`/non-blocking `next_block` readiness), `NodeSyncError`, `NodeForgeError`. **N-F-D/E (`run_loop_planner`, GREEN — full entry in the GREEN section):** `LoopStep` (4-variant incl. `ForgeTick`), `ForgeSlotStatus`, `LoopState`, `SyncStatus`, `ShutdownStatus`. **N-F-C (`ba02_evidence`, GREEN):** `AdeForgeRecord`, `PeerAcceptEvent`, `PeerAcceptSource`, `NoEvidenceReason`, `BA02Outcome`, `Ba02Manifest`, `BA02_MANIFEST_SCHEMA_VERSION`. Never semantic/canonical types. |
| **Interprets** | argv (closed mode set: `wire_only` \| `admission` \| `key_gen_kes` \| `produce` \| `node`); operator-supplied key/genesis/opcert file paths (delegated to `ade_runtime` parsers); evidence-manifest TOML schema; the admission runner interprets a peer's tag-24-wrapped BlockFetch `MsgBlock` payload via `ade_codec::unwrap_tag24`. **N-F-C:** `node_lifecycle` interprets the first-run extraction inputs (`--mithril-manifest-path`, `--json-seed-path`, `--consensus-inputs-path`) — all Mithril-bound, never forge inputs; `ba02_evidence::parse_peer_accept_events` is an allow-list parser over an operator-captured peer-accept JSONL log. **N-F-E:** the `ForgeTick` arm interprets the clock seam's wall-clock tick into a `SlotNo` (`millis_to_slot`) — the only nondeterministic input on the forge path, canonicalized at the RED seam before it reaches the pure `forge_slot_status` guard. |
| **MUST NOT** | (1) Construct semantic types bypassing the canonical decoders / `ade_runtime` parsers. (2) `produce_mode` (CN-PROD-04, CN-OUTBOUND-RELAY-01) — obtain initial state only via `bootstrap_initial_state`; re-validate each broadcast block through BLUE `self_accept`; emit outbound bytes only via `OutboundCommand` → `MuxPump`. (3) No synthetic forge state (N-T). (4) No durability in the produce_mode path (N-U scope). (5) `run_real_forge` (CN-FORGE-04, N-W) — no RED-side era dispatch for the leader-VRF alpha; non-Praos era fail-closes to `UnsupportedProducerEra`. (6) `admission::runner` (CN-WIRE-08, N-X) — no hand-rolled tag-24 parse; strip via `ade_codec::unwrap_tag24`. (7) `wire_only` — overstate semantic truth; closed JSONL vocabulary only. (8) operator-evidence manifest carries the closed schema (CN-OPERATOR-EVIDENCE-01). (9) closed mode set with no wildcard dispatch arm (`ci_check_node_mode_closure.sh`). (10) `produce_mode` recovered-surface fence (CN-CINPUT-02) — MUST pass only `SeedEpochConsensusSource::NotRequired`; stays diagnostic. **(11) `node_lifecycle` lifecycle owner (CN-NODE-01):** exactly one `PHASE4-N-F-C-LIFECYCLE-OWNER`; initial state SOLELY via `bootstrap_initial_state`; no parallel/cold init, no genesis/bundle/cold/graft fallback, no `recover_node_state` overclaim; `RequiredFromRecoveredProvenance` constructed only here + `bootstrap.rs` (`ci_check_lifecycle_owner_uses_bootstrap_initial_state.sh`). **(12) `run_relay_loop` / `node_sync` sync driver (CN-NODE-02 / DC-SYNC-02, N-F-D; strengthened N-F-E):** the relay loop advances the durable tip ONLY via `run_node_sync → pump_block` — no direct `pump_block(`, no manual tip advance (`.put_block`/`AdvanceTip`/`rollback_to_slot`), no follower-as-sync, no verdict-as-sync; `run_node_sync` is UNMODIFIED (`ci_check_node_run_loop_containment.sh`, `ci_check_node_sync_via_pump.sh`). **(13) `run_relay_loop` forge tick (CN-NODE-02 / CE-E-4 / DC-NODE-05, N-F-E):** the forge is reached ONLY through exactly one fenced `forge_one_from_recovered(` call (self-accept-only); MUST NOT call `run_real_forge` directly, correlate evidence (`correlate(` / `Ba02Manifest`), or serve/broadcast/gossip a forged block (`served_chain_admit`/`push_atomic`/`OutboundCommand`/`broadcast`/`block_fetch` all forbidden in the loop body); the forge advances no durable tip; forge is opt-in via `ForgeActivation` (`None` reproduces N-F-D relay) (`ci_check_node_run_loop_containment.sh`). **(14) `run_loop_planner` (DC-NODE-05, N-F-E):** `plan_loop_step` is content-blind — it MUST NOT reference `SlotNo` (only the `forge_slot_status` guard may); the closed `LoopStep`/`ForgeSlotStatus` set is pinned (`ci_check_loop_planner_closed.sh`). **(15) `node_sync::forge_one_from_recovered` consume-side fence (CN-CINPUT-03 / DC-CINPUT-02b):** derive the leadership `PoolDistrView` ONLY from the recovered surface via `from_seed_epoch_consensus_inputs(`; MUST NOT reach for the forge-time bundle nor fabricate a `SeedEpochConsensusInputs { … }` literal (`ci_check_consensus_input_provenance.sh` guard d). **(16) `ba02_evidence` (RO-LIVE-06 / BA-02 honesty):** the single `Ba02Manifest` constructor is `correlate`'s exact-match arm; MUST NOT treat any weaker signal as acceptance, recompute the forged-block hash, forge/admit/persist node state, or commit a synthetic manifest (`ci_check_ba02_evidence_closed.sh`). |
| **Inbound deps** | None (binary + integration tests). |
| **Outbound deps** | `ade_types`, `ade_core`, `ade_crypto`, `ade_ledger`, `ade_runtime`, `ade_network`, `ade_codec`, `serde`, `serde_json`, `tokio`. Dev-deps: `ade_testkit`, `tempfile`. (`node_sync` adds `cardano-crypto`/`ed25519-dalek` in its `#[cfg(test)]` operator-shell idiom only.) |
| **Entry points** | `main()`; `ade_node::run_node_lifecycle` (N-F-C, `--mode node`); `ade_node::node_lifecycle::{run_relay_loop, ForgeActivation}` (N-F-D/E); `ade_node::run_loop_planner::{plan_loop_step, forge_slot_status, LoopStep, ForgeSlotStatus}` (N-F-D/E, GREEN); `ade_node::run_node_until_shutdown`; `ade_node::produce_mode::{run_real_forge, run_produce_mode, *}`; `ade_node::cli::{Cli, ProduceCli, Mode}`; `ade_node::admission::*`; `ade_node::wire_only::*`; `ade_node::key_gen::*`; `ade_node::node_sync::{NodeBlockSource, run_node_sync, forge_one_from_recovered}` (library); `ade_node::ba02_evidence::{parse_peer_accept_events, correlate, Ba02Manifest}` (library). |
| **Key modules** | `lib.rs`, `cli.rs`, `node.rs`, `main.rs`, `node_lifecycle.rs` (the `--mode node` owner + `run_relay_loop` + `ForgeActivation`), `run_loop_planner.rs` (**GREEN** — the pure loop planner + forge step), `node_sync.rs` (the sync driver + recovered-forge handoff), `ba02_evidence.rs` (**GREEN** — BA-02 evidence correlator, library/unwired), `produce_mode.rs`, `wire_only.rs`, `key_gen.rs`, `admission/` (`bootstrap`, `runner`, `seed_to_snapshot`, `verdict`), `admission_log/`, `live_log/`. |

> **GREEN sub-classification:** `ade_node::run_loop_planner` and `ade_node::ba02_evidence` are GREEN-by-content (the planner is a pure content-blind decision function; the evidence correlator COMPARES already-authoritative outputs) — full entries in the GREEN section. `node_lifecycle` + `node_sync` are RED (they own the persistent stores / drive the apply path; the forge tick reads the wall-clock seam).

---

### `ade_core_interop`

| Attribute | Value |
|-----------|-------|
| **Purpose** | Live cardano-node interop driver. Hosts the `live_*_session` RED binaries (operator-action evidence harness) plus the N-E S4/S5 GREEN tx-submission bridges, and the chain-follow driver (`follow`). `follow` strips the peer's ChainSync `RollForward` `[era_tag, tag24(header_cbor)]` envelope via `ade_codec::unwrap_tag24`. |
| **Creates (RED-only)** | Live-session drivers and transcript types; never semantic types. |
| **MUST NOT** | (1) Construct semantic types from raw bytes. (2) Be depended on by any BLUE/GREEN crate (RED leaf). (3) Overstate semantic truth in evidence. (4) `follow` (CN-WIRE-08) — no hand-rolled tag-24 parse; strip via `ade_codec::unwrap_tag24`. |
| **Inbound deps** | None (RED leaf — binaries). |
| **Outbound deps** | `ade_core`, `ade_codec`, `ade_crypto`, `ade_ledger`, `ade_runtime`, `ade_network`, `ade_testkit`, `ade_types`, `tokio`. |
| **Entry points** | `ade_core_interop::bin::{live_tx_submission_session, live_block_production_session, live_block_fetch_session, live_block_follow_session}`; `ade_core_interop::follow::*`; `ade_core_interop::tx_submission`, `ade_core_interop::local_tx_submission` (GREEN sub-class). |
| **Key modules** | `bin/`, `follow.rs`, `tx_submission.rs`, `local_tx_submission.rs`. |

---

### `ade_network::mux::transport` *(RED)*

| Attribute | Value |
|-----------|-------|
| **Purpose** | The only place inside `ade_network` where socket I/O happens. RED tokio shell over the BLUE mux frame primitive and the GREEN session reducer. Provides `MuxTransportHandle` + closed `TransportError` + `DuplexCapacity::DEFAULT` + `spawn_duplex`. |
| **Creates (RED-only)** | `MuxTransportHandle`, `TransportError`, `DuplexCapacity`, `MuxTransport`. |
| **MUST NOT** | (1) Construct semantic types. (2) Bypass `mux::frame` for framing. (3) Live in BLUE scope. |
| **Inbound deps** | `ade_runtime::network::{mux_pump, n2n_dialer}`. |
| **Outbound deps** | `ade_network::mux::frame` (BLUE), `ade_network::session` (GREEN), `tokio`. |
| **Entry points** | `ade_network::mux::transport::{MuxTransportHandle, spawn_duplex, open_tcp}`. |
| **Key modules** | `mux/transport.rs`. |

---

### `ade_network` *(RED capture binaries — non-session)*

| Attribute | Value |
|-----------|-------|
| **Purpose** | Operator-action capture binaries (live evidence harness) inside `ade_network`. The `ade_chain_sync_capture` binary supports `--intersect-slot` / `--intersect-hash` flags (N-X) to capture a real Conway `RollForward` golden fixture. |
| **MUST NOT** | (1) Construct semantic types from raw bytes. (2) Overstate semantic truth in captured evidence. |
| **Outbound deps** | `ade_network` BLUE/GREEN submodules, `ade_codec`, `ade_types`, `tokio`. |

---

## Cross-Module Rules (project-wide)

### Dependency direction (RED → GREEN → BLUE; never outward)

```
ade_types     → {}
ade_codec     → {ade_types}
ade_crypto    → {ade_types, blake2, ed25519-dalek, cardano-crypto[vrf-draft03,kes-sum(test-only),dsign]}
ade_core      → {ade_types, ade_crypto, minicbor}
ade_ledger    → {ade_core, ade_plutus, ade_crypto, ade_codec, ade_types, minicbor, num-*}
ade_plutus    → {ade_crypto, ade_codec, ade_types, aiken_uplc, pallas-primitives(internal)}
ade_network   → BLUE submodules → {ade_codec, ade_types}; session(GREEN) → +BLUE submodules; transport(RED) → +tokio
ade_testkit   → {ade_core, ade_ledger, ade_plutus, ade_runtime, ade_crypto, ade_codec, ade_types, cardano-crypto}   [GREEN]
ade_runtime   → {ade_core, ade_crypto, ade_codec, ade_types, ade_ledger, ade_network, redb, serde*, bech32, base58, cardano-crypto[vrf-draft03,dsign], ed25519-dalek, tokio}   [RED]
ade_node      → {ade_types, ade_core, ade_crypto, ade_ledger, ade_runtime, ade_network, ade_codec, serde, serde_json, tokio}   [RED; run_loop_planner + ba02_evidence are GREEN-by-content]
ade_core_interop → {ade_core, ade_codec, ade_crypto, ade_ledger, ade_runtime, ade_network, ade_testkit, ade_types, tokio}   [RED leaf]
```

**Forbidden directions.** Any BLUE crate (or BLUE `ade_network` submodule) depending on `ade_runtime`, `ade_node`, `ade_core_interop`, or the RED half of `ade_network` is a CI failure (`ci_check_dependency_boundary.sh`). Any non-`ade_plutus` crate referring to `pallas_*` is a CI failure (`ci_check_pallas_quarantine.sh`). Any `cardano_crypto::kes` import outside `#[cfg(test)]` under `crates/ade_crypto/src/**` is a CI failure (`ci_check_kes_sum_compatibility.sh` Guard 3). A second block-envelope encoder or a `produce_mode` direct-transport write is a CI failure. A second `leader_vrf_input` authority, or a path accepting both TPraos and Praos VRF inputs for one era, is a CI failure (`ci_check_producer_praos_vrf.sh`). A second `wrap_tag24`/`unwrap_tag24` definition or a hand-rolled tag-24 parse in RED is a CI failure (`ci_check_tag24_wire_authority.sh`). A forward-sync `AdvanceTip` reachable before the block's durability writes (DC-SYNC-01), a Mithril/genesis bootstrap path bypassing the single `bootstrap_initial_state` authority (`ci_check_mithril_uses_bootstrap_initial_state.sh`), and an internal-ledger-fingerprint-vs-Haskell-hash equality assertion in the compatibility harness (DC-COMPAT-01) are CI failures. A registry rule citing a non-existent `crates/**.rs` / `ci/**.sh` `code_locus` is a CI failure (`ci_check_registry_code_locus_exists.sh`). A Mithril-bootstrap path that sources the anchor `seed_point` from the manifest (DC-MITHRIL-02 — `ci_check_mithril_seed_point_independence.sh`) is a CI failure. A forge-time path that populates the seed-epoch consensus-input sidecar or appends its WAL provenance is a CI failure (CN-CINPUT-02 — `ci_check_consensus_input_provenance.sh`). **N-F-C:** a second marked `PHASE4-N-F-C-LIFECYCLE-OWNER`, a parallel/cold storage-init, a genesis/bundle/cold/graft fallback, a `recover_node_state` overclaim, or a `RequiredFromRecoveredProvenance` construction outside {owner, `bootstrap.rs`} is a CI failure (CN-NODE-01 — `ci_check_lifecycle_owner_uses_bootstrap_initial_state.sh`); treating a weaker signal as BA-02 peer acceptance, or constructing a `Ba02Manifest` outside `correlate`'s exact-match arm, is a CI failure (RO-LIVE-06 — `ci_check_ba02_evidence_closed.sh`). **NEW (N-F-D): the relay-loop owner must advance the durable tip ONLY via `run_node_sync → pump_block`, drive each step from the GREEN closed-`LoopStep` planner, and the planner must own no authority** — a direct `pump_block`/manual tip advance/forge/evidence/verdict/follower/second-bootstrap reference in `run_relay_loop`, or a `LoopStep` outside the closed set, is a CI failure (CN-NODE-02 / DC-SYNC-02 — `ci_check_node_run_loop_containment.sh`, `ci_check_loop_planner_closed.sh`). **NEW (N-F-E): forge is subordinate and self-accept-only** — `plan_loop_step` referencing a `SlotNo` (content-blindness; only the `forge_slot_status` guard may), or `run_relay_loop` reaching the forge by any path other than exactly one fenced `forge_one_from_recovered` (no direct `run_real_forge`, no evidence correlation, no serve/broadcast/gossip of a forged block, no durable tip advance), is a CI failure (DC-NODE-05 / CN-NODE-02 / CE-E-4 — `ci_check_loop_planner_closed.sh`, `ci_check_node_run_loop_containment.sh`).

### Naming convention

All crates are prefixed `ade_`. TCB color is not encoded in the crate name. The authoritative classifier is `.idd-config.json` `core_paths` + the per-file `// Core Contract:` / `//! BLUE|GREEN|RED` banner + the cluster TCB Color Maps; CI scripts hard-code their BLUE list.

### Closed enums / registries (for SEAMS cross-reference)

Closed semantic surfaces detected at HEAD: `AcceptedMiniProtocol` (`ade_network::session`), `TagEnvelopeError` (`ade_codec::cbor::tag24`, N-X), `ExpectedVrfInput` (2-variant, `ade_core::consensus::vrf_cert`, N-W), `LeaderCheckVerdict` (2-variant, `ade_core::consensus::leader_check`), `OutboundCommand` + `DispatchError` (`ade_runtime::network`), `ProducerLogEvent` + `ForgeFailureReason` (incl. `UnsupportedProducerEra`), `ChainEvolutionError`, `ServedChainAdmitError`, `KesError` / `KesParseError`, the admission/wire-only/live-log JSONL vocabularies, the operator-evidence manifest TOML schema, the closed `CardanoEra` / Conway cert + governance enums. **N-Y:** `SeedProvenance` (closed, version-gated behind `ANCHOR_SCHEMA_VERSION = 2`), `MithrilImportError`, `GenesisSourceError`, `SyncEffect` (4-variant), `MithrilManifestError`, `PumpError`, `NodeRecoveryError`, the `sync_diff` observable `BlockVerdict` + `RegressionFixtureViolation`. **N-Z:** `MithrilBootstrapError`. **N-F-A:** `SeedEpochConsensusInputs` (closed canonical record, version-gated behind `SEED_CINPUT_SCHEMA_VERSION = 1`, SOLE codec), `SeedConsensusInputsError` (6-variant), `SeedConsensusMergeError` (2-variant), `SeedEpochConsensusSource` (2-variant), and the additive **WAL** variant `WalEntry::SeedEpochConsensusInputsImported` (tag 3). **N-F-C:** `Mode` (5-variant closed, no wildcard dispatch), `NodeStart` (2-variant), `NodeLifecycleError` / `NodeSyncError` / `NodeForgeError` (closed fail-closed surfaces), `NodeBlockSource` (2-variant), and the BA-02 evidence vocabulary `PeerAcceptEvent` / `PeerAcceptSource` / `NoEvidenceReason` / `BA02Outcome` + versioned `Ba02Manifest`. **N-F-D:** `LoopStep` (then 3-variant: `SyncOnce` / `Idle` / `HaltCleanly`, `ade_node::run_loop_planner`). **N-F-E:** `LoopStep` extended to **4-variant** (`+ ForgeTick`, additive — the closed-set CI gate now pins all four) and the **new** content-blind `ForgeSlotStatus` (2-variant: `Due` / `NotDue`). **Governance: `WalEntry` deliberately stays a CE-not-law surface — additively evolvable behind the WAL schema version (tags append-only); `LoopStep` is likewise an additively-evolvable closed planner vocabulary (3→4 variants is the additive `ForgeTick` extension), not a frozen registry law. The redb `chaindb` `SCHEMA_VERSION` (v3) is a versioned-gate, not a frozen contract.**

### CI enforcement (110 scripts under `ci/`)

The full list is mechanically obtainable via `ls ci/ci_check_*.sh` (110 at HEAD). CI lives entirely under `ci/` (`ci_dirs = ["ci"]`); there is no `.github/workflows/` invariant gate at HEAD. New / changed / load-bearing since the prior CODEMAP HEAD (PHASE4-N-F-C close, 108 scripts):

| Script | Enforces | Cluster / commit |
|---|---|---|
| `ci_check_loop_planner_closed.sh` | **NEW in the N-F-D span (counted here for the first time); EXTENDED N-F-E (DC-NODE-05)** — the GREEN planner carries `//! GREEN`, `LoopStep`/`plan_loop_step` are defined, and `LoopStep`/`ForgeSlotStatus` are pinned closed. N-F-E S1 scoped the `SlotNo` ban: it is forbidden ONLY inside `plan_loop_step` (content-blind step selection) — the pure `forge_slot_status` guard may consume a `SlotNo`. | N-F-D; extended N-F-E `214b0d3` |
| `ci_check_node_run_loop_containment.sh` | **NEW in the N-F-D span (counted here for the first time); EXTENDED N-F-E (CN-NODE-02 / CE-E-4)** — `run_relay_loop` must call `run_node_sync(` (sole tip-advance), reach no tip-mutation token directly, and reference no verdict/follower/second-bootstrap path. N-F-E S2 permits **exactly one** fenced `forge_one_from_recovered(` (self-accept-only), and forbids `run_real_forge`/`correlate(`/`Ba02Manifest` + the serve/broadcast tokens (`served_chain_admit`/`push_atomic`/`OutboundCommand`/`broadcast`/`block_fetch`) in the loop body. | N-F-D; extended N-F-E `98b488a` |
| `ci_check_consensus_input_provenance.sh` | CN-CINPUT-02 (populate-side + forge-time fence) + guard (d) the CONSUME-side fence (CN-CINPUT-03 / DC-CINPUT-02b): the forge handoff projects the leadership view via `from_seed_epoch_consensus_inputs(`, names no bundle/cold token, fabricates no `SeedEpochConsensusInputs { … }` literal. (Unchanged in N-F-E; the forge tick reaches the handoff through this fence.) | N-F-A; guard (d) N-F-C (carried) |
| `ci_check_lifecycle_owner_uses_bootstrap_initial_state.sh` | CN-NODE-01 — single `PHASE4-N-F-C-LIFECYCLE-OWNER`; both arms route through the single `bootstrap_initial_state` authority; no parallel/cold init, no genesis/bundle/cold/graft fallback. (Carried forward; both arms now converge into `run_relay_loop`.) | N-F-C (carried) |
| `ci_check_node_sync_via_pump.sh` | DC-SYNC-01 — `run_node_sync` advances the tip ONLY via `pump_block` (`run_node_sync` UNMODIFIED by N-F-D/E). | N-F-C (carried) |
| `ci_check_ba02_evidence_closed.sh` | RO-LIVE-06 / BA-02 honesty — single `Ba02Manifest` constructor inside `correlate`; no self-evidence token as acceptance; no committed synthetic manifest. | N-F-C (carried) |
| `ci_check_mithril_seed_point_independence.sh` | DC-MITHRIL-02 — the Mithril-bootstrap anchor `seed_point` is minted from `MithrilSeedPointInputs`, never laundered from the manifest. | N-Z (carried) |
| `ci_check_registry_code_locus_exists.sh` | traceability drift guard — every `crates/**.rs` + `ci/**.sh` path cited in any registry `code_locus` must exist on disk. | post-N-Y (carried) |

> Earlier-cluster scripts (N-A through N-Z, plus N-F-A/N-F-C) are present and counted in the 110 total. The per-script enforce/scope detail is in the registry's `ci_script` fields per rule.

---

## Generation notes

- Regenerated (**incremental, scoped refresh** catching up through the PHASE4-N-F-D + PHASE4-N-F-E cluster closes) at HEAD `cd2484f` (`git rev-parse --short HEAD`). The prior on-disk CODEMAP stopped at the PHASE4-N-F-C close (`71a6c80`/`a02e1f5` era). PHASE4-N-F-D (the live RELAY run-loop spine) closed at origin/main `7de1462` (S1 GREEN planner, S2 RED loop, S3a clean replay, S3b crash-recovery). PHASE4-N-F-E (the forge-tick wiring) closes now: S1 `214b0d3` (GREEN planner forge step), S2 `98b488a` (RED forge-tick wiring, self-accept-only), S3a `2484dd1` (forge-tick replay-equivalence), S3b `cd2484f` (single-epoch / KES fail-closed containment). An in-range commit `ffa76fc` repaired the N-F-D `node_sync` test suite + the `T-REC-03` `code_locus` (test-only / doc-only). The refresh is **scoped to the N-F-D/N-F-E `ade_node` surface + the mechanical counts that drifted**; every BLUE crate entry and the N-Q/N-R/N-S/N-W/N-X/N-Y/N-Z/N-F-A/N-F-C module entries are carried forward unchanged.
- **No BLUE crate was modified** by N-F-D or N-F-E (verified by recomputing every BLUE crate's `(pub )?struct|enum` count — all unchanged at `cd2484f`: `ade_codec` 11, `ade_types` 81, `ade_crypto` 21, `ade_core` 49, `ade_ledger` 177, `ade_plutus` 8; `ade_network` BLUE submodules 109). All N-F-D/N-F-E code is in the RED binary/driver `ade_node`: GREEN-by-content `run_loop_planner` (the pure planner + the `ForgeTick`/`ForgeSlotStatus`/`forge_slot_status` forge step), RED `node_lifecycle` (`run_relay_loop` + `ForgeActivation` + the `ForgeTick` branch), and RED `node_sync` (production unchanged in N-F-E; `#[cfg(test)]`-only additions). It uses `BTreeMap` (never `HashMap`), sorted keys, and no clock/rand/float in production paths (the sole `SlotNo` observation is the pure `forge_slot_status` guard; the sole wall-clock read is the existing RED clock seam).
- **Cluster-doc location:** the N-F-E cluster + S1/S2/S3a/S3b slice docs live under `docs/clusters/PHASE4-N-F-E/`; on close they archive under `docs/clusters/completed/`. All prior closed cluster docs are already under `docs/clusters/completed/`.
- Mechanical counts recomputed fresh from the tree at `cd2484f` (not copied): **11 crates** (Δ 0), **456 canonical types** (**Δ 0** — no BLUE crate touched; all N-F-D/N-F-E types live in RED `ade_node` and are not canonical-counted), **2169 tests** (**Δ +25** vs the prior on-disk doc's 2144 — the N-F-D/N-F-E surface: `run_loop_planner.rs` 11, plus the `node_lifecycle.rs`/`node_sync.rs` forge/replay/fail-closed tests + the `ffa76fc` N-F-D test-suite repair), **110 CI checks** (**Δ +2**: the two N-F-D run-loop gates `ci_check_loop_planner_closed.sh` + `ci_check_node_run_loop_containment.sh`, which the prior CODEMAP predated; N-F-E added NO new gate — it extended both in place), **310 registry rules** (Δ from the prior doc's 303-committed / 306-staged narrative: the N-F-C three rules + the four N-F-D rules landed, so HEAD now reads 310).
- **Boundary honesty (load-bearing, verbatim).** PHASE4-N-F-E is a **hermetic, single-epoch, self-accept-only** forge-tick wiring cluster. There is NO operator-key ingestion in `--mode node`; NO serve/broadcast/gossip of a forged block; NO durable apply / tip mutation by the forge; NO live peer / BA-02 / RO-LIVE claim. The forge handoff is reached by the `--mode node` loop only when a `ForgeActivation` is supplied (hermetic/test-only this cluster); the binary path passes `None` and reproduces the exact N-F-D relay behavior. The live unbounded-peer leg remains the RO-LIVE-01 follow-on. BA-02 is satisfied nowhere.
- **GAP — registry working-tree diff not yet committed.** `docs/ade-invariant-registry.toml` is `M` in `git status`. The committed-vs-worktree `id =` count is identical (310 = 310), so the header count is stable. The *diff* is the N-F-E close edits: the `DC-NODE-05` `declared → enforced` flip (tests + `ci_script` populated) and six `strengthened_in += "PHASE4-N-F-E"` bumps (CN-NODE-02, DC-SYNC-02, T-REC-03, DC-NODE-03, CN-PROD-02, DC-CINPUT-02b). They are owed alongside this CODEMAP at the close commit. (`.idd-config.json` `_invariant_registry_doc` still reads "309 entries"; `_head_deltas_baseline` is `51c9fbf` (N-F-C era) — bump on the N-F-E HEAD_DELTAS refresh.)
- TCB color for every new/changed module verified against the on-disk `// Core Contract:` / `//!` banner: `run_loop_planner.rs` opens `//! GREEN loop planner` → **GREEN** (pure, content-blind, closed-vocabulary; promotable to BLUE, never demotable); `node_lifecycle.rs` opens `//! RED \`--mode node\` Ade node lifecycle owner` → **RED** (owns the persistent stores, drives bootstrap/recovery + the relay loop, reads the wall-clock clock seam on the forge tick); `node_sync.rs` opens `//! RED` → **RED**; `ba02_evidence.rs` opens `//! GREEN BA-02 peer-acceptance evidence` → **GREEN**. Carried-forward colors unchanged.
- The doc is regenerated, not edited. If a value drifts, fix the source, not the doc.
