# Module Authority Map — Ade

> **Status:** Living architectural document. Regenerated; not hand-edited.
> Per-project instance of `~/.claude/methodology/templates/codemap.md`.

> 11 crates, 424 canonical types, 1672 tests, 61 CI checks at HEAD (`1946573` + PHASE4-N-K worktree).

---

## Conventions

- A **module** in Ade is a Cargo workspace crate (smallest independently-buildable unit). One exception: `ade_network` is split by *submodule color* — its BLUE submodules and its RED submodules are documented as two separate entries below, because `.idd-config.json` `core_paths` resolves BLUE at the submodule path level rather than crate-wide. The `ade_core::consensus` submodule sits *inside* the BLUE `ade_core` crate and is covered by that entry. The `ade_ledger::block_validity` / `ade_ledger::consensus_view` / `ade_ledger::tx_validity` / `ade_ledger::mempool::admit` / `ade_ledger::mempool::ingress` / `ade_ledger::cert_classify` modules sit inside the BLUE `ade_ledger` crate and are covered by that entry; the `ade_ledger::producer` sub-tree (PHASE4-N-C — `forge`, `self_accept`, `state`; PHASE4-N-G — `served_chain`), the `ade_ledger::block_body_hash` top-level module (PHASE4-N-C S4 single body-hash authority), the `ade_ledger::receive` sub-tree (PHASE4-N-H), the `ade_ledger::rollback` sub-tree (PHASE4-N-I — `traits`, `error`, `materialize`, `commit`), and the `ade_ledger::snapshot` sub-tree (PHASE4-N-J — `error`, `chain_dep`, `utxo_state`, `cert_state`, `epoch_state`, `gov_state`, `ledger`, `framing`) are likewise BLUE under the already-BLUE `ade_ledger` crate prefix. The RED `ade_ledger::consensus_input_extract`, the GREEN `ade_ledger::mempool::policy`, and the GREEN `ade_ledger::mempool::canonicalize` sit inside the BLUE `ade_ledger` crate but carry a different color by their own module doc-comment and the cluster TCB Color Maps — they are surfaced as sub-classification notes inside the `ade_ledger` entry. The `ade_testkit::mempool` sub-tree (PHASE4-N-E S2), the `ade_testkit::governance` sub-tree (PROPOSAL-PROCEDURES-DECODE PP-S2), and the `ade_testkit::producer` sub-tree (PHASE4-N-C) sit inside the GREEN `ade_testkit` crate, classified GREEN by their own module doc-comments and the cluster TCB Color Maps. **PHASE4-N-G — `ade_runtime::network` sub-tree** sits inside the RED `ade_runtime` crate and hosts the per-peer N2N **server** session driver (RED). **PHASE4-N-H — `ade_runtime::receive` sub-tree** sits inside the RED `ade_runtime` crate and hosts the GREEN+RED receive-side glue: `events_to_state` (GREEN), `in_memory_chain_write` (GREEN), `orchestrator` (RED). **PHASE4-N-I — `ade_runtime::rollback` sub-tree** sits inside the RED `ade_runtime` crate and hosts the GREEN+RED rollback adapter glue: `cadence` (GREEN), `in_memory_cache` (GREEN), `chaindb_block_source` (GREEN), `snapshot_writer` (RED). **PHASE4-N-J extends `ade_runtime::rollback` with `persistent_cache` (GREEN; closes DC-CONS-21).** **NEW — PHASE4-N-K extends `ade_runtime::rollback` with `persistent_writer` (GREEN; cadence-fidelity glue calling `PersistentSnapshotCache::capture` — DC-NODE-02), introduces the new top-level files `ade_runtime::bootstrap` (GREEN; sole `pub fn bootstrap_initial_state` — CN-NODE-01) and `ade_runtime::clock` (GREEN trait + GREEN `DeterministicClock` + the RED `SystemClock` sub-classified inside the same file — DC-NODE-03), and a new sub-tree `ade_runtime::orchestrator` (mixed) hosting the GREEN core reducer `core` + GREEN closed-vocabulary `event` + GREEN `state` + barrel `mod`, alongside the RED tokio runners `peer_session` + `leadership_session` + `n2n_server_pump`. **NEW — PHASE4-N-K reshapes `ade_node` from a hello-world stub into a lib+bin**: `src/lib.rs` (RED library entry; re-exports `Cli`/`CliError`/`run_node_until_shutdown`/`NodeStartupInputs`/`NodeShutdownEvidence`/`NodeRunError` + exit-code constants), `src/cli.rs` (RED argv parser), `src/node.rs` (RED `run_node_until_shutdown` lifecycle), and the refactored `src/main.rs` (RED bin shim).**
- Modules are listed by TCB color (BLUE → GREEN → RED), alphabetical within each color.
- TCB color sources, in order of authority:
  1. `.idd-config.json` `core_paths` — substring match against absolute path. BLUE matches: `ade_codec`, `ade_types`, `ade_crypto`, `ade_core`, `ade_ledger` (covers `ade_ledger::{snapshot, rollback, receive, producer, block_validity, tx_validity, mempool, ...}`), `ade_plutus`, and the 9 `ade_network` submodule paths (`mux/frame.rs`, `codec/`, `handshake/`, `chain_sync/`, `block_fetch/`, `tx_submission/`, `keep_alive/`, `peer_sharing/`, `n2c/`).
  2. `.idd-config.json` `_core_paths_doc` — `ade_runtime` is RED; `ade_testkit` is GREEN; `ade_node` is RED; `ade_network::mux::transport` and `ade_network::session` are RED; `ade_network::lib` and `ade_network::mux::mod` are GREEN barrels. `ade_core_interop` is **RED** per its `Cargo.toml` header comment and the PHASE4-N-B TCB Color Map; the `ade_core_interop::tx_submission` (S4) and `ade_core_interop::local_tx_submission` (S5) modules carry a GREEN sub-classification by their own module doc-comments. The RED binaries `ade_core_interop::bin::{live_tx_submission_session, live_block_production_session, live_block_fetch_session, live_block_follow_session}` are the operator-action live evidence pattern.
  3. `docs/clusters/completed/PHASE4-N-B/cluster.md` § "TCB Color Map" — `era_schedule`, `praos_state`, `vrf_cert`, `nonce`, `op_cert`, `leader_schedule`, `header_validate`, `fork_choice`, `rollback` (consensus-side header rollback applier) are BLUE; `chain_selector`, `candidate_fragment` are GREEN; `genesis_parser` is RED; `ade_testkit::consensus` is GREEN; `ade_core_interop` is RED.
  4. PHASE4-B1 — `ade_ledger::consensus_view`, `ade_ledger::block_validity` BLUE; `ade_core::consensus::{header_validate, kes_check}` extensions BLUE; `ade_testkit::validity` GREEN; `ade_ledger::consensus_input_extract` RED.
  5. PHASE4-B2 — `ade_ledger::tx_validity::*`, `ade_ledger::mempool::admit` BLUE; `ade_ledger::mempool::policy` GREEN (Tier-5); `ade_testkit::tx_validity` GREEN.
  6. PHASE4-B3/B5 — all new/changed modules BLUE under already-BLUE crate prefixes.
  7. PHASE4-N-E — `ade_ledger::mempool::ingress` BLUE (S1); `ade_ledger::mempool::canonicalize` GREEN (S3); `ade_testkit::mempool::ingress_replay` GREEN (S2); `ade_core_interop::tx_submission` GREEN (S4); `ade_core_interop::local_tx_submission` GREEN (S5); `ade_core_interop::bin::live_tx_submission_session` RED (S6).
  8. PROPOSAL-PROCEDURES-DECODE — `ade_codec::conway::governance` BLUE; new `ProposalProcedure` in `ade_types::conway::governance` BLUE; `ade_testkit::governance::proposal_procedures_replay` GREEN.
  9. PHASE4-N-C — `ade_ledger::producer::{forge, self_accept, state}` BLUE; `ade_ledger::block_body_hash` BLUE; `ade_codec::shelley::{opcert, tx_components}` BLUE; `ade_core::consensus::opcert_validate` BLUE; `ade_crypto::kes::KesSignature` BLUE; `ade_runtime::producer::{signing, keys, scheduler, broadcast}` RED; `ade_runtime::producer::tick_assembler` GREEN; `ade_testkit::producer::*` GREEN; `ade_core_interop::bin::live_block_production_session` RED.
  10. PHASE4-N-G — `ade_network::chain_sync::server`, `ade_network::block_fetch::server` BLUE; `ade_ledger::producer::served_chain` BLUE; `accepted_block_header_bytes` accessor BLUE; `ade_runtime::producer::{broadcast_to_served, served_chain_lookups}` GREEN; `ade_runtime::network::n2n_server` RED; `ade_core_interop::bin::live_block_fetch_session` RED.
  11. PHASE4-N-H — `ade_ledger::receive::{admitted, chain_write, events, pending_header_cache, reducer}` BLUE; `ade_runtime::receive::{events_to_state, in_memory_chain_write}` GREEN; `ade_runtime::receive::orchestrator` RED; `ade_core_interop::bin::live_block_follow_session` RED.
  12. PHASE4-N-I — `ade_ledger::rollback::{traits, error, materialize, commit}` BLUE; `ChainDbWrite::rollback_to_slot` extension BLUE; the `RollbackContext<'a>` + reducer extensions BLUE; `ade_runtime::rollback::{cadence, in_memory_cache, chaindb_block_source}` GREEN; `ade_runtime::rollback::snapshot_writer` RED.
  13. PHASE4-N-J — `ade_ledger::snapshot::{error, chain_dep, utxo_state, cert_state, epoch_state, gov_state, ledger, framing}` BLUE; `ade_runtime::rollback::persistent_cache` GREEN.
  14. **NEW — `docs/clusters/completed/PHASE4-N-K/cluster.md` § "TCB color map (FC/IS partition)"** — `ade_runtime::bootstrap` is **GREEN** (single `pub fn bootstrap_initial_state` composing `materialize_rolled_back_state` + `PersistentSnapshotCache::nearest_le`; closure gated by `ci/ci_check_bootstrap_closure.sh`, CN-NODE-01). `ade_runtime::clock` trait + `DeterministicClock` impl are **GREEN**; `ade_runtime::clock::SystemClock` (in the same file) is sub-classified **RED** (sole `SystemTime::now()` site in `ade_runtime`, gated by `ci/ci_check_clock_seam.sh`, DC-NODE-03). `ade_runtime::orchestrator::{mod, event, state, core}` are **GREEN** (closed event/effect/state vocabulary + pure `step` reducer; tokio-free; gated by `ci/ci_check_orchestrator_core_purity.sh`). `ade_runtime::rollback::persistent_writer` is **GREEN** (cadence-fidelity glue; gated by `ci/ci_check_persistent_writer_no_parallel_cadence.sh`, DC-NODE-02). `ade_runtime::orchestrator::{peer_session, leadership_session, n2n_server_pump}` are **RED** (tokio per-peer task + slot-tick pump + listening-socket spawner; peer-session structural isolation gated by `ci/ci_check_peer_session_isolation.sh`, DC-NODE-01). `ade_node::{cli, node, lib, main}` are **RED** (binary lifecycle + CLI parser + signal handler; single-bootstrap-call gated by `ci/ci_check_node_binary_uses_single_bootstrap.sh`). **No new BLUE in this cluster.**

- **Active cluster at HEAD (none).** PHASE4-N-K (orchestrator + `ade_node` binary) is **closed at HEAD `1946573` + PHASE4-N-K worktree** and archived under `docs/clusters/completed/PHASE4-N-K/` (closure record at `docs/clusters/completed/PHASE4-N-K/CLOSURE.md`). The cluster ships the single bootstrap authority, the GREEN orchestrator core reducer over a closed `OrchestratorEvent`/`OrchestratorEffect` vocabulary, the cadence-disciplined persistent-snapshot writer, the RED tokio runners (per-peer session + leadership session + listening-socket pump), and the `ade_node` lib+bin lifecycle with deterministic authority-fatal exit-code mapping. New registry rules at `status = "enforced"`: `CN-NODE-01` (single `pub fn bootstrap_initial_state` authority), `DC-NODE-01` (per-peer session structural isolation), `DC-NODE-02` (persistent writer routes cadence through `should_snapshot_after_block` exclusively — no parallel cadence policy), `DC-NODE-03` (clock seam — `SystemClock` is the sole wall-clock-reading site, `DeterministicClock` drives replay), `DC-NODE-04` (authority-fatal exit-code mapping + force-capture-on-shutdown for resume identity). Existing rules strengthened (`strengthened_in += "PHASE4-N-K"`): `T-DET-01`, `CN-CONS-08`, `CN-STORE-07`, `CN-STORE-08`, `DC-CONS-21`, `DC-STORE-08`. `DC-STORE-09` gains `open_obligation = "snapshot_schema_migration_follow_on_cluster"` (v1→v2 schema migration tooling is a snapshot-format lifecycle concern, NOT a node-binary concern — DC-NODE-04 deliberately does not carry this obligation). Existing operator-action `open_obligation`s carried unchanged: `CN-CONS-06` (live producer stake; via N-C), `RO-LIVE-01` (live block-fetch evidence; via N-G), `RO-LIVE-02` (live cross-impl follow evidence; via N-H) — closing these is a separate operator-action cluster that runs `ade_node` against a private cardano-node peer. The pre-existing `ade_network::session` placeholder gap is untouched. `docs/clusters/completed/` now contains 20 directories (the 19 prior closures plus PHASE4-N-K).

- **Delta since prior CODEMAP HEAD `f15102f` (this regeneration — PHASE4-N-K cluster, 8 slices S1–S8).**

  **Structural deltas summarized:**
  - **NEW GREEN top-level file `ade_runtime/src/bootstrap.rs`** (429 LOC, S1) — `bootstrap_initial_state` is the SOLE `pub fn` in the workspace returning the initial `(LedgerState, PraosChainDepState, Option<ChainTip>)` triple at node startup. Cold-start (genesis-only) and warm-start (snapshot-resume + replay-forward to chaindb tip) are two branches of one function — never parallel paths. Cold-start returns the `(genesis_ledger, genesis_chain_dep, None)` seed unchanged; warm-start composes `PersistentSnapshotCache::nearest_le(target_slot)` + `materialize_rolled_back_state(snapshot, target, …)` to produce the resumed `(ledger, chain_dep)` plus the original `ChainTip`. Closed `BootstrapError` sum with four variants `ChainDb(ChainDbError)` / `SnapshotMissing { chain_tip_slot: SlotNo }` / `Materialize(MaterializeError)` / `GenesisRequiredButAbsent`. `BootstrapInputs<'a, D: ChainDb, S: SnapshotStore + ?Sized>` borrows the storage backends, the `EraSchedule`, the `&dyn LedgerView`, and the optional genesis seed.
  - **NEW GREEN top-level file `ade_runtime/src/clock.rs`** (223 LOC, S1) — `Clock` trait (`now_millis(&self) -> u64`, `next_tick(&mut self) -> Option<u64>`); `DeterministicClock { anchor_millis, ticks: Vec<u64>, cursor: usize }` impl `Clock` (pure replay-driver); `SystemClock { slot_length_ms, next_boundary_millis }` impl `Clock` (RED sub-classified — the SOLE site of `SystemTime::now()` / `std::thread::sleep` in `ade_runtime`); pure helper `millis_to_slot(tick_millis, start_millis, start_slot, slot_length_ms) -> SlotNo`. Six inline tests cover deterministic-clock purity + ordering + anchor-before-first-tick + the three `millis_to_slot` arithmetic shapes (anchored, pre-anchor saturation, zero slot-length degenerate).
  - **NEW mixed sub-tree `ade_runtime/src/orchestrator/`** (7 files, ~1646 LOC). GREEN core: `mod.rs` (28 LOC; barrel — `pub mod {core, event, state, peer_session, leadership_session, n2n_server_pump}`; `pub use` re-exports `core::step`, `event::{AuthorityFatalKind, OrchestratorEffect, OrchestratorError, OrchestratorEvent, PeerHaltReason, PeerId, PeerRole}`, `state::{OrchestratorState, PerPeerReceiveVersions}`). `event.rs` (130 LOC, S2 — `PeerId(pub u64)`, closed 8-variant `OrchestratorEvent` sum {`SlotTick`/`PeerConnected`/`PeerDisconnected`/`PeerChainSyncFrame`/`PeerBlockFetchFrame`/`PeerN2nServerChainSyncFrame`/`PeerN2nServerBlockFetchFrame`/`Shutdown`}, 2-variant `PeerRole` sum {`UpstreamClient`/`DownstreamServer`}, closed 5-variant `OrchestratorEffect` sum {`SendToPeer`/`AdmittedBlock`/`CaptureSnapshot`/`PeerSessionHalted`/`ShutdownAcknowledged`}, closed 10-variant `PeerHaltReason` sum (no `String` anywhere — every per-peer-fatal kind maps to a known discriminant), closed 3-variant `AuthorityFatalKind` sum {`ChainWriteIo`/`SnapshotDecodeUnknownVersion`/`SnapshotDecodeFingerprintMismatch`}, single-variant `OrchestratorError::AuthorityFatal(AuthorityFatalKind)`). `state.rs` (88 LOC, S2 — `PerPeerReceiveVersions { chain_sync_version: ChainSyncVersion, block_fetch_version: BlockFetchVersion }`; `OrchestratorState { receive_state: ReceiveState, per_peer_receive: BTreeMap<PeerId, PerPeerReceiveVersions>, per_peer_server: BTreeMap<PeerId, PerPeerN2nServerState>, cadence: SnapshotCadence, last_persistent_snapshot_slot: Option<SlotNo>, last_observed_slot: Option<SlotNo>, shutdown_requested: bool }` — BTreeMap-keyed per-peer maps for deterministic iteration). `core.rs` (791 LOC, S2 — pure `pub fn step<W: ChainDbWrite>(state, event, chain_write, served_snapshot, era_schedule, ledger_view) -> Result<Vec<OrchestratorEffect>, OrchestratorError>` composing the N-H `dispatch_chain_sync_inbound` / `dispatch_block_fetch_inbound` receive-side authorities, the N-G `dispatch_chain_sync_frame` / `dispatch_block_fetch_frame` server-side authorities, and `should_snapshot_after_block`; per-peer-fatal errors fold into `PeerSessionHalted` effects; authority-fatal kinds surface as `OrchestratorError::AuthorityFatal(_)` to halt the binary). RED runners (3 files): `peer_session.rs` (230 LOC, S4 — `PeerInboundFrame { kind: PeerInboundFrameKind, bytes: Vec<u8> }` with closed 4-variant kind sum + `PeerSession` tokio task owning its own `mpsc::Receiver<PeerInboundFrame>`, forwarding frames to the orchestrator inbox via `events_out.send()`; per-peer structural isolation — one task per connected peer, no shared mutable state, no fanout spawn), `leadership_session.rs` (178 LOC, S5 — `SlotEraAnchor { start_slot: SlotNo, start_millis: u64, slot_length_ms: u32 }` + `LeadershipSession<C: Clock>` tokio task pulling `clock.next_tick()` and emitting `OrchestratorEvent::SlotTick { slot_millis, slot }` per tick), `n2n_server_pump.rs` (191 LOC, S6 — `PeerIdGenerator` (atomic monotonic counter, deterministic per-id allocation at the value level) + accept loop over a bound `TcpListener` that emits `PeerConnected { role: DownstreamServer, … }` and spawns one `PeerSession` per accepted connection).
  - **NEW GREEN file inside RED `ade_runtime::rollback/` sub-tree** `persistent_writer.rs` (245 LOC + inline tests, S3 — `PersistentSnapshotWriter<'a, S: SnapshotStore + ?Sized> { cache: PersistentSnapshotCache<'a, S>, cadence: SnapshotCadence, last_capture: Option<SlotNo> }`; method `on_admitted(slot, block_no, ledger, chain_dep) -> Result<bool, PersistentCacheError>` consults `should_snapshot_after_block` exclusively (DC-NODE-02 — `ci/ci_check_persistent_writer_no_parallel_cadence.sh` gates against any second cadence policy); method `force_capture(slot, ledger, chain_dep) -> Result<(), PersistentCacheError>` bypasses cadence for shutdown drain (DC-NODE-04 resume-identity). `rollback/mod.rs` extended with `pub mod persistent_writer;` + `pub use persistent_writer::PersistentSnapshotWriter;`.
  - **NEW RED lib+bin `ade_node`** (4 source files, refactored from a 1-file hello-world stub). `src/lib.rs` (23 LOC, S7 — library entry; `pub mod {cli, node}`; `pub use cli::{Cli, CliError}`; `pub use node::{NodeRunError, NodeShutdownEvidence, NodeStartupInputs, run_node_until_shutdown, EXIT_AUTHORITY_FATAL_DECODE, EXIT_AUTHORITY_FATAL_IO, EXIT_GENERIC_STARTUP}`). `src/cli.rs` (173 LOC, S7 — argv-style parser with no external clap dep; mandatory `--genesis-path PATH`; optional `--network NAME`/`--chain-db PATH`/`--snapshot-store PATH`/`--listen ADDR`/`--peer ADDR` (repeatable); closed 3-variant `CliError` sum). `src/node.rs` (254 LOC, S7 — `NodeStartupInputs<'a, D, S, C>` borrowing chaindb/snapshot_store/era_schedule/ledger_view/cadence/clock/anchor + optional genesis seed; closed 3-variant `NodeRunError` sum {`Bootstrap(BootstrapError)` / `AuthorityFatal(OrchestratorError)` / `PersistentWriterIo(PersistentCacheError)`} with `fn exit_code(&self) -> i32` mapping every variant to one of the three deterministic exit-code constants; `pub const EXIT_AUTHORITY_FATAL_IO: i32 = 10`, `pub const EXIT_AUTHORITY_FATAL_DECODE: i32 = 12`, `pub const EXIT_GENERIC_STARTUP: i32 = 1`; `NodeShutdownEvidence` struct returned on clean shutdown; `pub async fn run_node_until_shutdown` driving the full bootstrap → spawn-leadership-session → orchestrator inbox loop → drain → `PersistentSnapshotWriter::force_capture` → exit pipeline). `src/main.rs` (78 LOC, refactored — thin `fn main() -> ExitCode` wrapper around `Cli::parse_from` + an honest-scope readiness print; the actual N2N peer dialer + mux session driver above `MuxTransport::read_raw/write_raw` is operator-action work per RO-LIVE-01/RO-LIVE-02). The binary, when launched today, performs CLI parsing and prints the readiness line; bootstrap + orchestrator execution is exercised in-process by the integration tests below.
  - **NEW integration tests (4 files, 9 tests).** `crates/ade_runtime/tests/orchestrator_peer_isolation.rs` (329 LOC; 3 tests — DC-NODE-01: per-peer halt isolation under decode error, validity rejection, rollback-out-of-scope; sibling peers + slot-tick stream continue unaffected). `crates/ade_runtime/tests/orchestrator_replay_equivalence.rs` (234 LOC; 2 tests — DC-NODE-03: two `step` runs under `DeterministicClock` over the same `OrchestratorEvent` corpus produce byte-identical effect vectors and byte-identical `(LedgerFingerprint.combined, PraosChainDepState, ChainDb tip)`; also indirectly exercises T-DET-01 replay equivalence across the orchestrator core). `crates/ade_node/tests/shutdown_resume_identity.rs` (309 LOC; 3 tests — DC-NODE-04: `bootstrap → drive events → shutdown_drain → bootstrap` returns byte-identical state to the snapshot captured during shutdown, exercised end-to-end against a Conway 576 corpus block). `crates/ade_node/tests/authority_fatal_decode.rs` (123 LOC; 1 test — DC-NODE-04 fail-fast: `SnapshotDecodeError::UnknownVersion` / `FingerprintMismatch` at bootstrap exits non-zero deterministically, mapped to `EXIT_AUTHORITY_FATAL_DECODE = 12`).
  - **NEW closed canonical types in `ade_runtime` (none counted toward the BLUE total).** `PeerId`, `PeerRole`, `OrchestratorEvent`, `OrchestratorEffect`, `PeerHaltReason`, `AuthorityFatalKind`, `OrchestratorError`, `OrchestratorState`, `PerPeerReceiveVersions`, `PeerInboundFrame`, `PeerInboundFrameKind`, `PeerSession`, `PeerIdGenerator`, `LeadershipSession<C>`, `SlotEraAnchor`, `PersistentSnapshotWriter<'a, S>`, `BootstrapInputs<'a, D, S>`, `BootstrapError`, `Clock` (trait), `DeterministicClock`, `SystemClock`. In `ade_node`: `Cli`, `CliError`, `NodeStartupInputs<'a, D, S, C>`, `NodeShutdownEvidence`, `NodeRunError`. **All inside the RED `ade_runtime` + `ade_node` crate prefixes — not counted in the BLUE canonical-type total. BLUE canonical type count unchanged at 424.**
  - **NEW CI gates (+6):** `ci/ci_check_bootstrap_closure.sh` (S1; CN-NODE-01 — exactly one `pub fn` returning the initial triple in `bootstrap.rs`; forbids HashMap/wall-clock/tokio/rand in the bootstrap source; positive grep for `materialize_rolled_back_state` call site), `ci/ci_check_clock_seam.sh` (S1; DC-NODE-03 — `clock.rs` is the SOLE `SystemTime::now()` / `Instant::now()` site in `ade_runtime`; orchestrator core files contain none of `SystemTime`/`Instant`/`tokio::time::*`/`tokio::spawn`/`rand::*`/`HashMap`/`HashSet`/`f32`/`f64`), `ci/ci_check_orchestrator_core_purity.sh` (S2; DC-NODE-03 + general purity — the 4 GREEN orchestrator core files MUST NOT contain any RED construct), `ci/ci_check_persistent_writer_no_parallel_cadence.sh` (S3; DC-NODE-02 — no second `pub fn should_snapshot_after_block` anywhere; no second `SnapshotCadence`-like struct; no hardcoded "snapshot every N" constant in any writer file), `ci/ci_check_peer_session_isolation.sh` (S4; DC-NODE-01 — `peer_session.rs` owns its own `mpsc::Receiver`, never imports `ade_ledger::receive::*` / `ade_network::codec::*` directly, never spawns additional tasks), `ci/ci_check_node_binary_uses_single_bootstrap.sh` (S7; CN-NODE-01 + DC-NODE-04 — `ade_node` calls `bootstrap_initial_state` exactly once across the production body of `crates/ade_node/src/*.rs`). **CI script count: 55 → 61 (+6).**
  - **NEW registry rules / strengthenings:** 5 new entries — `CN-NODE-01` (enforced, single `pub fn bootstrap_initial_state` authority), `DC-NODE-01` (enforced, per-peer session structural isolation), `DC-NODE-02` (enforced, persistent writer no parallel cadence), `DC-NODE-03` (enforced, clock seam — `SystemClock` sole wall-clock site, `DeterministicClock` drives replay), `DC-NODE-04` (enforced, authority-fatal exit-code mapping + force-capture-on-shutdown). 6 existing rules strengthened: `T-DET-01`, `CN-CONS-08`, `CN-STORE-07`, `CN-STORE-08`, `DC-CONS-21`, `DC-STORE-08` all gain `PHASE4-N-K` in `strengthened_in`. `DC-STORE-09` gains `open_obligation += "snapshot_schema_migration_follow_on_cluster"` (schema-migration tooling stays on DC-STORE-09; DC-NODE-04 deliberately does not carry it). Registry count: 209 → 214.
  - **NEW test inventory (+42).** Per-crate at HEAD: `ade_codec` 162 (unchanged), `ade_types` 23 (unchanged), `ade_crypto` 51 (unchanged), `ade_core` 126 (unchanged), `ade_ledger` 574 (unchanged), `ade_plutus` 28 (unchanged), `ade_testkit` 312 (unchanged), `ade_runtime` 127 → **164 (+37)** (bootstrap inline 6 + clock inline 6 + orchestrator/core inline 12 + orchestrator/event inline 0 + orchestrator/state inline 0 + persistent_writer inline 5 + peer_session inline 0 + leadership_session inline 3 + n2n_server_pump inline 0 + integration tests orchestrator_peer_isolation 3 + orchestrator_replay_equivalence 2 = 37), `ade_network` 200 (unchanged), `ade_node` 0 → **5 (+5)** (shutdown_resume_identity 3 + authority_fatal_decode 1 + unit tests in cli.rs/node.rs 1 = 5; 4 of these in tests/ as integration), `ade_core_interop` 27 (unchanged). **Test inventory 1630 → 1672 (+42)**, reported approximate per the template's fallback rule (counts from `grep -cE "#\[test\]|#\[tokio::test\]"` workspace-wide).
  - **NEW crate-level outbound edges in `ade_node`** — the crate was a 1-file hello-world stub at the prior HEAD; PHASE4-N-K adds explicit `Cargo.toml` dependencies on `ade_types`, `ade_core`, `ade_ledger`, `ade_runtime`, `ade_network`, `ade_codec`, plus `tokio = { version = "1", features = ["net", "rt", "rt-multi-thread", "io-util", "macros", "time", "sync", "signal"] }`. Dev-deps gain `ade_testkit` + `tempfile`. **NEW crate-level outbound edge in `ade_runtime`** — explicit `tokio` dep gains `signal` to the existing feature set (the existing N-G/N-H tokio usage already pulled `net`/`rt`/`rt-multi-thread`/`io-util`/`macros`/`time`/`sync`). The GREEN orchestrator core (`core.rs`, `event.rs`, `state.rs`, `mod.rs`) MUST NOT import `tokio::*` — enforced by `ci/ci_check_orchestrator_core_purity.sh` + `ci/ci_check_clock_seam.sh`.

- Counts:
  - Crates: 11, from `Cargo.toml` `[workspace] members`. Unchanged.
  - Canonical types: 424, from `grep -rE "^(pub )?(struct|enum) "` across the full BLUE scope. Breakdown: `ade_codec` 10, `ade_types` 81, `ade_crypto` 13, `ade_core` 44, `ade_ledger` 159, `ade_plutus` 8, plus the 9 BLUE `ade_network` submodule paths 109. Registry `canonical_type_registry: null`, so a structural count is used. **No change since `f15102f`** — PHASE4-N-K added no BLUE canonical types; the new ~20 closed sums/structs all live inside `ade_runtime` (RED prefix) or `ade_node` (RED prefix).
  - Tests: 1672 — count of `#[test]` / `#[tokio::test]` attributes across `crates/`. Reported as approximate per the template's fallback rule. **+42 since `f15102f`**, breakdown above.
  - CI checks: 61 — file count under `ci/ci_check_*.sh`. **+6 since `f15102f`** — `ci_check_bootstrap_closure.sh`, `ci_check_clock_seam.sh`, `ci_check_orchestrator_core_purity.sh`, `ci_check_persistent_writer_no_parallel_cadence.sh`, `ci_check_peer_session_isolation.sh`, `ci_check_node_binary_uses_single_bootstrap.sh` (all PHASE4-N-K). Full inventory at HEAD: `admitted_block_closure`, `block_fetch_server_closure`, **`bootstrap_closure`** *(NEW)*, `broadcast_to_served_purity`, `cbor_round_trip`, `ce_n_a_5_proof`, `chaindb_contract`, `chaindb_crash_safety`, `chain_sync_server_closure`, **`clock_seam`** *(NEW)*, `consensus_closed_enums`, `constitution_coverage`, `conway_cert_classification_closed`, `credential_discriminant_closed`, `crypto_vectors`, `dependency_boundary`, `deposit_param_authority`, `differential_divergence`, `forbidden_patterns`, `forge_purity`, `gov_cert_accumulation_closed`, `hash_uses_wire_bytes`, `hfc_translation`, `ingress_chokepoints`, `ledger_determinism`, `mempool_ingress_closure`, `mempool_ingress_replay`, `module_headers`, `n2n_server_no_signing_dep`, `no_async_in_blue`, `no_chaindb_in_consensus_blue`, **`node_binary_uses_single_bootstrap`** *(NEW)*, `no_density_in_fork_choice`, `no_float_in_consensus`, `no_parallel_header_splitter`, `no_private_keys_in_corpus`, `no_producer_body_encoder`, `no_secrets`, `no_semantic_cfg`, `no_signing_in_blue`, `opcert_closed`, **`orchestrator_core_purity`** *(NEW)*, `pallas_quarantine`, **`peer_session_isolation`** *(NEW)*, **`persistent_writer_no_parallel_cadence`** *(NEW)*, `private_key_custody`, `producer_corpus_present`, `proposal_procedures_closed`, `receive_orchestrator_no_producer_dep`, `receive_paths_corpus_present`, `receive_reducer_closure`, `receive_replay_purity`, `recovery_contract`, `ref_provenance`, `rollback_materialize_closure`, `scheduler_closure`, `self_accept_gate`, `served_chain_closure`, `server_paths_corpus_present`, `snapshot_cadence_purity`, `snapshot_encoder_closure`. The forward-looking `ci/ci_check_no_fail_open_in_validation.sh` (DC-VAL-06 / DC-TXV) is still **not** shipped. No `.github/workflows/` yet.

---

## BLUE Modules — Pure Functional Core

> **Shared header (applies to every BLUE entry below).** Every `.rs` source file begins with the contract banner
> `// Core Contract:` and the following deny attributes are present in each crate's `lib.rs` (or, for `ade_network`,
> at the crate root — the BLUE submodules inherit them):
> `#![deny(unsafe_code)]`, `#![deny(clippy::unwrap_used)]`, `#![deny(clippy::expect_used)]`,
> `#![deny(clippy::panic)]`, `#![deny(clippy::float_arithmetic)]`.
> CI scripts that enforce the shared rules (all 7 scope the full BLUE set — the 6 BLUE crates plus the 9 BLUE `ade_network` submodule paths declared in `.idd-config.json` `core_paths`):
> - `ci/ci_check_module_headers.sh` — banner first-line check.
> - `ci/ci_check_forbidden_patterns.sh` — `HashMap`, `HashSet`, `IndexMap`/`IndexSet`/`indexmap::`, `SystemTime`, `Instant`, `std::fs`, `std::net`, `tokio`, `async fn`, `f32`/`f64`, `anyhow`, `rand::thread_rng`, `thread::spawn`, plus `unsafe` outside a documented allowlist.
> - `ci/ci_check_dependency_boundary.sh` — no BLUE crate depends on a RED crate.
> - `ci/ci_check_no_signing_in_blue.sh` — `SigningKey`/`SecretKey`/`PrivateKey`/`private_key`/`sign_message`/`sign_block` forbidden in BLUE.
> - `ci/ci_check_no_semantic_cfg.sh` — `#[cfg(feature = ...)]` and `cfg!(feature = ...)` forbidden in BLUE.
> - `ci/ci_check_hash_uses_wire_bytes.sh` — no hashing of `canonical_bytes` / re-encoded bytes in BLUE.
> - `ci/ci_check_ingress_chokepoints.sh` — only named `decode_*` chokepoints construct `PreservedCbor`.
> - `ci/ci_check_pallas_quarantine.sh` — `pallas-*` references confined to `ade_plutus`.
> - `ci/ci_check_no_async_in_blue.sh` *(PHASE4-N-A, S-A1)* — async constructs forbidden anywhere in the BLUE scope. Enforces DC-CORE-01.
>
> Three additional CI scripts narrow the shared header to the `ade_core::consensus` tree (PHASE4-N-B): `ci/ci_check_no_chaindb_in_consensus_blue.sh`, `ci/ci_check_no_float_in_consensus.sh`, `ci/ci_check_consensus_closed_enums.sh` (TARGETS scope: `crates/ade_core/src/consensus/`, `crates/ade_ledger/src/block_validity/`, `crates/ade_ledger/src/tx_validity/`, `crates/ade_ledger/src/mempool/`).
> A fourth narrow check enforces a single fork-choice rule: `ci/ci_check_no_density_in_fork_choice.sh` (DC-CONS-03).
> A fifth narrow check (PHASE4-B3) enforces deposit-parameter authority: `ci/ci_check_deposit_param_authority.sh` (DC-TXV-07).
> A sixth narrow check (PHASE4-B5) enforces gov-cert accumulation closure: `ci/ci_check_gov_cert_accumulation_closed.sh` (DC-LEDGER-09).
> A seventh narrow check (OQ5/COMMITTEE/DREP/ENACTMENT-COMMITTEE-FIDELITY + ENACTMENT-COMMITTEE-WRITEBACK) enforces credential-discriminant closure: `ci/ci_check_credential_discriminant_closed.sh` (DC-LEDGER-10).
> An eighth narrow check (PROPOSAL-PROCEDURES-DECODE, PP-S1) enforces proposal_procedures closure: `ci/ci_check_proposal_procedures_closed.sh` (DC-LEDGER-11).
>
> **Eight narrow checks added by PHASE4-N-C (the producer-authority gate set):** `ci_check_forge_purity.sh` (DC-CONS-13/14/15); `ci_check_no_producer_body_encoder.sh` (DC-CONS-16); `ci_check_no_private_keys_in_corpus.sh` (DC-CRYPTO-03/04/05 corpus discipline); `ci_check_opcert_closed.sh` (DC-CONS-11/12); `ci_check_private_key_custody.sh` (DC-CRYPTO-03/04/05 + OP-OPS-04); `ci_check_producer_corpus_present.sh` (CN-CONS-06 mechanical half); `ci_check_scheduler_closure.sh` (DC-CONS-13 + DC-MEM-03 + OP-OPS-05); `ci_check_self_accept_gate.sh` (CN-CONS-07).
>
> **Seven narrow checks added by PHASE4-N-G (the producer-side server-role gate set):** `ci_check_no_parallel_header_splitter.sh`; `ci_check_served_chain_closure.sh`; `ci_check_chain_sync_server_closure.sh`; `ci_check_block_fetch_server_closure.sh`; `ci_check_broadcast_to_served_purity.sh`; `ci_check_n2n_server_no_signing_dep.sh`; `ci_check_server_paths_corpus_present.sh`.
>
> **Four narrow checks added by PHASE4-N-H (the receive-side authority gate set):** `ci_check_admitted_block_closure.sh`; `ci_check_receive_reducer_closure.sh`; `ci_check_receive_replay_purity.sh`; `ci_check_receive_orchestrator_no_producer_dep.sh`; `ci_check_receive_paths_corpus_present.sh`.
>
> **Two narrow checks added by PHASE4-N-I (the rollback authority gate set):** `ci_check_rollback_materialize_closure.sh` (CN-STORE-07 + DC-CONS-22); `ci_check_snapshot_cadence_purity.sh` (DC-STORE-07).
>
> **One narrow check added by PHASE4-N-J (the snapshot encoder single-authority gate):** `ci_check_snapshot_encoder_closure.sh` (CN-STORE-08 + DC-STORE-08 + DC-STORE-09).
>
> **NEW — Six narrow checks added by PHASE4-N-K (the node-orchestrator gate set; all scope `ade_runtime` GREEN files + `ade_node` source — none target BLUE crates):**
> - `ci/ci_check_bootstrap_closure.sh` (S1; CN-NODE-01) — `crates/ade_runtime/src/bootstrap.rs` production body: forbidden patterns scan (no HashMap/SystemTime/Instant/tokio/rand); exactly one `pub fn` returning the initial state triple; positive grep that the warm-start branch calls `materialize_rolled_back_state`.
> - `ci/ci_check_clock_seam.sh` (S1; DC-NODE-03) — `crates/ade_runtime/src/clock.rs` is the SOLE site of `SystemTime::now()` / `Instant::now()` within `ade_runtime`; orchestrator core files contain none of those constructs.
> - `ci/ci_check_orchestrator_core_purity.sh` (S2; DC-NODE-03 + general purity) — scopes the 4 GREEN orchestrator core files (`core.rs`, `event.rs`, `state.rs`, `mod.rs`); forbids `SystemTime`/`Instant`/`tokio::time::*`/`tokio::spawn`/`rand::*`/`HashMap`/`HashSet`/floats.
> - `ci/ci_check_persistent_writer_no_parallel_cadence.sh` (S3; DC-NODE-02) — no second `pub fn should_snapshot_after_block` anywhere; no second `SnapshotCadence`-like struct; no hardcoded "snapshot every N" constant in any writer file.
> - `ci/ci_check_peer_session_isolation.sh` (S4; DC-NODE-01) — `crates/ade_runtime/src/orchestrator/peer_session.rs` owns its own `mpsc::Receiver`; never imports `ade_ledger::receive::*` or `ade_network::codec::*` directly (flows through orchestrator events); never spawns additional tasks.
> - `ci/ci_check_node_binary_uses_single_bootstrap.sh` (S7; CN-NODE-01 + DC-NODE-04) — `ade_node` calls `bootstrap_initial_state` exactly once across its production body.
>
> Two checks narrow the shared header to the wire-ingress path of `ade_ledger::mempool` (PHASE4-N-E): `ci_check_mempool_ingress_closure.sh` (DC-MEM-03, S1); `ci_check_mempool_ingress_replay.sh` (DC-MEM-04, S2 + S3).
>
> A BLUE crate or BLUE `ade_network` submodule that adds a feature flag, an async function, a `HashMap`, or a RED dep fails CI on push.
> The 3 RED-scope CI scripts (`ci_check_chaindb_contract.sh`, `ci_check_recovery_contract.sh`, `ci_check_chaindb_crash_safety.sh`) and the 1 evidence script `ci_check_ce_n_a_5_proof.sh` are not part of this shared header — they are documented in the cross-module CI matrix at the bottom.

---

### `ade_codec`

> **Status carried from prior CODEMAP — unchanged at HEAD `1946573` + PHASE4-N-K worktree.** PHASE4-N-K did not touch `ade_codec`. The existing snapshot-encoder consumption sites carry forward unchanged.

| Attribute | Value |
|-----------|-------|
| **Purpose** | Owns Cardano-canonical CBOR ingress: the only place in the workspace that turns raw bytes into typed semantic values, with wire-byte preservation for every hash-bearing structure. Also owns the standalone opcert byte authority (`shelley::opcert::{encode_opcert, decode_opcert}`) and the canonical Conway-tx preserved-byte splitter (`shelley::tx_components::split_conway_tx_components`). |
| **Creates** | `PreservedCbor<T>`, `RawCbor`, `BlockEnvelope`, `ByronDecodedBlock`, `CodecContext`, `CodecError` (incl. `UnknownCertTag`, `DuplicateMapKey`, `TrailingBytes`, `InvalidCborStructure`), `ContainerEncoding`, `IntWidth`, plus era-tagged block/tx wrappers under `byron/`, `shelley/`, `allegra/`, `mary/`, `alonzo/`, `babbage/`, `conway/`. Functions: `conway::cert::{decode_conway_certs, decode_drep}`, `conway::withdrawals::{decode_withdrawals, withdrawals_sum}`, `conway::governance::{decode_proposal_procedures, encode_proposal_procedures}`, `shelley::cert::read_pool_registration_cert`, both era `decode_stake_credential`, `shelley::opcert::{encode_opcert, decode_opcert, write_opcert_fields_into}`, `shelley::tx_components::split_conway_tx_components`. Types: `OpCertCodecError`, `TxComponents<'a>`. |
| **Interprets** | All canonical Cardano CBOR — block envelopes, era-specific blocks, tx bodies, tx outs, certificates, addresses. Conway certificate array (closed over CDDL tags 0..18, owner-complete) and Conway withdrawals map (deduplicated). Both era credential decoders preserve the key/script tag. Sole authority for `PreservedCbor::new` (constructor is `pub(crate)`). CIP-1694 `proposal_procedure` array. Standalone cardano-cli `OperationalCertificate` 4-tuple. Conway transaction 4-tuple (preserved-byte slicing only). |
| **MUST NOT** | (1) Construct `PreservedCbor` outside `ade_codec` (`pub(crate)` + `ci_check_ingress_chokepoints.sh`). (2) Re-encode wire bytes when computing hashes (`ci_check_hash_uses_wire_bytes.sh`). (3) Use any forbidden BLUE pattern. (4) Depend on any other workspace crate except `ade_types`. (5) `conway::cert` (DC-LEDGER-08) — no unknown-tag swallow; owner-complete; no catch-all. (6) `conway::withdrawals` — no last-wins on duplicate `RewardAccount`. (7) `decode_stake_credential` (DC-LEDGER-10) — must not erase the credential tag. (8) `conway::governance` (DC-LEDGER-11) — no silent skip on unknown `GovAction`; no opaque pass-through at body codec key 20. (9) `shelley::opcert` (DC-CONS-11/12) — cardano-byte-identical 4-tuple; dedicated `OpCertCodecError` variant per shape failure; not a second header-embedded opcert encoder (enforced by `ci_check_opcert_closed.sh`). (10) `shelley::tx_components` (DC-CONS-13/16) — preserved-byte slices that alias the input buffer (no `to_vec` / clones); reject non-4-tuple shapes; not re-encode the boolean validity flag or the auxiliary-data null. |
| **Inbound deps** | `ade_ledger` (heavy — `ade_ledger::rollback::materialize` uses `cbor::envelope::decode_block_envelope` for replay-forward era detection; `ade_ledger::receive::reducer` reuses `block_validity::decode_block`; `ade_ledger::snapshot::*` uses `ade_codec::cbor::{canonical_width, read_*, write_*, ContainerEncoding, IntWidth, MAJOR_NEGATIVE}` + `ade_codec::CodecError` across all 8 sub-state encoder/decoder files), `ade_plutus`, `ade_testkit`, `ade_network`, `ade_runtime`, `ade_core_interop`. No new inbound crate edge in PHASE4-N-K. |
| **Outbound deps** | `ade_types`. No external dependencies; std-only. Dev-deps: `serde_json`, `toml`. |
| **Entry points** | `ade_codec::cbor::envelope::decode_block_envelope`, `ade_codec::cbor`, `ade_codec::traits::AdeEncode`, `ade_codec::CodecContext`, per-era `decode_*_block`, `ade_codec::address::decode_address`. B2: `ade_codec::conway::tx::decode_conway_tx_body`. B3: `ade_codec::conway::cert::decode_conway_certs` and `ade_codec::conway::withdrawals::{decode_withdrawals, withdrawals_sum}`. PP-S1: `ade_codec::conway::governance::{decode_proposal_procedures, encode_proposal_procedures}`. N-C: `ade_codec::shelley::opcert::{encode_opcert, decode_opcert, OpCertCodecError}`, `ade_codec::shelley::tx_components::{split_conway_tx_components, TxComponents}`. |
| **Key modules** | `cbor/`, `byron/`, `shelley/` (incl. `cert.rs`, `opcert.rs`, `tx_components.rs`), `allegra/`, `mary/`, `alonzo/`, `babbage/`, `conway/`, `address/`, `preserved.rs`, `traits.rs`, `primitives.rs`, `error.rs`. |

---

### `ade_core`

> **Status carried from prior CODEMAP — unchanged at HEAD `1946573` + PHASE4-N-K worktree.** PHASE4-N-K did not touch `ade_core`. `PraosChainDepState` is now passed through the new `ade_runtime::bootstrap` warm-start branch (the bootstrap reconstructs `(LedgerState, PraosChainDepState)` from a decoded snapshot then folds the chaindb tail forward via `materialize_rolled_back_state`) — no new consumption surface inside `ade_core` itself.

| Attribute | Value |
|-----------|-------|
| **Purpose** | BLUE authoritative Praos consensus core. Owns canonical types and pure state-transitions that decide which header / chain Ade accepts: HFC era schedule + slot↔era↔time translation, Praos chain-dep state (nonce / op-cert counters), VRF cert verification + leader-eligibility predicate, KES signature + op-cert period verification wired into header admission, header validation pipeline, fork-choice, header-level rollback authority, leader-schedule query, canonical encodings of all chain-dep state and chain events. Owns the producer-side opcert acceptance authority (`opcert_validate`). |
| **Creates** | **Schedule:** `BootstrapAnchorHash`, `EraSchedule`, `EraSummary`, `EraLocation`. **State:** `PraosChainDepState`, `OpCertCounterMap`, `Nonce`. **Events/points:** `Point`, `ChainHash`, `BlockDistance`, `SecurityParam`, `ChainEvent`, `ChainSelectionReject`. **Errors:** `HFCError`, `SlotTimeError`, `OutsideForecastRange`, `HeaderValidationError`, `FieldError`, `FieldKind`, `VrfCertError`, `OpCertCounterError`, `NonceEvolutionError`, `LeaderScheduleError`, `OpCertError`. **Header surface:** `HeaderInput`, `HeaderVrf`, `HeaderKes`, `ValidatedHeaderSummary`, `HeaderApplied`. **Fork-choice:** `TiebreakerView`, `CandidateFragment`, `ChainSelectorState`, `ForkChoiceError`. **Op-cert/nonce:** `OpCertObservation`, `NonceInput`. **VRF:** `VrfRole`, `VerifiedVrf`, `StakeFraction`, `ActiveSlotsCoeff`. **Leader schedule:** `LeaderScheduleQuery`, `LeaderScheduleAnswer`. **Ledger view boundary:** `LedgerView` trait. **Rollback (header-level):** `RollBackRequest`, `RollBackApplied`. **Encoding:** `DecodeError`. 44 public types. |
| **Interprets** | Canonical inputs from the `ade_runtime` shell — `HeaderInput` projections, `EraSchedule` materialized once from genesis JSON, `LedgerView` snapshots from `ade_ledger::consensus_view::PoolDistrView`, ordered `StreamInput` events. KES check verifies hot KES key signature over the header body CBOR bytes. `opcert_validate` consumes an `OperationalCert`, cold key, expected period, and prev counter. PHASE4-N-J consumer site carries forward: `ade_ledger::snapshot::chain_dep` serializes `PraosChainDepState` directly. **PHASE4-N-K consumer note:** `LedgerView` is held as `Arc<dyn LedgerView + Send + Sync>` in `ade_node::node::NodeStartupInputs` and passed through to the orchestrator `step` reducer — no new BLUE consumption pattern, only a new lifetime/sharing shape outside BLUE. |
| **MUST NOT** | (1) Take or accept a `&ChainDb` reference (`ci_check_no_chaindb_in_consensus_blue.sh`). (2) Use any `f32`/`f64` (`ci_check_no_float_in_consensus.sh`). (3) Add `#[non_exhaustive]` to any consensus enum, open-tail `Other`/`Unknown`, owned `String` fields, or `Box<dyn ...>` (`ci_check_consensus_closed_enums.sh`). (4) Reference `density` in `fork_choice.rs`/`candidate.rs` outside a `// no-density:` annotation. (5) Read wall-clock. (6) Use `HashMap`/`HashSet`; `OpCertCounterMap` uses `BTreeMap`. (7) `async fn`, `.await`, `tokio`. (8) Construct a `PreservedCbor`. (9) Re-derive stake snapshots (DC-CONSENSUS-02). (10) Bypass the canonical `validate_and_apply_header` pipeline. (11) B1 fail-closed (DC-VAL-06). (12) B1 KES rule (DC-CRYPTO-01). (13) All shared-header BLUE rules. (14) `consensus::opcert_validate` (DC-CONS-11/12) — `OpCertError` closed; sub-check order pinned; `prev_counter == None` only first-opcert acceptance path; no external counter store. |
| **Inbound deps** | `ade_ledger`, `ade_runtime` (heavy — N-K consumes `EraSchedule`/`LedgerView`/`PraosChainDepState` in `bootstrap` + `orchestrator::core::step` + `node::run_node_until_shutdown`), `ade_testkit`, `ade_core_interop`. No new inbound crate edge in PHASE4-N-K (the existing `ade_runtime → ade_core` edge already carries `PraosChainDepState` and `LedgerView`). |
| **Outbound deps** | `ade_types`, `ade_crypto`. Dev-deps: `ade_testkit`, `serde_json`, `cardano-crypto`. |
| **Entry points** | `use ade_core::consensus::{...}` aggregator, `ade_core::consensus::ledger_view::LedgerView`, `ade_core::consensus::vrf_cert::*`, `ade_core::consensus::kes_check::*`, `ade_core::consensus::praos_state::*`, `ade_core::consensus::header_summary::*`. Top-level transitions: `validate_and_apply_header`, `select_best_chain`, `apply_rollback` (header-level), `apply_nonce_input`, `apply_op_cert`, `query_leader_schedule`, `verify_vrf_cert`, `tiebreaker_prefer`, `encode/decode_chain_dep_state`, `encode/decode_chain_event`. `ade_core::consensus::opcert_validate::{opcert_validate, OpCertError}`. |
| **Key modules** | `consensus/era_schedule.rs`, `consensus/praos_state.rs`, `consensus/events.rs`, `consensus/errors.rs`, `consensus/vrf_cert.rs`, `consensus/kes_check.rs`, `consensus/nonce.rs`, `consensus/op_cert.rs`, `consensus/leader_schedule.rs`, `consensus/header_summary.rs` + `consensus/header_validate.rs`, `consensus/candidate.rs`, `consensus/fork_choice.rs`, `consensus/rollback.rs`, `consensus/ledger_view.rs`, `consensus/encoding.rs`, `consensus/opcert_validate.rs`. |

---

### `ade_crypto`

> **Status carried from prior CODEMAP — unchanged at HEAD `1946573` + PHASE4-N-K worktree.** PHASE4-N-K did not touch `ade_crypto`.

| Attribute | Value |
|-----------|-------|
| **Purpose** | Owns pure cryptographic verification primitives — Blake2b hashing, Ed25519 / Byron-bootstrap signature verification, KES verification with operational certificates, VRF verification, plus the closed signature-artifact types (`KesSignature`, `VrfProof`, `VrfOutput`, `Ed25519Signature`) that BLUE consumes across the RED→BLUE boundary. Verification only — signing lives in `ade_runtime::producer::signing`. |
| **Creates** | `Blake2b224`, `Blake2b256`, `HashAlgorithm` trait, `Ed25519VerificationKey`, `Ed25519Signature`, `ByronExtendedVerificationKey`, `KesVerificationKey`, `KesPeriod`, `OperationalCertData`, `VrfVerificationKey`, `VrfProof`, `VrfOutput`, `CryptoError`, `KesSignature(pub [u8; SUM6_KES_SIG_LEN])`, `SUM6_KES_SIG_LEN: usize = 448`. |
| **Interprets** | Verification key / signature / proof byte structures. Not a CBOR parser — accepts already-decoded byte slices. |
| **MUST NOT** | (1) Implement signing (`ci_check_no_signing_in_blue.sh`). (2) Allocate global state. (3) Use any BLUE forbidden pattern. (4) Use `unsafe` outside the allowlisted FFI in `src/vrf.rs`. (5) `build_opcert_signable` must produce the spec-correct raw concatenation. (6) `KesSignature` (DC-CRYPTO-04) — closed length-pinned wrapper; only `from_bytes` construction; custom redacting `Debug`; no `PartialOrd`/`Ord`/`Hash` derives. (7) No `Drop` impl that zeroizes — `KesSignature` is BLUE and not secret. |
| **Inbound deps** | `ade_core`, `ade_ledger`, `ade_plutus`, `ade_testkit`, `ade_core_interop`, `ade_runtime`. No new inbound crate edge in PHASE4-N-K. |
| **Outbound deps** | `ade_types`, `blake2`, `ed25519-dalek`, `cardano-crypto` (vrf-draft03 + kes-sum + dsign features, `default-features = false`). |
| **Entry points** | `ade_crypto::blake2b::*`, `verify_ed25519`, `verify_byron_bootstrap`, `verify_kes`, `verify_opcert`, `verify_vrf`, `ade_crypto::kes::{KesSignature, SUM6_KES_SIG_LEN}`. |

---

### `ade_ledger`

> **Status carried from prior CODEMAP — unchanged at HEAD `1946573` + PHASE4-N-K worktree.** PHASE4-N-K added no `ade_ledger` source. All BLUE authorities (`admit_via_block_validity` for CN-CONS-08, `materialize_rolled_back_state` for CN-STORE-07, `framing::{encode,decode}_snapshot` for CN-STORE-08, `mempool_ingress` for DC-MEM-03, the `producer::*` surface, the `receive::*` surface, the `rollback::*` surface, the `snapshot::*` surface) are now composed end-to-end by the new GREEN `ade_runtime::orchestrator::core::step` reducer + GREEN `ade_runtime::bootstrap::bootstrap_initial_state` + GREEN `ade_runtime::rollback::persistent_writer::PersistentSnapshotWriter` — strengthenings under `CN-CONS-08`, `CN-STORE-07`, `CN-STORE-08`, `DC-CONS-21`, `DC-STORE-08`, `T-DET-01` all gain `PHASE4-N-K` in `strengthened_in`. No new consumption pattern requires a `MUST NOT` change.

| Attribute | Value |
|-----------|-------|
| **Purpose** | The functional core (ledger half): stateless ledger rules for every Cardano era; the B1 top-level block-validity verdict; the B2 top-level transaction-validity verdict + mempool admission; the B3 full Conway value-conservation accounting; the B4 closed Conway cert-state accumulation; the B5 closed Conway governance-cert accumulation; the live committee-enactment write-back; the single BLUE chokepoint `mempool::ingress::mempool_ingress` from wire ingress into `admit`; the BLUE producer authority (`forge_block`, `self_accept`, `AcceptedBlock` broadcast token, `block_body_hash` recipe); the BLUE producer-side served-chain index and single canonical header-projection authority; the BLUE receive-side header→body bridge authority (`receive/`); the BLUE rollback authority (`rollback/`); the BLUE canonical snapshot encoder/decoder authority (`snapshot/`). |
| **Creates** | All carry-forward (159 BLUE canonical types). No additions in PHASE4-N-K. |
| **Interprets** | Carry-forward — all per-era validation composers, the B1/B2/B3/B4/B5 verdict paths, the mempool wire chokepoint, the producer / receive / rollback / snapshot authorities. **PHASE4-N-K consumption note:** the orchestrator's `step` reducer composes `admit_via_block_validity` via the `dispatch_*_inbound` thin wrappers (CN-CONS-08 strengthening); the bootstrap's warm-start branch composes `materialize_rolled_back_state` (CN-STORE-07 strengthening); the persistent writer composes `framing::encode_snapshot` indirectly via `PersistentSnapshotCache::capture` (CN-STORE-08 strengthening) — none of these introduces a new BLUE entry point or weakens an existing MUST NOT. |
| **MUST NOT** | All carry-forward (1)–(50). No PHASE4-N-K additions. |
| **Inbound deps** | `ade_testkit`, `ade_core_interop`, `ade_runtime`. PHASE4-N-K **adds a new inbound edge from `ade_node`**: `ade_node`'s `node.rs` imports `ade_ledger::producer::ServedChainSnapshot`, `ade_ledger::receive::ReceiveState`, `ade_ledger::rollback::MaterializeError`, `ade_ledger::snapshot::error::SnapshotDecodeError`, `ade_ledger::state::LedgerState`. The orchestrator's `step` reducer imports the receive-side dispatch surface + `ChainDbWrite` + `ReceiveEffect`/`ReceiveError` + `ServedChainSnapshot`; the bootstrap imports `materialize_rolled_back_state`/`MaterializeError`/`TargetPoint` + `LedgerState`; the persistent writer imports `LedgerState`. All over existing crate-level edges (the `ade_runtime → ade_ledger` and the new `ade_node → ade_ledger` edges). |
| **Outbound deps** | `ade_types`, `ade_crypto`, `ade_codec`, `ade_plutus`, `ade_core`, `minicbor`, `num-bigint`, `num-integer`, `num-traits`. Dev-dep: `ade_testkit`. No new outbound crate edge in PHASE4-N-K. |
| **Entry points** | Carry-forward (all N-J + earlier surfaces). PHASE4-N-K consumption sites: `ade_ledger::receive::admit_via_block_validity` (via `dispatch_*_inbound` in `ade_runtime::receive::orchestrator`), `ade_ledger::rollback::materialize_rolled_back_state` (via `ade_runtime::bootstrap`), `ade_ledger::snapshot::framing::{encode_snapshot, decode_snapshot}` (via `ade_runtime::rollback::persistent_cache` and indirectly via `PersistentSnapshotWriter::on_admitted`). |
| **Key modules** | All carry-forward. No new module added in PHASE4-N-K. |

> **GREEN sub-classification (`ade_ledger::mempool::policy`, `ade_ledger::mempool::canonicalize`).** Carry-forward.
> **RED sub-classification (`ade_ledger::consensus_input_extract`).** Carry-forward.
> **`ade_ledger::producer::served_chain` BLUE classification (N-G).** Carry-forward.
> **`ade_ledger::receive` BLUE classification (N-H).** Carry-forward.
> **`ade_ledger::rollback` BLUE classification (N-I).** Carry-forward.
> **`ade_ledger::snapshot` BLUE classification (N-J).** Carry-forward.

---

### `ade_network` *(BLUE submodules)*

> **Status carried from prior CODEMAP — unchanged at HEAD `1946573` + PHASE4-N-K worktree.** PHASE4-N-K did not touch any BLUE submodule. `ChainSyncVersion`/`BlockFetchVersion` from `ade_network::codec::version` are now consumed by the new `OrchestratorEvent::PeerConnected` payload + `PerPeerReceiveVersions` struct in `ade_runtime::orchestrator::{event, state}` — no new BLUE export, only a new RED+GREEN consumer.

| Attribute | Value |
|-----------|-------|
| **Purpose** | Owns the Cardano Ouroboros mini-protocol authority — the closed wire grammar (CBOR codecs) and pure state machines for all 11 N2N + N2C mini-protocols, plus the BLUE Ouroboros mux frame primitive. Also owns the producer-side server-role reducer surface (PHASE4-N-G `chain_sync::server` + `block_fetch::server`). |
| **Creates** | **Wire grammar (`codec/`):** 11 closed mini-protocol message enums + closed payload types + structured `CodecError` + `ProtocolKind`. **State machines:** one closed `*State` / `*Agency` / `*Output` / `*Error` quad per protocol. **Mux primitives (`mux/frame.rs`):** `MuxFrame`, `MuxHeader`, `MuxMode`, `MiniProtocolId`, `MuxError`. **PHASE4-N-G (`chain_sync/server.rs` + `block_fetch/server.rs`):** `ServerReply(ServerVariant)` closed wrappers, `HeaderProjection`, `ProducerChainSyncServerState`, `ProducerServerError`, `ServerStep`, trait `ServedHeaderLookup`; `ProducerBlockFetchServerState`, `ProducerBlockFetchServerError`, `BlockFetchServerStep`, trait `ServedRangeLookup`. Functions: `producer_chain_sync_serve`, `producer_chain_sync_advance_tip`, `producer_block_fetch_serve`. |
| **Interprets** | The on-wire CBOR of every Ouroboros mini-protocol message (closed grammar) and the 8-byte Ouroboros mux frame header. Block/header/tx-body bytes inside RollForward / Block / Tx are opaque pass-through (DC-PROTO-06); LSQ Query/Result payloads also opaque. PHASE4-N-G server reducers interpret already-decoded `ChainSyncMessage` / `BlockFetchMessage` values. |
| **MUST NOT** | (1) `async fn`, `.await`, `tokio`, `async_std`, `futures::`, async-runtime spawn, or async timers in any file under the 9 BLUE paths. (2) Decode block / header / tx-body bytes. (3) Construct a `PreservedCbor` outside `ade_codec`'s named chokepoints. (4) Add `#[non_exhaustive]` to any wire-message enum or introduce a generic `Codec<P>` trait. (5) Define a generic `Agency<P>` wrapper. (6) Read a selected version from ambient session state. (7) Redefine `TxId` / `SlotNo` / `Hash32` / `CardanoEra`. (8) Depend on `pallas-network`. (9) Hold `String` in any error variant. (10) Use any other BLUE forbidden pattern. (11–13) PHASE4-N-G server-role MUST NOTs (carry-forward). |
| **Inbound deps** | `ade_core_interop` (live binaries), `ade_runtime` (carry-forward — the N-G/N-H/N-K consumers). PHASE4-N-K **adds a new inbound edge from `ade_node`**: `ade_node`'s `Cargo.toml` declares `ade_network` as a dep (currently used transitively through `ade_runtime`-re-exported types; no direct `ade_network::*` import in `ade_node` source today, but the manifest edge is registered for forward use). Dev-deps `ade_network → {ade_ledger, ade_testkit, ade_crypto, ade_core}` carry forward. |
| **Outbound deps** | `ade_types`, `ade_codec`. No external deps in the BLUE submodules. |
| **Entry points** | Codec entry points per protocol; `ade_network::codec::primitives::*`; `ade_network::codec::version::*` (now consumed by `OrchestratorEvent::PeerConnected`); `ade_network::codec::{CodecError, ProtocolKind}`. State-machine `*_transition` per protocol. Mux: `ade_network::mux::frame::*`. PHASE4-N-G: `ade_network::{chain_sync, block_fetch}::server::*`. |
| **Key modules** | `codec/` (11 protocols + `primitives.rs` + `version.rs` + `error.rs`); `handshake/`, `chain_sync/` (incl. PHASE4-N-G `server.rs`), `block_fetch/` (incl. PHASE4-N-G `server.rs`), `tx_submission/`, `keep_alive/`, `peer_sharing/`; `n2c/`; `mux/frame.rs`. |

---

### `ade_plutus`

> **Status carried from prior CODEMAP — unchanged at HEAD `1946573` + PHASE4-N-K worktree.** PHASE4-N-K did not touch `ade_plutus`.

| Attribute | Value |
|-----------|-------|
| **Purpose** | Quarantine boundary between the Ade-canonical ledger and the ported UPLC evaluator from `aiken-lang/aiken` (pinned to tag `v1.1.21`). |
| **Creates** | `PlutusScript`, `PlutusLanguage`, `EvalOutput`, `PlutusError`, `CostModels`, `DecoderMode`, `PerScriptResult`, `TxEvalResult`. |
| **Interprets** | UPLC scripts (Plutus V1/V2/V3) and `CostModels` CBOR. Phase-two transaction evaluation. `PlutusScript::from_cbor` is a named ingress chokepoint. |
| **MUST NOT** | (1) Re-export any `pallas_*` or `aiken_uplc::` type. (2) Allow another BLUE crate to bypass the canonical entry. (3) Activate PV11 builtins. (4) Use any BLUE-forbidden pattern. (5) Construct `PreservedCbor` outside `ade_codec`. |
| **Inbound deps** | `ade_ledger`, `ade_testkit`. |
| **Outbound deps** | `ade_types`, `ade_crypto`, `ade_codec`, `aiken_uplc` (git, tag `v1.1.21`), `pallas-primitives` (internal-only). |
| **Entry points** | `ade_plutus::eval_tx_phase_two`, `ade_plutus::tx_eval::*`, `ade_plutus::evaluator::*`, `ade_plutus::cost_model::*`. |
| **Key modules** | `evaluator.rs`, `cost_model.rs`, `script_context.rs`, `script_verdict.rs`, `tx_eval.rs`. |

---

### `ade_types`

> **Status carried from prior CODEMAP — unchanged at HEAD `1946573` + PHASE4-N-K worktree.** PHASE4-N-K did not touch `ade_types`. `SlotNo` / `Hash32` are now consumed by the new closed `OrchestratorEffect::AdmittedBlock { slot, hash }` and `OrchestratorEvent::SlotTick { slot, … }` payloads in `ade_runtime::orchestrator::event` — no new BLUE export.

| Attribute | Value |
|-----------|-------|
| **Purpose** | Owns the canonical Cardano domain vocabulary — primitives, era enum, address forms, era-tagged transaction bodies / outputs / certificates, governance types — used by every other workspace crate as the lingua franca. |
| **Creates** | `CardanoEra`, `SlotNo`, `BlockNo`, `EpochNo`, `Hash28`, `Hash32`, `Coin`, `Lovelace`, `NetworkId`, `Nonce`, `TxIn`, `RewardAccount`, `PoolId`, `Address`, `ByronAddress`, `Credential`, `StakeCredential`, `Certificate`, `PoolRegistrationCert`, `ConwayCert`, `CertDisposition` / `DepositEffect` / `CoinSource`, `MIRCert`, `MIRPot`, `DRep`, `GovAction`, `GovActionState`, `GovActionId`, `Anchor`, `ProposalProcedure`, `OperationalCert`, `NativeScript`, `PlutusV1Script`, `Datum`, `DatumOption`, `MultiAsset`, `AssetName`, `CostModel`, `ExUnits`, plus per-era tx-body / tx-out / witness wrappers. |
| **Interprets** | None — produce-only. |
| **MUST NOT** | (1) Construct or decode `PreservedCbor`. (2) Use any BLUE-forbidden pattern. (3) Depend on any workspace crate. (4) Add open/extensible variants to closed enums without a versioned gate. |
| **Inbound deps** | `ade_codec`, `ade_crypto`, `ade_ledger`, `ade_plutus`, `ade_runtime`, `ade_testkit`, `ade_network`, `ade_core`, `ade_core_interop`. **PHASE4-N-K adds a new inbound edge from `ade_node`** (manifest dep declared; consumed in `node.rs` via `ade_types::SlotNo`). |
| **Outbound deps** | None. |
| **Entry points** | `ade_types::CardanoEra`, `ade_types::tx::{Coin, TxIn, RewardAccount}`, `ade_types::{Hash32, SlotNo, Hash28, BlockNo, EpochNo}`, `ade_types::conway::tx::ConwayTxBody`, `ade_types::conway::cert::*`, `ade_types::conway::governance::{ProposalProcedure, Anchor, GovAction, GovActionId}`, `ade_types::shelley::block::{OperationalCert, ProtocolVersion, ShelleyHeader, ShelleyBlock, VrfData}`. |
| **Key modules** | `primitives.rs`, `era.rs`, `tx.rs`, `address/`, `byron/`, `shelley/`, `allegra/`, `mary/`, `alonzo/`, `babbage/`, `conway/`. |

---

## GREEN Modules — Deterministic Glue

> Deterministic, non-authoritative. May depend on BLUE; must not affect authoritative outputs.

### `ade_testkit`

> **Status carried from prior CODEMAP — unchanged at HEAD `1946573` + PHASE4-N-K worktree.** PHASE4-N-K did not add a sub-module to `ade_testkit`; the cluster's evidence surface is the 4 new integration tests under `crates/ade_runtime/tests/` and `crates/ade_node/tests/` (orchestrator_peer_isolation, orchestrator_replay_equivalence, shutdown_resume_identity, authority_fatal_decode) + the inline tests in the new GREEN files. `ade_node` adds `ade_testkit` as a dev-dep for these integration tests.

| Attribute | Value |
|-----------|-------|
| **Purpose** | Test infrastructure: differential harness, oracle snapshot loader, regression corpus, genesis loader, era mapping, transcript replay, diff reporting; N-B consensus harness; B1 block-validity harness (`validity/`); B2 transaction-validity harness (`tx_validity/`); N-E S2 mempool-ingress replay harness (`mempool/`); PP-S2 canonical synthetic `proposal_procedures` corpus + replay harness (`governance/`); PHASE4-N-C producer test harness (`producer/`). |
| **Creates** | Carry-forward (all entries from prior CODEMAP). No new types in PHASE4-N-K. |
| **Interprets** | Carry-forward. |
| **MUST NOT** | Carry-forward (1)–(16). |
| **Inbound deps** | None at compile time; consumption is via integration tests and dev-dep links from `ade_core`, `ade_runtime`, `ade_ledger`. `ade_core_interop` is a non-dev consumer. **PHASE4-N-K adds `ade_node` as a new dev-dep consumer** (Cargo.toml declares it; the integration tests under `crates/ade_node/tests/` use harness helpers for Conway-corpus block loading). |
| **Outbound deps** | `ade_types`, `ade_codec`, `ade_crypto`, `ade_core`, `ade_ledger`, `ade_plutus`, `ade_runtime`, `blake2`, `flate2`, `tar`, `serde`, `serde_json`, `toml`, `cardano-crypto` (dev-dep, N-C). |
| **Entry points** | `ade_testkit::harness::*`; N-B `consensus::*`; B1 `validity::*`; B2 `tx_validity::*`. N-E S2: `ade_testkit::mempool::*`. PP-S2: `ade_testkit::governance::*`. PHASE4-N-C: `ade_testkit::producer::*`. |
| **Key modules** | `harness/`, `consensus/`, `validity/`, `tx_validity/`, `mempool/`, `governance/`, `producer/` (N-C). |

> **Classification note carried forward.** `ade_testkit` reads files from disk in test helpers and drives BLUE authorities from corpus inputs.

---

### `ade_runtime::bootstrap` *(GREEN — PHASE4-N-K S1, NEW)*

> **Status: NEW at HEAD `1946573` + PHASE4-N-K worktree.** A GREEN top-level file inside the RED `ade_runtime` crate prefix. The shared crate prefix is RED per `.idd-config.json` `_core_paths_doc`; the GREEN sub-classification of this specific file is **by file** — `// Core Contract:` banner present, file-header docstring naming the GREEN classification, single-authority shape gated mechanically by `ci/ci_check_bootstrap_closure.sh` (CN-NODE-01). Surfaced here for the GREEN inventory; also covered as a sub-section of `ade_runtime` in the RED block below.

| Attribute | Value |
|-----------|-------|
| **Purpose** | The SOLE `pub fn` in the workspace returning the initial `(LedgerState, PraosChainDepState, Option<ChainTip>)` triple at node startup. Cold-start (genesis-only) and warm-start (snapshot-resume + replay-forward to chaindb tip) are two branches of one function — never parallel paths. Closes **CN-NODE-01**. |
| **Creates** | `BootstrapInputs<'a, D: ChainDb, S: SnapshotStore + ?Sized>` (borrows chaindb / snapshot_store / era_schedule / ledger_view + optional `(LedgerState, PraosChainDepState)` genesis seed); closed 4-variant `BootstrapError` sum `ChainDb(ChainDbError)` / `SnapshotMissing { chain_tip_slot: SlotNo }` / `Materialize(MaterializeError)` / `GenesisRequiredButAbsent`. Function: `bootstrap_initial_state(inputs) -> Result<(LedgerState, PraosChainDepState, Option<ChainTip>), BootstrapError>`. Branch table: `(chaindb.tip(), snapshot_slots non-empty?)` — `(None, empty)` → cold-start (returns `(genesis_ledger, genesis_chain_dep, None)`); `(Some(tip), any)` → warm-start at `tip.slot`; `(None, non-empty)` → warm-start at largest stored snapshot slot (recover from chaindb truncation). |
| **Interprets** | Cold-start interprets the operator-supplied genesis seed exactly once. Warm-start interprets snapshot bytes exactly twice via the BLUE single authorities — `PersistentSnapshotCache::nearest_le` (which calls `framing::decode_snapshot`, CN-STORE-08) for the snapshot read + `materialize_rolled_back_state` (CN-STORE-07) for the replay-forward to the chaindb tip. No CBOR parsing in this file; all decode + replay routes through BLUE. |
| **MUST NOT** | (1) Define a second `pub fn` returning the initial state triple — repo-wide enforced indirectly via `ci/ci_check_bootstrap_closure.sh` (the script asserts exactly one such function in `bootstrap.rs`). (2) Use `HashMap`/`HashSet`/`SystemTime`/`Instant`/`tokio`/`rand` in the production body. (3) Bypass `materialize_rolled_back_state` for warm-start — the script's positive grep requires the call site to exist. (4) Construct snapshot bytes outside `framing::encode_snapshot` (it never does — bootstrap is read-only on snapshots; writes route through `PersistentSnapshotWriter`). (5) Reorder the branch table — cold-start is reachable only when both `chaindb.tip()` is `None` and `snapshot_store.list_snapshot_slots()` is empty; any other shape is warm-start. (6) Surface decode failures as panics — `BootstrapError::Materialize(_)` carries the structured rejection. (7) Own storage lifecycle — the function borrows `&D` / `&S` so the caller (the `ade_node` binary) controls open/close/flush. |
| **Inbound deps** | `ade_node::node::run_node_until_shutdown` (sole production caller; gated by `ci/ci_check_node_binary_uses_single_bootstrap.sh` to exactly one call site across `ade_node` source); the 6 inline tests + the integration tests `orchestrator_replay_equivalence.rs` + `shutdown_resume_identity.rs` + `authority_fatal_decode.rs`. |
| **Outbound deps** | `ade_core::consensus::{era_schedule::EraSchedule, ledger_view::LedgerView, praos_state::PraosChainDepState}`, `ade_ledger::rollback::{materialize_rolled_back_state, MaterializeError, TargetPoint}`, `ade_ledger::state::LedgerState`, `ade_types::SlotNo`, `crate::chaindb::{ChainDb, ChainDbError, ChainTip, SnapshotStore}`, `crate::rollback::chaindb_block_source::ChainDbBlockSource`, `crate::rollback::persistent_cache::PersistentSnapshotCache`. No new outbound crate edge. |
| **Entry points** | `ade_runtime::bootstrap::{bootstrap_initial_state, BootstrapInputs, BootstrapError}`. Re-exported by `ade_node::lib` as part of the binary's startup vocabulary. |

---

### `ade_runtime::clock` *(GREEN trait + GREEN `DeterministicClock` — PHASE4-N-K S1, NEW; RED `SystemClock` is sub-classified RED in the same file)*

> **Status: NEW at HEAD `1946573` + PHASE4-N-K worktree.** A single file hosts the clock seam: the GREEN trait + the GREEN deterministic impl + the RED production impl. Splitting them across files was rejected to keep the seam single-sited and discoverable; the RED sub-classification is structural — `SystemClock` is the SOLE callsite of `std::time::SystemTime::now()` / `std::thread::sleep` in `ade_runtime` and is gated by `ci/ci_check_clock_seam.sh` to never leak outside this file. Cross-listed under `ade_runtime` in the RED block below.

| Attribute | Value |
|-----------|-------|
| **Purpose** | The orchestrator core consumes time exclusively through the `Clock` trait. Production uses `SystemClock` (RED sub-classified); tests and replay harnesses use `DeterministicClock` (pure — no wall-clock read). Closes **DC-NODE-03**. |
| **Creates** | `Clock` trait (`fn now_millis(&self) -> u64`, `fn next_tick(&mut self) -> Option<u64>`; `Send + Sync` super-trait bound). `DeterministicClock { anchor_millis: u64, ticks: Vec<u64>, cursor: usize }` impl `Clock` (pure; two instances built from same `(anchor_millis, ticks)` produce identical outputs). `SystemClock { slot_length_ms: u32, next_boundary_millis: u64 }` impl `Clock` (**RED** — `now_millis` reads `SystemTime::now()`; `next_tick` blocks via `std::thread::sleep` until the next slot boundary). Pure helper `millis_to_slot(tick_millis, start_millis, start_slot, slot_length_ms) -> SlotNo` (degenerate-safe: zero `slot_length_ms` returns `start_slot`; pre-anchor saturates). |
| **Interprets** | `DeterministicClock` interprets its pre-loaded tick vector exactly once per call; `SystemClock` interprets the OS wall clock once per `now_millis` and once per `next_tick`. |
| **MUST NOT** | (1) Define `SystemTime::now()` / `Instant::now()` / `std::thread::sleep` outside this file in `ade_runtime` — enforced by `ci/ci_check_clock_seam.sh`. (2) Add a second `Clock` impl in this file beyond the deterministic + system pair (replay needs exactly the deterministic shape; production needs exactly the system shape). (3) Use `HashMap`/`HashSet`/`rand` anywhere in the file. (4) Allow `SystemClock` to be reachable from BLUE — the `Clock` trait is the only export consumed across modules; concrete `SystemClock` lives in the RED runner (`leadership_session::LeadershipSession::new(SystemClock::new(slot_length_ms))`). (5) Use floats — `millis_to_slot` is integer arithmetic only. |
| **Inbound deps** | `ade_runtime::orchestrator::leadership_session` (consumes `Clock` + `SystemClock` for production), `ade_node::node` (passes a `Clock`-typed parameter into `run_node_until_shutdown`), the 6 inline tests + `orchestrator_replay_equivalence.rs` (uses `DeterministicClock`). |
| **Outbound deps** | `ade_types::SlotNo`, `std::time::{SystemTime, UNIX_EPOCH}`, `std::thread::sleep`. No new outbound crate edge. |
| **Entry points** | `ade_runtime::clock::{Clock, DeterministicClock, SystemClock, millis_to_slot}`. |

---

### `ade_runtime::orchestrator::{mod, event, state, core}` *(GREEN — PHASE4-N-K S2, NEW)*

> **Status: NEW at HEAD `1946573` + PHASE4-N-K worktree.** The GREEN sub-tree of `ade_runtime::orchestrator/`. Sibling RED files (`peer_session`, `leadership_session`, `n2n_server_pump`) under the same `orchestrator/` parent are listed in the RED block below. The GREEN classification rests on: (a) `// Core Contract:` banners on every file; (b) `ci/ci_check_orchestrator_core_purity.sh` enforces tokio-free / SystemTime-free / HashMap-free production bodies; (c) `ci/ci_check_clock_seam.sh` enforces that the core consumes time only via `Clock` trait calls.

| Attribute | Value |
|-----------|-------|
| **Purpose** | The pure orchestrator reducer that composes all PHASE4-N-A..N-J BLUE authorities into one running node-shaped state machine. `step(state, event, …) -> Result<Vec<OrchestratorEffect>, OrchestratorError>` is the canonical entry; same inputs always produce the same effect vector (T-DET-01 strengthening). RED runners translate sockets + clocks into `OrchestratorEvent`s and effects into socket writes. |
| **Creates** | `PeerId(pub u64)` (stable deterministic peer identifier; production runner allocates from a monotonic atomic counter, tests assign explicitly). Closed 8-variant `OrchestratorEvent` sum: `SlotTick { slot_millis, slot }` / `PeerConnected { peer_id, chain_sync_version, block_fetch_version, role }` / `PeerDisconnected { peer_id }` / `PeerChainSyncFrame { peer_id, bytes }` / `PeerBlockFetchFrame { peer_id, bytes }` / `PeerN2nServerChainSyncFrame { peer_id, bytes }` / `PeerN2nServerBlockFetchFrame { peer_id, bytes }` / `Shutdown`. Closed 2-variant `PeerRole` sum: `UpstreamClient` (receive side) / `DownstreamServer` (server side). Closed 5-variant `OrchestratorEffect` sum: `SendToPeer { peer_id, bytes }` / `AdmittedBlock { slot, hash }` / `CaptureSnapshot { slot }` / `PeerSessionHalted { peer_id, reason }` / `ShutdownAcknowledged`. Closed 10-variant `PeerHaltReason` sum (no `String`; every per-peer-fatal kind maps to a known discriminant — `ChainSyncDecodeError` / `BlockFetchDecodeError` / `ServerChainSyncDecodeError` / `ServerBlockFetchDecodeError` / `ReceiveValidityRejected` / `ReceiveHeaderBodyMismatch` / `ReceiveRollbackOutOfScope` / `ServerChainSyncProtocolError` / `ServerBlockFetchProtocolError` / `PeerUnknown`). Closed 3-variant `AuthorityFatalKind` sum: `ChainWriteIo` / `SnapshotDecodeUnknownVersion` / `SnapshotDecodeFingerprintMismatch`. Closed single-variant `OrchestratorError::AuthorityFatal(AuthorityFatalKind)`. `PerPeerReceiveVersions { chain_sync_version, block_fetch_version }` and `OrchestratorState { receive_state, per_peer_receive: BTreeMap<PeerId, PerPeerReceiveVersions>, per_peer_server: BTreeMap<PeerId, PerPeerN2nServerState>, cadence, last_persistent_snapshot_slot, last_observed_slot, shutdown_requested }`. Function: `step<W: ChainDbWrite>(&mut OrchestratorState, OrchestratorEvent, &mut W, &ServedChainSnapshot, &EraSchedule, &dyn LedgerView) -> Result<Vec<OrchestratorEffect>, OrchestratorError>`. |
| **Interprets** | Pre-decoded `OrchestratorEvent`s. Authority routing (per `core.rs` doc): `PeerChainSyncFrame` → `receive::dispatch_chain_sync_inbound` (N-H authority); `PeerBlockFetchFrame` → `receive::dispatch_block_fetch_inbound` (N-H); `PeerN2nServerChainSyncFrame` → `network::n2n_server::dispatch_chain_sync_frame` (N-G); `PeerN2nServerBlockFetchFrame` → `network::n2n_server::dispatch_block_fetch_frame` (N-G); `SlotTick` → state-only (records `last_observed_slot`); `PeerConnected` / `PeerDisconnected` → state-only (install/remove per-peer map entry); `Shutdown` → sets `shutdown_requested` + emits `ShutdownAcknowledged`. After every receive-side `Admitted` effect, consults `should_snapshot_after_block` (N-I authority) and emits `CaptureSnapshot { slot }` iff the cadence policy returns true. Never re-implements an authority; always composes. |
| **MUST NOT** | (1) Import `tokio::*` in any of the 4 core files (`core.rs`, `event.rs`, `state.rs`, `mod.rs`) — enforced by `ci/ci_check_orchestrator_core_purity.sh` + `ci/ci_check_clock_seam.sh`. (2) Use `SystemTime` / `Instant` / `rand::*` / `HashMap` / `HashSet` / `f32` / `f64` in any core file (same scripts). (3) Bypass `admit_via_block_validity` / `commit_rollback` / `framing::{encode,decode}_snapshot` — every authority call routes through the N-G/N-H/N-I/N-J dispatch wrappers. (4) Cross-peer state mutation — each peer's state lives in its own `BTreeMap<PeerId, _>` entry; the orchestrator drops one entry on `PeerSessionHalted` (DC-NODE-01 — `ci/ci_check_peer_session_isolation.sh` proves this structurally at the peer-session source). (5) Re-implement cadence — every snapshot-capture decision goes through `should_snapshot_after_block` (DC-NODE-02 — `ci/ci_check_persistent_writer_no_parallel_cadence.sh`). (6) Silent fallback on `UnknownVersion` / `FingerprintMismatch` — these map to `AuthorityFatalKind` variants that halt the binary. (7) Emit a `CaptureSnapshot` effect without consulting `should_snapshot_after_block` first. (8) Surface `String` in any event/effect/state field — every closed enum is `String`-free for closed-discriminant discipline. (9) Define a second `pub fn step` anywhere in `ade_runtime`. (10) Allow `step` to advance state after `shutdown_requested` is set (the early-return idiom is `if state.shutdown_requested && !matches!(event, Shutdown) { return Ok(Vec::new()); }`). |
| **Inbound deps** | `ade_node::node::run_node_until_shutdown` (sole production caller); the integration tests `orchestrator_peer_isolation.rs` + `orchestrator_replay_equivalence.rs` + `shutdown_resume_identity.rs`. |
| **Outbound deps** | `ade_core::consensus::{era_schedule::EraSchedule, ledger_view::LedgerView}`, `ade_ledger::producer::ServedChainSnapshot`, `ade_ledger::receive::{ChainDbWrite, ReceiveEffect, ReceiveError, ReceiveState}`, `ade_network::codec::version::{BlockFetchVersion, ChainSyncVersion}`, `ade_types::{Hash32, SlotNo}`, `crate::clock::Clock` (consumed transitively via runners — the core itself does not import `Clock`; only `SlotTick` arrives with `slot_millis` pre-computed by the runner), `crate::receive::orchestrator::{dispatch_block_fetch_inbound, dispatch_chain_sync_inbound, PerPeerReceiveState, ReceiveDispatchError}`, `crate::network::n2n_server::{dispatch_block_fetch_frame, dispatch_chain_sync_frame, DispatchError as ServerDispatchError, PerPeerN2nServerState}`, `crate::rollback::cadence::{SnapshotCadence, should_snapshot_after_block}`. No new outbound crate edge. |
| **Entry points** | `ade_runtime::orchestrator::step`, `ade_runtime::orchestrator::{OrchestratorState, OrchestratorEvent, OrchestratorEffect, OrchestratorError, PeerHaltReason, PeerId, PeerRole, AuthorityFatalKind, PerPeerReceiveVersions}` (all re-exported by `mod.rs` `pub use`). Consumed by `ade_node::node`. |

---

### `ade_runtime::rollback::persistent_writer` *(GREEN — PHASE4-N-K S3, NEW)*

> **Status: NEW at HEAD `1946573` + PHASE4-N-K worktree.** A GREEN file inside the existing `ade_runtime::rollback/` sub-tree (alongside the three N-I GREEN files, the one N-I RED file, and the one N-J GREEN file `persistent_cache.rs`). GREEN classification gated by `ci/ci_check_persistent_writer_no_parallel_cadence.sh` (DC-NODE-02) — repo-wide check that no second `pub fn should_snapshot_after_block` and no parallel cadence-defining struct exists anywhere in `ade_runtime`.

| Attribute | Value |
|-----------|-------|
| **Purpose** | Cadence-fidelity glue between the orchestrator's `CaptureSnapshot` effect (S2) and `PersistentSnapshotCache::capture` (N-J S8). The writer NEVER decides cadence on its own — every "capture or skip" decision routes through `should_snapshot_after_block`. Snapshot eviction is explicitly out of scope per the PHASE4-N-K invariants sketch. Closes **DC-NODE-02**. |
| **Creates** | `PersistentSnapshotWriter<'a, S: SnapshotStore + ?Sized> { cache: PersistentSnapshotCache<'a, S>, cadence: SnapshotCadence, last_capture: Option<SlotNo> }`. Methods: `new(store, cadence)`, `cadence()`, `last_captured_slot()`, `on_admitted(slot, block_no, ledger, chain_dep) -> Result<bool, PersistentCacheError>` (consults `should_snapshot_after_block`; returns `Ok(true)` if captured, `Ok(false)` if skipped), `force_capture(slot, ledger, chain_dep) -> Result<(), PersistentCacheError>` (bypasses cadence for shutdown drain — DC-NODE-04 resume-identity). No new error type; reuses `PersistentCacheError` from N-J. |
| **Interprets** | Per-admission `(slot, block_no, ledger, chain_dep)` quadruples. Translates each into either a no-op or one `PersistentSnapshotCache::capture` call. Never decodes snapshot bytes; never constructs them outside `framing::encode_snapshot` (via the cache). |
| **MUST NOT** | (1) Re-implement cadence — must call `should_snapshot_after_block` exclusively (DC-NODE-02; `ci/ci_check_persistent_writer_no_parallel_cadence.sh` enforces no second `pub fn should_snapshot_after_block` anywhere). (2) Define a second `SnapshotCadence`-like struct (same script). (3) Hardcode a parallel "snapshot every N" constant in this file (same script). (4) Construct snapshot bytes outside `framing::encode_snapshot` — `PersistentSnapshotCache::capture` is the only call path (CN-STORE-08 strengthening). (5) Own the `SnapshotStore` lifecycle — the cache borrows `&'a S`; the caller (the `ade_node` binary) controls open/close/flush. (6) Surface storage I/O failures silently — `PersistentCacheError::Store(ChainDbError::Io(_))` is authority-fatal at the binary boundary (DC-NODE-04, mapped via `NodeRunError::PersistentWriterIo` → `EXIT_AUTHORITY_FATAL_IO=10`). (7) Update `last_capture` unless a capture actually succeeded (the `on_admitted` skip path leaves it unchanged; the `force_capture` path always updates). |
| **Inbound deps** | `ade_node::node::run_node_until_shutdown` (sole production caller; instantiates one writer per node and routes orchestrator `AdmittedBlock` effects + the shutdown-drain `force_capture` call through it); the 5 inline tests in this file. |
| **Outbound deps** | `ade_core::consensus::praos_state::PraosChainDepState`, `ade_ledger::state::LedgerState`, `ade_types::{BlockNo, SlotNo}`, `crate::chaindb::SnapshotStore`, `crate::rollback::cadence::{SnapshotCadence, should_snapshot_after_block}`, `crate::rollback::persistent_cache::{PersistentCacheError, PersistentSnapshotCache}`. No new outbound crate edge. |
| **Entry points** | `ade_runtime::rollback::persistent_writer::PersistentSnapshotWriter`. Re-exported at `ade_runtime::rollback::*` via `pub use persistent_writer::PersistentSnapshotWriter;`. |

---

### `ade_runtime::producer::{broadcast_to_served, served_chain_lookups}` *(GREEN — PHASE4-N-G S5)*

> **Status carried from prior CODEMAP — unchanged at HEAD `1946573` + PHASE4-N-K worktree.** PHASE4-N-K did not touch this GREEN sub-tree.

| Attribute | Value |
|-----------|-------|
| **Purpose** | Bridges PHASE4-N-C's `BroadcastQueue` (RED) and PHASE4-N-G's `ServedChainSnapshot` (BLUE) to the `ServedHeaderLookup` / `ServedRangeLookup` trait seams the BLUE server reducers consume. |
| **Creates** | `ServedChainLookups<'a>`; function `drain_and_admit`. |
| **MUST NOT** | Carry-forward. |
| **Inbound deps** | `ade_runtime::network::n2n_server`; `ade_runtime::orchestrator::core::step` (NEW — passes the `ServedChainSnapshot` through to the reducer's server-side dispatchers); the integration tests under `ade_runtime/tests/`. |
| **Outbound deps** | `ade_ledger::{producer, block_validity}`, `ade_network::{chain_sync::server, block_fetch::server, codec::chain_sync}`, `ade_types`. |
| **Entry points** | `ade_runtime::producer::broadcast_to_served::drain_and_admit`, `ade_runtime::producer::served_chain_lookups::ServedChainLookups`. |

---

### `ade_runtime::receive::{events_to_state, in_memory_chain_write}` *(GREEN — PHASE4-N-H S3, extended PHASE4-N-I S3)*

> **Status carried from prior CODEMAP — unchanged at HEAD `1946573` + PHASE4-N-K worktree.** PHASE4-N-K did not touch this GREEN sub-pair. `ChainDbWriter` is now the production `ChainDbWrite` impl wired into `ade_node::node::run_node_until_shutdown` (paired with `bootstrap_initial_state` for the warm-start commit).

| Attribute | Value |
|-----------|-------|
| **Purpose** | Bridges PHASE4-N-A's `ForkChoiceSignal` / `BatchDeliveryEvent` to the BLUE `ReceiveEvent` stream, and wires the BLUE `ChainDbWrite` trait to any `ade_runtime::chaindb::ChainDb` impl. (Includes the N-I `rollback_to_slot` impl on `ChainDbWriter`.) |
| **Creates** | Functions `lift_chain_sync_signal` + `lift_block_fetch_event`. Struct `ChainDbWriter<'a, D: ChainDb>` with `impl<'a, D: ChainDb> ChainDbWrite for ChainDbWriter<'a, D>`. |
| **MUST NOT** | Carry-forward (1)–(8). |
| **Inbound deps** | `ade_runtime::receive::orchestrator`; `ade_node::node::run_node_until_shutdown` (NEW — instantiates the writer); the integration tests under `ade_runtime/tests/receive_*.rs`. |
| **Outbound deps** | Carry-forward — no new edges in N-K. |
| **Entry points** | `ade_runtime::receive::events_to_state::{lift_chain_sync_signal, lift_block_fetch_event}`, `ade_runtime::receive::in_memory_chain_write::ChainDbWriter`. |

---

### `ade_runtime::rollback::{cadence, in_memory_cache, chaindb_block_source}` *(GREEN — PHASE4-N-I S4)*

> **Status carried from prior CODEMAP — unchanged at HEAD `1946573` + PHASE4-N-K worktree.** PHASE4-N-K did not touch the three N-I GREEN files. `should_snapshot_after_block` + `SnapshotCadence` are now consumed by both the new `PersistentSnapshotWriter` (S3) and the new `orchestrator::core::step` (S2 — `CaptureSnapshot` emission); DC-NODE-02 enforces single-authority via repo-wide grep.

| Attribute | Value |
|-----------|-------|
| **Purpose** | Production impls of the BLUE `ade_ledger::rollback` trait seams + the cadence policy that decides when to snapshot. |
| **Creates** | `SnapshotCadence { every_n_blocks: u32 }` + `should_snapshot_after_block(SlotNo, BlockNo, SnapshotCadence, Option<SlotNo>) -> bool`; `InMemorySnapshotCache { entries: BTreeMap<SlotNo, (LedgerState, PraosChainDepState)> }` with `impl SnapshotReader`; `ChainDbBlockSource<'a, D: ChainDb> { pub db: &'a D }` with `impl<'a, D: ChainDb> BlockSource`. |
| **MUST NOT** | Carry-forward (1)–(8); **NEW from PHASE4-N-K — `should_snapshot_after_block` MUST remain the SOLE cadence-decision function in `ade_runtime`** (DC-NODE-02; `ci/ci_check_persistent_writer_no_parallel_cadence.sh`). |
| **Inbound deps** | `ade_runtime::rollback::{snapshot_writer, persistent_writer}` (NEW for persistent_writer); `ade_runtime::orchestrator::core::step` (NEW — consults `should_snapshot_after_block` after every admit); `ade_runtime::rollback::persistent_cache` (consumed in cross-impl tests). |
| **Outbound deps** | Carry-forward. |
| **Entry points** | `ade_runtime::rollback::cadence::{SnapshotCadence, should_snapshot_after_block}`, `ade_runtime::rollback::in_memory_cache::InMemorySnapshotCache`, `ade_runtime::rollback::chaindb_block_source::ChainDbBlockSource`. |

---

### `ade_runtime::rollback::persistent_cache` *(GREEN — PHASE4-N-J S8)*

> **Status carried from prior CODEMAP — unchanged at HEAD `1946573` + PHASE4-N-K worktree.** PHASE4-N-K did not touch this file. The new `PersistentSnapshotWriter` (S3) wraps `PersistentSnapshotCache::capture`; the new `bootstrap_initial_state` (S1) reads via `PersistentSnapshotCache::nearest_le`. Both consumers are GREEN; the cache itself remains the single bridge between the BLUE framing layer and the `SnapshotStore` trait.

| Attribute | Value |
|-----------|-------|
| **Purpose** | Bridges the BLUE `ade_ledger::snapshot::framing::{encode_snapshot, decode_snapshot}` pair to any `ade_runtime::chaindb::SnapshotStore` impl. **Closes DC-CONS-21.** |
| **Creates** | `PersistentSnapshotCache<'a, S: SnapshotStore + ?Sized> { store: &'a S }` with `impl SnapshotReader`; closed 3-variant `PersistentCacheError`; `pub const PERSISTENT_CACHE_SCHEMA_VERSION: u32 = SCHEMA_VERSION`. |
| **MUST NOT** | Carry-forward (1)–(8). |
| **Inbound deps** | The 6 inline tests; `ade_runtime::rollback::persistent_writer` (NEW — wraps `capture`); `ade_runtime::bootstrap::bootstrap_initial_state` (NEW — calls `nearest_le` for warm-start); `ade_node::node` (transitively via the writer + bootstrap). |
| **Outbound deps** | Carry-forward — no new outbound crate edge. |
| **Entry points** | `ade_runtime::rollback::persistent_cache::{PersistentSnapshotCache, PersistentCacheError, PERSISTENT_CACHE_SCHEMA_VERSION}`. |

---

## RED Modules — Imperative Shell

> I/O, network, storage, clocks, retries. May depend on BLUE/GREEN. Must not modify core state directly or construct semantic types unsafely.

### `ade_core_interop` *(PHASE4-N-B S-B10; PHASE4-N-E S4/S5/S6; PHASE4-N-C S7; PHASE4-N-G S7; PHASE4-N-H S6)*

> **Status carried from prior CODEMAP — unchanged at HEAD `1946573` + PHASE4-N-K worktree.** PHASE4-N-K did not add a new live binary. The persistent-snapshot + orchestrator path becomes operator-exercisable through the new `ade_node` binary (which is the more direct host for the orchestrator + persistent-snapshot wiring than the `live_*_session` precedents) — the `live_block_follow_session` binary continues to host the cross-impl live-evidence half for RO-LIVE-02.

| Attribute | Value |
|-----------|-------|
| **Purpose** | Live cardano-node interop driver. Hosts the five `live_*_session` RED binaries plus the N-E S4/S5 GREEN bridges (`tx_submission` + `local_tx_submission`). |
| **Creates** | `fresh_orchestrator` (N-B). N-E S4 sub-module: `PeerAccumulator` (GREEN). N-E S5 sub-module: `ClientAccumulator` (GREEN). Functions: `event_to_ingress` + `ingest_n2n_events` (S4); `local_event_to_ingress` + `ingest_n2c_events` (S5). The five `live_*_session` binaries are RED glue with no public Rust types. |
| **MUST NOT** | (1)–(11) carry-forward. |
| **Inbound deps** | None. |
| **Outbound deps** | `ade_core`, `ade_codec`, `ade_crypto`, `ade_ledger`, `ade_runtime`, `ade_network`, `ade_testkit`, `ade_types`, `tokio`. No new edges in N-K. |
| **Entry points** | `ade_core_interop::fresh_orchestrator`, the five `live_*_session` binaries. |
| **Key modules** | `src/lib.rs`, `src/follow.rs` (N-B), `src/tx_submission.rs` (N-E S4), `src/local_tx_submission.rs` (N-E S5), `src/bin/live_*_session.rs`. |

> **Gap surfaced (carried).** CE-N-B-6 manual evidence pending. CE-N-E-6 CLOSED. CE-N-E-7 deferred as `CE-NODE-N2C-LTX`. CE-N-C-8 enforced + `CN-CONS-06.open_obligation = blocked_until_operator_stake_available`. CE-N-G-8 (RO-LIVE-01) partial + same operator-action obligation. CE-N-H-6 (RO-LIVE-02) partial + same. **NEW from PHASE4-N-K** — `ade_node` is now the more natural host for the operator-action work (it has explicit `--peer`/`--listen` CLI flags); the `live_*_session` binaries continue as smaller-scope per-protocol witnesses.

---

### `ade_network` *(RED submodules + capture binaries)*

> **Status carried from prior CODEMAP — unchanged at HEAD `1946573` + PHASE4-N-K worktree.** PHASE4-N-K did not touch the RED half of `ade_network`. The `session::mod.rs` placeholder is still empty; the absent mux session driver is what the new `ade_node` binary's honest-scope stub note refers to.

| Attribute | Value |
|-----------|-------|
| **Purpose** | The only place inside `ade_network` where I/O happens. |
| **Creates** | `MuxTransport`. `session/mod.rs` is an empty placeholder. |
| **MUST NOT** | Carry-forward (1)–(6). |
| **Inbound deps** | `ade_core_interop`. |
| **Outbound deps** | `tokio`, `ade_types`, `ade_codec`, BLUE `ade_network::codec::*`, `ade_network::mux::frame`. |
| **Entry points** | `ade_network::mux::transport::*`. The capture binaries. |
| **Key modules** | `mux/transport.rs`, `session/mod.rs`, `bin/capture_*`. |

> **Gap surfaced (carried).** `ade_network::session::mod.rs` is still a placeholder. The `ade_node::main` binary readiness stub explicitly defers the chain-sync/block-fetch socket pump to operator-action work per RO-LIVE-01/RO-LIVE-02.

---

### `ade_node`

> **Status change at HEAD `1946573` + PHASE4-N-K worktree (PHASE4-N-K S7 — refactored from a 1-file hello-world stub into a lib+bin with deterministic authority-fatal exit-code mapping).** Between `f15102f` and HEAD, `ade_node` gained: `src/lib.rs` (23 LOC; library entry exposing the binary's `run` lifecycle as a function so integration tests can drive shutdown-resume identity in-process), `src/cli.rs` (173 LOC; argv-style parser, closed 3-variant `CliError`), `src/node.rs` (254 LOC; the `run_node_until_shutdown` lifecycle + closed 3-variant `NodeRunError` + the three exit-code constants), and a fully refactored `src/main.rs` (78 LOC; thin `fn main() -> ExitCode` wrapper). Cargo.toml gained dependencies on `ade_types`, `ade_core`, `ade_ledger`, `ade_runtime`, `ade_network`, `ade_codec`, `tokio` (with the full feature set `net + rt + rt-multi-thread + io-util + macros + time + sync + signal`); dev-deps gained `ade_testkit` + `tempfile`. Two new integration tests under `crates/ade_node/tests/`: `shutdown_resume_identity.rs` (3 tests; DC-NODE-04) + `authority_fatal_decode.rs` (1 test; DC-NODE-04 fail-fast).

| Attribute | Value |
|-----------|-------|
| **Purpose** | Binary entry point for the node process. Composes `ade_runtime::bootstrap::bootstrap_initial_state` (CN-NODE-01) → `OrchestratorState` over the bootstrap output → `LeadershipSession` over the injected `Clock` → the orchestrator inbox loop driving `step` per event → shutdown drain with `PersistentSnapshotWriter::force_capture` for DC-NODE-04 resume identity. The binary today, when launched, performs CLI parsing + an honest-scope readiness print; the actual N2N peer dialer + mux session driver above `MuxTransport::read_raw/write_raw` is operator-action work per RO-LIVE-01/RO-LIVE-02. The full bootstrap → orchestrator → drain → bootstrap cycle is exercised in-process by the `shutdown_resume_identity` integration test. |
| **Creates** | (lib re-exports) `Cli { genesis_path, network, chain_db_path, snapshot_store_path, listen_addr, peer_addrs }`, closed 3-variant `CliError` sum (`MissingGenesisPath` / `UnknownFlag(String)` / `FlagMissingValue(String)`), `NodeStartupInputs<'a, D: ChainDb + SnapshotStore, S: SnapshotStore, C: Clock + Send + 'static>` borrowing chaindb/snapshot_store/era_schedule/ledger_view (`Arc<dyn LedgerView + Send + Sync>`)/cadence/leadership_clock/leadership_anchor + optional genesis seed, `NodeShutdownEvidence` (returned on clean shutdown; used by the DC-NODE-04 integration test to assert byte-identical resume state), closed 3-variant `NodeRunError` sum (`Bootstrap(BootstrapError)` / `AuthorityFatal(OrchestratorError)` / `PersistentWriterIo(PersistentCacheError)`) with `fn exit_code(&self) -> i32`. `pub const EXIT_AUTHORITY_FATAL_IO: i32 = 10`, `pub const EXIT_AUTHORITY_FATAL_DECODE: i32 = 12`, `pub const EXIT_GENERIC_STARTUP: i32 = 1`. Function: `pub async fn run_node_until_shutdown(inputs, …) -> Result<NodeShutdownEvidence, NodeRunError>`. |
| **Interprets** | argv (CLI parser). OS signals (SIGINT/SIGTERM) translated by `main.rs` into `OrchestratorEvent::Shutdown` (this part is the honest-scope stub today; the signal-handler wiring will be exercised end-to-end once the socket pump lands). Authority-fatal kinds translated into deterministic exit codes via `NodeRunError::exit_code()`. |
| **MUST NOT** | (1) Bypass `ade_codec` to construct semantic types from bytes. (2) Modify `ade_ledger` or `ade_core::consensus` state in place — transitions route through the BLUE authority surface (`block_validity::*`, `tx_validity::*`, `mempool::admit`, `mempool::mempool_ingress`, `producer::forge::forge_block` + `producer::self_accept::self_accept`, `producer::served_chain::served_chain_admit`, `receive::admit_via_block_validity` + `receive::receive_apply`, `rollback::materialize_rolled_back_state` + `rollback::commit_rollback`, `snapshot::framing::{encode_snapshot, decode_snapshot}`, or `consensus::*` BLUE transitions). (3) Take an inbound dep from any other crate. (4) Read versions out of ambient context. (5) Construct an `OrchestratorState` outside `ade_runtime::orchestrator::state::OrchestratorState::new` (the constructor is the only path). (6) Reach the mempool other than through `mempool_ingress`. (7) `CE-NODE-N2C-LTX` forward-looking. (8) PP-S1 forward-looking. (9) PHASE4-N-C / CN-CONS-07 forward-looking. (10) PHASE4-N-G forward-looking. (11) PHASE4-N-H forward-looking. (12) PHASE4-N-I forward-looking (rollback context). (13) PHASE4-N-J — drive `PersistentSnapshotCache::new(&snapshot_store)` as the production restart-safe `SnapshotReader`; surface authority-fatal decode errors (`SnapshotDecodeError::UnknownVersion` / `FingerprintMismatch`) deterministically (NEW: DC-NODE-04 now closes this — both map to `AuthorityFatalKind::SnapshotDecodeUnknownVersion` / `…FingerprintMismatch` → `EXIT_AUTHORITY_FATAL_DECODE = 12`). **(14) NEW — PHASE4-N-K (CN-NODE-01 + DC-NODE-01 + DC-NODE-02 + DC-NODE-03 + DC-NODE-04):** `ade_node` MUST call `bootstrap_initial_state` exactly once across its production body — enforced by `ci/ci_check_node_binary_uses_single_bootstrap.sh` (the script counts `bootstrap_initial_state(` invocations in `crates/ade_node/src/*.rs` with cfg(test) stripped; result must equal 1). MUST NOT construct the initial `(LedgerState, PraosChainDepState, ChainTip)` triple via any second path. MUST instantiate exactly one `PersistentSnapshotWriter` per node run; the orchestrator routes `CaptureSnapshot` effects through it; the shutdown drain force-captures via `PersistentSnapshotWriter::force_capture` (DC-NODE-04 resume identity). MUST map every `NodeRunError` variant to a deterministic exit code via `exit_code()` (DC-NODE-04 — `binary_halts_on_authority_fatal_decode_error` integration test enforces this end-to-end). MUST NOT introduce any RED-color code in the GREEN orchestrator core (already enforced upstream by `ci/ci_check_orchestrator_core_purity.sh` — the binary consumes the core via the GREEN re-exports only). MUST consume time only via `Clock` (DC-NODE-03 — `SystemClock` is the production `Clock` impl; tests pass `DeterministicClock`). MUST NOT cross-mutate per-peer state (DC-NODE-01 — the binary holds per-peer state inside the `OrchestratorState::per_peer_{receive,server}` BTreeMaps; `ci/ci_check_peer_session_isolation.sh` proves the per-peer task structurally cannot bypass that). MUST surface `SnapshotDecodeError::UnknownVersion` / `FingerprintMismatch` at bootstrap as authority-fatal (DC-NODE-04 — `binary_halts_on_authority_fatal_decode_error`). MUST NOT bypass `should_snapshot_after_block` (DC-NODE-02 — `ci/ci_check_persistent_writer_no_parallel_cadence.sh`). |
| **Inbound deps** | None (binary). The lib re-exports are consumed by the two integration tests under `crates/ade_node/tests/`. |
| **Outbound deps** | `ade_types`, `ade_core`, `ade_ledger`, `ade_runtime`, `ade_network`, `ade_codec`, `tokio` (`net + rt + rt-multi-thread + io-util + macros + time + sync + signal`). Dev-deps: `ade_testkit`, `tempfile`. |
| **Entry points** | `main()` in `crates/ade_node/src/main.rs` (thin shim). `ade_node::run_node_until_shutdown` is the library entry the integration tests + future operator-action binaries drive in-process. `ade_node::{Cli, CliError, NodeStartupInputs, NodeShutdownEvidence, NodeRunError, EXIT_AUTHORITY_FATAL_IO, EXIT_AUTHORITY_FATAL_DECODE, EXIT_GENERIC_STARTUP}`. |

> **Gap surfaced (NEW from PHASE4-N-K, honest-scope).** The orchestrator core + bootstrap + persistent writer + leadership session + per-peer session shape + listening-socket pump are real and mechanically evidenced. The actual Ouroboros mux + handshake driver above `MuxTransport::read_raw`/`write_raw` that would feed real chain-sync / block-fetch frames into the orchestrator inbox is operator-action work tracked by RO-LIVE-01 / RO-LIVE-02. The binary today, when launched, performs the CLI parse + an honest-scope readiness print and runs idle until SIGTERM — matching the `live_block_follow_session` honest-stub pattern. Mechanical evidence for DC-NODE-01 is provided by the orchestrator-core dispatch tests + the `orchestrator_peer_isolation.rs` integration tests against the pure reducer. Mechanical evidence for DC-NODE-04 is provided by the in-process `shutdown_resume_identity.rs` integration test which exercises the full bootstrap → orchestrator → drain → bootstrap cycle on a Conway 576 corpus block, plus `authority_fatal_decode.rs` which proves the no-silent-retry property for `SnapshotDecodeError::UnknownVersion` / `FingerprintMismatch`.

---

### `ade_runtime`

> **Status change at HEAD `1946573` + PHASE4-N-K worktree (PHASE4-N-K S1–S6 — two new GREEN top-level files `bootstrap.rs` + `clock.rs`, one new GREEN file inside the existing `rollback/` sub-tree `persistent_writer.rs`, and a new mixed-color sub-tree `orchestrator/` with 4 GREEN files + 3 RED files).** Between `f15102f` and HEAD, `ade_runtime` gained: `bootstrap.rs` (429 LOC, GREEN), `clock.rs` (223 LOC, GREEN trait + GREEN `DeterministicClock` + RED `SystemClock` sub-classified), `orchestrator/mod.rs` (28 LOC, GREEN barrel), `orchestrator/event.rs` (130 LOC, GREEN), `orchestrator/state.rs` (88 LOC, GREEN), `orchestrator/core.rs` (791 LOC, GREEN — pure `step` reducer with 12 inline tests), `orchestrator/peer_session.rs` (230 LOC, RED — tokio per-peer task), `orchestrator/leadership_session.rs` (178 LOC, RED — tokio slot-tick pump), `orchestrator/n2n_server_pump.rs` (191 LOC, RED — tokio listening-socket spawner), `rollback/persistent_writer.rs` (245 LOC, GREEN — cadence-disciplined `PersistentSnapshotWriter`). `rollback/mod.rs` extended with `pub mod persistent_writer;` + `pub use persistent_writer::PersistentSnapshotWriter;`. Cargo.toml gained explicit `tokio` dep with `signal` added to the existing feature set. `src/lib.rs` extended with `pub mod {bootstrap, clock, orchestrator};`. Two new integration test files under `crates/ade_runtime/tests/`: `orchestrator_peer_isolation.rs` (3 tests) + `orchestrator_replay_equivalence.rs` (2 tests).

| Attribute | Value |
|-----------|-------|
| **Purpose** | The imperative shell. Hosts (a) the `ChainDb` / `SnapshotStore` storage abstractions (N-D); (b) the GREEN chain-selector orchestrator + GREEN `CandidateFragment` materializer (N-B); (c) the RED genesis-text parser (N-B); (d) the RED producer-side scaffolding (`producer/`); (e) the RED producer-side N2N server pump (`network/n2n_server.rs`, N-G); (f) the GREEN broadcast-to-served adapter (`producer/{broadcast_to_served, served_chain_lookups}.rs`, N-G); (g) the GREEN receive-side adapter pair + the RED receive-side N2N client orchestrator (`receive/`, N-H); (h) the GREEN rollback adapter trio (`rollback/{cadence, in_memory_cache, chaindb_block_source}.rs`, N-I); (i) the RED snapshot-write hook (`rollback/snapshot_writer.rs`, N-I); (j) the GREEN persistent snapshot cache (`rollback/persistent_cache.rs`, N-J); **(k) NEW — the GREEN single bootstrap authority (`bootstrap.rs`, N-K S1) — `bootstrap_initial_state` composing `materialize_rolled_back_state` + `PersistentSnapshotCache::nearest_le` (CN-NODE-01)**; **(l) NEW — the clock seam (`clock.rs`, N-K S1) — GREEN `Clock` trait + GREEN `DeterministicClock` + RED `SystemClock` (DC-NODE-03)**; **(m) NEW — the GREEN orchestrator core (`orchestrator/{mod, event, state, core}.rs`, N-K S2) — closed event/effect/state vocabulary + pure `step` reducer that composes all PHASE4-N-A..N-J BLUE authorities into one running node-shaped state machine**; **(n) NEW — the GREEN persistent-snapshot writer (`rollback/persistent_writer.rs`, N-K S3) — cadence-disciplined wrapper over `PersistentSnapshotCache::capture` (DC-NODE-02)**; **(o) NEW — the RED tokio runners (`orchestrator/{peer_session, leadership_session, n2n_server_pump}.rs`, N-K S4-S6) — per-peer task + slot-tick pump + listening-socket spawner**. The crate remains RED at the prefix level; the new GREEN files (`bootstrap.rs`, `clock.rs`, `orchestrator/{mod, event, state, core}.rs`, `rollback/persistent_writer.rs`) join the existing GREEN-by-file list; RED-by-file gains `orchestrator/{peer_session, leadership_session, n2n_server_pump}.rs` + `clock.rs::SystemClock` (file-scoped sub-classification). |
| **Creates** | Carry-forward (all N-J + earlier types). **NEW — PHASE4-N-K S1-S6 (GREEN):** `BootstrapInputs<'a, D, S>`, `BootstrapError` (closed 4-variant sum), `bootstrap_initial_state` function; `Clock` trait, `DeterministicClock`, `millis_to_slot` helper; `PeerId`, `OrchestratorEvent` (closed 8-variant), `PeerRole` (closed 2-variant), `OrchestratorEffect` (closed 5-variant), `PeerHaltReason` (closed 10-variant), `AuthorityFatalKind` (closed 3-variant), `OrchestratorError` (closed 1-variant), `OrchestratorState`, `PerPeerReceiveVersions`, `step` function; `PersistentSnapshotWriter<'a, S>`. **NEW — PHASE4-N-K S1, S4-S6 (RED):** `SystemClock`; `PeerInboundFrame`, `PeerInboundFrameKind` (closed 4-variant), `PeerSession`; `SlotEraAnchor`, `LeadershipSession<C>`; `PeerIdGenerator`. None of these are counted in the BLUE canonical-type total (`ade_runtime` is a RED-prefix crate). |
| **Interprets** | Block bytes for content-addressed storage. Genesis JSON. N-A `StreamInput` events. PHASE4-N-C/N-G/N-H/N-I/N-J carry-forward. **NEW — PHASE4-N-K:** `bootstrap` interprets `(chaindb.tip(), snapshot_slots)` to choose cold-start vs warm-start; `clock` interprets the OS wall clock (RED `SystemClock`) or a pre-loaded tick vector (GREEN `DeterministicClock`); `orchestrator::core::step` interprets pre-decoded `OrchestratorEvent`s and routes each to the right BLUE authority via the GREEN N-G/N-H dispatchers; `persistent_writer::on_admitted` interprets `(slot, block_no)` against the cadence policy; the RED runners interpret tokio I/O events (`mpsc::Receiver::recv`, `TcpListener::accept`, `Clock::next_tick`). |
| **MUST NOT** | (1)–(27) carry-forward (all prior crate-wide and per-sub-tree rules). **(28) NEW — `bootstrap` (CN-NODE-01, S1, GREEN):** the SOLE `pub fn` returning the initial state triple; cold-start vs warm-start are two branches of one function — never parallel paths; MUST NOT use `HashMap`/`HashSet`/`SystemTime`/`Instant`/`tokio`/`rand`; warm-start MUST route through `materialize_rolled_back_state` (positive grep). **(29) NEW — `clock` (DC-NODE-03, S1, GREEN trait + GREEN/RED impls):** SOLE `SystemTime::now()` / `Instant::now()` site in `ade_runtime` (`ci/ci_check_clock_seam.sh`); orchestrator core files contain none of those constructs; MUST NOT add a second `Clock` impl beyond the deterministic + system pair; MUST use integer arithmetic only in `millis_to_slot`. **(30) NEW — `orchestrator/{mod, event, state, core}` (S2, GREEN):** SOLE `pub fn step` in `ade_runtime`; MUST NOT import `tokio::*` in any core file (`ci/ci_check_orchestrator_core_purity.sh` + `ci/ci_check_clock_seam.sh`); MUST NOT use `SystemTime`/`Instant`/`rand::*`/`HashMap`/`HashSet`/`f32`/`f64` in any core file (same scripts); MUST NOT bypass `admit_via_block_validity` / `commit_rollback` / `framing::{encode,decode}_snapshot` — every authority call routes through the N-G/N-H/N-I/N-J dispatch wrappers; MUST NOT cross-peer mutate state — each peer's state lives in its own `BTreeMap` entry; MUST NOT silent-fallback on `UnknownVersion` / `FingerprintMismatch` — these map to `AuthorityFatalKind`; MUST NOT emit `CaptureSnapshot` without consulting `should_snapshot_after_block` first; MUST NOT surface `String` in any event/effect/state field; MUST NOT define a second `pub fn step` anywhere in `ade_runtime`; MUST NOT advance state after `shutdown_requested` is set. **(31) NEW — `rollback/persistent_writer` (DC-NODE-02, S3, GREEN):** MUST consult `should_snapshot_after_block` exclusively (`ci/ci_check_persistent_writer_no_parallel_cadence.sh`); MUST NOT define a second `SnapshotCadence`-like struct or a parallel "snapshot every N" constant; MUST construct snapshot bytes only via `PersistentSnapshotCache::capture` (CN-STORE-08 strengthening); MUST surface `PersistentCacheError::Store(ChainDbError::Io(_))` as authority-fatal (DC-NODE-04 — `EXIT_AUTHORITY_FATAL_IO=10`); MUST NOT update `last_capture` unless a capture actually succeeded. **(32) NEW — `orchestrator/peer_session` (DC-NODE-01, S4, RED):** MUST own its own `mpsc::Receiver`; MUST NOT import `ade_ledger::receive::*` or `ade_network::codec::*` directly (flows through orchestrator events); MUST NOT spawn additional tokio tasks (each peer task is one task, no fanout). **(33) NEW — `orchestrator/leadership_session` (DC-NODE-03 consumer, S5, RED):** consumes `Clock` via the trait surface; MUST NOT call `SystemTime::now()` directly (`ci/ci_check_clock_seam.sh`). **(34) NEW — `orchestrator/n2n_server_pump` (S6, RED):** deterministic-at-the-value-level per-id allocation via the monotonic `PeerIdGenerator`; honest-scope stub — actual mux session wiring is operator-action work per RO-LIVE-01/RO-LIVE-02. |
| **Inbound deps** | `ade_testkit` (non-dev), `ade_core_interop` (heavy). **NEW from PHASE4-N-K** — `ade_node` is a heavy new consumer (the lib+bin imports `ade_runtime::bootstrap::*`, `ade_runtime::chaindb::*`, `ade_runtime::clock::Clock`, `ade_runtime::orchestrator::{event, state, leadership_session, step}`, `ade_runtime::receive::ChainDbWriter`, `ade_runtime::rollback::{cadence::SnapshotCadence, persistent_writer::PersistentSnapshotWriter, PersistentCacheError}`); the 4 integration tests `orchestrator_peer_isolation.rs` + `orchestrator_replay_equivalence.rs` + `shutdown_resume_identity.rs` + `authority_fatal_decode.rs`. |
| **Outbound deps** | `ade_types`, `ade_core`, `ade_crypto`, `ade_codec`, `ade_ledger`, `ade_network`, `redb`, `serde_json`, `cardano-crypto`, `ed25519-dalek`, `tokio` (the `signal` feature is **NEW from PHASE4-N-K**; the existing N-G/N-H usage already pulled `net + rt + rt-multi-thread + io-util + macros + time + sync`). **No new outbound crate edge.** Dev-deps: `tempfile`, `ade_testkit`. |
| **Entry points** | All N-J + earlier surfaces. **NEW — PHASE4-N-K:** `ade_runtime::bootstrap::{bootstrap_initial_state, BootstrapInputs, BootstrapError}`; `ade_runtime::clock::{Clock, DeterministicClock, SystemClock, millis_to_slot}`; `ade_runtime::orchestrator::{step, OrchestratorState, OrchestratorEvent, OrchestratorEffect, OrchestratorError, PeerHaltReason, PeerId, PeerRole, AuthorityFatalKind, PerPeerReceiveVersions}`; `ade_runtime::orchestrator::peer_session::{PeerInboundFrame, PeerInboundFrameKind, PeerSession}`; `ade_runtime::orchestrator::leadership_session::{LeadershipSession, SlotEraAnchor}`; `ade_runtime::orchestrator::n2n_server_pump::PeerIdGenerator`; `ade_runtime::rollback::PersistentSnapshotWriter`. All re-exported at the appropriate module roots via `pub use`. |
| **Key modules** | All prior keys. **NEW — top-level:** `bootstrap.rs`, `clock.rs`. **NEW — `orchestrator/`:** `mod.rs`, `event.rs`, `state.rs`, `core.rs` (GREEN); `peer_session.rs`, `leadership_session.rs`, `n2n_server_pump.rs` (RED). **EXTENDED — `rollback/`:** one new file `persistent_writer.rs` (GREEN). |
| **Mechanical enforcement** | Carry-forward (chaindb / recovery / crash-safety + 8 N-C + 7 N-G + 5 N-H + 2 N-I + 1 N-J CI scripts). **NEW — six PHASE4-N-K CI scripts back the new orchestrator + bootstrap + clock + writer surface:** `ci_check_bootstrap_closure.sh` (S1; CN-NODE-01), `ci_check_clock_seam.sh` (S1; DC-NODE-03), `ci_check_orchestrator_core_purity.sh` (S2; DC-NODE-03 + general purity), `ci_check_persistent_writer_no_parallel_cadence.sh` (S3; DC-NODE-02), `ci_check_peer_session_isolation.sh` (S4; DC-NODE-01), `ci_check_node_binary_uses_single_bootstrap.sh` (S7; CN-NODE-01 + DC-NODE-04). |

> **Gap surfaced (carried + extended).** **NEW from PHASE4-N-K** — the GREEN/RED partition inside the new `ade_runtime::orchestrator/` sub-tree is gated mechanically for the 4 GREEN core files (`ci/ci_check_orchestrator_core_purity.sh`) and the peer-session RED file (`ci/ci_check_peer_session_isolation.sh`); the leadership-session + server-pump RED files are gated indirectly via `ci/ci_check_clock_seam.sh` (forbidding their use of `SystemTime::now()` outside `clock.rs`). The broader partition rests on file-header banners + review. **NEW — `clock.rs` is intentionally single-file** despite hosting both GREEN and RED — splitting would weaken the single-sited-seam property gated by `ci/ci_check_clock_seam.sh`; the RED sub-classification of `SystemClock` is documented in the file header and never reachable from BLUE code paths. **NEW — no live operator-action binary for the orchestrator-driven path** — the `ade_node` binary today performs CLI parse + readiness print and runs idle; the actual N2N peer dialer + mux session driver above `MuxTransport::read_raw/write_raw` is operator-action work per RO-LIVE-01 / RO-LIVE-02. Mechanical evidence for the orchestrator's correctness comes from the 4 new integration tests + the 5 new narrow CI scripts + the bootstrap-closure script. DC-NODE-04 closes mechanically via the in-process `shutdown_resume_identity` test even before the socket pump lands; the rule is independent of the operator-action half.

---

## Cross-Module Rules (project-wide)

### Dependency direction

`ade_core_interop` → `{ade_core, ade_codec, ade_crypto, ade_ledger, ade_runtime, ade_network, ade_testkit, ade_types, tokio}` is legal (RED leaf binary). **No new crate-level outbound edge in PHASE4-N-K.**
`ade_runtime` → `{ade_core, ade_crypto, ade_codec, ade_types, ade_ledger, ade_network, redb, serde_json, cardano-crypto, ed25519-dalek, tokio}` is legal (RED → BLUE). **PHASE4-N-K added the `signal` feature to the existing `tokio` dep; no new crate edge.**
`ade_node` → `{ade_types, ade_core, ade_ledger, ade_runtime, ade_network, ade_codec, tokio}` is legal (RED → BLUE/GREEN). **PHASE4-N-K introduced this entire edge set — `ade_node` was previously a stub with zero outbound workspace edges.** Dev-deps: `ade_testkit`, `tempfile`.
`ade_testkit` → `{ade_core, ade_ledger, ade_plutus, ade_runtime, ade_crypto, ade_codec, ade_types, cardano-crypto}` is legal (GREEN). **PHASE4-N-K added `ade_node` as a new dev-dep consumer.**
`ade_network` (BLUE submodules) → `{ade_codec, ade_types}` is legal.
`ade_network` (RED submodules + capture bins) → `{tokio, ade_codec, ade_types, ade_network::codec::*, ade_network::mux::frame}` is legal.
`ade_ledger` → `{ade_core, ade_plutus, ade_crypto, ade_codec, ade_types, minicbor}` is legal (BLUE among BLUEs).
`ade_core` → `{ade_types, ade_crypto, minicbor}` is legal (BLUE among BLUEs).
`ade_plutus` → `{ade_crypto, ade_codec, ade_types}` is legal.
`ade_crypto` → `{ade_types}` is legal.
`ade_codec` → `{ade_types}` is legal.
`ade_types` → `{}`.

**Forbidden directions.** Any BLUE crate (or BLUE `ade_network` submodule) depending on `ade_runtime`, `ade_node`, `ade_core_interop`, or the RED half of `ade_network` is a CI failure (`ci_check_dependency_boundary.sh` + `ci_check_no_async_in_blue.sh`). Any non-`ade_plutus` crate referring to `pallas_*` is a CI failure (`ci_check_pallas_quarantine.sh`). Any reference to `ChainDb` / `chain_db` inside `crates/ade_core/src/consensus/` is a CI failure (`ci_check_no_chaindb_in_consensus_blue.sh`). All B1/B2/B3/B5/N-E/PP-S1/N-C/N-G/N-H/N-I/N-J narrow dependency notes carry forward. **NEW — PHASE4-N-K dependency notes (the 6 node-orchestrator CI gates pin specific properties):** any second `pub fn` in `bootstrap.rs` returning the initial state triple is a CI failure (`ci_check_bootstrap_closure.sh`, CN-NODE-01). Any `SystemTime::now()` / `Instant::now()` / `std::thread::sleep` outside `clock.rs` inside `ade_runtime` is a CI failure (`ci_check_clock_seam.sh`, DC-NODE-03). Any `SystemTime`/`Instant`/`tokio::time::*`/`tokio::spawn`/`rand::*`/`HashMap`/`HashSet`/float in any of the 4 orchestrator core files is a CI failure (`ci_check_orchestrator_core_purity.sh`). Any second `pub fn should_snapshot_after_block` or second `SnapshotCadence`-like struct anywhere is a CI failure (`ci_check_persistent_writer_no_parallel_cadence.sh`, DC-NODE-02). Any direct `ade_ledger::receive::*` or `ade_network::codec::*` import inside `peer_session.rs` is a CI failure (`ci_check_peer_session_isolation.sh`, DC-NODE-01). More than one `bootstrap_initial_state(` invocation across `crates/ade_node/src/*.rs` production body is a CI failure (`ci_check_node_binary_uses_single_bootstrap.sh`, CN-NODE-01 + DC-NODE-04). The GREEN orchestrator core (`orchestrator/{core, event, state, mod}.rs`) MUST NOT import `tokio::*` even though the broader `ade_runtime` crate now depends on `tokio` for the RED runner files.

### Naming convention

All crates are prefixed `ade_`. TCB color is not encoded in the crate name. The authoritative classifier is `.idd-config.json` `core_paths` plus the cluster doc TCB Color Maps for sub-crate scopes; CI scripts hard-code their BLUE list. The seven full-BLUE-scoped scripts use the full BLUE set: 6 BLUE crates + 9 BLUE `ade_network` paths. The B3, B5, OQ5, PP-S1, N-C (8), N-G (7), N-H (5), N-I (2), N-J (1) narrow scripts narrow as previously documented. **NEW — the 6 PHASE4-N-K scripts scope `ade_runtime` and `ade_node` only — none target BLUE crates:** `ci_check_bootstrap_closure.sh` scopes `crates/ade_runtime/src/bootstrap.rs`; `ci_check_clock_seam.sh` scopes `crates/ade_runtime/src/`; `ci_check_orchestrator_core_purity.sh` scopes the 4 files under `crates/ade_runtime/src/orchestrator/{core, event, state, mod}.rs`; `ci_check_persistent_writer_no_parallel_cadence.sh` scopes `crates/ade_runtime/src/rollback/` + `crates/ade_runtime/src/orchestrator/` + repo-wide for the second-`pub fn`-or-struct check; `ci_check_peer_session_isolation.sh` scopes `crates/ade_runtime/src/orchestrator/peer_session.rs`; `ci_check_node_binary_uses_single_bootstrap.sh` scopes `crates/ade_node/src/`. **No CI script scopes the BLUE crates in PHASE4-N-K** — the cluster's invariants are properties of the GREEN orchestrator + the binary wiring, not of any BLUE module.

### CI enforcement (61 scripts under `ci/`)

| Script | Enforces | Scope |
|---|---|---|
| `ci_check_admitted_block_closure.sh` *(PHASE4-N-H S1)* | CE-N-H-1 + CN-CONS-08 + CN-PROTO-07 | repo-wide grep gate + `crates/ade_ledger/src/receive/events.rs` |
| `ci_check_block_fetch_server_closure.sh` *(PHASE4-N-G S4)* | DC-CONS-17 foundation | `crates/ade_network/src/block_fetch/server.rs` |
| **`ci_check_bootstrap_closure.sh`** *(NEW — PHASE4-N-K S1)* | **CN-NODE-01 — exactly one `pub fn` returning the initial state triple in `bootstrap.rs`; no HashMap/wall-clock/tokio/rand in production body; positive grep that the warm-start branch calls `materialize_rolled_back_state`** | `crates/ade_runtime/src/bootstrap.rs` |
| `ci_check_broadcast_to_served_purity.sh` *(PHASE4-N-G S5)* | CE-N-G-5 | `crates/ade_runtime/src/producer/{broadcast_to_served,served_chain_lookups}.rs` |
| `ci_check_cbor_round_trip.sh` | T-ENC-03, DC-CBOR-01, DC-CBOR-02 | golden corpus |
| `ci_check_ce_n_a_5_proof.sh` *(PHASE4-N-A, S-A10)* | CE-N-A-5 5-condition evidence | `ade_network` (RED + real-capture corpus) |
| `ci_check_chaindb_contract.sh` | DC-STORE-02, DC-STORE-03, CN-STORE-04, CN-STORE-05 | `ade_runtime --lib chaindb::` (RED) |
| `ci_check_chaindb_crash_safety.sh` | T-REC-01 (crash variant), DC-STORE-01, CN-STORE-03 | `ade_runtime --test stress_kill_harness` (RED) |
| `ci_check_chain_sync_server_closure.sh` *(PHASE4-N-G S3)* | DC-PROTO-08 | `crates/ade_network/src/chain_sync/server.rs` |
| **`ci_check_clock_seam.sh`** *(NEW — PHASE4-N-K S1)* | **DC-NODE-03 — `clock.rs` is SOLE site of `SystemTime::now()` / `Instant::now()` in `ade_runtime`; orchestrator core files contain none of `SystemTime`/`Instant`/`tokio::time::*`/`tokio::spawn`/`rand::*`/`HashMap`/`HashSet`/floats** | `crates/ade_runtime/src/` (full subdir scan) + `crates/ade_runtime/src/orchestrator/{core,event,state,mod}.rs` (narrow) |
| `ci_check_consensus_closed_enums.sh` | DC-CONS-04, DC-CONS-10, T-DET-01, DC-VAL-02/04/05/06, DC-TXV-01/02/04/05, DC-MEM-01/02 | `consensus/` + `block_validity/` + `tx_validity/` + `mempool/` |
| `ci_check_constitution_coverage.sh` | invariant-registry ↔ code/test coverage | repo-wide |
| `ci_check_conway_cert_classification_closed.sh` *(PHASE4-B3F)* | DC-TXV-06 | `ade_types::conway::cert` + `ade_codec::conway::cert` + `ade_ledger::cert_classify` |
| `ci_check_credential_discriminant_closed.sh` *(OQ5)* | DC-LEDGER-10 | `ade_types::shelley::cert` + both era `ade_codec` decoders + `ade_ledger::{state, governance, fingerprint, rules}` + `ade_types::conway::governance` |
| `ci_check_crypto_vectors.sh` | crypto KAT regression | `ade_crypto` |
| `ci_check_dependency_boundary.sh` | T-BOUND-02 — BLUE ⇎ RED separation | full BLUE |
| `ci_check_deposit_param_authority.sh` *(PHASE4-B3)* | DC-TXV-07 | 6 BLUE crates |
| `ci_check_differential_divergence.sh` | DC-DIFF-* | replay outputs |
| `ci_check_forbidden_patterns.sh` | T-CORE-02 + `unsafe` allowlist | full BLUE |
| `ci_check_forge_purity.sh` *(PHASE4-N-C S3)* | DC-CONS-13/14/15 | `crates/ade_ledger/src/producer/forge.rs` |
| `ci_check_gov_cert_accumulation_closed.sh` *(PHASE4-B5)* | DC-LEDGER-09 | `ade_ledger::{gov_cert, rules, error, state}` |
| `ci_check_hash_uses_wire_bytes.sh` | DC-CBOR-02, T-ENC-01 | full BLUE |
| `ci_check_hfc_translation.sh` | DC-EPOCH-02 | `ade_ledger::hfc` |
| `ci_check_ingress_chokepoints.sh` | DC-INGRESS-01, T-INGRESS-01 | full BLUE |
| `ci_check_ledger_determinism.sh` | DC-LEDGER-01 | `ade_ledger` |
| `ci_check_mempool_ingress_closure.sh` *(PHASE4-N-E, S1)* | DC-MEM-03 | `ade_ledger::mempool/{ingress,admit,mod}.rs` + repo-wide `admit(` scan |
| `ci_check_mempool_ingress_replay.sh` *(PHASE4-N-E, S2 + S3)* | DC-MEM-04 | `ade_testkit::mempool/*` + `ade_testkit::lib.rs` + `ade_ledger::mempool::canonicalize` |
| `ci_check_module_headers.sh` | CE-04 contract banner | full BLUE |
| `ci_check_n2n_server_no_signing_dep.sh` *(PHASE4-N-G S6)* | Key-boundary | `crates/ade_runtime/src/network/` |
| `ci_check_no_async_in_blue.sh` *(PHASE4-N-A, S-A1)* | DC-CORE-01 | full BLUE |
| `ci_check_no_chaindb_in_consensus_blue.sh` *(PHASE4-N-B, S-B1)* | DC-CORE-01 + DC-CONS-07 | `crates/ade_core/src/consensus/` |
| **`ci_check_node_binary_uses_single_bootstrap.sh`** *(NEW — PHASE4-N-K S7)* | **CN-NODE-01 + DC-NODE-04 — `ade_node` calls `bootstrap_initial_state` exactly once across production body** | `crates/ade_node/src/` |
| `ci_check_no_density_in_fork_choice.sh` *(PHASE4-N-B, S-B8)* | DC-CONS-03 | `fork_choice.rs` + `candidate.rs` |
| `ci_check_no_float_in_consensus.sh` *(PHASE4-N-B, S-B1)* | T-CORE-02 + DC-CONS-07/08/09 | `crates/ade_core/src/consensus/` |
| `ci_check_no_parallel_header_splitter.sh` *(PHASE4-N-G S1)* | DC-CONS-16 strengthening + DC-CONS-18 | repo-wide grep gate |
| `ci_check_no_private_keys_in_corpus.sh` *(PHASE4-N-C S3)* | DC-CRYPTO-03/04/05 corpus discipline | `crates/ade_testkit/src/producer/fixtures.rs` + `crates/ade_testkit/fixtures/producer/` |
| `ci_check_no_producer_body_encoder.sh` *(PHASE4-N-C S4)* | DC-CONS-16 | repo-wide grep gate |
| `ci_check_no_secrets.sh` | no credentials/IPs/keys | repo-wide |
| `ci_check_no_semantic_cfg.sh` | T-BUILD-01 | full BLUE |
| `ci_check_no_signing_in_blue.sh` | CE-05, T-KEY-01 | full BLUE |
| `ci_check_opcert_closed.sh` *(PHASE4-N-C S2)* | DC-CONS-11/12 | `crates/ade_codec/src/shelley/opcert.rs` + `crates/ade_core/src/consensus/opcert_validate.rs` |
| **`ci_check_orchestrator_core_purity.sh`** *(NEW — PHASE4-N-K S2)* | **DC-NODE-03 + general purity — 4 GREEN orchestrator core files contain no `SystemTime`/`Instant`/`tokio::time::*`/`tokio::spawn`/`rand::*`/`HashMap`/`HashSet`/`f32`/`f64`** | `crates/ade_runtime/src/orchestrator/{core,event,state,mod}.rs` |
| `ci_check_pallas_quarantine.sh` | O-29.2 | non-`ade_plutus` |
| **`ci_check_peer_session_isolation.sh`** *(NEW — PHASE4-N-K S4)* | **DC-NODE-01 — `peer_session.rs` owns own `mpsc::Receiver`; no direct `ade_ledger::receive::*` / `ade_network::codec::*` import; no fanout spawn** | `crates/ade_runtime/src/orchestrator/peer_session.rs` |
| **`ci_check_persistent_writer_no_parallel_cadence.sh`** *(NEW — PHASE4-N-K S3)* | **DC-NODE-02 — no second `pub fn should_snapshot_after_block`; no second `SnapshotCadence`-like struct; no hardcoded "snapshot every N" constant in any writer file** | `crates/ade_runtime/src/rollback/` + `crates/ade_runtime/src/orchestrator/` + repo-wide |
| `ci_check_private_key_custody.sh` *(PHASE4-N-C S1)* | DC-CRYPTO-03/04/05 + OP-OPS-04 | full BLUE + `crates/ade_runtime/src/producer/` |
| `ci_check_producer_corpus_present.sh` *(PHASE4-N-C S7)* | CN-CONS-06 mechanical half | `crates/ade_testkit/src/producer/fixtures.rs` |
| `ci_check_proposal_procedures_closed.sh` *(PROPOSAL-PROCEDURES-DECODE PP-S1)* | DC-LEDGER-11 | `conway/{governance,tx}.rs` paths + repo-wide `ProposalProcedure {` scan |
| `ci_check_receive_orchestrator_no_producer_dep.sh` *(PHASE4-N-H S4)* | Key-boundary | `crates/ade_runtime/src/receive/` |
| `ci_check_receive_paths_corpus_present.sh` *(PHASE4-N-H S6)* | CE-N-H-5 + CE-N-H-6 binary presence | N-H integration tests + binary src + procedure doc |
| `ci_check_receive_reducer_closure.sh` *(PHASE4-N-H S2)* | CE-N-H-2 + CN-CONS-08 + DC-CONS-19 | `crates/ade_ledger/src/receive/reducer.rs` |
| `ci_check_receive_replay_purity.sh` *(PHASE4-N-H S3)* | CE-N-H-3 + DC-PROTO-09 | `crates/ade_runtime/src/receive/{events_to_state,in_memory_chain_write}.rs` |
| `ci_check_recovery_contract.sh` | T-REC-01, T-REC-02, DC-STORE-05 | `ade_runtime --lib recovery::` (RED) |
| `ci_check_ref_provenance.sh` | DC-REF-01 | reference corpus |
| `ci_check_rollback_materialize_closure.sh` *(PHASE4-N-I S2)* | CN-STORE-07 + DC-CONS-22 | `crates/ade_ledger/src/rollback/materialize.rs` + `crates/ade_ledger/src/rollback/*.rs` |
| `ci_check_scheduler_closure.sh` *(PHASE4-N-C S6)* | DC-CONS-13 + DC-MEM-03 + OP-OPS-05 | `crates/ade_runtime/src/producer/{scheduler,broadcast,tick_assembler}.rs` |
| `ci_check_self_accept_gate.sh` *(PHASE4-N-C S5)* | CN-CONS-07 | `crates/ade_ledger/src/producer/self_accept.rs` + repo-wide `AcceptedBlock` literal scan |
| `ci_check_served_chain_closure.sh` *(PHASE4-N-G S2)* | CN-CONS-07 strengthening + CE-N-G-2 | `crates/ade_ledger/src/producer/served_chain.rs` |
| `ci_check_server_paths_corpus_present.sh` *(PHASE4-N-G S7)* | CE-N-G-7 + CE-N-G-8 binary presence | N-G integration tests + binary src + procedure doc |
| `ci_check_snapshot_cadence_purity.sh` *(PHASE4-N-I S4)* | DC-STORE-07 | `crates/ade_runtime/src/rollback/*.rs` |
| `ci_check_snapshot_encoder_closure.sh` *(PHASE4-N-J S7)* | CN-STORE-08 + DC-STORE-08 + DC-STORE-09 | `crates/ade_ledger/src/snapshot/{framing,ledger,chain_dep}.rs` + repo-wide |

> **Post-`f15102f` CI delta. +6.** The new gates are the PHASE4-N-K node-orchestrator gate set. **CI inventory 55 → 61.** The forward-looking `ci/ci_check_no_fail_open_in_validation.sh` (DC-VAL-06 / DC-TXV) is still **not** shipped — carried gap.

> **Carried residual gaps (unchanged at HEAD, plus the new N-K-introduced ones).** Carry-forward gaps from the prior CODEMAP are all still open. **(rr) NEW — `ade_node` honest-scope stub** — the binary today performs CLI parse + an honest-scope readiness print and runs idle; the actual N2N peer dialer + mux session driver above `MuxTransport::read_raw/write_raw` is operator-action work per RO-LIVE-01 / RO-LIVE-02. The orchestrator core + bootstrap + persistent writer + leadership session + per-peer session shape + listening-socket pump are real and mechanically evidenced; the live-binary wiring half remains the carried gap. **(ss) NEW — `clock.rs` hosts both GREEN (`Clock` trait + `DeterministicClock`) and RED (`SystemClock`) in one file** — gated by `ci/ci_check_clock_seam.sh` as the single-sited seam; splitting was rejected to keep the seam discoverable. **(tt) NEW — no live operator-action binary for the orchestrator-driven path** — `ade_node` is now the more natural host for the operator-action work (it has explicit `--peer` / `--listen` / `--snapshot-store` CLI flags); the existing `live_*_session` binaries continue as per-protocol witnesses, but the full orchestrator drive belongs to `ade_node` once the socket pump lands. **(uu) NEW — `DC-STORE-09` carries `open_obligation = "snapshot_schema_migration_follow_on_cluster"`** — DC-NODE-04 deliberately does NOT carry this; schema-migration tooling is a snapshot-format lifecycle concern, not a node-binary concern. **(vv) NEW — `RO-LIVE-01` / `RO-LIVE-02` / `CN-CONS-06` live-evidence halves remain `blocked_until_operator_peer_available`** — closing these is a separate operator-action cluster running `ade_node` against a private cardano-node peer.
