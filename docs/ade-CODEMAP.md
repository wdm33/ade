# Module Authority Map — Ade

> **Status:** Living architectural document. Regenerated; not hand-edited.
> Per-project instance of `~/.claude/methodology/templates/codemap.md`.

> 11 crates, 380 canonical types, 1365 tests, 31 CI checks at HEAD (`caa5ce8`).

---

## Conventions

- A **module** in Ade is a Cargo workspace crate (smallest independently-buildable unit). One exception: `ade_network` is split by *submodule color* — its BLUE submodules and its RED submodules are documented as two separate entries below, because `.idd-config.json` `core_paths` resolves BLUE at the submodule path level rather than crate-wide. The `ade_core::consensus` submodule sits *inside* the BLUE `ade_core` crate and is covered by that entry. The `ade_ledger::block_validity` / `ade_ledger::consensus_view` / `ade_ledger::tx_validity` / `ade_ledger::mempool::admit` / `ade_ledger::mempool::ingress` / `ade_ledger::cert_classify` modules sit inside the BLUE `ade_ledger` crate and are covered by that entry; the RED `ade_ledger::consensus_input_extract`, the GREEN `ade_ledger::mempool::policy`, and the GREEN `ade_ledger::mempool::canonicalize` sit inside the BLUE `ade_ledger` crate but carry a different color by their own module doc-comment and the cluster TCB Color Maps — they are surfaced as sub-classification notes inside the `ade_ledger` entry. The `ade_testkit::mempool` sub-tree (PHASE4-N-E S2) sits inside the GREEN `ade_testkit` crate, classified GREEN by its own module doc-comment and the cluster TCB Color Map.
- Modules are listed by TCB color (BLUE → GREEN → RED), alphabetical within each color.
- TCB color sources, in order of authority:
  1. `.idd-config.json` `core_paths` — substring match against absolute path. BLUE matches: `ade_codec`, `ade_types`, `ade_crypto`, `ade_core`, `ade_ledger`, `ade_plutus`, and the 9 `ade_network` submodule paths (`mux/frame.rs`, `codec/`, `handshake/`, `chain_sync/`, `block_fetch/`, `tx_submission/`, `keep_alive/`, `peer_sharing/`, `n2c/`).
  2. `.idd-config.json` `_core_paths_doc` — `ade_runtime` is RED; `ade_testkit` is GREEN; `ade_node` is RED; `ade_network::mux::transport` and `ade_network::session` are RED; `ade_network::lib` and `ade_network::mux::mod` are GREEN barrels. `ade_core_interop` is **RED** per its `Cargo.toml` header comment and the PHASE4-N-B TCB Color Map; the `ade_core_interop::tx_submission` (S4) and `ade_core_interop::local_tx_submission` (S5) modules carry a GREEN sub-classification by their own module doc-comments and the PHASE4-N-E TCB Color Map (deterministic pure-fn bridges). The new RED binary `ade_core_interop::bin::live_tx_submission_session` (S6) is the operator-action live half — RED by its own header comment ("RED — `live_tx_submission_session` binary. Operator evidence-capture pass for CE-N-E-6") and matches the established `live_consensus_session` pattern.
  3. `docs/clusters/PHASE4-N-B/cluster.md` § "TCB Color Map" — `era_schedule`, `praos_state`, `vrf_cert`, `nonce`, `op_cert`, `leader_schedule`, `header_validate`, `fork_choice`, `rollback` are BLUE; `chain_selector`, `candidate_fragment` are GREEN; `genesis_parser` is RED; `ade_testkit::consensus` is GREEN; `ade_core_interop` is RED.
  4. `docs/clusters/PHASE4-B1/cluster.md` § "TCB Color Map" — `ade_ledger::consensus_view`, `ade_ledger::block_validity` are BLUE; `ade_core::consensus::{header_validate, kes_check}` extensions are BLUE; `ade_testkit::validity` is GREEN; `ade_ledger::consensus_input_extract` is RED (parses an external dump format rather than a canonical type).
  5. PHASE4-B2 (tx-validity agreement) — `ade_ledger::tx_validity::{required_signers, witness, verdict, encoding, phase1, transition}` and `ade_ledger::mempool::admit` are **BLUE** (per their module doc-comments / `// Core Contract:` banners); `ade_ledger::mempool::policy` is **GREEN** (Tier-5, declared "GREEN (Tier-5)" in its own header); `ade_testkit::tx_validity::{extract, adversarial, valid_synthetic, mod}` is **GREEN**.
  6. PHASE4-B3 (full Conway tx value-conservation accounting) — all new and changed modules are **BLUE**, under the already-BLUE `ade_codec` / `ade_ledger` / `ade_types` crate prefixes (`.idd-config.json` `core_paths`).
  7. PHASE4-B5 (Conway governance-certificate accumulation) — all new and changed modules are **BLUE**, under the already-BLUE `ade_ledger` crate prefix.
  8. **`docs/clusters/completed/PHASE4-N-E/cluster.md` § "TCB Color Map"** (cluster archived at HEAD; see the archived-cluster note below) — `ade_ledger::mempool::ingress` is **BLUE** (S1; per its module doc-comment "the single BLUE chokepoint into `admit` from wire ingress"; gated by `ci/ci_check_mempool_ingress_closure.sh`); `ade_ledger::mempool::canonicalize` is **GREEN** (S3); `ade_testkit::mempool::ingress_replay` is **GREEN** (S2); `ade_core_interop::tx_submission` is **GREEN** (S4); `ade_core_interop::local_tx_submission` is **GREEN** (S5); **`ade_core_interop::bin::live_tx_submission_session` is RED (S6 — operator-action live evidence binary; matches `live_consensus_session` pattern)**. The S4/S5 modules sit physically inside the RED `ade_core_interop` crate; the GREEN sub-classification is review-and-doc-enforced. The S6 binary lives under `src/bin/` and is RED by file-level classification.

- **Archived cluster reference.** PHASE4-N-E was archived between the prior CODEMAP HEAD (`350130e`) and this HEAD (`caa5ce8`) — cluster docs are now under `docs/clusters/completed/PHASE4-N-E/` (10 files: `cluster.md`, `N-E-S1.md` through `N-E-S6.md`, `CE-N-E-6_PROCEDURE.md`, `CE-N-E-7_PROCEDURE.md`, **`CE-N-E-6_2026-05-25.log`**). All references to PHASE4-N-E cluster docs now resolve under the archive path. **Only `docs/clusters/PHASE4-N-B/` remains active** (the live consensus tip-agreement closure pending operator evidence).

- **Delta since prior CODEMAP HEAD `350130e` (this regeneration):**
  - **S6 (`d1068b3`, RED — new operator-action binary) — `live_tx_submission_session.rs`.** New file `crates/ade_core_interop/src/bin/live_tx_submission_session.rs` (552 LOC at HEAD; user-specified ~390 LOC at the S6 introduction commit). Sustained-window N2N tx-submission2 probe against a real cardano-node 11.x preprod relay. Two execution modes: a hermetic default `main` that prints a readiness banner and exits (drives the `#[ignore]` build-and-start closure-gate test), and a `--connect` live pass that performs the full handshake → tx-submission2 → BLUE state-machine validation loop and writes a transcript to `docs/clusters/PHASE4-N-E/CE-N-E-6_<date>.log`. The binary imports the GREEN N-E S4 surface (`ade_core_interop::tx_submission::{event_to_ingress, PeerAccumulator}`) plus BLUE network codecs and state machines (`ade_network::codec::tx_submission`, `ade_network::tx_submission::tx_submission2_transition`) and BLUE ledger types (`ade_ledger::mempool::{IngressSource, PeerId}`). A new `[[bin]]` stanza was added to `crates/ade_core_interop/Cargo.toml`.
  - **S6 closure-gate test (`d1068b3`).** New `crates/ade_core_interop/tests/live_tx_submission_session.rs` (80 LOC; 1 `#[test]`, `#[ignore]`-gated). Asserts the binary builds and starts and prints the readiness banner without opening a socket. Total tests under `crates/ade_core_interop/tests/` at HEAD: 7 (`tx_submission_ingress.rs`) + 8 (`local_tx_submission_ingress.rs`) + 6 (`follow_offline_replay.rs`) + 1 (`live_consensus_session.rs`) + 1 (`live_tx_submission_session.rs`) = 23.
  - **Binary correctness fix (`caa5ce8`).** Retry-on-`io::ErrorKind::TimedOut` for transient relay socket timeouts, plus an elapsed-time logging fix. No public-API change; no new types; no new test; no new CI script.
  - **Live evidence captured (`caa5ce8`) — CE-N-E-6 closure.** New `docs/clusters/completed/PHASE4-N-E/CE-N-E-6_2026-05-25.log` (9 lines, condensed transcript): real preprod cardano-node relay, N2N v15 handshake accepted, 1 tx-submission2 frame received from peer, BLUE `tx_submission2_transition` drove the protocol grammar correctly, 97-second session. This is the operator-action evidence that closes CE-N-E-6's live half (the deterministic code half closed at `ca3f23a` via the S4 GREEN bridge + 7 integration tests).
  - **Cluster archived (`350130e`, `caa5ce8`).** `docs/clusters/PHASE4-N-E/` moved to `docs/clusters/completed/PHASE4-N-E/` (10 files). Only PHASE4-N-B remains under `docs/clusters/` at HEAD.
  - **CE-N-E-7 deferred.** Live N2C evidence (`cardano-cli transaction submit` against an Ade-hosted UDS endpoint) requires Ade to host an N2C **server**, which is the future node-binary cluster's responsibility. Carried forward as cross-cluster obligation `CE-NODE-N2C-LTX` in the node-binary cluster's prerequisites. Procedure spec retained at `docs/clusters/completed/PHASE4-N-E/CE-N-E-7_PROCEDURE.md` as the canonical spec for future closure; no production library code is owed by PHASE4-N-E.

  **Structural deltas summarized:**
  - **NEW RED binary:** `ade_core_interop::bin::live_tx_submission_session` (S6). Operator-action evidence binary; matches the `live_consensus_session` pattern.
  - **NEW Cargo `[[bin]]` entry:** `crates/ade_core_interop/Cargo.toml` gains `name = "live_tx_submission_session"`, `path = "src/bin/live_tx_submission_session.rs"`. No new dependency; the binary uses crates already in the manifest.
  - **NEW `#[ignore]`-gated closure-gate test:** `crates/ade_core_interop/tests/live_tx_submission_session.rs` (1 test). **Test inventory 1364 → 1365.**
  - **NEW canonical types:** none. S6 added no public Rust types — it's a binary glue layer over the already-shipped S4 GREEN surface.
  - **NEW closed CI gates:** none. S6 ships no new CI script, consistent with the operator-evidence-log pattern established by CE-N-B-6 / `live_consensus_session.rs` (closure-gate test is `#[ignore]`-gated and closure is evidence-log driven). CI inventory stays at 31.

- Counts:
  - Crates: 11, from `Cargo.toml` `[workspace] members`. Unchanged.
  - Canonical types: 380, from `grep -rE "^(pub )?(struct|enum) "` across the full BLUE scope. Breakdown: `ade_codec` 8, `ade_types` 80, `ade_crypto` 12, `ade_core` 43, `ade_ledger` 131, `ade_plutus` 8, plus the 9 BLUE `ade_network` submodule paths 98. Registry `canonical_type_registry: null`, so a structural count is used. **Unchanged since `350130e`** — S6 added no public Rust types (binary glue only over the S4 GREEN surface).
  - Tests: 1365 — count of `#[test]` / `#[tokio::test]` attributes across `crates/`. Reported as approximate per the template's fallback rule (test runner not executed). Per-crate at HEAD: `ade_codec` 134, `ade_types` 23, `ade_crypto` 49, `ade_core` 119, `ade_ledger` 478, `ade_plutus` 28, `ade_testkit` 300, `ade_runtime` 33, `ade_network` 176, `ade_node` 0, `ade_core_interop` 25 (was 24 — +1 for the new `live_tx_submission_session.rs` closure-gate test). **+1 since `350130e`** — the S6 closure-gate test (`#[ignore]`-gated; hermetic build-and-start assertion). The new test is `#[ignore]`-gated so it does not run in default `cargo test`, but it is counted in the inventory per the methodology's mechanical grep rule.
  - CI checks: 31 — file count under `ci/ci_check_*.sh`. **Unchanged since `350130e`** — S6 ships no new CI script. Full inventory at HEAD: `cbor_round_trip`, `ce_n_a_5_proof`, `chaindb_contract`, `chaindb_crash_safety`, `consensus_closed_enums`, `constitution_coverage`, `conway_cert_classification_closed`, `credential_discriminant_closed`, `crypto_vectors`, `dependency_boundary`, `deposit_param_authority`, `differential_divergence`, `forbidden_patterns`, `gov_cert_accumulation_closed`, `hash_uses_wire_bytes`, `hfc_translation`, `ingress_chokepoints`, `ledger_determinism`, `mempool_ingress_closure`, `mempool_ingress_replay`, `module_headers`, `no_async_in_blue`, `no_chaindb_in_consensus_blue`, `no_density_in_fork_choice`, `no_float_in_consensus`, `no_secrets`, `no_semantic_cfg`, `no_signing_in_blue`, `pallas_quarantine`, `recovery_contract`, `ref_provenance`. The forward-looking `ci/ci_check_no_fail_open_in_validation.sh` (the `if len == K { check } else { skip }` grep gate, DC-VAL-06 / DC-TXV) is still **not** shipped. No `.github/workflows/` yet.

---

## BLUE Modules — Pure Functional Core

> **Shared header (applies to every BLUE entry below).** Every `.rs` source file begins with the contract banner
> `// Core Contract:` and the following deny attributes are present in each crate's `lib.rs` (or, for `ade_network`,
> at the crate root — the BLUE submodules inherit them):
> `#![deny(unsafe_code)]`, `#![deny(clippy::unwrap_used)]`, `#![deny(clippy::expect_used)]`,
> `#![deny(clippy::panic)]`, `#![deny(clippy::float_arithmetic)]`.
> CI scripts that enforce the shared rules (all 7 scope the full BLUE set — the 6 BLUE crates plus the 9 BLUE `ade_network` submodule paths declared in `.idd-config.json` `core_paths`):
> - `ci/ci_check_module_headers.sh` — banner first-line check.
> - `ci/ci_check_forbidden_patterns.sh` — `HashMap`, `HashSet`, `IndexMap`/`IndexSet`/`indexmap::`, `SystemTime`, `Instant`, `std::fs`, `std::net`, `tokio`, `async fn`, `f32`/`f64`, `anyhow`, `rand::thread_rng`, `thread::spawn`, plus `unsafe` outside a documented allowlist.
> - `ci/ci_check_dependency_boundary.sh` — no BLUE crate depends on a RED crate.
> - `ci/ci_check_no_signing_in_blue.sh` — `SigningKey`/`SecretKey`/`PrivateKey`/`private_key`/`sign_message`/`sign_block` forbidden in BLUE.
> - `ci/ci_check_no_semantic_cfg.sh` — `#[cfg(feature = ...)]` and `cfg!(feature = ...)` forbidden in BLUE.
> - `ci/ci_check_hash_uses_wire_bytes.sh` — no hashing of `canonical_bytes` / re-encoded bytes in BLUE.
> - `ci/ci_check_ingress_chokepoints.sh` — only named `decode_*` chokepoints construct `PreservedCbor`. Named-chokepoint list covers `decode_block_envelope`, the per-era block decoders, `decode_address`, and `PlutusScript::from_cbor` in `ade_plutus`.
> - `ci/ci_check_pallas_quarantine.sh` — `pallas-*` references confined to `ade_plutus`.
> - `ci/ci_check_no_async_in_blue.sh` *(PHASE4-N-A, S-A1)* — async constructs forbidden anywhere in the BLUE scope. Enforces DC-CORE-01.
>
> Three additional CI scripts narrow the shared header to the `ade_core::consensus` tree (PHASE4-N-B): `ci/ci_check_no_chaindb_in_consensus_blue.sh`, `ci/ci_check_no_float_in_consensus.sh`, `ci/ci_check_consensus_closed_enums.sh` (TARGETS scope: `crates/ade_core/src/consensus/`, `crates/ade_ledger/src/block_validity/`, `crates/ade_ledger/src/tx_validity/`, `crates/ade_ledger/src/mempool/`).
> A fourth narrow check enforces a single fork-choice rule: `ci/ci_check_no_density_in_fork_choice.sh` (DC-CONS-03).
> A fifth narrow check (PHASE4-B3) enforces deposit-parameter authority: `ci/ci_check_deposit_param_authority.sh` (DC-TXV-07).
> A sixth narrow check (PHASE4-B5) enforces gov-cert accumulation closure: `ci/ci_check_gov_cert_accumulation_closed.sh` (DC-LEDGER-09).
> A seventh narrow check (OQ5/COMMITTEE/DREP/ENACTMENT-COMMITTEE-FIDELITY + ENACTMENT-COMMITTEE-WRITEBACK) enforces credential-discriminant closure: `ci/ci_check_credential_discriminant_closed.sh` (DC-LEDGER-10).
>
> Two checks narrow the shared header to the wire-ingress path of `ade_ledger::mempool` (PHASE4-N-E):
> - `ci/ci_check_mempool_ingress_closure.sh` (DC-MEM-03, S1) — five mechanical guards on the ingress chokepoint (closure, `IngressSource` 2-variant closedness, `accumulating` writer freeze, `admit(` caller restriction to `admit.rs` + `ingress.rs`, source-invariance of `mempool_ingress`).
> - `ci/ci_check_mempool_ingress_replay.sh` (DC-MEM-04, S2 + S3) — six mechanical guards on the replay harness + canonicalizer (harness shape, `mempool_ingress` (not `admit`) usage, required test names, single-step shape, canonicalizer existence and pure-fn shape).
>
> A BLUE crate or BLUE `ade_network` submodule that adds a feature flag, an async function, a `HashMap`, or a RED dep fails CI on push.
> The 3 RED-scope CI scripts (`ci_check_chaindb_contract.sh`, `ci_check_recovery_contract.sh`, `ci_check_chaindb_crash_safety.sh`) and the 1 evidence script `ci_check_ce_n_a_5_proof.sh` are not part of this shared header — they are documented in the cross-module CI matrix at the bottom.

---

### `ade_codec`

> **Status carried from prior CODEMAP — unchanged at HEAD `caa5ce8`.** No change since `350130e`. All B3/B3F/B4 + OQ5-CREDENTIAL-FIDELITY narrative carries forward (closed-grammar Conway decoders under `conway/`, owner-complete `decode_conway_certs`, `decode_drep`, deduplicated `decode_withdrawals`, trailing-byte rejection, discriminant-preserving `decode_stake_credential` in both eras).

| Attribute | Value |
|-----------|-------|
| **Purpose** | Owns Cardano-canonical CBOR ingress: the only place in the workspace that turns raw bytes into typed semantic values, with wire-byte preservation for every hash-bearing structure. |
| **Creates** | `PreservedCbor<T>`, `RawCbor`, `BlockEnvelope`, `ByronDecodedBlock`, `CodecContext`, `CodecError` (incl. `UnknownCertTag`, `DuplicateMapKey`, `TrailingBytes`), `ContainerEncoding`, `IntWidth`, plus era-tagged block/tx wrappers under `byron/`, `shelley/`, `allegra/`, `mary/`, `alonzo/`, `babbage/`, `conway/`. Functions (not types): `conway::cert::{decode_conway_certs, decode_drep}`, `conway::withdrawals::{decode_withdrawals, withdrawals_sum}`, `shelley::cert::read_pool_registration_cert`, both era `decode_stake_credential`. |
| **Interprets** | All canonical Cardano CBOR — block envelopes, era-specific blocks, tx bodies, tx outs, certificates, addresses. Conway certificate array (closed over CDDL tags 0..18, owner-complete) and Conway withdrawals map (deduplicated). Both era credential decoders preserve the key/script tag. Sole authority for `PreservedCbor::new` (constructor is `pub(crate)`). |
| **MUST NOT** | (1) Construct `PreservedCbor` outside `ade_codec` (enforced by `pub(crate)` + `ci_check_ingress_chokepoints.sh`). (2) Re-encode wire bytes when computing hashes (`ci_check_hash_uses_wire_bytes.sh`). (3) Use any forbidden BLUE pattern. (4) Depend on any other workspace crate except `ade_types`. (5) **`conway::cert` (DC-LEDGER-08):** silently accept an unknown or removed certificate tag — tags ≥19 reject with `CodecError::UnknownCertTag`, tags 5/6 decode to `ConwayCert::RemovedInConway`; no catch-all arm; trailing bytes after the cert array reject (`CodecError::TrailingBytes`); preallocation bounded by remaining input; owner payloads retained; `decode_drep` is closed (no catch-all). (6) **`conway::withdrawals`:** never last-wins on a duplicate `RewardAccount` key — `CodecError::DuplicateMapKey`. (7) **`shelley::cert` / `conway::cert` `decode_stake_credential` (DC-LEDGER-10):** must not erase the credential tag — discriminant is read and mapped to `StakeCredential::{KeyHash, ScriptHash}`; unknown tag rejects, never tag-discard, never bare-hash coercion (enforced by `ci_check_credential_discriminant_closed.sh`). |
| **Inbound deps** | `ade_ledger`, `ade_plutus`, `ade_testkit`, `ade_network`, `ade_runtime`, `ade_core_interop`. |
| **Outbound deps** | `ade_types`. No external dependencies; std-only. Dev-deps: `serde_json`, `toml`. |
| **Entry points** | `ade_codec::cbor::envelope::decode_block_envelope` (31 import sites), `ade_codec::cbor`, `ade_codec::traits::AdeEncode`, `ade_codec::CodecContext`, per-era `decode_*_block` functions, `ade_codec::address::decode_address`. B2: `ade_codec::conway::tx::decode_conway_tx_body`. B3: `ade_codec::conway::cert::decode_conway_certs` and `ade_codec::conway::withdrawals::{decode_withdrawals, withdrawals_sum}`. B4: owner-complete `decode_conway_certs`; `ade_codec::shelley::cert::decode_certificates`. |
| **Key modules** | `cbor/`, `byron/`, `shelley/` (incl. `cert.rs`'s shared `pub(crate) read_pool_registration_cert`), `allegra/`, `mary/`, `alonzo/`, `babbage/`, `conway/` (incl. `cert.rs` owner-complete + `decode_drep` + discriminant-preserving `decode_stake_credential`; `withdrawals.rs`), `address/`, `preserved.rs` (`PreservedCbor`), `traits.rs`, `primitives.rs`, `error.rs`. |

---

### `ade_core`

> **Status carried from prior CODEMAP — unchanged at HEAD `caa5ce8`.** PHASE4-N-E S6 did not touch `ade_core`.

| Attribute | Value |
|-----------|-------|
| **Purpose** | BLUE authoritative Praos consensus core. Owns canonical types and pure state-transitions that decide which header / chain Ade accepts: HFC era schedule + slot↔era↔time translation, Praos chain-dep state (nonce / op-cert counters), VRF cert verification (TPraos two-proof **and** Praos single-proof) + leader-eligibility predicate, KES signature + op-cert period verification wired into header admission, header validation pipeline, fork-choice (`(BlockNo, TiebreakerView)`), rollback authority, leader-schedule query, and canonical encodings of all chain-dep state and chain events. |
| **Creates** | **Schedule:** `BootstrapAnchorHash`, `EraSchedule`, `EraSummary`, `EraLocation`. **State:** `PraosChainDepState`, `OpCertCounterMap`, `Nonce`. **Events/points:** `Point`, `ChainHash`, `BlockDistance`, `SecurityParam`, `ChainEvent`, `ChainSelectionReject`. **Errors:** `HFCError`, `SlotTimeError`, `OutsideForecastRange`, `HeaderValidationError`, `FieldError`, `FieldKind`, `VrfCertError`, `OpCertCounterError`, `NonceEvolutionError`, `LeaderScheduleError`. **Header surface:** `HeaderInput`, `HeaderVrf` (closed TPraos/Praos VRF discriminant), `HeaderKes`, `ValidatedHeaderSummary`, `HeaderApplied`. **Fork-choice:** `TiebreakerView`, `CandidateFragment`, `ChainSelectorState`, `ForkChoiceError`. **Op-cert/nonce:** `OpCertObservation`, `NonceInput`. **VRF:** `VrfRole`, `VerifiedVrf`, `StakeFraction`, `ActiveSlotsCoeff`. **Leader schedule:** `LeaderScheduleQuery`, `LeaderScheduleAnswer`. **Ledger view boundary:** `LedgerView` trait. **Rollback:** `RollBackRequest`, `RollBackApplied`. **Encoding:** `DecodeError`. 43 public types across the consensus tree. |
| **Interprets** | Canonical inputs from the `ade_runtime` shell — `HeaderInput` projections, `EraSchedule` materialized once from genesis JSON, `LedgerView` snapshots from `ade_ledger::consensus_view::PoolDistrView`, and an ordered stream of `StreamInput` events. KES check verifies the hot KES key signature over the header **body** CBOR bytes. All inputs are typed values; BLUE never reads raw bytes, files, sockets, or a chain store. Also decodes own canonically-encoded `PraosChainDepState` and `ChainEvent` for replay. |
| **MUST NOT** | (1) Take or accept a `&ChainDb` / `&chain_db` / any storage reference (`ci_check_no_chaindb_in_consensus_blue.sh`, DC-CORE-01 + DC-CONS-07). (2) Use any `f32`/`f64` (`ci_check_no_float_in_consensus.sh`); leader-eligibility predicate uses Q.123 fixed-point in `u128`. (3) Add `#[non_exhaustive]` to any consensus enum, define an open-tail `Other`/`Unknown`, hold owned `String` fields in `errors.rs`/`encoding.rs`/`events.rs`, or use `Box<dyn ...>` (`ci_check_consensus_closed_enums.sh`, DC-CONS-04 + DC-CONS-10 + T-DET-01). (4) Reference `density` in `fork_choice.rs`/`candidate.rs` outside a `// no-density:` annotation (`ci_check_no_density_in_fork_choice.sh`, DC-CONS-03). (5) Read wall-clock time. (6) Use `HashMap`/`HashSet`; `OpCertCounterMap` uses `BTreeMap`. (7) `async fn`, `.await`, `tokio` (`ci_check_no_async_in_blue.sh`). (8) Construct a `PreservedCbor`. (9) Re-derive stake snapshots — consume `LedgerView` only (DC-CONSENSUS-02). (10) Bypass the canonical `validate_and_apply_header` pipeline. (11) **B1 fail-closed rule (DC-VAL-06):** never apply a fixed-size crypto-field check via `if len == K { check } else { skip }`. (12) **B1 KES rule (DC-CRYPTO-01):** never accept a header whose KES signature or op-cert period fails; `verify_header_kes` is fail-closed. (13) All shared-header BLUE rules. |
| **Inbound deps** | `ade_ledger` (B1 — `block_validity` calls `validate_and_apply_header`), `ade_runtime` (consensus orchestrator + genesis parser), `ade_testkit`, `ade_core_interop`. |
| **Outbound deps** | `ade_types`, `ade_crypto` (`blake2b`, `verify_vrf`, `kes::{verify_kes, verify_opcert, build_opcert_signable}`), `minicbor`. Dev-deps: `ade_testkit`, `serde_json`, `cardano-crypto` (vrf-draft03; tests only). |
| **Entry points** | `use ade_core::consensus::{...}` aggregator, `ade_core::consensus::ledger_view::LedgerView`, `ade_core::consensus::vrf_cert::{vrf_input, verify_praos_vrf, praos_vrf_input, praos_leader_value, praos_nonce_value}`, `ade_core::consensus::kes_check::{verify_header_kes, expect_size}`, `ade_core::consensus::praos_state::{Nonce, PraosChainDepState}`, `ade_core::consensus::header_summary::{HeaderInput, HeaderVrf, HeaderKes, ValidatedHeaderSummary}`. Top-level transitions: `validate_and_apply_header`, `select_best_chain`, `apply_rollback`, `apply_nonce_input`, `apply_op_cert`, `query_leader_schedule` + `is_leader_for_vrf_output`, `verify_vrf_cert` + `verify_praos_vrf` + `check_leader_claim` + `is_leader`, `tiebreaker_prefer`, `encode/decode_chain_dep_state`, `encode/decode_chain_event`. |
| **Key modules** | `consensus/era_schedule.rs`, `consensus/praos_state.rs`, `consensus/events.rs`, `consensus/errors.rs`, `consensus/vrf_cert.rs`, `consensus/kes_check.rs`, `consensus/nonce.rs`, `consensus/op_cert.rs`, `consensus/leader_schedule.rs`, `consensus/header_summary.rs` + `consensus/header_validate.rs`, `consensus/candidate.rs`, `consensus/fork_choice.rs`, `consensus/rollback.rs`, `consensus/ledger_view.rs`, `consensus/encoding.rs`. |

---

### `ade_crypto`

> **Status carried from prior CODEMAP — unchanged at HEAD `caa5ce8`.**

| Attribute | Value |
|-----------|-------|
| **Purpose** | Owns pure cryptographic verification primitives — Blake2b hashing, Ed25519 / Byron-bootstrap signature verification, KES verification with operational certificates, VRF verification. Verification only — signing lives in `ade_runtime`. |
| **Creates** | `Blake2b224`, `Blake2b256`, `HashAlgorithm` trait, `Ed25519VerificationKey`, `Ed25519Signature`, `ByronExtendedVerificationKey`, `KesVerificationKey`, `KesPeriod`, `OperationalCertData`, `VrfVerificationKey`, `VrfProof`, `VrfOutput`, `CryptoError`. |
| **Interprets** | Verification key / signature / proof byte structures. Not a CBOR parser — accepts already-decoded byte slices. |
| **MUST NOT** | (1) Implement signing (`ci_check_no_signing_in_blue.sh`). (2) Allocate global state — verification is `fn(&[u8], &[u8]) -> bool`. (3) Use any BLUE forbidden pattern. (4) Use `unsafe` outside the allowlisted FFI in `src/vrf.rs`. (5) **B1:** `build_opcert_signable` must produce the spec-correct **raw concatenation** of the op-cert fields, not a CBOR-wrapped form. |
| **Inbound deps** | `ade_core`, `ade_ledger`, `ade_plutus`, `ade_testkit`, `ade_core_interop`. |
| **Outbound deps** | `ade_types`, `blake2`, `ed25519-dalek`, `cardano-crypto` (vrf-draft03 + kes-sum + dsign features, `default-features = false`). |
| **Entry points** | `ade_crypto::blake2b::blake2b_256` (top external), `blake2b_224`, `blake2b_256`, `block_header_hash`, `transaction_id`, `script_hash`, `credential_hash`, `verify_ed25519`, `verify_byron_bootstrap`, `verify_kes`, `verify_opcert`, `verify_vrf`. B2: `ade_crypto::{blake2b_224, verify_ed25519, Ed25519Signature, Ed25519VerificationKey}` consumed by `ade_ledger::tx_validity::witness::verify_required_witnesses`. `ade_crypto::vrf::*` consumed by `ade_core::consensus::vrf_cert`; `ade_crypto::kes::*` consumed by `ade_core::consensus::kes_check`. |

---

### `ade_ledger`

> **Status carried from prior CODEMAP — unchanged at HEAD `caa5ce8`.** PHASE4-N-E S6 did not touch `ade_ledger`. S6 is a binary glue layer over the already-shipped S1/S3 BLUE+GREEN surface (`mempool::{ingress, canonicalize}`) and consumes `ade_ledger::mempool::{IngressSource, PeerId}` as a downstream importer.
>
> **All prior cluster status notes carry forward unchanged.** PHASE4-N-E S1 (`mempool::ingress` BLUE chokepoint, DC-MEM-03) + S3 (`mempool::canonicalize` GREEN per-peer canonicalizer, DC-MEM-04). ENACTMENT-COMMITTEE-WRITEBACK (live committee writeback via `governance::apply_committee_enactment`, DC-EPOCH-01 + DC-LEDGER-10); ENACTMENT-COMMITTEE-FIDELITY / DREP-VOTE-FIDELITY / COMMITTEE-CRED-FIDELITY / OQ5-CREDENTIAL-FIDELITY (discriminated `StakeCredential` throughout the gov surface, DC-LEDGER-10); PHASE4-B5 (`gov_cert::apply_conway_gov_cert`, DC-LEDGER-09); PHASE4-B4 (`delegation::apply_conway_cert` owner-tagged, DC-LEDGER-08); PHASE4-B3 (`cert_classify`, full Conway value-conservation, deposit-param authority, DC-TXV-06/07 + T-CONSERV-01); PHASE4-B2 (`tx_validity`, `mempool::admit` BLUE, `mempool::policy` GREEN, DC-TXV-01..05 + DC-MEM-01/02); PHASE4-B1 (`block_validity` + `consensus_view`, DC-VAL-02..06).

| Attribute | Value |
|-----------|-------|
| **Purpose** | The functional core (ledger half): stateless ledger rules for every Cardano era — UTxO transitions, certificate effects, fee/value conservation, epoch boundary accounting, hard-fork translations, governance ratification/enactment, canonical fingerprint of `LedgerState`; the B1 top-level **block-validity** verdict; the B2 top-level **transaction-validity** verdict (`tx_validity`, DC-TXV-01..05) plus the **mempool admission** gate (`mempool::admit`, DC-MEM-01); the **full Conway value-conservation accounting** (`cert_classify` + `pparams`/`state` deposit-param authority + `conway::check_conway_coin_conservation`, DC-TXV-06/07 + T-CONSERV-01); the **closed, total, era-versioned Conway cert-state accumulation** (`delegation::apply_conway_cert`, DC-LEDGER-08); the **closed, total, era-versioned Conway governance-certificate accumulation** (`gov_cert::apply_conway_gov_cert`, DC-LEDGER-09); the live committee-enactment write-back (DC-EPOCH-01); and the **single BLUE chokepoint `mempool::ingress::mempool_ingress` from wire ingress into `admit` (DC-MEM-03)** — the only sanctioned production path from typed `IngressEvent`s into the mempool. |
| **Creates** | Carry-forward: `LedgerState`, `EpochState`, `UTxOState`, `TxOut`, `Value`, `MultiAsset`, `AssetName`, `DelegationState`, `CertState`, `PoolParams`, `ConwayGovState`, `GovActionState`, `RatificationResult`, `EnactmentEffects`, `ProtocolParameters`, `BlockApplyResult`, `BlockVerdict`, `TxVerdict`, `ScriptPosture`, `EpochBoundaryResult`, `LedgerFingerprint`, `Rational`, `WitnessInfo`, the layered `LedgerError` family. **B1 (`consensus_view`, `block_validity`):** `PoolDistrView`, `PoolEntry`, `BlockValidityOutcome`, `DecodedBlock`, `BlockValidityVerdict`, `BlockRejectClass`, `BlockValidityError`, `FieldError`, `FieldKind`, `MissingInput`, `VerdictSurface`, `SurfaceDecodeError`. **B2 (`tx_validity`, `mempool`):** `TxValidityVerdict`, `TxRejectClass`, `TxValidityError`, `TxValidityOutcome`, `TxVerdictSurface`, `TxSurfaceDecodeError`, `DecodedTx`, `VKeyWitnessRef`, `WitnessClosureError`, `WitnessField`, `SignerSource`, `RequiredSigners`, `RequiredSignerError`, `RequiredSignerField`, `ResolvedInputs`, `ResolvedOutput`, `MempoolState`, `AdmitOutcome` (BLUE); `OrderPolicy` (GREEN). **B3 (`pparams`, `error`):** `ConwayOnlyDepositParams` (post-B5: + `drep_activity`), `ConwayDepositParams`, `ValidationEnvironmentError` (incl. `MissingConwayDepositParams`; post-B5: + `MissingDRepActivityParam`, `DRepActivityOverflow`), `UnsupportedStateDependentDepositAccounting`, `EraInvalidCertificateError`. **B4 (`delegation`):** `ConwayCertAction`, `GovernanceOwner`, `GovernanceCertEffect`, `OwnerTaggedEffect`, `ConwayCertOutcome`, `ConwayCertEnv`. **B5 (`state`):** `GovCertEnv`. **PHASE4-N-E S1 (`mempool::ingress`):** `IngressSource` (closed 2-variant: `N2N`, `N2C`; no `#[non_exhaustive]`), `IngressEvent`. **PHASE4-N-E S3 (`mempool::canonicalize`):** `PeerId(pub Vec<u8>)`, `PeerSubmissionQueue { peer, source, txs }`. Functions: `cert_classify::classify`, `delegation::{apply_conway_cert, conway_cert_action}`, `gov_cert::apply_conway_gov_cert`, `governance::apply_committee_enactment`, `mempool::ingress::mempool_ingress` (S1), `mempool::canonicalize::canonicalize_peer_streams` (S3). |
| **Interprets** | Decoded canonical types from `ade_codec`. Hosts per-era validation composers and the late-era witness decoder. **B1:** `block_validity::header_input::decode_block`; `block_validity::encoding::*`. **B2:** `tx_validity::phase1::decode_tx` lifts the preserved Conway tx-body slice; `tx_validity::encoding::*` is the canonical per-tx replay/comparison surface. **B3:** `conway::check_conway_coin_conservation` decodes the cert array via `ade_codec::conway::cert::decode_conway_certs`, classifies via `cert_classify::classify`, decodes withdrawals via `ade_codec::conway::withdrawals::decode_withdrawals`, sums exactly (`i128`), balances the full equation. **B4:** `rules::accumulate_tx_certs` era-dispatches. **B5:** also feeds each owner-complete `ConwayCert` into `gov_cert::apply_conway_gov_cert` (when `gov_state.is_some()`). **ENACTMENT-COMMITTEE-WRITEBACK:** `rules::apply_epoch_boundary*` consumes `EnactmentEffects` and applies them via `governance::apply_committee_enactment`. **PHASE4-N-E S1 (`mempool::ingress`):** decodes nothing — `mempool_ingress` is a pure pass-through that forwards `event.tx_bytes()` to `admit`; the only interpretation is the closed `IngressSource` variant, which is **metadata only** and forbidden from changing the verdict. |
| **MUST NOT** | (1) Perform I/O. (2) Use `HashMap`, floats, or any other BLUE-forbidden pattern (`ci_check_forbidden_patterns.sh`). (3) Hash anything other than wire bytes (`ci_check_hash_uses_wire_bytes.sh`). (4) Construct `PreservedCbor` directly outside named decoders (`ci_check_ingress_chokepoints.sh`). (5) Depend on `ade_runtime` (`ci_check_dependency_boundary.sh`). (6) Use signing primitives or `#[cfg(feature)]` gates. (7) Omit `// Core Contract:` banner. (8) **B1 `consensus_view` (DC-CONSENSUS-02):** must not rederive a stake snapshot; `BTreeMap` only. (9) **B1 `block_validity` (DC-VAL-02/03):** must not produce `Valid` while skipping the header or body authority. (10) **B1 (DC-VAL-05):** invalid path returns unchanged input states plus structured error; no partial mutation. (11) **B1/B2 (DC-VAL-06):** every crypto-input/field-size check rejects on wrong size/shape; `if X.len() == K { check } else { skip }` is forbidden. (12) **B2 `tx_validity` (DC-TXV-01):** must be pure function of `(LedgerState, tx_cbor)`. (13) **B2 (DC-TXV-02):** never `Valid` while skipping phase-1 or (when Plutus present) phase-2. (14) **B2 (DC-TXV-04):** `Valid` yields applied `LedgerState'`; `Invalid` returns unchanged state + structured reason. (15) **B2 (DC-TXV-05):** `required_signers` is CLOSED, era-versioned. (16) **B2 `witness` (DC-VAL-06 / CN-LEDGER-09):** witness coverage is fail-closed. (17) **B2 `mempool::admit` (DC-MEM-01):** admit iff `tx_validity` is `Valid` — NO FALSE ACCEPT. (18) **B2 `mempool::policy` (GREEN, DC-MEM-02):** must not call `tx_validity`, touch accumulating state, or change any admit verdict. (19) **B3 `cert_classify` (DC-TXV-06):** must not guess a state-dependent deposit/refund; closed total over `ConwayCert`. (20) **B3 `pparams` / `state` (DC-TXV-07):** every deposit/refund amount sourced from canonical state. (21) **B3 `conway::check_conway_coin_conservation` (T-CONSERV-01 / CN-LEDGER-07):** full equation runs for every Conway tx with §9.1 reject precedence. (22) **B3 `fingerprint` (DC-LEDGER-01):** Conway deposit-param fold byte-identical when `conway_deposit_params == None`. (23) **B4 `delegation::apply_conway_cert` (DC-LEDGER-08):** must not reduce `ConwayCert` to 7-variant Shelley; owner-complete; removed tags 5/6 → `EraInvalidCertificate`. (24) **B4 `rules::accumulate_tx_certs` / `process_block_certificates` (DC-LEDGER-08):** must not swallow decode/apply errors. (25) **B5 `gov_cert::apply_conway_gov_cert` (DC-LEDGER-09):** must not mutate B4-owned `CertState`; closed `ConwayCert` match (no `_ =>`, `ci_check_gov_cert_accumulation_closed.sh`); env fail-fast `MissingDRepActivityParam`. (26) **B5 `rules` (DC-LEDGER-09):** must not observe-and-drop governance effects. (27) **OQ5 + COMMITTEE + DREP + ENACTMENT-COMMITTEE-FIDELITY/WRITEBACK (DC-LEDGER-10):** gov maps key on discriminated `StakeCredential`, never tag-erased `Hash28`; `apply_committee_enactment` present and called from `rules.rs` epoch boundary; structured 5-field `UpdateCommittee` in `fingerprint::write_gov_action` (enforced by `ci_check_credential_discriminant_closed.sh`). (28) **PHASE4-N-E S1 `mempool::ingress::mempool_ingress` (DC-MEM-03, N-E-N7 / N-E-8):** must not branch on `event.source` — the verdict is a function of `(state, tx_bytes)` alone; `IngressSource` is metadata only. `mempool_ingress(s, IngressEvent { source: N2N, b }) == mempool_ingress(s, IngressEvent { source: N2C, b })` for all `(s, b)`. Must not re-encode, normalize, or truncate `tx_bytes`. `MempoolState.accumulating` must not be written from any production source outside `mempool/admit.rs`. `admit()` must not be called from any production caller outside `mempool/admit.rs` (definition) or `mempool/ingress.rs` (the bridge); tests/ and benches/ are exempt. `IngressSource` must remain a closed 2-variant enum without `#[non_exhaustive]`. All five guards mechanically enforced by `ci/ci_check_mempool_ingress_closure.sh`. (29) **PHASE4-N-E S3 `mempool::canonicalize::canonicalize_peer_streams` (DC-MEM-04 multi-peer half):** must be pure; deterministic round-robin by sorted `PeerId` (byte-lex), tie-break by source byte tag (N2N=0, N2C=1); output is invariant to input iteration order; tx bytes pass verbatim. No `HashMap`/`HashSet`/`tokio::`/`async`/`SystemTime`/`Instant`/`rand`/`thread_rng`/`RwLock`/`Mutex` (enforced by `ci/ci_check_mempool_ingress_replay.sh` check 6). |
| **Inbound deps** | `ade_testkit`, `ade_core_interop` (S4/S5 import `mempool::*` and `state::LedgerState`; **S6's new binary `live_tx_submission_session` imports `ade_ledger::mempool::{IngressSource, PeerId}` as a downstream consumer** — does not add a new manifest edge, since the `ade_core_interop → ade_ledger` edge was added at S4). |
| **Outbound deps** | `ade_types`, `ade_crypto`, `ade_codec`, `ade_plutus`, `ade_core` (B1), `minicbor`, `num-bigint`, `num-integer`, `num-traits`. Dev-dep: `ade_testkit`. No new outbound crate edge in S6. |
| **Entry points** | `ade_ledger::rules::apply_block` (8 import sites), `apply_block_classified`, `apply_block_with_accounting`, `apply_block_with_verdicts`, `apply_conway_tx_to_utxo` (B2), `apply_epoch_boundary_full`, `ade_ledger::state::{LedgerState, EpochState}`, `ade_ledger::pparams::{ProtocolParameters, ConwayOnlyDepositParams, ConwayDepositParams}`, `ade_ledger::utxo::UTxOState`, `ade_ledger::hfc::translate_era`, `ade_ledger::fingerprint::fingerprint`, `ade_ledger::governance::*`, `ade_ledger::delegation::*`, `ade_ledger::epoch::*`, `ade_ledger::witness::*`. **B1:** `ade_ledger::block_validity::*`, `ade_ledger::consensus_view::{PoolDistrView, PoolEntry}`. **B2:** `ade_ledger::tx_validity::{...}`, `ade_ledger::mempool::{admit, MempoolState, AdmitOutcome, order, OrderPolicy}`. **B3:** `ade_ledger::cert_classify::classify`, `ade_ledger::conway::check_conway_coin_conservation`. **B4:** `ade_ledger::delegation::{apply_conway_cert, ...}`; `ade_ledger::rules::accumulate_tx_certs`. **B5:** `ade_ledger::gov_cert::apply_conway_gov_cert`; `ade_ledger::state::{GovCertEnv, LedgerState::gov_cert_env}`. **ENACTMENT-COMMITTEE-WRITEBACK:** `ade_ledger::governance::apply_committee_enactment`. **PHASE4-N-E:** `ade_ledger::mempool::{mempool_ingress, IngressEvent, IngressSource}` (S1; the BLUE chokepoint), `ade_ledger::mempool::{canonicalize_peer_streams, PeerId, PeerSubmissionQueue}` (S3; the GREEN canonicalizer). **S6's new binary imports `IngressSource` + `PeerId` for the live operator-action probe.** |
| **Key modules** | `byron.rs`, `shelley.rs`, `mary.rs`, `alonzo.rs`, `babbage.rs`, `conway.rs`, `rules.rs`, `state.rs`, `utxo.rs`, `delegation.rs`, `epoch.rs`, `governance.rs`, `hfc.rs`, `late_era_validation.rs`, `phase.rs`, `plutus_eval.rs`, `pparams.rs`, `rational.rs`, `scripts.rs`, `value.rs`, `witness.rs`, `fingerprint.rs`, `error.rs`, `gov_cert.rs` (B5), `consensus_view.rs` (B1, BLUE), `block_validity/{mod.rs, verdict.rs, encoding.rs, transition.rs, header_input.rs}` (B1, BLUE), `consensus_input_extract.rs` (B1, **RED**), `tx_validity/{mod.rs, required_signers.rs, witness.rs, verdict.rs, encoding.rs, phase1.rs, transition.rs}` (B2, BLUE), `mempool/{mod.rs, admit.rs (BLUE), policy.rs (GREEN), ingress.rs (BLUE, N-E S1), canonicalize.rs (GREEN, N-E S3)}`, `cert_classify.rs` (B3, BLUE). |

> **GREEN sub-classification (`ade_ledger::mempool::policy`).** Physically inside the BLUE `ade_ledger` crate but **GREEN (Tier-5)** per its own `// Core Contract:` header. Pure projection over already-admitted tx ids; never calls `tx_validity`, never touches accumulating state, cannot change an admit verdict (DC-MEM-02). **Enforcement note:** the BLUE/GREEN partition inside `ade_ledger::mempool` is *by module*, not mechanical — no CI script forbids `policy.rs` from importing `tx_validity`. Surfaced as a gap.

> **GREEN sub-classification (`ade_ledger::mempool::canonicalize`).** Physically inside the BLUE `ade_ledger` crate prefix but **GREEN** per its own module doc-comment ("pure function of the inputs; no I/O, no concurrency, no `HashMap`/`HashSet`, no clocks") and the PHASE4-N-E TCB Color Map (now archived under `docs/clusters/completed/`). Adversarial determinism guard `ci/ci_check_mempool_ingress_replay.sh` check 6 enforces the no-`HashMap`/no-async/no-clock shape at the source-text level; the broader BLUE/GREEN partition inside `ade_ledger::mempool` remains review-and-test-enforced.

> **RED sub-classification (`ade_ledger::consensus_input_extract`).** Physically inside the BLUE `ade_ledger` crate but **RED** per its own module doc-comment and the PHASE4-B1 TCB Color Map. Reached only by RED snapshot loader and GREEN corpus harness, never by `block_validity` or `tx_validity` — review-and-doc invariant, not mechanical.

> **All prior closure narratives unchanged at HEAD.** B3 closed-deposit-classification + deposit-param authority; B2 closed-enum properties; Conway value-conservation; B4 cert-state accumulation; B5 governance-cert accumulation; credential discriminant fidelity (DC-LEDGER-10); ENACTMENT-COMMITTEE-WRITEBACK live committee writeback (DC-EPOCH-01); DC-MEM-03 (closed ingress chokepoint; CI-enforced); DC-MEM-04 (single-step replay byte-identity + multi-peer canonicalizer determinism; CI-enforced). The Conway block-body vkey-witness closure is still not wired into the `rules.rs` block-body loop (carried adjacent gap).

---

### `ade_network` *(BLUE submodules)*

> **Status carried from prior CODEMAP — unchanged at HEAD `caa5ce8`.** Per-submodule BLUE scoping per `.idd-config.json` `_core_paths_doc`. The crate is split into its BLUE half (this entry) and its RED half (further down). Two submodules — `lib.rs` (the barrel) and `mux/mod.rs` (a re-export shim) — are GREEN and carry no authority. Slice provenance: cluster PHASE4-N-A, slices S-A1 through S-A10. PHASE4-N-E S6 did not modify any file under the 9 BLUE submodule paths; the new `live_tx_submission_session` binary consumes the already-shipped `InventoryEvent` (from `tx_submission/`), `TxSubmission2Message` / `tx_submission2_transition` / `TxSubmission2State` / `TxSubmission2Agency` / `TxSubmission2Output` (also from `tx_submission/`), `HandshakeMessage` + `encode_handshake_message` + `decode_handshake_message` + `VersionParams` + `VersionTable` (from `handshake/` and `codec/handshake/`), `N2NVersion` + `TxSubmission2Version` (from `codec/version/`), and the mux frame primitives (`encode_frame`, `MuxFrame`, `MuxHeader`, `MuxMode`, `MiniProtocolId`, `HEADER_LEN`) — all read-only from the bridge side.

| Attribute | Value |
|-----------|-------|
| **Purpose** | Owns the Cardano Ouroboros mini-protocol authority — the closed wire grammar (CBOR codecs) and pure state machines for all 11 N2N + N2C mini-protocols, plus the BLUE Ouroboros mux frame primitive. Encoders/decoders and transitions only; bytes-on-the-wire and effect emission belong to the RED half of the crate. |
| **Creates** | **Wire grammar (`codec/`):** `HandshakeMessage`, `N2cHandshakeMessage`, `ChainSyncMessage`, `BlockFetchMessage`, `TxSubmission2Message`, `KeepAliveMessage`, `PeerSharingMessage`, `LocalChainSyncMessage`, `LocalTxSubmissionMessage`, `LocalStateQueryMessage`, `LocalTxMonitorMessage`; closed payload types `VersionTable`/`VersionParams`/`N2cVersionTable`/`N2cVersionParams`, `RefuseReason`/`N2cRefuseReason`, `Point`, `Tip`, `Range`, `TxIdAndSize`, `TxAcceptance`, `TxRejection`, `KeepAliveCookie`, `PeerAddress`, `AcquireFailure`, `QueryPayload`, `ResultPayload`, `MempoolMeasures`/`MempoolSizeAndCapacity`/`MeasureSizeAndCapacity`/`MeasureName`; structured error taxonomy `CodecError` + `ProtocolKind`; typed per-protocol version newtypes. **State machines:** one closed `*State` / `*Agency` / `*Output` / `*Error` quad per protocol plus event/value carriers (incl. `InventoryEvent` consumed by the N-E S4 bridge + S6 binary and `LocalTxSubmissionEvent` consumed by the N-E S5 bridge). **Mux primitives (`mux/frame.rs`):** `MuxFrame`, `MuxHeader`, `MuxMode`, `MiniProtocolId`, `MuxError`. |
| **Interprets** | The on-wire CBOR of every Ouroboros mini-protocol message (closed grammar) and the 8-byte Ouroboros mux frame header. Block, header, and tx-body bytes inside RollForward / Block / Tx are *not* interpreted at this layer (opaque pass-through per DC-PROTO-06); LSQ Query/Result payloads also opaque. |
| **MUST NOT** | (1) `async fn`, `.await`, `tokio`, `async_std`, `futures::`, async-runtime spawn, or async timers in any file under the 9 BLUE paths (`ci_check_no_async_in_blue.sh`; DC-CORE-01). (2) Decode block / header / tx-body bytes — opaque `Vec<u8>` (DC-PROTO-06). (3) Construct a `PreservedCbor` outside `ade_codec`'s named chokepoints. (4) Add `#[non_exhaustive]` to any wire-message enum, introduce a generic `Codec<P>` trait, or use `dyn MessageHandler` (DC-PROTO-03/04). (5) Define a generic `Agency<P>` wrapper. (6) Read a selected version from ambient session state — explicit input to every transition (DC-PROTO-06). (7) Redefine `TxId` / `SlotNo` / `Hash32` / `CardanoEra` — use `ade_types`. (8) Depend on `pallas-network`. (9) Hold `String` in any error variant. (10) Use any other BLUE forbidden pattern. |
| **Inbound deps** | `ade_core_interop` (live consensus session binary, N-E S4/S5 bridges, **and new S6 `live_tx_submission_session` binary** — all RED → BLUE). At the library level no BLUE crate imports `ade_network`. The 7 capture binaries import the BLUE codecs internally; the 19 integration tests exercise the BLUE surface end-to-end. |
| **Outbound deps** | `ade_types`, `ade_codec` (for `CodecError::MalformedCbor`). No external deps in the BLUE submodules; the `tokio` line in `Cargo.toml` is confined to `mux/transport.rs` and the capture binaries (RED). |
| **Entry points** | **Codec** (11 protocols, each `encode_<protocol>_message` / `decode_<protocol>_message`); `ade_network::codec::primitives::*` (8 import sites); `ade_network::codec::version::{N2NVersion, TxSubmission2Version, ...}`; `ade_network::codec::{CodecError, ProtocolKind}`. **State machines:** `ade_network::handshake::{n2n_transition, n2c_transition, select_n2n_version, select_n2c_version, N2N_SUPPORTED, N2C_SUPPORTED}`; per-protocol `*_transition` functions (incl. `tx_submission2_transition` consumed by the new S6 binary). **N-E S4/S5/S6 consumers:** `ade_network::tx_submission::{InventoryEvent, tx_submission2_transition, TxSubmission2State, TxSubmission2Agency, TxSubmission2Output}` and `ade_network::n2c::local_tx_submission::LocalTxSubmissionEvent`. **Mux:** `ade_network::mux::frame::{encode_frame, decode_frame, MuxFrame, MuxHeader, MuxMode, MiniProtocolId, MuxError, HEADER_LEN, MAX_PAYLOAD}` (8 import sites). |
| **Key modules** | `codec/` (11 message-codec modules + `primitives.rs` + `version.rs` + `error.rs`); `handshake/`, `chain_sync/`, `block_fetch/`, `tx_submission/`, `keep_alive/`, `peer_sharing/`; `n2c/` (4 local-* submodules); `mux/frame.rs`. |

---

### `ade_plutus`

> **Status carried from prior CODEMAP — unchanged at HEAD `caa5ce8`.**

| Attribute | Value |
|-----------|-------|
| **Purpose** | Quarantine boundary between the Ade-canonical ledger and the ported UPLC evaluator from `aiken-lang/aiken` (pinned to tag `v1.1.21`, commit `42babe5d`). Exposes Ade-canonical types only; aiken- and pallas-originated types are strictly internal. |
| **Creates** | `PlutusScript`, `PlutusLanguage`, `EvalOutput`, `PlutusError`, `CostModels`, `DecoderMode`, `PerScriptResult`, `TxEvalResult`. |
| **Interprets** | UPLC scripts (Plutus V1/V2/V3) and `CostModels` CBOR. Phase-two transaction evaluation. `PlutusScript::from_cbor` is a named ingress chokepoint, allowlisted in `ci_check_ingress_chokepoints.sh` Check 3. |
| **MUST NOT** | (1) Re-export any `pallas_*` or `aiken_uplc::` type (`ci_check_pallas_quarantine.sh`). (2) Allow another BLUE crate to bypass the canonical entry. (3) Activate PV11 builtins. (4) Use any BLUE-forbidden pattern. (5) Construct `PreservedCbor` outside `ade_codec`. |
| **Inbound deps** | `ade_ledger`, `ade_testkit`. |
| **Outbound deps** | `ade_types`, `ade_crypto`, `ade_codec`, `aiken_uplc` (git, tag `v1.1.21`), `pallas-primitives` (internal-only). |
| **Entry points** | `ade_plutus::eval_tx_phase_two`, `ade_plutus::tx_eval::{SlotConfig, MAINNET_SLOT_CONFIG}`, `ade_plutus::evaluator::{programs_alpha_equivalent, EvalOutput, PlutusLanguage, PlutusScript}`, `ade_plutus::cost_model::{CostModels, DecoderMode, decode_cost_models}`. B2: `tx_validity` phase-2 dispatch reaches `ade_plutus` through `ade_ledger::plutus_eval::try_evaluate_tx`, not directly. |
| **Key modules** | `evaluator.rs`, `cost_model.rs`, `script_context.rs`, `script_verdict.rs`, `tx_eval.rs`. |

---

### `ade_types`

> **Status carried from prior CODEMAP — unchanged at HEAD `caa5ce8`.** PHASE4-N-E S6 did not touch `ade_types`. Crate stays at 80 canonical types; remains produce-only and closed.

| Attribute | Value |
|-----------|-------|
| **Purpose** | Owns the canonical Cardano domain vocabulary — primitives, era enum, address forms, era-tagged transaction bodies / outputs / certificates, governance types — used by every other workspace crate as the lingua franca. The schema, separated from the codec. |
| **Creates** | `CardanoEra`, `SlotNo`, `BlockNo`, `EpochNo`, `Hash28`, `Hash32`, `Coin`, `Lovelace`, `NetworkId`, `Nonce`, `TxIn`, `RewardAccount` (B3), `PoolId`, `Address`, `ByronAddress`, `Credential`, `StakeCredential` (OQ5: closed `enum { KeyHash, ScriptHash }` + `hash()` accessor), `Certificate`, `PoolRegistrationCert` (B4: `owners: Vec<Hash28>`), `ConwayCert` (B3 closed + B4 owner-complete), `CertDisposition` / `DepositEffect` / `CoinSource` (B3), `MIRCert`, `MIRPot`, `DRep`, `GovAction` (ENACTMENT-COMMITTEE-WRITEBACK: `UpdateCommittee` structured over discriminated `StakeCredential`), `GovActionState` (COMMITTEE/DREP: discriminated `committee_votes` + `drep_votes`), `GovActionId`, `Anchor`, `OperationalCert`, `NativeScript`, `PlutusV1Script`, `Datum`, `DatumOption`, `MultiAsset`, `AssetName`, `CostModel`, `ExUnits`, plus per-era tx-body / tx-out / witness wrappers (incl. `conway::tx::ConwayTxBody`) and Byron era block headers. |
| **Interprets** | None — produce-only. Domain types are constructed by `ade_codec` decoders or `ade_ledger` rules. |
| **MUST NOT** | (1) Construct or decode `PreservedCbor`. (2) Use any BLUE-forbidden pattern. (3) Depend on any workspace crate — root of the dependency DAG. (4) Add open/extensible variants to closed era / certificate / governance enums without a versioned gate; this applies to closed `ConwayCert` / `CertDisposition` / `DepositEffect` / `CoinSource`: no `#[non_exhaustive]`, no open-tail `Other`/`Unknown`; `ConwayCert::RemovedInConway { tag }` is an explicit closed marker. **B4 (DC-LEDGER-08):** owner-completion enrichment of `ConwayCert` and the `owners` field on `PoolRegistrationCert` must stay additive within the closed surface. **OQ5 (DC-LEDGER-10):** must not revert to tag-erasing tuple struct `StakeCredential(Hash28)` (enforced by `ci_check_credential_discriminant_closed.sh`). |
| **Inbound deps** | `ade_codec`, `ade_crypto`, `ade_ledger`, `ade_plutus`, `ade_runtime`, `ade_testkit`, `ade_network`, `ade_core`, `ade_core_interop`. |
| **Outbound deps** | None. |
| **Entry points** | `ade_types::CardanoEra`, `ade_types::tx::{Coin, TxIn, RewardAccount}`, `ade_types::{Hash32, SlotNo}`, `ade_types::Hash28`, `ade_types::conway::tx::ConwayTxBody`, `ade_types::conway::cert::{ConwayCert, CertDisposition, DepositEffect, CoinSource}`. |
| **Key modules** | `primitives.rs`, `era.rs`, `tx.rs`, `address/`, `byron/`, `shelley/`, `allegra/`, `mary/`, `alonzo/`, `babbage/`, `conway/` (B3 + B4 owner-complete; ENACTMENT-COMMITTEE-WRITEBACK: structured `GovAction::UpdateCommittee`). |

---

## GREEN Modules — Deterministic Glue

> Deterministic, non-authoritative. May depend on BLUE; must not affect authoritative outputs.

### `ade_testkit`

> **Status carried from prior CODEMAP — unchanged at HEAD `caa5ce8`.** PHASE4-N-E S6 did not touch `ade_testkit`. The S2 ingress-replay harness sub-tree (`mempool/`) and all prior B-track / B1 / B2 / B3 / B4 / B5 / OQ5 narrative carry forward verbatim.
>
> The post-3180e27 snapshot-loader follow-ups (168ac02 — `parse_with_origin_slot`, dual-layout `parse_registered_credentials`, fail-closed `parse_cold_credential_set`/`parse_cold_credential_epoch_map`), the c78ec76 `#[ignore]`-gated `emit_reward_provenance` corpus generator, the b9cfaf9 committee-oracle test, the 396664a corpus-alignment commits all carry forward in `harness/` and `tests/`.

| Attribute | Value |
|-----------|-------|
| **Purpose** | Test infrastructure: differential harness, oracle snapshot loader, regression corpus, genesis loader, era mapping, transcript replay, diff reporting; N-B consensus harness; B1 block-validity harness (`validity/`); B2 transaction-validity harness (`tx_validity/`); N-E S2 mempool-ingress replay harness (`mempool/`) that drives the B-track corpus through the BLUE `mempool_ingress` bridge. Not authoritative — produces comparisons of authoritative outputs against oracle/corpus data and serves the `replay_cmd` test suites. |
| **Creates** | **Harness (pre-N-B, selected):** `Era`, `OracleManifest`, `LoadedSnapshot`, `RegressionCorpus`, `ComparisonSurface`, `DifferentialReport`, `DiffReport`, `Divergence`, `LedgerDiffReport`, `ProtocolDiffReport`, `BlockFields`, `ExpectedVerdict`, `HarnessError`, `Transcript`, `StateHash`, stub applicators, violation reports. `SnapshotHeader` (post-168ac02: + `slot: u64`). **Consensus (N-B):** `LedgerViewStub`, `EpochStakeFixture`, `PoolFixture`, `ReplayStep`, `ReplayResult`. **Validity (B1):** `ConwayValidityCorpus` + `CorpusLoadError`, production-`PoolDistrView` builder, GREEN replay harness, M1–M6 mutators. **B2 (`tx_validity/`):** `ExtractedTx` + `ExtractError`, `TxReplay`, `ValidCase` + `DependentPair`, `WitnessMutation` + `WitnessMutationOutcome` + `SyntheticMutation` + `SyntheticCase`. **N-E S2 (`mempool/`):** `BTrackCase { event, base, expected }`, closed `ExpectedOutcome { Admit, Reject(TxRejectClass) }`. Functions: `parse_with_origin_slot` (168ac02), `wrap_as_ingress`, `b_track_corpus_as_ingress`, `replay_ingress_trace` (all S2). |
| **Interprets** | Oracle reference snapshots, the regression-corpus manifest, the N-B consensus corpus, the B1 validity corpus, on-wire Conway transactions inside committed Conway-576 corpus blocks (B2). **B3:** the snapshot loader parses Conway deposit parameters from snapshot bytes (allowlisted non-canonical-state deposit-param source under DC-TXV-07). **168ac02:** snapshot-loader tip-slot extraction + dual UMElem layouts. **ENACTMENT-COMMITTEE-WRITEBACK:** `parse_gov_action` tag-4 decodes the structured Conway shape. **N-E S2:** the harness does not interpret bytes — it constructs `IngressEvent` envelopes around already-canonical tx CBOR produced by the B-track generators, and folds them through the BLUE `mempool_ingress` bridge. |
| **MUST NOT** | (1) Affect authoritative outputs — testkit code is excluded from BLUE forbidden-pattern scans and may use `HashMap`/`serde_json`/`flate2`/`tar`, but its results must never feed back into BLUE/RED authoritative state. (2) Be linked from any BLUE crate. (3) Introduce nondeterminism that leaks into stored fixtures. (4) `consensus::ledger_view_stub` must use `BTreeMap` only. (5) Stream-replay driver must report identical event sequences across runs. (6) **B1 `validity::replay`** must report identical verdict streams across runs and must not alter the BLUE verdict. (7) **B2 `tx_validity`** must not alter the BLUE `tx_validity` verdict; `no_mutation_is_ever_valid` is the CE-B2-4 false-accept guard. (8) **B3** snapshot-loader deposit-param construction must never be reached by the BLUE deposit-conservation path at runtime; fixture-materialization helper only. (9) **168ac02** `parse_cold_credential_set` / `parse_cold_credential_epoch_map` must reject a truncated declared-length set/map deterministically. (10) **168ac02** `parse_registered_credentials` must reject an unknown UMElem layout. (11) **N-E S2 `mempool::ingress_replay` (DC-MEM-04):** `replay_ingress_trace` must call `mempool_ingress` (the BLUE bridge), not `admit` directly — enforced by `ci/ci_check_mempool_ingress_replay.sh` check 2. The fold must be single-step per OQ-6: no `chunks`/`chunks_exact`/`partition`/`par_iter`/`rayon`/`tokio::spawn` — enforced by check 4. The five test names referenced by the registry's DC-MEM-04 `tests` array must exist in `tests/mempool_ingress_replay.rs` — enforced by check 3. The harness must reuse the B-track corpus verbatim. |
| **Inbound deps** | None at compile time. All consumption is through its own integration tests and dev-dep links from `ade_core`, `ade_runtime`, `ade_ledger`. `ade_core_interop` is a non-dev consumer (S4/S5 import `ade_testkit::mempool::replay_ingress_trace`; S6's `live_tx_submission_session` binary does not import `ade_testkit`). |
| **Outbound deps** | `ade_types`, `ade_codec`, `ade_crypto`, `ade_core`, `ade_ledger`, `ade_plutus`, `ade_runtime`, `blake2`, `flate2`, `tar`, `serde`, `serde_json`, `toml`. |
| **Entry points** | `ade_testkit::harness::snapshot_loader::LoadedSnapshot`, `extract_state_from_tarball` / `parse_*` family; N-B: `ade_testkit::consensus::{ledger_view_stub::*, stream_replay::*, corpus::*}`; B1: `ade_testkit::validity::{corpus::ConwayValidityCorpus, ledger_view::*, replay::*, adversarial::*}`; B2: `ade_testkit::tx_validity::{extract_corpus_txs, extract_block_txs, ExtractedTx, replay_tx_validity, validate_tx_against_corpus, TxReplay, build_valid, build_dependent_pair, mutate_witness, build_synthetic, judge}`. N-E S2: `ade_testkit::mempool::{wrap_as_ingress, b_track_corpus_as_ingress, replay_ingress_trace, BTrackCase, ExpectedOutcome}` (consumed by `ade_core_interop::tx_submission::ingest_n2n_events` and `ade_core_interop::local_tx_submission::ingest_n2c_events`). |
| **Key modules** | **`harness/`**, **`consensus/`** (N-B), **`validity/`** (B1), **`tx_validity/`** (B2): `mod.rs`, `extract.rs`, `adversarial.rs`, `valid_synthetic.rs`, **`mempool/`** (N-E S2): `mod.rs`, `ingress_replay.rs`. **Integration tests of note:** `tests/emit_reward_provenance.rs` (c78ec76), `tests/epoch_oracle_comparison.rs` (b9cfaf9), `tests/mempool_ingress_replay.rs` (N-E S2, 5 tests), `tests/mempool_ingress_canonicalize.rs` (N-E S3, 2 tests). |

> **Classification note.** `ade_testkit` reads files from disk in test helpers and drives BLUE authorities from corpus inputs. I/O exists only to materialize oracle/corpus fixtures consumed by tests and to compose deterministic GREEN harnesses for replay equivalence. By IDD doctrine that is GREEN. The crate is "demotable to RED" if/when it grows orchestration code that mutates authoritative state.

---

## RED Modules — Imperative Shell

> I/O, network, storage, clocks, retries. May depend on BLUE/GREEN. Must not modify core state directly or construct semantic types unsafely.

### `ade_core_interop` *(PHASE4-N-B S-B10; PHASE4-N-E S4/S5/S6)*

> **Status change at HEAD `caa5ce8` (PHASE4-N-E S6 — new RED operator-action binary inside the existing RED crate prefix).** One new source file and one new Cargo `[[bin]]` entry land between `350130e` and HEAD:
>
> - **S6 — `src/bin/live_tx_submission_session.rs` (RED).** 552 LOC at HEAD. Sustained-window N2N tx-submission2 probe against a real cardano-node 11.x preprod relay. Imports the GREEN S4 bridge surface (`ade_core_interop::tx_submission::{event_to_ingress, PeerAccumulator}`), the BLUE network state machines (`ade_network::tx_submission::{tx_submission2_transition, InventoryEvent, TxSubmission2Agency, TxSubmission2Output, TxSubmission2State}`), the BLUE network codecs (`ade_network::codec::tx_submission::{decode_tx_submission_message, encode_tx_submission_message, TxSubmission2Message}` + handshake codec + primitives), the version newtypes (`N2NVersion`, `TxSubmission2Version`), the mux frame primitive (`encode_frame`, `MuxFrame`, `MuxHeader`, `MuxMode`, `MiniProtocolId`, `HEADER_LEN`), and BLUE ledger types (`ade_ledger::mempool::{IngressSource, PeerId}`). Two execution modes: a hermetic default `main` that prints `live_tx_submission_session ready` and exits (drives the `#[ignore]`-gated build-and-start test), and a `--connect` live pass that performs the full N2N handshake → tx-submission2 protocol loop → BLUE state-machine validation, writing a transcript to `docs/clusters/PHASE4-N-E/CE-N-E-6_<date>.log`. Supports `--network {mainnet,preprod,preview}`, `--max-seconds`, `--max-frames` flags. Uses `tokio` (already in the crate's manifest), `std::time::{Instant, SystemTime}`, and `std::fs` for the transcript write — all RED-allowlisted in this crate.
> - **S6 — `tests/live_tx_submission_session.rs` (RED closure-gate test).** 80 LOC, 1 `#[test]`, `#[ignore]`-gated. Hermetic gate: asserts the binary builds and starts and prints the `live_tx_submission_session ready` banner without opening a socket. The live evidence capture (an actual preprod connection) is performed by running `cargo run --bin live_tx_submission_session -- --connect --network preprod --max-seconds 600 --max-frames 1000` and committing the resulting log.
> - **S6 — `[[bin]]` entry in `Cargo.toml`.** Added `[[bin]] name = "live_tx_submission_session" path = "src/bin/live_tx_submission_session.rs"` adjacent to the existing `live_consensus_session` entry. No new manifest dependency — the binary uses crates already declared at S4 (`ade_ledger`, `ade_network`, `tokio`, etc.).
> - **Correctness fix at `caa5ce8`.** Retry-on-`io::ErrorKind::TimedOut` in the live-pass socket read loop, plus an elapsed-time logging fix. No public-API or test-surface change.
> - **CE-N-E-6 live evidence committed (`caa5ce8`).** `docs/clusters/completed/PHASE4-N-E/CE-N-E-6_2026-05-25.log` (9 lines, condensed transcript): real preprod relay, N2N v15 handshake accepted, 1 tx-submission2 frame received from the peer, BLUE `tx_submission2_transition` drove the protocol grammar correctly, 97-second session. This evidences the live half of CE-N-E-6 (the deterministic GREEN code half closed at `ca3f23a` via the S4 bridge + 7 integration tests).
>
> **Cluster archive note.** PHASE4-N-E was archived between `350130e` and HEAD; all PHASE4-N-E cluster docs and the new evidence log now resolve under `docs/clusters/completed/PHASE4-N-E/`. CE-N-E-7 (live N2C) was deferred as cross-cluster obligation `CE-NODE-N2C-LTX` to the future node-binary cluster (Ade must host an N2C **server** to receive `cardano-cli` submissions, which is the node-binary cluster's responsibility); the procedure spec is retained at `docs/clusters/completed/PHASE4-N-E/CE-N-E-7_PROCEDURE.md` as the canonical spec for future closure.
>
> All prior S4/S5 GREEN-sub-classification narrative carries forward unchanged: `src/tx_submission.rs` (S4 — `event_to_ingress`, `PeerAccumulator`, `ingest_n2n_events`) and `src/local_tx_submission.rs` (S5 — `local_event_to_ingress`, `ClientAccumulator`, `ingest_n2c_events`) remain pure deterministic bridges; the production socket loop is the RED operator-action half, now physically realized for N2N as the S6 binary.

| Attribute | Value |
|-----------|-------|
| **Purpose** | Live cardano-node interop driver. Hosts (a) the N-B consensus orchestrator readiness probe `fresh_orchestrator(k, initial_nonce)` and its `live_consensus_session` binary; (b) the N-E S4 GREEN adapter + per-peer accumulator + `ingest_n2n_events` orchestrator that bridges N2N `InventoryEvent`s into `mempool_ingress`; (c) the N-E S5 GREEN adapter + per-client accumulator + `ingest_n2c_events` orchestrator that bridges N2C `LocalTxSubmissionEvent`s into the same chokepoint; (d) **NEW — the N-E S6 `live_tx_submission_session` RED binary that performs a sustained-window live N2N tx-submission2 probe against a real cardano-node preprod relay, drives the BLUE protocol state machine on live wire bytes, and writes a transcript for CE-N-E-6 closure**. The crate is RED per `Cargo.toml` and the PHASE4-N-B / PHASE4-N-E TCB Color Maps; the S4/S5 sub-modules carry a GREEN sub-classification by their own module doc-comments. The S6 binary is RED by its own header comment ("RED — `live_tx_submission_session` binary. Operator evidence-capture pass for CE-N-E-6"). |
| **Creates** | `fresh_orchestrator(k, initial_nonce) -> OrchestratorState` (no new public type). **N-E S4:** `PeerAccumulator { peer, txs }` (sub-classified GREEN; not in the BLUE canonical-type count). **N-E S5:** `ClientAccumulator { client, txs }` (sub-classified GREEN; not in the BLUE canonical-type count). Functions (no new types): `event_to_ingress` + `ingest_n2n_events` (S4), `local_event_to_ingress` + `ingest_n2c_events` (S5). **N-E S6:** no public Rust types — the binary is RED glue over the already-shipped S4 GREEN surface (`event_to_ingress` + `PeerAccumulator`) and the BLUE `ade_network` codecs / state machines. |
| **Interprets** | Live N2N byte streams from a real cardano-node 10.6.2 / 11.x peer (the orchestrator readiness side and the new S6 binary; bytes flow in via TCP into BLUE codecs / state machines / consensus transitions without re-interpretation). **N-E S4/S5 sub-modules interpret nothing at the bytes level** — they take already-decoded `InventoryEvent` / `LocalTxSubmissionEvent` values from `ade_network`, wrap the carried `tx_bytes` in `IngressEvent`s under a fixed `IngressSource`, and forward through the BLUE bridge. The S6 binary similarly interprets nothing semantically — it drives the BLUE protocol grammar on live wire bytes and emits the deterministic operator-evidence transcript. |
| **MUST NOT** | (1) Contain authoritative state-transition logic. (2) Be depended on by any other workspace crate. (3) Run by default in CI — the closure-gate tests are `#[ignore]`-gated (CE-N-B-6, CE-N-E-6). (4) Bypass `ade_codec`'s `PreservedCbor` chokepoints. (5) Read versions out of ambient context (DC-PROTO-06). (6) Vendor cardano-node mainnet genesis blobs into the crate. (7) **N-E S4/S5:** must not branch on `IngressSource` in any verdict path. Must not re-encode `tx_bytes` between the network event and the `IngressEvent` envelope. Must not introduce a non-deterministic orchestrator (`ingest_n2n_events` / `ingest_n2c_events` are pure functions of the inputs). Must not skip `canonicalize_peer_streams` before `replay_ingress_trace`. Must not write a production-callable `admit(` invocation; the S4/S5 bridges reach `admit` only transitively through `mempool_ingress` via `replay_ingress_trace` — enforced by `ci/ci_check_mempool_ingress_closure.sh` check 4. (8) **NEW — N-E S6 `live_tx_submission_session` binary (RED):** must not call `admit(` directly (it imports the S4 GREEN bridge surface only, which reaches `admit` transitively through `mempool_ingress`). Must not redefine or shadow `IngressSource` / `IngressEvent` / `PeerId` — types come from `ade_ledger::mempool::*`. Must not redefine wire-message codecs or state-machine transitions — uses `ade_network::codec::tx_submission::*` and `ade_network::tx_submission::tx_submission2_transition` as imported. Must keep the binary entrypoint behavior fork legible: a hermetic default `main` (no I/O beyond `println!`) plus an explicit `--connect` live pass; the `#[ignore]`-gated test asserts the hermetic main exits successfully without touching the network. Must not omit the elapsed-time logging fix landed at `caa5ce8` (operator evidence depends on it for the 97s session marker). Must not skip the retry-on-`io::ErrorKind::TimedOut` semantic — the live pass treats `TimedOut` as a transient read rather than a session-failing error. |
| **Inbound deps** | None. |
| **Outbound deps** | `ade_core`, `ade_codec`, `ade_crypto`, `ade_ledger`, `ade_runtime`, `ade_network`, `ade_testkit`, `ade_types`, `tokio`. **No new outbound crate edge in S6** — the binary uses crates already in the manifest (the `ade_ledger` edge was added at S4). Total Cargo deps: 8 workspace crates + `tokio`. |
| **Entry points** | `ade_core_interop::fresh_orchestrator(k, initial_nonce)`, binary `live_consensus_session` (CE-N-B-6 evidence). N-E S4: `ade_core_interop::tx_submission::{event_to_ingress, PeerAccumulator, ingest_n2n_events}` exercised by `crates/ade_core_interop/tests/tx_submission_ingress.rs` (7 tests). N-E S5: `ade_core_interop::local_tx_submission::{local_event_to_ingress, ClientAccumulator, ingest_n2c_events}` exercised by `crates/ade_core_interop/tests/local_tx_submission_ingress.rs` (8 tests). **NEW — N-E S6:** binary `live_tx_submission_session` (CE-N-E-6 evidence; sustained-window N2N tx-submission2 probe). Closure-gate test in `crates/ade_core_interop/tests/live_tx_submission_session.rs` (1 `#[ignore]` test). |
| **Key modules** | `src/lib.rs`, `src/follow.rs` (N-B follow-mode bridge for CE-N-B-6), `src/tx_submission.rs` (N-E S4), `src/local_tx_submission.rs` (N-E S5), `src/bin/live_consensus_session.rs`, **`src/bin/live_tx_submission_session.rs` (NEW — N-E S6)**. |

> **Gap surfaced (carried + extended).** `ade_core_interop` at HEAD is a *readiness probe + bridge + live evidence capture binary*. CE-N-B-6 (live consensus tip agreement) **closes via manual evidence log against a real cardano-node peer; live capture pending operator run.** CE-N-E-6 (live N2N tx-submission evidence) is **CLOSED at HEAD** — both the deterministic GREEN bridge half (S4, `ca3f23a`) and the live operator-evidence half (S6, `d1068b3` + `caa5ce8` correctness fix + `CE-N-E-6_2026-05-25.log` transcript) are in place. CE-N-E-7 (live N2C tx-submission evidence) is **deferred** to the future node-binary cluster as cross-cluster obligation `CE-NODE-N2C-LTX` — Ade must host an N2C server to receive `cardano-cli` submissions, which the current crate cannot do. The S4/S5 GREEN sub-classification inside the RED `ade_core_interop` crate prefix is review-and-doc enforced (no narrow CI gate scopes the partition); the S6 RED binary classification is similarly review-and-doc enforced (the binary lives under `src/bin/`, matching the established `live_consensus_session.rs` pattern).

---

### `ade_network` *(RED submodules + capture binaries)*

> **Status carried from prior CODEMAP — unchanged at HEAD `caa5ce8`.** The RED half of the `ade_network` crate: `mux::transport`, `session`, and the 7 `src/bin/capture_*` binaries. Provenance: cluster PHASE4-N-A. PHASE4-N-E S6 did not touch this crate; the new `live_tx_submission_session` binary lives in `ade_core_interop`, not `ade_network` (matching the established `live_consensus_session` placement).

| Attribute | Value |
|-----------|-------|
| **Purpose** | The only place inside `ade_network` where I/O happens — tokio-based TCP / Unix-socket scaffolding for the Ouroboros mux layer (`mux::transport`), the session-composition placeholder (`session`), and 7 capture binaries that perform real handshakes against pinned cardano-node 11.0.1 peers and write the recorded byte stream as canonical replay corpus. |
| **Creates** | `MuxTransport` (tokio TcpStream wrapper). `session/mod.rs` is an empty placeholder. The capture binaries produce no public Rust types. |
| **Interprets** | Live TCP / Unix-socket byte streams from cardano-node 11.0.1 peers; bytes flow in via `MuxTransport::read_raw` into the BLUE mux frame decoder and protocol codecs without re-interpretation. |
| **MUST NOT** | (1) Contain protocol logic. (2) Define or modify any closed wire-message enum, agency type, or state. (3) Read a selected protocol version from ambient state and pass it into a BLUE transition implicitly (DC-PROTO-06). (4) Replace the BLUE codec / state machine with a "fast path" (CN-WIRE-07). (5) Bypass `ade_codec`'s `PreservedCbor` chokepoints. (6) Be depended on by any BLUE submodule of `ade_network`. |
| **Inbound deps** | `ade_core_interop`. At the library level, no BLUE crate imports the RED submodules. |
| **Outbound deps** | `tokio`, `ade_types`, `ade_codec`, plus the BLUE `ade_network::codec::*` and `ade_network::mux::frame`. |
| **Entry points** | `ade_network::mux::transport::{MuxTransport, open_tcp}`. The capture binaries. `session::*` exposes no entry points at HEAD. |
| **Key modules** | `mux/transport.rs`, `session/mod.rs` (empty placeholder), `bin/capture_*`. |

> **Gap surfaced (carried).** `ade_network::session::mod.rs` is still a placeholder. The session driver remains owed by a future orchestration cluster — likely the same node-binary cluster that owns `CE-NODE-N2C-LTX`.

---

### `ade_node`

> **Status carried from prior CODEMAP — unchanged at HEAD `caa5ce8`.**

| Attribute | Value |
|-----------|-------|
| **Purpose** | Binary entry point for the node process. Currently a hello-world stub. Reserved as the assembly point for runtime + ledger + chain DB + network + consensus orchestrator + mempool + the deferred N2C server that will close `CE-NODE-N2C-LTX`. |
| **Creates** | None. |
| **Interprets** | None yet. |
| **MUST NOT** | (1) Bypass `ade_codec` to construct semantic types from bytes. (2) Modify `ade_ledger` or `ade_core::consensus` state in place — transitions go through `ade_ledger::rules::*`, `ade_ledger::block_validity::*`, `ade_ledger::tx_validity::*`, `ade_ledger::mempool::admit`, `ade_ledger::mempool::mempool_ingress` (the only sanctioned wire-ingress path under DC-MEM-03), or `ade_core::consensus::*` BLUE transitions. (3) Take an inbound dep from any other crate. (4) Read versions out of ambient context and pass them into `ade_network` BLUE transitions implicitly (DC-PROTO-06). (5) Construct an `OrchestratorState` outside `ade_runtime::consensus::chain_selector` or `ade_core_interop::fresh_orchestrator`. (6) Once `ade_node` grows beyond a stub: must not call `admit()` directly; must reach the mempool only through `mempool_ingress` (forward-looking, mechanically enforced by `ci/ci_check_mempool_ingress_closure.sh` check 4 when ade_node first introduces a production call site). (7) **Forward-looking — `CE-NODE-N2C-LTX`:** the N2C local-tx-submission server, when it lands, must drive the `ade_core_interop::local_tx_submission::ingest_n2c_events` GREEN bridge — it must not introduce a second, parallel path into the mempool. |
| **Inbound deps** | None. |
| **Outbound deps** | None at present. |
| **Entry points** | `main()` in `crates/ade_node/src/main.rs`. |

> **Gap surfaced (carried).** `ade_node` is still a hello-world stub; its MUST NOT list is forward-looking with nothing to enforce today.

---

### `ade_runtime`

> **Status carried from prior CODEMAP — unchanged at HEAD `caa5ce8`.** Hosts storage + recovery (cluster N-D) and the consensus orchestrator (`consensus/`, cluster N-B). The crate remains RED; `chain_selector.rs` / `candidate_fragment.rs` are GREEN by file-level classification, `genesis_parser.rs` is RED. PHASE4-N-E S6 did not change `ade_runtime` — the S6 binary's home is `ade_core_interop` (project's established RED live-interop crate, matching `live_consensus_session.rs`).

| Attribute | Value |
|-----------|-------|
| **Purpose** | The imperative shell. Hosts (a) the `ChainDb` / `SnapshotStore` storage abstractions and their in-memory / redb-backed impls, the crash-safety harness, and the snapshot + forward-replay recovery primitive (N-D); (b) the GREEN chain-selector orchestrator + GREEN `CandidateFragment` materializer (N-B); (c) the RED genesis-text parser that materializes the BLUE `EraSchedule` and computes the `BootstrapAnchorHash` (N-B). |
| **Creates** | **ChainDb (`chaindb/`):** `ChainDb` trait, `SnapshotStore` trait, `KillStrategy<D>` trait, `ChainTip`, `StoredBlock`, `ChainDbError`, `InMemoryChainDb`, `PersistentChainDb`, `PersistentChainDbOptions`, `SyncCadence`, `NoKill`, `BlockIter<'a>`. **Recovery (`recovery.rs`):** `Recoverable` trait, `StartingState`, `RecoveryReport<R>`, `RecoveryError<E>`. **Genesis (`consensus/genesis_parser.rs` — RED):** `NetworkMagic`, `GenesisBlob`, `GenesisBundle<'a>`, `GenesisParseError`. **Orchestrator (`consensus/chain_selector.rs` — GREEN):** `StreamInput`, `OrchestratorError`, `OrchestratorState`, `RollbackSnapshot`, `DEFAULT_SNAPSHOT_LIMIT`. **Materializer (`consensus/candidate_fragment.rs` — GREEN):** `build_candidate_fragment`. |
| **Interprets** | Block bytes for content-addressed storage. Snapshot bytes are byte-opaque to chaindb. Genesis JSON for the four blobs → typed `EraSummary` + `system_start_unix_ms` + immutable `EraSchedule`. N-A `StreamInput` events → canonical BLUE input shape threaded through BLUE transitions. |
| **MUST NOT** | (1) Depend on `ade_ledger`. (2) Leak `redb` through the `chaindb::*` public surface. (3) Have a second public chaindb path. (4) Couple snapshots to a specific state type. (5) Apply automatic snapshot pruning. (6) Allow partial-recovery success. (7) Add async recovery surface. (8) Treat "not found" as an error path or silently retry on real errors. (9) **Consensus sub-tree:** the GREEN `chain_selector` must not affect authoritative outputs; no `HashMap`/`HashSet`, no async, no wall-clock, no global mutable state. (10) **Genesis parser:** must not be reached by BLUE; structured errors only. (11) **`candidate_fragment` materializer:** must not own state — pure constructor. |
| **Inbound deps** | `ade_testkit` (non-dev dep, used by `stream_replay`), `ade_core_interop`. The binary `chaindb_kill_target` lives at `src/bin/`. |
| **Outbound deps** | `ade_types`, `ade_core`, `ade_crypto` (`blake2b_256`), `ade_codec`, `redb`, `serde_json`. Dev-deps: `tempfile`, `ade_testkit`, `cardano-crypto` (tests only). |
| **Entry points** | **Chaindb / recovery:** `ade_runtime::chaindb::{ChainDb, SnapshotStore, PersistentChainDb, PersistentChainDbOptions, SyncCadence, InMemoryChainDb, run_contract_tests, run_snapshot_contract_tests, run_crash_safety_tests}`, `ade_runtime::recovery::{recover, Recoverable}`. **Consensus:** `ade_runtime::consensus::chain_selector::{process_stream_input, OrchestratorError, OrchestratorState, RollbackSnapshot, StreamInput, DEFAULT_SNAPSHOT_LIMIT}`, `ade_runtime::consensus::candidate_fragment::build_candidate_fragment`, `ade_runtime::consensus::genesis_parser::{compute_anchor_hash, parse_genesis, GenesisBlob, GenesisBundle, GenesisParseError, NetworkMagic}`. |
| **Key modules** | **`chaindb/`**: `mod.rs`, `contract.rs`, `snapshot_contract.rs`, `crash_safety.rs`, `in_memory.rs`, `persistent.rs`, `types.rs`, `error.rs`. **`recovery.rs`**. **`consensus/`**: `mod.rs`, `genesis_parser.rs` (RED), `candidate_fragment.rs` (GREEN), `chain_selector.rs` (GREEN). **`bin/chaindb_kill_target.rs`**. |
| **Mechanical enforcement** | Three dedicated CI scripts back the chaindb / recovery / crash-safety surface: `ci/ci_check_chaindb_contract.sh`, `ci/ci_check_recovery_contract.sh`, `ci/ci_check_chaindb_crash_safety.sh`. The consensus sub-tree has no dedicated CI script. |

> **Gap surfaced (carried).** No dedicated CI script enforces the GREEN/RED partition *inside* `ade_runtime::consensus`. At HEAD the partition is "by file" and enforced only by review + header comments.

---

## Cross-Module Rules (project-wide)

### Dependency direction

`ade_core_interop` → `{ade_core, ade_codec, ade_crypto, ade_ledger, ade_runtime, ade_network, ade_testkit, ade_types, tokio}` is legal (RED leaf binary). **No new outbound edge in S6** — the new binary uses crates already in the manifest.
`ade_runtime` → `{ade_core, ade_crypto, ade_codec, ade_types, redb, serde_json}` is legal (RED + GREEN glue).
`ade_testkit` → `{ade_core, ade_ledger, ade_plutus, ade_runtime, ade_crypto, ade_codec, ade_types}` is legal (GREEN).
`ade_network` (BLUE submodules) → `{ade_codec, ade_types}` is legal (BLUE among BLUEs).
`ade_network` (RED submodules + capture bins) → `{tokio, ade_codec, ade_types, ade_network::codec::*, ade_network::mux::frame}` is legal.
**`ade_ledger` → `{ade_core, ade_plutus, ade_crypto, ade_codec, ade_types, minicbor}` is legal (BLUE among BLUEs).** The `ade_ledger → ade_core` edge (PHASE4-B1) is **acyclic**. PHASE4-N-E added no new outbound edge from `ade_ledger` itself.
`ade_core` → `{ade_types, ade_crypto, minicbor}` is legal (BLUE among BLUEs).
`ade_plutus` → `{ade_crypto, ade_codec, ade_types}` is legal.
`ade_crypto` → `{ade_types}` is legal.
`ade_codec` → `{ade_types}` is legal.
`ade_types` → `{}`.

**Forbidden directions.** Any BLUE crate (or BLUE `ade_network` submodule) depending on `ade_runtime`, `ade_node`, `ade_core_interop`, or the RED half of `ade_network` is a CI failure (`ci_check_dependency_boundary.sh` + `ci_check_no_async_in_blue.sh`). Any non-`ade_plutus` crate referring to `pallas_*` is a CI failure (`ci_check_pallas_quarantine.sh`). Any reference to `ChainDb` / `chain_db` inside `crates/ade_core/src/consensus/` is a CI failure (`ci_check_no_chaindb_in_consensus_blue.sh`). **B1/B2/N-E note:** the BLUE `tx_validity`/`mempool::admit`/`mempool::ingress` and GREEN `mempool::policy`/`mempool::canonicalize` all live under the already-BLUE `ade_ledger` crate prefix; the GREEN/RED/BLUE *sub-crate* partition inside `ade_ledger::mempool` and `ade_ledger::consensus_input_extract` is review-and-test-enforced for the broad partition, with two narrow grep gates pinning specific properties: `ci_check_mempool_ingress_closure.sh` (DC-MEM-03 — `admit()` callers + `MempoolState.accumulating` writers + `IngressSource` closure + `mempool_ingress` source-invariance) and `ci_check_mempool_ingress_replay.sh` (DC-MEM-04 — harness shape + canonicalizer determinism shape). **B3 note:** any BLUE-crate deposit/refund literal next to a deposit field, or any BLUE read of a testkit `ConwayGovParams`, is a CI failure (`ci_check_deposit_param_authority.sh`, DC-TXV-07). **N-E note:** any production caller of `admit(` outside `mempool/admit.rs` (definition) or `mempool/ingress.rs` (the bridge) is a CI failure (`ci_check_mempool_ingress_closure.sh` check 4). Tests under `crates/*/tests/` and benches/ are exempt; the `ade_core_interop::{tx_submission, local_tx_submission}` bridges and the **new S6 `live_tx_submission_session` RED binary** reach `admit` only transitively through `mempool_ingress` via `replay_ingress_trace` (or, for the live binary, never — the binary only drives the network protocol layer and feeds events through the S4 GREEN bridge without invoking `admit` in the live pass), satisfying the gate.

### Naming convention

All crates are prefixed `ade_`. TCB color is not encoded in the crate name. The authoritative classifier is `.idd-config.json` `core_paths` plus the cluster doc TCB Color Maps for sub-crate scopes; CI scripts hard-code their BLUE list. The seven full-BLUE-scoped scripts (`ci_check_module_headers.sh`, `ci_check_no_semantic_cfg.sh`, `ci_check_no_signing_in_blue.sh`, `ci_check_hash_uses_wire_bytes.sh`, `ci_check_ingress_chokepoints.sh`, `ci_check_dependency_boundary.sh`, `ci_check_no_async_in_blue.sh`) use the full BLUE set: the 6 BLUE crates plus the 9 BLUE `ade_network` submodule paths. The B3 `ci_check_deposit_param_authority.sh` scopes the 6 BLUE crates. Four narrow scripts target `crates/ade_core/src/consensus/`. The two N-E scripts (`ci_check_mempool_ingress_closure.sh`, `ci_check_mempool_ingress_replay.sh`) scope `crates/ade_ledger/src/mempool/{ingress.rs, admit.rs, canonicalize.rs, mod.rs}` and `crates/ade_testkit/src/mempool/{mod.rs, ingress_replay.rs}` + `crates/ade_testkit/tests/mempool_ingress_replay.rs`. **No script narrowly scopes `crates/ade_ledger/src/tx_validity/`, `crates/ade_types/src/conway/`, or `crates/ade_ledger/src/delegation.rs`** — the B2/B3/B4 closed-enum / closed-grammar / owner-tagged-action properties on those paths rely on the full-BLUE scans plus the compiler-exhaustive `match` and named tests. **No CI script scopes the new S6 `live_tx_submission_session.rs` binary** — its RED classification rests on its header comment, its placement under `src/bin/`, and the `#[ignore]`-gated closure-gate test (matching the established `live_consensus_session.rs` precedent).

### CI enforcement (31 scripts under `ci/`)

| Script | Enforces | Scope |
|---|---|---|
| `ci_check_cbor_round_trip.sh` | T-ENC-03, DC-CBOR-01, DC-CBOR-02 | golden corpus |
| `ci_check_ce_n_a_5_proof.sh` *(PHASE4-N-A, S-A10)* | CE-N-A-5 5-condition evidence | `ade_network` (RED + real-capture corpus) |
| `ci_check_chaindb_contract.sh` | DC-STORE-02, DC-STORE-03, CN-STORE-04, CN-STORE-05 | `ade_runtime --lib chaindb::` (RED) |
| `ci_check_chaindb_crash_safety.sh` | T-REC-01 (crash variant), DC-STORE-01, CN-STORE-03; CE-N-D-1 gate | `ade_runtime --test stress_kill_harness` (RED) |
| `ci_check_consensus_closed_enums.sh` *(PHASE4-N-B, S-B2; extended B1, B2)* | DC-CONS-04, DC-CONS-10, T-DET-01, DC-VAL-02/04/05/06, DC-TXV-01/02/04/05, DC-MEM-01/02 | `consensus/` + `block_validity/` + `tx_validity/` + `mempool/` |
| `ci_check_constitution_coverage.sh` | invariant-registry ↔ code/test coverage | repo-wide |
| `ci_check_conway_cert_classification_closed.sh` *(PHASE4-B3F)* | DC-TXV-06 — Conway cert-classification closure | `ade_types::conway::cert` + `ade_codec::conway::cert` + `ade_ledger::cert_classify` |
| `ci_check_credential_discriminant_closed.sh` *(OQ5 + COMMITTEE + DREP + ENACTMENT-COMMITTEE-FIDELITY + WRITEBACK)* | DC-LEDGER-10 — credential discriminant fidelity | `ade_types::shelley::cert` + both era `ade_codec` decoders + `ade_ledger::{state, governance, fingerprint, rules}` + `ade_types::conway::governance` |
| `ci_check_crypto_vectors.sh` | crypto KAT regression | `ade_crypto` |
| `ci_check_dependency_boundary.sh` | T-BOUND-02 — BLUE ⇎ RED separation | full BLUE (6 crates + 9 `ade_network` paths) |
| `ci_check_deposit_param_authority.sh` *(PHASE4-B3)* | DC-TXV-07 — canonical deposit-param authority | 6 BLUE crates |
| `ci_check_differential_divergence.sh` | DC-DIFF-* | replay outputs |
| `ci_check_forbidden_patterns.sh` | T-CORE-02 — no `HashMap`/floats/clocks/etc. + `unsafe` allowlist | full BLUE |
| `ci_check_gov_cert_accumulation_closed.sh` *(PHASE4-B5)* | DC-LEDGER-09 — Conway gov-cert accumulation closure | `ade_ledger::{gov_cert,rules,error,state}` |
| `ci_check_hash_uses_wire_bytes.sh` | DC-CBOR-02, T-ENC-01 | full BLUE |
| `ci_check_hfc_translation.sh` | DC-EPOCH-02 (CE-73-semantic) | `ade_ledger::hfc` |
| `ci_check_ingress_chokepoints.sh` | DC-INGRESS-01, T-INGRESS-01 | full BLUE |
| `ci_check_ledger_determinism.sh` | DC-LEDGER-01 (CE-74) | `ade_ledger` |
| `ci_check_mempool_ingress_closure.sh` *(PHASE4-N-E, S1)* | DC-MEM-03 — closed `IngressEvent` chokepoint into `admit`; `IngressSource` 2-variant closed; `MempoolState.accumulating` write site frozen to `admit.rs`; production `admit(` callers restricted to `admit.rs` + `ingress.rs`; `mempool_ingress` body forbidden from branching on `source`. Strengthens DC-MEM-01. | `crates/ade_ledger/src/mempool/{ingress.rs, admit.rs, mod.rs}` + repo-wide `admit(` caller scan |
| `ci_check_mempool_ingress_replay.sh` *(PHASE4-N-E, S2 + S3)* | DC-MEM-04 — ingress-replay harness exists + exports + reaches `mempool_ingress` (not `admit`); 5 required test names present; no batching/parallel helpers in the harness (single-step per OQ-6); canonicalizer present + exports + uses no `HashMap`/`HashSet`/async/clock/RNG. | `crates/ade_testkit/src/mempool/{mod.rs, ingress_replay.rs}` + `crates/ade_testkit/tests/mempool_ingress_replay.rs` + `crates/ade_testkit/src/lib.rs` + `crates/ade_ledger/src/mempool/canonicalize.rs` |
| `ci_check_module_headers.sh` | CE-04 contract banner | full BLUE |
| `ci_check_no_async_in_blue.sh` *(PHASE4-N-A, S-A1)* | DC-CORE-01 | full BLUE |
| `ci_check_no_chaindb_in_consensus_blue.sh` *(PHASE4-N-B, S-B1)* | DC-CORE-01 + DC-CONS-07 | `crates/ade_core/src/consensus/` |
| `ci_check_no_density_in_fork_choice.sh` *(PHASE4-N-B, S-B8)* | DC-CONS-03 | `fork_choice.rs` + `candidate.rs` |
| `ci_check_no_float_in_consensus.sh` *(PHASE4-N-B, S-B1)* | T-CORE-02 + DC-CONS-07/08/09 | `crates/ade_core/src/consensus/` |
| `ci_check_no_secrets.sh` | no credentials/IPs/keys in tree | repo-wide |
| `ci_check_no_semantic_cfg.sh` | T-BUILD-01 — no `#[cfg(feature)]` | full BLUE |
| `ci_check_no_signing_in_blue.sh` | CE-05, T-KEY-01 — signing in RED only | full BLUE |
| `ci_check_pallas_quarantine.sh` | O-29.2 — `pallas-*` confined to `ade_plutus` | non-`ade_plutus` |
| `ci_check_recovery_contract.sh` | T-REC-01, T-REC-02, DC-STORE-05 | `ade_runtime --lib recovery::` (RED) |
| `ci_check_ref_provenance.sh` | DC-REF-01 — manifest checksum integrity | reference corpus |

> **Post-350130e CI delta. None.** S6 ships no new CI script — the operator-evidence-log pattern (closure-gate test `#[ignore]`-gated; live evidence committed as `CE-N-E-6_<date>.log`) matches the established CE-N-B-6 / `live_consensus_session.rs` precedent and does not require a dedicated CI gate. The forward-looking `ci/ci_check_no_fail_open_in_validation.sh` (the `if len == K { check } else { skip }` grep gate, DC-VAL-06 / DC-TXV) is still **not** shipped — carried forward from prior CODEMAP as a known residual gap. Total stays at 31.

> **Carried residual gaps (unchanged at HEAD).** (a) `ci/ci_check_no_fail_open_in_validation.sh` not shipped. (b) The `ade_ledger::mempool` BLUE/GREEN broad partition is review-and-test-enforced, not mechanical (the two narrow N-E gates pin specific properties but not the whole partition — `policy.rs` and `canonicalize.rs` could still import `tx_validity` without a script failing). (c) The `ade_ledger::consensus_input_extract` RED sub-classification is review-and-doc-enforced, not mechanical. (d) The `ade_runtime::consensus` GREEN/RED partition is "by file" — no dedicated CI script. (e) **CE-N-B-6 manual evidence (live tip-agreement) still pending operator log** — the `live_consensus_session` binary exists and the closure-gate test is in place, but the live capture log is not yet committed (in contrast to CE-N-E-6, which is now CLOSED at HEAD via `CE-N-E-6_2026-05-25.log`). (f) The no-owned-`String` / no-`Box<dyn>` shape of `ade_types::conway::cert` types is outside the `TARGETS` array of `ci_check_consensus_closed_enums.sh`. (g) The `ade_ledger::delegation` owner-tagged action types are outside any narrow closed-enum grep gate. (h) DC-LEDGER-08 / DC-LEDGER-09 / DC-LEDGER-10's real epoch-576 oracle is environment-blocked (absent UMap snapshot). (i) The Conway block-body vkey-witness closure is still not wired into the `rules.rs` block-body loop. (j) The GREEN sub-classification of `ade_core_interop::{tx_submission, local_tx_submission}` inside the RED `ade_core_interop` crate prefix is review-and-doc enforced; no narrow CI gate scopes the partition. (k) The GREEN sub-classification of `ade_ledger::mempool::canonicalize` inside the BLUE `ade_ledger` crate prefix is partially gated by `ci_check_mempool_ingress_replay.sh` check 6 (no `HashMap`/async/clock) but not at the broad partition level. (l) **NEW — the S6 `live_tx_submission_session` binary's RED classification is review-and-doc enforced.** No CI script scopes the new binary; its RED color rests on its header comment, its placement under `src/bin/`, and the matching precedent from `live_consensus_session.rs`. (m) **NEW — `CE-NODE-N2C-LTX` is a deferred cross-cluster obligation** carried into the future node-binary cluster (Ade must host an N2C server to receive `cardano-cli` submissions, which the current crate cannot do). Procedure spec at `docs/clusters/completed/PHASE4-N-E/CE-N-E-7_PROCEDURE.md` is the canonical spec for future closure.
