# Module Authority Map — Ade

> **Status:** Living architectural document. Regenerated; not hand-edited.
> Per-project instance of `~/.claude/methodology/templates/codemap.md`.

> 11 crates, 424 canonical types, 1630 tests, 55 CI checks at HEAD (`f15102f`).

---

## Conventions

- A **module** in Ade is a Cargo workspace crate (smallest independently-buildable unit). One exception: `ade_network` is split by *submodule color* — its BLUE submodules and its RED submodules are documented as two separate entries below, because `.idd-config.json` `core_paths` resolves BLUE at the submodule path level rather than crate-wide. The `ade_core::consensus` submodule sits *inside* the BLUE `ade_core` crate and is covered by that entry. The `ade_ledger::block_validity` / `ade_ledger::consensus_view` / `ade_ledger::tx_validity` / `ade_ledger::mempool::admit` / `ade_ledger::mempool::ingress` / `ade_ledger::cert_classify` modules sit inside the BLUE `ade_ledger` crate and are covered by that entry; the `ade_ledger::producer` sub-tree (PHASE4-N-C — `forge`, `self_accept`, `state`; PHASE4-N-G — `served_chain`), the `ade_ledger::block_body_hash` top-level module (PHASE4-N-C S4 single body-hash authority), the `ade_ledger::receive` sub-tree (PHASE4-N-H), the `ade_ledger::rollback` sub-tree (PHASE4-N-I — `traits`, `error`, `materialize`, `commit`), and **NEW — the `ade_ledger::snapshot` sub-tree (PHASE4-N-J — `error`, `chain_dep`, `utxo_state`, `cert_state`, `epoch_state`, `gov_state`, `ledger`, `framing`)** are likewise BLUE under the already-BLUE `ade_ledger` crate prefix. The RED `ade_ledger::consensus_input_extract`, the GREEN `ade_ledger::mempool::policy`, and the GREEN `ade_ledger::mempool::canonicalize` sit inside the BLUE `ade_ledger` crate but carry a different color by their own module doc-comment and the cluster TCB Color Maps — they are surfaced as sub-classification notes inside the `ade_ledger` entry. The `ade_testkit::mempool` sub-tree (PHASE4-N-E S2), the `ade_testkit::governance` sub-tree (PROPOSAL-PROCEDURES-DECODE PP-S2), and the `ade_testkit::producer` sub-tree (PHASE4-N-C) sit inside the GREEN `ade_testkit` crate, classified GREEN by their own module doc-comments and the cluster TCB Color Maps. **PHASE4-N-G — `ade_runtime::network` sub-tree** sits inside the RED `ade_runtime` crate and hosts the per-peer N2N **server** session driver (RED). **PHASE4-N-H — `ade_runtime::receive` sub-tree** sits inside the RED `ade_runtime` crate and hosts the GREEN+RED receive-side glue: `events_to_state` (GREEN), `in_memory_chain_write` (GREEN), `orchestrator` (RED). **PHASE4-N-I — `ade_runtime::rollback` sub-tree** sits inside the RED `ade_runtime` crate and hosts the GREEN+RED rollback adapter glue: `cadence` (GREEN; `should_snapshot_after_block` + `SnapshotCadence`), `in_memory_cache` (GREEN; `InMemorySnapshotCache` impl `SnapshotReader`), `chaindb_block_source` (GREEN; `ChainDbBlockSource<'a, D: ChainDb>` impl `BlockSource`), `snapshot_writer` (RED; `maybe_capture_snapshot` post-admission hook). **NEW — PHASE4-N-J extends the `ade_runtime::rollback` sub-tree with `persistent_cache` (GREEN; `PersistentSnapshotCache<'a, S: SnapshotStore>` impl `SnapshotReader` — bridges `ade_ledger::snapshot::framing` to `ade_runtime::chaindb::SnapshotStore`; closes `DC-CONS-21`).**
- Modules are listed by TCB color (BLUE → GREEN → RED), alphabetical within each color.
- TCB color sources, in order of authority:
  1. `.idd-config.json` `core_paths` — substring match against absolute path. BLUE matches: `ade_codec`, `ade_types`, `ade_crypto`, `ade_core`, `ade_ledger` (covers the new `ade_ledger::snapshot` sub-tree and the prior `ade_ledger::rollback` sub-tree), `ade_plutus`, and the 9 `ade_network` submodule paths (`mux/frame.rs`, `codec/`, `handshake/`, `chain_sync/`, `block_fetch/`, `tx_submission/`, `keep_alive/`, `peer_sharing/`, `n2c/`).
  2. `.idd-config.json` `_core_paths_doc` — `ade_runtime` is RED; `ade_testkit` is GREEN; `ade_node` is RED; `ade_network::mux::transport` and `ade_network::session` are RED; `ade_network::lib` and `ade_network::mux::mod` are GREEN barrels. `ade_core_interop` is **RED** per its `Cargo.toml` header comment and the PHASE4-N-B TCB Color Map; the `ade_core_interop::tx_submission` (S4) and `ade_core_interop::local_tx_submission` (S5) modules carry a GREEN sub-classification by their own module doc-comments. The RED binaries `ade_core_interop::bin::{live_tx_submission_session, live_block_production_session, live_block_fetch_session, live_block_follow_session}` are the operator-action live evidence pattern (RED by their own file-header comments and the established `live_consensus_session` precedent).
  3. `docs/clusters/completed/PHASE4-N-B/cluster.md` § "TCB Color Map" — `era_schedule`, `praos_state`, `vrf_cert`, `nonce`, `op_cert`, `leader_schedule`, `header_validate`, `fork_choice`, `rollback` (the consensus-side header rollback applier, not the ledger-side `ade_ledger::rollback`) are BLUE; `chain_selector`, `candidate_fragment` are GREEN; `genesis_parser` is RED; `ade_testkit::consensus` is GREEN; `ade_core_interop` is RED.
  4. `docs/clusters/completed/PHASE4-B1/cluster.md` § "TCB Color Map" — `ade_ledger::consensus_view`, `ade_ledger::block_validity` are BLUE; `ade_core::consensus::{header_validate, kes_check}` extensions are BLUE; `ade_testkit::validity` is GREEN; `ade_ledger::consensus_input_extract` is RED.
  5. PHASE4-B2 (tx-validity agreement) — `ade_ledger::tx_validity::*` and `ade_ledger::mempool::admit` are BLUE; `ade_ledger::mempool::policy` is GREEN (Tier-5); `ade_testkit::tx_validity` is GREEN.
  6. PHASE4-B3/B5 (tx value-conservation + Conway governance-cert accumulation) — all new/changed modules are BLUE under the already-BLUE crate prefixes.
  7. PHASE4-N-E (mempool wire ingress) — `ade_ledger::mempool::ingress` is BLUE (S1); `ade_ledger::mempool::canonicalize` is GREEN (S3); `ade_testkit::mempool::ingress_replay` is GREEN (S2); `ade_core_interop::tx_submission` is GREEN (S4); `ade_core_interop::local_tx_submission` is GREEN (S5); `ade_core_interop::bin::live_tx_submission_session` is RED (S6).
  8. PROPOSAL-PROCEDURES-DECODE — `ade_codec::conway::governance` is BLUE; the new `ProposalProcedure` canonical type in `ade_types::conway::governance` is BLUE; `ade_testkit::governance::proposal_procedures_replay` is GREEN.
  9. PHASE4-N-C — `ade_ledger::producer::{forge, self_accept, state}` BLUE; `ade_ledger::block_body_hash` BLUE; `ade_codec::shelley::{opcert, tx_components}` BLUE; `ade_core::consensus::opcert_validate` BLUE; `ade_crypto::kes::KesSignature` BLUE; `ade_runtime::producer::{signing, keys, scheduler, broadcast}` RED; `ade_runtime::producer::tick_assembler` GREEN; `ade_testkit::producer::*` GREEN; `ade_core_interop::bin::live_block_production_session` RED.
  10. PHASE4-N-G — `ade_network::chain_sync::server` and `ade_network::block_fetch::server` BLUE; `ade_ledger::producer::served_chain` BLUE; the `accepted_block_header_bytes` accessor on `ade_ledger::block_validity::header_input` BLUE; `ade_runtime::producer::{broadcast_to_served, served_chain_lookups}` GREEN; `ade_runtime::network::n2n_server` RED; `ade_core_interop::bin::live_block_fetch_session` RED.
  11. `docs/clusters/completed/PHASE4-N-H/cluster.md` § "TCB Color Map" — `ade_ledger::receive::{admitted, chain_write, events, pending_header_cache, reducer}` are BLUE; `ade_runtime::receive::{events_to_state, in_memory_chain_write}` are GREEN; `ade_runtime::receive::orchestrator` is RED; `ade_core_interop::bin::live_block_follow_session` is RED.
  12. `docs/clusters/completed/PHASE4-N-I/cluster.md` § "TCB Color Map" — `ade_ledger::rollback::{traits, error, materialize, commit}` (S1, S2, S3) are **BLUE** (under the already-BLUE `ade_ledger` crate prefix; closure shape gated mechanically by `ci/ci_check_rollback_materialize_closure.sh`); the `ChainDbWrite::rollback_to_slot(slot)` trait method extension on `ade_ledger::receive::chain_write` (S3) is **BLUE** (preserves the admission-gate-shape extension into the rollback path); the `RollbackContext<'a>` struct + `receive_apply`'s new `rollback_ctx: Option<&RollbackContext>` parameter + the new `roll_backward` helper in `ade_ledger::receive::reducer` (S6) are **BLUE** (under the already-BLUE `ade_ledger::receive` sub-tree; closure shape gated by the existing `ci/ci_check_receive_reducer_closure.sh`); `ade_runtime::rollback::{cadence, in_memory_cache, chaindb_block_source}` (S4) are **GREEN** (pure deterministic glue + thin adapters; cadence-purity + cache-purity gated mechanically by `ci/ci_check_snapshot_cadence_purity.sh`); `ade_runtime::rollback::snapshot_writer` (S5) is **RED** (post-admission write hook composing RED dispatch outcomes with the GREEN cadence + cache).
  13. **NEW — `docs/clusters/PHASE4-N-J/cluster.md` § Slice index TCB column** — `ade_ledger::snapshot::{error, chain_dep, utxo_state, cert_state, epoch_state, gov_state, ledger, framing}` (S1–S7) are **BLUE** (under the already-BLUE `ade_ledger` crate prefix; canonical encode/decode for `(LedgerState, PraosChainDepState)`; single-authority shape gated mechanically by `ci/ci_check_snapshot_encoder_closure.sh` over the public `encode_snapshot`/`decode_snapshot`/`encode_ledger_state`/`decode_ledger_state`/`encode_chain_dep`/`decode_chain_dep` function names and the `SCHEMA_VERSION` constant — repo-wide grep gate per CN-STORE-08, DC-STORE-08, DC-STORE-09). `ade_runtime::rollback::persistent_cache` (S8) is **GREEN** (pure adapter bridging `ade_ledger::snapshot::framing::{encode_snapshot, decode_snapshot}` to `ade_runtime::chaindb::SnapshotStore`; impl `SnapshotReader` for `PersistentSnapshotCache`; closes DC-CONS-21). No new operator-action live binary; no new RO-LIVE rule.

- **Active cluster at HEAD (none).** PHASE4-N-J (persistent ledger snapshot encoder) is **closed at HEAD `f15102f`** and archived (cluster artifacts still under `docs/clusters/PHASE4-N-J/`; cluster-close archive move happens with this regeneration's grounding-doc refresh). The cluster closes the PHASE4-N-I deferred `open_obligation` on `DC-CONS-21` (snapshot encode/decode round-trip equivalence). The cluster's new registry rules at `status = "enforced"`: `DC-STORE-08` (snapshot embedded source fingerprint cross-check), `DC-STORE-09` (snapshot version-tag rejection before payload work), `CN-STORE-08` (single combined-snapshot encoder/decoder authority). `DC-CONS-21` flips to `enforced` (the `open_obligation` is removed; the persistent on-disk encoder is shipped and the round-trip-equivalence property is mechanically proven by the cross-impl `persistent_cache_matches_in_memory_cache_semantics` test against `InMemorySnapshotCache`). Existing rules strengthened in PHASE4-N-J (per the cluster doc): `T-DET-01` (`strengthened_in += "PHASE4-N-J"` — snapshot bytes are a new authoritative-deterministic surface), `T-ENC-01` (`strengthened_in += "PHASE4-N-J"` — canonical persisted/replay byte path; note this is internal replay-evidence bytes, NOT Cardano wire-byte hash-critical), `DC-CONS-20` (`strengthened_in += "PHASE4-N-J"` — rollback atomicity now restart-safe), `DC-CONS-22` (`strengthened_in += "PHASE4-N-J"` — replay-forward over persistent snapshots equals direct-apply), `CN-STORE-07` (`strengthened_in += "PHASE4-N-J"` — single materialize authority's input source now persistent). Existing operator-action `open_obligation`s carried unchanged from N-I close: `CN-CONS-06` (live producer stake; via N-C), `RO-LIVE-01` (live block-fetch evidence; via N-G), `RO-LIVE-02` (live cross-impl follow evidence; via N-H). The pre-existing `ade_network::session` placeholder gap is untouched. `docs/clusters/` currently contains the archive of the PHASE4-N-B follow-on (CE-N-B-6 manual evidence still pending operator log), the active PHASE4-N-J directory (about to move to `completed/`), and the closed `completed/` set (19 directories: COMMITTEE-CRED-FIDELITY, DREP-VOTE-FIDELITY, ENACTMENT-COMMITTEE-FIDELITY, ENACTMENT-COMMITTEE-WRITEBACK, OQ5-CREDENTIAL-FIDELITY, PHASE4-B1..B5, PHASE4-B3F, PHASE4-N-A, N-B, N-C, N-D, N-E, N-G, N-H, N-I, PROPOSAL-PROCEDURES-DECODE; PHASE4-N-J archives after this regeneration).

- **Delta since prior CODEMAP HEAD `75f75da` (this regeneration — PHASE4-N-J cluster, 8 slices S1–S8 over commits `ab4165a..f15102f`).**

  **Structural deltas summarized:**
  - **NEW BLUE sub-tree:** `ade_ledger::snapshot/` (S1–S7). Nine files: `mod.rs` (42 LOC barrel — `pub use` re-exports `encode_chain_dep`/`decode_chain_dep`, `encode_utxo_state`/`decode_utxo_state`, `encode_cert_state`/`decode_cert_state`, `encode_epoch_state`/`decode_epoch_state`, `encode_gov_state`/`decode_gov_state`, `encode_pparams`/`decode_pparams`, `encode_conway_deposit_params`/`decode_conway_deposit_params`, `encode_ledger_state`/`decode_ledger_state`, `encode_snapshot`/`decode_snapshot`, `SCHEMA_VERSION`, and the three closed error types); `error.rs` (130 LOC + 3 inline tests — closed `SnapshotEncodeError` sum with one variant `EraNotSupported { era: CardanoEra }`; closed `SnapshotDecodeError` sum with five variants `Cbor(CodecError)` / `UnknownVersion { expected: u32, found: u32 }` / `FingerprintMismatch { expected: Hash32, actual: Hash32 }` / `EraNotSupported { era: CardanoEra }` / `Structural { reason: StructuralReason }`; closed `StructuralReason` sum with nine variants `ArrayLengthMismatch` / `MapLengthExceeded` / `UnexpectedNull` / `UnexpectedNonNull` / `NonceLengthMismatch` / `PoolIdLengthMismatch` / `Hash32LengthMismatch` / `Hash28LengthMismatch` / `EraTagOutOfRange` — no `String` anywhere); `chain_dep.rs` (261 LOC + 5 inline tests, S1 — `encode_chain_dep`/`decode_chain_dep` for `PraosChainDepState` over a definite array(9) of evolving/candidate/epoch/previous_epoch/lab nonces + optional last_epoch_block/last_slot/last_block_no + op_cert_counters; BTreeMap traversal everywhere; all containers definite-length); `utxo_state.rs` (443 LOC + 5 inline tests, S2 — `encode_utxo_state`/`decode_utxo_state` for `UTxOState` over a canonical `map(N) [TxIn -> tagged_tx_out]` with three TxOut shapes (Byron/ShelleyMary/AlonzoPlus); BTreeMap traversal; AlonzoPlus carries opaque raw bytes for restart-safe field-level round-trip without re-implementing Alonzo+ semantics); `cert_state.rs` (387 LOC + 4 inline tests, S3 — `encode_cert_state`/`decode_cert_state` for `CertState` over a definite array(5) of registrations/delegations/rewards/pools/retiring maps; StakeCredential as `array(2)[variant, bytes(28)]`; PoolParams as `array(7)`); `epoch_state.rs` (279 LOC + 3 inline tests, S4 — `encode_epoch_state`/`decode_epoch_state` for `EpochState` over a definite array(7) of epoch/slot/snapshot_state/reserves/treasury/block_production/epoch_fees; SnapshotState is `array(3)` of stake_snapshot per Mark/Set/Go); `gov_state.rs` (1093 LOC + 7 inline tests, S5 — `encode_pparams`/`decode_pparams` for `ProtocolParameters` over a definite array(24); `encode_conway_deposit_params`/`decode_conway_deposit_params`; `encode_gov_state`/`decode_gov_state` for `ConwayGovState` including the closed 7-variant GovAction enum, vote delegations, committee, drep expiry, voting thresholds, committee hot keys); `ledger.rs` (356 LOC + 7 inline tests, S6 — `encode_ledger_state`/`decode_ledger_state` for `LedgerState` over a definite array(9) of era + max_lovelace_supply + track_utxo flag + the five sub-state-encoded byte payloads (S2-S5 outputs as nested `bstr`) + two optional gov-state/deposit-params byte payloads; sub-state bodies ride inside `bstr` containers so the outer decoder hands each slice to its specialized decoder without sharing offset state; pre-Conway → `SnapshotEncodeError::EraNotSupported`); `framing.rs` (273 LOC + 6 inline tests, S7 — `encode_snapshot`/`decode_snapshot` for `(LedgerState, PraosChainDepState)` over a definite array(4) of `[version, source_fingerprint(32), ledger_state_bytes, chain_dep_bytes]`; `pub const SCHEMA_VERSION: u32 = 1`; version tag is read + verified BEFORE any payload work (DC-STORE-09 fail-fast); embedded source fingerprint is `fingerprint(ledger).combined` and is verified after decode via `decode_snapshot`'s recompute step (DC-STORE-08); **CN-STORE-08 single combined-snapshot encoder/decoder authority**: the `encode_snapshot`/`decode_snapshot` pair in this module is the SOLE public authority for `(LedgerState, PraosChainDepState) <-> bytes` conversion in the workspace, enforced by `ci_check_snapshot_encoder_closure.sh`'s repo-wide grep on the public function names).
  - **NEW GREEN file (inside RED `ade_runtime::rollback`):** `persistent_cache.rs` (251 LOC + 6 inline tests, S8 — `PersistentSnapshotCache<'a, S: SnapshotStore + ?Sized> { store: &'a S }` borrows a `SnapshotStore` and impls `SnapshotReader`. Reader path: `list_snapshot_slots()` (ascending per trait contract) → `.iter().rev().find(|s| *s <= target_slot)` → `get_snapshot(slot)` → `framing::decode_snapshot(&bytes)`; decode failures yield `None` (treats corrupt/missing snapshots as "no usable snapshot here", never panics). Writer path: `capture(slot, ledger, chain_dep)` → `framing::encode_snapshot` → `SnapshotStore::put_snapshot`. Closed `PersistentCacheError` sum with three variants `Encode(SnapshotEncodeError)` / `Decode(SnapshotDecodeError)` / `Store(ChainDbError)`. `pub const PERSISTENT_CACHE_SCHEMA_VERSION: u32 = SCHEMA_VERSION` mirrors framing for out-of-crate consumers that want to pin the wire version without depending on `ade_ledger::snapshot::framing`).
  - **NEW closed canonical types (+3 BLUE).** Per source file: `SnapshotEncodeError`, `SnapshotDecodeError`, `StructuralReason` (in `ade_ledger::snapshot::error`). The GREEN type `PersistentSnapshotCache<'a, S: SnapshotStore>` and the closed error sum `PersistentCacheError` live inside the RED `ade_runtime` crate prefix and are not counted in the BLUE canonical-type total. **Canonical type count: 421 → 424 (+3).** Per-BLUE-crate breakdown at HEAD: `ade_codec` 10, `ade_types` 81, `ade_crypto` 13, `ade_core` 44, `ade_ledger` **159 (+3)**, `ade_plutus` 8, BLUE `ade_network` 109 (5+38+9+11+10+5+5+5+21) = 424.
  - **NEW CI gates (+1):** `ci/ci_check_snapshot_encoder_closure.sh` (S7; CN-STORE-08 + DC-STORE-08 + DC-STORE-09; the only public `encode_snapshot`/`decode_snapshot` pair lives at `crates/ade_ledger/src/snapshot/framing.rs` — repo-wide grep gate on the function names; the only public `encode_ledger_state`/`decode_ledger_state` pair lives at `crates/ade_ledger/src/snapshot/ledger.rs`; the only public `encode_chain_dep`/`decode_chain_dep` pair lives at `crates/ade_ledger/src/snapshot/chain_dep.rs`; the only `pub const SCHEMA_VERSION` lives at `framing.rs` (DC-STORE-09); positive presence of `FingerprintMismatch` reference in `framing.rs` (DC-STORE-08 cross-check site) + `UnknownVersion` reference in `framing.rs` (DC-STORE-09 fail-fast site)). **CI script count: 54 → 55 (+1).**
  - **NEW registry rules / strengthenings:** 3 new entries — `DC-STORE-08` (enforced, snapshot embedded source fingerprint cross-check), `DC-STORE-09` (enforced, snapshot version-tag rejection before payload work), `CN-STORE-08` (enforced, single combined-snapshot encoder/decoder authority — the `framing::{encode_snapshot, decode_snapshot}` pair is the sole project-wide authority for `(LedgerState, PraosChainDepState) <-> bytes`). `DC-CONS-21` (already declared in N-I) **flips from `declared` (with `open_obligation = "persistent_ledger_snapshot_encoding_follow_on_cluster"`) to `enforced`** — the open_obligation is removed; `strengthened_in` gains `PHASE4-N-J`. 5 existing rules strengthened: `T-DET-01`, `T-ENC-01`, `DC-CONS-20`, `DC-CONS-22`, `CN-STORE-07` all gain `PHASE4-N-J` in `strengthened_in`.
  - **NEW test inventory (+46).** Per-crate at HEAD: `ade_codec` 162 (unchanged), `ade_types` 23 (unchanged), `ade_crypto` 51 (unchanged), `ade_core` 126 (unchanged), `ade_ledger` 534 → **574 (+40)** (snapshot inline: error 3 + chain_dep 5 + utxo_state 5 + cert_state 4 + epoch_state 3 + gov_state 7 + ledger 7 + framing 6 = 40), `ade_plutus` 28 (unchanged), `ade_testkit` 312 (unchanged), `ade_runtime` 121 → **127 (+6)** (persistent_cache inline 6), `ade_network` 200 (unchanged), `ade_node` 0, `ade_core_interop` 27 (unchanged). **Test inventory 1584 → 1630 (+46)**, reported approximate per the template's fallback rule (test runner not executed; counts from `grep -cE "#\[test\]|#\[tokio::test\]"` workspace-wide).
  - **No new crate-level outbound edges.** PHASE4-N-J reuses the established `ade_runtime → {ade_ledger, ade_core, ade_codec, ade_types, ade_crypto}` and `ade_ledger → {ade_core, ade_codec, ade_types, ade_crypto, ade_plutus, minicbor}` sets. The new `ade_ledger::snapshot::*` files import `ade_codec::cbor::{canonical_width, read_*, write_*, ContainerEncoding, IntWidth, MAJOR_NEGATIVE}` + `ade_codec::CodecError` + `ade_core::consensus::praos_state::{Nonce, OpCertCounterMap, PraosChainDepState}` + `ade_types::{shelley::cert::StakeCredential, tx::{Coin, PoolId, TxIn}, address::Address, conway::{cert::DRep, governance::*}, BlockNo, CardanoEra, EpochNo, Hash28, Hash32, SlotNo}` + intra-crate `crate::{delegation, epoch, fingerprint, pparams, rational, state, utxo, value}` over existing edges. The new `ade_runtime::rollback::persistent_cache` file imports `ade_ledger::rollback::SnapshotReader` + `ade_ledger::snapshot::{SnapshotDecodeError, SnapshotEncodeError, framing::{decode_snapshot, encode_snapshot, SCHEMA_VERSION}}` + `ade_ledger::state::LedgerState` + `ade_core::consensus::praos_state::PraosChainDepState` + `crate::chaindb::{ChainDbError, SnapshotStore}` over existing edges.

- Counts:
  - Crates: 11, from `Cargo.toml` `[workspace] members`. Unchanged.
  - Canonical types: 424, from `grep -rE "^(pub )?(struct|enum) "` across the full BLUE scope. Breakdown: `ade_codec` 10, `ade_types` 81, `ade_crypto` 13, `ade_core` 44, `ade_ledger` **159 (+3)**, `ade_plutus` 8, plus the 9 BLUE `ade_network` submodule paths 109. Registry `canonical_type_registry: null`, so a structural count is used. **+3 since `75f75da`** — N-J snapshot closed-error sums.
  - Tests: 1630 — count of `#[test]` / `#[tokio::test]` attributes across `crates/`. Reported as approximate per the template's fallback rule. **+46 since `75f75da`**, breakdown above.
  - CI checks: 55 — file count under `ci/ci_check_*.sh`. **+1 since `75f75da`** — `ci_check_snapshot_encoder_closure.sh` (PHASE4-N-J S7). Full inventory at HEAD: `admitted_block_closure`, `block_fetch_server_closure`, `broadcast_to_served_purity`, `cbor_round_trip`, `ce_n_a_5_proof`, `chaindb_contract`, `chaindb_crash_safety`, `chain_sync_server_closure`, `consensus_closed_enums`, `constitution_coverage`, `conway_cert_classification_closed`, `credential_discriminant_closed`, `crypto_vectors`, `dependency_boundary`, `deposit_param_authority`, `differential_divergence`, `forbidden_patterns`, `forge_purity`, `gov_cert_accumulation_closed`, `hash_uses_wire_bytes`, `hfc_translation`, `ingress_chokepoints`, `ledger_determinism`, `mempool_ingress_closure`, `mempool_ingress_replay`, `module_headers`, `n2n_server_no_signing_dep`, `no_async_in_blue`, `no_chaindb_in_consensus_blue`, `no_density_in_fork_choice`, `no_float_in_consensus`, `no_parallel_header_splitter`, `no_private_keys_in_corpus`, `no_producer_body_encoder`, `no_secrets`, `no_semantic_cfg`, `no_signing_in_blue`, `opcert_closed`, `pallas_quarantine`, `private_key_custody`, `producer_corpus_present`, `proposal_procedures_closed`, `receive_orchestrator_no_producer_dep`, `receive_paths_corpus_present`, `receive_reducer_closure`, `receive_replay_purity`, `recovery_contract`, `ref_provenance`, `rollback_materialize_closure`, `scheduler_closure`, `self_accept_gate`, `served_chain_closure`, `server_paths_corpus_present`, `snapshot_cadence_purity`, `snapshot_encoder_closure` *(NEW)*. The forward-looking `ci/ci_check_no_fail_open_in_validation.sh` (the `if len == K { check } else { skip }` grep gate, DC-VAL-06 / DC-TXV) is still **not** shipped. No `.github/workflows/` yet.

---

## BLUE Modules — Pure Functional Core

> **Shared header (applies to every BLUE entry below).** Every `.rs` source file begins with the contract banner
> `// Core Contract:` and the following deny attributes are present in each crate's `lib.rs` (or, for `ade_network`,
> at the crate root — the BLUE submodules inherit them):
> `#![deny(unsafe_code)]`, `#![deny(clippy::unwrap_used)]`, `#![deny(clippy::expect_used)]`,
> `#![deny(clippy::panic)]`, `#![deny(clippy::float_arithmetic)]`.
> CI scripts that enforce the shared rules (all 7 scope the full BLUE set — the 6 BLUE crates plus the 9 BLUE `ade_network` submodule paths declared in `.idd-config.json` `core_paths`):
> - `ci/ci_check_module_headers.sh` — banner first-line check.
> - `ci/ci_check_forbidden_patterns.sh` — `HashMap`, `HashSet`, `IndexMap`/`IndexSet`/`indexmap::`, `SystemTime`, `Instant`, `std::fs`, `std::net`, `tokio`, `async fn`, `f32`/`f64`, `anyhow`, `rand::thread_rng`, `thread::spawn`, plus `unsafe` outside a documented allowlist.
> - `ci/ci_check_dependency_boundary.sh` — no BLUE crate depends on a RED crate.
> - `ci/ci_check_no_signing_in_blue.sh` — `SigningKey`/`SecretKey`/`PrivateKey`/`private_key`/`sign_message`/`sign_block` forbidden in BLUE.
> - `ci/ci_check_no_semantic_cfg.sh` — `#[cfg(feature = ...)]` and `cfg!(feature = ...)` forbidden in BLUE.
> - `ci/ci_check_hash_uses_wire_bytes.sh` — no hashing of `canonical_bytes` / re-encoded bytes in BLUE.
> - `ci/ci_check_ingress_chokepoints.sh` — only named `decode_*` chokepoints construct `PreservedCbor`.
> - `ci/ci_check_pallas_quarantine.sh` — `pallas-*` references confined to `ade_plutus`.
> - `ci/ci_check_no_async_in_blue.sh` *(PHASE4-N-A, S-A1)* — async constructs forbidden anywhere in the BLUE scope. Enforces DC-CORE-01.
>
> Three additional CI scripts narrow the shared header to the `ade_core::consensus` tree (PHASE4-N-B): `ci/ci_check_no_chaindb_in_consensus_blue.sh`, `ci/ci_check_no_float_in_consensus.sh`, `ci/ci_check_consensus_closed_enums.sh` (TARGETS scope: `crates/ade_core/src/consensus/`, `crates/ade_ledger/src/block_validity/`, `crates/ade_ledger/src/tx_validity/`, `crates/ade_ledger/src/mempool/`).
> A fourth narrow check enforces a single fork-choice rule: `ci/ci_check_no_density_in_fork_choice.sh` (DC-CONS-03).
> A fifth narrow check (PHASE4-B3) enforces deposit-parameter authority: `ci/ci_check_deposit_param_authority.sh` (DC-TXV-07).
> A sixth narrow check (PHASE4-B5) enforces gov-cert accumulation closure: `ci/ci_check_gov_cert_accumulation_closed.sh` (DC-LEDGER-09).
> A seventh narrow check (OQ5/COMMITTEE/DREP/ENACTMENT-COMMITTEE-FIDELITY + ENACTMENT-COMMITTEE-WRITEBACK) enforces credential-discriminant closure: `ci/ci_check_credential_discriminant_closed.sh` (DC-LEDGER-10).
> An eighth narrow check (PROPOSAL-PROCEDURES-DECODE, PP-S1) enforces proposal_procedures closure: `ci/ci_check_proposal_procedures_closed.sh` (DC-LEDGER-11).
>
> **Eight narrow checks added by PHASE4-N-C (the producer-authority gate set):**
> - `ci/ci_check_forge_purity.sh` (DC-CONS-13/14/15) — forge body purity.
> - `ci/ci_check_no_producer_body_encoder.sh` (DC-CONS-16) — single body-hash authority, repo-wide.
> - `ci/ci_check_no_private_keys_in_corpus.sh` (DC-CRYPTO-03/04/05 corpus discipline) — producer + (N-G S5 extension) server-paths corpora private-key-free.
> - `ci/ci_check_opcert_closed.sh` (DC-CONS-11/12) — closed opcert encoders + acceptance authority.
> - `ci/ci_check_private_key_custody.sh` (DC-CRYPTO-03/04/05 + OP-OPS-04) — signing primitives RED-confined.
> - `ci/ci_check_producer_corpus_present.sh` (CN-CONS-06 mechanical half) — fixture-presence guard.
> - `ci/ci_check_scheduler_closure.sh` (DC-CONS-13 + DC-MEM-03 + OP-OPS-05) — RED scheduler purity + BroadcastQueue value-take.
> - `ci/ci_check_self_accept_gate.sh` (CN-CONS-07) — type-level broadcast-gate guards.
>
> **Seven narrow checks added by PHASE4-N-G (the producer-side server-role gate set):**
> - `ci/ci_check_no_parallel_header_splitter.sh` (DC-CONS-16 strengthening + DC-CONS-18, S1) — repo-wide grep gate: the only `pub fn .*header_bytes` site is `accepted_block_header_bytes` in `crates/ade_ledger/src/block_validity/header_input.rs`.
> - `ci/ci_check_served_chain_closure.sh` (CN-CONS-07 strengthening + CE-N-G-2, S2) — scopes `crates/ade_ledger/src/producer/served_chain.rs`.
> - `ci/ci_check_chain_sync_server_closure.sh` (DC-PROTO-08, S3) — scopes `crates/ade_network/src/chain_sync/server.rs`.
> - `ci/ci_check_block_fetch_server_closure.sh` (DC-CONS-17 enforcement foundation, S4) — scopes `crates/ade_network/src/block_fetch/server.rs`.
> - `ci/ci_check_broadcast_to_served_purity.sh` (CE-N-G-5, S5) — scopes the GREEN adapter pair under `ade_runtime::producer/`.
> - `ci/ci_check_n2n_server_no_signing_dep.sh` (S6 key-boundary doctrine) — scopes `crates/ade_runtime/src/network/`.
> - `ci/ci_check_server_paths_corpus_present.sh` (S7 presence) — checks the two integration tests + binary src + Cargo.toml entry + procedure doc.
>
> **Four narrow checks added by PHASE4-N-H (the receive-side authority gate set):**
> - `ci/ci_check_admitted_block_closure.sh` (S1; CE-N-H-1 + CN-PROTO-07) — repo-wide grep on `AdmittedBlock` construction + events.rs closure.
> - `ci/ci_check_receive_reducer_closure.sh` (S2; CE-N-H-2 + CN-CONS-08 + DC-CONS-19) — scopes `crates/ade_ledger/src/receive/reducer.rs`.
> - `ci/ci_check_receive_replay_purity.sh` (S3; CE-N-H-3 + DC-PROTO-09) — scopes `crates/ade_runtime/src/receive/{events_to_state,in_memory_chain_write}.rs`.
> - `ci/ci_check_receive_orchestrator_no_producer_dep.sh` (S4; key-boundary) — scopes `crates/ade_runtime/src/receive/`.
> - `ci/ci_check_receive_paths_corpus_present.sh` (S6; presence) — checks the three N-H integration tests + binary src + Cargo.toml entry + procedure doc.
>
> **Two narrow checks added by PHASE4-N-I (the rollback authority gate set):**
> - `ci/ci_check_rollback_materialize_closure.sh` (S2; CN-STORE-07 + DC-CONS-22) — scopes `crates/ade_ledger/src/rollback/materialize.rs` for production purity (no `HashMap`/`HashSet`/wall-clock/`tokio`/`rand`); **CN-STORE-07 single-authority scan over `crates/ade_ledger/src/rollback/*.rs`** — exactly one `pub fn` whose signature contains `(LedgerState[^{]*PraosChainDepState` (the function name is `materialize_rolled_back_state`); positive grep for `block_validity` call site (CN-CONS-08 single admission authority for the underlying step) and for `pub fn materialize_rolled_back_state` presence.
> - `ci/ci_check_snapshot_cadence_purity.sh` (S4; DC-STORE-07) — scopes `crates/ade_runtime/src/rollback/*.rs` for production purity (no `HashMap`/`HashSet`/wall-clock/`tokio`/`rand`); **SnapshotCadence struct-body single-field discipline** — exactly one `pub <type>:` field in the struct body, field name must contain `every_n_blocks` (operator-tunable cadence pinned out of scope at the type level); positive presence of `pub fn should_snapshot_after_block`, `impl SnapshotReader for InMemorySnapshotCache`, `impl<...> BlockSource for ChainDbBlockSource`.
>
> **NEW — One narrow check added by PHASE4-N-J (the snapshot encoder single-authority gate):**
> - `ci/ci_check_snapshot_encoder_closure.sh` (S7; CN-STORE-08 + DC-STORE-08 + DC-STORE-09) — repo-wide grep on the public encoder/decoder function names: the only `pub fn encode_snapshot`/`pub fn decode_snapshot` pair lives at `crates/ade_ledger/src/snapshot/framing.rs`; the only `pub fn encode_ledger_state`/`pub fn decode_ledger_state` pair lives at `crates/ade_ledger/src/snapshot/ledger.rs`; the only `pub fn encode_chain_dep`/`pub fn decode_chain_dep` pair lives at `crates/ade_ledger/src/snapshot/chain_dep.rs`; the only `pub const SCHEMA_VERSION` lives at `framing.rs` (DC-STORE-09 single version-tag authority); positive presence of `FingerprintMismatch` reference in `framing.rs` (DC-STORE-08 cross-check site) + `UnknownVersion` reference in `framing.rs` (DC-STORE-09 fail-fast site).
>
> Two checks narrow the shared header to the wire-ingress path of `ade_ledger::mempool` (PHASE4-N-E):
> - `ci/ci_check_mempool_ingress_closure.sh` (DC-MEM-03, S1).
> - `ci/ci_check_mempool_ingress_replay.sh` (DC-MEM-04, S2 + S3).
>
> A BLUE crate or BLUE `ade_network` submodule that adds a feature flag, an async function, a `HashMap`, or a RED dep fails CI on push.
> The 3 RED-scope CI scripts (`ci_check_chaindb_contract.sh`, `ci_check_recovery_contract.sh`, `ci_check_chaindb_crash_safety.sh`) and the 1 evidence script `ci_check_ce_n_a_5_proof.sh` are not part of this shared header — they are documented in the cross-module CI matrix at the bottom.

---

### `ade_codec`

> **Status carried from prior CODEMAP — unchanged at HEAD `f15102f`.** PHASE4-N-J did not touch `ade_codec`. The new `ade_ledger::snapshot::*` encoders use the existing `ade_codec::cbor::{canonical_width, read_*, write_*, ContainerEncoding, IntWidth, MAJOR_NEGATIVE}` primitives (a new heavy site) and the existing `ade_codec::CodecError` sum — the snapshot decoder wraps `CodecError` in `SnapshotDecodeError::Cbor(_)` rather than redefining a CBOR error sum. No `ade_codec` API change.

| Attribute | Value |
|-----------|-------|
| **Purpose** | Owns Cardano-canonical CBOR ingress: the only place in the workspace that turns raw bytes into typed semantic values, with wire-byte preservation for every hash-bearing structure. Also owns the standalone opcert byte authority (`shelley::opcert::{encode_opcert, decode_opcert}`) and the canonical Conway-tx preserved-byte splitter (`shelley::tx_components::split_conway_tx_components`). |
| **Creates** | `PreservedCbor<T>`, `RawCbor`, `BlockEnvelope`, `ByronDecodedBlock`, `CodecContext`, `CodecError` (incl. `UnknownCertTag`, `DuplicateMapKey`, `TrailingBytes`, `InvalidCborStructure`), `ContainerEncoding`, `IntWidth`, plus era-tagged block/tx wrappers under `byron/`, `shelley/`, `allegra/`, `mary/`, `alonzo/`, `babbage/`, `conway/`. Functions: `conway::cert::{decode_conway_certs, decode_drep}`, `conway::withdrawals::{decode_withdrawals, withdrawals_sum}`, `conway::governance::{decode_proposal_procedures, encode_proposal_procedures}`, `shelley::cert::read_pool_registration_cert`, both era `decode_stake_credential`, `shelley::opcert::{encode_opcert, decode_opcert, write_opcert_fields_into}`, `shelley::tx_components::split_conway_tx_components`. Types: `OpCertCodecError`, `TxComponents<'a>`. |
| **Interprets** | All canonical Cardano CBOR — block envelopes, era-specific blocks, tx bodies, tx outs, certificates, addresses. Conway certificate array (closed over CDDL tags 0..18, owner-complete) and Conway withdrawals map (deduplicated). Both era credential decoders preserve the key/script tag. Sole authority for `PreservedCbor::new` (constructor is `pub(crate)`). CIP-1694 `proposal_procedure` array. Standalone cardano-cli `OperationalCertificate` 4-tuple. Conway transaction 4-tuple (preserved-byte slicing only). |
| **MUST NOT** | (1) Construct `PreservedCbor` outside `ade_codec` (`pub(crate)` + `ci_check_ingress_chokepoints.sh`). (2) Re-encode wire bytes when computing hashes (`ci_check_hash_uses_wire_bytes.sh`). (3) Use any forbidden BLUE pattern. (4) Depend on any other workspace crate except `ade_types`. (5) `conway::cert` (DC-LEDGER-08) — no unknown-tag swallow; owner-complete; no catch-all. (6) `conway::withdrawals` — no last-wins on duplicate `RewardAccount`. (7) `decode_stake_credential` (DC-LEDGER-10) — must not erase the credential tag. (8) `conway::governance` (DC-LEDGER-11) — no silent skip on unknown `GovAction`; no opaque pass-through at body codec key 20. (9) `shelley::opcert` (DC-CONS-11/12) — cardano-byte-identical 4-tuple; dedicated `OpCertCodecError` variant per shape failure; not a second header-embedded opcert encoder (enforced by `ci_check_opcert_closed.sh`). (10) `shelley::tx_components` (DC-CONS-13/16) — preserved-byte slices that alias the input buffer (no `to_vec` / clones); reject non-4-tuple shapes; not re-encode the boolean validity flag or the auxiliary-data null. |
| **Inbound deps** | `ade_ledger` (heavy — `ade_ledger::rollback::materialize` uses `cbor::envelope::decode_block_envelope` for replay-forward era detection; `ade_ledger::receive::reducer` reuses `block_validity::decode_block`; **NEW heavy consumption site in PHASE4-N-J: `ade_ledger::snapshot::*` uses `ade_codec::cbor::{canonical_width, read_array_header, read_bytes, read_map_header, read_any_int, write_array_header, write_bytes_canonical, write_map_header, write_uint_canonical, write_null, ContainerEncoding, IntWidth, MAJOR_NEGATIVE}` + `ade_codec::CodecError` across all 8 sub-state encoder/decoder files**), `ade_plutus`, `ade_testkit`, `ade_network`, `ade_runtime`, `ade_core_interop`. No new inbound crate edge in PHASE4-N-J. |
| **Outbound deps** | `ade_types`. No external dependencies; std-only. Dev-deps: `serde_json`, `toml`. |
| **Entry points** | `ade_codec::cbor::envelope::decode_block_envelope`, `ade_codec::cbor` (now also a heavy use site from `ade_ledger::snapshot::*` for the canonical-CBOR write helpers), `ade_codec::traits::AdeEncode`, `ade_codec::CodecContext`, per-era `decode_*_block`, `ade_codec::address::decode_address`. B2: `ade_codec::conway::tx::decode_conway_tx_body`. B3: `ade_codec::conway::cert::decode_conway_certs` and `ade_codec::conway::withdrawals::{decode_withdrawals, withdrawals_sum}`. PP-S1: `ade_codec::conway::governance::{decode_proposal_procedures, encode_proposal_procedures}`. N-C: `ade_codec::shelley::opcert::{encode_opcert, decode_opcert, OpCertCodecError}`, `ade_codec::shelley::tx_components::{split_conway_tx_components, TxComponents}`. |
| **Key modules** | `cbor/`, `byron/`, `shelley/` (incl. `cert.rs`, `opcert.rs`, `tx_components.rs`), `allegra/`, `mary/`, `alonzo/`, `babbage/`, `conway/`, `address/`, `preserved.rs`, `traits.rs`, `primitives.rs`, `error.rs`. |

---

### `ade_core`

> **Status carried from prior CODEMAP — unchanged at HEAD `f15102f`.** PHASE4-N-J did not touch `ade_core`. The N-C-introduced `consensus::opcert_validate.rs` module carries forward unchanged. The `ade_core::consensus::praos_state::{Nonce, OpCertCounterMap, PraosChainDepState}` surface is consumed unchanged by the new `ade_ledger::snapshot::chain_dep` encoder/decoder (which serializes `PraosChainDepState`'s nine fields onto the wire as a canonical array(9) — the encoder walks `evolving_nonce`/`candidate_nonce`/`epoch_nonce`/`previous_epoch_nonce`/`lab_nonce`/`last_epoch_block`/`last_slot`/`last_block_no`/`op_cert_counters` and the decoder reconstructs via `PraosChainDepState { ... }` field-literal). No new consumption site beyond a new BLUE encoder/decoder boundary.

| Attribute | Value |
|-----------|-------|
| **Purpose** | BLUE authoritative Praos consensus core. Owns canonical types and pure state-transitions that decide which header / chain Ade accepts: HFC era schedule + slot↔era↔time translation, Praos chain-dep state (nonce / op-cert counters), VRF cert verification + leader-eligibility predicate, KES signature + op-cert period verification wired into header admission, header validation pipeline, fork-choice, **header-level rollback authority (`consensus::rollback::apply_rollback`)** — distinct from the ledger-side `ade_ledger::rollback` (N-I) — leader-schedule query, canonical encodings of all chain-dep state and chain events. Owns the producer-side opcert acceptance authority (`opcert_validate`). |
| **Creates** | **Schedule:** `BootstrapAnchorHash`, `EraSchedule`, `EraSummary`, `EraLocation`. **State:** `PraosChainDepState`, `OpCertCounterMap`, `Nonce`. **Events/points:** `Point`, `ChainHash`, `BlockDistance`, `SecurityParam`, `ChainEvent`, `ChainSelectionReject`. **Errors:** `HFCError`, `SlotTimeError`, `OutsideForecastRange`, `HeaderValidationError`, `FieldError`, `FieldKind`, `VrfCertError`, `OpCertCounterError`, `NonceEvolutionError`, `LeaderScheduleError`, `OpCertError`. **Header surface:** `HeaderInput`, `HeaderVrf`, `HeaderKes`, `ValidatedHeaderSummary`, `HeaderApplied`. **Fork-choice:** `TiebreakerView`, `CandidateFragment`, `ChainSelectorState`, `ForkChoiceError`. **Op-cert/nonce:** `OpCertObservation`, `NonceInput`. **VRF:** `VrfRole`, `VerifiedVrf`, `StakeFraction`, `ActiveSlotsCoeff`. **Leader schedule:** `LeaderScheduleQuery`, `LeaderScheduleAnswer`. **Ledger view boundary:** `LedgerView` trait. **Rollback (header-level):** `RollBackRequest`, `RollBackApplied`. **Encoding:** `DecodeError`. 44 public types. |
| **Interprets** | Canonical inputs from the `ade_runtime` shell — `HeaderInput` projections, `EraSchedule` materialized once from genesis JSON, `LedgerView` snapshots from `ade_ledger::consensus_view::PoolDistrView`, ordered `StreamInput` events. KES check verifies hot KES key signature over the header body CBOR bytes. `opcert_validate` consumes an `OperationalCert`, cold key, expected period, and prev counter. **New consumption site in PHASE4-N-J:** `ade_ledger::snapshot::chain_dep::{encode_chain_dep, decode_chain_dep}` serialize `PraosChainDepState`'s 9 fields directly (canonical array(9) of evolving/candidate/epoch/previous_epoch/lab nonces + optional last_epoch_block/last_slot/last_block_no + op_cert_counters); the snapshot framing layer composes the encoded `PraosChainDepState` bytes alongside the encoded `LedgerState` bytes per `framing::encode_snapshot` (DC-STORE-08 + CN-STORE-08 + DC-STORE-09). |
| **MUST NOT** | (1) Take or accept a `&ChainDb` reference (`ci_check_no_chaindb_in_consensus_blue.sh`). (2) Use any `f32`/`f64` (`ci_check_no_float_in_consensus.sh`). (3) Add `#[non_exhaustive]` to any consensus enum, open-tail `Other`/`Unknown`, owned `String` fields, or `Box<dyn ...>` (`ci_check_consensus_closed_enums.sh`). (4) Reference `density` in `fork_choice.rs`/`candidate.rs` outside a `// no-density:` annotation. (5) Read wall-clock. (6) Use `HashMap`/`HashSet`; `OpCertCounterMap` uses `BTreeMap`. (7) `async fn`, `.await`, `tokio`. (8) Construct a `PreservedCbor`. (9) Re-derive stake snapshots (DC-CONSENSUS-02). (10) Bypass the canonical `validate_and_apply_header` pipeline. (11) B1 fail-closed (DC-VAL-06). (12) B1 KES rule (DC-CRYPTO-01). (13) All shared-header BLUE rules. (14) `consensus::opcert_validate` (DC-CONS-11/12) — `OpCertError` closed; sub-check order pinned; `prev_counter == None` only first-opcert acceptance path; no external counter store. |
| **Inbound deps** | `ade_ledger` (B1 + N-C + N-H + N-I + **NEW heavy site in N-J: `ade_ledger::snapshot::chain_dep` serializes `PraosChainDepState` — also indirectly via `ade_ledger::snapshot::framing` which combines the encoded chain_dep with encoded ledger bytes**), `ade_runtime`, `ade_testkit`, `ade_core_interop`. No new inbound crate edge in PHASE4-N-J — the snapshot-side consumption rides on `ade_ledger → ade_core`. |
| **Outbound deps** | `ade_types`, `ade_crypto`. Dev-deps: `ade_testkit`, `serde_json`, `cardano-crypto`. |
| **Entry points** | `use ade_core::consensus::{...}` aggregator, `ade_core::consensus::ledger_view::LedgerView`, `ade_core::consensus::vrf_cert::{vrf_input, verify_praos_vrf, ...}`, `ade_core::consensus::kes_check::{verify_header_kes, expect_size}`, `ade_core::consensus::praos_state::*` (now also consumed by `ade_ledger::snapshot::chain_dep` as owned `PraosChainDepState` for encode/decode), `ade_core::consensus::header_summary::*`. Top-level transitions: `validate_and_apply_header`, `select_best_chain`, `apply_rollback` (header-level), `apply_nonce_input`, `apply_op_cert`, `query_leader_schedule` + `is_leader_for_vrf_output`, `verify_vrf_cert` + `verify_praos_vrf` + `check_leader_claim` + `is_leader`, `tiebreaker_prefer`, `encode/decode_chain_dep_state`, `encode/decode_chain_event`. `ade_core::consensus::opcert_validate::{opcert_validate, OpCertError}`. |
| **Key modules** | `consensus/era_schedule.rs`, `consensus/praos_state.rs`, `consensus/events.rs`, `consensus/errors.rs`, `consensus/vrf_cert.rs`, `consensus/kes_check.rs`, `consensus/nonce.rs`, `consensus/op_cert.rs`, `consensus/leader_schedule.rs`, `consensus/header_summary.rs` + `consensus/header_validate.rs`, `consensus/candidate.rs`, `consensus/fork_choice.rs`, `consensus/rollback.rs` (header-level rollback), `consensus/ledger_view.rs`, `consensus/encoding.rs`, `consensus/opcert_validate.rs`. |

---

### `ade_crypto`

> **Status carried from prior CODEMAP — unchanged at HEAD `f15102f`.** PHASE4-N-J did not touch `ade_crypto`.

| Attribute | Value |
|-----------|-------|
| **Purpose** | Owns pure cryptographic verification primitives — Blake2b hashing, Ed25519 / Byron-bootstrap signature verification, KES verification with operational certificates, VRF verification, plus the closed signature-artifact types (`KesSignature`, `VrfProof`, `VrfOutput`, `Ed25519Signature`) that BLUE consumes across the RED→BLUE boundary. Verification only — signing lives in `ade_runtime::producer::signing`. |
| **Creates** | `Blake2b224`, `Blake2b256`, `HashAlgorithm` trait, `Ed25519VerificationKey`, `Ed25519Signature`, `ByronExtendedVerificationKey`, `KesVerificationKey`, `KesPeriod`, `OperationalCertData`, `VrfVerificationKey`, `VrfProof`, `VrfOutput`, `CryptoError`, `KesSignature(pub [u8; SUM6_KES_SIG_LEN])`, `SUM6_KES_SIG_LEN: usize = 448`. |
| **Interprets** | Verification key / signature / proof byte structures. Not a CBOR parser — accepts already-decoded byte slices. |
| **MUST NOT** | (1) Implement signing (`ci_check_no_signing_in_blue.sh`). (2) Allocate global state. (3) Use any BLUE forbidden pattern. (4) Use `unsafe` outside the allowlisted FFI in `src/vrf.rs`. (5) `build_opcert_signable` must produce the spec-correct raw concatenation. (6) `KesSignature` (DC-CRYPTO-04) — closed length-pinned wrapper; only `from_bytes` construction; custom redacting `Debug`; no `PartialOrd`/`Ord`/`Hash` derives. (7) No `Drop` impl that zeroizes — `KesSignature` is BLUE and not secret. |
| **Inbound deps** | `ade_core`, `ade_ledger`, `ade_plutus`, `ade_testkit`, `ade_core_interop`, `ade_runtime`. No new inbound crate edge in PHASE4-N-J. |
| **Outbound deps** | `ade_types`, `blake2`, `ed25519-dalek`, `cardano-crypto` (vrf-draft03 + kes-sum + dsign features, `default-features = false`). |
| **Entry points** | `ade_crypto::blake2b::blake2b_256`, `blake2b_224`, `blake2b_256`, `block_header_hash`, `transaction_id`, `script_hash`, `credential_hash`, `verify_ed25519`, `verify_byron_bootstrap`, `verify_kes`, `verify_opcert`, `verify_vrf`, `ade_crypto::kes::{KesSignature, SUM6_KES_SIG_LEN}`. |

---

### `ade_ledger`

> **Status change at HEAD `f15102f` (PHASE4-N-J S1–S7 — one new BLUE sub-tree under `snapshot/` containing canonical encode/decode for `(LedgerState, PraosChainDepState)`).** Between `75f75da` and HEAD: the new `ade_ledger::snapshot` sub-module (9 files, ~3,264 LOC + 40 inline tests) hosts the BLUE canonical snapshot encoder/decoder authority: `error.rs` (S1) defines closed `SnapshotEncodeError` + `SnapshotDecodeError` + `StructuralReason` sums (no `String` anywhere — `Cbor(CodecError)`, `UnknownVersion { expected, found }`, `FingerprintMismatch { expected, actual }`, `EraNotSupported { era }`, `Structural { reason }` cover every decode failure; `StructuralReason` is a 9-variant closed sum enumerating every structural rejection a sub-state decoder can produce — `ArrayLengthMismatch` / `MapLengthExceeded` / `UnexpectedNull` / `UnexpectedNonNull` / `NonceLengthMismatch` / `PoolIdLengthMismatch` / `Hash32LengthMismatch` / `Hash28LengthMismatch` / `EraTagOutOfRange`). The five sub-state encoder/decoder modules `chain_dep.rs` (S1), `utxo_state.rs` (S2), `cert_state.rs` (S3), `epoch_state.rs` (S4), `gov_state.rs` (S5) mirror the field walks of `ade_ledger::fingerprint` exactly — same field order, same BTreeMap iteration discipline, same `null`/non-null `Option` discriminant; this property is what makes the decoded state's recomputed fingerprint binding under DC-STORE-08. `ledger.rs` (S6) assembles the five sub-state encoders into the full `LedgerState` snapshot via a definite array(9) of `era` + `max_lovelace_supply` + `track_utxo` + the five sub-state byte payloads (nested `bstr`) + two optional gov-state/deposit-params byte payloads; sub-state bodies ride inside `bstr` containers so the outer decoder hands each slice to its specialized decoder without sharing offset state. `framing.rs` (S7) wraps `(LedgerState, PraosChainDepState)` as a single byte payload with the schema-version tag `SCHEMA_VERSION: u32 = 1` + the embedded `fingerprint(ledger).combined` cross-check — **CN-STORE-08 sole `pub fn encode_snapshot`/`pub fn decode_snapshot` pair for combined snapshot bytes in the project**, enforced repo-wide by `ci_check_snapshot_encoder_closure.sh`. The encoder rejects pre-Conway with `EraNotSupported { era }` at both the `framing` layer and the `ledger` layer; the decoder reads the version tag BEFORE any payload work (DC-STORE-09 fail-fast) and recomputes the source fingerprint AFTER decoding both halves (DC-STORE-08 cross-check). **DC-CONS-21 closes here**: persistent snapshot encode/decode round-trip equivalence is mechanically proven by the new framing-layer tests + the cross-impl `persistent_cache_matches_in_memory_cache_semantics` test in `ade_runtime::rollback::persistent_cache`. All prior B-track / N-E / PROPOSAL-PROCEDURES-DECODE / N-C / N-G / N-H / N-I narrative carries forward unchanged.

| Attribute | Value |
|-----------|-------|
| **Purpose** | The functional core (ledger half): stateless ledger rules for every Cardano era; the B1 top-level block-validity verdict; the B2 top-level transaction-validity verdict + mempool admission; the B3 full Conway value-conservation accounting; the B4 closed Conway cert-state accumulation; the B5 closed Conway governance-cert accumulation; the live committee-enactment write-back; the single BLUE chokepoint `mempool::ingress::mempool_ingress` from wire ingress into `admit`; the BLUE producer authority (`forge_block`, `self_accept`, `AcceptedBlock` broadcast token, `block_body_hash` recipe); the BLUE producer-side served-chain index and single canonical header-projection authority; the BLUE receive-side header→body bridge authority (`receive/`); the BLUE rollback authority (`rollback/`); **NEW — PHASE4-N-J: the BLUE canonical snapshot encoder/decoder authority (`snapshot/`): single combined-snapshot encoder/decoder pair (`framing::{encode_snapshot, decode_snapshot}` — CN-STORE-08), single `LedgerState` encoder/decoder pair (`ledger::{encode_ledger_state, decode_ledger_state}`), single `PraosChainDepState` encoder/decoder pair (`chain_dep::{encode_chain_dep, decode_chain_dep}`), five sub-state encoders (`utxo_state`, `cert_state`, `epoch_state`, `gov_state::{pparams, conway_deposit_params, gov_state}`), pinned schema version (`SCHEMA_VERSION: u32 = 1` — DC-STORE-09), embedded source-fingerprint cross-check (DC-STORE-08), closed error sums (`SnapshotEncodeError` + `SnapshotDecodeError` + `StructuralReason`). Scope: Conway-only encoder; pre-Conway → `EraNotSupported` structurally at both encode and decode**. |
| **Creates** | Carry-forward (all N-I + earlier types). **NEW — PHASE4-N-J (BLUE):** `SnapshotEncodeError` (closed enum with one variant `EraNotSupported { era: CardanoEra }`, in `snapshot::error`); `SnapshotDecodeError` (closed enum with five variants `Cbor(CodecError)` / `UnknownVersion { expected: u32, found: u32 }` / `FingerprintMismatch { expected: Hash32, actual: Hash32 }` / `EraNotSupported { era: CardanoEra }` / `Structural { reason: StructuralReason }`, in `snapshot::error`); `StructuralReason` (closed enum with nine variants `ArrayLengthMismatch` / `MapLengthExceeded` / `UnexpectedNull` / `UnexpectedNonNull` / `NonceLengthMismatch` / `PoolIdLengthMismatch` / `Hash32LengthMismatch` / `Hash28LengthMismatch` / `EraTagOutOfRange`, in `snapshot::error`). Functions (no new public types attached): `encode_chain_dep`/`decode_chain_dep` (S1, `snapshot::chain_dep`); `encode_utxo_state`/`decode_utxo_state` (S2, `snapshot::utxo_state`); `encode_cert_state`/`decode_cert_state` (S3, `snapshot::cert_state`); `encode_epoch_state`/`decode_epoch_state` (S4, `snapshot::epoch_state`); `encode_pparams`/`decode_pparams` + `encode_conway_deposit_params`/`decode_conway_deposit_params` + `encode_gov_state`/`decode_gov_state` (S5, `snapshot::gov_state`); `encode_ledger_state`/`decode_ledger_state` (S6, `snapshot::ledger`); `encode_snapshot`/`decode_snapshot` + `pub const SCHEMA_VERSION: u32 = 1` (S7, `snapshot::framing`). |
| **Interprets** | Decoded canonical types from `ade_codec`. Hosts per-era validation composers and the late-era witness decoder. B1: `block_validity::header_input::decode_block`. B2: `tx_validity::phase1::decode_tx`. B3: `conway::check_conway_coin_conservation`. B4: `rules::accumulate_tx_certs`. B5: `gov_cert::apply_conway_gov_cert`. PHASE4-N-E S1: `mempool_ingress` is a pure pass-through. PHASE4-N-C: `producer::forge::forge_block` / `producer::self_accept::self_accept`. PHASE4-N-G: `producer::served_chain::served_chain_admit`. PHASE4-N-H: `receive::admitted::admit_via_block_validity` (single admission authority). PHASE4-N-I: `rollback::materialize::materialize_rolled_back_state` (single materialize authority). **NEW — PHASE4-N-J:** `snapshot::framing::encode_snapshot` interprets the source `(LedgerState, PraosChainDepState)` exactly once on encode — invokes `encode_ledger_state` (composing five sub-state encoders) + `encode_chain_dep` + computes `fingerprint(ledger).combined` and embeds it inline. `snapshot::framing::decode_snapshot` interprets the bytes exactly once on decode — reads the version tag and rejects unknown versions BEFORE any payload work (DC-STORE-09), reads the embedded fingerprint, invokes `decode_ledger_state` + `decode_chain_dep`, then recomputes `fingerprint(decoded_ledger).combined` and rejects on mismatch (DC-STORE-08). The five sub-state decoders interpret their bytes positionally over the canonical CBOR primitives in `ade_codec::cbor` — every container is definite-length, every map iteration is BTreeMap, every length / variant rejection surfaces as a `StructuralReason` not a string. |
| **MUST NOT** | (1) Perform I/O. (2) Use `HashMap`, floats, or any other BLUE-forbidden pattern. (3) Hash anything other than wire bytes. (4) Construct `PreservedCbor` outside named decoders. (5) Depend on `ade_runtime`. (6) Use signing primitives or `#[cfg(feature)]` gates. (7) Omit `// Core Contract:` banner. (8–45) B1/B2/B3/B4/B5/N-E/PP-S1/N-C/N-G/N-H/N-I carry-forward (see prior CODEMAP for detail; the rollback-side CN-STORE-07 + DC-CONS-22 + DC-CONS-20 + DC-STORE-07 rules + the receive::reducer extension all still apply). **(46) NEW — `snapshot::framing` (CN-STORE-08 + DC-STORE-08 + DC-STORE-09, S7):** `framing::encode_snapshot`/`framing::decode_snapshot` MUST remain the only `pub fn` pair in the project encoding/decoding combined `(LedgerState, PraosChainDepState)` bytes — enforced repo-wide by `ci/ci_check_snapshot_encoder_closure.sh`'s grep gate. MUST embed the source `fingerprint(ledger).combined` inline on encode and recompute + cross-check it after decoding both halves; corruption / schema drift surfaces as `SnapshotDecodeError::FingerprintMismatch { expected, actual }`. MUST read the version tag BEFORE any payload work (no partial decode on unknown versions — `SnapshotDecodeError::UnknownVersion` is the first error surfaced on a version mismatch); the only `pub const SCHEMA_VERSION` in the workspace lives here. MUST reject pre-Conway encodes with `SnapshotEncodeError::EraNotSupported { era }`. MUST hold all containers definite-length and use BTreeMap iteration only (no `HashMap` — would break determinism). **(47) NEW — `snapshot::ledger` (CN-STORE-08 strengthening, S6):** `ledger::encode_ledger_state`/`ledger::decode_ledger_state` MUST remain the only `pub fn` pair encoding/decoding `LedgerState` bytes — enforced by the same script. MUST reject pre-Conway with `SnapshotEncodeError::EraNotSupported`. Sub-state bodies MUST ride inside `bstr` containers so the outer decoder can hand each slice to its specialized decoder without sharing offset state. **(48) NEW — `snapshot::chain_dep` (CN-STORE-08 strengthening, S1):** `chain_dep::encode_chain_dep`/`chain_dep::decode_chain_dep` MUST remain the only `pub fn` pair encoding/decoding `PraosChainDepState` bytes — enforced by the same script. MUST serialize all five nonces as fixed-length `bytes(32)`, all three `Option<...Block/Slot/BlockNo>` as `null | uint`, and `op_cert_counters` as a canonical `array(N) [array(3)[hash28, kes_period, counter]]` over BTreeMap iteration. **(49) NEW — `snapshot::{utxo_state, cert_state, epoch_state, gov_state}` (DC-STORE-08 grounding, S2–S5):** every sub-state encoder MUST mirror the field walk of `ade_ledger::fingerprint::fingerprint` exactly — same field order, same BTreeMap iteration discipline, same `null`/non-null `Option` discriminant. Departure from this property breaks DC-STORE-08's fingerprint cross-check binding because the decoded state's recomputed fingerprint would no longer equal the embedded source fingerprint even on bit-identical decode. Every sub-state decoder MUST surface structural rejection as a closed `StructuralReason` variant (the closed sum has nine variants — `ArrayLengthMismatch` / `MapLengthExceeded` / `UnexpectedNull` / `UnexpectedNonNull` / `NonceLengthMismatch` / `PoolIdLengthMismatch` / `Hash32LengthMismatch` / `Hash28LengthMismatch` / `EraTagOutOfRange`) — no `String`, no `Box<dyn Error>`, no `#[non_exhaustive]`. **(50) NEW — `snapshot::error` (S1):** all three enums MUST remain closed (no `#[non_exhaustive]`), `String`-free, and MUST NOT add new variants without a versioned gate per the closed-semantic-surface rule. |
| **Inbound deps** | `ade_testkit`, `ade_core_interop`, `ade_runtime`. PHASE4-N-G + N-H + N-I heavy sites (carried). **NEW heavy consumption sites in PHASE4-N-J** — `ade_runtime::rollback::persistent_cache` consumes `ade_ledger::snapshot::framing::{encode_snapshot, decode_snapshot, SCHEMA_VERSION}` + `ade_ledger::snapshot::{SnapshotDecodeError, SnapshotEncodeError}` + `ade_ledger::rollback::SnapshotReader` (re-export) + `ade_ledger::state::LedgerState`; the integration site for the new persistent-cache GREEN reader composes a full pipeline of `framing` + `SnapshotStore`. |
| **Outbound deps** | `ade_types`, `ade_crypto`, `ade_codec`, `ade_plutus`, `ade_core`, `minicbor`, `num-bigint`, `num-integer`, `num-traits`. Dev-dep: `ade_testkit`. **No new outbound crate edge in PHASE4-N-J** — the new `snapshot` module reuses the established `ade_ledger → {ade_core, ade_codec, ade_types}` edges. The new sub-state encoder/decoder files import `ade_codec::cbor::{canonical_width, read_*, write_*, ContainerEncoding, IntWidth, MAJOR_NEGATIVE}` + `ade_codec::CodecError` + `ade_core::consensus::praos_state::{Nonce, OpCertCounterMap, PraosChainDepState}` + `ade_types::{shelley::cert::StakeCredential, tx::{Coin, PoolId, TxIn}, address::Address, conway::{cert::DRep, governance::*}, BlockNo, CardanoEra, EpochNo, Hash28, Hash32, SlotNo}` over existing edges; intra-crate they import `crate::{delegation, epoch, fingerprint, pparams, rational, state, utxo, value}`. |
| **Entry points** | Carry-forward (all N-I + earlier surfaces). **NEW — PHASE4-N-J:** `ade_ledger::snapshot::framing::{encode_snapshot, decode_snapshot, SCHEMA_VERSION}`, `ade_ledger::snapshot::ledger::{encode_ledger_state, decode_ledger_state}`, `ade_ledger::snapshot::chain_dep::{encode_chain_dep, decode_chain_dep}`, `ade_ledger::snapshot::utxo_state::{encode_utxo_state, decode_utxo_state}`, `ade_ledger::snapshot::cert_state::{encode_cert_state, decode_cert_state}`, `ade_ledger::snapshot::epoch_state::{encode_epoch_state, decode_epoch_state}`, `ade_ledger::snapshot::gov_state::{encode_pparams, decode_pparams, encode_conway_deposit_params, decode_conway_deposit_params, encode_gov_state, decode_gov_state}`, `ade_ledger::snapshot::{SnapshotEncodeError, SnapshotDecodeError, StructuralReason}`. All re-exported at the `ade_ledger::snapshot` module root via `pub use`. Consumed by `ade_runtime::rollback::persistent_cache` (the GREEN counterpart cache adapter) and the existing `ade_runtime::rollback::*` integration tests. |
| **Key modules** | All prior keys (`byron.rs`, `shelley.rs`, …, `rules.rs`, `state.rs`, `utxo.rs`, …, `block_validity/`, `tx_validity/`, `mempool/`, `cert_classify.rs`, `block_body_hash.rs`, `producer/`, `receive/`, `rollback/`). **NEW — `snapshot/`** (PHASE4-N-J S1 + S2 + S3 + S4 + S5 + S6 + S7): `snapshot/mod.rs` (42 LOC barrel; re-exports the full snapshot surface), `snapshot/error.rs` (130 LOC + 3 inline tests — `SnapshotEncodeError` + `SnapshotDecodeError` + `StructuralReason`), `snapshot/chain_dep.rs` (261 LOC + 5 inline tests — `encode_chain_dep` + `decode_chain_dep`), `snapshot/utxo_state.rs` (443 LOC + 5 inline tests — `encode_utxo_state` + `decode_utxo_state` over the 3 tagged TxOut shapes), `snapshot/cert_state.rs` (387 LOC + 4 inline tests — `encode_cert_state` + `decode_cert_state` over 5-field CertState array), `snapshot/epoch_state.rs` (279 LOC + 3 inline tests — `encode_epoch_state` + `decode_epoch_state` over 7-field EpochState array), `snapshot/gov_state.rs` (1093 LOC + 7 inline tests — pparams 24-field array, ConwayGovState including the 7-variant GovAction sum), `snapshot/ledger.rs` (356 LOC + 7 inline tests — `encode_ledger_state` + `decode_ledger_state` 9-field array assembling S2-S5), `snapshot/framing.rs` (273 LOC + 6 inline tests — `encode_snapshot` + `decode_snapshot` + `SCHEMA_VERSION`). |

> **GREEN sub-classification (`ade_ledger::mempool::policy`, `ade_ledger::mempool::canonicalize`).** Carry-forward.

> **RED sub-classification (`ade_ledger::consensus_input_extract`).** Carry-forward.

> **`ade_ledger::producer::served_chain` BLUE classification (N-G).** Carry-forward.

> **`ade_ledger::receive` BLUE classification (N-H).** Carry-forward.

> **`ade_ledger::rollback` BLUE classification (N-I).** Carry-forward.

> **NEW — `ade_ledger::snapshot` BLUE classification rests on four layers:** (a) the crate prefix is BLUE under `.idd-config.json` `core_paths`; (b) every file begins with the `// Core Contract:` banner (enforced by `ci_check_module_headers.sh`); (c) the narrow CI gate `ci_check_snapshot_encoder_closure.sh` pins the CN-STORE-08 single-authority shape repo-wide across the workspace via grep on the public encoder/decoder function names + the `SCHEMA_VERSION` constant + the DC-STORE-08 / DC-STORE-09 cross-check sites in `framing.rs`; (d) the broader BLUE forbidden-pattern shape (no `HashMap`/`HashSet`/wall-clock/`tokio`/`rand`/floats/async) is covered by the existing full-BLUE gates `ci_check_forbidden_patterns.sh` + `ci_check_no_async_in_blue.sh` + `ci_check_no_float_in_consensus.sh` + the others. Sub-state encoders use BTreeMap iteration only and definite-length containers everywhere; mirroring `ade_ledger::fingerprint` is a property of the field-walk source rather than a CI-enforced invariant — the closure evidence for DC-STORE-08 is the cross-impl `persistent_cache_matches_in_memory_cache_semantics` test (proves the persistent reader's `nearest_le` agrees with the in-memory reader's `nearest_le` across 10 probe points × 4 stored snapshots) + the framing-layer fingerprint-mismatch test.

> **All prior closure narratives unchanged at HEAD.** B-track + N-E + PROPOSAL-PROCEDURES-DECODE + N-C + N-G + N-H + N-I all carry forward.

---

### `ade_network` *(BLUE submodules)*

> **Status carried from prior CODEMAP — unchanged at HEAD `f15102f`.** PHASE4-N-J did not touch any BLUE submodule of `ade_network`. The pre-existing PHASE4-N-A `chain_sync::signal::ForkChoiceSignal` is consumed unchanged by the existing GREEN adapter `ade_runtime::receive::events_to_state`; the new persistent cache's reader sits one layer further from the wire (it serves snapshot bytes back to `ade_ledger::rollback::materialize_rolled_back_state`, which is itself driven by the N-H receive-side bridge consuming the `ForkChoiceSignal::RollBackward` lift).

| Attribute | Value |
|-----------|-------|
| **Purpose** | Owns the Cardano Ouroboros mini-protocol authority — the closed wire grammar (CBOR codecs) and pure state machines for all 11 N2N + N2C mini-protocols, plus the BLUE Ouroboros mux frame primitive. Also owns the producer-side server-role reducer surface (PHASE4-N-G `chain_sync::server` + `block_fetch::server`). |
| **Creates** | **Wire grammar (`codec/`):** 11 closed mini-protocol message enums + closed payload types + structured `CodecError` + `ProtocolKind`. **State machines:** one closed `*State` / `*Agency` / `*Output` / `*Error` quad per protocol. **Mux primitives (`mux/frame.rs`):** `MuxFrame`, `MuxHeader`, `MuxMode`, `MiniProtocolId`, `MuxError`. **PHASE4-N-G (`chain_sync/server.rs` + `block_fetch/server.rs`):** `ServerReply(ServerVariant)` closed wrappers, `HeaderProjection`, `ProducerChainSyncServerState`, `ProducerServerError`, `ServerStep`, trait `ServedHeaderLookup`; `ProducerBlockFetchServerState`, `ProducerBlockFetchServerError`, `BlockFetchServerStep`, trait `ServedRangeLookup`. Functions: `producer_chain_sync_serve`, `producer_chain_sync_advance_tip`, `producer_block_fetch_serve`. |
| **Interprets** | The on-wire CBOR of every Ouroboros mini-protocol message (closed grammar) and the 8-byte Ouroboros mux frame header. Block/header/tx-body bytes inside RollForward / Block / Tx are opaque pass-through (DC-PROTO-06); LSQ Query/Result payloads also opaque. PHASE4-N-G server reducers interpret already-decoded `ChainSyncMessage` / `BlockFetchMessage` values. |
| **MUST NOT** | (1) `async fn`, `.await`, `tokio`, `async_std`, `futures::`, async-runtime spawn, or async timers in any file under the 9 BLUE paths. (2) Decode block / header / tx-body bytes. (3) Construct a `PreservedCbor` outside `ade_codec`'s named chokepoints. (4) Add `#[non_exhaustive]` to any wire-message enum or introduce a generic `Codec<P>` trait. (5) Define a generic `Agency<P>` wrapper. (6) Read a selected version from ambient session state. (7) Redefine `TxId` / `SlotNo` / `Hash32` / `CardanoEra`. (8) Depend on `pallas-network`. (9) Hold `String` in any error variant. (10) Use any other BLUE forbidden pattern. (11–13) PHASE4-N-G server-role MUST NOTs (carry-forward). |
| **Inbound deps** | `ade_core_interop` (live consensus session binary + N-E S6 + N-C S7 + N-G S7 + N-H S6 RED binaries; all RED → BLUE). The non-dev edge `ade_runtime → ade_network` (introduced by PHASE4-N-G S5/S6, extended by PHASE4-N-H `receive::*`) carries forward. **No new inbound crate edge in PHASE4-N-J.** Dev-deps `ade_network → {ade_ledger, ade_testkit, ade_crypto, ade_core}` carry forward. |
| **Outbound deps** | `ade_types`, `ade_codec`. No external deps in the BLUE submodules. |
| **Entry points** | Codec entry points per protocol; `ade_network::codec::primitives::*`; `ade_network::codec::version::*`; `ade_network::codec::{CodecError, ProtocolKind}`. State-machine `*_transition` per protocol. Mux: `ade_network::mux::frame::{encode_frame, decode_frame, ...}`. PHASE4-N-G: `ade_network::chain_sync::server::*` + `ade_network::block_fetch::server::*`. |
| **Key modules** | `codec/` (11 protocols + `primitives.rs` + `version.rs` + `error.rs`); `handshake/`, `chain_sync/` (incl. PHASE4-N-G `server.rs`), `block_fetch/` (incl. PHASE4-N-G `server.rs`), `tx_submission/`, `keep_alive/`, `peer_sharing/`; `n2c/`; `mux/frame.rs`. |

---

### `ade_plutus`

> **Status carried from prior CODEMAP — unchanged at HEAD `f15102f`.** PHASE4-N-J did not touch `ade_plutus`.

| Attribute | Value |
|-----------|-------|
| **Purpose** | Quarantine boundary between the Ade-canonical ledger and the ported UPLC evaluator from `aiken-lang/aiken` (pinned to tag `v1.1.21`). Exposes Ade-canonical types only; aiken- and pallas-originated types are strictly internal. |
| **Creates** | `PlutusScript`, `PlutusLanguage`, `EvalOutput`, `PlutusError`, `CostModels`, `DecoderMode`, `PerScriptResult`, `TxEvalResult`. |
| **Interprets** | UPLC scripts (Plutus V1/V2/V3) and `CostModels` CBOR. Phase-two transaction evaluation. `PlutusScript::from_cbor` is a named ingress chokepoint. |
| **MUST NOT** | (1) Re-export any `pallas_*` or `aiken_uplc::` type. (2) Allow another BLUE crate to bypass the canonical entry. (3) Activate PV11 builtins. (4) Use any BLUE-forbidden pattern. (5) Construct `PreservedCbor` outside `ade_codec`. |
| **Inbound deps** | `ade_ledger`, `ade_testkit`. |
| **Outbound deps** | `ade_types`, `ade_crypto`, `ade_codec`, `aiken_uplc` (git, tag `v1.1.21`), `pallas-primitives` (internal-only). |
| **Entry points** | `ade_plutus::eval_tx_phase_two`, `ade_plutus::tx_eval::*`, `ade_plutus::evaluator::*`, `ade_plutus::cost_model::*`. |
| **Key modules** | `evaluator.rs`, `cost_model.rs`, `script_context.rs`, `script_verdict.rs`, `tx_eval.rs`. |

---

### `ade_types`

> **Status carried from prior CODEMAP — unchanged at HEAD `f15102f`.** PHASE4-N-J did not add any canonical type to `ade_types` — the new closed canonical types all live in `ade_ledger::snapshot::error`. `ade_types::{Hash32, Hash28, CardanoEra, SlotNo, EpochNo, BlockNo}` continue as the lingua franca; the snapshot error sums embed them in their fields and the snapshot encoders walk them as field-level primitives.

| Attribute | Value |
|-----------|-------|
| **Purpose** | Owns the canonical Cardano domain vocabulary — primitives, era enum, address forms, era-tagged transaction bodies / outputs / certificates, governance types — used by every other workspace crate as the lingua franca. The schema, separated from the codec. |
| **Creates** | `CardanoEra`, `SlotNo`, `BlockNo`, `EpochNo`, `Hash28`, `Hash32`, `Coin`, `Lovelace`, `NetworkId`, `Nonce`, `TxIn`, `RewardAccount`, `PoolId`, `Address`, `ByronAddress`, `Credential`, `StakeCredential`, `Certificate`, `PoolRegistrationCert`, `ConwayCert`, `CertDisposition` / `DepositEffect` / `CoinSource`, `MIRCert`, `MIRPot`, `DRep`, `GovAction`, `GovActionState`, `GovActionId`, `Anchor`, `ProposalProcedure`, `OperationalCert`, `NativeScript`, `PlutusV1Script`, `Datum`, `DatumOption`, `MultiAsset`, `AssetName`, `CostModel`, `ExUnits`, plus per-era tx-body / tx-out / witness wrappers. |
| **Interprets** | None — produce-only. Domain types are constructed by `ade_codec` decoders or `ade_ledger` rules. |
| **MUST NOT** | (1) Construct or decode `PreservedCbor`. (2) Use any BLUE-forbidden pattern. (3) Depend on any workspace crate. (4) Add open/extensible variants to closed enums without a versioned gate. |
| **Inbound deps** | `ade_codec`, `ade_crypto`, `ade_ledger`, `ade_plutus`, `ade_runtime`, `ade_testkit`, `ade_network`, `ade_core`, `ade_core_interop`. |
| **Outbound deps** | None. |
| **Entry points** | `ade_types::CardanoEra`, `ade_types::tx::{Coin, TxIn, RewardAccount}`, `ade_types::{Hash32, SlotNo, Hash28, BlockNo, EpochNo}`, `ade_types::conway::tx::ConwayTxBody`, `ade_types::conway::cert::*`, `ade_types::conway::governance::{ProposalProcedure, Anchor, GovAction, GovActionId}`, `ade_types::shelley::block::{OperationalCert, ProtocolVersion, ShelleyHeader, ShelleyBlock, VrfData}`. |
| **Key modules** | `primitives.rs`, `era.rs`, `tx.rs`, `address/`, `byron/`, `shelley/`, `allegra/`, `mary/`, `alonzo/`, `babbage/`, `conway/`. |

---

## GREEN Modules — Deterministic Glue

> Deterministic, non-authoritative. May depend on BLUE; must not affect authoritative outputs.

### `ade_testkit`

> **Status carried from prior CODEMAP — unchanged at HEAD `f15102f`.** PHASE4-N-J did NOT add a `snapshot/` sub-module to `ade_testkit` — the cluster's evidence surface is entirely inline tests in `ade_ledger::snapshot::*` (40 tests across S1-S7 file groups, including the framing-layer round-trip / version-rejection / fingerprint-mismatch / pre-Conway-rejection / cross-fingerprint-equivalence tests) plus the 6 cross-impl tests in `ade_runtime::rollback::persistent_cache` (including `persistent_cache_matches_in_memory_cache_semantics` — the canonical DC-STORE-08 cross-impl evidence: persistent reader's `nearest_le` agrees with in-memory reader's `nearest_le` across 10 probe points × 4 stored snapshots). No new corpus fixture was added.

| Attribute | Value |
|-----------|-------|
| **Purpose** | Test infrastructure: differential harness, oracle snapshot loader, regression corpus, genesis loader, era mapping, transcript replay, diff reporting; N-B consensus harness; B1 block-validity harness (`validity/`); B2 transaction-validity harness (`tx_validity/`); N-E S2 mempool-ingress replay harness (`mempool/`); PP-S2 canonical synthetic `proposal_procedures` corpus + replay harness (`governance/`); PHASE4-N-C producer test harness (`producer/`). |
| **Creates** | Carry-forward (all entries from prior CODEMAP). No new types in PHASE4-N-J. |
| **Interprets** | Carry-forward. |
| **MUST NOT** | Carry-forward (1)–(16). |
| **Inbound deps** | None at compile time; consumption is via integration tests and dev-dep links from `ade_core`, `ade_runtime`, `ade_ledger`. `ade_core_interop` is a non-dev consumer. PHASE4-N-I added new dev-dep consumption (carry-forward); **no new dev-dep consumption site in PHASE4-N-J** — the new snapshot inline tests use only crate-internal helpers (`LedgerState::new` + per-field constructor literals from `ade_ledger`'s own type surface), and the cross-impl `persistent_cache_matches_in_memory_cache_semantics` test builds its `LedgerState` + `PraosChainDepState` via the same crate-internal helpers (so the test does not pull `ade_testkit` corpus loaders). |
| **Outbound deps** | `ade_types`, `ade_codec`, `ade_crypto`, `ade_core`, `ade_ledger`, `ade_plutus`, `ade_runtime`, `blake2`, `flate2`, `tar`, `serde`, `serde_json`, `toml`, `cardano-crypto` (dev-dep, N-C). |
| **Entry points** | `ade_testkit::harness::*`; N-B `consensus::*`; B1 `validity::*` (incl. `validity::ConwayValidityCorpus::load`); B2 `tx_validity::*`. N-E S2: `ade_testkit::mempool::*`. PP-S2: `ade_testkit::governance::*`. PHASE4-N-C: `ade_testkit::producer::*`. |
| **Key modules** | `harness/`, `consensus/`, `validity/`, `tx_validity/`, `mempool/`, `governance/`, `producer/` (N-C). **No new module added by N-J** — the planned `snapshot/` sub-tree was not materialized; the equivalent evidence lives inline in `crates/ade_ledger/src/snapshot/*.rs` + the new cross-impl tests in `crates/ade_runtime/src/rollback/persistent_cache.rs`. |

> **Classification note carried forward.** `ade_testkit` reads files from disk in test helpers and drives BLUE authorities from corpus inputs.

> **Gap surfaced (carried).** The cluster plan implicitly anticipated a `ade_testkit::snapshot::*` sub-tree (mirroring `producer/` + `validity/` + the deferred `receive/` + the deferred `rollback/`); the shipped artifacts live entirely as inline tests in the BLUE encoders themselves + 6 cross-impl tests in the GREEN `ade_runtime::rollback::persistent_cache`. **No new CI presence-gate** was added for the snapshot evidence (the closure rests on the inline tests + the new single-authority `ci_check_snapshot_encoder_closure.sh` grep gate). The deviation is intentional and documented here so future work does not re-introduce a duplicate harness under `ade_testkit`. Whether `ade_testkit::snapshot::*` should be added as a thin re-export shim for cross-crate consumption (e.g., a future `ade_node` integration test that round-trips a real Cardano-mainnet `LedgerState` snapshot) is a follow-on judgment call — at HEAD there is no consumer that would benefit.

---

### `ade_runtime::producer::{broadcast_to_served, served_chain_lookups}` *(GREEN — PHASE4-N-G S5)*

> **Status carried from prior CODEMAP — unchanged at HEAD `f15102f`.** PHASE4-N-J did not touch this GREEN sub-tree. Carry-forward.

| Attribute | Value |
|-----------|-------|
| **Purpose** | Bridges PHASE4-N-C's `BroadcastQueue` (RED) and PHASE4-N-G's `ServedChainSnapshot` (BLUE) to the `ServedHeaderLookup` / `ServedRangeLookup` trait seams the BLUE server reducers consume. |
| **Creates** | `ServedChainLookups<'a>`; function `drain_and_admit`. |
| **MUST NOT** | Carry-forward. |
| **Inbound deps** | `ade_runtime::network::n2n_server`; the integration tests under `ade_runtime/tests/`. |
| **Outbound deps** | `ade_ledger::{producer, block_validity}`, `ade_network::{chain_sync::server, block_fetch::server, codec::chain_sync}`, `ade_types`. |
| **Entry points** | `ade_runtime::producer::broadcast_to_served::drain_and_admit`, `ade_runtime::producer::served_chain_lookups::ServedChainLookups`. |

---

### `ade_runtime::receive::{events_to_state, in_memory_chain_write}` *(GREEN — PHASE4-N-H S3, extended PHASE4-N-I S3)*

> **Status carried from prior CODEMAP — unchanged at HEAD `f15102f`.** PHASE4-N-J did not touch this GREEN sub-pair. The N-I `ChainDbWrite::rollback_to_slot` impl on `ChainDbWriter` is reachable from the N-J snapshot path only transitively (an N-J snapshot read by `PersistentSnapshotCache::nearest_le` is the input to N-I's `materialize_rolled_back_state`, which feeds N-I's `commit_rollback`, which calls `chain_write.rollback_to_slot` — the GREEN `ChainDbWriter` adapter's impl is the production target).

| Attribute | Value |
|-----------|-------|
| **Purpose** | Bridges PHASE4-N-A's `ade_network::chain_sync::signal::ForkChoiceSignal` and `ade_network::block_fetch::event::BatchDeliveryEvent` to the BLUE `ade_ledger::receive::ReceiveEvent` stream, and wires the BLUE `ChainDbWrite` trait to any `ade_runtime::chaindb::ChainDb` impl. (Includes the N-I `rollback_to_slot` impl on `ChainDbWriter`.) |
| **Creates** | Functions `lift_chain_sync_signal` + `lift_block_fetch_event` (in `events_to_state`). Struct `ChainDbWriter<'a, D: ChainDb>` with `impl<'a, D: ChainDb> ChainDbWrite for ChainDbWriter<'a, D>` — `write_admitted` derives `(slot, hash)` via `decode_block` and calls `ChainDb::put_block`; `rollback_to_slot` calls `ChainDb::rollback_to_slot` over the underlying chain store. |
| **MUST NOT** | (1) Affect authoritative outputs. (2) Introduce nondeterminism (no `tokio`, no `SystemTime`/`Instant`, no `rand`, no `HashMap` — `ci/ci_check_receive_replay_purity.sh`). (3) `events_to_state` MUST NOT decode header_bytes or block_bytes. (4) `in_memory_chain_write` MAY decode the AdmittedBlock bytes once to derive the `(slot, hash)` key. (5–7) Carry-forward. (8) Carry-forward N-I `rollback_to_slot` rules. |
| **Inbound deps** | `ade_runtime::receive::orchestrator`; the integration tests under `ade_runtime/tests/receive_*.rs`. |
| **Outbound deps** | `ade_ledger::receive::{ReceiveEvent, TargetPoint, TipPoint, AdmittedBlock, ChainDbWrite, ChainWriteError, ChainWriteErrorKind}`, `ade_ledger::block_validity::decode_block`, `ade_network::{chain_sync::signal::*, block_fetch::event::*}`, `ade_types::{Hash32, SlotNo}`, `crate::chaindb::{ChainDb, ChainDbError, StoredBlock}`. No new edges in N-J. |
| **Entry points** | `ade_runtime::receive::events_to_state::{lift_chain_sync_signal, lift_block_fetch_event}`, `ade_runtime::receive::in_memory_chain_write::ChainDbWriter`. |

> **GREEN/RED partition risk note (carried).** The GREEN sub-classification rests on (a) module-header convention, (b) `ci_check_receive_replay_purity.sh`, and (c) the negative-grep enforcement of the no-decode rule on `events_to_state`. The `rollback_to_slot` extension is gated by review + the existing replay-purity check.

---

### `ade_runtime::rollback::{cadence, in_memory_cache, chaindb_block_source}` *(GREEN — PHASE4-N-I S4)*

> **Status carried from prior CODEMAP — unchanged at HEAD `f15102f`.** PHASE4-N-J did not touch the three N-I GREEN files; the new N-J GREEN file `persistent_cache.rs` is a sibling under the same `ade_runtime::rollback/` parent (see the next sub-section). The N-I in-memory + N-J persistent cache pair are observationally equivalent under DC-CONS-21's round-trip property; the cross-impl `persistent_cache_matches_in_memory_cache_semantics` test in `persistent_cache.rs` is the canonical evidence.

| Attribute | Value |
|-----------|-------|
| **Purpose** | Production implementations of the BLUE `ade_ledger::rollback` trait seams + the cadence policy that decides when to snapshot. `cadence` is the pure decision function `should_snapshot_after_block` plus the BLUE-structural single-field `SnapshotCadence { every_n_blocks: u32 }` type. `in_memory_cache` is the `InMemorySnapshotCache` impl of `SnapshotReader` — BTreeMap-backed canonical iteration. `chaindb_block_source` adapts any `ChainDb` impl into a `BlockSource` for the replay-forward driver. |
| **Creates** | `SnapshotCadence { every_n_blocks: u32 }` + `should_snapshot_after_block(SlotNo, BlockNo, SnapshotCadence, Option<SlotNo>) -> bool`; `InMemorySnapshotCache { entries: BTreeMap<SlotNo, (LedgerState, PraosChainDepState)> }` with `impl SnapshotReader`; `ChainDbBlockSource<'a, D: ChainDb> { pub db: &'a D }` with `impl<'a, D: ChainDb> BlockSource`. |
| **MUST NOT** | Carry-forward (1)–(8). |
| **Inbound deps** | `ade_runtime::rollback::snapshot_writer` (RED hook); the integration test under `ade_runtime/tests/receive_rollback_integration.rs`. **NEW — the N-J `ade_runtime::rollback::persistent_cache` (GREEN sibling) consumes `InMemorySnapshotCache` directly in its `persistent_cache_matches_in_memory_cache_semantics` cross-impl test, which builds an `InMemorySnapshotCache` alongside a `PersistentSnapshotCache` and probes both at 10 query points across 4 stored snapshots.** |
| **Outbound deps** | `ade_ledger::rollback::{SnapshotReader, BlockSource}`, `ade_ledger::receive::ReceiveState`, `ade_ledger::state::LedgerState`, `ade_core::consensus::praos_state::PraosChainDepState`, `ade_types::{SlotNo, BlockNo}`, `crate::chaindb::ChainDb`. |
| **Entry points** | `ade_runtime::rollback::cadence::{SnapshotCadence, should_snapshot_after_block}`, `ade_runtime::rollback::in_memory_cache::InMemorySnapshotCache`, `ade_runtime::rollback::chaindb_block_source::ChainDbBlockSource`. |

> **GREEN/RED partition risk note.** Carry-forward.

---

### `ade_runtime::rollback::persistent_cache` *(GREEN — PHASE4-N-J S8, NEW)*

> **Status: NEW at HEAD `f15102f`.** A GREEN file physically inside the RED `ade_runtime` crate prefix and the existing `ade_runtime::rollback/` sub-tree (alongside the three N-I GREEN files + one N-I RED file). The shared crate prefix is RED per `.idd-config.json` `_core_paths_doc`; the GREEN sub-classification of this specific file is **by file** — `// Core Contract:` banner present, file-header docstring naming the GREEN classification, and the closure shape gated mechanically by `ci/ci_check_snapshot_encoder_closure.sh` (which pins the framing-layer single-authority shape that this file consumes). Documented as a sub-section of `ade_runtime` in the RED block below; surfaced here for the GREEN inventory.

| Attribute | Value |
|-----------|-------|
| **Purpose** | Bridges the BLUE `ade_ledger::snapshot::framing::{encode_snapshot, decode_snapshot}` pair to any `ade_runtime::chaindb::SnapshotStore` impl, so the rollback driver can serve `nearest_le` lookups from restart-safe storage. **Closes DC-CONS-21** (persistent snapshot encode/decode round-trip equivalence) by shipping the production `SnapshotReader` impl backed by canonical bytes — the in-memory `InMemorySnapshotCache` (N-I) and the persistent `PersistentSnapshotCache` (N-J) are observationally equivalent under the cross-impl `persistent_cache_matches_in_memory_cache_semantics` test. |
| **Creates** | `PersistentSnapshotCache<'a, S: SnapshotStore + ?Sized> { store: &'a S }` — borrows the store, holds no in-memory state, safe to instantiate per lookup. Methods: `new(store: &'a S) -> Self`; `capture(slot: SlotNo, ledger: &LedgerState, chain_dep: &PraosChainDepState) -> Result<(), PersistentCacheError>` (encode → put). `impl<'a, S: SnapshotStore + ?Sized> SnapshotReader for PersistentSnapshotCache<'a, S>` — `nearest_le(target_slot)` walks `store.list_snapshot_slots()` (ascending per trait contract), takes the largest ≤ target via `.iter().rev().find(...)`, fetches bytes via `get_snapshot(slot)`, decodes via `framing::decode_snapshot`. Decode failures yield `None` (treats corrupt/missing snapshots as "no usable snapshot here", never panics — the loud surface is the writer-side `PersistentCacheError`). Closed `PersistentCacheError` sum with three variants `Encode(SnapshotEncodeError)` / `Decode(SnapshotDecodeError)` / `Store(ChainDbError)`. `pub const PERSISTENT_CACHE_SCHEMA_VERSION: u32 = SCHEMA_VERSION` mirrors the framing constant for out-of-crate consumers (ops dashboards) that want to pin the cache wire version without importing the BLUE crate. |
| **MUST NOT** | (1) Affect authoritative outputs — this is a deterministic adapter over canonical bytes. (2) Introduce nondeterminism — no `tokio`, no `SystemTime`/`Instant`, no `rand`, no `HashMap` (the BLUE `framing` layer holds the canonical bytes; this file is the trait adapter). (3) Own the `SnapshotStore` lifecycle — the cache borrows `&'a S` so the caller (operator-action layer) controls open/close/flush. (4) Construct snapshot bytes outside `framing::encode_snapshot` — the encode call site is the SOLE writer authority per CN-STORE-08; any parallel byte-construction would weaken DC-STORE-08 + DC-STORE-09. (5) Surface decode failures as panics — `nearest_le` MUST return `None` on decode failure so the orchestrator can skip past a corrupt snapshot and try the next-older slot; the writer-side `capture` does surface errors loudly via `PersistentCacheError`. (6) Add a second `SnapshotReader` impl in this file — the file hosts exactly one production trait impl. (7) Reorder the reader's `list_snapshot_slots → find_largest_le → get_snapshot → decode` sequence — the trait contract guarantees ascending order from `list_snapshot_slots` and the `rev().find()` adapter walks in descending order to find the largest ≤ target; reordering would change the candidate selection. (8) Cache decoded state across lookups — `PersistentSnapshotCache` holds no in-memory state; each `nearest_le` call re-reads from the store. |
| **Inbound deps** | The 6 inline tests in this file. Forward-looking: the operator-action layer (a future `ade_node` cluster) will instantiate `PersistentSnapshotCache::new(&snapshot_store)` as the production `SnapshotReader` for the rollback context, optionally alongside an `InMemorySnapshotCache` warm-read layer. |
| **Outbound deps** | `ade_ledger::rollback::SnapshotReader`, `ade_ledger::snapshot::framing::{decode_snapshot, encode_snapshot, SCHEMA_VERSION}`, `ade_ledger::snapshot::{SnapshotDecodeError, SnapshotEncodeError}`, `ade_ledger::state::LedgerState`, `ade_core::consensus::praos_state::PraosChainDepState`, `ade_types::SlotNo`, `crate::chaindb::{ChainDbError, SnapshotStore}`. **No new crate-level outbound edge** — reuses `ade_runtime → {ade_ledger, ade_core, ade_types}`. |
| **Entry points** | `ade_runtime::rollback::persistent_cache::{PersistentSnapshotCache, PersistentCacheError, PERSISTENT_CACHE_SCHEMA_VERSION}`. All re-exported at `ade_runtime::rollback::*` via `pub use`. |

> **GREEN/RED partition risk note.** Same class as the N-I GREEN trio: file-header banner + module-header convention + the N-J narrow single-authority CI gate on the BLUE framing layer it consumes. The broader `ade_runtime::rollback/` sub-partition (3 GREEN N-I files + 1 RED N-I file + 1 GREEN N-J file = 5 production files) remains "by file"; no dedicated CI script for the broad partition.

---

## RED Modules — Imperative Shell

> I/O, network, storage, clocks, retries. May depend on BLUE/GREEN. Must not modify core state directly or construct semantic types unsafely.

### `ade_core_interop` *(PHASE4-N-B S-B10; PHASE4-N-E S4/S5/S6; PHASE4-N-C S7; PHASE4-N-G S7; PHASE4-N-H S6)*

> **Status carried from prior CODEMAP — unchanged at HEAD `f15102f`.** PHASE4-N-J did not add a new live binary or new operator-action surface — persistent snapshots become exercisable end-to-end through the existing `live_block_follow_session` operator-action binary once the operator wires the socket layer + supplies a `RollbackContext` whose `SnapshotReader` is a `PersistentSnapshotCache` over a real `SnapshotStore`. The CE-N-H-6 procedure doc carries forward as the live-evidence procedure for the persistent-snapshot half too.

| Attribute | Value |
|-----------|-------|
| **Purpose** | Live cardano-node interop driver. Hosts the five `live_*_session` RED binaries plus the N-E S4/S5 GREEN bridges (`tx_submission` + `local_tx_submission`). |
| **Creates** | `fresh_orchestrator` (N-B). N-E S4 sub-module: `PeerAccumulator` (GREEN). N-E S5 sub-module: `ClientAccumulator` (GREEN). Functions: `event_to_ingress` + `ingest_n2n_events` (S4); `local_event_to_ingress` + `ingest_n2c_events` (S5). The five `live_*_session` binaries are RED glue with no public Rust types. |
| **Interprets** | Live N2N byte streams from a real cardano-node peer (carry-forward across N-B + N-E + N-C + N-G + N-H). |
| **MUST NOT** | (1)–(11) carry-forward (see prior CODEMAP). No N-J additions. |
| **Inbound deps** | None. |
| **Outbound deps** | `ade_core`, `ade_codec`, `ade_crypto`, `ade_ledger`, `ade_runtime`, `ade_network`, `ade_testkit`, `ade_types`, `tokio`. No new edges in N-J. |
| **Entry points** | `ade_core_interop::fresh_orchestrator(k, initial_nonce)`, binaries `live_consensus_session` + `live_tx_submission_session` + `live_block_production_session` + `live_block_fetch_session` + `live_block_follow_session`. |
| **Key modules** | `src/lib.rs`, `src/follow.rs` (N-B), `src/tx_submission.rs` (N-E S4), `src/local_tx_submission.rs` (N-E S5), `src/bin/live_consensus_session.rs`, `src/bin/live_tx_submission_session.rs` (N-E S6), `src/bin/live_block_production_session.rs` (N-C S7), `src/bin/live_block_fetch_session.rs` (N-G S7), `src/bin/live_block_follow_session.rs` (N-H S6). |

> **Gap surfaced (carried).** CE-N-B-6 closes via manual evidence log against a real cardano-node peer; live capture pending operator run. CE-N-E-6 is CLOSED at HEAD. CE-N-E-7 is deferred as `CE-NODE-N2C-LTX`. CE-N-C-8 is at `status = "enforced"` with `open_obligation` on `CN-CONS-06`: `blocked_until_operator_stake_available`. CE-N-G-8 (RO-LIVE-01) is at `status = "partial"` with `open_obligation = "blocked_until_operator_peer_available"`. CE-N-H-6 (RO-LIVE-02) is at `status = "partial"` with the same operator-action obligation. **No new RO-LIVE rule in PHASE4-N-J** — the persistent-snapshot path's live half rides on the same `live_block_follow_session` binary (the binary's `--connect` mode would now optionally accept a `--snapshot-store-path` flag to instantiate a `PersistentSnapshotCache`; today the flag set is forward-looking).

---

### `ade_network` *(RED submodules + capture binaries)*

> **Status carried from prior CODEMAP — unchanged at HEAD `f15102f`.** The RED half of `ade_network`: `mux::transport`, `session`, and the 7 `src/bin/capture_*` binaries. PHASE4-N-J did not touch this half of the crate.

| Attribute | Value |
|-----------|-------|
| **Purpose** | The only place inside `ade_network` where I/O happens — tokio-based TCP / Unix-socket scaffolding (`mux::transport`), the session-composition placeholder (`session`), and capture binaries that perform real handshakes against pinned cardano-node 11.0.1 peers. |
| **Creates** | `MuxTransport`. `session/mod.rs` is an empty placeholder. The capture binaries produce no public Rust types. |
| **Interprets** | Live TCP / Unix-socket byte streams; bytes flow into the BLUE mux frame decoder and protocol codecs without re-interpretation. |
| **MUST NOT** | (1) Contain protocol logic. (2) Define or modify any closed wire-message enum, agency type, or state. (3) Read a selected protocol version from ambient state. (4) Replace BLUE codec / state machine with a "fast path". (5) Bypass `ade_codec`'s `PreservedCbor` chokepoints. (6) Be depended on by any BLUE submodule of `ade_network`. |
| **Inbound deps** | `ade_core_interop`. At the library level no BLUE crate imports the RED submodules. |
| **Outbound deps** | `tokio`, `ade_types`, `ade_codec`, plus BLUE `ade_network::codec::*` and `ade_network::mux::frame`. |
| **Entry points** | `ade_network::mux::transport::{MuxTransport, open_tcp}`. The capture binaries. |
| **Key modules** | `mux/transport.rs`, `session/mod.rs` (placeholder), `bin/capture_*`. |

> **Gap surfaced (carried, unchanged at HEAD).** `ade_network::session::mod.rs` is still a placeholder. The N-G/N-H honest-scope stubs of the tokio socket wiring still sit at the boundary between the pure dispatch drivers in `ade_runtime::{network::n2n_server, receive::orchestrator}` and the absent session-composition layer. PHASE4-N-J did not surface or close any N-network gap.

---

### `ade_node`

> **Status carried from prior CODEMAP — unchanged at HEAD `f15102f`.** Still a hello-world stub. PHASE4-N-J did not touch `ade_node`; the persistent-snapshot bridge is reachable today through the new inline tests in `ade_ledger::snapshot::*` + the 6 inline tests in `ade_runtime::rollback::persistent_cache` — not through any `ade_node` orchestration.

| Attribute | Value |
|-----------|-------|
| **Purpose** | Binary entry point for the node process. Reserved as the assembly point for runtime + ledger + chain DB + network + consensus orchestrator + mempool + the deferred N2C server + the producer-side N2N server + the receive-side N2N client pump + the snapshot-cache plumbing (now extended with both `InMemorySnapshotCache` (N-I) and `PersistentSnapshotCache` (N-J) as composable production `SnapshotReader` impls). |
| **Creates** | None. |
| **Interprets** | None yet. |
| **MUST NOT** | (1) Bypass `ade_codec` to construct semantic types from bytes. (2) Modify `ade_ledger` or `ade_core::consensus` state in place — transitions go through `ade_ledger::rules::*`, `ade_ledger::block_validity::*`, `ade_ledger::tx_validity::*`, `ade_ledger::mempool::admit`, `ade_ledger::mempool::mempool_ingress`, `ade_ledger::producer::forge::forge_block` + `ade_ledger::producer::self_accept::self_accept`, `ade_ledger::producer::served_chain::served_chain_admit`, `ade_ledger::receive::admit_via_block_validity` + `ade_ledger::receive::receive_apply`, `ade_ledger::rollback::materialize_rolled_back_state` + `ade_ledger::rollback::commit_rollback` (N-I), **or `ade_ledger::snapshot::framing::{encode_snapshot, decode_snapshot}` (NEW — N-J; the SOLE pub fn pair for combined snapshot bytes per CN-STORE-08)**, or `ade_core::consensus::*` BLUE transitions. (3) Take an inbound dep from any other crate. (4) Read versions out of ambient context. (5) Construct an `OrchestratorState` outside `ade_runtime::consensus::chain_selector` or `ade_core_interop::fresh_orchestrator`. (6) Forward-looking — must reach the mempool only through `mempool_ingress`. (7) Forward-looking `CE-NODE-N2C-LTX`. (8) PP-S1 forward-looking. (9) Forward-looking PHASE4-N-C / CN-CONS-07. (10) Forward-looking PHASE4-N-G. (11) Forward-looking PHASE4-N-H. (12) Forward-looking PHASE4-N-I (rollback context). **(13) NEW — Forward-looking PHASE4-N-J / CN-STORE-08 + DC-STORE-08 + DC-STORE-09 + DC-CONS-21:** any `ade_node`-level snapshot wiring MUST drive `ade_runtime::rollback::persistent_cache::PersistentSnapshotCache::new(&snapshot_store)` to instantiate the production restart-safe `SnapshotReader` (or compose it with `InMemorySnapshotCache` as a warm-read layer); MUST NOT construct snapshot bytes outside `ade_ledger::snapshot::framing::encode_snapshot` (CN-STORE-08 single-authority — repo-wide grep gate already enforces); MUST NOT decode snapshot bytes outside `ade_ledger::snapshot::framing::decode_snapshot` (same gate); MUST surface `PersistentCacheError::Encode(EraNotSupported)` to the operator surface as a configuration error (pre-Conway snapshots are out of scope at this cluster's scope); MUST surface `SnapshotDecodeError::FingerprintMismatch` and `SnapshotDecodeError::UnknownVersion` as corruption / upgrade-path signals (the corrupt-bytes case is silently handled by `PersistentSnapshotCache::nearest_le` returning `None` — the orchestrator skips past the corrupt snapshot to the next-older slot; the `UnknownVersion` case is a more serious upgrade-path mismatch that warrants operator attention). The default `ade_node` binary MUST stay hermetic — persistent-snapshot wiring requires explicit operator opt-in matching the existing producer-mode + receiving-mode precedents. |
| **Inbound deps** | None. |
| **Outbound deps** | None at present. |
| **Entry points** | `main()` in `crates/ade_node/src/main.rs`. |

> **Gap surfaced (carried).** `ade_node` is still a hello-world stub; its MUST NOT list is forward-looking with nothing to enforce today.

---

### `ade_runtime`

> **Status change at HEAD `f15102f` (PHASE4-N-J S8 — one new GREEN file `rollback/persistent_cache.rs` inside the existing `ade_runtime::rollback/` sub-tree).** Between `75f75da` and HEAD, `ade_runtime` gained `rollback/persistent_cache.rs` (251 LOC + 6 inline tests, **GREEN**) — `PersistentSnapshotCache<'a, S: SnapshotStore>` implements `SnapshotReader` over an externally-owned `SnapshotStore`, bridging `ade_ledger::snapshot::framing::{encode_snapshot, decode_snapshot}` (the BLUE single-authority encoder/decoder pair) to the existing N-D storage trait. Closes `DC-CONS-21`. `rollback/mod.rs` gained one `pub mod persistent_cache;` line and one `pub use` block re-exporting `PersistentSnapshotCache`, `PersistentCacheError`, `PERSISTENT_CACHE_SCHEMA_VERSION`. No other file in `ade_runtime` was touched. All prior N-I / N-H / N-G / N-D / N-B / N-A / N-C narrative carries forward unchanged.

| Attribute | Value |
|-----------|-------|
| **Purpose** | The imperative shell. Hosts (a) the `ChainDb` / `SnapshotStore` storage abstractions (N-D); (b) the GREEN chain-selector orchestrator + GREEN `CandidateFragment` materializer (N-B); (c) the RED genesis-text parser (N-B); (d) the RED producer-side scaffolding (`producer/`); (e) the RED producer-side N2N server pump (`network/n2n_server.rs`, N-G S6); (f) the GREEN broadcast-to-served adapter (`producer/{broadcast_to_served, served_chain_lookups}.rs`, N-G S5); (g) the GREEN receive-side adapter pair + the RED receive-side N2N client orchestrator (`receive/`, N-H S3 + S4); (h) the GREEN rollback adapter trio (`rollback/{cadence, in_memory_cache, chaindb_block_source}.rs`, N-I S4) implementing the BLUE `SnapshotReader` + `BlockSource` trait seams plus the cadence decision function with its single-field BLUE-structural `SnapshotCadence`; (i) the RED snapshot-write hook (`rollback/snapshot_writer.rs`, N-I S5); **(j) NEW — the GREEN persistent snapshot cache (`rollback/persistent_cache.rs`, N-J S8) — `PersistentSnapshotCache<'a, S: SnapshotStore>` impl `SnapshotReader`, bridging the BLUE `ade_ledger::snapshot::framing` encoder/decoder pair to the N-D `SnapshotStore` trait; observationally equivalent to `InMemorySnapshotCache` under DC-CONS-21**. The crate remains RED at the prefix level; the new `persistent_cache.rs` joins the existing GREEN-by-file list (`chain_selector.rs` / `candidate_fragment.rs` / `tick_assembler.rs` / `broadcast_to_served.rs` / `served_chain_lookups.rs` / `receive/events_to_state.rs` / `receive/in_memory_chain_write.rs` / `rollback/cadence.rs` / `rollback/in_memory_cache.rs` / `rollback/chaindb_block_source.rs`); RED-by-file remains `genesis_parser.rs` / `signing.rs` / `keys.rs` / `scheduler.rs` / `broadcast.rs` / `network/n2n_server.rs` / `receive/orchestrator.rs` / `rollback/snapshot_writer.rs`. |
| **Creates** | Carry-forward (all N-I + earlier types). **NEW — PHASE4-N-J S8 (GREEN):** `PersistentSnapshotCache<'a, S: SnapshotStore + ?Sized> { store: &'a S }` + `impl<'a, S: SnapshotStore + ?Sized> SnapshotReader for PersistentSnapshotCache<'a, S>` + `pub fn capture(&self, slot, ledger, chain_dep) -> Result<(), PersistentCacheError>` + closed `PersistentCacheError` sum (3 variants: `Encode(SnapshotEncodeError)` / `Decode(SnapshotDecodeError)` / `Store(ChainDbError)`) + `pub const PERSISTENT_CACHE_SCHEMA_VERSION: u32 = SCHEMA_VERSION`. All in `rollback::persistent_cache`. |
| **Interprets** | Block bytes for content-addressed storage. Genesis JSON. N-A `StreamInput` events. PHASE4-N-C/N-G/N-H carry-forward. PHASE4-N-I carry-forward (cadence decision, in-memory cache lookups, chaindb block range, snapshot writer hook). **NEW — PHASE4-N-J:** `rollback::persistent_cache` interprets snapshot bytes exactly twice — once on `capture` (via `framing::encode_snapshot`, the BLUE single authority) and once on `nearest_le` (via `framing::decode_snapshot`, the same BLUE single authority); the file itself contains no CBOR parsing — it only adapts the `SnapshotStore`'s opaque-bytes interface (`put_snapshot` / `get_snapshot` / `list_snapshot_slots`) to the `SnapshotReader`'s `(SlotNo, LedgerState, PraosChainDepState)` triple via the framing layer. |
| **MUST NOT** | (1)–(26) carry-forward (all prior crate-wide and per-sub-tree rules). **(27) NEW — `rollback::persistent_cache` (DC-CONS-21 + CN-STORE-08, S8, GREEN):** MUST construct snapshot bytes ONLY via `framing::encode_snapshot` and decode them ONLY via `framing::decode_snapshot` (CN-STORE-08 — repo-wide grep gate already enforces; this file is one of the few legitimate consumers). MUST NOT cache decoded `LedgerState`/`PraosChainDepState` across `nearest_le` calls — `PersistentSnapshotCache` holds no in-memory state; each call re-reads from the store (operator-controlled warm cache lives separately as the N-I `InMemorySnapshotCache`). MUST NOT panic on decode failure — `nearest_le` returns `None` for unreadable / corrupt / wrong-version snapshot bytes, allowing the orchestrator to skip past a corrupt snapshot to the next-older slot via the store's ascending `list_snapshot_slots`. MUST NOT add a second `SnapshotReader` impl in this file — the file hosts exactly one production trait impl. MUST NOT own the `SnapshotStore` lifecycle — the cache borrows `&'a S` (with `?Sized` for trait-object compatibility) so the operator-action layer controls open/close/flush. MUST surface writer-side errors loudly via the closed `PersistentCacheError` sum — `Encode(SnapshotEncodeError)` for pre-Conway or shape-rejected ledger states, `Decode(SnapshotDecodeError)` for any decode path not handled by `nearest_le`'s silent-skip (currently unused on the writer path; preserved as a forward-looking variant), `Store(ChainDbError)` for storage-layer failures. The `PERSISTENT_CACHE_SCHEMA_VERSION` constant MUST mirror `framing::SCHEMA_VERSION` exactly — drift would let out-of-crate consumers pin a wrong wire version. |
| **Inbound deps** | `ade_testkit` (non-dev dep, used by `stream_replay`), `ade_core_interop` (heavy consumer at HEAD — all five `live_*_session` binaries import from `ade_runtime`). The binary `chaindb_kill_target` lives at `src/bin/`. **NEW heavy consumption sites in PHASE4-N-J** — the 6 inline tests in `persistent_cache.rs` itself consume `ade_runtime::chaindb::InMemoryChainDb` (impls `SnapshotStore`) + `ade_runtime::rollback::in_memory_cache::InMemorySnapshotCache` (for the cross-impl equivalence test). No new outside consumer of `PersistentSnapshotCache` yet — when the operator-action wiring lands in a future node-binary cluster (forward-looking note 13 on `ade_node`), it will compose `PersistentSnapshotCache::new(&snapshot_store)` as the primary production `SnapshotReader` (optionally backed by an `InMemorySnapshotCache` warm-read layer). |
| **Outbound deps** | `ade_types`, `ade_core`, `ade_crypto`, `ade_codec`, `ade_ledger`, `ade_network`, `redb`, `serde_json`, `cardano-crypto`, `ed25519-dalek`. **No new outbound crate edge in PHASE4-N-J** — `persistent_cache` reuses the established `ade_runtime → {ade_ledger, ade_core}` edges (imports `ade_ledger::rollback::SnapshotReader` + `ade_ledger::snapshot::{framing::{decode_snapshot, encode_snapshot, SCHEMA_VERSION}, SnapshotDecodeError, SnapshotEncodeError}` + `ade_ledger::state::LedgerState` + `ade_core::consensus::praos_state::PraosChainDepState` + `ade_types::SlotNo` + `crate::chaindb::{ChainDbError, SnapshotStore}`). Dev-deps: `tempfile`, `ade_testkit`. |
| **Entry points** | All N-I + earlier surfaces (`chaindb::*`, `recovery::*`, `consensus::*`, `producer::*`, `network::*`, `receive::*`, `rollback::*`). **NEW — PHASE4-N-J `rollback::persistent_cache` (GREEN):** `ade_runtime::rollback::persistent_cache::{PersistentSnapshotCache, PersistentCacheError, PERSISTENT_CACHE_SCHEMA_VERSION}`. All re-exported at `ade_runtime::rollback::*` via `pub use`. |
| **Key modules** | All prior keys (`chaindb/`, `recovery.rs`, `consensus/`, `bin/chaindb_kill_target.rs`, `producer/`, `network/`, `receive/`, `rollback/`). **EXTENDED — `rollback/`** (PHASE4-N-J S8): one new file `rollback/persistent_cache.rs` (251 LOC + 6 inline tests, **GREEN**) — `PersistentSnapshotCache` + `PersistentCacheError` + `PERSISTENT_CACHE_SCHEMA_VERSION` + 6 inline tests: `persistent_cache_capture_then_nearest_le_round_trips` (3 snapshots at slots 100/200/300; probes 250→200, 300→300, 99999→300, 50→None), `persistent_cache_matches_in_memory_cache_semantics` (cross-impl reader equivalence at 10 probe points × 4 stored snapshots — canonical DC-CONS-21 evidence), `persistent_cache_empty_store_returns_none`, `persistent_cache_rejects_pre_conway_on_capture` (Babbage ledger → `Encode(EraNotSupported)`), `persistent_cache_corrupt_bytes_yields_none_from_reader` (corrupt-bytes write → reader silently skips), `persistent_cache_schema_version_mirrors_framing` (pinned `PERSISTENT_CACHE_SCHEMA_VERSION == framing::SCHEMA_VERSION`). `rollback/mod.rs` extended with `pub mod persistent_cache;` + a `pub use persistent_cache::{...}` block. |
| **Mechanical enforcement** | Carry-forward (chaindb / recovery / crash-safety + 8 N-C + 7 N-G + 5 N-H + 2 N-I CI scripts). **NEW — one PHASE4-N-J CI script backs the persistent-cache surface inside `ade_runtime` (indirectly):** `ci/ci_check_snapshot_encoder_closure.sh` is BLUE-scoped on `crates/ade_ledger/src/snapshot/` for its three function-name single-authority scans + the SCHEMA_VERSION single-authority scan, but the script's repo-wide grep makes it indirectly enforce CN-STORE-08 against `ade_runtime` too — any new file in `ade_runtime` defining a public `encode_snapshot`/`decode_snapshot`/`encode_ledger_state`/`decode_ledger_state`/`encode_chain_dep`/`decode_chain_dep` function would fail the gate. **No new `ade_runtime`-presence CI script** for the new persistent-cache file — the closure rests on the 6 inline tests in the file itself + the cross-impl equivalence test. |

> **Gap surfaced (carried + extended).** No dedicated CI script enforces the GREEN/RED partition *inside* `ade_runtime::consensus` or `ade_runtime::producer` or `ade_runtime::network` or `ade_runtime::receive` or `ade_runtime::rollback`. **NEW — same gap class extends to `ade_runtime::rollback::persistent_cache`:** the GREEN sub-classification rests on file-header banner + the BLUE-scoped `ci_check_snapshot_encoder_closure.sh` script (which proves the file consumes the single-authority encoder/decoder via the broader repo-wide grep gate); the broader sub-partition rests on file-header banners + review. **NEW — no live operator-action binary for the persistent-snapshot path** — the persistent-cache layer rides on the existing N-H `live_block_follow_session` binary; the binary's `--connect` mode remains honest-scope stubbed pending operator-action; a future operator run will naturally exercise the new `PersistentSnapshotCache` if/when the operator wires a `--snapshot-store-path` flag (forward-looking). The N-H `RO-LIVE-02` rule continues to carry the live-evidence obligation; no new RO-LIVE rule was added for the persistent-snapshot half. **DC-CONS-21 now closes** — the `open_obligation = "persistent_ledger_snapshot_encoding_follow_on_cluster"` is removed; both in-memory (N-I) and persistent (N-J) `SnapshotReader` impls coexist as observationally-equivalent production implementations of the same BLUE trait seam.

---

## Cross-Module Rules (project-wide)

### Dependency direction

`ade_core_interop` → `{ade_core, ade_codec, ade_crypto, ade_ledger, ade_runtime, ade_network, ade_testkit, ade_types, tokio}` is legal (RED leaf binary). **No new crate-level outbound edge in PHASE4-N-J.**
`ade_runtime` → `{ade_core, ade_crypto, ade_codec, ade_types, ade_ledger, ade_network, redb, serde_json, cardano-crypto, ed25519-dalek}` is legal (RED → BLUE). **No new crate-level outbound edge in PHASE4-N-J** — the new `rollback/persistent_cache.rs` reuses the established edges.
`ade_testkit` → `{ade_core, ade_ledger, ade_plutus, ade_runtime, ade_crypto, ade_codec, ade_types, cardano-crypto}` is legal (GREEN).
`ade_network` (BLUE submodules) → `{ade_codec, ade_types}` is legal.
`ade_network` (RED submodules + capture bins) → `{tokio, ade_codec, ade_types, ade_network::codec::*, ade_network::mux::frame}` is legal.
`ade_ledger` → `{ade_core, ade_plutus, ade_crypto, ade_codec, ade_types, minicbor}` is legal (BLUE among BLUEs). **The new `snapshot` module reuses these edges — no new outbound from `ade_ledger`** (the snapshot encoders import `ade_codec::cbor::*` + `ade_codec::CodecError` + `ade_core::consensus::praos_state::*` + `ade_types::{shelley::cert::StakeCredential, tx::*, address::Address, conway::*, *}` over existing edges; intra-crate they import `crate::{delegation, epoch, fingerprint, pparams, rational, state, utxo, value}`).
`ade_core` → `{ade_types, ade_crypto, minicbor}` is legal (BLUE among BLUEs).
`ade_plutus` → `{ade_crypto, ade_codec, ade_types}` is legal.
`ade_crypto` → `{ade_types}` is legal.
`ade_codec` → `{ade_types}` is legal.
`ade_types` → `{}`.

**Forbidden directions.** Any BLUE crate (or BLUE `ade_network` submodule) depending on `ade_runtime`, `ade_node`, `ade_core_interop`, or the RED half of `ade_network` is a CI failure (`ci_check_dependency_boundary.sh` + `ci_check_no_async_in_blue.sh`). Any non-`ade_plutus` crate referring to `pallas_*` is a CI failure (`ci_check_pallas_quarantine.sh`). Any reference to `ChainDb` / `chain_db` inside `crates/ade_core/src/consensus/` is a CI failure (`ci_check_no_chaindb_in_consensus_blue.sh`). B1/B2/N-E — BLUE/GREEN sub-partition inside `ade_ledger::mempool` is review-and-test-enforced for the broad partition, with two narrow grep gates. B3 — deposit-param-authority gate. PP-S1 — `ProposalProcedure {…}` struct-literal scan. PHASE4-N-C dependency notes (the 8 producer CI gates pin specific properties). PHASE4-N-G dependency notes (the 7 producer-side-server CI gates pin specific properties). PHASE4-N-H dependency notes (the 5 receive-side CI gates pin specific properties). PHASE4-N-I dependency notes (the 2 rollback-authority CI gates pin specific properties). **NEW — PHASE4-N-J dependency notes (the 1 snapshot-encoder single-authority CI gate pins specific properties):** any `pub fn encode_snapshot`/`pub fn decode_snapshot` outside `crates/ade_ledger/src/snapshot/framing.rs` is a CI failure (`ci_check_snapshot_encoder_closure.sh`, CN-STORE-08). Any `pub fn encode_ledger_state`/`pub fn decode_ledger_state` outside `crates/ade_ledger/src/snapshot/ledger.rs` is a CI failure (same script). Any `pub fn encode_chain_dep`/`pub fn decode_chain_dep` outside `crates/ade_ledger/src/snapshot/chain_dep.rs` is a CI failure (same script). Any `pub const SCHEMA_VERSION` outside `crates/ade_ledger/src/snapshot/framing.rs` is a CI failure (same script — DC-STORE-09 single version-tag authority). Absence of `FingerprintMismatch` or `UnknownVersion` references inside `framing.rs` is a CI failure (same script — DC-STORE-08 + DC-STORE-09 cross-check sites must exist).

### Naming convention

All crates are prefixed `ade_`. TCB color is not encoded in the crate name. The authoritative classifier is `.idd-config.json` `core_paths` plus the cluster doc TCB Color Maps for sub-crate scopes; CI scripts hard-code their BLUE list. The seven full-BLUE-scoped scripts use the full BLUE set: 6 BLUE crates + 9 BLUE `ade_network` paths. The B3 `ci_check_deposit_param_authority.sh` scopes the 6 BLUE crates. Four narrow scripts target `crates/ade_core/src/consensus/`. The two N-E scripts scope `crates/ade_ledger/src/mempool/*` + `crates/ade_testkit/src/mempool/*`. The PP-S1 `ci_check_proposal_procedures_closed.sh` scopes `conway/{governance.rs, tx.rs}` paths. The 8 N-C scripts narrow as previously documented. The 7 N-G scripts narrow as previously documented. The 5 N-H scripts narrow as previously documented. The 2 N-I scripts narrow as previously documented. **NEW — the 1 PHASE4-N-J script narrows as follows:** `ci_check_snapshot_encoder_closure.sh` scopes the three canonical sites `crates/ade_ledger/src/snapshot/{framing,ledger,chain_dep}.rs` for positive presence + uses a repo-wide grep against `$REPO_ROOT/crates` to enforce the single-authority shape for the six function names + the one `SCHEMA_VERSION` constant; the script does not need a comment-stripping AWK helper because the function-name regex `pub fn ${fn}\b` is precise enough to avoid false-positive matches inside doc-comments / `cfg(test)` blocks (no `cfg(test)` test currently defines a public function named `encode_snapshot` / `decode_snapshot` / etc.). **No CI script narrowly scopes the new `ade_runtime::rollback::persistent_cache.rs`** — the closure rests on the 6 inline tests + the broader `ci_check_snapshot_encoder_closure.sh` repo-wide single-authority gate that indirectly enforces persistent_cache's correct consumption pattern (it cannot define a competing `encode_snapshot`/`decode_snapshot`).

### CI enforcement (55 scripts under `ci/`)

| Script | Enforces | Scope |
|---|---|---|
| `ci_check_admitted_block_closure.sh` *(PHASE4-N-H S1)* | CE-N-H-1 + CN-CONS-08 + CN-PROTO-07 | repo-wide grep gate + `crates/ade_ledger/src/receive/events.rs` |
| `ci_check_block_fetch_server_closure.sh` *(PHASE4-N-G S4)* | DC-CONS-17 foundation | `crates/ade_network/src/block_fetch/server.rs` |
| `ci_check_broadcast_to_served_purity.sh` *(PHASE4-N-G S5)* | CE-N-G-5 | `crates/ade_runtime/src/producer/{broadcast_to_served,served_chain_lookups}.rs` |
| `ci_check_cbor_round_trip.sh` | T-ENC-03, DC-CBOR-01, DC-CBOR-02 | golden corpus |
| `ci_check_ce_n_a_5_proof.sh` *(PHASE4-N-A, S-A10)* | CE-N-A-5 5-condition evidence | `ade_network` (RED + real-capture corpus) |
| `ci_check_chaindb_contract.sh` | DC-STORE-02, DC-STORE-03, CN-STORE-04, CN-STORE-05 | `ade_runtime --lib chaindb::` (RED) |
| `ci_check_chaindb_crash_safety.sh` | T-REC-01 (crash variant), DC-STORE-01, CN-STORE-03 | `ade_runtime --test stress_kill_harness` (RED) |
| `ci_check_chain_sync_server_closure.sh` *(PHASE4-N-G S3)* | DC-PROTO-08 | `crates/ade_network/src/chain_sync/server.rs` |
| `ci_check_consensus_closed_enums.sh` | DC-CONS-04, DC-CONS-10, T-DET-01, DC-VAL-02/04/05/06, DC-TXV-01/02/04/05, DC-MEM-01/02 | `consensus/` + `block_validity/` + `tx_validity/` + `mempool/` |
| `ci_check_constitution_coverage.sh` | invariant-registry ↔ code/test coverage | repo-wide |
| `ci_check_conway_cert_classification_closed.sh` *(PHASE4-B3F)* | DC-TXV-06 | `ade_types::conway::cert` + `ade_codec::conway::cert` + `ade_ledger::cert_classify` |
| `ci_check_credential_discriminant_closed.sh` *(OQ5)* | DC-LEDGER-10 | `ade_types::shelley::cert` + both era `ade_codec` decoders + `ade_ledger::{state, governance, fingerprint, rules}` + `ade_types::conway::governance` |
| `ci_check_crypto_vectors.sh` | crypto KAT regression | `ade_crypto` |
| `ci_check_dependency_boundary.sh` | T-BOUND-02 — BLUE ⇎ RED separation | full BLUE |
| `ci_check_deposit_param_authority.sh` *(PHASE4-B3)* | DC-TXV-07 | 6 BLUE crates |
| `ci_check_differential_divergence.sh` | DC-DIFF-* | replay outputs |
| `ci_check_forbidden_patterns.sh` | T-CORE-02 + `unsafe` allowlist | full BLUE |
| `ci_check_forge_purity.sh` *(PHASE4-N-C S3)* | DC-CONS-13/14/15 | `crates/ade_ledger/src/producer/forge.rs` |
| `ci_check_gov_cert_accumulation_closed.sh` *(PHASE4-B5)* | DC-LEDGER-09 | `ade_ledger::{gov_cert, rules, error, state}` |
| `ci_check_hash_uses_wire_bytes.sh` | DC-CBOR-02, T-ENC-01 | full BLUE |
| `ci_check_hfc_translation.sh` | DC-EPOCH-02 | `ade_ledger::hfc` |
| `ci_check_ingress_chokepoints.sh` | DC-INGRESS-01, T-INGRESS-01 | full BLUE |
| `ci_check_ledger_determinism.sh` | DC-LEDGER-01 | `ade_ledger` |
| `ci_check_mempool_ingress_closure.sh` *(PHASE4-N-E, S1)* | DC-MEM-03 | `ade_ledger::mempool/{ingress,admit,mod}.rs` + repo-wide `admit(` scan |
| `ci_check_mempool_ingress_replay.sh` *(PHASE4-N-E, S2 + S3)* | DC-MEM-04 | `ade_testkit::mempool/*` + `ade_testkit::lib.rs` + `ade_ledger::mempool::canonicalize` |
| `ci_check_module_headers.sh` | CE-04 contract banner | full BLUE |
| `ci_check_n2n_server_no_signing_dep.sh` *(PHASE4-N-G S6)* | Key-boundary | `crates/ade_runtime/src/network/` |
| `ci_check_no_async_in_blue.sh` *(PHASE4-N-A, S-A1)* | DC-CORE-01 | full BLUE |
| `ci_check_no_chaindb_in_consensus_blue.sh` *(PHASE4-N-B, S-B1)* | DC-CORE-01 + DC-CONS-07 | `crates/ade_core/src/consensus/` |
| `ci_check_no_density_in_fork_choice.sh` *(PHASE4-N-B, S-B8)* | DC-CONS-03 | `fork_choice.rs` + `candidate.rs` |
| `ci_check_no_float_in_consensus.sh` *(PHASE4-N-B, S-B1)* | T-CORE-02 + DC-CONS-07/08/09 | `crates/ade_core/src/consensus/` |
| `ci_check_no_parallel_header_splitter.sh` *(PHASE4-N-G S1)* | DC-CONS-16 strengthening + DC-CONS-18 | repo-wide grep gate |
| `ci_check_no_private_keys_in_corpus.sh` *(PHASE4-N-C S3)* | DC-CRYPTO-03/04/05 corpus discipline | `crates/ade_testkit/src/producer/fixtures.rs` + `crates/ade_testkit/fixtures/producer/` |
| `ci_check_no_producer_body_encoder.sh` *(PHASE4-N-C S4)* | DC-CONS-16 | repo-wide grep gate |
| `ci_check_no_secrets.sh` | no credentials/IPs/keys | repo-wide |
| `ci_check_no_semantic_cfg.sh` | T-BUILD-01 | full BLUE |
| `ci_check_no_signing_in_blue.sh` | CE-05, T-KEY-01 | full BLUE |
| `ci_check_opcert_closed.sh` *(PHASE4-N-C S2)* | DC-CONS-11/12 | `crates/ade_codec/src/shelley/opcert.rs` + `crates/ade_core/src/consensus/opcert_validate.rs` |
| `ci_check_pallas_quarantine.sh` | O-29.2 | non-`ade_plutus` |
| `ci_check_private_key_custody.sh` *(PHASE4-N-C S1)* | DC-CRYPTO-03/04/05 + OP-OPS-04 | full BLUE + `crates/ade_runtime/src/producer/` |
| `ci_check_producer_corpus_present.sh` *(PHASE4-N-C S7)* | CN-CONS-06 mechanical half | `crates/ade_testkit/src/producer/fixtures.rs` |
| `ci_check_proposal_procedures_closed.sh` *(PROPOSAL-PROCEDURES-DECODE PP-S1)* | DC-LEDGER-11 | `conway/{governance,tx}.rs` paths + repo-wide `ProposalProcedure {` scan |
| `ci_check_receive_orchestrator_no_producer_dep.sh` *(PHASE4-N-H S4)* | Key-boundary | `crates/ade_runtime/src/receive/` |
| `ci_check_receive_paths_corpus_present.sh` *(PHASE4-N-H S6)* | CE-N-H-5 + CE-N-H-6 binary presence | N-H integration tests + binary src + procedure doc |
| `ci_check_receive_reducer_closure.sh` *(PHASE4-N-H S2)* | CE-N-H-2 + CN-CONS-08 + DC-CONS-19 | `crates/ade_ledger/src/receive/reducer.rs` |
| `ci_check_receive_replay_purity.sh` *(PHASE4-N-H S3)* | CE-N-H-3 + DC-PROTO-09 | `crates/ade_runtime/src/receive/{events_to_state,in_memory_chain_write}.rs` |
| `ci_check_recovery_contract.sh` | T-REC-01, T-REC-02, DC-STORE-05 | `ade_runtime --lib recovery::` (RED) |
| `ci_check_ref_provenance.sh` | DC-REF-01 | reference corpus |
| `ci_check_rollback_materialize_closure.sh` *(PHASE4-N-I S2)* | CN-STORE-07 + DC-CONS-22 | `crates/ade_ledger/src/rollback/materialize.rs` (production-purity) + `crates/ade_ledger/src/rollback/*.rs` (single-authority scan) |
| `ci_check_scheduler_closure.sh` *(PHASE4-N-C S6)* | DC-CONS-13 + DC-MEM-03 + OP-OPS-05 | `crates/ade_runtime/src/producer/{scheduler,broadcast,tick_assembler}.rs` |
| `ci_check_self_accept_gate.sh` *(PHASE4-N-C S5)* | CN-CONS-07 | `crates/ade_ledger/src/producer/self_accept.rs` + repo-wide `AcceptedBlock` literal scan |
| `ci_check_served_chain_closure.sh` *(PHASE4-N-G S2)* | CN-CONS-07 strengthening + CE-N-G-2 | `crates/ade_ledger/src/producer/served_chain.rs` |
| `ci_check_server_paths_corpus_present.sh` *(PHASE4-N-G S7)* | CE-N-G-7 + CE-N-G-8 binary presence | N-G integration tests + binary src + procedure doc |
| `ci_check_snapshot_cadence_purity.sh` *(PHASE4-N-I S4)* | DC-STORE-07 | `crates/ade_runtime/src/rollback/*.rs` |
| **`ci_check_snapshot_encoder_closure.sh`** *(NEW — PHASE4-N-J S7)* | **CN-STORE-08 + DC-STORE-08 + DC-STORE-09 — single `pub fn encode_snapshot`/`decode_snapshot` pair lives at `framing.rs`; single `pub fn encode_ledger_state`/`decode_ledger_state` pair lives at `ledger.rs`; single `pub fn encode_chain_dep`/`decode_chain_dep` pair lives at `chain_dep.rs`; single `pub const SCHEMA_VERSION` lives at `framing.rs`; positive `FingerprintMismatch` + `UnknownVersion` references in `framing.rs`** | `crates/ade_ledger/src/snapshot/{framing,ledger,chain_dep}.rs` (canonical-site presence) + `$REPO_ROOT/crates` (repo-wide single-authority grep) |

> **Post-`75f75da` CI delta. +1.** The new gate is the PHASE4-N-J snapshot-encoder single-authority gate. **CI inventory 54 → 55.** The forward-looking `ci/ci_check_no_fail_open_in_validation.sh` (DC-VAL-06 / DC-TXV) is still **not** shipped — carried gap.

> **Carried residual gaps (unchanged at HEAD, plus the new N-J-introduced ones).** Carry-forward gaps from the prior CODEMAP are all still open EXCEPT that **(kk) closes — DC-CONS-21 (persistent snapshot encode/decode round-trip equivalence) now carries `status = "enforced"`** (the persistent encoder ships in PHASE4-N-J; round-trip equivalence is mechanically proven by the framing-layer round-trip test + the cross-impl `persistent_cache_matches_in_memory_cache_semantics` test against `InMemorySnapshotCache`; `strengthened_in` gains `PHASE4-N-J`). **(mm) NEW — `ade_runtime::rollback::persistent_cache` GREEN sub-classification inside the RED `ade_runtime` crate prefix is gated indirectly by `ci_check_snapshot_encoder_closure.sh` (the repo-wide single-authority grep proves the file cannot define a competing encoder/decoder) but the broader BLUE/GREEN/RED partition is "by file" — no dedicated CI script for the broad partition.** **(nn) NEW — no presence-gate CI script for the 6 inline tests in `persistent_cache.rs`** — unlike the N-G/N-H precedent that added presence scripts; the closure rests on the inline tests themselves + the cross-impl equivalence test. **(oo) NEW — no live operator-action binary for the persistent-snapshot path** — the persistent-snapshot half rides on the existing N-H `live_block_follow_session` binary; the binary's `--connect` mode remains honest-scope stubbed pending operator-action; a future operator run will naturally exercise `PersistentSnapshotCache` if/when the operator wires a `--snapshot-store-path` flag (forward-looking). The N-H `RO-LIVE-02` rule continues to carry the live-evidence obligation; no new RO-LIVE rule was added for the persistent-snapshot half. **(pp) NEW — `ade_testkit` did not gain a `snapshot/` sub-tree** even though the cluster plan implicitly anticipated one; the shipped artifacts live entirely as inline tests in the BLUE encoders + the 6 cross-impl tests in the GREEN `ade_runtime::rollback::persistent_cache`. Same shape as the N-G + N-H + N-I deviations; intentional (no cross-crate consumer would benefit at HEAD). **(qq) NEW — pre-Conway snapshot encode/decode is explicitly out of scope** — both `framing::encode_snapshot` and `ledger::encode_ledger_state` reject pre-Conway with `SnapshotEncodeError::EraNotSupported { era }`; both `framing::decode_snapshot` and `ledger::decode_ledger_state` surface `SnapshotDecodeError::EraNotSupported { era }` on bytes embedding a pre-Conway era tag. Widening to pre-Conway is a future cluster (the encoder's sub-state walks would need additional shapes for Byron/Shelley/Allegra/Mary/Alonzo/Babbage `LedgerState` variants). The in-scope Conway+ shape closes the DC-CONS-21 round-trip property fully.
