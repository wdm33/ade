# Module Authority Map — Ade

> **Status:** Living architectural document. Regenerated; not hand-edited.
> Per-project instance of `~/.claude/methodology/templates/codemap.md`.

> 11 crates, 376 canonical types, 1316 tests, 29 CI checks at HEAD (`a6b8de7`).

---

## Conventions

- A **module** in Ade is a Cargo workspace crate (smallest independently-buildable unit). One exception: `ade_network` is split by *submodule color* — its BLUE submodules and its RED submodules are documented as two separate entries below, because `.idd-config.json` `core_paths` resolves BLUE at the submodule path level rather than crate-wide. The `ade_core::consensus` submodule sits *inside* the BLUE `ade_core` crate and is covered by that entry. The `ade_ledger::block_validity` / `ade_ledger::consensus_view` / `ade_ledger::tx_validity` / `ade_ledger::mempool::admit` / `ade_ledger::cert_classify` modules sit inside the BLUE `ade_ledger` crate and are covered by that entry; the RED `ade_ledger::consensus_input_extract` and the GREEN `ade_ledger::mempool::policy` sit inside the BLUE `ade_ledger` crate but carry a different color by their own module doc-comment and the cluster TCB Color Maps — they are surfaced as sub-classification notes inside the `ade_ledger` entry.
- Modules are listed by TCB color (BLUE → GREEN → RED), alphabetical within each color.
- TCB color sources, in order of authority:
  1. `.idd-config.json` `core_paths` — substring match against absolute path. BLUE matches: `ade_codec`, `ade_types`, `ade_crypto`, `ade_core`, `ade_ledger`, `ade_plutus`, and the 9 `ade_network` submodule paths (`mux/frame.rs`, `codec/`, `handshake/`, `chain_sync/`, `block_fetch/`, `tx_submission/`, `keep_alive/`, `peer_sharing/`, `n2c/`).
  2. `.idd-config.json` `_core_paths_doc` — `ade_runtime` is RED; `ade_testkit` is GREEN; `ade_node` is RED; `ade_network::mux::transport` and `ade_network::session` are RED; `ade_network::lib` and `ade_network::mux::mod` are GREEN barrels. `ade_core_interop` is **RED** per its `Cargo.toml` header comment and the PHASE4-N-B TCB Color Map.
  3. `docs/clusters/PHASE4-N-B/cluster.md` § "TCB Color Map" — `era_schedule`, `praos_state`, `vrf_cert`, `nonce`, `op_cert`, `leader_schedule`, `header_validate`, `fork_choice`, `rollback` are BLUE; `chain_selector`, `candidate_fragment` are GREEN; `genesis_parser` is RED; `ade_testkit::consensus` is GREEN; `ade_core_interop` is RED.
  4. `docs/clusters/PHASE4-B1/cluster.md` § "TCB Color Map" — `ade_ledger::consensus_view`, `ade_ledger::block_validity` are BLUE; `ade_core::consensus::{header_validate, kes_check}` extensions are BLUE; `ade_testkit::validity` is GREEN; `ade_ledger::consensus_input_extract` is RED (parses an external dump format rather than a canonical type).
  5. PHASE4-B2 (tx-validity agreement) — `ade_ledger::tx_validity::{required_signers, witness, verdict, encoding, phase1, transition}` and `ade_ledger::mempool::admit` are **BLUE** (per their module doc-comments / `// Core Contract:` banners); `ade_ledger::mempool::policy` is **GREEN** (Tier-5, declared "GREEN (Tier-5)" in its own header); `ade_testkit::tx_validity::{extract, adversarial, valid_synthetic, mod}` is **GREEN**.
  6. PHASE4-B3 (full Conway tx value-conservation accounting) — all new and changed modules are **BLUE**, under the already-BLUE `ade_codec` / `ade_ledger` / `ade_types` crate prefixes (`.idd-config.json` `core_paths`): `ade_codec::conway::{cert, withdrawals}`, `ade_ledger::cert_classify`, the `ConwayOnlyDepositParams` / `ConwayDepositParams` additions to `ade_ledger::pparams`, the `conway_deposit_params` field + `conway_deposit_view()` on `ade_ledger::state`, the new `ValidationEnvironmentError` / `LedgerError` variants in `ade_ledger::error`, the full-equation `check_conway_coin_conservation` in `ade_ledger::conway`, the deposit-param fold in `ade_ledger::fingerprint`, and the `ConwayCert` / `CertDisposition` / `DepositEffect` / `CoinSource` types in `ade_types::conway::cert` plus `RewardAccount` in `ade_types::tx`.
  7. PHASE4-B5 (Conway governance-certificate accumulation) — all new and changed modules are **BLUE**, under the already-BLUE `ade_ledger` crate prefix (`.idd-config.json` `core_paths`): the new `ade_ledger::gov_cert` module (`apply_conway_gov_cert`), the `GovCertEnv` type + `LedgerState::gov_cert_env()` accessor in `ade_ledger::state`, the `ConwayOnlyDepositParams.drep_activity` field in `ade_ledger::pparams`, the `ValidationEnvironmentError::{MissingDRepActivityParam, DRepActivityOverflow}` variants in `ade_ledger::error`, the `Option<ConwayGovState>`-threading `accumulate_tx_certs` / `process_block_certificates` and gov-state-carrying `apply_block` in `ade_ledger::rules`, and the `drep_activity` extension to the Conway-deposit fingerprint tag in `ade_ledger::fingerprint`.
- Counts:
  - Crates: 11, from `Cargo.toml` `[workspace] members`.
  - Canonical types: 376, from `grep -rE "^(pub )?(struct|enum) "` across the full BLUE scope. Breakdown: `ade_codec` 8, `ade_types` 80, `ade_crypto` 12, `ade_core` 43, `ade_ledger` 127, `ade_plutus` 8, plus the 9 BLUE `ade_network` submodule paths 98. Registry `canonical_type_registry: null`, so a structural count is used. **Unchanged since the PHASE4-B5 close (OQ5-CREDENTIAL-FIDELITY).** OQ5 was a type-fidelity refactor with **no net new canonical type**: `ade_types::shelley::cert::StakeCredential` changed shape from the tuple-struct `StakeCredential(pub Hash28)` to the closed 2-variant `enum { KeyHash(Hash28), ScriptHash(Hash28) }` (plus a `hash()` accessor) — the same already-counted type, a new discriminated shape, so `ade_types` stays at 80. The `ConwayGovState` re-key (`Hash28` → `StakeCredential` on `vote_delegations` / `committee_hot_keys` / `drep_expiry`) and the `fingerprint::write_stake_credential` discriminant-emitting change are field-type / serializer changes on existing types (`ConwayGovState` stays counted once), not new types; `ade_ledger` stays at 127. **Also unchanged in COMMITTEE-CRED-FIDELITY** — the `GovActionState.committee_votes` re-type (`Vec<(Hash28, Vote)>` → `Vec<(StakeCredential, Vote)>`) in `ade_types::conway::governance` and the `ConwayGovState.committee` re-key (`Hash28` → `StakeCredential`) in `ade_ledger::state` are field-type changes on existing types (`GovActionState`, `ConwayGovState`), not new types; the new `fingerprint::write_committee_vote_list` is a serializer function, not a type; `ade_types` stays at 80 and `ade_ledger` stays at 127. **Also unchanged in DREP-VOTE-FIDELITY** — the `GovActionState.drep_votes` re-type (`Vec<(Hash28, Vote)>` → `Vec<(StakeCredential, Vote)>`) in `ade_types::conway::governance` (the `spo_votes` list stays `Hash28`-keyed by design) is a field-type change on the existing closed `GovActionState`, not a new type; the `ade_ledger::governance::lookup_stake` exact-variant DRep resolution and the `ade_ledger::fingerprint` rename `write_committee_vote_list` → `write_credential_vote_list` are a resolution change and a serializer rename on existing functions, not types; `ade_types` stays at 80 and `ade_ledger` stays at 127. **Also unchanged in ENACTMENT-COMMITTEE-FIDELITY** — the `EnactmentEffects.committee_changes` re-type (`Option<(Vec<Hash28>, Vec<(Hash28, u64)>)>` → `Option<(Vec<StakeCredential>, Vec<(StakeCredential, u64)>)>`) in `ade_ledger::governance` is a field-type change on the existing `EnactmentEffects` type (a dormant field — `UpdateCommittee` enactment is still a no-op; the migration prevents a future committee-enactment write-back from re-collapsing the discriminated `ConwayGovState.committee` map), not a new type; `ade_ledger` stays at 127. **Prior +1 (PHASE4-B5)** — from PHASE4-B5: the `GovCertEnv` struct in `ade_ledger::state` (the explicit fail-fast gov-cert environment carrying `current_epoch` + `drep_activity`; `ade_ledger` 126 → 127). The new `ade_ledger::gov_cert::apply_conway_gov_cert` is a function (not a type); the `drep_activity` field on `ConwayOnlyDepositParams` and the `MissingDRepActivityParam` / `DRepActivityOverflow` error variants enrich existing types (`ConwayOnlyDepositParams`, `ValidationEnvironmentError`) in place. **Prior +6 (PHASE4-B4)** — in `ade_ledger::delegation` (`GovernanceOwner`, `GovernanceCertEffect`, `OwnerTaggedEffect`, `ConwayCertAction`, `ConwayCertEnv`, `ConwayCertOutcome` — the owner-tagged Conway apply model; `ade_ledger` 120 → 126). The owner-completion of `ConwayCert` (`ade_types::conway::cert`) and the `owners` field on `PoolRegistrationCert` (`ade_types::shelley::cert`) enrich existing types in place rather than adding new ones, so `ade_types` stays at 80; `ade_codec::conway::cert` added the `decode_drep` helper (a function, not a type) and `ade_codec::shelley::cert` added the shared `read_pool_registration_cert` (a function), so `ade_codec` stays at 8. **Prior +12 (PHASE4-B3)** — all from PHASE4-B3, split across `ade_types` (+4: `ConwayCert`, `CertDisposition`, `DepositEffect`, `CoinSource` in `conway::cert.rs`, and `RewardAccount` in `tx.rs` — note `ConwayCert` replaced an enum already counted in the prior `ade_types` total, so the `ade_types` line moves 76 → 80), `ade_core` (+1, unrelated drift), and `ade_ledger` (+7: the `ConwayOnlyDepositParams` / `ConwayDepositParams` views in `pparams.rs`, the `ValidationEnvironmentError` / `UnsupportedStateDependentDepositAccounting` / `EraInvalidCertificateError` taxonomies in `error.rs`, and the new accounting helpers/wrappers in `conway.rs` / `cert_classify.rs`). `ade_codec` stays at 8 — `conway::cert` and `conway::withdrawals` add functions and `CodecError` variants, not new `struct`/`enum` types.
  - Tests: 1310 — count of `#[test]` / `#[tokio::test]` attributes across `crates/`. Reported as approximate per the template's fallback rule (test runner not executed). **+8 since the PHASE4-B5 close** — all from OQ5-CREDENTIAL-FIDELITY: the discriminant-preservation + unknown-tag-reject codec tests in `crates/ade_codec/tests/shelley_credential_discriminant.rs` and `crates/ade_codec/tests/conway_cert_decode_complete.rs` (`shelley_credential_preserves_discriminant`, `conway_credential_preserves_discriminant`, `unknown_credential_tag_rejects`), the fingerprint discriminant-distinguishing unit test in `fingerprint.rs` (`discriminant_changes_fingerprint`), and the B-fidelity corpus in `crates/ade_ledger/tests/credential_fidelity_corpus.rs` (`keyhash_scripthash_same_bytes_are_distinct_certstate`, `keyhash_scripthash_same_bytes_are_distinct_govstate`, `discriminant_changes_fingerprint_corpus`, `credential_accumulation_replays_byte_identical`). These are the eight `tests` entries on the now-`enforced` DC-LEDGER-10 registry rule. **+3 since the OQ5 close** — all from COMMITTEE-CRED-FIDELITY (the committee-credential half of the same discriminant-fidelity work, strengthening DC-LEDGER-10): the committee cross-resolve negatives + fingerprint-distinguishing tests in `crates/ade_ledger/tests/credential_fidelity_corpus.rs` (`committee_keyhash_scripthash_do_not_cross_resolve`, `committee_keyhash_scripthash_same_bytes_distinct`, `committee_discriminant_changes_fingerprint`). These are appended to DC-LEDGER-10's `tests` array; the rule is still one rule, still `enforced`. **+2 since the COMMITTEE-CRED-FIDELITY close** — both from DREP-VOTE-FIDELITY (the DRep-vote half of the same discriminant-fidelity work, again strengthening DC-LEDGER-10, `strengthened_in += DREP-VOTE-FIDELITY`): the DRep cross-resolve negative in `crates/ade_ledger/src/governance.rs` (`drep_keyhash_scripthash_do_not_cross_resolve`) and the DRep-vote fingerprint-distinguishing test in `crates/ade_ledger/tests/credential_fidelity_corpus.rs` (`drep_vote_discriminant_changes_fingerprint`). These are appended to DC-LEDGER-10's `tests` array; the rule is still one rule, still `enforced`. **+1 since the DREP-VOTE-FIDELITY close** — from ENACTMENT-COMMITTEE-FIDELITY (the enactment-effects half of the same discriminant-fidelity work, again strengthening DC-LEDGER-10, `strengthened_in += ENACTMENT-COMMITTEE-FIDELITY`): the dormant-field discriminant-distinct unit test in `crates/ade_ledger/src/governance.rs` (`enactment_committee_changes_keyhash_scripthash_distinct`). It is appended to DC-LEDGER-10's `tests` array; the rule is still one rule, still `enforced`. **Prior +17 (PHASE4-B5)** — all from PHASE4-B5: the native gov-apply unit tests in `gov_cert.rs` (`gov_apply_total_over_18_tags`, `composite_gov_half_applied_once_certstate_untouched_by_b5`, `drep_expiry_uses_epoch_plus_activity`, `env_free_gov_certs_need_no_env`, `drep_register_missing_env_is_fail_fast`, `drep_expiry_overflow_is_fail_closed`, `gov_apply_is_deterministic`), the env fail-fast tests in `state.rs` (`gov_cert_env_present_ok`, `gov_cert_env_missing_drep_activity_is_fail_fast`), the block-path accumulation tests in `rules.rs` (`gov_accumulation_applies_drep_registration_into_gov_state`, `gov_apply_error_halts_accumulation`), and the B5-S4 gov-state corpus in `tests/gov_state_corpus.rs` (`positive_synthetic_gov_state_accumulates`, `gov_state_accumulation_replays_byte_identical`, `adversarial_drep_register_update_missing_env_rejected`, `adversarial_drep_expiry_overflow_rejected`, `adversarial_decode_layer_rejects_guard_gov_path`, `adversarial_double_resign_is_deterministic`). **Prior +17 (PHASE4-B4)** — all from PHASE4-B4: the owner-payload-retention + DRep-grammar codec tests (`each_tag_retains_owner_payloads`, `drep_grammar_total`), the apply-model totality tests in `delegation.rs` (`conway_cert_action_total`, `apply_outcome_agrees_with_action`, `pool_registration_populates_owners_from_cert`, `removed_tag_rejects_as_era_invalid`, `drep_registration_is_owner_tagged_not_applied`), the era-dispatch + fail-closed accumulation tests in `rules.rs` (`era_dispatch_conway_accumulates_via_conway_path`, `era_dispatch_shelley_accumulates_via_shelley_path`, `conway_decode_error_is_fail_closed`, `conway_unknown_tag_is_fail_closed`, `conway_removed_tag_is_fail_closed`, `conway_apply_error_is_fail_closed`, `conway_governance_cert_routed_out_of_scope`), and the B4-S5 cert-state corpus (`positive_synthetic_cert_state_accumulates`, `cert_state_replay_byte_identical`, `adversarial_no_false_accept`). **Prior +2 (PHASE4-B3F)** (PHASE4-B3F: `crates/ade_codec/tests/conway_cert_classification.rs` gained `trailing_bytes_after_cert_array_rejected` and `huge_array_count_rejects_without_overallocating`, covering the hardened `decode_conway_certs` trailing-byte rejection and bounded preallocation). The PHASE4-B3 close added +39 (the `conway::cert` closed-grammar unit tests including the unknown-tag / removed-tag / no-catch-all cases, the `conway::withdrawals` duplicate-key + exact-sum tests, the `cert_classify` disposition-coverage tests, the `pparams` / `state` `conway_deposit_view` fail-fast tests, the `fingerprint` byte-identity-when-absent + fold-when-present tests, and the `conway` full-equation conservation tests).
  - CI checks: 29 — file count under `ci/ci_check_*.sh`. **+1 in OQ5-CREDENTIAL-FIDELITY** — DC-LEDGER-10 added the dedicated `ci/ci_check_credential_discriminant_closed.sh`: it fails if `StakeCredential` is no longer the closed `enum` in `ade_types::shelley::cert` (or the old tuple-struct `StakeCredential(Hash28)` shape reappears), if either era decoder (`ade_codec::shelley::cert` / `ade_codec::conway::cert`) stops mapping the credential tag to `KeyHash`/`ScriptHash` or reverts to a tag-discard form (`let (_cred_type|_tag …`), or if any tuple-construction `StakeCredential(<hash>)` coercion reappears on the BLUE path (`ade_codec` / `ade_ledger` / `ade_types`) — bare-hash coercion would re-introduce the discriminant collapse. **No CI delta in COMMITTEE-CRED-FIDELITY** — no new `ci_check_*.sh` file was added; the existing `ci/ci_check_credential_discriminant_closed.sh` (DC-LEDGER-10) was **extended** to also cover the committee credential surface (committee map keys + committee-vote discriminant), so the file count stays at 29. **No CI delta in DREP-VOTE-FIDELITY** — no new `ci_check_*.sh` file was added; the same `ci/ci_check_credential_discriminant_closed.sh` (DC-LEDGER-10) was **extended again** to also cover the DRep credential surface (`GovActionState.drep_votes` must stay `Vec<(StakeCredential, Vote)>`, no reversion to `Hash28`, and the DRep-vote serializer routes through the renamed `write_credential_vote_list`), so the file count stays at 29. **No CI delta in ENACTMENT-COMMITTEE-FIDELITY** — no new `ci_check_*.sh` file was added; the same `ci/ci_check_credential_discriminant_closed.sh` (DC-LEDGER-10) was **extended again** to also cover the enactment-effects surface (`EnactmentEffects.committee_changes` must stay `Option<(Vec<StakeCredential>, Vec<(StakeCredential, u64)>)>`, no reversion to `Hash28`), so the file count stays at 29. **Prior +1 (PHASE4-B5)** — DC-LEDGER-09 added the dedicated `ci/ci_check_gov_cert_accumulation_closed.sh`: it fails if `apply_conway_gov_cert` is missing or its `ConwayCert` dispatch grows a `_ =>` catch-all arm, if the B4 "routed out of B4 mutation scope" observe-and-drop comment reappears in `rules.rs` or `accumulate_tx_certs` stops calling `apply_conway_gov_cert`, if the DRep-expiry arithmetic is an unchecked `current_epoch + drep_activity` rather than `checked_add` (deterministic fail-closed via `DRepActivityOverflow`), or if the `MissingDRepActivityParam` env fail-fast / `gov_cert_env()` constructor are absent. **Prior — unchanged in PHASE4-B4** — DC-LEDGER-08 reuses the existing full-BLUE `ci/ci_check_forbidden_patterns.sh` (plus the compiler-exhaustive `match` totality + the B4 test set) as its `ci_script` rather than adding a new gate. The prior **+1 (PHASE4-B3F)** added **one new CI script**, `ci/ci_check_conway_cert_classification_closed.sh` (enforces DC-TXV-06 — fails if the Conway cert-classification closure regresses: an open-tail `Other`/`Unknown` variant or `#[non_exhaustive]` on `ConwayCert`/`CertDisposition`/`DepositEffect`/`CoinSource`, a catch-all decoder arm that constructs a `ConwayCert`, or a `_ =>` wildcard in `cert_classify::classify`). The earlier PHASE4-B3 close had added `ci/ci_check_deposit_param_authority.sh` (DC-TXV-07). The forward-looking `ci/ci_check_no_fail_open_in_validation.sh` (the `if len == K { check } else { skip }` grep gate, DC-VAL-06 / DC-TXV) is still **not** shipped. No `.github/workflows/` yet.

---

## BLUE Modules — Pure Functional Core

> **Shared header (applies to every BLUE entry below).** Every `.rs` source file begins with the contract banner
> `// Core Contract:` and the following deny attributes are present in each crate's `lib.rs` (or, for `ade_network`,
> at the crate root — the BLUE submodules inherit them):
> `#![deny(unsafe_code)]`, `#![deny(clippy::unwrap_used)]`, `#![deny(clippy::expect_used)]`,
> `#![deny(clippy::panic)]`, `#![deny(clippy::float_arithmetic)]`.
> CI scripts that enforce the shared rules (all 7 scope the full BLUE set — the 6 BLUE crates plus the 9 BLUE `ade_network` submodule paths declared in `.idd-config.json` `core_paths`):
> - `ci/ci_check_module_headers.sh` — banner first-line check.
> - `ci/ci_check_forbidden_patterns.sh` — `HashMap`, `HashSet`, `IndexMap`/`IndexSet`/`indexmap::`, `SystemTime`, `Instant`, `std::fs`, `std::net`, `tokio`, `async fn`, `f32`/`f64`, `anyhow`, `rand::thread_rng`, `thread::spawn`, plus `unsafe` outside a documented allowlist.
> - `ci/ci_check_dependency_boundary.sh` — no BLUE crate depends on a RED crate.
> - `ci/ci_check_no_signing_in_blue.sh` — `SigningKey`/`SecretKey`/`PrivateKey`/`private_key`/`sign_message`/`sign_block` forbidden in BLUE.
> - `ci/ci_check_no_semantic_cfg.sh` — `#[cfg(feature = ...)]` and `cfg!(feature = ...)` forbidden in BLUE.
> - `ci/ci_check_hash_uses_wire_bytes.sh` — no hashing of `canonical_bytes` / re-encoded bytes in BLUE.
> - `ci/ci_check_ingress_chokepoints.sh` — only named `decode_*` chokepoints construct `PreservedCbor`. The named-chokepoint list covers `decode_block_envelope`, the per-era block decoders (`decode_byron_ebb_block`, `decode_byron_regular_block`, `decode_shelley_block`, `decode_allegra_block`, `decode_mary_block`, `decode_alonzo_block`, `decode_babbage_block`, `decode_conway_block`), `decode_address`, and `PlutusScript::from_cbor` in `ade_plutus`. Check 3 explicitly allowlists `ade_plutus/src/evaluator.rs` because Plutus script CBOR is a distinct ingress surface from block CBOR (its decoder goes through aiken/pallas rather than the ade_codec primitives).
> - `ci/ci_check_pallas_quarantine.sh` — `pallas-*` references confined to `ade_plutus`.
> - `ci/ci_check_no_async_in_blue.sh` *(PHASE4-N-A, S-A1)* — `async fn`, `.await`, `tokio`, `async_std`, `futures::`, `tokio::spawn` / `async_std::task::spawn` / `smol::spawn`, and `tokio::time::*` / `async_std::task::sleep` / `futures_timer::*` forbidden anywhere in the BLUE scope. Self-test mode (`--self-test`) plants a synthetic violation and verifies the scanner trips on it. Enforces DC-CORE-01.
>
> Three additional CI scripts narrow the shared header to the `ade_core::consensus` tree specifically (PHASE4-N-B):
> - `ci/ci_check_no_chaindb_in_consensus_blue.sh` — `ChainDb` / `chain_db` token forbidden anywhere under `crates/ade_core/src/consensus/`. Strengthens DC-CORE-01 + DC-CONS-07.
> - `ci/ci_check_no_float_in_consensus.sh` — `f32`/`f64` token forbidden anywhere under `crates/ade_core/src/consensus/`. Narrows T-CORE-02 + DC-CONS-07/08/09 to the consensus surface.
> - `ci/ci_check_consensus_closed_enums.sh` — `#[non_exhaustive]`, open-tail `Other`/`Unknown` enum variants, owned `String` fields, and `Box<dyn ...>` are all forbidden. `TARGETS` scope: `crates/ade_core/src/consensus/`, `crates/ade_ledger/src/block_validity/`, `crates/ade_ledger/src/tx_validity/`, `crates/ade_ledger/src/mempool/` (extended for B1 then B2). Strengthens DC-CONS-04 + DC-CONS-10 + T-DET-01, and gates DC-VAL-02/04/05/06 + DC-TXV-01/02/04/05 + DC-MEM-01/02.
>
> A fourth narrow check enforces a single fork-choice rule:
> - `ci/ci_check_no_density_in_fork_choice.sh` — the term `density` (case-insensitive) is forbidden in `crates/ade_core/src/consensus/fork_choice.rs` and `crates/ade_core/src/consensus/candidate.rs`, with a documented `// no-density:` annotation as the only allowed exception. Strengthens DC-CONS-03 by mechanically forbidding the Genesis/catch-up scoring shape inside caught-up Praos.
>
> A fifth narrow check (PHASE4-B3) enforces deposit-parameter authority over the BLUE crates:
> - `ci/ci_check_deposit_param_authority.sh` — across `BLUE_CRATES=(ade_codec ade_types ade_crypto ade_core ade_ledger ade_plutus)`, every deposit/refund amount (`key_deposit`, `pool_deposit`, `drep_deposit`, `gov_action_deposit`) must be sourced from canonical ledger state — `ProtocolParameters.{key_deposit,pool_deposit}` plus `LedgerState.conway_deposit_params` — and NEVER from a literal constant written next to a deposit field, nor from a testkit `ConwayGovParams`. The only legitimate write path is the RED snapshot loader in `ade_testkit` populating `conway_deposit_params` from parsed snapshot bytes. Enforces DC-TXV-07.
>
> A BLUE crate or BLUE `ade_network` submodule that adds a feature flag, an async function, a `HashMap`, or a RED dep fails CI on push.
> The 3 RED-scope CI scripts added in commit `78da6c9` (`ci_check_chaindb_contract.sh`, `ci_check_recovery_contract.sh`, `ci_check_chaindb_crash_safety.sh`) and the 1 evidence script added at S-A10 (`ci_check_ce_n_a_5_proof.sh`) are not part of this shared header. They are documented in the cross-module CI matrix at the bottom.
>
> **PHASE4-B1 note.** The B1 BLUE surface (`ade_ledger::consensus_view`, `ade_ledger::block_validity::*`, `ade_core::consensus::kes_check`, the Praos-VRF + KES + `HeaderVrf` extensions to `ade_core::consensus`) inherits the shared-header scope above — it lives under the already-BLUE `ade_ledger` and `ade_core` crate prefixes, so `ci_check_forbidden_patterns.sh`, `ci_check_no_async_in_blue.sh`, and the consensus-narrow checks all cover it. No CI script was added for B1.
>
> **PHASE4-B2 note.** The new B2 BLUE surface (`ade_ledger::tx_validity::*`, `ade_ledger::mempool::admit`, `ade_ledger::conway::check_conway_coin_conservation`, `ade_ledger::rules::apply_conway_tx_to_utxo`) lives under the already-BLUE `ade_ledger` crate prefix, so the **seven full-BLUE-scoped** scripts (`ci_check_module_headers.sh`, `ci_check_forbidden_patterns.sh`, `ci_check_no_async_in_blue.sh`, `ci_check_dependency_boundary.sh`, `ci_check_no_signing_in_blue.sh`, `ci_check_no_semantic_cfg.sh`, `ci_check_hash_uses_wire_bytes.sh`, `ci_check_ingress_chokepoints.sh`) all cover it without modification — every `tx_validity`/`mempool` file carries the `// Core Contract:` banner and uses `BTreeMap`/`BTreeSet`/`Vec` only. **`ci_check_consensus_closed_enums.sh` was extended in B2** — its `TARGETS` and `STRING_SCOPE` arrays now include `crates/ade_ledger/src/tx_validity/` and `crates/ade_ledger/src/mempool/`, so the closed-enum / no-`String` / no-`Box<dyn>` properties of the B2 verdict, signer, and admission taxonomies are **grep-gated** (DC-TXV-01..05 / DC-MEM-01/02 cite it as their `ci_script`; the script passes). The remaining consensus-narrow scripts (no-float / no-chaindb / no-density) stay scoped to `crates/ade_core/src/consensus/` by design — they encode consensus-only rules. The one still-unshipped gate is the `if len == K { check } else { skip }` fail-open grep (`ci_check_no_fail_open_in_validation.sh`); the fail-open *prohibition* itself is structurally discharged (helper-based size guards) + test-enforced. See the per-module gap note.
>
> **PHASE4-B3 note.** The new B3 BLUE surface (`ade_codec::conway::{cert, withdrawals}`, `ade_ledger::cert_classify`, the `ConwayOnly­DepositParams` / `ConwayDepositParams` views and `conway_deposit_params` field, the full-equation `check_conway_coin_conservation`, the deposit-param fold in `fingerprint`, and the `ConwayCert` / `CertDisposition` / `DepositEffect` / `CoinSource` types) lives entirely under the already-BLUE `ade_codec` / `ade_ledger` / `ade_types` crate prefixes, so all seven full-BLUE-scoped scripts cover it without modification. **The closed `ConwayCert` / `CertDisposition` / `DepositEffect` / `CoinSource` enums live in `ade_types::conway::cert.rs`, which is NOT in the `TARGETS` array of `ci_check_consensus_closed_enums.sh`** — that script scopes the consensus / block-validity / tx-validity / mempool sub-trees, not the `ade_types` schema crate. Their closed shape (no `#[non_exhaustive]`, no open-tail `Other`/`Unknown`) and the closure of the cert decoder (`CodecError::UnknownCertTag` for tags ≥19, `RemovedInConway` for tags 5/6, no catch-all accept arm) plus the no-`_ =>`-wildcard property of `cert_classify::classify` are now **grep-gated by `ci_check_conway_cert_classification_closed.sh` (PHASE4-B3F)** — DC-TXV-06 is therefore CI-enforced, not only compiler-exhaustive-match + tests. (The no-owned-`String` / no-`Box<dyn>` shape of those `ade_types::conway::cert` types remains outside the closed-enum `TARGETS` array and is still test-and-review-enforced.) **B3 added `ci_check_deposit_param_authority.sh` (DC-TXV-07); B3F added `ci_check_conway_cert_classification_closed.sh` (DC-TXV-06)**, both documented below.

> **PHASE4-B4 note.** The new B4 BLUE surface lives entirely under the already-BLUE `ade_codec` / `ade_types` / `ade_ledger` crate prefixes, so all seven full-BLUE-scoped scripts cover it without modification — the owner-completed `ConwayCert`, the enriched `PoolRegistrationCert`, the new `decode_conway_certs` / `decode_drep` / shared `read_pool_registration_cert` decoders, the `apply_conway_cert` owner-tagged apply model + its types, and the era-dispatched `accumulate_tx_certs` in `rules.rs` all carry the `// Core Contract:` banner and use `BTreeMap`/`BTreeSet`/`Vec` only. **B4 added no new CI script.** DC-LEDGER-08 (closed, total, era-versioned Conway cert-state accumulation) cites the existing full-BLUE `ci_check_forbidden_patterns.sh` as its `ci_script`; its closure properties are discharged mechanically by the compiler-exhaustive `match` in `delegation::apply_conway_cert` / `delegation::conway_cert_action` (total over all 18 Conway tags + both removed tags 5/6) and the named B4 test set (owner-payload retention, DRep-grammar totality, apply-vs-action agreement, era-dispatch fail-closed, synthetic-positive + replay + adversarial-no-false-accept). The closed `ConwayCert` shape is additionally grep-gated by the B3F `ci_check_conway_cert_classification_closed.sh` — the owner-completion enriched the variants' fields but did not add an open tail, a `#[non_exhaustive]`, or a catch-all accept arm, so that gate continues to pass. **The new `ade_ledger::delegation` owner-tagged apply types (`ConwayCertAction`, `GovernanceOwner`, `GovernanceCertEffect`, `OwnerTaggedEffect`, `ConwayCertOutcome`, `ConwayCertEnv`) live in `crates/ade_ledger/src/delegation.rs`, which is NOT in the `TARGETS` array of `ci_check_consensus_closed_enums.sh`** — their closed shape (no `#[non_exhaustive]`, no open-tail `Other`/`Unknown`) is compiler-exhaustive-match + test-and-review-enforced. Surfaced as a (narrow) gap below.

> **PHASE4-B5 note.** The new B5 BLUE surface lives entirely under the already-BLUE `ade_ledger` crate prefix, so all seven full-BLUE-scoped scripts cover it without modification — the new `gov_cert.rs`, the `GovCertEnv` + `gov_cert_env()` additions to `state.rs`, the `drep_activity` field on `pparams.rs`, the new `error.rs` variants, and the gov-threading `rules.rs` / `fingerprint.rs` changes all carry the `// Core Contract:` banner and use `BTreeMap`/`BTreeSet`/`Vec` only. **B5 added one new CI script — `ci_check_gov_cert_accumulation_closed.sh` (DC-LEDGER-09, `status=enforced`).** That gate grep-defends four properties of the gov-accumulation surface: (1) `apply_conway_gov_cert` exists and its `ConwayCert` match has no `_ =>` catch-all (a new variant breaks the build instead of silently dropping its governance effect); (2) the B4 observe-and-drop is gone — the "routed out of B4 mutation scope" comment must not reappear and `accumulate_tx_certs` must call `apply_conway_gov_cert`; (3) DRep expiry uses `checked_add` (no unchecked `current_epoch + drep_activity` that would wrap silently in release); (4) the `MissingDRepActivityParam` env fail-fast and the `gov_cert_env()` constructor are present. Mechanical closure of totality is the compiler-exhaustive `match` in `apply_conway_gov_cert` (total over all 18 Conway tags + removed tags 5/6) plus the 17 named B5 tests (gov-apply totality, composite-half-once, env fail-fast, checked-overflow fail-closed, determinism, block-path accumulation, and the B5-S4 synthetic-positive + byte-identical replay + adversarial corpus). **No new crate edge** — `gov_cert` consumes the existing `ade_types::conway::cert::ConwayCert` and `ade_ledger::state::ConwayGovState`.

> **OQ5-CREDENTIAL-FIDELITY note.** The OQ5 surface lives entirely under the already-BLUE `ade_types` / `ade_codec` / `ade_ledger` crate prefixes, so all seven full-BLUE-scoped scripts cover it without modification — the closed `StakeCredential` enum, the discriminant-preserving decoders, the `ConwayGovState` re-key, and the discriminant-emitting `write_stake_credential` all carry the `// Core Contract:` banner and use `BTreeMap` only. **OQ5 added one new CI script — `ci/ci_check_credential_discriminant_closed.sh` (DC-LEDGER-10, `status=enforced`).** That gate grep-defends three properties: (1) `StakeCredential` is the closed `enum` in `ade_types::shelley::cert` and the tuple-struct `StakeCredential(Hash28)` shape has not reappeared; (2) both era decoders map the tag to `KeyHash`/`ScriptHash` and carry no tag-discard `let (_cred_type|_tag, _)` form; (3) no bare-hash `StakeCredential(<hash>)` tuple-construction coercion remains on the BLUE path (`ade_codec` / `ade_ledger` / `ade_types`). **Extended in COMMITTEE-CRED-FIDELITY** — the same gate now also defends the committee credential surface: `ConwayGovState.committee` must stay `StakeCredential`-keyed and `GovActionState.committee_votes` must stay `Vec<(StakeCredential, Vote)>` (no reversion to `Hash28`), so a committee member or a committee vote cannot collapse a key-hash and a script-hash sharing 28 bytes. **Extended again in DREP-VOTE-FIDELITY** — the same gate now also defends the DRep credential surface: `GovActionState.drep_votes` must stay `Vec<(StakeCredential, Vote)>` (no reversion to `Hash28`), and the DRep-vote serializer must route through `write_credential_vote_list` (the rename of `write_committee_vote_list`, now serving committee + DRep) so a DRep voter cannot collapse a key-hash and a script-hash sharing 28 bytes. The `spo_votes` list stays `Hash28`-keyed by design (a declared non-goal surface). **No new file** — the existing script was extended; CI count stays 29. It is **not** part of the shared-header block — it is a single-rule narrow gate, documented per-module above. **No new crate edge, no new module, no net new canonical type** (`StakeCredential` is the same `ade_types` type, re-shaped). The discriminated representation is a deliberate cert-state + gov-state fingerprint migration (T-DET-01).

---

### `ade_codec`

> **Status change in PHASE4-B3.** `ade_codec` gained two new closed-grammar Conway decoders under `conway/`: `conway::cert` (`decode_conway_certs` — the Conway-complete certificate decoder, a closed grammar over CDDL tags `0..18` with `CodecError::UnknownCertTag` for tags ≥19, tags 5/6 decoding to the `ConwayCert::RemovedInConway` marker, and **no catch-all accept arm**) and `conway::withdrawals` (`decode_withdrawals` → `BTreeMap<RewardAccount, Coin>` plus `withdrawals_sum` → exact `i128`, rejecting a repeated map key with `CodecError::DuplicateMapKey` — never last-wins). Two new `CodecError` variants — `UnknownCertTag { tag, offset }` and `DuplicateMapKey { offset }` — were added; no new public `struct`/`enum` types.
>
> **Hardening in PHASE4-B3F.** `conway::cert::decode_conway_certs` now **rejects trailing bytes after the cert array** (`CodecError::TrailingBytes`, parity with `decode_withdrawals`) and **bounds its preallocation by the remaining input** rather than trusting the declared array count — so a huge declared count over a short buffer cannot over-allocate. Two regression tests cover both (`trailing_bytes_after_cert_array_rejected`, `huge_array_count_rejects_without_overallocating`).
>
> **Status change in PHASE4-B4.** `decode_conway_certs` is now **owner-complete**: every `ConwayCert` variant retains all owner payloads (stake / DRep / committee credentials, pool id, full `PoolRegistrationCert` incl. `pool_owners`, DRep delegation targets), not just the deposit/refund fields the B3 conservation projection needed. A new closed `decode_drep` reads the `drep = [0,addr_keyhash // 1,script_hash // 2 // 3]` grammar with no catch-all (an unknown variant tag rejects deterministically). The single shared pool-params decoder moved to `crates/ade_codec/src/shelley/cert.rs` as `pub(crate) read_pool_registration_cert` (era-stable Shelley..Conway, now retaining `pool_owners`) and is called by **both** era cert decoders — one decode site, not two. The Conway decoder still has no catch-all accept arm, still rejects tags ≥19 with `CodecError::UnknownCertTag`, >
> **Status change in OQ5-CREDENTIAL-FIDELITY.** Both era credential decoders now **preserve the key/script discriminant**. `shelley::cert::decode_stake_credential` and `conway::cert::decode_stake_credential` read the credential type tag and map `0 → StakeCredential::KeyHash`, `1 → StakeCredential::ScriptHash`, rejecting any other tag with `CodecError::InvalidCborStructure { detail: "unknown stake credential type" }` — the prior tag-discarding `let (_cred_type, _) = …` / `let (_tag, _) = …` form (which collapsed key-hash and script-hash credentials onto a bare `Hash28`) is gone. `StakeCredential` (in `ade_types`) is now the closed 2-variant enum; the decoders construct only via `::KeyHash` / `::ScriptHash`, never the tuple form. Closed grammar: an unknown discriminant is a deterministic reject, never an accept.

| Attribute | Value |
|-----------|-------|
| **Purpose** | Owns Cardano-canonical CBOR ingress: the only place in the workspace that turns raw bytes into typed semantic values, with wire-byte preservation for every hash-bearing structure. |
| **Creates** | `PreservedCbor<T>`, `RawCbor`, `BlockEnvelope`, `ByronDecodedBlock`, `CodecContext`, `CodecError` (B3: + `UnknownCertTag`, `DuplicateMapKey` variants), `ContainerEncoding`, `IntWidth`, plus era-tagged block/tx wrappers under `byron/`, `shelley/`, `allegra/`, `mary/`, `alonzo/`, `babbage/`, `conway/`. **B3:** `conway::cert::decode_conway_certs` and `conway::withdrawals::{decode_withdrawals, withdrawals_sum}` (functions, not types). **B4:** `conway::cert::decode_drep` and the relocated shared `shelley::cert::read_pool_registration_cert` (functions, not types — `decode_conway_certs` now produces the owner-complete `ConwayCert`). |
| **Interprets** | All canonical Cardano CBOR — block envelopes, era-specific blocks, tx bodies, tx outs, certificates, addresses. **B3:** the Conway certificate array (tx-body key 4, closed over CDDL tags 0..18) and the Conway withdrawals map (tx-body key 5, deduplicated). **B4:** the Conway certificate array decode is now owner-complete — every variant's owner payloads (credentials, pool id, full pool params incl. `pool_owners`, DRep delegation target) are retained, and the DRep delegation grammar is decoded by the closed `decode_drep`. Sole authority for `PreservedCbor::new` (constructor is `pub(crate)`). |
| **MUST NOT** | (1) Construct `PreservedCbor` outside `ade_codec` (enforced by `pub(crate)` + `ci_check_ingress_chokepoints.sh`). (2) Re-encode wire bytes when computing hashes (forbidden by `ci_check_hash_uses_wire_bytes.sh`). (3) Use any forbidden BLUE pattern (see shared header). (4) Depend on any other workspace crate except `ade_types`. (5) **B3 — `conway::cert`:** silently accept an unknown or removed certificate tag — there is no catch-all arm; tags ≥19 reject with `CodecError::UnknownCertTag` and tags 5/6 decode to the explicit `RemovedInConway` marker (never an accept). **B3F:** must also not silently ignore trailing bytes after the cert array (rejected with `CodecError::TrailingBytes`, parity with `decode_withdrawals`) nor preallocate from an untrusted declared array count (preallocation is bounded by remaining input). **B4 (DC-LEDGER-08):** must not drop owner payloads when decoding a `ConwayCert` — the decode is owner-complete (every authoritative owner field is retained, only owner-less fields like cert anchors, pool relays, and pool metadata are structurally consumed and dropped); `read_pool_registration_cert` must retain `pool_owners`; `decode_drep` is closed (an unknown DRep variant tag rejects, never a catch-all accept); the single shared `read_pool_registration_cert` is the only pool-params decode site (no second era-specific copy). (6) **B3 — `conway::withdrawals`:** sum a partial or duplicate-key withdrawals map — a repeated `RewardAccount` key rejects with `CodecError::DuplicateMapKey`, never last-wins, so `withdrawals_sum` only ever runs over a fully-validated, deduplicated map. (7) **OQ5 — `shelley::cert` / `conway::cert` `decode_stake_credential` (DC-LEDGER-10):** must not erase the credential key/script tag — the discriminant is read and mapped to `StakeCredential::{KeyHash, ScriptHash}`; an unknown tag rejects with `CodecError::InvalidCborStructure` (`"unknown stake credential type"`), never a tag-discarding `let (_cred_type|_tag, _)` decode and never a bare-`Hash28` `StakeCredential(<hash>)` coercion (enforced by `ci_check_credential_discriminant_closed.sh`). |
| **Inbound deps** | `ade_ledger`, `ade_plutus`, `ade_testkit`, `ade_network`, `ade_runtime` (consensus genesis parser uses canonical CBOR primitives for the bootstrap anchor); read-side imports from RED test code go through `ade_testkit`. |
| **Outbound deps** | `ade_types`. No external dependencies; std-only. Dev-deps: `serde_json`, `toml`. |
| **Entry points** | `ade_codec::cbor::envelope::decode_block_envelope` (31 import sites), `ade_codec::cbor` module, `ade_codec::traits::AdeEncode`, `ade_codec::CodecContext`, `ade_codec::byron::decode_byron_block`, `ade_codec::shelley::decode_shelley_block`, `ade_codec::allegra::decode_allegra_block`, `ade_codec::mary::decode_mary_block`, `ade_codec::alonzo::decode_alonzo_block`, `ade_codec::babbage::decode_babbage_block`, `ade_codec::conway::decode_conway_block`, `ade_codec::address::decode_address`. **B2:** `ade_codec::conway::tx::decode_conway_tx_body` and `ade_codec::cbor::{read_array_header, skip_item, ContainerEncoding}` are consumed by `ade_ledger::tx_validity::phase1::decode_tx` to lift the preserved Conway tx-body slice. **NEW B3:** `ade_codec::conway::cert::decode_conway_certs` and `ade_codec::conway::withdrawals::{decode_withdrawals, withdrawals_sum}` are consumed by `ade_ledger::conway::check_conway_coin_conservation` to decode and account the cert + withdrawal terms of the value-conservation equation. **NEW B4:** `ade_codec::conway::cert::decode_conway_certs` (now owner-complete) is consumed by `ade_ledger::rules::accumulate_tx_certs` (Conway branch) which feeds each decoded `ConwayCert` into `ade_ledger::delegation::apply_conway_cert`; `ade_codec::shelley::cert::decode_certificates` is consumed by the Shelley..Babbage branch of the same dispatcher. Also `ade_codec::cbor::{canonical_width, write_array_header, write_bytes_canonical, ContainerEncoding}` — the canonical-CBOR primitive set consumed by `ade_runtime::consensus::genesis_parser::compute_anchor_hash`. |
| **Key modules** | `cbor/` (envelope + primitive reader/writer), `byron/`, `shelley/`, `allegra/`, `mary/`, `alonzo/`, `babbage/`, `conway/` (era-tagged decoders; B3 added `conway/cert.rs` + `conway/withdrawals.rs`; B4 made `conway/cert.rs` owner-complete + added `decode_drep`), `shelley/` (B4: the shared `pub(crate) read_pool_registration_cert` in `shelley/cert.rs`, called by both era decoders), `address/`, `preserved.rs` (`PreservedCbor`), `traits.rs` (`AdeEncode`/`AdeDecode`/`CodecContext`), `primitives.rs`, `error.rs` (B3: + `UnknownCertTag`, `DuplicateMapKey`). |

---

### `ade_core`

> **Status change in PHASE4-N-B.** `ade_core` hosts the entire Praos consensus authority surface under `consensus/`. **PHASE4-B1 extended it**: a real single-VRF Praos verify path (`verify_praos_vrf` / `praos_vrf_input` / `praos_leader_value` / `praos_nonce_value`) alongside the existing two-proof TPraos path; a new `kes_check` module that wires `ade_crypto::kes` into header validation with the fail-closed `expect_size` guard; a `HeaderVrf` enum (TPraos two-proof vs Praos single-proof) and `HeaderKes` carrier in `header_summary`; and `FieldKind` / `FieldError` plus new `HeaderValidationError` variants in `errors.rs` for the Praos branch of `header_validate`. **PHASE4-B2 and PHASE4-B3 did not change `ade_core`.**

| Attribute | Value |
|-----------|-------|
| **Purpose** | BLUE authoritative Praos consensus core. Owns the canonical types and pure state-transitions that decide which header / chain Ade accepts: HFC era schedule + slot↔era↔time translation, Praos chain-dep state (nonce / op-cert counters), VRF cert verification (TPraos two-proof **and** Praos single-proof) + leader-eligibility predicate, KES signature + op-cert period verification wired into header admission, header validation pipeline (TPraos and Praos branches), fork-choice (`(BlockNo, TiebreakerView)`), rollback authority, leader-schedule query, and canonical encodings of all chain-dep state and chain events. |
| **Creates** | **Schedule (`era_schedule.rs`):** `BootstrapAnchorHash`, `EraSchedule`, `EraSummary`, `EraLocation`. **State (`praos_state.rs`):** `PraosChainDepState`, `OpCertCounterMap`, `Nonce`. **Events / points (`events.rs`):** `Point`, `ChainHash`, `BlockDistance`, `SecurityParam`, `ChainEvent`, `ChainSelectionReject`. **Errors (`errors.rs`):** `HFCError`, `SlotTimeError`, `OutsideForecastRange`, `HeaderValidationError`, `FieldError`, `FieldKind` *(B1)*, `VrfCertError`, `OpCertCounterError`, `NonceEvolutionError`, `LeaderScheduleError`. **Header surface (`header_summary.rs`, `header_validate.rs`):** `HeaderInput`, `HeaderVrf` *(B1; closed TPraos/Praos VRF discriminant)*, `HeaderKes` *(B1)*, `ValidatedHeaderSummary`, `HeaderApplied`. **KES (`kes_check.rs` — B1):** no public types; `expect_size` + `verify_header_kes`. **Fork-choice (`candidate.rs`, `fork_choice.rs`):** `TiebreakerView`, `CandidateFragment`, `ChainSelectorState`, `ForkChoiceError`. **Op-cert / nonce (`op_cert.rs`, `nonce.rs`):** `OpCertObservation`, `NonceInput`. **VRF (`vrf_cert.rs`):** `VrfRole`, `VerifiedVrf`, `StakeFraction`, `ActiveSlotsCoeff`. **Leader schedule (`leader_schedule.rs`):** `LeaderScheduleQuery`, `LeaderScheduleAnswer`. **Ledger view boundary (`ledger_view.rs`):** `LedgerView` trait. **Rollback (`rollback.rs`):** `RollBackRequest`, `RollBackApplied`. **Encoding (`encoding.rs`):** `DecodeError`. 43 public types across the consensus tree; no public items elsewhere in `ade_core`. |
| **Interprets** | Canonical inputs from the `ade_runtime` shell — `HeaderInput` projections (already-parsed header fields, now carrying a `HeaderVrf` discriminant and a `HeaderKes` carrier), `EraSchedule` materialized once from genesis JSON, `LedgerView` snapshots from `ade_ledger`'s production `PoolDistrView`, and an ordered stream of `StreamInput` events. The KES check verifies the hot KES key signature over the header **body** CBOR bytes (`currentKESPeriod = slot / SLOTS_PER_KES_PERIOD`, mainnet `129_600`). All inputs are typed values; BLUE never reads raw bytes, files, sockets, or a chain store. Also decodes its own canonically-encoded `PraosChainDepState` and `ChainEvent` for replay. |
| **MUST NOT** | (1) Take or accept a `&ChainDb` / `&chain_db` / any storage reference — enforced by `ci_check_no_chaindb_in_consensus_blue.sh` (DC-CORE-01 + DC-CONS-07). (2) Use any `f32`/`f64` token — enforced by `ci_check_no_float_in_consensus.sh`; the leader-eligibility predicate uses Q.123 fixed-point in `u128`. (3) Add `#[non_exhaustive]` to any consensus enum, define an open-tail `Other`/`Unknown` variant, hold owned `String` fields in `errors.rs` / `encoding.rs` / `events.rs`, or use `Box<dyn ...>` — enforced by `ci_check_consensus_closed_enums.sh` (DC-CONS-04 + DC-CONS-10 + T-DET-01). (4) Reference the word `density` in `fork_choice.rs` or `candidate.rs` outside a `// no-density:` annotation — enforced by `ci_check_no_density_in_fork_choice.sh` (DC-CONS-03). (5) Read wall-clock time — `slot_to_time_ms` is pure integer arithmetic; DC-CONS-08. (6) Use `HashMap` / `HashSet` anywhere; `OpCertCounterMap` and per-epoch fixture maps use `BTreeMap`. (7) Contain `async fn`, `.await`, `tokio`, etc. — `ci_check_no_async_in_blue.sh`. (8) Construct a `PreservedCbor`. (9) Re-derive stake snapshots — consume `LedgerView` only (DC-CONSENSUS-02). (10) Bypass the canonical `validate_and_apply_header` pipeline. (11) **B1 fail-closed rule (DC-VAL-06):** never apply a fixed-size crypto-field check via `if len == K { check } else { skip }` — every size guard goes through `kes_check::expect_size`. (12) **B1 KES rule (DC-CRYPTO-01):** never accept a header whose KES signature or op-cert period fails; `verify_header_kes` is fail-closed. (13) All shared-header BLUE rules. |
| **Inbound deps** | `ade_ledger` *(B1 — `block_validity` calls `validate_and_apply_header` and the Praos VRF/KES surface; acyclic, see dependency-direction section)*, `ade_runtime` (consensus orchestrator + genesis parser), `ade_testkit` (consensus + validity harnesses, ledger-view stubs, stream-replay driver), `ade_core_interop` (live interop binary). |
| **Outbound deps** | `ade_types`, `ade_crypto` (`blake2b`, `verify_vrf`, and B1's `kes::{verify_kes, verify_opcert, build_opcert_signable}`), `minicbor` (canonical encoding of `PraosChainDepState` and `ChainEvent`). Dev-deps: `ade_testkit`, `serde_json`, `cardano-crypto` (vrf-draft03; tests only). |
| **Entry points** | **Most-reached BLUE consensus imports:** `use ade_core::consensus::{...}` aggregator, `ade_core::consensus::ledger_view::LedgerView`, `ade_core::consensus::vrf_cert::{vrf_input, verify_praos_vrf, praos_vrf_input, praos_leader_value, praos_nonce_value}`, `ade_core::consensus::kes_check::{verify_header_kes, expect_size}`, `ade_core::consensus::praos_state::{Nonce, PraosChainDepState}`, `ade_core::consensus::header_summary::{HeaderInput, HeaderVrf, HeaderKes, ValidatedHeaderSummary}`. **Top-level transitions:** `validate_and_apply_header`, `select_best_chain`, `apply_rollback`, `apply_nonce_input`, `apply_op_cert`, `query_leader_schedule` + `is_leader_for_vrf_output`, `verify_vrf_cert` + `verify_praos_vrf` + `check_leader_claim` + `is_leader`, `tiebreaker_prefer`, `encode_chain_dep_state` / `decode_chain_dep_state` / `encode_chain_event` / `decode_chain_event`. |
| **Key modules** | `consensus/era_schedule.rs`, `consensus/praos_state.rs`, `consensus/events.rs`, `consensus/errors.rs`, `consensus/vrf_cert.rs`, `consensus/kes_check.rs`, `consensus/nonce.rs`, `consensus/op_cert.rs`, `consensus/leader_schedule.rs`, `consensus/header_summary.rs` + `consensus/header_validate.rs`, `consensus/candidate.rs`, `consensus/fork_choice.rs`, `consensus/rollback.rs`, `consensus/ledger_view.rs`, `consensus/encoding.rs`. |

---

### `ade_crypto`

| Attribute | Value |
|-----------|-------|
| **Purpose** | Owns pure cryptographic verification primitives — Blake2b hashing, Ed25519 / Byron-bootstrap signature verification, KES verification with operational certificates, VRF verification. Verification only — signing lives in `ade_runtime`. |
| **Creates** | `Blake2b224`, `Blake2b256`, `HashAlgorithm` trait, `Ed25519VerificationKey`, `Ed25519Signature`, `ByronExtendedVerificationKey`, `KesVerificationKey`, `KesPeriod`, `OperationalCertData`, `VrfVerificationKey`, `VrfProof`, `VrfOutput`, `CryptoError`. |
| **Interprets** | Verification key / signature / proof byte structures. Not a CBOR parser — accepts already-decoded byte slices from `ade_codec` consumers. |
| **MUST NOT** | (1) Implement signing (enforced by `ci_check_no_signing_in_blue.sh`). (2) Allocate global state — verification is `fn(&[u8], &[u8]) -> bool`. (3) Use any BLUE forbidden pattern (shared header). (4) Use `unsafe` outside the allowlisted FFI in `src/vrf.rs`. (5) **B1:** `build_opcert_signable` must produce the spec-correct **raw concatenation** of the op-cert fields, not a CBOR-wrapped form. |
| **Inbound deps** | `ade_core` (consensus VRF wrapping, KES via `kes_check`, blake2b), `ade_ledger`, `ade_plutus`, `ade_testkit`. |
| **Outbound deps** | `ade_types`, `blake2`, `ed25519-dalek`, `cardano-crypto` (vrf-draft03 + kes-sum + dsign features, `default-features = false`). |
| **Entry points** | `ade_crypto::blake2b::blake2b_256` (top external), plus re-exports `blake2b_224`, `blake2b_256`, `block_header_hash`, `transaction_id`, `script_hash`, `credential_hash`, `verify_ed25519`, `verify_byron_bootstrap`, `verify_kes`, `verify_opcert`, `verify_vrf`. **B2:** `ade_crypto::{blake2b_224, verify_ed25519, Ed25519Signature, Ed25519VerificationKey}` are consumed by `ade_ledger::tx_validity::witness::verify_required_witnesses` for fail-closed key-hash coverage + Ed25519 verification over the preserved tx body hash. `ade_crypto::vrf::*` consumed by `ade_core::consensus::vrf_cert`; `ade_crypto::kes::*` consumed by `ade_core::consensus::kes_check`. |

---

### `ade_ledger`

> **Status change in ENACTMENT-COMMITTEE-FIDELITY (DC-LEDGER-10 strengthened, `strengthened_in += ENACTMENT-COMMITTEE-FIDELITY`).** A tiny preventive type migration on the one remaining gov-enactment surface still typed on a bare `Hash28`. `governance::EnactmentEffects.committee_changes` re-typed `Option<(Vec<Hash28>, Vec<(Hash28, u64)>)>` → `Option<(Vec<StakeCredential>, Vec<(StakeCredential, u64)>)>` (removed cold committee credentials, added cold credentials with expiry). The field is **dormant** — `enact_proposals`'s `GovAction::UpdateCommittee` arm is still a no-op (committee changes held as raw CBOR; full enactment deferred), so `committee_changes` is always `None` at HEAD. The migration is preventive: when `UpdateCommittee` enactment is implemented it must write back into the discriminant-keyed `ConwayGovState.committee` map, and a `Hash28`-typed effect would re-collapse a key-hash and a script-hash committee credential sharing 28 bytes — typing the effect on the discriminated `StakeCredential` now makes that re-collapse structurally unrepresentable. **Still one rule (DC-LEDGER-10), still `enforced`, no new module, no new crate edge, no net new type** (`EnactmentEffects` is field-re-typed in place), **no new CI script** (`ci/ci_check_credential_discriminant_closed.sh` extended to also defend `EnactmentEffects.committee_changes`). +1 test (`enactment_committee_changes_keyhash_scripthash_distinct`, appended to DC-LEDGER-10's `tests` array).
>
> **Status change in DREP-VOTE-FIDELITY (DC-LEDGER-10 strengthened, `strengthened_in += DREP-VOTE-FIDELITY`).** The DRep half of the same credential-fidelity work — the one remaining gov-state vote surface OQ5/COMMITTEE still resolved through a bare `Hash28`. `governance::evaluate_ratification` / `check_ratification`'s DRep tally now resolves a DRep vote by **exact credential variant** — `lookup_stake` maps `StakeCredential::KeyHash → DRep::KeyHash` and `StakeCredential::ScriptHash → DRep::ScriptHash` and reads that single DRep-stake key, with **no key/script OR-fallback**, so a key-hash DRep voter can never tally a script-hash DRep's stake of equal 28 bytes (matching cardano-node's `Credential`-keyed DRep distribution). `fingerprint`'s `write_committee_vote_list` was renamed `write_credential_vote_list` (it now serves both the committee-vote list and the DRep-vote list — both emit the credential discriminant before the hash), and `drep_votes` is routed through it (replacing the prior `Hash28`-only `write_vote_list` on that field); `spo_votes` stays on `write_vote_list` by design. **Still one rule (DC-LEDGER-10), still `enforced`, no new module, no new crate edge, no net new type, no new CI script** — `GovActionState.drep_votes` (in `ade_types::conway::governance`) is a field-type re-keying on an existing type; `write_credential_vote_list` is a renamed serializer function.
>
> **Status change in OQ5-CREDENTIAL-FIDELITY.** `ade_ledger` re-keyed its Conway governance state on the discriminated credential and made the canonical fingerprint discriminant-faithful (DC-LEDGER-10, `status=enforced`). `state::ConwayGovState` re-keyed `vote_delegations` / `committee_hot_keys` / `drep_expiry` from bare `Hash28` to `ade_types::shelley::cert::StakeCredential` — a key-hash and a script-hash credential sharing 28 bytes are now distinct authoritative-state keys (matching cardano-node's `Credential`-keyed UMap/VState). `fingerprint::write_stake_credential` now emits the discriminant (`0`/`1`) followed by the 28-byte hash, and the gov-map fingerprint writers (`drep_expiry`, `vote_delegations`, `committee_hot_keys`) call it instead of `write_hash28`, so two states differing only in a credential's key/script tag fingerprint differently (a deliberate cert-state + gov-state fingerprint migration, T-DET-01). `gov_cert::apply_conway_gov_cert` consumes the discriminated credential directly (`credential.clone()`, `hot_credential.clone()`, etc.) instead of projecting `.0`. `cert_classify` and `delegation` consume the discriminated credential (test helpers build via `::KeyHash`). **`governance::evaluate_ratification` / `check_ratification` and `rules::apply_epoch_boundary_with_registrations` use `cred.hash()` ONLY as a read-only boundary adapter** against the `Hash28`-keyed stake-distribution snapshot (a declared non-goal surface): the `DRep::{KeyHash,ScriptHash}` discriminant is rebuilt into the matching `StakeCredential` variant to read `drep_expiry`. **No new module, no new crate edge, no net new type** — `StakeCredential` is the same already-counted `ade_types` type with a discriminated shape.
>
> **Status change in COMMITTEE-CRED-FIDELITY (DC-LEDGER-10 strengthened, `strengthened_in += COMMITTEE-CRED-FIDELITY`).** The committee half of the same credential-fidelity work — the one credential surface OQ5 still resolved through `KeyHash`. `state::ConwayGovState.committee` is re-keyed `Hash28` → `StakeCredential`, so a committee member that is a key-hash and one that is a script-hash sharing 28 bytes are now distinct authoritative-state members (matching cardano-node's `Credential`-keyed committee). `governance::evaluate_ratification` / `check_ratification` now take `committee_members: &BTreeMap<StakeCredential, u64>` and `committee_hot_keys: &BTreeMap<StakeCredential, StakeCredential>` and resolve a vote by **full-credential equality** — the committee-resolution path no longer projects through `cred.hash()`, so a hot/cold authorization cannot cross-resolve a key-hash vote onto a script-hash member. `fingerprint` gained `write_committee_vote_list` (committee votes emit the credential discriminant before the hash) and the committee map writer now uses `write_stake_credential` instead of `write_hash28`, so two gov-states differing only in a committee member's key/script tag fingerprint differently. **Still one rule, still `enforced`, no new module, no new crate edge, no net new type** — `GovActionState.committee_votes` (in `ade_types::conway::governance`) and `ConwayGovState.committee` are field-type re-keyings on existing types; `write_committee_vote_list` is a serializer function.
>
> **Status change in PHASE4-B5.** `ade_ledger` shipped the **closed, total, era-versioned Conway governance-certificate accumulation** (DC-LEDGER-09, `status=enforced`), retiring B4's observe-and-drop disposition for governance certs. New BLUE module `gov_cert` — `apply_conway_gov_cert(&ConwayGovState, &ConwayCert, Option<&GovCertEnv>) -> Result<ConwayGovState, LedgerError>`: a native, total dispatch over the owner-complete `ConwayCert` that applies the governance-affecting certs into `ConwayGovState` — vote-delegation (tags 9/10/12/13) → `vote_delegations[cred] = drep`; committee auth (14) → `committee_hot_keys[hot] = cold`; committee cold-resign (15) → clears that member's hot authorizations; DRep register/update (16/18) → `drep_expiry[cred] = env.current_epoch + env.drep_activity` (env-driven, `checked_add`); DRep unregister (17) → removes `drep_expiry[cred]`. It mutates **only** governance-owned fields of `ConwayGovState`; it neither takes nor returns the B4-owned `CertState`, so it cannot mutate delegation/pool state and cannot double-apply the delegation/pool half of composite certs (10/12/13). `state` gained `GovCertEnv { current_epoch, drep_activity }` + the fail-fast `LedgerState::gov_cert_env()` accessor (the only `GovCertEnv` constructor); `pparams` gained the Conway-only `ConwayOnlyDepositParams.drep_activity`; `error` gained `ValidationEnvironmentError::{MissingDRepActivityParam, DRepActivityOverflow}`. `rules::accumulate_tx_certs` / `process_block_certificates` now thread `Option<ConwayGovState>` and apply the gov half (replacing B4's observe-and-drop), and `apply_block` carries `gov_state` forward instead of nulling it — `ConwayGovState` becomes a deterministic function of (boundary-loaded base then replayed block-stream cert effects). `fingerprint`'s Conway-deposit tag extended 2→3 fields for `drep_activity`.
>
> **Status change in PHASE4-B4.** `ade_ledger` shipped the **closed, total, era-versioned Conway cert-state accumulation** (DC-LEDGER-08, `status=enforced`). `delegation` gained the native owner-tagged Conway apply model: `apply_conway_cert(state, cert, env) -> Result<ConwayCertOutcome, LedgerError>`, the closed action classifier `conway_cert_action(&ConwayCert) -> ConwayCertAction` (compiler-exhaustive over all 18 tags + both removed tags 5/6), and the new types `ConwayCertAction`, `GovernanceOwner`, `GovernanceCertEffect`, `OwnerTaggedEffect`, `ConwayCertOutcome`, `ConwayCertEnv`. Delegation/pool certs mutate B4-owned `CertState`; governance-affecting certs (vote-delegation, committee auth/resign, DRep register/unregister/update) are **owner-tagged to `ConwayGovState` and routed out of B4's mutation scope** — observed and returned in `ConwayCertOutcome.owner_tagged`, never silently neutralized and never applied here (a future PHASE4-B5 governance cluster applies them); composite certs (tags 10/12/13) carry both a `CertState` mutation and an owner-tagged effect. Removed tags 5/6 reject with `LedgerError::EraInvalidCertificate`. `apply_pool_registration` now populates `PoolParams.owners` from the enriched cert. `rules::process_block_certificates` now era-dispatches through the new `accumulate_tx_certs` (Conway → `decode_conway_certs` + `apply_conway_cert`; Shelley..Babbage → `decode_certificates` + `apply_cert`): the prior `_era` discard and **both fail-open swallows are removed** — decode/apply errors propagate as structured `LedgerError` and halt the block transition (the "non-fatal during replay" rationale was false; this path runs only at `track_utxo`, i.e. with full state present). `cert_classify`'s B3 deposit projection was updated to the enriched `ConwayCert` with byte-identical dispositions preserved.
>
> **Status change in PHASE4-B3.** `ade_ledger` closed the Conway tx value-conservation accounting (the documented B2 follow-up). New BLUE submodule `cert_classify` (`classify(&ConwayCert, &ConwayDepositParams, &CertState) -> Result<CertDisposition, UnsupportedStateDependentDepositAccounting>` — a total, closed map over `ConwayCert` that returns a structured reject rather than guessing a state-dependent deposit/refund). `pparams` gained `ConwayOnlyDepositParams` (`drep_deposit`, `gov_action_deposit`) and the `ConwayDepositParams` view; `state` gained `LedgerState.conway_deposit_params: Option<ConwayOnlyDepositParams>` (Some iff Conway) + a fail-fast `conway_deposit_view()`; `error` gained `ValidationEnvironmentError::MissingConwayDepositParams` and the `LedgerError::{EraInvalidCertificate, UnsupportedStateDependentDeposit, ValidationEnvironment}` variants. **`conway::check_conway_coin_conservation` now enforces the FULL preservation-of-value equation** — `consumed = inputs + withdrawals + refunded_deposits == produced = outputs + fee + donation + new_deposits`, with the §9.1 reject precedence (decode → structural → state-dependent → conservation last); the B2 cert/withdrawal early-out (the known false-accept path) is **REMOVED**. `fingerprint::fingerprint_pparams` folds the Conway deposit params under `CONWAY_DEPOSIT_PARAMS_TAG` when present (byte-identical for non-Conway states). This closes the deposit/refund/withdrawal value-conservation follow-up flagged in B2.
>
> **Prior status (PHASE4-B2).** `ade_ledger` gained the transaction-validity authority (`tx_validity::*`, `mempool::admit` BLUE, `mempool::policy` GREEN) and the pure-spend `check_conway_coin_conservation` that B3 has now generalized to the full equation.
>
> **Prior status (PHASE4-B1).** `ade_ledger` gained the block-validity authority and depends on `ade_core` (acyclic). BLUE submodules `consensus_view` (`PoolDistrView`) and `block_validity::*`; one RED submodule `consensus_input_extract`.

| Attribute | Value |
|-----------|-------|
| **Purpose** | The functional core (ledger half): stateless ledger rules for every Cardano era — UTxO transitions, certificate effects, fee/value conservation, epoch boundary accounting, hard-fork translations, governance ratification/enactment, canonical fingerprint of `LedgerState`; the B1 top-level **block-validity** verdict; the B2 top-level **transaction-validity** verdict (`tx_validity`, DC-TXV-01..05) plus the **mempool admission** gate (`mempool::admit`, DC-MEM-01); and, as of B3, the **full Conway value-conservation accounting** — closed certificate-deposit classification (`cert_classify`, DC-TXV-06) sourcing every deposit/refund from canonical deposit-parameter authority (`pparams`/`state`, DC-TXV-07); and, as of B4, the **closed, total, era-versioned Conway cert-state accumulation** — the owner-tagged Conway apply model (`delegation::apply_conway_cert`, DC-LEDGER-08) and the era-dispatched fail-closed accumulator (`rules::accumulate_tx_certs`) that route governance certs to `ConwayGovState` and propagate decode/apply errors instead of swallowing them; and, as of B5, the **closed, total, era-versioned Conway governance-certificate accumulation** — the native gov-cert apply model (`gov_cert::apply_conway_gov_cert`, DC-LEDGER-09) that applies vote-delegation / committee / DRep certs into `ConwayGovState` (env-driven DRep expiry with `checked_add` fail-closed) while leaving B4-owned `CertState` untouched, with `rules::accumulate_tx_certs` now threading and carrying `ConwayGovState` forward (replacing B4's observe-and-drop). |
| **Creates** | Pre-B1 (selected): `LedgerState`, `EpochState`, `UTxOState`, `TxOut`, `Value`, `MultiAsset`, `AssetName`, `DelegationState`, `CertState`, `PoolParams`, `ConwayGovState`, `GovActionState`, `RatificationResult`, `EnactmentEffects`, `ProtocolParameters`, `BlockApplyResult`, `BlockVerdict`, `TxVerdict`, `ScriptPosture`, `EpochBoundaryResult`, `LedgerFingerprint`, `Rational`, `WitnessInfo`, plus the layered `LedgerError` family (incl. `ConservationError`, `WitnessError`, `RequiredSignerDerivation`). **B1 — `consensus_view`:** `PoolDistrView`, `PoolEntry`. **B1 — `block_validity`:** `BlockValidityOutcome`, `DecodedBlock`, `BlockValidityVerdict`, `BlockRejectClass`, `BlockValidityError`, `FieldError`, `FieldKind`, `MissingInput`, `VerdictSurface`, `SurfaceDecodeError`. **B2 — `tx_validity`:** `TxValidityVerdict`, `TxRejectClass`, `TxValidityError`, `TxValidityOutcome`, `TxVerdictSurface`, `TxSurfaceDecodeError`, `DecodedTx`, `VKeyWitnessRef`, `WitnessClosureError`, `WitnessField`, `SignerSource`, `RequiredSigners`, `RequiredSignerError`, `RequiredSignerField`, `ResolvedInputs`, `ResolvedOutput`. **B2 — `mempool`:** `MempoolState`, `AdmitOutcome` (BLUE); `OrderPolicy` (GREEN). **NEW B3 — `pparams`:** `ConwayOnlyDepositParams` (`drep_deposit`, `gov_action_deposit`), `ConwayDepositParams` (the view combining `ProtocolParameters.{key_deposit,pool_deposit}` with the Conway-only pair). **NEW B3 — `error`:** `ValidationEnvironmentError` (incl. `MissingConwayDepositParams`), `UnsupportedStateDependentDepositAccounting`, `EraInvalidCertificateError`, plus the `LedgerError::{EraInvalidCertificate, UnsupportedStateDependentDeposit, ValidationEnvironment}` variants. **NEW B3 — `cert_classify`:** `classify` (function; consumes the `ade_types` `CertDisposition`/`DepositEffect`/`CoinSource` closed sum types). **NEW B4 — `delegation`:** `ConwayCertAction`, `GovernanceOwner`, `GovernanceCertEffect`, `OwnerTaggedEffect`, `ConwayCertOutcome`, `ConwayCertEnv` (the owner-tagged Conway cert-state apply model; the existing `CertState` / `DelegationState` / `PoolState` / `PoolParams` carry through, with `PoolParams.owners` now populated from the enriched cert). The B4 era-dispatch (`rules::accumulate_tx_certs`) and apply entry (`delegation::apply_conway_cert`, `delegation::conway_cert_action`) are functions, not types. **NEW B5 — `state`:** `GovCertEnv` (`current_epoch`, `drep_activity` — the explicit fail-fast gov-cert environment; the only new public type in B5). **B5 — `pparams`:** `ConwayOnlyDepositParams` gained the `drep_activity` field (enriched in place). **B5 — `error`:** `ValidationEnvironmentError` gained the `MissingDRepActivityParam` / `DRepActivityOverflow` variants (enriched in place). **B5 — `gov_cert`:** `apply_conway_gov_cert` (function; mutates only governance-owned fields of the existing `ConwayGovState` — `vote_delegations`, `committee_hot_keys`, `drep_expiry`). |
| **Interprets** | Decoded canonical types from `ade_codec`. Hosts the per-era validation composers and the late-era witness decoder. **B1:** `block_validity::header_input::decode_block`; `block_validity::encoding::*`. **B2:** `tx_validity::phase1::decode_tx` lifts the preserved Conway tx-body slice (`tx_id = blake2b_256(body_slice)` over preserved bytes, T-ENC-01); `tx_validity::encoding::*` is the canonical per-tx replay/comparison surface. **NEW B3:** `conway::check_conway_coin_conservation` decodes the certificate array via `ade_codec::conway::cert::decode_conway_certs`, classifies each cert via `cert_classify::classify` into a `DepositEffect` sourced from `conway_deposit_view()`, decodes the withdrawals map via `ade_codec::conway::withdrawals::decode_withdrawals` and sums it exactly (`i128`), then balances the full equation. **NEW B4:** `rules::accumulate_tx_certs` decodes each tx's certificate array era-correctly — Conway via `ade_codec::conway::cert::decode_conway_certs` (owner-complete) feeding `delegation::apply_conway_cert`, Shelley..Babbage via `ade_codec::shelley::cert::decode_certificates` feeding `delegation::apply_cert` — and threads the resulting `CertState` through the block. **NEW B5:** `rules::accumulate_tx_certs` additionally feeds each owner-complete `ConwayCert` (when governance is tracked, `gov_state.is_some()`) into `gov_cert::apply_conway_gov_cert`, reading the governance payloads (DRep target, committee hot/cold credentials, DRep credential) directly off the cert and mutating only `ConwayGovState`; DRep register/update consult the fail-fast `LedgerState::gov_cert_env()` for `current_epoch + drep_activity` (a missing param is a structured `MissingDRepActivityParam`, an overflow a `DRepActivityOverflow`, never a defaulted expiry). |
| **MUST NOT** | (1) Perform I/O (shared header). (2) Use `HashMap`, floats, or any other BLUE-forbidden pattern (`ci_check_forbidden_patterns.sh`). (3) Hash anything other than wire bytes (`ci_check_hash_uses_wire_bytes.sh`). (4) Construct `PreservedCbor` directly outside the named decoders (`ci_check_ingress_chokepoints.sh`). (5) Depend on `ade_runtime` (`ci_check_dependency_boundary.sh`). (6) Use signing primitives or add `#[cfg(feature)]` gates. (7) Omit the `// Core Contract:` banner. (8) **B1 — `consensus_view` (DC-CONSENSUS-02):** must not rederive a stake snapshot; `BTreeMap` only. (9) **B1 — `block_validity` (DC-VAL-02/03):** must not produce `Valid` while skipping the header or the body authority. (10) **B1 (DC-VAL-05):** invalid path returns unchanged input states plus a structured error; no partial mutation. (11) **B1/B2 (DC-VAL-06):** every crypto-input / field-size check rejects on wrong size or shape and never silently skips; `if X.len() == K { check } else { skip }` is forbidden. (12) **B2 — `tx_validity` (DC-TXV-01):** `tx_validity` must be a pure function of `(LedgerState, tx_cbor)`. (13) **B2 (DC-TXV-02):** never produce `Valid` while skipping phase-1 or (when Plutus scripts are present) phase-2. (14) **B2 (DC-TXV-04):** a `Valid` tx yields an applied `LedgerState'`; an `Invalid` tx returns the input state unchanged plus a structured reason. (15) **B2 (DC-TXV-05):** `required_signers` must be a CLOSED, era-versioned function over every signer source. (16) **B2 — `witness` (DC-VAL-06 / CN-LEDGER-09):** witness coverage is fail-closed. (17) **B2 — `mempool::admit` (DC-MEM-01):** admit a tx iff `tx_validity` is `Valid` — NO FALSE ACCEPT. (18) **B2 — `mempool::policy` (GREEN, DC-MEM-02):** must not call `tx_validity`, touch the accumulating state, or change any admit verdict. (19) **NEW B3 — `cert_classify` (DC-TXV-06):** must not guess a state-dependent deposit/refund — classification is total and closed over `ConwayCert`; a refund/deposit that cannot be resolved from the registration `CertState` returns `UnsupportedStateDependentDepositAccounting`, never a fabricated amount and never the `key_deposit` param (which can drift from the amount recorded at registration); a known-but-removed tag maps to `CertDisposition::NotValidInConway`, never an accept. (20) **NEW B3 — `pparams` / `state` (DC-TXV-07):** must not source any deposit/refund amount from non-canonical or testkit state — every `key_deposit` / `pool_deposit` / `drep_deposit` / `gov_action_deposit` comes from `ProtocolParameters` + `LedgerState.conway_deposit_params` via `conway_deposit_view()`, which is `Some` iff the state is Conway and fails fast with `ValidationEnvironmentError::MissingConwayDepositParams` otherwise (enforced by `ci_check_deposit_param_authority.sh`). (21) **NEW B3 — `conway::check_conway_coin_conservation` (T-CONSERV-01 / CN-LEDGER-07, strengthened):** must not accept a cert/withdrawal-bearing tx without the full value check — the B2 early-out is removed; the full equation `consumed == produced` runs for every Conway tx, with the §9.1 reject precedence (decode failure → structural → state-dependent deposit failure → conservation last). (22) **NEW B3 — `fingerprint` (DC-LEDGER-01):** the Conway deposit-param fold under `CONWAY_DEPOSIT_PARAMS_TAG` must be byte-identical to the prior fingerprint for any non-Conway state (`conway_deposit_params == None` ⇒ unchanged bytes). (23) **NEW B4 — `delegation::apply_conway_cert` (DC-LEDGER-08):** must not reduce a `ConwayCert` into the 7-variant Shelley `Certificate`, must not flatten any cert to neutral (there is no `Neutral` action — every defined Conway tag has an owner), must not drop owner payloads, and must not swallow an apply error; governance-affecting certs are owner-tagged to `ConwayGovState` and routed out of B4's mutation scope (observed, not applied, not neutralized); removed tags 5/6 reject with `LedgerError::EraInvalidCertificate`. (24) **NEW B4 — `rules::accumulate_tx_certs` / `process_block_certificates` (DC-LEDGER-08):** must not swallow a decode or apply error — the prior "non-fatal during replay" fail-open swallows are removed; decode/apply errors propagate as structured `LedgerError` and halt the block transition; Conway bytes must dispatch to the Conway decoder (`decode_conway_certs`), never the Shelley 6-variant decoder. (25) **NEW B5 — `gov_cert::apply_conway_gov_cert` (DC-LEDGER-09):** must not mutate the B4-owned `CertState` (it neither takes nor returns it), must not double-apply the delegation/pool half of composite certs (10/12/13 — only the DRep target is applied here), must not drop a governance payload, must not leave a non-exhaustive `ConwayCert` match (closed over all 18 tags; no `_ =>` arm — enforced by `ci_check_gov_cert_accumulation_closed.sh`), and must not default a DRep expiry — the env is required for tags 16/18 and a missing `drep_activity` fails fast with `MissingDRepActivityParam`, while an arithmetic overflow halts deterministically with `DRepActivityOverflow` (`checked_add`, never an unchecked `current_epoch + drep_activity` that would wrap silently in release). (26) **NEW B5 — `rules::accumulate_tx_certs` / `process_block_certificates` (DC-LEDGER-09):** must not observe-and-drop governance effects — the B4 "routed out of B4 mutation scope" disposition is retired; when governance is tracked the gov half is applied via `apply_conway_gov_cert` and `gov_state` is carried forward through `apply_block` (never nulled); a gov-apply error propagates as a structured `LedgerError` and halts the block transition (no best-effort accumulation). (27) **NEW OQ5 — `state::ConwayGovState` + `fingerprint::write_stake_credential` (DC-LEDGER-10):** the governance maps (`vote_delegations`, `committee_hot_keys`, `drep_expiry`, and — **COMMITTEE-CRED-FIDELITY** — `committee`) must key on the discriminated `StakeCredential`, never on a tag-erased `Hash28`, the committee-vote list (`GovActionState.committee_votes`) and — **DREP-VOTE-FIDELITY** — the DRep-vote list (`GovActionState.drep_votes`) must carry the discriminated `StakeCredential` (not a bare `Hash28`; `spo_votes` stays `Hash28` by design), and the canonical fingerprint must serialize the credential discriminant before its hash (including the committee map via `write_stake_credential` and both the committee-vote and DRep-vote lists via the shared `write_credential_vote_list`) — a key-hash and a script-hash credential sharing 28 bytes must be distinct keys and must fingerprint differently. **COMMITTEE-CRED-FIDELITY:** committee ratification resolution must not collapse the discriminant — `evaluate_ratification` / `check_ratification` resolve a committee vote by full-credential equality, never by projecting a vote or a member through `cred.hash()`, so a hot/cold authorization cannot cross-resolve a key-hash vote onto a script-hash member. **DREP-VOTE-FIDELITY:** DRep ratification resolution must not collapse the discriminant either — `evaluate_ratification` / `check_ratification`'s `lookup_stake` resolves a DRep vote by exact credential variant (`KeyHash → DRep::KeyHash`, `ScriptHash → DRep::ScriptHash`), never a key/script OR-fallback, so a key-hash DRep voter cannot tally a script-hash DRep's stake of equal bytes. **ENACTMENT-COMMITTEE-FIDELITY:** the enactment effect `governance::EnactmentEffects.committee_changes` must carry the discriminated `StakeCredential` (the cold committee credentials, removed and added-with-expiry), never a bare `Hash28` — even though the field is dormant (`UpdateCommittee` enactment is still a no-op, so it is always `None`), a `Hash28`-typed effect would re-collapse the discriminated `ConwayGovState.committee` map on a future write-back; the discriminated type makes that re-collapse unrepresentable (no reversion to `Hash28`, enforced by the extended `ci_check_credential_discriminant_closed.sh`). `cred.hash()` is permitted ONLY as a read-only boundary adapter in `governance.rs` / `rules.rs` against the `Hash28`-keyed stake-distribution **snapshot** surface (a declared non-goal — DRep-expiry / reward-account / pool-owner reads), never on the committee path and never to construct an authoritative gov-state / cert-state key (enforced by the extended `ci_check_credential_discriminant_closed.sh`). |
| **Inbound deps** | `ade_testkit`. |
| **Outbound deps** | `ade_types`, `ade_crypto`, `ade_codec`, `ade_plutus`, `ade_core` *(B1)*, `minicbor`, `num-bigint`, `num-integer`, `num-traits`. No external runtime / I/O crates. Dev-dep: `ade_testkit`. **B3 added no new crate edge** — `cert_classify` consumes the `ade_types` closed cert types; `check_conway_coin_conservation` reuses the existing `ade_codec` edge for `conway::cert` + `conway::withdrawals`. |
| **Entry points** | `ade_ledger::rules::apply_block` (8 import sites), `apply_block_classified`, `apply_block_with_accounting`, `apply_block_with_verdicts`, `apply_conway_tx_to_utxo` *(B2 — shared single-tx UTxO transition)*, `apply_epoch_boundary_full`, `ade_ledger::state::{LedgerState, EpochState}` (B3: `LedgerState::conway_deposit_view`), `ade_ledger::pparams::{ProtocolParameters, ConwayOnlyDepositParams, ConwayDepositParams}`, `ade_ledger::utxo::UTxOState`, `ade_ledger::hfc::translate_era`, `ade_ledger::fingerprint::fingerprint`, `ade_ledger::governance::*`, `ade_ledger::delegation::*`, `ade_ledger::epoch::*`, `ade_ledger::witness::{decode_all_plutus_scripts_in_block, decode_witness_infos}`. **B1:** `ade_ledger::block_validity::{block_validity, BlockValidityVerdict, BlockRejectClass, BlockValidityError, encode_verdict_surface, decode_verdict_surface, decode_block}`, `ade_ledger::consensus_view::{PoolDistrView, PoolEntry}`. **B2:** `ade_ledger::tx_validity::{tx_validity, TxValidityOutcome, TxValidityVerdict, TxRejectClass, TxValidityError, encode_tx_verdict_surface, decode_tx_verdict_surface, decode_tx, tx_phase_one, required_signers, tx_derived_required_signers, RequiredSigners, SignerSource, verify_required_witnesses, VKeyWitnessRef}`, `ade_ledger::mempool::{admit, MempoolState, AdmitOutcome, order, OrderPolicy}`. **NEW B3:** `ade_ledger::cert_classify::classify`, `ade_ledger::conway::check_conway_coin_conservation` (internal to `validate_conway_state_backed`). **NEW B4:** `ade_ledger::delegation::{apply_conway_cert, conway_cert_action, ConwayCertEnv, ConwayCertOutcome, ConwayCertAction, GovernanceOwner, GovernanceCertEffect, OwnerTaggedEffect}`; `ade_ledger::rules::accumulate_tx_certs` (internal to `process_block_certificates`, reached from `apply_block*` at `track_utxo`). **NEW B5:** `ade_ledger::gov_cert::apply_conway_gov_cert`; `ade_ledger::state::{GovCertEnv, LedgerState::gov_cert_env}`; `ade_ledger::pparams::ConwayOnlyDepositParams` (now carrying `drep_activity`); `ade_ledger::error::ValidationEnvironmentError::{MissingDRepActivityParam, DRepActivityOverflow}`. `accumulate_tx_certs` now threads `Option<ConwayGovState>` and applies the gov half. |
| **Key modules** | `byron.rs`, `shelley.rs`, `mary.rs`, `alonzo.rs`, `babbage.rs`, `conway.rs` (per-era composers; B3 generalized `check_conway_coin_conservation` to the full preservation-of-value equation inside `validate_conway_state_backed`, removing the B2 cert/withdrawal early-out), `rules.rs` (top-level `apply_block*`; B2 added `apply_conway_tx_to_utxo`; B4 era-dispatches certs via `accumulate_tx_certs` and removed both fail-open swallows in `process_block_certificates`; B5 threads `Option<ConwayGovState>` through `accumulate_tx_certs` / `process_block_certificates`, applies the gov half via `gov_cert::apply_conway_gov_cert`, and carries `gov_state` forward through `apply_block`), `state.rs` (B3: `conway_deposit_params` field + `conway_deposit_view()`; B5: `GovCertEnv` type + fail-fast `gov_cert_env()` accessor; OQ5: `ConwayGovState` re-keyed `Hash28` → `StakeCredential` on `vote_delegations` / `committee_hot_keys` / `drep_expiry`; COMMITTEE-CRED-FIDELITY: `ConwayGovState.committee` re-keyed `Hash28` → `StakeCredential`), `utxo.rs`, `delegation.rs` (B4: `apply_conway_cert` + `conway_cert_action` owner-tagged Conway apply model and its types; `apply_pool_registration` now populates `PoolParams.owners`), `epoch.rs`, `governance.rs`, `hfc.rs`, `late_era_validation.rs`, `phase.rs`, `plutus_eval.rs`, `pparams.rs` (B3: `ConwayOnlyDepositParams` + `ConwayDepositParams`; B5: `ConwayOnlyDepositParams.drep_activity`), `rational.rs`, `scripts.rs`, `value.rs`, `witness.rs`, `fingerprint.rs` (B3: `CONWAY_DEPOSIT_PARAMS_TAG` fold; B5: Conway-deposit fingerprint extended 2→3 fields for `drep_activity`; OQ5: `write_stake_credential` emits discriminant+hash and the gov-map writers use it; COMMITTEE-CRED-FIDELITY: `write_committee_vote_list` emits the committee-vote credential discriminant and the committee map writer uses `write_stake_credential`; DREP-VOTE-FIDELITY: `write_committee_vote_list` renamed `write_credential_vote_list` (serves committee + DRep) and `drep_votes` is routed through it), `error.rs` (B3: `ValidationEnvironmentError`, `UnsupportedStateDependentDepositAccounting`, `EraInvalidCertificateError` + new `LedgerError` variants; B5: `ValidationEnvironmentError::{MissingDRepActivityParam, DRepActivityOverflow}`). **NEW B5:** `gov_cert.rs` (BLUE — native `apply_conway_gov_cert` total over the 18 Conway tags + removed tags 5/6, mutating only governance-owned `ConwayGovState` fields; OQ5: consumes the discriminated `StakeCredential` directly, no `.0` projection). `governance.rs` (OQ5: `evaluate_ratification` / `check_ratification` re-typed to `BTreeMap<StakeCredential, …>`, using `cred.hash()` as a read-only adapter against the `Hash28`-keyed stake snapshot; COMMITTEE-CRED-FIDELITY: the committee path now resolves by full-credential equality — `committee_members` / `committee_hot_keys` are `StakeCredential`-keyed and the committee-vote scan no longer projects through `cred.hash()`; DREP-VOTE-FIDELITY: the DRep tally's `lookup_stake` resolves by exact credential variant with no key/script OR-fallback; ENACTMENT-COMMITTEE-FIDELITY: `EnactmentEffects.committee_changes` re-typed to the discriminated `StakeCredential` — a dormant field, `enact_proposals`'s `UpdateCommittee` arm is still a no-op). **B1:** `consensus_view.rs` (BLUE), `block_validity/{mod.rs, verdict.rs, encoding.rs, transition.rs, header_input.rs}` (BLUE), `consensus_input_extract.rs` (**RED**). **B2:** `tx_validity/{mod.rs, required_signers.rs, witness.rs, verdict.rs, encoding.rs, phase1.rs, transition.rs}` (BLUE), `mempool/{mod.rs, admit.rs (BLUE), policy.rs (GREEN)}`. **NEW B3:** `cert_classify.rs` (BLUE — closed `classify`; B4 projection updated to the enriched `ConwayCert`, byte-identical dispositions). |

> **GREEN sub-classification (`ade_ledger::mempool::policy`).** Physically inside the BLUE `ade_ledger` crate but **GREEN (Tier-5)** per its own `// Core Contract:` header. It is a pure projection over the already-admitted tx ids; it never calls `tx_validity`, never touches the accumulating state, and cannot change an admit verdict (DC-MEM-02). Tier-5-below-Tier-1 is asserted by the test `policy_does_not_change_validity`. **Enforcement note:** the BLUE/GREEN partition inside `ade_ledger::mempool` is *by module*, not mechanical — no CI script forbids `policy.rs` from importing `tx_validity`. Currently a review-and-test invariant. Surfaced as a gap.

> **RED sub-classification (`ade_ledger::consensus_input_extract`).** Physically inside the BLUE `ade_ledger` crate but **RED** per its own module doc-comment and the PHASE4-B1 TCB Color Map: it tail-scans an external UTxO-HD dump format for the five `PraosState` nonces (`Nonce`, `PraosNonces`, `NonceScanError`). It is reached only by the RED snapshot loader and the GREEN corpus harness, never by `block_validity` or `tx_validity` — but this is a review-and-doc invariant, not a mechanical one. Surfaced as a gap.

> **B3 closed-deposit-classification + deposit-param authority — partially grep-gated.** DC-TXV-07 (deposit-param authority) cites the **new** `ci/ci_check_deposit_param_authority.sh` as its `ci_script`, which scans every BLUE crate for deposit literals and testkit-`ConwayGovParams` reads (script passes). DC-TXV-06 (cert-deposit classification closure) is now **grep-gated by the dedicated `ci/ci_check_conway_cert_classification_closed.sh` (PHASE4-B3F)** in addition to the compiler-exhaustive `match` in `cert_classify::classify` and its named `cargo test` coverage tests. The gate fails on: a `#[non_exhaustive]` or open-tail `Other`/`Unknown` variant on `ConwayCert` / `CertDisposition` / `DepositEffect` / `CoinSource` in `ade_types::conway::cert.rs`; absence of `CodecError::UnknownCertTag` or any catch-all (`_ =>`) arm that constructs a `ConwayCert` in `ade_codec::conway::cert.rs`; or any `_ =>` wildcard in `cert_classify::classify`. **Residual gap (narrowed):** the no-owned-`String` / no-`Box<dyn>` shape of the `ade_types::conway::cert` types is still outside the `TARGETS` array of `ci_check_consensus_closed_enums.sh` — extending that array to `crates/ade_types/src/conway/` would fold the remaining string/`Box<dyn>` properties into a grep gate too. The closure properties the B3F gate already covers (non-exhaustive, open-tail, catch-all, wildcard) are no longer a gap. Surfaced as a (narrowed) gap.

> **B2 closed-enum properties — grep-gated; one residual fail-open gate still owed.** DC-TXV-01..05 and DC-MEM-01/02 cite `ci/ci_check_consensus_closed_enums.sh` (`TARGETS` + `STRING_SCOPE` extended in B2 to include `tx_validity/` and `mempool/`). The one still-unshipped check is `ci/ci_check_no_fail_open_in_validation.sh` — a grep gate against the `if len == K { check } else { skip }` shape across the whole ledger validation path (owed from B1). The fail-closed witness-coverage *property* itself is structurally discharged + test-enforced; only its dedicated grep gate is outstanding.

> **Conway value-conservation follow-up — CLOSED in PHASE4-B3.** The B2-S4 pure-spend fail-open and the B2 deposit/refund/withdrawal deferral are now closed: `check_conway_coin_conservation` enforces the full preservation-of-value equation `consumed = inputs + withdrawals + refunded_deposits == produced = outputs + fee + donation + new_deposits` with the §9.1 reject precedence, the cert/withdrawal early-out removed. Deposits/refunds are classified by the closed `cert_classify::classify` (DC-TXV-06) and sourced from canonical deposit-param authority (DC-TXV-07). This discharges the `project_value_conservation_deposits_deferred` follow-up. **Remaining adjacent gap:** the Conway **block-body loop** in `rules.rs` still reuses the Shelley-era applicator and does not re-run the per-tx vkey-witness closure that `tx_validity` provides (the B2-carried `project_conway_body_witness_gap`); wiring `tx_phase_one` / `verify_required_witnesses` into the Conway block-body path is the natural remaining closure and a post-B3 item.

> **Conway cert-state accumulation — CLOSED in PHASE4-B4 (DC-LEDGER-08, `status=enforced`).** The B3-carried fail-open in the cert path is closed: `rules::process_block_certificates` no longer discards the era or swallows decode/apply errors as "non-fatal during replay" — it era-dispatches through `accumulate_tx_certs` (Conway → owner-complete `decode_conway_certs` + `apply_conway_cert`; Shelley..Babbage → `decode_certificates` + `apply_cert`), and any decode or apply error halts the block transition as a structured `LedgerError`. The Conway apply is native owner-tagged: delegation/pool effects mutate B4-owned `CertState`, governance effects are owner-tagged to `ConwayGovState` and routed out of mutation scope (observed, not swallowed, not applied — applied by PHASE4-B5), composite tags 10/12/13 do both, and removed tags 5/6 reject as `EraInvalidCertificate`. Closure is mechanical via the compiler-exhaustive `match` (total over all 18 tags + 5/6) plus the named B4 tests (`conway_cert_action_total`, `apply_outcome_agrees_with_action`, `each_tag_retains_owner_payloads`, `drep_grammar_total`, the four `conway_*_is_fail_closed` dispatch tests, `conway_governance_cert_routed_out_of_scope`, and the B4-S5 corpus `positive_synthetic_cert_state_accumulates` / `cert_state_replay_byte_identical` / `adversarial_no_false_accept`); the `ci_script` is the existing full-BLUE `ci_check_forbidden_patterns.sh` (no new gate). **Remaining open obligation (environment-blocked, not a code gap):** the real epoch-576 cert-state-vs-cardano-node oracle is blocked by an absent epoch-576 UMap snapshot (same constraint class as DC-TXV-06 / DC-TXV-03); B4 closes mechanically with owner-complete decode + total owner-tagged apply + era-dispatched fail-closed accumulation + synthetic positive/replay/adversarial corpus. **Adjacent gaps unchanged by B4:** the Conway block-body vkey-witness closure is still not wired into the `rules.rs` block-body loop, and the owner-tagged `ConwayGovState` effects are not yet applied (PHASE4-B5).

> **Conway governance-cert accumulation — CLOSED in PHASE4-B5 (DC-LEDGER-09, `status=enforced`).** The B4-carried obligation — the owner-tagged `ConwayGovState` effects observed but not applied — is now closed. The new BLUE `gov_cert::apply_conway_gov_cert` is a native, total dispatch over the owner-complete `ConwayCert` that applies vote-delegation (9/10/12/13 → `vote_delegations`), committee auth/resign (14/15 → `committee_hot_keys`), and DRep register/unregister/update (16/17/18 → `drep_expiry`, env-driven with `checked_add`) into `ConwayGovState`, mutating **only** governance-owned fields — it neither takes nor returns the B4-owned `CertState`, so it cannot mutate delegation/pool state and cannot double-apply the delegation/pool half of composite certs (10/12/13). `rules::accumulate_tx_certs` / `process_block_certificates` now thread `Option<ConwayGovState>`, apply the gov half (replacing B4's observe-and-drop — the "routed out of B4 mutation scope" disposition is retired), and `apply_block` carries `gov_state` forward instead of nulling it, so `ConwayGovState` is a deterministic function of (boundary-loaded base then replayed block-stream cert effects). DRep expiry is computed only from the explicit `GovCertEnv` (`current_epoch + drep_activity`); a missing `drep_activity` fails fast with `MissingDRepActivityParam` and an arithmetic overflow halts deterministically with `DRepActivityOverflow` (never a defaulted or silently-wrapped expiry). Closure is mechanical via the compiler-exhaustive `match` in `apply_conway_gov_cert` (total over all 18 tags + 5/6) **plus the dedicated `ci/ci_check_gov_cert_accumulation_closed.sh` grep gate** (no `_ =>` arm; observe-and-drop comment forbidden; `accumulate_tx_certs` must call `apply_conway_gov_cert`; `checked_add` required; `MissingDRepActivityParam` + `gov_cert_env()` present), plus the 17 named B5 tests (gov-apply totality, composite-half-once with `CertState` untouched, env fail-fast, checked-overflow fail-closed, determinism, block-path accumulation halt-on-error, and the B5-S4 synthetic-positive + byte-identical replay + adversarial corpus). **Remaining open obligation (environment-blocked, not a code gap):** the real epoch-576 governance-state (VState)-vs-cardano-node oracle is blocked by an absent epoch-576 UMap/ledger snapshot (same constraint class as DC-LEDGER-08 / DC-TXV-06 / DC-TXV-03). **Two declared follow-ups (not part of this accumulation closure):** (OQ-3) the GOVCERT committee-membership tx-validity gate is separable; (OQ-5) the credential key/script discriminant collapse — **CLOSED in OQ5-CREDENTIAL-FIDELITY (DC-LEDGER-10, `status=enforced`)**: `StakeCredential` is now a closed `{KeyHash, ScriptHash}` sum, both era decoders preserve the tag, `ConwayGovState`'s `vote_delegations` / `committee_hot_keys` / `drep_expiry` are re-keyed on the discriminated credential, and the canonical fingerprint serializes the discriminant (see the OQ5 closure note below). **Adjacent gap unchanged by B5:** the Conway block-body vkey-witness closure is still not wired into the `rules.rs` block-body loop.

> **Credential discriminant fidelity — CLOSED in OQ5-CREDENTIAL-FIDELITY (DC-LEDGER-10, `status=enforced`).** The B5-carried OQ-5 follow-up — `ConwayGovState` keying governance state on a tag-erased `Hash28` because the codec dropped the credential key/script discriminant — is now closed. Credential identity is faithful end-to-end: (1) `ade_types::shelley::cert::StakeCredential` is the closed 2-variant `enum { KeyHash(Hash28), ScriptHash(Hash28) }` (+ a `hash()` boundary-adapter accessor), making a discriminant-less credential structurally unrepresentable on the BLUE path; (2) both era decoders (`ade_codec::shelley::cert` / `ade_codec::conway::cert` `decode_stake_credential`) preserve the tag and reject an unknown discriminant deterministically; (3) `ConwayGovState`'s `vote_delegations` / `committee_hot_keys` / `drep_expiry` are re-keyed on the discriminated credential, matching cardano-node's `Credential`-keyed UMap/VState; (4) `fingerprint::write_stake_credential` serializes the discriminant before the hash, so two states differing only in a credential's key/script tag fingerprint differently (a deliberate cert-state + gov-state fingerprint migration, T-DET-01). `governance.rs` / `rules.rs` use `cred.hash()` ONLY as a read-only boundary adapter against the `Hash28`-keyed stake-distribution snapshot (a declared non-goal surface — see the registry `open_obligation`). Closure is mechanical via the closed enum + the discriminant-preserving decoders **plus the dedicated `ci/ci_check_credential_discriminant_closed.sh` grep gate** (StakeCredential is the closed enum / not the tuple struct; both decoders map to `KeyHash`/`ScriptHash` and carry no tag-discard form; no bare-hash `StakeCredential(<hash>)` coercion on the BLUE path), plus the 8 named OQ5 tests (decoder discriminant-preservation + unknown-tag-reject, fingerprint discriminant-distinguishing, and the credential-fidelity corpus: distinct-keys cert-state, distinct-keys gov-state, fingerprint distinguishing, byte-identical replay). **Remaining open obligation (environment-blocked, not a code gap):** real-chain agreement of the discriminated credential keys vs cardano-node's `Credential`-keyed UMap/VState is blocked by absent epoch-576/boundary snapshots (same constraint class as DC-LEDGER-08 / DC-LEDGER-09 / DC-TXV-06), reclassified per tier doctrine. **Declared non-goals (separate work, not this cluster):** the withdrawal / required-signer / address credential discriminant, the `Hash28`-keyed stake-distribution snapshot, and the Byron credential surface.

> **Committee credential fidelity — CLOSED in COMMITTEE-CRED-FIDELITY (DC-LEDGER-10 strengthened, `strengthened_in += COMMITTEE-CRED-FIDELITY`).** The OQ5 follow-up surface — the committee credential, the one gov-state member set OQ5 still resolved through `KeyHash` — is now discriminant-faithful. `GovActionState.committee_votes` (`ade_types::conway::governance`) carries `Vec<(StakeCredential, Vote)>` (the DRep / SPO vote lists stay `Hash28`-keyed by design); `ConwayGovState.committee` (`ade_ledger::state`) is `StakeCredential`-keyed; committee ratification (`governance::evaluate_ratification` / `check_ratification`) resolves a vote by **full-credential equality** with no `.hash()` on the committee path, so a hot/cold authorization cannot cross-resolve a key-hash vote onto a script-hash member; the canonical fingerprint emits the committee discriminant via the new `write_committee_vote_list` (committee votes) and `write_stake_credential` (committee map). The GREEN snapshot loader's `parse_committee_state` + `parse_committee_vote_map` preserve the committee tag. Closure is mechanical via the discriminated types + the **extended** `ci/ci_check_credential_discriminant_closed.sh` (no new file — committee map keys + committee-vote discriminant added to the existing gate) plus the 3 named tests (`committee_keyhash_scripthash_do_not_cross_resolve`, `committee_keyhash_scripthash_same_bytes_distinct`, `committee_discriminant_changes_fingerprint`). **Still one rule (DC-LEDGER-10), still `enforced`, no new module, no new crate edge, no net new type, no new CI script.** **Remaining open obligation (environment-blocked, not a code gap):** real-chain agreement of the discriminated committee keys vs cardano-node's `Credential`-keyed committee state is blocked by absent epoch-576/boundary snapshots (same constraint class as the rest of DC-LEDGER-10). **Declared non-goals unchanged:** the withdrawal / required-signer / address credential discriminant, the `Hash28`-keyed stake-distribution snapshot, the Byron credential surface, and the still-`KeyHash`-projected DRep/SPO vote lists.

> **DRep-vote credential fidelity — CLOSED in DREP-VOTE-FIDELITY (DC-LEDGER-10 strengthened, `strengthened_in += DREP-VOTE-FIDELITY`).** The COMMITTEE follow-up surface — the DRep-vote list, the gov-state vote surface still resolved through a bare `Hash28` — is now discriminant-faithful. `GovActionState.drep_votes` (`ade_types::conway::governance`) carries `Vec<(StakeCredential, Vote)>` (the `spo_votes` list stays `Hash28`-keyed by design); DRep ratification (`governance::evaluate_ratification` / `check_ratification`) resolves a DRep vote by **exact credential variant** via `lookup_stake` (`KeyHash → DRep::KeyHash`, `ScriptHash → DRep::ScriptHash`) with no key/script OR-fallback, so a key-hash DRep voter cannot tally a script-hash DRep's stake of equal 28 bytes; the canonical fingerprint emits the DRep-vote discriminant via the renamed shared `write_credential_vote_list` (committee + DRep). The GREEN snapshot loader's `parse_credential_vote_map` (rename of `parse_committee_vote_map`, serving committee + DRep, tag-preserving) preserves the DRep-vote tag. Closure is mechanical via the discriminated types + the **extended** `ci/ci_check_credential_discriminant_closed.sh` (no new file — DRep-vote discriminant added to the existing gate) plus the 2 named tests (`drep_keyhash_scripthash_do_not_cross_resolve`, `drep_vote_discriminant_changes_fingerprint`). **Still one rule (DC-LEDGER-10), still `enforced`, no new module, no new crate edge, no net new type, no new CI script.** **Remaining open obligation (environment-blocked, not a code gap):** real-chain agreement of the discriminated DRep keys vs cardano-node's `Credential`-keyed DRep distribution is blocked by absent epoch-576/boundary snapshots (same constraint class as the rest of DC-LEDGER-10). **Declared non-goals unchanged:** the withdrawal / required-signer / address credential discriminant, the `Hash28`-keyed stake-distribution snapshot, the Byron credential surface, and the still-`KeyHash`-projected SPO vote list.

> **Enactment-effects committee credential fidelity — CLOSED in ENACTMENT-COMMITTEE-FIDELITY (DC-LEDGER-10 strengthened, `strengthened_in += ENACTMENT-COMMITTEE-FIDELITY`).** The one remaining gov-enactment surface still typed on a bare `Hash28` — the committee-change effect produced by ratified-proposal enactment — is now discriminant-faithful by construction. `governance::EnactmentEffects.committee_changes` re-typed `Option<(Vec<Hash28>, Vec<(Hash28, u64)>)>` → `Option<(Vec<StakeCredential>, Vec<(StakeCredential, u64)>)>` (removed cold committee credentials; added cold credentials with expiry). This is a **preventive** migration: the field is **dormant** (`enact_proposals`'s `GovAction::UpdateCommittee` arm is still a no-op — committee changes are held as raw CBOR, full enactment deferred — so `committee_changes` is always `None` at HEAD), but when `UpdateCommittee` enactment is implemented it must write back into the discriminant-keyed `ConwayGovState.committee` map (made `StakeCredential`-keyed in COMMITTEE-CRED-FIDELITY), and a `Hash28`-typed effect would re-collapse a key-hash and a script-hash committee credential sharing 28 bytes on write-back. Typing the effect on the discriminated `StakeCredential` now makes that re-collapse structurally unrepresentable. Closure is mechanical via the discriminated type + the **extended** `ci/ci_check_credential_discriminant_closed.sh` (no new file — `EnactmentEffects.committee_changes` must stay `Option<(Vec<StakeCredential>, Vec<(StakeCredential, u64)>)>`, no reversion to `Hash28`) plus the 1 named test (`enactment_committee_changes_keyhash_scripthash_distinct`). **Still one rule (DC-LEDGER-10), still `enforced`, no new module, no new crate edge, no net new type, no new CI script.** **Remaining open obligation (not a code gap):** `UpdateCommittee` enactment itself (the dormant `enact_proposals` arm) is a separate future work item; this cluster only makes the effect type collapse-proof ahead of that work. **Declared non-goals unchanged:** the withdrawal / required-signer / address credential discriminant, the `Hash28`-keyed stake-distribution snapshot, the Byron credential surface, and the still-`Hash28`-keyed SPO vote list.

---

### `ade_network` *(BLUE submodules)*

> Per-submodule BLUE scoping per `.idd-config.json` `_core_paths_doc`. The crate is split below into its BLUE half (this entry) and its RED half (further down). Two submodules — `lib.rs` (the barrel) and `mux/mod.rs` (a re-export shim) — are GREEN and carry no authority. Slice provenance: cluster PHASE4-N-A, slices S-A1 through S-A10.

| Attribute | Value |
|-----------|-------|
| **Purpose** | Owns the Cardano Ouroboros mini-protocol authority — the closed wire grammar (CBOR codecs) and pure state machines for all 11 N2N + N2C mini-protocols, plus the BLUE Ouroboros mux frame primitive. Encoders/decoders and transitions only; bytes-on-the-wire and effect emission belong to the RED half of the crate. |
| **Creates** | **Wire grammar (`codec/`):** `HandshakeMessage`, `N2cHandshakeMessage`, `ChainSyncMessage`, `BlockFetchMessage`, `TxSubmission2Message`, `KeepAliveMessage`, `PeerSharingMessage`, `LocalChainSyncMessage`, `LocalTxSubmissionMessage`, `LocalStateQueryMessage`, `LocalTxMonitorMessage`; closed payload types `VersionTable`/`VersionParams`/`N2cVersionTable`/`N2cVersionParams`, `RefuseReason`/`N2cRefuseReason`, `Point`, `Tip`, `Range`, `TxIdAndSize`, `TxAcceptance`, `TxRejection`, `KeepAliveCookie`, `PeerAddress`, `AcquireFailure`, `QueryPayload`, `ResultPayload`, `MempoolMeasures`/`MempoolSizeAndCapacity`/`MeasureSizeAndCapacity`/`MeasureName`; structured error taxonomy `CodecError` + `ProtocolKind`; typed per-protocol version newtypes. **State machines:** one closed `*State` / `*Agency` / `*Output` / `*Error` quad per protocol plus event/value carriers. **Mux primitives (`mux/frame.rs`):** `MuxFrame`, `MuxHeader`, `MuxMode`, `MiniProtocolId`, `MuxError`. |
| **Interprets** | The on-wire CBOR of every Ouroboros mini-protocol message (closed grammar) and the 8-byte Ouroboros mux frame header. Block, header, and tx-body bytes inside RollForward / Block / Tx are *not* interpreted at this layer (opaque pass-through per DC-PROTO-06); LSQ Query/Result payloads are also opaque. |
| **MUST NOT** | (1) Contain `async fn`, `.await`, `tokio`, `async_std`, `futures::`, async-runtime spawn, or async timers in any file under the 9 BLUE paths (`ci_check_no_async_in_blue.sh`; DC-CORE-01). (2) Decode block / header / tx-body bytes — opaque `Vec<u8>` (DC-PROTO-06). (3) Construct a `PreservedCbor` outside `ade_codec`'s named chokepoints. (4) Add `#[non_exhaustive]` to any wire-message enum, introduce a generic `Codec<P>` trait, or use `dyn MessageHandler` (DC-PROTO-03/04). (5) Define a generic `Agency<P>` wrapper. (6) Read a selected version from ambient session state — explicit input to every transition (DC-PROTO-06). (7) Redefine `TxId` / `SlotNo` / `Hash32` / `CardanoEra` — use `ade_types`. (8) Depend on `pallas-network`. (9) Hold `String` in any error variant. (10) Use any other BLUE forbidden pattern. |
| **Inbound deps** | `ade_core_interop` (live consensus session binary, RED → RED). At the library level no BLUE crate imports `ade_network`. The 7 capture binaries import the BLUE codecs internally; the 19 integration tests exercise the BLUE surface end-to-end. |
| **Outbound deps** | `ade_types`, `ade_codec` (for `CodecError::MalformedCbor`). No external deps in the BLUE submodules; the `tokio` line in `Cargo.toml` is confined to `mux/transport.rs` and the capture binaries (RED). |
| **Entry points** | **Codec** (11 protocols, each `encode_<protocol>_message` / `decode_<protocol>_message`); `ade_network::codec::primitives::*` (8 import sites); `ade_network::codec::version::*`; `ade_network::codec::{CodecError, ProtocolKind}`. **State machines:** `ade_network::handshake::{n2n_transition, n2c_transition, select_n2n_version, select_n2c_version, N2N_SUPPORTED, N2C_SUPPORTED}`; per-protocol `*_transition` functions. **Mux:** `ade_network::mux::frame::{encode_frame, decode_frame, MuxFrame, MuxHeader, MuxMode, MiniProtocolId, MuxError, HEADER_LEN, MAX_PAYLOAD}` (8 import sites). |
| **Key modules** | `codec/` (11 message-codec modules + `primitives.rs` + `version.rs` + `error.rs`); `handshake/`, `chain_sync/`, `block_fetch/`, `tx_submission/`, `keep_alive/`, `peer_sharing/`; `n2c/` (4 local-* submodules); `mux/frame.rs`. |

---

### `ade_plutus`

| Attribute | Value |
|-----------|-------|
| **Purpose** | Quarantine boundary between the Ade-canonical ledger and the ported UPLC evaluator from `aiken-lang/aiken` (pinned to tag `v1.1.21`, commit `42babe5d`). Exposes Ade-canonical types only; aiken- and pallas-originated types are strictly internal. |
| **Creates** | `PlutusScript`, `PlutusLanguage`, `EvalOutput`, `PlutusError`, `CostModels`, `DecoderMode`, `PerScriptResult`, `TxEvalResult`. |
| **Interprets** | UPLC scripts (Plutus V1/V2/V3) and `CostModels` CBOR. Phase-two transaction evaluation. `PlutusScript::from_cbor` is a named ingress chokepoint, allowlisted in `ci_check_ingress_chokepoints.sh` Check 3. |
| **MUST NOT** | (1) Re-export any `pallas_*` or `aiken_uplc::` type (`ci_check_pallas_quarantine.sh`). (2) Allow another BLUE crate to bypass the canonical entry. (3) Activate PV11 builtins. (4) Use any BLUE-forbidden pattern (shared header). (5) Construct `PreservedCbor` outside `ade_codec`. |
| **Inbound deps** | `ade_ledger`, `ade_testkit`. |
| **Outbound deps** | `ade_types`, `ade_crypto`, `ade_codec`, `aiken_uplc` (git, tag `v1.1.21`), `pallas-primitives` (internal-only). |
| **Entry points** | `ade_plutus::eval_tx_phase_two`, `ade_plutus::tx_eval::{SlotConfig, MAINNET_SLOT_CONFIG}`, `ade_plutus::evaluator::{programs_alpha_equivalent, EvalOutput, PlutusLanguage, PlutusScript}`, `ade_plutus::cost_model::{CostModels, DecoderMode, decode_cost_models}`. **B2:** the `tx_validity` phase-2 dispatch reaches `ade_plutus` through `ade_ledger::plutus_eval::try_evaluate_tx` (the existing wrapper), not directly. |
| **Key modules** | `evaluator.rs`, `cost_model.rs`, `script_context.rs`, `script_verdict.rs`, `tx_eval.rs`. |

---

### `ade_types`

> **Status change in PHASE4-B3.** `ade_types::conway::cert` gained the closed `ConwayCert` enum (the Conway-complete certificate domain type the `ade_codec` decoder produces and `cert_classify` consumes) plus the closed deposit-effect sum types `CertDisposition`, `DepositEffect`, and `CoinSource`. `ade_types::tx` gained `RewardAccount([u8; 29])` (the withdrawals-map key produced by `ade_codec::conway::withdrawals`). The crate remains produce-only.
>
> **Status change in PHASE4-B4.** `ConwayCert` (in `conway/cert.rs`) was **enriched in place** to be owner-complete — each variant now carries the per-variant owner payloads the cert-state accumulator needs (stake/DRep/committee credentials, pool id, DRep delegation target). Tag 3 is now `PoolRegistration(PoolRegistrationCert)` (the full shared pool-registration cert, not a bare deposit marker) and tag 4 carries `pool_id` + `epoch`. `PoolRegistrationCert` (in `shelley/cert.rs`) gained an `owners: Vec<Hash28>` field (`pool_owners`, retained for the `PoolParams.owners` pledge-owner set; relays and pool metadata remain unstored). No new public type was added — the enrichment is additive on existing closed enums/structs, >
> **Status change in OQ5-CREDENTIAL-FIDELITY.** `ade_types::shelley::cert::StakeCredential` changed shape from the tag-erasing tuple struct `StakeCredential(pub Hash28)` to the closed 2-variant `enum { KeyHash(Hash28), ScriptHash(Hash28) }`, with a `hash(&self) -> &Hash28` accessor (the discriminant-erased projection, documented as ONLY for boundary adapters that genuinely key on bare bytes — never to re-key authoritative cert/gov state). `Ord` derives lexicographically over `(variant, hash)`, so a key-hash and a script-hash sharing the same 28 bytes are distinct map keys. **No net new type** — the same already-counted type, a new discriminated shape; the crate stays at 80 and remains produce-only and closed. This makes a discriminant-less credential structurally unrepresentable on the BLUE authority path (DC-LEDGER-10).
>
> **Status change in COMMITTEE-CRED-FIDELITY.** `ade_types::conway::governance::GovActionState.committee_votes` was re-typed `Vec<(Hash28, Vote)>` → `Vec<(StakeCredential, Vote)>` so a committee vote carries the discriminated credential (the `drep_votes` / `spo_votes` lists stayed `Hash28`-keyed at that point). **No net new type** — a field-type change on the existing closed `GovActionState`; the crate stays at 80 and remains produce-only.
>
> **Status change in DREP-VOTE-FIDELITY.** `ade_types::conway::governance::GovActionState.drep_votes` was re-typed `Vec<(Hash28, Vote)>` → `Vec<(StakeCredential, Vote)>` so a DRep vote carries the discriminated credential (the `spo_votes` list stays `Hash28`-keyed by design). **No net new type** — a field-type change on the existing closed `GovActionState`; the crate stays at 80 and remains produce-only.

| Attribute | Value |
|-----------|-------|
| **Purpose** | Owns the canonical Cardano domain vocabulary — primitives, era enum, address forms, era-tagged transaction bodies / outputs / certificates, governance types — used by every other workspace crate as the lingua franca. The schema, separated from the codec. |
| **Creates** | `CardanoEra`, `SlotNo`, `BlockNo`, `EpochNo`, `Hash28`, `Hash32`, `Coin`, `Lovelace`, `NetworkId`, `Nonce`, `TxIn`, `RewardAccount` *(NEW B3 — `[u8; 29]`)*, `PoolId`, `Address`, `ByronAddress`, `Credential`, `StakeCredential` *(OQ5 — now the closed `enum { KeyHash(Hash28), ScriptHash(Hash28) }` + `hash()` accessor, replacing the tuple struct; same type, new discriminated shape)*, `Certificate`, `PoolRegistrationCert` *(B4 — `owners: Vec<Hash28>`)*, `ConwayCert` *(B3 — closed Conway cert enum; B4 — enriched to owner-complete: per-variant credentials, pool id, `PoolRegistration(PoolRegistrationCert)` at tag 3, DRep targets)*, `CertDisposition` / `DepositEffect` / `CoinSource` *(NEW B3 — closed deposit-effect sum types)*, `MIRCert`, `MIRPot`, `DRep`, `GovAction`, `GovActionId`, `Anchor`, `OperationalCert`, `NativeScript`, `PlutusV1Script`, `Datum`, `DatumOption`, `MultiAsset`, `AssetName`, `CostModel`, `ExUnits`, plus per-era tx-body / tx-out / witness wrappers (incl. `conway::tx::ConwayTxBody`, consumed by the B2 required-signer enumeration) and Byron era block headers. |
| **Interprets** | None — produce-only. Domain types are constructed by `ade_codec` decoders or `ade_ledger` rules. |
| **MUST NOT** | (1) Construct or decode `PreservedCbor`. (2) Use any BLUE-forbidden pattern. (3) Depend on any workspace crate — root of the dependency DAG, no `[dependencies]` block. (4) Add open/extensible variants to closed era / certificate / governance enums without a versioned gate — **B3** extends this to the closed `ConwayCert` / `CertDisposition` / `DepositEffect` / `CoinSource` enums: no `#[non_exhaustive]`, no open-tail `Other`/`Unknown` variant; `ConwayCert::RemovedInConway { tag }` is an explicit closed marker, not an open tail. **B4 — `ConwayCert` / `PoolRegistrationCert`:** the owner-completion enrichment must stay additive within the closed surface — adding owner-payload fields to existing variants and the `owners` field to `PoolRegistrationCert` must not introduce an open tail, a `#[non_exhaustive]`, or a catch-all variant (DC-LEDGER-08; the closed `ConwayCert` shape is grep-gated by `ci_check_conway_cert_classification_closed.sh`). **OQ5 — `StakeCredential` (DC-LEDGER-10):** must not revert to the tag-erasing tuple struct `StakeCredential(Hash28)` — credential identity is a closed sum over `{KeyHash, ScriptHash}`, never a bare `Hash28`; the `hash()` accessor is a read-only discriminant-erased projection for boundary adapters and must not be used to construct authoritative credential keys (enforced by `ci_check_credential_discriminant_closed.sh`). |
| **Inbound deps** | `ade_codec`, `ade_crypto`, `ade_ledger`, `ade_plutus`, `ade_runtime`, `ade_testkit`, `ade_network`, `ade_core`, `ade_core_interop`. |
| **Outbound deps** | None. |
| **Entry points** | `ade_types::CardanoEra` (most-used name in the workspace), `ade_types::tx::{Coin, TxIn, RewardAccount}`, `ade_types::{Hash32, SlotNo}`, `ade_types::Hash28`, `ade_types::conway::tx::ConwayTxBody`, `ade_types::conway::cert::{ConwayCert, CertDisposition, DepositEffect, CoinSource}`. |
| **Key modules** | `primitives.rs`, `era.rs`, `tx.rs` (B3: `RewardAccount`), `address/`, `byron/`, `shelley/` (B4: `cert.rs` — `PoolRegistrationCert.owners`; OQ5: `cert.rs` — `StakeCredential` closed enum + `hash()`), `allegra/`, `mary/`, `alonzo/`, `babbage/`, `conway/` (B3: `cert.rs` carries the closed `ConwayCert` + deposit-effect sum types; B4: `ConwayCert` enriched to owner-complete). |

---

## GREEN Modules — Deterministic Glue

> Deterministic, non-authoritative. May depend on BLUE; must not affect authoritative outputs.

### `ade_testkit`

> **Status change in PHASE4-B2.** `ade_testkit` added a new GREEN harness sub-tree `tx_validity/` (PHASE4-B2-S3/S4): a corpus tx extractor (`extract`), a deterministic adversarial mutator set (`adversarial` — witness family A + synthetic family B), a synthetic valid-case / dependent-pair builder (`valid_synthetic`), and the GREEN driver (`mod`). **PHASE4-B3 did not add a harness sub-tree;** `ade_testkit` remains the legitimate (RED-snapshot-loader) write path that populates `LedgerState.conway_deposit_params` from parsed snapshot bytes — `ci_check_deposit_param_authority.sh` explicitly allowlists the testkit snapshot-loader as the only place that may construct `ConwayOnlyDepositParams` from parsed input rather than from canonical state. The crate remains GREEN. **OQ5-CREDENTIAL-FIDELITY (GREEN):** the snapshot loader's gov-map and DRep-registration parses (`harness/snapshot_loader.rs`) now build the discriminated `StakeCredential` (preserving the key/script tag) when populating `ConwayGovState.vote_delegations` / `committee_hot_keys` / `drep_expiry`, matching the BLUE re-key (DC-LEDGER-10); the `epoch_oracle_comparison` test was updated to the discriminated keys. Still GREEN — a fixture-materialization helper, not an authoritative path. **COMMITTEE-CRED-FIDELITY (GREEN):** the snapshot loader's `parse_committee_state` + `parse_committee_vote_map` (`harness/snapshot_loader.rs`) preserve the committee credential tag when populating `ConwayGovState.committee` and the committee-vote lists, matching the BLUE re-key (DC-LEDGER-10). Still GREEN. **DREP-VOTE-FIDELITY (GREEN):** `parse_committee_vote_map` was renamed `parse_credential_vote_map` (it now serves both the committee-vote and DRep-vote maps, tag-preserving), so the GREEN snapshot loader materializes the discriminated `StakeCredential` for `GovActionState.drep_votes` as well, matching the BLUE re-key (DC-LEDGER-10). Still GREEN — a fixture-materialization helper, not an authoritative path.
>
> **Prior status (PHASE4-N-B / PHASE4-B1).** Depends on `ade_runtime` (used by `consensus::stream_replay`); B1 added the `validity/` block-validity harness.

| Attribute | Value |
|-----------|-------|
| **Purpose** | Test infrastructure: differential harness, oracle snapshot loader, regression corpus, genesis loader, era mapping, transcript replay, diff reporting; the N-B consensus harness; the B1 block-validity harness (`validity/`); and the B2 transaction-validity harness (`tx_validity/`). Not authoritative — produces comparisons of authoritative outputs against oracle/corpus data and serves the `replay_cmd` test suites. |
| **Creates** | **Harness (pre-N-B, selected):** `Era`, `OracleManifest`, `LoadedSnapshot`, `RegressionCorpus`, `ComparisonSurface`, `DifferentialReport`, `DiffReport`, `Divergence`, `LedgerDiffReport`, `ProtocolDiffReport`, `BlockFields`, `ExpectedVerdict`, `HarnessError`, `Transcript`, `StateHash`, plus stub applicators and violation reports. **Consensus (N-B):** `LedgerViewStub`, `EpochStakeFixture`, `PoolFixture`, `ReplayStep`, `ReplayResult`. **Validity (B1, `validity/`):** `ConwayValidityCorpus` + `CorpusLoadError`, the production-`PoolDistrView` builder, the GREEN replay harness, the M1–M6 mutators. **B2 (`tx_validity/`):** `ExtractedTx` + `ExtractError` (`extract`), `TxReplay` (`mod`), `ValidCase` + `DependentPair` (`valid_synthetic`), `WitnessMutation` + `WitnessMutationOutcome` + `SyntheticMutation` + `SyntheticCase` (`adversarial`). |
| **Interprets** | Oracle reference snapshots, the regression-corpus manifest, the N-B consensus corpus, the B1 validity corpus, and the on-wire Conway transactions inside the committed Conway-576 corpus blocks (B2). **B3:** the snapshot loader parses Conway deposit parameters (`drep_deposit`, `gov_action_deposit`) from snapshot bytes and constructs the `ConwayOnlyDepositParams` written into `LedgerState.conway_deposit_params` — the one allowlisted non-canonical-state deposit-param source under DC-TXV-07. |
| **MUST NOT** | (1) Affect authoritative outputs — testkit code is excluded from BLUE forbidden-pattern scans and may use `HashMap`/`serde_json`/`flate2`/`tar`, but its results must never feed back into BLUE/RED authoritative state. (2) Be linked from any BLUE crate. (3) Introduce nondeterminism that leaks into stored fixtures. (4) `consensus::ledger_view_stub` must use `BTreeMap` only. (5) The stream-replay driver must report identical event sequences across runs. (6) **B1 — `validity::replay`** must report identical verdict streams across runs and must not alter the BLUE verdict. (7) **B2 — `tx_validity`** must not alter the BLUE `tx_validity` verdict — the adversarial verdict-surface stream must replay byte-identically (`adversarial_replays_identically`); `no_mutation_is_ever_valid` is the CE-B2-4 false-accept guard. (8) **B3** — the snapshot-loader deposit-param construction must never be reached by the BLUE deposit-conservation path at runtime; it is a fixture-materialization helper only (`ci_check_deposit_param_authority.sh` allowlists it precisely so the BLUE crates carry no deposit literals). |
| **Inbound deps** | None at compile time. All consumption is through its own integration tests and dev-dep links from `ade_core`, `ade_runtime`, `ade_ledger`, `ade_core_interop`. |
| **Outbound deps** | `ade_types`, `ade_codec`, `ade_crypto`, `ade_core`, `ade_ledger`, `ade_plutus`, `ade_runtime`, `blake2`, `flate2`, `tar`, `serde`, `serde_json`, `toml`. |
| **Entry points** | `ade_testkit::harness::snapshot_loader::LoadedSnapshot`, the `extract_state_from_tarball` / `parse_*` family, `ade_testkit::harness::genesis_loader::load_genesis_utxo`; N-B: `ade_testkit::consensus::{ledger_view_stub::*, stream_replay::*, corpus::*}`; B1: `ade_testkit::validity::{corpus::ConwayValidityCorpus, ledger_view::*, replay::*, adversarial::*}`; B2: `ade_testkit::tx_validity::{extract_corpus_txs, extract_block_txs, ExtractedTx, replay_tx_validity, validate_tx_against_corpus, TxReplay, build_valid, build_dependent_pair, mutate_witness, build_synthetic, judge}`. |
| **Key modules** | **`harness/`** (pre-N-B; B3: the snapshot loader's deposit-param parse). **`consensus/`** (N-B). **`validity/`** (B1). **`tx_validity/`** (B2): `mod.rs`, `extract.rs`, `adversarial.rs`, `valid_synthetic.rs`. |

> **Classification note.** `ade_testkit` reads files from disk in test helpers and drives BLUE authorities from corpus inputs. I/O exists only to materialize oracle/corpus fixtures consumed by tests and to compose deterministic GREEN harnesses for replay equivalence. By IDD doctrine that is GREEN. The crate is "demotable to RED" if/when it grows orchestration code that mutates authoritative state.

---

## RED Modules — Imperative Shell

> I/O, network, storage, clocks, retries. May depend on BLUE/GREEN. Must not modify core state directly or construct semantic types unsafely.

### `ade_core_interop` *(PHASE4-N-B, S-B10)*

| Attribute | Value |
|-----------|-------|
| **Purpose** | Live cardano-node interop driver for the consensus orchestrator. Builds the orchestrator at genesis (`fresh_orchestrator(k, initial_nonce)`), connects to a pinned-Docker cardano-node 10.6.2 peer over N2N via `ade_network`, feeds arriving headers and rollbacks into `ade_runtime::consensus::chain_selector::process_stream_input`, and asserts best-chain agreement for a sustained window. The release-evidence test is `#[ignore]`-gated. |
| **Creates** | One library helper: `ade_core_interop::fresh_orchestrator(k, initial_nonce) -> OrchestratorState`. No new public types. |
| **Interprets** | Live N2N byte streams from a real cardano-node 10.6.2 peer; bytes flow in via `ade_network::mux::transport::MuxTransport` into BLUE codecs / state machines / consensus transitions without re-interpretation. |
| **MUST NOT** | (1) Contain authoritative state-transition logic. (2) Be depended on by any other workspace crate. (3) Run by default in CI — the closure-gate test is `#[ignore]`-gated. (4) Bypass `ade_codec`'s `PreservedCbor` chokepoints. (5) Read versions out of ambient context (DC-PROTO-06). (6) Vendor cardano-node mainnet genesis blobs into the crate. |
| **Inbound deps** | None. |
| **Outbound deps** | `ade_core`, `ade_runtime`, `ade_network`, `ade_testkit`, `ade_types`, `tokio`. |
| **Entry points** | `ade_core_interop::fresh_orchestrator(k, initial_nonce)`, binary `live_consensus_session`. Closure-gate test (`#[ignore]`). |
| **Key modules** | `src/lib.rs`, `src/bin/live_consensus_session.rs`. |

> **Gap surfaced.** `ade_core_interop` at HEAD is a *readiness probe*; the full live tip-agreement loop is the operator's manual task. CE-N-B-6 closes via manual evidence log, not via CI. Unchanged by B1/B2/B3.

---

### `ade_network` *(RED submodules + capture binaries)*

> The RED half of the `ade_network` crate: `mux::transport`, `session`, and the 7 `src/bin/capture_*` binaries. Provenance: cluster PHASE4-N-A.

| Attribute | Value |
|-----------|-------|
| **Purpose** | The only place inside `ade_network` where I/O happens — tokio-based TCP / Unix-socket scaffolding for the Ouroboros mux layer (`mux::transport`), the session-composition placeholder (`session`), and 7 capture binaries that perform real handshakes against pinned cardano-node 11.0.1 peers and write the recorded byte stream as canonical replay corpus. |
| **Creates** | `MuxTransport` (tokio TcpStream wrapper). `session/mod.rs` is an empty placeholder. The capture binaries produce no public Rust types. |
| **Interprets** | Live TCP / Unix-socket byte streams from cardano-node 11.0.1 peers; bytes flow in via `MuxTransport::read_raw` into the BLUE mux frame decoder and protocol codecs without re-interpretation. |
| **MUST NOT** | (1) Contain protocol logic. (2) Define or modify any closed wire-message enum, agency type, or state. (3) Read a selected protocol version from ambient state and pass it into a BLUE transition implicitly (DC-PROTO-06). (4) Replace the BLUE codec / state machine with a "fast path" (CN-WIRE-07). (5) Bypass `ade_codec`'s `PreservedCbor` chokepoints. (6) Be depended on by any BLUE submodule of `ade_network`. |
| **Inbound deps** | `ade_core_interop`. At the library level, no BLUE crate imports the RED submodules. |
| **Outbound deps** | `tokio`, `ade_types`, `ade_codec`, plus the BLUE `ade_network::codec::*` and `ade_network::mux::frame`. |
| **Entry points** | `ade_network::mux::transport::{MuxTransport, open_tcp}`. The capture binaries. `session::*` exposes no entry points at HEAD. |
| **Key modules** | `mux/transport.rs`, `session/mod.rs` (empty placeholder), `bin/capture_*`. |

> **Gap surfaced.** `ade_network::session::mod.rs` is still a placeholder. The session driver remains owed by a future orchestration cluster.

---

### `ade_node`

| Attribute | Value |
|-----------|-------|
| **Purpose** | Binary entry point for the node process. Currently a hello-world stub. Reserved as the assembly point for runtime + ledger + chain DB + network + consensus orchestrator + mempool. |
| **Creates** | None. |
| **Interprets** | None yet. |
| **MUST NOT** | (1) Bypass `ade_codec` to construct semantic types from bytes. (2) Modify `ade_ledger` or `ade_core::consensus` state in place — transitions go through `ade_ledger::rules::*`, `ade_ledger::block_validity::*`, `ade_ledger::tx_validity::*`, `ade_ledger::mempool::admit`, or `ade_core::consensus::*` BLUE transitions. (3) Take an inbound dep from any other crate. (4) Read versions out of ambient context and pass them into `ade_network` BLUE transitions implicitly (DC-PROTO-06). (5) Construct an `OrchestratorState` outside `ade_runtime::consensus::chain_selector` or `ade_core_interop::fresh_orchestrator`. |
| **Inbound deps** | None. |
| **Outbound deps** | None at present. |
| **Entry points** | `main()` in `crates/ade_node/src/main.rs`. |

> **Gap surfaced.** `ade_node` is still a hello-world stub; its MUST NOT list is forward-looking with nothing to enforce today.

---

### `ade_runtime`

> **Status change in PHASE4-N-B.** `ade_runtime` hosts storage + recovery (cluster N-D) and the consensus orchestrator (`consensus/`, cluster N-B). The crate remains RED; `chain_selector.rs` / `candidate_fragment.rs` are GREEN by file-level classification, `genesis_parser.rs` is RED. PHASE4-B1, PHASE4-B2, and PHASE4-B3 did not change `ade_runtime`.

| Attribute | Value |
|-----------|-------|
| **Purpose** | The imperative shell. Hosts (a) the `ChainDb` / `SnapshotStore` storage abstractions and their in-memory / redb-backed impls, the crash-safety harness, and the snapshot + forward-replay recovery primitive (N-D); (b) the GREEN chain-selector orchestrator + GREEN `CandidateFragment` materializer (N-B); (c) the RED genesis-text parser that materializes the BLUE `EraSchedule` and computes the `BootstrapAnchorHash` (N-B). |
| **Creates** | **ChainDb (`chaindb/`):** `ChainDb` trait, `SnapshotStore` trait, `KillStrategy<D>` trait, `ChainTip`, `StoredBlock`, `ChainDbError`, `InMemoryChainDb`, `PersistentChainDb`, `PersistentChainDbOptions`, `SyncCadence`, `NoKill`, `BlockIter<'a>`. **Recovery (`recovery.rs`):** `Recoverable` trait, `StartingState`, `RecoveryReport<R>`, `RecoveryError<E>`. **Genesis (`consensus/genesis_parser.rs` — RED):** `NetworkMagic`, `GenesisBlob`, `GenesisBundle<'a>`, `GenesisParseError`. **Orchestrator (`consensus/chain_selector.rs` — GREEN):** `StreamInput`, `OrchestratorError`, `OrchestratorState`, `RollbackSnapshot`, `DEFAULT_SNAPSHOT_LIMIT`. **Materializer (`consensus/candidate_fragment.rs` — GREEN):** `build_candidate_fragment`. |
| **Interprets** | Block bytes for content-addressed storage. Snapshot bytes are byte-opaque to chaindb. Genesis JSON for the four blobs → typed `EraSummary` + `system_start_unix_ms` + immutable `EraSchedule`. N-A `StreamInput` events → canonical BLUE input shape threaded through BLUE transitions. |
| **MUST NOT** | (1) Depend on `ade_ledger`. (2) Leak `redb` through the `chaindb::*` public surface. (3) Have a second public chaindb path. (4) Couple snapshots to a specific state type. (5) Apply automatic snapshot pruning. (6) Allow partial-recovery success. (7) Add async recovery surface. (8) Treat "not found" as an error path or silently retry on real errors. (9) **Consensus sub-tree:** the GREEN `chain_selector` must not affect authoritative outputs; no `HashMap`/`HashSet`, no async, no wall-clock, no global mutable state. (10) **Genesis parser:** must not be reached by BLUE; structured errors only. (11) **`candidate_fragment` materializer:** must not own state — pure constructor. |
| **Inbound deps** | `ade_testkit` (non-dev dep, used by `stream_replay`), `ade_core_interop`. The binary `chaindb_kill_target` lives at `src/bin/`. |
| **Outbound deps** | `ade_types`, `ade_core`, `ade_crypto` (`blake2b_256`), `ade_codec`, `redb`, `serde_json`. Dev-deps: `tempfile`, `ade_testkit`, `cardano-crypto` (tests only). |
| **Entry points** | **Chaindb / recovery:** `ade_runtime::chaindb::{ChainDb, SnapshotStore, PersistentChainDb, PersistentChainDbOptions, SyncCadence, InMemoryChainDb, run_contract_tests, run_snapshot_contract_tests, run_crash_safety_tests}`, `ade_runtime::recovery::{recover, Recoverable}`. **Consensus:** `ade_runtime::consensus::chain_selector::{process_stream_input, OrchestratorError, OrchestratorState, RollbackSnapshot, StreamInput, DEFAULT_SNAPSHOT_LIMIT}`, `ade_runtime::consensus::candidate_fragment::build_candidate_fragment`, `ade_runtime::consensus::genesis_parser::{compute_anchor_hash, parse_genesis, GenesisBlob, GenesisBundle, GenesisParseError, NetworkMagic}`. |
| **Key modules** | **`chaindb/`**: `mod.rs`, `contract.rs`, `snapshot_contract.rs`, `crash_safety.rs`, `in_memory.rs`, `persistent.rs`, `types.rs`, `error.rs`. **`recovery.rs`**. **`consensus/`**: `mod.rs`, `genesis_parser.rs` (RED), `candidate_fragment.rs` (GREEN), `chain_selector.rs` (GREEN). **`bin/chaindb_kill_target.rs`**. |
| **Mechanical enforcement** | Three dedicated CI scripts back the chaindb / recovery / crash-safety surface: `ci/ci_check_chaindb_contract.sh`, `ci/ci_check_recovery_contract.sh`, `ci/ci_check_chaindb_crash_safety.sh`. The consensus sub-tree has no dedicated CI script. |

> **Gap surfaced.** No dedicated CI script enforces the GREEN/RED partition *inside* `ade_runtime::consensus`. At HEAD the partition is "by file" and enforced only by review + header comments. Candidate name (forward): `ci/ci_check_consensus_runtime_partition.sh`.

---

## Cross-Module Rules (project-wide)

### Dependency direction

`ade_core_interop` → `{ade_core, ade_runtime, ade_network, ade_testkit, ade_types, tokio}` is legal (RED leaf binary).
`ade_runtime` → `{ade_core, ade_crypto, ade_codec, ade_types, redb, serde_json}` is legal (RED + GREEN glue).
`ade_testkit` → `{ade_core, ade_ledger, ade_plutus, ade_runtime, ade_crypto, ade_codec, ade_types}` is legal (GREEN).
`ade_network` (BLUE submodules) → `{ade_codec, ade_types}` is legal (BLUE among BLUEs).
`ade_network` (RED submodules + capture bins) → `{tokio, ade_codec, ade_types, ade_network::codec::*, ade_network::mux::frame}` is legal.
**`ade_ledger` → `{ade_core, ade_plutus, ade_crypto, ade_codec, ade_types, minicbor}` is legal (BLUE among BLUEs).** The `ade_ledger → ade_core` edge (PHASE4-B1) is **acyclic**. **PHASE4-B2, PHASE4-B3, PHASE4-B4, and PHASE4-B5 added no new crate edge** — B3's `cert_classify` consumes the `ade_types` closed cert types and `check_conway_coin_conservation` reuses the existing `ade_ledger → ade_codec` edge for `conway::cert` + `conway::withdrawals`; B4's `accumulate_tx_certs` reuses the same `ade_ledger → ade_codec` edge (`conway::cert` owner-complete + `shelley::cert`) and `apply_conway_cert` consumes the enriched `ade_types::conway::cert::ConwayCert`; B5's `gov_cert::apply_conway_gov_cert` consumes the same enriched `ade_types::conway::cert::ConwayCert` and the local `ade_ledger::state::ConwayGovState` — no new edge.
`ade_core` → `{ade_types, ade_crypto, minicbor}` is legal (BLUE among BLUEs).
`ade_plutus` → `{ade_crypto, ade_codec, ade_types}` is legal.
`ade_crypto` → `{ade_types}` is legal.
`ade_codec` → `{ade_types}` is legal.
`ade_types` → `{}`.

**Forbidden directions.** Any BLUE crate (or BLUE `ade_network` submodule) depending on `ade_runtime`, `ade_node`, `ade_core_interop`, or the RED half of `ade_network` is a CI failure (`ci_check_dependency_boundary.sh` + `ci_check_no_async_in_blue.sh`). Any non-`ade_plutus` crate referring to `pallas_*` is a CI failure (`ci_check_pallas_quarantine.sh`). Any reference to `ChainDb` / `chain_db` inside `crates/ade_core/src/consensus/` is a CI failure (`ci_check_no_chaindb_in_consensus_blue.sh`). **B1/B2 note:** the BLUE `tx_validity`/`mempool::admit` and GREEN `mempool::policy` all live under the already-BLUE `ade_ledger` crate prefix; the GREEN/RED/BLUE *sub-crate* partition inside `ade_ledger::mempool` and `ade_ledger::consensus_input_extract` is review-and-test-enforced, not mechanical (see gap notes). **B3 note:** any BLUE-crate deposit/refund literal next to a deposit field, or any BLUE read of a testkit `ConwayGovParams`, is a CI failure (`ci_check_deposit_param_authority.sh`, DC-TXV-07) — the sole allowlisted non-canonical deposit-param source is the RED snapshot loader in `ade_testkit`.

### Naming convention

All crates are prefixed `ade_`. TCB color is not encoded in the crate name. The authoritative classifier is `.idd-config.json` `core_paths` plus the cluster doc TCB Color Maps for sub-crate scopes; CI scripts hard-code their BLUE list. The seven full-BLUE-scoped scripts (`ci_check_module_headers.sh`, `ci_check_no_semantic_cfg.sh`, `ci_check_no_signing_in_blue.sh`, `ci_check_hash_uses_wire_bytes.sh`, `ci_check_ingress_chokepoints.sh`, `ci_check_dependency_boundary.sh`, `ci_check_no_async_in_blue.sh`) use the full BLUE set: the 6 BLUE crates plus the 9 BLUE `ade_network` submodule paths. The B3 `ci_check_deposit_param_authority.sh` scopes the 6 BLUE crates (`BLUE_CRATES=(ade_codec ade_types ade_crypto ade_core ade_ledger ade_plutus)`). Four narrow scripts target `crates/ade_core/src/consensus/`. **No script narrowly scopes `crates/ade_ledger/src/tx_validity/`, `crates/ade_ledger/src/mempool/`, `crates/ade_types/src/conway/`, or `crates/ade_ledger/src/delegation.rs`** — the B2/B3/B4 closed-enum / closed-grammar / owner-tagged-action properties on those paths rely on the full-BLUE scans plus the compiler-exhaustive `match` and named tests. (`ci_check_conway_cert_classification_closed.sh` does scope `ade_types::conway::cert` + `ade_codec::conway::cert` + `ade_ledger::cert_classify` for the cert-classification closure; the B5 `ci_check_gov_cert_accumulation_closed.sh` does scope `crates/ade_ledger/src/{gov_cert.rs,rules.rs,error.rs,state.rs}` for the gov-cert accumulation closure.)

### CI enforcement (28 scripts under `ci/`)

| Script | Enforces | Scope |
|---|---|---|
| `ci_check_cbor_round_trip.sh` | T-ENC-03, DC-CBOR-01, DC-CBOR-02 | golden corpus |
| `ci_check_ce_n_a_5_proof.sh` *(PHASE4-N-A, S-A10)* | CE-N-A-5 5-condition evidence | `ade_network` (RED + real-capture corpus) |
| `ci_check_chaindb_contract.sh` | DC-STORE-02, DC-STORE-03, CN-STORE-04, CN-STORE-05 | `ade_runtime --lib chaindb::` (RED) |
| `ci_check_chaindb_crash_safety.sh` | T-REC-01 (crash variant), DC-STORE-01, CN-STORE-03; CE-N-D-1 gate | `ade_runtime --test stress_kill_harness` (RED) |
| `ci_check_consensus_closed_enums.sh` *(PHASE4-N-B, S-B2; extended B1, B2)* | DC-CONS-04, DC-CONS-10, T-DET-01, DC-VAL-02/04/05/06, DC-TXV-01/02/04/05, DC-MEM-01/02 | `consensus/` + `block_validity/` + `tx_validity/` + `mempool/` |
| `ci_check_constitution_coverage.sh` | invariant-registry ↔ code/test coverage | repo-wide |
| `ci_check_conway_cert_classification_closed.sh` *(PHASE4-B3F)* | DC-TXV-06 — Conway cert-classification closure (no `#[non_exhaustive]`/open-tail on the cert types, no catch-all accept arm in the decoder, no `_ =>` in `classify`) | `ade_types::conway::cert` + `ade_codec::conway::cert` + `ade_ledger::cert_classify` |
| `ci_check_crypto_vectors.sh` | crypto KAT regression | `ade_crypto` |
| `ci_check_dependency_boundary.sh` | T-BOUND-02 — BLUE ⇎ RED separation | full BLUE (6 crates + 9 `ade_network` paths) |
| `ci_check_deposit_param_authority.sh` *(PHASE4-B3)* | DC-TXV-07 — canonical deposit-param authority | 6 BLUE crates (deposit literals + testkit `ConwayGovParams` reads) |
| `ci_check_differential_divergence.sh` | DC-DIFF-* | replay outputs |
| `ci_check_forbidden_patterns.sh` | T-CORE-02 — no `HashMap`/floats/clocks/etc. + `unsafe` allowlist | full BLUE |
| `ci_check_gov_cert_accumulation_closed.sh` *(PHASE4-B5)* | DC-LEDGER-09 — Conway gov-cert accumulation closure (no `_ =>` in `apply_conway_gov_cert`; B4 observe-and-drop comment forbidden; `accumulate_tx_certs` must call `apply_conway_gov_cert`; `checked_add` DRep expiry; `MissingDRepActivityParam` + `gov_cert_env()` present) | `ade_ledger::{gov_cert,rules,error,state}` |
| `ci_check_hash_uses_wire_bytes.sh` | DC-CBOR-02, T-ENC-01 | full BLUE |
| `ci_check_hfc_translation.sh` | DC-EPOCH-02 (CE-73-semantic) | `ade_ledger::hfc` |
| `ci_check_ingress_chokepoints.sh` | DC-INGRESS-01, T-INGRESS-01 | full BLUE |
| `ci_check_ledger_determinism.sh` | DC-LEDGER-01 (CE-74) | `ade_ledger` |
| `ci_check_module_headers.sh` | CE-04 contract banner | full BLUE |
| `ci_check_no_async_in_blue.sh` *(PHASE4-N-A, S-A1)* | DC-CORE-01 | full BLUE |
| `ci_check_no_chaindb_in_consensus_blue.sh` *(PHASE4-N-B, S-B1)* | DC-CORE-01 + DC-CONS-07 | `crates/ade_core/src/consensus/` |
| `ci_check_no_density_in_fork_choice.sh` *(PHASE4-N-B, S-B8)* | DC-CONS-03 | `fork_choice.rs` + `candidate.rs` |
| `ci_check_no_float_in_consensus.sh` *(PHASE4-N-B, S-B1)* | T-CORE-02 + DC-CONS-07/08/09 | `crates/ade_core/src/consensus/` |
| `ci_check_no_secrets.sh` | no credentials/IPs/keys in tree | repo-wide |
| `ci_check_no_semantic_cfg.sh` | T-BUILD-01 — no `#[cfg(feature)]` | full BLUE |
| `ci_check_no_signing_in_blue.sh` | CE-05, T-KEY-01 — signing in RED only | full BLUE |
| `ci_check_pallas_quarantine.sh` | O-29.2 — `pallas-*` confined to `ade_plutus` | non-`ade_plutus` |
| `ci_check_recovery_contract.sh` | T-REC-01, T-REC-02, DC-STORE-05 | `ade_runtime --lib recovery::` (RED) |
| `ci_check_ref_provenance.sh` | DC-REF-01 — manifest checksum integrity | reference corpus |

> **PHASE4-B3 enforcement note.** B3 added **one new CI script**, `ci_check_deposit_param_authority.sh` (DC-TXV-07), and added the registry rules **DC-TXV-06** (cert-deposit classification closure) and **DC-TXV-07** (deposit-param authority), while **strengthening** `T-CONSERV-01` / `CN-LEDGER-07` (full preservation-of-value conservation, `strengthened_in += ["PHASE4-B3"]`) and `DC-VAL-06` (no silent skip — the cert/withdrawal early-out is removed). DC-TXV-07 flips `declared → enforced` via the new grep gate (it scans the 6 BLUE crates for deposit literals and testkit-`ConwayGovParams` reads; script passes). DC-TXV-06's closure is now **grep-gated** by `ci_check_conway_cert_classification_closed.sh` (PHASE4-B3F) in addition to the compiler-exhaustive `match` in `cert_classify::classify` and named `cargo test` targets; DC-TXV-06 is `status=enforced`. The strengthened `T-CONSERV-01` / `CN-LEDGER-07` are exercised by the new `conway` full-equation conservation tests (deposit/refund/withdrawal/donation balance + §9.1 reject precedence) and remain backed by `ci_check_ledger_determinism.sh`. Residual gaps: (a) **narrowed in B3F** — the non-exhaustive / open-tail / catch-all / `_ =>`-wildcard closure of the `ConwayCert` / `CertDisposition` / `DepositEffect` / `CoinSource` enums and the cert decoder is now grep-gated by `ci_check_conway_cert_classification_closed.sh`; the residue is only the no-owned-`String` / no-`Box<dyn>` shape of those `ade_types::conway::cert` types, still outside `ci_check_consensus_closed_enums.sh` `TARGETS` (extending it to `crates/ade_types/src/conway/` would close it); (b) the B1-owed fail-open grep gate `ci_check_no_fail_open_in_validation.sh` is **still not shipped**; (c) the Conway block-body vkey-witness closure is still not wired into the `rules.rs` block-body loop (the value-conservation deferral is now CLOSED by B3, but this adjacent witness gap remains). These join the pre-existing residual gaps (mempool BLUE/GREEN partition, runtime-consensus partition, CE-N-B-6 manual evidence, B1's RED `consensus_input_extract` partition).

> **PHASE4-B3F follow-up note.** B3F added one new CI script, `ci_check_conway_cert_classification_closed.sh` (DC-TXV-06, now `status=enforced`), hardened `ade_codec::conway::cert::decode_conway_certs` to reject trailing bytes after the cert array (`CodecError::TrailingBytes`, parity with `decode_withdrawals`) and to bound preallocation by remaining input (no new `CodecError` variant — `TrailingBytes` is pre-existing), and added two regression tests in `crates/ade_codec/tests/conway_cert_classification.rs`. The invariant registry already records DC-VAL-06 `strengthened_in += ["PHASE4-B3F"]`. No new module, crate, or canonical type; counts move tests 1266 → 1268 and CI 26 → 27.

> **PHASE4-B4 enforcement note.** B4 added **no new CI script** and **no new crate edge**. It added the registry rule **DC-LEDGER-08** (closed, total, era-versioned Conway cert-state accumulation, `status=enforced`, `cluster=CL-LEDGER-VERDICT`, `strengthened_in += ["PHASE4-B4"]`), whose `ci_script` is the existing full-BLUE `ci/ci_check_forbidden_patterns.sh`; mechanical closure is the compiler-exhaustive `match` in `delegation::apply_conway_cert` / `conway_cert_action` (total over all 18 Conway tags + removed tags 5/6) plus the 16 named B4 tests (owner-payload retention, DRep-grammar totality, apply-vs-action agreement, era-dispatch fail-closed, synthetic-positive + byte-identical replay + adversarial-no-false-accept). The closed `ConwayCert` shape continues to pass the B3F `ci_check_conway_cert_classification_closed.sh` — the owner-completion enriched variant fields without adding an open tail, `#[non_exhaustive]`, or a catch-all accept arm. **+6 canonical types** (the `ade_ledger::delegation` owner-tagged apply types), **+17 tests**, CI unchanged at 27. **Residual gaps after B4:** (a) the `ade_ledger::delegation` owner-tagged action types are outside any narrow closed-enum grep gate (compiler-match + test + review enforced); (b) DC-LEDGER-08's real epoch-576 cert-state oracle is environment-blocked (absent UMap snapshot); (c) the owner-tagged `ConwayGovState` effects are observed but not yet applied (PHASE4-B5); (d) the Conway block-body vkey-witness closure is still not wired into the `rules.rs` block-body loop. These join the pre-existing residual gaps (B1-owed `ci_check_no_fail_open_in_validation.sh`, mempool BLUE/GREEN partition, runtime-consensus partition, CE-N-B-6 manual evidence, B1's RED `consensus_input_extract` partition, and the no-owned-`String` shape of `ade_types::conway::cert` outside `ci_check_consensus_closed_enums.sh` `TARGETS`).

> **PHASE4-B5 enforcement note.** B5 added **one new CI script** — `ci_check_gov_cert_accumulation_closed.sh` (DC-LEDGER-09, `status=enforced`) — and **no new crate edge**. It added the registry rule **DC-LEDGER-09** (closed, total, era-versioned Conway governance-certificate accumulation, `cluster=CL-LEDGER-VERDICT`, `cross_ref=[DC-LEDGER-08, DC-VAL-06, DC-TXV-06, T-DET-01, CN-LEDGER-07]`), which retires B4's observe-and-drop disposition for governance certs. Mechanical closure is the compiler-exhaustive `match` in `gov_cert::apply_conway_gov_cert` (total over all 18 Conway tags + removed tags 5/6) plus the new grep gate (no `_ =>` arm; forbidden observe-and-drop comment; `accumulate_tx_certs` must call `apply_conway_gov_cert`; `checked_add` DRep expiry; `MissingDRepActivityParam` + `gov_cert_env()` present) plus the 17 named B5 tests. **+1 canonical type** (`GovCertEnv` in `ade_ledger::state`), **+17 tests**, **+1 CI script** (counts: types 375 → 376, tests 1285 → 1302, CI 27 → 28). **Residual gaps after B5:** (a) DC-LEDGER-09's real epoch-576 governance-state (VState) oracle is environment-blocked (absent UMap/ledger snapshot); (b) the GOVCERT committee-membership tx-validity gate is a declared separable follow-up (OQ-3); (c) `ConwayGovState` keys governance state on bare `Hash28` while the codec collapses the key/script credential discriminant (OQ-5 — a cross-cutting credential-surface follow-up); (d) the Conway block-body vkey-witness closure is still not wired into the `rules.rs` block-body loop. These join the pre-existing residual gaps (B1-owed `ci_check_no_fail_open_in_validation.sh`, mempool BLUE/GREEN partition, runtime-consensus partition, CE-N-B-6 manual evidence, B1's RED `consensus_input_extract` partition, the no-owned-`String` shape of `ade_types::conway::cert`, and the `ade_ledger::delegation` owner-tagged action types outside any narrow closed-enum grep gate).
