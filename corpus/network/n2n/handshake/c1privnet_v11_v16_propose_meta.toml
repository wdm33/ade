# Captured real cardano-node N2N handshake fixture (PHASE4-N-F-G-L grounding artifact).
# The EXACT failing peer: the live C1 private-net follower (cardano-node 11.0.1, magic 42) whose :3002
# dial into Ade's serve produced HandshakeDecodeError NodeToNodeV_15 "unknown encoding: TInt 1".
# This is the canonical RESPONDER (MsgAcceptVersion) encoding Ade's serve responder MUST emit.
peer = "C1 private-net follower cardano-node 11.0.1 (127.0.0.1:3010)"
captured_at_utc = "2026-06-04"
network_magic = 42
versions_proposed = [11, 12, 13, 14, 15, 16]
negotiated = 15  # cardano-node 11.0.1 max-common N2N = V15 (NOT V16).
protocol = "Handshake (N2N mini-protocol 0)"
direction_recv = "Responder"
recv_bytes_len = 17  # 8-byte mux header + 9-byte payload (magic 42 = 2-byte int)
recv_payload_hex = "83010f84182af500f4"
# Decoded: [1, 15, [42, true, 0, false]]
#   83 = array(3) MsgAcceptVersion ; 01 = accept tag ; 0f = version 15 ;
#   84 = array(4) versionData ; 18 2a = networkMagic 42 ; f5 = diffusionMode(bool) true ;
#   00 = peerSharing(int) 0 ; f4 = query(bool) false.
# The V15 versionData is a FOUR-element array. Cross-check: corpus preprod_v11_v16_propose_recv.cbor
# (magic 1) has the SAME shape (payload 83010f8401f500f4 = [1,15,[1,true,0,false]]).
# FIX TARGET (S2): Ade serve responder (n2n_supported_for_magic -> n2n(magic)) must emit this 4-element
# V15 versionData. Do NOT loosen Ade's decoder to accept TInt 1 -- fix the ENCODER to the closed grammar.
