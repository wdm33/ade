[package]
name = "ade_core"
version = "0.1.0"
edition = "2021"

[dependencies]
ade_types = { path = "../ade_types" }
minicbor = { version = "0.25", default-features = false, features = ["alloc"] }

[dev-dependencies]
serde_json = "1"
