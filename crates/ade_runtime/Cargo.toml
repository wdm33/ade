[package]
name = "ade_runtime"
version = "0.1.0"
edition = "2021"

[dependencies]
ade_types = { path = "../ade_types" }
# Persistent ChainDb backing store (S-34, Tier 5 choice).
# See docs/clusters/PHASE4-N-D/S-34.md §O-34.1.
# Pure Rust, ACID, single-file. MIT/Apache.
redb = "2"

[dev-dependencies]
tempfile = "3"
